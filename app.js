
// --- Icon palette (launcher icons) ---
const ICON_PALETTE_ALT = [
  "#FF7A1A", // arancio vivo
  "#0B1F3A", // blu notte
  "#43B5FF", // azzurro cielo
  "#A79C91", // tortora medio
  "#123E7A", // blu profondo
  "#FFB36B", // arancio morbido
  "#A9DCFF", // azzurro chiaro
  "#D8D0C7"  // tortora chiaro
];

function _hashStr(str){
  let h = 2166136261;
  for (let i=0;i<str.length;i++) {
    h ^= str.charCodeAt(i);
    h = Math.imul(h, 16777619);
  }
  return (h >>> 0);
}

function applyIconPalette(){
  try{
    const HOME_ICON_COLORS = {
      goOspite: "#0B1F3A",
      goCalendario: "#245EA8",
      openLauncher: "#67BDEB",
      goTassaSoggiorno: "#9DD8F7",
      goPulizie: "#F29C50",
      goLavanderia: "#F6B67A",
      goOrePuliziaHome: "#C7B198",
      goStatistiche: "#D9CCC0",
      goProdotti: "#AFC9D8",
      goDbImport: "#67BDEB",
      goDbExport: "#245EA8"
    };
    const statsIconColors = {
      goStatGen: "#245EA8",
      goStatMensili: "#4D9CC5",
      goStatSpese: "#F29C50",
      goStatRicevute: "#F6B67A",
      goStatChannel: "#C7B198",
      goStatPulizie: "#D9CCC0",
      goStatPiscina: "#D9CCC0",
      goStatCancellazioni: "#AFC9D8"
    };
    document.querySelectorAll('#page-home .home-main').forEach((btn) => {
      const c = (typeof __launcherIconResolveHex__ === 'function')
        ? __launcherIconResolveHex__(btn.id, HOME_ICON_COLORS[btn.id] || "#4D9CC5")
        : (HOME_ICON_COLORS[btn.id] || "#4D9CC5");
      btn.style.setProperty('--ico-color', c);
      const glyph = btn.querySelector('.home-main-glyph');
      if (glyph) glyph.style.color = c;
      const svg = btn.querySelector('svg.ui-ico');
      if (svg){
        svg.style.color = c;
        svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
          node.style.stroke = 'currentColor';
          node.style.fill = 'none';
        });
      }
    });
    document.querySelectorAll('#page-statistiche .home-main').forEach((btn) => {
      const c = (typeof __launcherIconResolveHex__ === 'function')
        ? __launcherIconResolveHex__(btn.id, statsIconColors[btn.id] || "#4D9CC5")
        : (statsIconColors[btn.id] || "#4D9CC5");
      btn.style.setProperty('--ico-color', c);
      const glyph = btn.querySelector('.home-main-glyph');
      if (glyph) glyph.style.color = c;
      const svg = btn.querySelector('svg.ui-ico');
      if (svg){
        svg.style.color = c;
        svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
          node.style.stroke = 'currentColor';
          node.style.fill = 'none';
        });
      }
    });
    if (typeof __applySettingsLauncherIconColors__ === 'function') __applySettingsLauncherIconColors__();
  }catch(_){ }
}


// dDAE_1.020 — iOS BFCache: rebind tappable Home icons
try{
  window.addEventListener("pageshow", () => { try{ bindHomeStrongTap(); }catch(_){ } try{ __syncTopbarCenterLayout__(); }catch(_){ } }, { passive:true });
}catch(_){ }
try{ window.addEventListener('resize', () => { try{ __syncTopbarCenterLayout__(); }catch(_){ } }, { passive:true }); }catch(_){ }
try{ window.addEventListener('orientationchange', () => { try{ __syncTopbarCenterLayout__(); }catch(_){ } }, { passive:true }); }catch(_){ }
try{ document.addEventListener('DOMContentLoaded', () => { try{ __syncTopbarCenterLayout__(); }catch(_){ } }, { once:true }); }catch(_){ }
/* global API_BASE_URL, API_KEY */

/**
 * Build: 2.652
 */
const BUILD_VERSION = "2.914";

const __BACKUP_GRAPHIC_DEFAULTS_VERSION__ = 'dDAE_2.687';
const __BACKUP_GRAPHIC_DEFAULTS_LOCAL_STORAGE__ = {"dDAE_spese_card_visual_v1":"{\"CAT:iva_22\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"\",\"opacity\":0.25},\"CAT:tassa_soggiorno\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_4\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_10\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:contanti\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"\",\"opacity\":0.25},\"CAT:fuori_budget\":{\"bg\":\"indigo-4\",\"border\":\"indigo-4\",\"fg\":\"\",\"opacity\":0.25}}","ddae_graph_colors_2026_ricevute":"{\"Senza ricevuta\":\"gray-5\",\"Con ricevuta\":\"sky-5\"}","dDAE_stat_shared_line_chart_local_v1_statmensili":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"gray-6\",\"opacity\":0.25}","dDAE_statcard_colors_statcancellazioni":"{\"cancellate\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-5\",\"opacity\":0.75},\"totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.8},\"percentuale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","dDAE_spese_card_opacity_v1":"{\"CAT:iva_22\":0.25,\"CAT:tassa_soggiorno\":0.25,\"CAT:iva_4\":0.25,\"CAT:iva_10\":0.25,\"CAT:contanti\":0.25,\"CAT:fuori_budget\":0.25}","dDAE_stat_fiscal_btn_visual_v1":"{\"on\":{\"bg\":\"orange-5\",\"border\":\"orange-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"acid-5\",\"border\":\"acid-5\",\"fg\":\"gray-1\",\"opacity\":1}}","dDAE_design_bg_opacity_v1":"0.5","dDAE_pill_colors_v1":"{\"settingsYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"gray-4\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-3\",\"border\":\"red-6\",\"opacity\":0.25},\"opSettingsYearPill\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"opSettingsLogoutBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.5},\"taxYearBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"taxEstimateBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"setTassaMaxNottiBtn\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.75},\"settingsConfigCancelBtn\":{\"fg\":\"gray-1\",\"bg\":\"red-6\",\"border\":\"red-6\",\"opacity\":1},\"settingsConfigSaveBtn\":{\"fg\":\"gray-1\",\"bg\":\"green-5\",\"border\":\"green-5\",\"opacity\":1},\"setTassaFieldPill\":{\"fg\":\"gray-1\",\"bg\":\"gray-4\",\"border\":\"gray-4\",\"opacity\":0.75}}","dDAE_statcard_colors_statchannel":"{\"channel\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"direct\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"channel:ch-1773321894018\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"channel:ch-1773320830990\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"blue-6\",\"opacity\":0.75},\"channel:ch-1773854223825\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"beige-5\",\"opacity\":0.75}}","dDAE_tax_page_card_visual_v1":"{\"taxPayingCard\":{\"bg\":\"sky-2\",\"border\":\"sky-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxTotalRow\":{\"bg\":\"gray-1\",\"border\":\"gray-2\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxKidsCard\":{\"bg\":\"gray-2\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxReducedCard\":{\"bg\":\"beige-2\",\"border\":\"beige-3\",\"fg\":\"gray-6\",\"opacity\":0.8}}","ddae_graph_colors_2026_occupazione-mensile":"{\"Gennaio\":\"gray-3\",\"Febbraio\":\"gray-3\",\"Marzo\":\"acid-5\",\"Aprile\":\"green-5\",\"Maggio\":\"mint-5\",\"Giugno\":\"yellow-4\",\"Luglio\":\"orange-4\",\"Agosto\":\"red-5\",\"Settembre\":\"pink-5\",\"Ottobre\":\"violet-5\",\"Novembre\":\"gray-3\",\"Dicembre\":\"gray-3\"}","dDAE_stat_shared_line_chart_visual_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_statcard_colors_statprenotazioni":"{\"ricevute\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"occupazione\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"cancellazioni\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"booking\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"pulizie\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"graph-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75}}","dDAE_statgen_compare_btn_visual_v1":"{\"on\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":0.75}}","dDAE_statcard_colors_statgen":"{\"fatturato-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"spese-totali\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"iva-da-versare\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"guadagno-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"giacenza-in-cassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-4\",\"opacity\":0.75},\"statgen-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"statgen-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statgen-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"statgen-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statgen-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statgen-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"statgen-7\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75}}","ddae_app_text_ui_v1":"{\"bold\":false,\"size\":\"1\"}","dDAE_guest_filter_button_visual_v1":"{\"guestToday\":{\"active\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-4\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.5},\"today\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"3days\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"gray-1\",\"opacity\":1},\"all\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByArrivo\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByCheckout\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByInserimento\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByNome\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}}}","dDAE_date_range_calendar_theme_v1":"{\"bg\":\"sky-2\",\"border\":\"sky-2\",\"fg\":\"gray-5\",\"opacity\":0.5}","ddae_stat_fiscal_mode":"forfettario","dDAE_tax_quarter_visual_v1":"{\"taxQ1Btn\":{\"bg\":\"blue-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ2Btn\":{\"bg\":\"acid-5\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ3Btn\":{\"bg\":\"yellow-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ4Btn\":{\"bg\":\"orange-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75}}","dDAE_single_action_button_visual_v1":"{\"confirmYesNoNo\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"confirmYesNoYes\":{\"bg\":\"green-5\",\"border\":\"green-6\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupExport\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupImport\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_save\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_cancel\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByArrivo\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestToday\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByInserimento\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByNome\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"channelEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionWhatsApp\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionCall\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionSms\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorGraphColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnContanti\":{\"bg\":\"acid-6\",\"border\":\"acid-6\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnFuoriBudget\":{\"bg\":\"violet-5\",\"border\":\"violet-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnIva4\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnTassa\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnIva22\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnIva10\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"gray-1\",\"opacity\":0.75},\"licenseDateRangeTrigger\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.5},\"licenseGeneratorConfirm\":{\"bg\":\"green-4\",\"border\":\"green-4\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseGeneratorCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseDateRangeCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseDateRangeApply\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseDateRangeNext\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"blue-4\",\"opacity\":0.75},\"licenseDateRangePrev\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"blue-4\",\"opacity\":0.75}}","dDAE_statcard_colors_statmensili":"{\"Gennaio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Febbraio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Marzo\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"Aprile\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"Maggio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"Giugno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"Luglio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-4\",\"opacity\":0.75},\"Agosto\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"Settembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"pink-5\",\"opacity\":0.75},\"Ottobre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"violet-5\",\"opacity\":0.75},\"Novembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Dicembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75}}","dDAE_pill_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"blue-4\",\"opacity\":0.8}","dDAE_statgen_compare_year_v1":"2025","dDAE_roomsettings_text_button_visual_v1":"{\"1\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"acid-5\",\"border\":\"acid-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"2\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"yellow-4\",\"border\":\"yellow-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"3\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"bold\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"gray-3\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":1}}}","dDAE_stat_shared_line_chart_local_v1_statchannel":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_guest_filter_theme_v1":"{\"guestToday\":{\"inactive\":{\"bg\":\"sky-4\",\"border\":\"sky-4\",\"fg\":\"gray-1\",\"opacity\":1},\"active\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByArrivo\":{\"inactive\":{\"bg\":\"sky-4\",\"border\":\"sky-4\",\"fg\":\"gray-1\",\"opacity\":1},\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByInserimento\":{\"inactive\":{\"bg\":\"sky-4\",\"border\":\"sky-4\",\"fg\":\"gray-1\",\"opacity\":1},\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByNome\":{\"inactive\":{\"bg\":\"sky-4\",\"border\":\"sky-4\",\"fg\":\"gray-1\",\"opacity\":1},\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1}}}","dDAE_room_settings_shell_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.5}","dDAE_launcher_icon_colors_v2":"{\"goProdotti\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goTassaSoggiorno\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goPulizie\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOspite\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goCalendario\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"openLauncher\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goLavanderia\":{\"fg\":\"orange-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOrePuliziaHome\":{\"fg\":\"beige-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatistiche\":{\"fg\":\"beige-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsRoomsBtn\":{\"fg\":\"yellow-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsChannelBtn\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPiscina\":{\"fg\":\"beige-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatCancellazioni\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatGen\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatMensili\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-6\"},\"settingsLaundryCatalogBtn\":{\"fg\":\"mint-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsLanguageBtn\":{\"fg\":\"sky-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPrenotazioni\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsSaveBtn\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsDbBtn\":{\"fg\":\"acid-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsConfigBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsExportRosterBtn\":{\"fg\":\"violet-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsOperatoriBtn\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatSpese\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsYearPill\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsLanguageBtn\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsDarkBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsCodeBtn\":{\"fg\":\"violet-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goDbSync\":{\"fg\":\"gray-1\",\"bg\":\"sky-6\",\"border\":\"sky-5\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatRicevute\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatChannel\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatPulizie\":{\"fg\":\"gray-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"settingsMasterBtn\":{\"fg\":\"violet-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.5}}","dDAE_statistics_card_theme_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-4\"}","dDAE_stat_shared_line_chart_local_v1_statspese":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-5\",\"opacity\":0.25}","dDAE_statcard_colors_statspese":"{\"totale-spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"ssRowContanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"ssRowTassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"ssRowIva22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"ssRowIva10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"ssRowIva4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"statspese-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statspese-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-6\",\"opacity\":0.75},\"statspese-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statspese-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"contanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-6\",\"opacity\":0.75},\"tassa-soggiorno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"iva-22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"iva-10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"iva-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.75}}","ddae_graph_colors_2026_booking":"{\"Senza Booking\":\"beige-3\",\"Con Booking\":\"sky-6\"}","ddae_graph_colors_2026_cancellazioni":"{\"Attive\":\"sky-5\",\"Cancellate\":\"red-6\"}","dDAE_stat_shared_line_chart_local_v1_statgen":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_card_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-3\",\"border\":\"gray-3\",\"opacity\":0.5}","dDAE_launcher_grid_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_guest_list_card_visual_v1":"{\"o_1775159136175_205486\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"guestListCard\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775106659733_825381\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775037422844_962992\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774807633140_367898\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774794698864_733386\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778434592_574573\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778165179_164222\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774695769615_181323\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774615813484_431417\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774590073970_135493\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774549182199_792803\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774349131194_396554\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774294070132_23569\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774203333472_622307\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773778665053_934130\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773762596108_240957\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773521761925_160207\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773515378156_532027\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773512685129_208087\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772051711254_242171\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883347678_865997\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883151252_58020\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771798354312_572208\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771792262655_567122\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772990996388_449132\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772915461283_912499\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772911991089_836567\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772892000802_222541\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772891893203_185070\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665612075_899841\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665387668_658518\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772463249357_353585\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771768094736_352578\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771423554826_640378\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771403404996_105952\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771198573691_351745\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771013142824_95142\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770194242017_352048\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770061837013_848028\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769361483914_35511\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769337888993_664215\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769104205714_730627\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836330926_608858\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836070933_131311\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835923047_6795\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835740696_729080\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835674348_109544\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835594932_997589\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835494641_952861\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835302917_647166\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835082197_683146\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834969050_816317\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834741905_422496\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834624196_315583\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834490336_837552\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834134049_32972\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25}}","dDAE_header_action_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_statcard_colors_statpulizie":"{\"operatore-costanza\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"operatore-giovanna\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"operatore-gero\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"beige-5\",\"opacity\":0.75}}","ddae_graph_colors_2026_spese":"{\"Contanti\":\"acid-5\",\"Tassa soggiorno\":\"beige-4\",\"IVA 22%\":\"red-6\",\"IVA 10%\":\"orange-5\",\"IVA 4%\":\"sky-5\"}","dDAE_statgen_compare_enabled_v1":"0","dDAE_header_action_colors_v1":"{\"backBtnTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistiche\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheMensili\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheSpese\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePrenotazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheCancellazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAzienda\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAmministratore\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAdminInputsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePiscina\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeSettingsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.5},\"opImportRosterTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"opLogoutTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnIrapTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"guestBackTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"authImportBackupTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"hamburgerBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"prodAddBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"prodResetBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddOperatoreCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddChannelCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddLaundryComponentCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddSpesa\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnNewGuestTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnLaundryGenerateTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaSimTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnPiscinaBackfillTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaShareBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calPrevMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calNextMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanPrev\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanToday\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanNext\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheChannel\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.5},\"btnBackStatistichePulizie\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"statFiscalModeBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareToggleBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareYearBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"piscinaResetMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_roomsettings_theme_button_visual_v1":"{\"1\":{\"bg\":\"red-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1}},\"2\":{\"bg\":\"orange-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"3\":{\"bg\":\"yellow-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"4\":{\"bg\":\"acid-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}}}","dDAE_statgen_reg_chart_visual_v1":"{\"bg\":\"gray-2\",\"border\":\"gray-5\",\"fg\":\"yellow-5\",\"opacity\":0.25}","dDAE_stat_shared_line_chart_local_v1_statpulizie":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-6\",\"opacity\":0.25}"};
const __BACKUP_GRAPHIC_DEFAULTS_THEME_SLOTS__ = {"slots":{"1":{"roomsUi":{"nights":{"bg":"pink-5","border":"pink-5","fg":"","opacity":1},"options":{"m":{"bg":"blue-3","border":"blue-3","fg":"","opacity":1},"g":{"bg":"beige-4","border":"beige-4","fg":"","opacity":1},"c":{"bg":"green-5","border":"green-5","fg":"","opacity":1}},"beds":{"matrimoniale":{"bg":"red-4","border":"red-4","fg":"","opacity":1},"singolo":{"bg":"blue-4","border":"blue-4","fg":"","opacity":1},"culla":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":1}},"rooms":{"1":{"bg":"red-5","border":"red-5","fg":"","opacity":0.75},"2":{"bg":"orange-4","border":"orange-4","fg":"","opacity":0.75},"3":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":0.75},"4":{"bg":"acid-5","border":"acid-5","fg":"","opacity":0.75},"5":{"bg":"sky-5","border":"sky-5","fg":"","opacity":0.75},"6":{"bg":"indigo-5","border":"indigo-5","fg":"gray-1","opacity":0.75},"7":{"bg":"red-4","border":"red-4","fg":"","opacity":0.75},"8":{"bg":"pink-4","border":"pink-4","fg":"","opacity":0.75},"9":{"bg":"indigo-3","border":"indigo-3","fg":"","opacity":0.75},"10":{"bg":"indigo-4","border":"indigo-4","fg":"","opacity":0.75},"11":{"bg":"blue-3","border":"blue-3","fg":"","opacity":0.75},"12":{"bg":"mint-4","border":"mint-4","fg":"","opacity":0.75}}},"launcherGridTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-3","opacity":0.75},"launcherIconColors":{"goProdotti":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goTassaSoggiorno":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goPulizie":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goOspite":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goCalendario":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"openLauncher":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goLavanderia":{"fg":"orange-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goOrePuliziaHome":{"fg":"beige-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatistiche":{"fg":"beige-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsRoomsBtn":{"fg":"yellow-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsChannelBtn":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPiscina":{"fg":"beige-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatCancellazioni":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatGen":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatMensili":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-6"},"settingsLaundryCatalogBtn":{"fg":"mint-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsLanguageBtn":{"fg":"sky-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPrenotazioni":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsSaveBtn":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsDbBtn":{"fg":"acid-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsConfigBtn":{"fg":"red-6","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsExportRosterBtn":{"fg":"violet-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsOperatoriBtn":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatSpese":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsYearPill":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsLanguageBtn":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsDarkBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsCodeBtn":{"fg":"violet-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goDbSync":{"fg":"gray-1","bg":"sky-6","border":"sky-5","opacity":0.5},"settingsLogoutBtn":{"fg":"red-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatRicevute":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatChannel":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatPulizie":{"fg":"gray-3","bg":"gray-1","border":"gray-3","opacity":0.75},"settingsMasterBtn":{"fg":"violet-6","bg":"gray-1","border":"gray-3","opacity":0.5}},"statisticsCardTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-4","opacity":0.75},"designBgOpacity":0.5,"headerActionTheme":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.75},"headerActionColors":{"backBtnTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistiche":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheMensili":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheSpese":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePrenotazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheCancellazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAzienda":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAmministratore":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAdminInputsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePiscina":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"homeSettingsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.5},"opImportRosterTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"opLogoutTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnIrapTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"guestBackTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"authImportBackupTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"hamburgerBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"prodAddBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"prodResetBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddOperatoreCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddChannelCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddLaundryComponentCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddSpesa":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnNewGuestTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnLaundryGenerateTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaSimTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnPiscinaBackfillTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaShareBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calPrevMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calNextMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanPrev":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanToday":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanNext":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheChannel":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.5},"btnBackStatistichePulizie":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"statFiscalModeBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"statGenCompareToggleBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"statGenCompareYearBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"piscinaResetMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75}},"pillTheme":{"fg":"sky-5","bg":"gray-1","border":"blue-4","opacity":0.75},"pillColors":{"settingsYearPill":{"fg":"gray-1","bg":"sky-5","border":"gray-4","opacity":0.5},"settingsLogoutBtn":{"fg":"red-6","bg":"gray-3","border":"red-6","opacity":0.25},"opSettingsYearPill":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"opSettingsLogoutBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.5},"taxYearBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"taxEstimateBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"setTassaMaxNottiBtn":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.75},"settingsConfigCancelBtn":{"fg":"gray-1","bg":"red-6","border":"red-6","opacity":1},"settingsConfigSaveBtn":{"fg":"gray-1","bg":"green-5","border":"green-5","opacity":1},"setTassaFieldPill":{"fg":"gray-1","bg":"gray-4","border":"gray-4","opacity":0.75}},"singleActionButtonVisuals":{"confirmYesNoNo":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"confirmYesNoYes":{"bg":"green-5","border":"green-6","fg":"gray-1","opacity":1},"channelEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"laundryCatalogEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"operatoriEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"operatoriEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"laundryCatalogEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"settingsConfigCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"settingsConfigSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupExport":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupImport":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"settingsBackupCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"rc_save":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"rc_cancel":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"guestSortByArrivo":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestToday":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByInserimento":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByNome":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":0.75},"channelEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"guestPhoneActionWhatsApp":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestPhoneActionCall":{"bg":"gray-4","border":"gray-4","fg":"gray-1","opacity":1},"guestPhoneActionSms":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"channelEditorGraphColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":0.75},"spesaCatBtnContanti":{"bg":"acid-6","border":"acid-6","fg":"gray-1","opacity":0.75},"spesaCatBtnFuoriBudget":{"bg":"violet-5","border":"violet-5","fg":"gray-1","opacity":0.75},"spesaCatBtnIva4":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":0.75},"spesaCatBtnTassa":{"bg":"gray-4","border":"gray-4","fg":"gray-1","opacity":0.75},"spesaCatBtnIva22":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":0.75},"spesaCatBtnIva10":{"bg":"orange-4","border":"orange-4","fg":"gray-1","opacity":0.75},"licenseDateRangeTrigger":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":0.5},"licenseGeneratorConfirm":{"bg":"green-4","border":"green-4","fg":"gray-1","opacity":1},"licenseGeneratorCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"licenseDateRangeCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"licenseDateRangeApply":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"licenseDateRangeNext":{"bg":"gray-1","border":"gray-3","fg":"blue-4","opacity":0.75},"licenseDateRangePrev":{"bg":"gray-1","border":"gray-3","fg":"blue-4","opacity":0.75}},"statsThemeStorage":{"dDAE_spese_card_visual_v1":"{\"CAT:iva_22\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"\",\"opacity\":0.25},\"CAT:tassa_soggiorno\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_4\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_10\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:contanti\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"\",\"opacity\":0.25},\"CAT:fuori_budget\":{\"bg\":\"indigo-4\",\"border\":\"indigo-4\",\"fg\":\"\",\"opacity\":0.25}}","ddae_graph_colors_2026_ricevute":"{\"Senza ricevuta\":\"gray-5\",\"Con ricevuta\":\"sky-5\"}","dDAE_stat_shared_line_chart_local_v1_statmensili":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"gray-6\",\"opacity\":0.25}","dDAE_statcard_colors_statcancellazioni":"{\"cancellate\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-5\",\"opacity\":0.75},\"totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.8},\"percentuale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","dDAE_spese_card_opacity_v1":"{\"CAT:iva_22\":0.25,\"CAT:tassa_soggiorno\":0.25,\"CAT:iva_4\":0.25,\"CAT:iva_10\":0.25,\"CAT:contanti\":0.25,\"CAT:fuori_budget\":0.25}","dDAE_stat_fiscal_btn_visual_v1":"{\"on\":{\"bg\":\"orange-5\",\"border\":\"orange-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"acid-5\",\"border\":\"acid-5\",\"fg\":\"gray-1\",\"opacity\":1}}","dDAE_design_bg_opacity_v1":"0.5","dDAE_pill_colors_v1":"{\"settingsYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"gray-4\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-3\",\"border\":\"red-6\",\"opacity\":0.25},\"opSettingsYearPill\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"opSettingsLogoutBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.5},\"taxYearBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"taxEstimateBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"setTassaMaxNottiBtn\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.75},\"settingsConfigCancelBtn\":{\"fg\":\"gray-1\",\"bg\":\"red-6\",\"border\":\"red-6\",\"opacity\":1},\"settingsConfigSaveBtn\":{\"fg\":\"gray-1\",\"bg\":\"green-5\",\"border\":\"green-5\",\"opacity\":1},\"setTassaFieldPill\":{\"fg\":\"gray-1\",\"bg\":\"gray-4\",\"border\":\"gray-4\",\"opacity\":0.75}}","dDAE_statcard_colors_statchannel":"{\"channel\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"direct\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"channel:ch-1773321894018\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"channel:ch-1773320830990\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"blue-6\",\"opacity\":0.75},\"channel:ch-1773854223825\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"beige-5\",\"opacity\":0.75}}","dDAE_tax_page_card_visual_v1":"{\"taxPayingCard\":{\"bg\":\"sky-2\",\"border\":\"sky-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxTotalRow\":{\"bg\":\"gray-1\",\"border\":\"gray-2\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxKidsCard\":{\"bg\":\"gray-2\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxReducedCard\":{\"bg\":\"beige-2\",\"border\":\"beige-3\",\"fg\":\"gray-6\",\"opacity\":0.8}}","ddae_graph_colors_2026_occupazione-mensile":"{\"Gennaio\":\"gray-3\",\"Febbraio\":\"gray-3\",\"Marzo\":\"acid-5\",\"Aprile\":\"green-5\",\"Maggio\":\"mint-5\",\"Giugno\":\"yellow-4\",\"Luglio\":\"orange-4\",\"Agosto\":\"red-5\",\"Settembre\":\"pink-5\",\"Ottobre\":\"violet-5\",\"Novembre\":\"gray-3\",\"Dicembre\":\"gray-3\"}","dDAE_stat_shared_line_chart_visual_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_statcard_colors_statprenotazioni":"{\"ricevute\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"occupazione\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"cancellazioni\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"booking\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"pulizie\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"graph-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75}}","dDAE_statgen_compare_btn_visual_v1":"{\"on\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":0.75}}","dDAE_statcard_colors_statgen":"{\"fatturato-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"spese-totali\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"iva-da-versare\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"guadagno-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"giacenza-in-cassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-4\",\"opacity\":0.75},\"statgen-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"statgen-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statgen-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"statgen-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statgen-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statgen-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"statgen-7\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75}}","ddae_app_text_ui_v1":"{\"bold\":false,\"size\":\"1\"}","dDAE_guest_filter_button_visual_v1":"{\"guestToday\":{\"active\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-4\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.5},\"today\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"3days\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"gray-1\",\"opacity\":1},\"all\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByArrivo\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByInserimento\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByNome\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}}}","ddae_stat_fiscal_mode":"forfettario","dDAE_tax_quarter_visual_v1":"{\"taxQ1Btn\":{\"bg\":\"blue-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ2Btn\":{\"bg\":\"acid-5\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ3Btn\":{\"bg\":\"yellow-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ4Btn\":{\"bg\":\"orange-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75}}","dDAE_single_action_button_visual_v1":"{\"confirmYesNoNo\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"confirmYesNoYes\":{\"bg\":\"green-5\",\"border\":\"green-6\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupExport\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupImport\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_save\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_cancel\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByArrivo\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestToday\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByInserimento\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByNome\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"channelEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionWhatsApp\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionCall\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionSms\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorGraphColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnContanti\":{\"bg\":\"acid-6\",\"border\":\"acid-6\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnFuoriBudget\":{\"bg\":\"violet-5\",\"border\":\"violet-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnIva4\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnTassa\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnIva22\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"spesaCatBtnIva10\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"gray-1\",\"opacity\":0.75},\"licenseDateRangeTrigger\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.5},\"licenseGeneratorConfirm\":{\"bg\":\"green-4\",\"border\":\"green-4\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseGeneratorCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseDateRangeCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseDateRangeApply\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"licenseDateRangeNext\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"blue-4\",\"opacity\":0.75},\"licenseDateRangePrev\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"blue-4\",\"opacity\":0.75}}","dDAE_statcard_colors_statmensili":"{\"Gennaio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Febbraio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Marzo\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"Aprile\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"Maggio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"Giugno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"Luglio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-4\",\"opacity\":0.75},\"Agosto\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"Settembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"pink-5\",\"opacity\":0.75},\"Ottobre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"violet-5\",\"opacity\":0.75},\"Novembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Dicembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75}}","dDAE_pill_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"blue-4\",\"opacity\":0.8}","dDAE_statgen_compare_year_v1":"2025","dDAE_roomsettings_text_button_visual_v1":"{\"1\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"acid-5\",\"border\":\"acid-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"2\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"yellow-4\",\"border\":\"yellow-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"3\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"bold\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"gray-3\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":1}}}","dDAE_stat_shared_line_chart_local_v1_statchannel":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_shell_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.5}","dDAE_launcher_icon_colors_v2":"{\"goProdotti\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goTassaSoggiorno\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goPulizie\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOspite\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goCalendario\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"openLauncher\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goLavanderia\":{\"fg\":\"orange-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOrePuliziaHome\":{\"fg\":\"beige-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatistiche\":{\"fg\":\"beige-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsRoomsBtn\":{\"fg\":\"yellow-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsChannelBtn\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPiscina\":{\"fg\":\"beige-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatCancellazioni\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatGen\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatMensili\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-6\"},\"settingsLaundryCatalogBtn\":{\"fg\":\"mint-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsLanguageBtn\":{\"fg\":\"sky-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPrenotazioni\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsSaveBtn\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsDbBtn\":{\"fg\":\"acid-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsConfigBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsExportRosterBtn\":{\"fg\":\"violet-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsOperatoriBtn\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatSpese\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsYearPill\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsLanguageBtn\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsDarkBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsCodeBtn\":{\"fg\":\"violet-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goDbSync\":{\"fg\":\"gray-1\",\"bg\":\"sky-6\",\"border\":\"sky-5\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatRicevute\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatChannel\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatPulizie\":{\"fg\":\"gray-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"settingsMasterBtn\":{\"fg\":\"violet-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.5}}","dDAE_statistics_card_theme_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-4\"}","dDAE_stat_shared_line_chart_local_v1_statspese":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-5\",\"opacity\":0.25}","dDAE_statcard_colors_statspese":"{\"totale-spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"ssRowContanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"ssRowTassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"ssRowIva22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"ssRowIva10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"ssRowIva4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"statspese-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statspese-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-6\",\"opacity\":0.75},\"statspese-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statspese-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"contanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-6\",\"opacity\":0.75},\"tassa-soggiorno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"iva-22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"iva-10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"iva-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.75}}","ddae_graph_colors_2026_booking":"{\"Senza Booking\":\"beige-3\",\"Con Booking\":\"sky-6\"}","ddae_graph_colors_2026_cancellazioni":"{\"Attive\":\"sky-5\",\"Cancellate\":\"red-6\"}","dDAE_stat_shared_line_chart_local_v1_statgen":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_card_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-3\",\"border\":\"gray-3\",\"opacity\":0.5}","dDAE_launcher_grid_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_guest_list_card_visual_v1":"{\"o_1775159136175_205486\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"guestListCard\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775106659733_825381\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775037422844_962992\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774807633140_367898\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774794698864_733386\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778434592_574573\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778165179_164222\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774695769615_181323\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774615813484_431417\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774590073970_135493\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774549182199_792803\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774349131194_396554\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774294070132_23569\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774203333472_622307\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773778665053_934130\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773762596108_240957\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773521761925_160207\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773515378156_532027\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773512685129_208087\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772051711254_242171\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883347678_865997\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883151252_58020\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771798354312_572208\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771792262655_567122\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772990996388_449132\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772915461283_912499\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772911991089_836567\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772892000802_222541\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772891893203_185070\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665612075_899841\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665387668_658518\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772463249357_353585\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771768094736_352578\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771423554826_640378\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771403404996_105952\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771198573691_351745\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771013142824_95142\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770194242017_352048\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770061837013_848028\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769361483914_35511\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769337888993_664215\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769104205714_730627\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836330926_608858\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836070933_131311\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835923047_6795\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835740696_729080\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835674348_109544\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835594932_997589\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835494641_952861\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835302917_647166\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835082197_683146\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834969050_816317\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834741905_422496\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834624196_315583\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834490336_837552\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834134049_32972\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25}}","dDAE_header_action_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_statcard_colors_statpulizie":"{\"operatore-costanza\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"operatore-giovanna\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"operatore-gero\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"beige-5\",\"opacity\":0.75}}","ddae_graph_colors_2026_spese":"{\"Contanti\":\"acid-5\",\"Tassa soggiorno\":\"beige-4\",\"IVA 22%\":\"red-6\",\"IVA 10%\":\"orange-5\",\"IVA 4%\":\"sky-5\"}","dDAE_statgen_compare_enabled_v1":"0","dDAE_header_action_colors_v1":"{\"backBtnTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistiche\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheMensili\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheSpese\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePrenotazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheCancellazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAzienda\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAmministratore\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAdminInputsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePiscina\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeSettingsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.5},\"opImportRosterTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"opLogoutTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnIrapTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"guestBackTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"authImportBackupTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"hamburgerBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"prodAddBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"prodResetBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddOperatoreCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddChannelCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddLaundryComponentCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddSpesa\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnNewGuestTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnLaundryGenerateTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaSimTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnPiscinaBackfillTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaShareBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calPrevMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calNextMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanPrev\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanToday\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanNext\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheChannel\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.5},\"btnBackStatistichePulizie\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"statFiscalModeBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareToggleBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareYearBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"piscinaResetMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_roomsettings_theme_button_visual_v1":"{\"1\":{\"bg\":\"red-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1}},\"2\":{\"bg\":\"orange-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"3\":{\"bg\":\"yellow-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"4\":{\"bg\":\"acid-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}}}","dDAE_stat_shared_line_chart_local_v1_statpulizie":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-6\",\"opacity\":0.25}"},"roomThemeButtonVisuals":{"1":{"bg":"red-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":1}},"2":{"bg":"orange-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"3":{"bg":"yellow-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"4":{"bg":"acid-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}}},"textButtonVisuals":{"1":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"acid-5","border":"acid-5","fg":"gray-1","opacity":0.75}},"2":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"yellow-4","border":"yellow-5","fg":"gray-1","opacity":0.75}},"3":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":0.75}},"bold":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"gray-3","border":"gray-4","fg":"gray-6","opacity":1}}},"dateRangeCalendarTheme":{"bg":"sky-2","border":"sky-2","fg":"gray-5","opacity":0.5}},"2":{"roomsUi":{"nights":{"bg":"pink-5","border":"pink-5","fg":"","opacity":1},"options":{"m":{"bg":"blue-3","border":"blue-3","fg":"","opacity":1},"g":{"bg":"beige-4","border":"beige-4","fg":"","opacity":1},"c":{"bg":"green-5","border":"green-5","fg":"","opacity":1}},"beds":{"matrimoniale":{"bg":"red-4","border":"red-4","fg":"","opacity":1},"singolo":{"bg":"blue-4","border":"blue-4","fg":"","opacity":1},"culla":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":1}},"rooms":{"1":{"bg":"red-5","border":"red-5","fg":"","opacity":0.75},"2":{"bg":"orange-4","border":"orange-4","fg":"","opacity":0.75},"3":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":0.75},"4":{"bg":"acid-5","border":"acid-5","fg":"","opacity":0.75},"5":{"bg":"sky-5","border":"sky-5","fg":"","opacity":0.75},"6":{"bg":"indigo-5","border":"indigo-5","fg":"gray-1","opacity":0.75},"7":{"bg":"red-4","border":"red-4","fg":"","opacity":0.75},"8":{"bg":"pink-4","border":"pink-4","fg":"","opacity":0.75},"9":{"bg":"indigo-3","border":"indigo-3","fg":"","opacity":0.75},"10":{"bg":"indigo-4","border":"indigo-4","fg":"","opacity":0.75},"11":{"bg":"blue-3","border":"blue-3","fg":"","opacity":0.75},"12":{"bg":"mint-4","border":"mint-4","fg":"","opacity":0.75}}},"launcherGridTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-3","opacity":0.75},"launcherIconColors":{"goProdotti":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goTassaSoggiorno":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goPulizie":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goOspite":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goCalendario":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"openLauncher":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goLavanderia":{"fg":"orange-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goOrePuliziaHome":{"fg":"beige-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatistiche":{"fg":"beige-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsRoomsBtn":{"fg":"yellow-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsChannelBtn":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPiscina":{"fg":"beige-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatCancellazioni":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatGen":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatMensili":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-6"},"settingsLaundryCatalogBtn":{"fg":"mint-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsLanguageBtn":{"fg":"sky-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPrenotazioni":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsSaveBtn":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsDbBtn":{"fg":"acid-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsConfigBtn":{"fg":"red-6","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsExportRosterBtn":{"fg":"violet-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsOperatoriBtn":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatSpese":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsYearPill":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsLanguageBtn":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsDarkBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsCodeBtn":{"fg":"violet-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goDbSync":{"fg":"gray-1","bg":"sky-6","border":"sky-6","opacity":0.5},"settingsLogoutBtn":{"fg":"red-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatRicevute":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatChannel":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatPulizie":{"fg":"gray-3","bg":"gray-1","border":"gray-3","opacity":0.75}},"statisticsCardTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-4","opacity":0.75},"designBgOpacity":0.75,"headerActionTheme":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.75},"headerActionColors":{"backBtnTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistiche":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheMensili":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheSpese":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePrenotazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheCancellazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAzienda":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAmministratore":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAdminInputsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePiscina":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"homeSettingsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.5},"opImportRosterTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"opLogoutTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnIrapTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"guestBackTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"authImportBackupTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"hamburgerBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"prodAddBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"prodResetBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddOperatoreCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddChannelCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddLaundryComponentCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddSpesa":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnNewGuestTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnLaundryGenerateTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaSimTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnPiscinaBackfillTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaShareBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calPrevMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calNextMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanPrev":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanToday":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanNext":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheChannel":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"btnBackStatistichePulizie":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statFiscalModeBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statGenCompareToggleBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statGenCompareYearBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"piscinaResetMonthBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75}},"pillTheme":{"fg":"sky-5","bg":"gray-1","border":"blue-4","opacity":0.75},"pillColors":{"settingsYearPill":{"fg":"gray-1","bg":"sky-5","border":"gray-4","opacity":0.5},"settingsLogoutBtn":{"fg":"red-6","bg":"gray-3","border":"red-6","opacity":0.25},"opSettingsYearPill":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"opSettingsLogoutBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.5},"taxYearBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"taxEstimateBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"setTassaMaxNottiBtn":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.75},"settingsConfigCancelBtn":{"fg":"gray-1","bg":"red-6","border":"red-6","opacity":1},"settingsConfigSaveBtn":{"fg":"gray-1","bg":"green-5","border":"green-5","opacity":1},"setTassaFieldPill":{"fg":"gray-1","bg":"gray-4","border":"gray-4","opacity":0.75}},"singleActionButtonVisuals":{"confirmYesNoNo":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"confirmYesNoYes":{"bg":"green-5","border":"green-6","fg":"gray-1","opacity":1},"channelEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"laundryCatalogEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"operatoriEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"operatoriEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"laundryCatalogEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"settingsConfigCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"settingsConfigSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupExport":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupImport":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"settingsBackupCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"rc_save":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"rc_cancel":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"guestSortByArrivo":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestToday":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByInserimento":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByNome":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":0.75},"channelEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"guestPhoneActionWhatsApp":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestPhoneActionCall":{"bg":"gray-4","border":"gray-4","fg":"gray-1","opacity":1},"guestPhoneActionSms":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1}},"statsThemeStorage":{"dDAE_spese_card_visual_v1":"{\"CAT:iva_22\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"\",\"opacity\":0.25},\"CAT:tassa_soggiorno\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_4\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_10\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:contanti\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"\",\"opacity\":0.25}}","ddae_graph_colors_2026_ricevute":"{\"Senza ricevuta\":\"gray-5\",\"Con ricevuta\":\"sky-5\"}","dDAE_stat_shared_line_chart_local_v1_statmensili":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"sky-5\",\"opacity\":0.25}","dDAE_statcard_colors_statcancellazioni":"{\"cancellate\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-5\",\"opacity\":0.75},\"totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.8},\"percentuale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","dDAE_spese_card_opacity_v1":"{\"CAT:iva_22\":0.25,\"CAT:tassa_soggiorno\":0.25,\"CAT:iva_4\":0.25,\"CAT:iva_10\":0.25,\"CAT:contanti\":0.25}","dDAE_stat_fiscal_btn_visual_v1":"{\"on\":{\"bg\":\"orange-5\",\"border\":\"orange-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":1}}","dDAE_design_bg_opacity_v1":"0.75","dDAE_pill_colors_v1":"{\"settingsYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"gray-4\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-3\",\"border\":\"red-6\",\"opacity\":0.25},\"opSettingsYearPill\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"opSettingsLogoutBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.5},\"taxYearBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"taxEstimateBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"setTassaMaxNottiBtn\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.75},\"settingsConfigCancelBtn\":{\"fg\":\"gray-1\",\"bg\":\"red-6\",\"border\":\"red-6\",\"opacity\":1},\"settingsConfigSaveBtn\":{\"fg\":\"gray-1\",\"bg\":\"green-5\",\"border\":\"green-5\",\"opacity\":1},\"setTassaFieldPill\":{\"fg\":\"gray-1\",\"bg\":\"gray-4\",\"border\":\"gray-4\",\"opacity\":0.75}}","dDAE_statcard_colors_statchannel":"{\"channel\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"direct\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75}}","dDAE_tax_page_card_visual_v1":"{\"taxPayingCard\":{\"bg\":\"sky-2\",\"border\":\"sky-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxTotalRow\":{\"bg\":\"gray-1\",\"border\":\"gray-2\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxKidsCard\":{\"bg\":\"gray-2\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxReducedCard\":{\"bg\":\"beige-2\",\"border\":\"beige-3\",\"fg\":\"gray-6\",\"opacity\":0.8}}","ddae_graph_colors_2026_occupazione-mensile":"{\"Gennaio\":\"gray-3\",\"Febbraio\":\"gray-3\",\"Marzo\":\"acid-5\",\"Aprile\":\"green-5\",\"Maggio\":\"mint-5\",\"Giugno\":\"yellow-4\",\"Luglio\":\"orange-4\",\"Agosto\":\"red-5\",\"Settembre\":\"pink-5\",\"Ottobre\":\"violet-5\",\"Novembre\":\"gray-3\",\"Dicembre\":\"gray-3\"}","dDAE_stat_shared_line_chart_visual_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_statcard_colors_statprenotazioni":"{\"ricevute\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"occupazione\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"cancellazioni\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"booking\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"pulizie\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"graph-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75}}","dDAE_statcard_colors_statgen":"{\"fatturato-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"spese-totali\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"iva-da-versare\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"guadagno-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"giacenza-in-cassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-4\",\"opacity\":0.75},\"statgen-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"statgen-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statgen-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"statgen-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statgen-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statgen-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"statgen-7\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75}}","dDAE_statgen_compare_btn_visual_v1":"{\"on\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","ddae_app_text_ui_v1":"{\"bold\":false,\"size\":\"1\"}","dDAE_guest_filter_button_visual_v1":"{\"guestToday\":{\"active\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-4\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.5},\"today\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"3days\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"gray-1\",\"opacity\":1},\"all\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByArrivo\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByInserimento\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByNome\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}}}","dDAE_single_action_button_visual_v1":"{\"confirmYesNoNo\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"confirmYesNoYes\":{\"bg\":\"green-5\",\"border\":\"green-6\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupExport\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupImport\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_save\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_cancel\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByArrivo\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestToday\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByInserimento\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByNome\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"channelEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionWhatsApp\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionCall\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionSms\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1}}","ddae_stat_fiscal_mode":"forfettario","dDAE_tax_quarter_visual_v1":"{\"taxQ1Btn\":{\"bg\":\"blue-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ2Btn\":{\"bg\":\"acid-5\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ3Btn\":{\"bg\":\"yellow-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ4Btn\":{\"bg\":\"orange-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75}}","dDAE_statgen_compare_year_v1":"2025","dDAE_statcard_colors_statmensili":"{\"Gennaio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Febbraio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Marzo\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"Aprile\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"Maggio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"Giugno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"Luglio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-4\",\"opacity\":0.75},\"Agosto\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"Settembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"pink-5\",\"opacity\":0.75},\"Ottobre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"violet-5\",\"opacity\":0.75},\"Novembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Dicembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75}}","dDAE_pill_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"blue-4\",\"opacity\":0.8}","dDAE_roomsettings_text_button_visual_v1":"{\"1\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"acid-5\",\"border\":\"acid-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"2\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"yellow-4\",\"border\":\"yellow-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"3\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"bold\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"gray-3\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":1}}}","dDAE_stat_shared_line_chart_local_v1_statchannel":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_shell_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.5}","dDAE_launcher_icon_colors_v2":"{\"goProdotti\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goTassaSoggiorno\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goPulizie\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOspite\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goCalendario\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"openLauncher\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goLavanderia\":{\"fg\":\"orange-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOrePuliziaHome\":{\"fg\":\"beige-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatistiche\":{\"fg\":\"beige-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsRoomsBtn\":{\"fg\":\"yellow-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsChannelBtn\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPiscina\":{\"fg\":\"beige-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatCancellazioni\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatGen\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatMensili\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-6\"},\"settingsLaundryCatalogBtn\":{\"fg\":\"mint-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsLanguageBtn\":{\"fg\":\"sky-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPrenotazioni\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsSaveBtn\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsDbBtn\":{\"fg\":\"acid-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsConfigBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsExportRosterBtn\":{\"fg\":\"violet-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsOperatoriBtn\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatSpese\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsYearPill\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsLanguageBtn\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsDarkBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsCodeBtn\":{\"fg\":\"violet-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goDbSync\":{\"fg\":\"gray-1\",\"bg\":\"sky-6\",\"border\":\"sky-6\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatRicevute\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatChannel\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatPulizie\":{\"fg\":\"gray-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_statistics_card_theme_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-4\"}","dDAE_stat_shared_line_chart_local_v1_statspese":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-5\",\"opacity\":0.25}","dDAE_statcard_colors_statspese":"{\"totale-spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"ssRowContanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"ssRowTassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"ssRowIva22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"ssRowIva10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"ssRowIva4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"statspese-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statspese-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-6\",\"opacity\":0.75},\"statspese-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statspese-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"contanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-6\",\"opacity\":0.75},\"tassa-soggiorno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"iva-22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"iva-10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"iva-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.75}}","ddae_graph_colors_2026_booking":"{\"Senza Booking\":\"beige-3\",\"Con Booking\":\"sky-6\"}","ddae_graph_colors_2026_cancellazioni":"{\"Attive\":\"sky-5\",\"Cancellate\":\"red-6\"}","dDAE_stat_shared_line_chart_local_v1_statgen":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_card_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-3\",\"border\":\"gray-3\",\"opacity\":0.5}","dDAE_launcher_grid_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_guest_list_card_visual_v1":"{\"o_1775159136175_205486\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"guestListCard\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775106659733_825381\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775037422844_962992\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774807633140_367898\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774794698864_733386\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778434592_574573\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778165179_164222\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774695769615_181323\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774615813484_431417\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774590073970_135493\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774549182199_792803\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774349131194_396554\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774294070132_23569\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774203333472_622307\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773778665053_934130\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773762596108_240957\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773521761925_160207\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773515378156_532027\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773512685129_208087\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772051711254_242171\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883347678_865997\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883151252_58020\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771798354312_572208\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771792262655_567122\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772990996388_449132\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772915461283_912499\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772911991089_836567\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772892000802_222541\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772891893203_185070\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665612075_899841\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665387668_658518\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772463249357_353585\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771768094736_352578\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771423554826_640378\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771403404996_105952\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771198573691_351745\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771013142824_95142\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770194242017_352048\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770061837013_848028\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769361483914_35511\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769337888993_664215\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769104205714_730627\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836330926_608858\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836070933_131311\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835923047_6795\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835740696_729080\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835674348_109544\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835594932_997589\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835494641_952861\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835302917_647166\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835082197_683146\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834969050_816317\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834741905_422496\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834624196_315583\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834490336_837552\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834134049_32972\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25}}","dDAE_header_action_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_statcard_colors_statpulizie":"{\"operatore-costanza\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"operatore-giovanna\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"operatore-gero\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"beige-5\",\"opacity\":0.75}}","ddae_graph_colors_2026_spese":"{\"Contanti\":\"acid-5\",\"Tassa soggiorno\":\"beige-4\",\"IVA 22%\":\"red-6\",\"IVA 10%\":\"orange-5\",\"IVA 4%\":\"sky-5\"}","dDAE_statgen_compare_enabled_v1":"1","dDAE_header_action_colors_v1":"{\"backBtnTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistiche\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheMensili\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheSpese\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePrenotazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheCancellazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAzienda\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAmministratore\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAdminInputsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePiscina\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeSettingsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.5},\"opImportRosterTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"opLogoutTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnIrapTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"guestBackTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"authImportBackupTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"hamburgerBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"prodAddBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"prodResetBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddOperatoreCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddChannelCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddLaundryComponentCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddSpesa\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnNewGuestTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnLaundryGenerateTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaSimTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnPiscinaBackfillTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaShareBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calPrevMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calNextMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanPrev\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanToday\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanNext\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheChannel\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"btnBackStatistichePulizie\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statFiscalModeBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareToggleBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareYearBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"piscinaResetMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_roomsettings_theme_button_visual_v1":"{\"1\":{\"bg\":\"red-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1}},\"2\":{\"bg\":\"orange-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"3\":{\"bg\":\"yellow-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"4\":{\"bg\":\"acid-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}}}","dDAE_stat_shared_line_chart_local_v1_statpulizie":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-6\",\"opacity\":0.25}"},"roomThemeButtonVisuals":{"1":{"bg":"red-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":1}},"2":{"bg":"orange-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"3":{"bg":"yellow-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"4":{"bg":"acid-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}}},"textButtonVisuals":{"1":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"acid-5","border":"acid-5","fg":"gray-1","opacity":0.75}},"2":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"yellow-4","border":"yellow-5","fg":"gray-1","opacity":0.75}},"3":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":0.75}},"bold":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"gray-3","border":"gray-4","fg":"gray-6","opacity":1}}}},"3":{"roomsUi":{"nights":{"bg":"pink-5","border":"pink-5","fg":"","opacity":1},"options":{"m":{"bg":"blue-3","border":"blue-3","fg":"","opacity":1},"g":{"bg":"beige-4","border":"beige-4","fg":"","opacity":1},"c":{"bg":"green-5","border":"green-5","fg":"","opacity":1}},"beds":{"matrimoniale":{"bg":"red-4","border":"red-4","fg":"","opacity":1},"singolo":{"bg":"blue-4","border":"blue-4","fg":"","opacity":1},"culla":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":1}},"rooms":{"1":{"bg":"red-5","border":"red-5","fg":"","opacity":0.75},"2":{"bg":"orange-4","border":"orange-4","fg":"","opacity":0.75},"3":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":0.75},"4":{"bg":"acid-5","border":"acid-5","fg":"","opacity":0.75},"5":{"bg":"sky-5","border":"sky-5","fg":"","opacity":0.75},"6":{"bg":"indigo-5","border":"indigo-5","fg":"gray-1","opacity":0.75},"7":{"bg":"red-4","border":"red-4","fg":"","opacity":0.75},"8":{"bg":"pink-4","border":"pink-4","fg":"","opacity":0.75},"9":{"bg":"indigo-3","border":"indigo-3","fg":"","opacity":0.75},"10":{"bg":"indigo-4","border":"indigo-4","fg":"","opacity":0.75},"11":{"bg":"blue-3","border":"blue-3","fg":"","opacity":0.75},"12":{"bg":"mint-4","border":"mint-4","fg":"","opacity":0.75}}},"launcherGridTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-3","opacity":0.75},"launcherIconColors":{"goProdotti":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goTassaSoggiorno":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goPulizie":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goOspite":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goCalendario":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"openLauncher":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goLavanderia":{"fg":"orange-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goOrePuliziaHome":{"fg":"beige-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatistiche":{"fg":"beige-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsRoomsBtn":{"fg":"yellow-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsChannelBtn":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPiscina":{"fg":"beige-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatCancellazioni":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatGen":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatMensili":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-6"},"settingsLaundryCatalogBtn":{"fg":"mint-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsLanguageBtn":{"fg":"sky-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPrenotazioni":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsSaveBtn":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsDbBtn":{"fg":"acid-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsConfigBtn":{"fg":"red-6","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsExportRosterBtn":{"fg":"violet-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsOperatoriBtn":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatSpese":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsYearPill":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsLanguageBtn":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsDarkBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsCodeBtn":{"fg":"violet-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goDbSync":{"fg":"gray-1","bg":"sky-6","border":"sky-6","opacity":0.5},"settingsLogoutBtn":{"fg":"red-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatRicevute":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatChannel":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatPulizie":{"fg":"gray-3","bg":"gray-1","border":"gray-3","opacity":0.75}},"statisticsCardTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-4","opacity":0.75},"designBgOpacity":0.75,"headerActionTheme":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.75},"headerActionColors":{"backBtnTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistiche":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheMensili":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheSpese":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePrenotazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheCancellazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAzienda":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAmministratore":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAdminInputsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePiscina":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"homeSettingsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.5},"opImportRosterTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"opLogoutTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnIrapTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"guestBackTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"authImportBackupTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"hamburgerBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"prodAddBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"prodResetBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddOperatoreCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddChannelCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddLaundryComponentCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddSpesa":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnNewGuestTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnLaundryGenerateTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaSimTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnPiscinaBackfillTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaShareBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calPrevMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calNextMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanPrev":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanToday":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanNext":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheChannel":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"btnBackStatistichePulizie":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statFiscalModeBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statGenCompareToggleBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statGenCompareYearBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"piscinaResetMonthBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75}},"pillTheme":{"fg":"sky-5","bg":"gray-1","border":"blue-4","opacity":0.75},"pillColors":{"settingsYearPill":{"fg":"gray-1","bg":"sky-5","border":"gray-4","opacity":0.5},"settingsLogoutBtn":{"fg":"red-6","bg":"gray-3","border":"red-6","opacity":0.25},"opSettingsYearPill":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"opSettingsLogoutBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.5},"taxYearBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"taxEstimateBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"setTassaMaxNottiBtn":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.75},"settingsConfigCancelBtn":{"fg":"gray-1","bg":"red-6","border":"red-6","opacity":1},"settingsConfigSaveBtn":{"fg":"gray-1","bg":"green-5","border":"green-5","opacity":1},"setTassaFieldPill":{"fg":"gray-1","bg":"gray-4","border":"gray-4","opacity":0.75}},"singleActionButtonVisuals":{"confirmYesNoNo":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"confirmYesNoYes":{"bg":"green-5","border":"green-6","fg":"gray-1","opacity":1},"channelEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"laundryCatalogEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"operatoriEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"operatoriEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"laundryCatalogEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"settingsConfigCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"settingsConfigSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupExport":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupImport":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"settingsBackupCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"rc_save":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"rc_cancel":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"guestSortByArrivo":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestToday":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByInserimento":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByNome":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":0.75},"channelEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"guestPhoneActionWhatsApp":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestPhoneActionCall":{"bg":"gray-4","border":"gray-4","fg":"gray-1","opacity":1},"guestPhoneActionSms":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1}},"statsThemeStorage":{"dDAE_spese_card_visual_v1":"{\"CAT:iva_22\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"\",\"opacity\":0.25},\"CAT:tassa_soggiorno\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_4\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_10\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:contanti\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"\",\"opacity\":0.25}}","ddae_graph_colors_2026_ricevute":"{\"Senza ricevuta\":\"gray-5\",\"Con ricevuta\":\"sky-5\"}","dDAE_stat_shared_line_chart_local_v1_statmensili":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"sky-5\",\"opacity\":0.25}","dDAE_statcard_colors_statcancellazioni":"{\"cancellate\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-5\",\"opacity\":0.75},\"totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.8},\"percentuale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","dDAE_spese_card_opacity_v1":"{\"CAT:iva_22\":0.25,\"CAT:tassa_soggiorno\":0.25,\"CAT:iva_4\":0.25,\"CAT:iva_10\":0.25,\"CAT:contanti\":0.25}","dDAE_stat_fiscal_btn_visual_v1":"{\"on\":{\"bg\":\"orange-5\",\"border\":\"orange-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":1}}","dDAE_design_bg_opacity_v1":"0.75","dDAE_pill_colors_v1":"{\"settingsYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"gray-4\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-3\",\"border\":\"red-6\",\"opacity\":0.25},\"opSettingsYearPill\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"opSettingsLogoutBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.5},\"taxYearBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"taxEstimateBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"setTassaMaxNottiBtn\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.75},\"settingsConfigCancelBtn\":{\"fg\":\"gray-1\",\"bg\":\"red-6\",\"border\":\"red-6\",\"opacity\":1},\"settingsConfigSaveBtn\":{\"fg\":\"gray-1\",\"bg\":\"green-5\",\"border\":\"green-5\",\"opacity\":1},\"setTassaFieldPill\":{\"fg\":\"gray-1\",\"bg\":\"gray-4\",\"border\":\"gray-4\",\"opacity\":0.75}}","dDAE_statcard_colors_statchannel":"{\"channel\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"direct\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75}}","dDAE_tax_page_card_visual_v1":"{\"taxPayingCard\":{\"bg\":\"sky-2\",\"border\":\"sky-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxTotalRow\":{\"bg\":\"gray-1\",\"border\":\"gray-2\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxKidsCard\":{\"bg\":\"gray-2\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxReducedCard\":{\"bg\":\"beige-2\",\"border\":\"beige-3\",\"fg\":\"gray-6\",\"opacity\":0.8}}","ddae_graph_colors_2026_occupazione-mensile":"{\"Gennaio\":\"gray-3\",\"Febbraio\":\"gray-3\",\"Marzo\":\"acid-5\",\"Aprile\":\"green-5\",\"Maggio\":\"mint-5\",\"Giugno\":\"yellow-4\",\"Luglio\":\"orange-4\",\"Agosto\":\"red-5\",\"Settembre\":\"pink-5\",\"Ottobre\":\"violet-5\",\"Novembre\":\"gray-3\",\"Dicembre\":\"gray-3\"}","dDAE_stat_shared_line_chart_visual_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_statcard_colors_statprenotazioni":"{\"ricevute\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"occupazione\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"cancellazioni\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"booking\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"pulizie\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"graph-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75}}","dDAE_statcard_colors_statgen":"{\"fatturato-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"spese-totali\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"iva-da-versare\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"guadagno-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"giacenza-in-cassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-4\",\"opacity\":0.75},\"statgen-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"statgen-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statgen-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"statgen-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statgen-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statgen-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"statgen-7\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75}}","dDAE_statgen_compare_btn_visual_v1":"{\"on\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","ddae_app_text_ui_v1":"{\"bold\":false,\"size\":\"1\"}","dDAE_guest_filter_button_visual_v1":"{\"guestToday\":{\"active\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-4\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.5},\"today\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"3days\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"gray-1\",\"opacity\":1},\"all\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByArrivo\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByInserimento\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByNome\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}}}","dDAE_single_action_button_visual_v1":"{\"confirmYesNoNo\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"confirmYesNoYes\":{\"bg\":\"green-5\",\"border\":\"green-6\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupExport\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupImport\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_save\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_cancel\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByArrivo\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestToday\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByInserimento\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByNome\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"channelEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionWhatsApp\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionCall\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionSms\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1}}","ddae_stat_fiscal_mode":"forfettario","dDAE_tax_quarter_visual_v1":"{\"taxQ1Btn\":{\"bg\":\"blue-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ2Btn\":{\"bg\":\"acid-5\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ3Btn\":{\"bg\":\"yellow-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ4Btn\":{\"bg\":\"orange-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75}}","dDAE_statgen_compare_year_v1":"2025","dDAE_statcard_colors_statmensili":"{\"Gennaio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Febbraio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Marzo\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"Aprile\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"Maggio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"Giugno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"Luglio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-4\",\"opacity\":0.75},\"Agosto\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"Settembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"pink-5\",\"opacity\":0.75},\"Ottobre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"violet-5\",\"opacity\":0.75},\"Novembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Dicembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75}}","dDAE_pill_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"blue-4\",\"opacity\":0.8}","dDAE_roomsettings_text_button_visual_v1":"{\"1\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"acid-5\",\"border\":\"acid-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"2\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"yellow-4\",\"border\":\"yellow-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"3\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"bold\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"gray-3\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":1}}}","dDAE_stat_shared_line_chart_local_v1_statchannel":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_shell_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.5}","dDAE_launcher_icon_colors_v2":"{\"goProdotti\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goTassaSoggiorno\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goPulizie\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOspite\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goCalendario\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"openLauncher\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goLavanderia\":{\"fg\":\"orange-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOrePuliziaHome\":{\"fg\":\"beige-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatistiche\":{\"fg\":\"beige-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsRoomsBtn\":{\"fg\":\"yellow-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsChannelBtn\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPiscina\":{\"fg\":\"beige-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatCancellazioni\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatGen\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatMensili\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-6\"},\"settingsLaundryCatalogBtn\":{\"fg\":\"mint-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsLanguageBtn\":{\"fg\":\"sky-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPrenotazioni\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsSaveBtn\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsDbBtn\":{\"fg\":\"acid-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsConfigBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsExportRosterBtn\":{\"fg\":\"violet-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsOperatoriBtn\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatSpese\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsYearPill\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsLanguageBtn\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsDarkBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsCodeBtn\":{\"fg\":\"violet-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goDbSync\":{\"fg\":\"gray-1\",\"bg\":\"sky-6\",\"border\":\"sky-6\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatRicevute\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatChannel\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatPulizie\":{\"fg\":\"gray-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_statistics_card_theme_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-4\"}","dDAE_stat_shared_line_chart_local_v1_statspese":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-5\",\"opacity\":0.25}","dDAE_statcard_colors_statspese":"{\"totale-spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"ssRowContanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"ssRowTassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"ssRowIva22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"ssRowIva10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"ssRowIva4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"statspese-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statspese-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-6\",\"opacity\":0.75},\"statspese-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statspese-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"contanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-6\",\"opacity\":0.75},\"tassa-soggiorno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"iva-22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"iva-10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"iva-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.75}}","ddae_graph_colors_2026_booking":"{\"Senza Booking\":\"beige-3\",\"Con Booking\":\"sky-6\"}","ddae_graph_colors_2026_cancellazioni":"{\"Attive\":\"sky-5\",\"Cancellate\":\"red-6\"}","dDAE_stat_shared_line_chart_local_v1_statgen":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_card_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-3\",\"border\":\"gray-3\",\"opacity\":0.5}","dDAE_launcher_grid_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_guest_list_card_visual_v1":"{\"o_1775159136175_205486\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"guestListCard\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775106659733_825381\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775037422844_962992\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774807633140_367898\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774794698864_733386\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778434592_574573\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778165179_164222\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774695769615_181323\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774615813484_431417\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774590073970_135493\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774549182199_792803\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774349131194_396554\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774294070132_23569\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774203333472_622307\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773778665053_934130\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773762596108_240957\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773521761925_160207\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773515378156_532027\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773512685129_208087\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772051711254_242171\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883347678_865997\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883151252_58020\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771798354312_572208\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771792262655_567122\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772990996388_449132\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772915461283_912499\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772911991089_836567\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772892000802_222541\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772891893203_185070\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665612075_899841\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665387668_658518\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772463249357_353585\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771768094736_352578\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771423554826_640378\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771403404996_105952\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771198573691_351745\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771013142824_95142\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770194242017_352048\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770061837013_848028\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769361483914_35511\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769337888993_664215\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769104205714_730627\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836330926_608858\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836070933_131311\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835923047_6795\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835740696_729080\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835674348_109544\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835594932_997589\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835494641_952861\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835302917_647166\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835082197_683146\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834969050_816317\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834741905_422496\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834624196_315583\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834490336_837552\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834134049_32972\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25}}","dDAE_header_action_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_statcard_colors_statpulizie":"{\"operatore-costanza\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"operatore-giovanna\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"operatore-gero\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"beige-5\",\"opacity\":0.75}}","ddae_graph_colors_2026_spese":"{\"Contanti\":\"acid-5\",\"Tassa soggiorno\":\"beige-4\",\"IVA 22%\":\"red-6\",\"IVA 10%\":\"orange-5\",\"IVA 4%\":\"sky-5\"}","dDAE_statgen_compare_enabled_v1":"1","dDAE_header_action_colors_v1":"{\"backBtnTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistiche\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheMensili\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheSpese\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePrenotazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheCancellazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAzienda\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAmministratore\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAdminInputsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePiscina\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeSettingsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.5},\"opImportRosterTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"opLogoutTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnIrapTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"guestBackTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"authImportBackupTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"hamburgerBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"prodAddBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"prodResetBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddOperatoreCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddChannelCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddLaundryComponentCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddSpesa\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnNewGuestTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnLaundryGenerateTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaSimTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnPiscinaBackfillTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaShareBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calPrevMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calNextMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanPrev\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanToday\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanNext\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheChannel\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"btnBackStatistichePulizie\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statFiscalModeBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareToggleBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareYearBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"piscinaResetMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_roomsettings_theme_button_visual_v1":"{\"1\":{\"bg\":\"red-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1}},\"2\":{\"bg\":\"orange-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"3\":{\"bg\":\"yellow-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"4\":{\"bg\":\"acid-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}}}","dDAE_stat_shared_line_chart_local_v1_statpulizie":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-6\",\"opacity\":0.25}"},"roomThemeButtonVisuals":{"1":{"bg":"red-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":1}},"2":{"bg":"orange-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"3":{"bg":"yellow-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"4":{"bg":"acid-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}}},"textButtonVisuals":{"1":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"acid-5","border":"acid-5","fg":"gray-1","opacity":0.75}},"2":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"yellow-4","border":"yellow-5","fg":"gray-1","opacity":0.75}},"3":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":0.75}},"bold":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"gray-3","border":"gray-4","fg":"gray-6","opacity":1}}}},"4":{"roomsUi":{"nights":{"bg":"pink-5","border":"pink-5","fg":"","opacity":1},"options":{"m":{"bg":"blue-3","border":"blue-3","fg":"","opacity":1},"g":{"bg":"beige-4","border":"beige-4","fg":"","opacity":1},"c":{"bg":"green-5","border":"green-5","fg":"","opacity":1}},"beds":{"matrimoniale":{"bg":"red-4","border":"red-4","fg":"","opacity":1},"singolo":{"bg":"blue-4","border":"blue-4","fg":"","opacity":1},"culla":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":1}},"rooms":{"1":{"bg":"red-5","border":"red-5","fg":"","opacity":0.75},"2":{"bg":"orange-4","border":"orange-4","fg":"","opacity":0.75},"3":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":0.75},"4":{"bg":"acid-5","border":"acid-5","fg":"","opacity":0.75},"5":{"bg":"sky-5","border":"sky-5","fg":"","opacity":0.75},"6":{"bg":"indigo-5","border":"indigo-5","fg":"gray-1","opacity":0.75},"7":{"bg":"red-4","border":"red-4","fg":"","opacity":0.75},"8":{"bg":"pink-4","border":"pink-4","fg":"","opacity":0.75},"9":{"bg":"indigo-3","border":"indigo-3","fg":"","opacity":0.75},"10":{"bg":"indigo-4","border":"indigo-4","fg":"","opacity":0.75},"11":{"bg":"blue-3","border":"blue-3","fg":"","opacity":0.75},"12":{"bg":"mint-4","border":"mint-4","fg":"","opacity":0.75}}},"launcherGridTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-3","opacity":0.75},"launcherIconColors":{"goProdotti":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goTassaSoggiorno":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goPulizie":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goOspite":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goCalendario":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"openLauncher":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goLavanderia":{"fg":"orange-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goOrePuliziaHome":{"fg":"beige-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatistiche":{"fg":"beige-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsRoomsBtn":{"fg":"yellow-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsChannelBtn":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPiscina":{"fg":"beige-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatCancellazioni":{"fg":"orange-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatGen":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatMensili":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-6"},"settingsLaundryCatalogBtn":{"fg":"mint-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsLanguageBtn":{"fg":"sky-3","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatPrenotazioni":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsSaveBtn":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsDbBtn":{"fg":"acid-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsConfigBtn":{"fg":"red-6","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsExportRosterBtn":{"fg":"violet-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsOperatoriBtn":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatSpese":{"fg":"orange-5","bg":"gray-1","border":"gray-3","opacity":0.25},"settingsYearPill":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsLanguageBtn":{"fg":"sky-4","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsDarkBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-3","opacity":0.25},"opSettingsCodeBtn":{"fg":"violet-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goDbSync":{"fg":"gray-1","bg":"sky-6","border":"sky-6","opacity":0.5},"settingsLogoutBtn":{"fg":"red-4","bg":"gray-1","border":"gray-3","opacity":0.25},"goStatRicevute":{"fg":"beige-3","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatChannel":{"fg":"gray-4","bg":"gray-1","border":"gray-3","opacity":0.75},"goStatPulizie":{"fg":"gray-3","bg":"gray-1","border":"gray-3","opacity":0.75}},"statisticsCardTheme":{"fg":"blue-4","bg":"gray-1","border":"gray-4","opacity":0.75},"designBgOpacity":0.75,"headerActionTheme":{"fg":"sky-5","bg":"gray-1","border":"gray-3","opacity":0.75},"headerActionColors":{"backBtnTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistiche":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheMensili":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheSpese":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePrenotazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheCancellazioni":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAzienda":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheAmministratore":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAdminInputsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatistichePiscina":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"homeSettingsTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.5},"opImportRosterTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"opLogoutTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnIrapTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"guestBackTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"authImportBackupTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"hamburgerBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.75},"prodAddBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"prodResetBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddOperatoreCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddChannelCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddLaundryComponentCard":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnAddSpesa":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnNewGuestTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnLaundryGenerateTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaSimTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnPiscinaBackfillTop":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"piscinaShareBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calPrevMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calTodayBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"calNextMonthBtn":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanPrev":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanToday":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"cleanNext":{"fg":"sky-6","bg":"gray-2","border":"gray-3","opacity":0.25},"btnBackStatisticheChannel":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"btnBackStatistichePulizie":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statFiscalModeBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statGenCompareToggleBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"statGenCompareYearBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75},"piscinaResetMonthBtn":{"fg":"sky-6","bg":"gray-1","border":"gray-3","opacity":0.75}},"pillTheme":{"fg":"sky-5","bg":"gray-1","border":"blue-4","opacity":0.75},"pillColors":{"settingsYearPill":{"fg":"gray-1","bg":"sky-5","border":"gray-4","opacity":0.5},"settingsLogoutBtn":{"fg":"red-6","bg":"gray-3","border":"red-6","opacity":0.25},"opSettingsYearPill":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"opSettingsLogoutBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"homeYearPill":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.5},"taxYearBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"taxEstimateBtn":{"fg":"gray-6","bg":"gray-1","border":"gray-4","opacity":0.8},"setTassaMaxNottiBtn":{"fg":"gray-1","bg":"sky-5","border":"sky-5","opacity":0.75},"settingsConfigCancelBtn":{"fg":"gray-1","bg":"red-6","border":"red-6","opacity":1},"settingsConfigSaveBtn":{"fg":"gray-1","bg":"green-5","border":"green-5","opacity":1},"setTassaFieldPill":{"fg":"gray-1","bg":"gray-4","border":"gray-4","opacity":0.75}},"singleActionButtonVisuals":{"confirmYesNoNo":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"confirmYesNoYes":{"bg":"green-5","border":"green-6","fg":"gray-1","opacity":1},"channelEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"laundryCatalogEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"laundryCatalogEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"operatoriEditorCancel":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"operatoriEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"operatoriEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"laundryCatalogEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"settingsConfigCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"settingsConfigSave":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupExport":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"settingsBackupImport":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1},"settingsBackupCancel":{"bg":"red-6","border":"red-6","fg":"gray-1","opacity":1},"rc_save":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"rc_cancel":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":1},"guestSortByArrivo":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestToday":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByInserimento":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestSortByNome":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"channelEditorDelete":{"bg":"red-5","border":"red-5","fg":"gray-1","opacity":0.75},"channelEditorTagColor":{"bg":"yellow-4","border":"yellow-4","fg":"gray-1","opacity":1},"guestPhoneActionWhatsApp":{"bg":"green-5","border":"green-5","fg":"gray-1","opacity":1},"guestPhoneActionCall":{"bg":"gray-4","border":"gray-4","fg":"gray-1","opacity":1},"guestPhoneActionSms":{"bg":"sky-5","border":"sky-5","fg":"gray-1","opacity":1}},"statsThemeStorage":{"dDAE_spese_card_visual_v1":"{\"CAT:iva_22\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"\",\"opacity\":0.25},\"CAT:tassa_soggiorno\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_4\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"\",\"opacity\":0.25},\"CAT:iva_10\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"\",\"opacity\":0.25},\"CAT:contanti\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"\",\"opacity\":0.25}}","ddae_graph_colors_2026_ricevute":"{\"Senza ricevuta\":\"gray-5\",\"Con ricevuta\":\"sky-5\"}","dDAE_stat_shared_line_chart_local_v1_statmensili":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"sky-5\",\"opacity\":0.25}","dDAE_statcard_colors_statcancellazioni":"{\"cancellate\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-5\",\"opacity\":0.75},\"totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.8},\"percentuale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","dDAE_spese_card_opacity_v1":"{\"CAT:iva_22\":0.25,\"CAT:tassa_soggiorno\":0.25,\"CAT:iva_4\":0.25,\"CAT:iva_10\":0.25,\"CAT:contanti\":0.25}","dDAE_stat_fiscal_btn_visual_v1":"{\"on\":{\"bg\":\"orange-5\",\"border\":\"orange-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":1}}","dDAE_design_bg_opacity_v1":"0.75","dDAE_pill_colors_v1":"{\"settingsYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"gray-4\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-3\",\"border\":\"red-6\",\"opacity\":0.25},\"opSettingsYearPill\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"opSettingsLogoutBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.5},\"taxYearBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"taxEstimateBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.8},\"setTassaMaxNottiBtn\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-5\",\"opacity\":0.75},\"settingsConfigCancelBtn\":{\"fg\":\"gray-1\",\"bg\":\"red-6\",\"border\":\"red-6\",\"opacity\":1},\"settingsConfigSaveBtn\":{\"fg\":\"gray-1\",\"bg\":\"green-5\",\"border\":\"green-5\",\"opacity\":1},\"setTassaFieldPill\":{\"fg\":\"gray-1\",\"bg\":\"gray-4\",\"border\":\"gray-4\",\"opacity\":0.75}}","dDAE_statcard_colors_statchannel":"{\"channel\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"direct\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75}}","dDAE_tax_page_card_visual_v1":"{\"taxPayingCard\":{\"bg\":\"sky-2\",\"border\":\"sky-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxTotalRow\":{\"bg\":\"gray-1\",\"border\":\"gray-2\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxKidsCard\":{\"bg\":\"gray-2\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.8},\"taxReducedCard\":{\"bg\":\"beige-2\",\"border\":\"beige-3\",\"fg\":\"gray-6\",\"opacity\":0.8}}","ddae_graph_colors_2026_occupazione-mensile":"{\"Gennaio\":\"gray-3\",\"Febbraio\":\"gray-3\",\"Marzo\":\"acid-5\",\"Aprile\":\"green-5\",\"Maggio\":\"mint-5\",\"Giugno\":\"yellow-4\",\"Luglio\":\"orange-4\",\"Agosto\":\"red-5\",\"Settembre\":\"pink-5\",\"Ottobre\":\"violet-5\",\"Novembre\":\"gray-3\",\"Dicembre\":\"gray-3\"}","dDAE_stat_shared_line_chart_visual_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_statcard_colors_statprenotazioni":"{\"ricevute\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"occupazione\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"cancellazioni\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"booking\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"pulizie\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"graph-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"statprenotazioni-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.8},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75}}","dDAE_statcard_colors_statgen":"{\"fatturato-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"spese-totali\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"senza-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"con-ricevuta\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"iva-da-versare\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"guadagno-totale\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"giacenza-in-cassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-4\",\"opacity\":0.75},\"statgen-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"statgen-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statgen-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.75},\"statgen-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statgen-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statgen-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"statgen-7\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75}}","dDAE_statgen_compare_btn_visual_v1":"{\"on\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"off\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75}}","ddae_app_text_ui_v1":"{\"bold\":false,\"size\":\"1\"}","dDAE_guest_filter_button_visual_v1":"{\"guestToday\":{\"active\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-4\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":0.5},\"today\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"3days\":{\"bg\":\"orange-4\",\"border\":\"orange-4\",\"fg\":\"gray-1\",\"opacity\":1},\"all\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByArrivo\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByInserimento\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}},\"guestSortByNome\":{\"active\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"distractive\":{\"bg\":\"sky-3\",\"border\":\"sky-3\",\"fg\":\"gray-1\",\"opacity\":1}}}","dDAE_single_action_button_visual_v1":"{\"confirmYesNoNo\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"confirmYesNoYes\":{\"bg\":\"green-5\",\"border\":\"green-6\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorCancel\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"operatoriEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"laundryCatalogEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsConfigSave\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupExport\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupImport\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1},\"settingsBackupCancel\":{\"bg\":\"red-6\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_save\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"rc_cancel\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByArrivo\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestToday\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByInserimento\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestSortByNome\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"channelEditorDelete\":{\"bg\":\"red-5\",\"border\":\"red-5\",\"fg\":\"gray-1\",\"opacity\":0.75},\"channelEditorTagColor\":{\"bg\":\"yellow-4\",\"border\":\"yellow-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionWhatsApp\":{\"bg\":\"green-5\",\"border\":\"green-5\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionCall\":{\"bg\":\"gray-4\",\"border\":\"gray-4\",\"fg\":\"gray-1\",\"opacity\":1},\"guestPhoneActionSms\":{\"bg\":\"sky-5\",\"border\":\"sky-5\",\"fg\":\"gray-1\",\"opacity\":1}}","ddae_stat_fiscal_mode":"forfettario","dDAE_tax_quarter_visual_v1":"{\"taxQ1Btn\":{\"bg\":\"blue-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ2Btn\":{\"bg\":\"acid-5\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ3Btn\":{\"bg\":\"yellow-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75},\"taxQ4Btn\":{\"bg\":\"orange-4\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":0.75}}","dDAE_statgen_compare_year_v1":"2025","dDAE_statcard_colors_statmensili":"{\"Gennaio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Febbraio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Marzo\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"Aprile\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"Maggio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"mint-5\",\"opacity\":0.75},\"Giugno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"Luglio\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-4\",\"opacity\":0.75},\"Agosto\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-5\",\"opacity\":0.75},\"Settembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"pink-5\",\"opacity\":0.75},\"Ottobre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"violet-5\",\"opacity\":0.75},\"Novembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75},\"Dicembre\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-3\",\"opacity\":0.75}}","dDAE_pill_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"blue-4\",\"opacity\":0.8}","dDAE_roomsettings_text_button_visual_v1":"{\"1\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"acid-5\",\"border\":\"acid-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"2\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"yellow-4\",\"border\":\"yellow-5\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"3\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":0.75}},\"bold\":{\"distractive\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"active\":{\"bg\":\"gray-3\",\"border\":\"gray-4\",\"fg\":\"gray-6\",\"opacity\":1}}}","dDAE_stat_shared_line_chart_local_v1_statchannel":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_shell_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-4\",\"opacity\":0.5}","dDAE_launcher_icon_colors_v2":"{\"goProdotti\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goTassaSoggiorno\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goPulizie\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOspite\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goCalendario\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"openLauncher\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goLavanderia\":{\"fg\":\"orange-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goOrePuliziaHome\":{\"fg\":\"beige-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatistiche\":{\"fg\":\"beige-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsRoomsBtn\":{\"fg\":\"yellow-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsChannelBtn\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPiscina\":{\"fg\":\"beige-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatCancellazioni\":{\"fg\":\"orange-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatGen\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatMensili\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeYearPill\":{\"fg\":\"gray-1\",\"bg\":\"sky-5\",\"border\":\"sky-6\"},\"settingsLaundryCatalogBtn\":{\"fg\":\"mint-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsLanguageBtn\":{\"fg\":\"sky-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatPrenotazioni\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsSaveBtn\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsDbBtn\":{\"fg\":\"acid-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsConfigBtn\":{\"fg\":\"red-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsExportRosterBtn\":{\"fg\":\"violet-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsOperatoriBtn\":{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatSpese\":{\"fg\":\"orange-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"settingsYearPill\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsLanguageBtn\":{\"fg\":\"sky-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsDarkBtn\":{\"fg\":\"gray-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"opSettingsCodeBtn\":{\"fg\":\"violet-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goDbSync\":{\"fg\":\"gray-1\",\"bg\":\"sky-6\",\"border\":\"sky-6\",\"opacity\":0.5},\"settingsLogoutBtn\":{\"fg\":\"red-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.25},\"goStatRicevute\":{\"fg\":\"beige-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatChannel\":{\"fg\":\"gray-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"goStatPulizie\":{\"fg\":\"gray-3\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_statistics_card_theme_v1":"{\"bg\":\"gray-1\",\"border\":\"gray-4\"}","dDAE_stat_shared_line_chart_local_v1_statspese":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-5\",\"opacity\":0.25}","dDAE_statcard_colors_statspese":"{\"totale-spese\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"ssRowContanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-5\",\"opacity\":0.75},\"ssRowTassa\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"ssRowIva22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"ssRowIva10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"ssRowIva4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.75},\"statspese-1\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"red-6\",\"opacity\":0.75},\"statspese-3\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-6\",\"opacity\":0.75},\"statspese-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-5\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"statspese-6\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"green-5\",\"opacity\":0.75},\"contanti\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"acid-6\",\"opacity\":0.75},\"tassa-soggiorno\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"beige-5\",\"opacity\":0.75},\"iva-22\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-5\",\"opacity\":0.75},\"iva-10\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"iva-4\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"yellow-4\",\"opacity\":0.75},\"statspese-2\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"\",\"opacity\":0.75}}","ddae_graph_colors_2026_booking":"{\"Senza Booking\":\"beige-3\",\"Con Booking\":\"sky-6\"}","ddae_graph_colors_2026_cancellazioni":"{\"Attive\":\"sky-5\",\"Cancellate\":\"red-6\"}","dDAE_stat_shared_line_chart_local_v1_statgen":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"red-6\",\"opacity\":0.25}","dDAE_room_settings_card_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-3\",\"border\":\"gray-3\",\"opacity\":0.5}","dDAE_launcher_grid_theme_v1":"{\"fg\":\"blue-4\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_guest_list_card_visual_v1":"{\"o_1775159136175_205486\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"guestListCard\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775106659733_825381\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1775037422844_962992\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774807633140_367898\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774794698864_733386\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778434592_574573\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774778165179_164222\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774695769615_181323\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774615813484_431417\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774590073970_135493\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774549182199_792803\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774349131194_396554\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774294070132_23569\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1774203333472_622307\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773778665053_934130\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773762596108_240957\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773521761925_160207\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773515378156_532027\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1773512685129_208087\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772051711254_242171\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883347678_865997\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771883151252_58020\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771798354312_572208\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771792262655_567122\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772990996388_449132\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772915461283_912499\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772911991089_836567\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772892000802_222541\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772891893203_185070\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665612075_899841\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772665387668_658518\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1772463249357_353585\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771768094736_352578\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771423554826_640378\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771403404996_105952\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771198573691_351745\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1771013142824_95142\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770194242017_352048\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1770061837013_848028\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769361483914_35511\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769337888993_664215\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1769104205714_730627\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836330926_608858\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768836070933_131311\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835923047_6795\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835740696_729080\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835674348_109544\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835594932_997589\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835494641_952861\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835302917_647166\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768835082197_683146\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834969050_816317\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834741905_422496\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834624196_315583\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834490336_837552\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25},\"o_1768834134049_32972\":{\"bg\":\"gray-3\",\"border\":\"gray-3\",\"fg\":\"gray-5\",\"opacity\":0.25}}","dDAE_header_action_theme_v1":"{\"fg\":\"sky-5\",\"bg\":\"gray-1\",\"border\":\"gray-3\"}","dDAE_statcard_colors_statpulizie":"{\"operatore-costanza\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"sky-6\",\"opacity\":0.75},\"operatore-giovanna\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"orange-5\",\"opacity\":0.75},\"operatore-gero\":{\"bg\":\"gray-1\",\"border\":\"gray-4\",\"fg\":\"beige-5\",\"opacity\":0.75}}","ddae_graph_colors_2026_spese":"{\"Contanti\":\"acid-5\",\"Tassa soggiorno\":\"beige-4\",\"IVA 22%\":\"red-6\",\"IVA 10%\":\"orange-5\",\"IVA 4%\":\"sky-5\"}","dDAE_statgen_compare_enabled_v1":"1","dDAE_header_action_colors_v1":"{\"backBtnTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistiche\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheMensili\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheSpese\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePrenotazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheCancellazioni\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAzienda\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheAmministratore\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAdminInputsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatistichePiscina\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"homeSettingsTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.5},\"opImportRosterTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"opLogoutTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnIrapTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"guestBackTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"authImportBackupTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"hamburgerBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.75},\"prodAddBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"prodResetBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddOperatoreCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddChannelCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddLaundryComponentCard\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnAddSpesa\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnNewGuestTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnLaundryGenerateTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaSimTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnPiscinaBackfillTop\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"piscinaShareBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calPrevMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calTodayBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"calNextMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanPrev\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanToday\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"cleanNext\":{\"fg\":\"sky-6\",\"bg\":\"gray-2\",\"border\":\"gray-3\",\"opacity\":0.25},\"btnBackStatisticheChannel\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"btnBackStatistichePulizie\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statFiscalModeBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareToggleBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"statGenCompareYearBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75},\"piscinaResetMonthBtn\":{\"fg\":\"sky-6\",\"bg\":\"gray-1\",\"border\":\"gray-3\",\"opacity\":0.75}}","dDAE_roomsettings_theme_button_visual_v1":"{\"1\":{\"bg\":\"red-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25},\"active\":{\"bg\":\"red-5\",\"border\":\"red-6\",\"fg\":\"gray-1\",\"opacity\":1}},\"2\":{\"bg\":\"orange-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"3\":{\"bg\":\"yellow-4\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}},\"4\":{\"bg\":\"acid-5\",\"border\":\"gray-3\",\"fg\":\"gray-1\",\"opacity\":1,\"distractive\":{\"bg\":\"gray-1\",\"border\":\"gray-3\",\"fg\":\"gray-6\",\"opacity\":0.25}}}","dDAE_stat_shared_line_chart_local_v1_statpulizie":"{\"bg\":\"gray-1\",\"border\":\"gray-5\",\"fg\":\"orange-6\",\"opacity\":0.25}"},"roomThemeButtonVisuals":{"1":{"bg":"red-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":1}},"2":{"bg":"orange-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"3":{"bg":"yellow-4","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}},"4":{"bg":"acid-5","border":"gray-3","fg":"gray-1","opacity":1,"distractive":{"bg":"gray-1","border":"gray-3","fg":"gray-6","opacity":0.25}}},"textButtonVisuals":{"1":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"acid-5","border":"acid-5","fg":"gray-1","opacity":0.75}},"2":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"yellow-4","border":"yellow-5","fg":"gray-1","opacity":0.75}},"3":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"red-5","border":"red-6","fg":"gray-1","opacity":0.75}},"bold":{"distractive":{"bg":"gray-3","border":"gray-3","fg":"gray-6","opacity":0.75},"active":{"bg":"gray-3","border":"gray-4","fg":"gray-6","opacity":1}}}}},"activeSlot":"1"};
const __BACKUP_GRAPHIC_DEFAULTS_ROOMS_UI__ = {"nights":{"bg":"pink-5","border":"pink-5","fg":"","opacity":1},"options":{"m":{"bg":"blue-3","border":"blue-3","fg":"","opacity":1},"g":{"bg":"beige-4","border":"beige-4","fg":"","opacity":1},"c":{"bg":"green-5","border":"green-5","fg":"","opacity":1}},"beds":{"matrimoniale":{"bg":"red-4","border":"red-4","fg":"","opacity":1},"singolo":{"bg":"blue-4","border":"blue-4","fg":"","opacity":1},"culla":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":1}},"rooms":{"1":{"bg":"red-5","border":"red-5","fg":"","opacity":0.75},"2":{"bg":"orange-4","border":"orange-4","fg":"","opacity":0.75},"3":{"bg":"yellow-4","border":"yellow-4","fg":"","opacity":0.75},"4":{"bg":"acid-5","border":"acid-5","fg":"","opacity":0.75},"5":{"bg":"sky-5","border":"sky-5","fg":"","opacity":0.75},"6":{"bg":"indigo-5","border":"indigo-5","fg":"gray-1","opacity":0.75},"7":{"bg":"red-4","border":"red-4","fg":"","opacity":0.75},"8":{"bg":"pink-4","border":"pink-4","fg":"","opacity":0.75},"9":{"bg":"indigo-3","border":"indigo-3","fg":"","opacity":0.75},"10":{"bg":"indigo-4","border":"indigo-4","fg":"","opacity":0.75},"11":{"bg":"blue-3","border":"blue-3","fg":"","opacity":0.75},"12":{"bg":"mint-4","border":"mint-4","fg":"","opacity":0.75}}};

function __ensureGuestArrivalFilterVisualStates__(){
  try{
    const map = __loadGuestFilterButtonVisualMap__();
    const guestToday = (map.guestToday && typeof map.guestToday === 'object') ? map.guestToday : {};
    let changed = false;
    ['today','3days','7days','-3days','-7days','all'].forEach((key) => {
      if (!guestToday[key] || typeof guestToday[key] !== 'object'){
        guestToday[key] = __guestFilterButtonDefaultVisual__('guestToday', key);
        changed = true;
      }
    });
    if (changed || !map.guestToday){
      map.guestToday = guestToday;
    }
    const scrollToday = (map.guestScrollTodayBtn && typeof map.guestScrollTodayBtn === 'object') ? map.guestScrollTodayBtn : {};
    ['active','distractive'].forEach((key) => {
      if (!scrollToday[key] || typeof scrollToday[key] !== 'object'){
        scrollToday[key] = __guestFilterButtonDefaultVisual__('guestScrollTodayBtn', key);
        changed = true;
      }
    });
    if (changed || !map.guestScrollTodayBtn){
      map.guestScrollTodayBtn = scrollToday;
      __saveGuestFilterButtonVisualMap__(map);
    }
  }catch(_){ }
}

function __seedBackupGraphicDefaults__(){
  try{
    try{ state.guestShowPreventiviOnly = (localStorage.getItem("dDAE_guestShowPreventiviOnly") === "1"); }catch(_){ state.guestShowPreventiviOnly = false; }

  try{ __ensureGuestArrivalFilterVisualStates__(); }catch(_){ }
    if (typeof localStorage === 'undefined') return;
    const data = __BACKUP_GRAPHIC_DEFAULTS_LOCAL_STORAGE__;
    Object.keys(data || {}).forEach((key) => {
      try{
        const current = localStorage.getItem(key);
        if (current === null || current === undefined) localStorage.setItem(key, String(data[key] ?? ''));
      }catch(_ ){}
    });
    const themeDefaults = __BACKUP_GRAPHIC_DEFAULTS_THEME_SLOTS__;
    if (themeDefaults && typeof themeDefaults === 'object'){
      try{
        if (localStorage.getItem('dDAE_roomsettings_theme_slots_v1') === null) localStorage.setItem('dDAE_roomsettings_theme_slots_v1', JSON.stringify(themeDefaults.slots || {}));
      }catch(_ ){}
      try{
        const activeSlot = String(themeDefaults.activeSlot || '').trim();
        if (localStorage.getItem('dDAE_roomsettings_theme_active_slot_v1') === null && activeSlot) localStorage.setItem('dDAE_roomsettings_theme_active_slot_v1', activeSlot);
      }catch(_ ){}
    }
    try{ localStorage.setItem('dDAE_graphic_defaults_seeded_v1', __BACKUP_GRAPHIC_DEFAULTS_VERSION__); }catch(_ ){}
  }catch(_ ){}
}


// Local DB keys (local-first)
const __DB_KEYS__ = {
  admin: "dDAE_local_admin_db",
  operator: "dDAE_local_operator_db"
};


// ===== Modalità LOCALE (IndexedDB) — build offline =====
const __LOCAL_MODE__ = true;
// Versione schema export/import (incrementare solo se cambia il formato datasets)
const __LOCAL_SCHEMA_VERSION__ = 1;

// IndexedDB: un unico store key-value con snapshot per tabella
const __IDB_NAME__ = "dDAE_LOCAL_DB_v1";
const __IDB_VER__ = 1;
const __IDB_STORE__ = "kv";

const __ALL_TABLES__ = [
  "utenti",
  "impostazioni",
  "ospiti",
  "stanze",
  "servizi",
  "spese",
  "pulizie",
  "lavanderia",
  "operatori",
  "motivazioni",
  "colazione",
  "prodotti_pulizia",
  "ospiti_eliminati",
  "piscina"
];

// Dataset Amministratore (completo)
const __ADMIN_TABLES__ = __ALL_TABLES__.slice();

// Dataset Operatore (subset)
const __OP_TABLES__ = [
  "utenti",
  "impostazioni",
  "ospiti",
  "stanze",
  "servizi",
  "pulizie",
  "lavanderia",
  "operatori",
  "colazione",
  "prodotti_pulizia",
  "piscina",
];

const __idbState = { p: null };

function __idbOpen__(){
  if (__idbState.p) return __idbState.p;
  __idbState.p = new Promise((resolve, reject) => {
    try{
      const req = indexedDB.open(__IDB_NAME__, __IDB_VER__);
      req.onupgradeneeded = () => {
        const db = req.result;
        if (!db.objectStoreNames.contains(__IDB_STORE__)){
          db.createObjectStore(__IDB_STORE__, { keyPath: "k" });
        }
      };
      req.onsuccess = () => resolve(req.result);
      req.onerror = () => reject(req.error || new Error("IndexedDB error"));
    }catch(e){ reject(e); }
  });
  return __idbState.p;
}

async function __kvGet__(k){
  try{
    const db = await __idbOpen__();
    return await new Promise((resolve) => {
      try{
        const tx = db.transaction(__IDB_STORE__, "readonly");
        const st = tx.objectStore(__IDB_STORE__);
        const rq = st.get(k);
        rq.onsuccess = () => resolve(rq.result ? rq.result.v : null);
        rq.onerror = () => resolve(null);
      }catch(_){ resolve(null); }
    });
  }catch(_){ return null; }
}

async function __kvSet__(k, v){
  try{
    const db = await __idbOpen__();
    return await new Promise((resolve) => {
      try{
        const tx = db.transaction(__IDB_STORE__, "readwrite");
        tx.oncomplete = () => resolve(true);
        tx.onerror = () => resolve(false);
        tx.objectStore(__IDB_STORE__).put({ k, v });
      }catch(_){ resolve(false); }
    });
  }catch(_){ return false; }
}

async function __kvDel__(k){
  try{
    const db = await __idbOpen__();
    return await new Promise((resolve) => {
      try{
        const tx = db.transaction(__IDB_STORE__, "readwrite");
        tx.oncomplete = () => resolve(true);
        tx.onerror = () => resolve(false);
        tx.objectStore(__IDB_STORE__).delete(k);
      }catch(_){ resolve(false); }
    });
  }catch(_){ return false; }
}

async function __kvKeys__(prefix){
  try{
    const db = await __idbOpen__();
    const pfx = String(prefix || "");
    return await new Promise((resolve) => {
      const out = [];
      try{
        const tx = db.transaction(__IDB_STORE__, "readonly");
        const st = tx.objectStore(__IDB_STORE__);
        const rq = st.openCursor();
        rq.onsuccess = () => {
          const cursor = rq.result;
          if (!cursor){ resolve(out); return; }
          const k = String(cursor.key || cursor.value?.k || "");
          if (!pfx || k.startsWith(pfx)) out.push(k);
          cursor.continue();
        };
        rq.onerror = () => resolve(out);
      }catch(_){ resolve(out); }
    });
  }catch(_){ return []; }
}

function __tblKey__(name){
  // "utenti" deve essere globale sul dispositivo (serve per login dopo logout)
  try{ if (String(name||"").trim().toLowerCase() === "utenti") return `global:tbl:utenti`; }catch(_){ }
  return `ctx:${__ctxUid__()}:${__ctxYear__()}:tbl:${name}`;
}

async function __tblGet__(name, fallback){
  const v = await __kvGet__(__tblKey__(name));
  if (v === null || v === undefined) return fallback;
  return v;
}

async function __tblSet__(name, data){
  return __kvSet__(__tblKey__(name), data);
}

async function __tblDel__(name){
  return __kvDel__(__tblKey__(name));
}

function __nowIso__(){ return new Date().toISOString(); }

function __normIsoDate__(s){
  const v0 = String(s || "").trim();
  if (!v0) return "";

  // accetta YYYY-MM-DD
  if (/^\d{4}-\d{2}-\d{2}$/.test(v0)) return v0;

  // accetta DD/MM/YYYY o DD-MM-YYYY
  let m = v0.match(/^(\d{1,2})[\/\-](\d{1,2})[\/\-](\d{4})$/);
  if (m){
    const dd = String(m[1]).padStart(2,"0");
    const mm = String(m[2]).padStart(2,"0");
    const yy = String(m[3]);
    return `${yy}-${mm}-${dd}`;
  }

  // seriale Excel (giorni) -> YYYY-MM-DD (soglia: > 20000 ~ 1954)
  if (/^\d+(\.\d+)?$/.test(v0)){
    const n = Number(v0);
    if (isFinite(n) && n > 20000 && n < 90000){
      try{
        // Excel day 0: 1899-12-30 (compatibile con Sheets)
        const base = new Date(Date.UTC(1899, 11, 30));
        const d = new Date(base.getTime() + Math.round(n)*24*3600*1000);
        const y = d.getUTCFullYear();
        const mo = String(d.getUTCMonth()+1).padStart(2,"0");
        const da = String(d.getUTCDate()).padStart(2,"0");
        return `${y}-${mo}-${da}`;
      }catch(_){}
    }
  }

  // fallback: prova parse
  try{
    const d = new Date(v0);
    if (!isNaN(d.getTime())){
      const y = d.getFullYear();
      const m2 = String(d.getMonth()+1).padStart(2,"0");
      const da = String(d.getDate()).padStart(2,"0");
      return `${y}-${m2}-${da}`;
    }
  }catch(_){}
  return v0;
}

// =========================
// Spese: data di riferimento = quella mostrata nella card (dataSpesa || data || data_spesa)
// =========================
function __spesaCardDateISO__(row){
  try{
    return __normIsoDate__((row && (row.dataSpesa || row.data || row.data_spesa)) || "");
  }catch(_){
    return "";
  }
}
function __filterSpeseByCardDateRange__(rows, fromISO, toISO){
  const list = Array.isArray(rows) ? rows : [];
  const from = __normIsoDate__(fromISO);
  const to = __normIsoDate__(toISO);
  if (!from || !to) return list.slice();
  return list.filter((r)=>{
    const d = __spesaCardDateISO__(r);
    if (!d) return false;
    return d >= from && d <= to;
  });
}



function __dateInRange__(d, from, to){
  if (!d) return true;
  const x = __normIsoDate__(d);
  const a = from ? __normIsoDate__(from) : "";
  const b = to ? __normIsoDate__(to) : "";
  if (a && x < a) return false;
  if (b && x > b) return false;
  return true;
}

function __overlapRange__(start, end, from, to){
  const s = __normIsoDate__(start);
  const e = __normIsoDate__(end);
  const a = from ? __normIsoDate__(from) : "";
  const b = to ? __normIsoDate__(to) : "";
  if (!a && !b) return true;
  // overlap: e >= a && s <= b (se mancano, considera aperti)
  if (a && e && e < a) return false;
  if (b && s && s > b) return false;
  return true;
}

function __normBool01(v){
  if (v === true) return true;
  if (v === false) return false;
  const s = String(v ?? "").trim().toLowerCase();
  return (s === "1" || s === "true" || s === "yes" || s === "y");
}

const __MASTER_CODE__ = "8472163950842716";
const __MASTER_EMAIL__ = "kwdp2nq2px@privaterelay.appleid.com";
const __LICENSE_HASH_SALT__ = "dDAE_2.653_LICENSE";
const __LICENSE_REQUEST_PREFIX__ = "7";
const __LICENSE_UNLOCK_PREFIX__ = "8";
const __LICENSE_SERIAL_EPOCH__ = Date.UTC(2020, 0, 1) / 86400000;
let __authPendingUser__ = null;
let __authPendingReason__ = "";
let __authUiApi__ = null;

function __licenseDigitsHash__(input, len){
  const size = Math.max(1, Number(len) || 1);
  let acc = 0;
  const mod = 10 ** Math.min(size, 12);
  const src = String(input || "");
  for (let i = 0; i < src.length; i++){
    acc = (acc * 131 + src.charCodeAt(i) + 17) % mod;
  }
  return String(acc).padStart(size, "0").slice(-size);
}
function __licenseNormalizeDigits__(value){ return String(value || "").replace(/\D+/g, ""); }
function __licenseAccountDigitsFromUser__(user){
  const uid = String(user?.user_id || user?.id || user?.userId || user?.username || "").trim();
  const uname = String(user?.username || user?.user || "").trim().toLowerCase();
  const role = String(user?.ruolo || user?.role || "admin").trim().toLowerCase();
  return __licenseDigitsHash__([__LICENSE_HASH_SALT__, uid, uname, role].join("|"), 10);
}
function __licenseRequestCodeForUser__(user){
  const account = __licenseAccountDigitsFromUser__(user);
  const base = `${__LICENSE_REQUEST_PREFIX__}${account}`;
  const check = __licenseDigitsHash__(["REQ", base, __LICENSE_HASH_SALT__].join("|"), 6);
  return `${base}${check}`;
}
function __licenseParseRequestCode__(code){
  const digits = __licenseNormalizeDigits__(code);
  if (!digits || !digits.startsWith(__LICENSE_REQUEST_PREFIX__) || digits.length !== 17) return null;
  const base = digits.slice(0, 11);
  const account = digits.slice(1, 11);
  const check = digits.slice(11);
  const expected = __licenseDigitsHash__(["REQ", base, __LICENSE_HASH_SALT__].join("|"), 6);
  if (check !== expected) return null;
  return { code: digits, account };
}
function __licenseDateToSerial__(iso){
  const s = String(iso || "").trim();
  if (!/^\d{4}-\d{2}-\d{2}$/.test(s)) return "";
  const ts = Date.parse(`${s}T00:00:00Z`);
  if (!Number.isFinite(ts)) return "";
  const days = Math.round(ts / 86400000 - __LICENSE_SERIAL_EPOCH__);
  if (days < 0 || days > 99999) return "";
  return String(days).padStart(5, "0");
}
function __licenseSerialToDate__(serial){
  const n = parseInt(String(serial || "").trim(), 10);
  if (!Number.isFinite(n) || n < 0) return "";
  const ts = (n + __LICENSE_SERIAL_EPOCH__) * 86400000;
  const d = new Date(ts);
  const y = d.getUTCFullYear();
  const m = String(d.getUTCMonth() + 1).padStart(2, "0");
  const day = String(d.getUTCDate()).padStart(2, "0");
  return `${y}-${m}-${day}`;
}
function __licenseGenerateUnlockCode__(requestCode, startDate, endDate){
  const parsed = __licenseParseRequestCode__(requestCode);
  if (!parsed) throw new Error("Codice richiesta non valido");
  let start = String(startDate || "").trim();
  let end = String(endDate || "").trim();
  if (!start && !end) throw new Error("Seleziona il periodo");
  if (start && !end) end = start;
  if (end && !start) start = end;
  if (end < start){ const swap = start; start = end; end = swap; }
  const startSerial = __licenseDateToSerial__(start);
  const endSerial = __licenseDateToSerial__(end);
  if (!startSerial || !endSerial) throw new Error("Periodo non valido");
  const base = `${__LICENSE_UNLOCK_PREFIX__}${parsed.account}${startSerial}${endSerial}`;
  const check = __licenseDigitsHash__(["UNLOCK", base, __LICENSE_HASH_SALT__].join("|"), 6);
  return `${base}${check}`;
}
function __licenseParseUnlockCodeForUser__(code, user){
  const digits = __licenseNormalizeDigits__(code);
  if (digits === __MASTER_CODE__) return { type:"master" };
  if (!digits || !digits.startsWith(__LICENSE_UNLOCK_PREFIX__) || digits.length !== 27) return null;
  const account = digits.slice(1, 11);
  const expectedAccount = __licenseAccountDigitsFromUser__(user);
  if (account !== expectedAccount) return null;
  const startSerial = digits.slice(11, 16);
  const endSerial = digits.slice(16, 21);
  const base = digits.slice(0, 21);
  const check = digits.slice(21);
  const expected = __licenseDigitsHash__(["UNLOCK", base, __LICENSE_HASH_SALT__].join("|"), 6);
  if (check !== expected) return null;
  const startDate = __licenseSerialToDate__(startSerial);
  const endDate = __licenseSerialToDate__(endSerial);
  if (!startDate || !endDate || endDate < startDate) return null;
  return { type:"license", code: digits, startDate, endDate, account };
}
function __licenseInfoFromRow__(row){
  const start = String(row?.licenseStart || row?.license_start || "").trim();
  const end = String(row?.licenseEnd || row?.license_end || "").trim();
  return {
    unlockCode: String(row?.licenseUnlockCode || row?.license_unlock_code || "").trim(),
    requestCode: String(row?.licenseRequestCode || row?.license_request_code || "").trim(),
    start,
    end,
    status: String(row?.licenseStatus || row?.license_status || "").trim().toLowerCase(),
    isMaster: __normBool01(row?.isMaster) || String(row?.licenseType || row?.license_type || "").trim().toLowerCase() === "master"
  };
}
function __licenseStateForRow__(row, refDate){
  const info = __licenseInfoFromRow__(row || {});
  if (info.isMaster) return { valid:true, master:true, expired:false, pending:false, start:info.start, end:info.end };
  const today = String(refDate || todayISO()).trim() || todayISO();
  const hasRange = !!(info.start && info.end);
  const valid = !!(hasRange && info.status !== "revoked" && info.start <= today && info.end >= today);
  const expired = !!(hasRange && info.end < today);
  return { valid, master:false, expired, pending:!valid, start:info.start, end:info.end };
}
function __isMasterSession__(session){
  const s = session || state?.session || loadSession();
  return !!(s && (__normBool01(s?.isMaster) || String(s?.licenseType || "").trim().toLowerCase() === "master"));
}
async function __getUsersRows__(){
  const rows = await __tblGet__("utenti", []);
  return Array.isArray(rows) ? rows : [];
}
async function __findUserRowByIdentity__(identity){
  const rows = await __getUsersRows__();
  const uid = String(identity?.user_id || identity?.id || identity?.userId || "").trim();
  const uname = String(identity?.username || identity?.user || "").trim().toLowerCase();
  return rows.find((row) => {
    const rid = String(row?.id || row?.user_id || row?.userId || "").trim();
    const run = String(row?.username || row?.user || "").trim().toLowerCase();
    return (uid && rid && uid === rid) || (uname && run && uname === run);
  }) || null;
}
async function __saveUserRow__(nextRow){
  const rows = await __getUsersRows__();
  const uid = String(nextRow?.id || nextRow?.user_id || nextRow?.userId || nextRow?.username || "").trim();
  const uname = String(nextRow?.username || nextRow?.user || "").trim().toLowerCase();
  const idx = rows.findIndex((row) => {
    const rid = String(row?.id || row?.user_id || row?.userId || row?.username || "").trim();
    const run = String(row?.username || row?.user || "").trim().toLowerCase();
    return (uid && rid && uid === rid) || (uname && run && uname === run);
  });
  if (idx >= 0) rows[idx] = nextRow;
  else rows.push(nextRow);
  await __tblSet__("utenti", rows);
  return nextRow;
}
async function __updateUserLicense__(identity, patch){
  const row = await __findUserRowByIdentity__(identity);
  if (!row) throw new Error("Account non trovato");
  const now = __nowIso__();
  const next = Object.assign({}, row, patch || {}, { updatedAt: now });
  if (!next.licenseRequestCode) next.licenseRequestCode = __licenseRequestCodeForUser__(next);
  await __saveUserRow__(next);
  return next;
}
function __sessionExtrasFromUserRow__(row){
  const info = __licenseInfoFromRow__(row || {});
  return {
    isMaster: !!info.isMaster,
    licenseType: info.isMaster ? "master" : "time",
    licenseStatus: info.status || (info.isMaster ? "master" : (info.unlockCode ? "active" : "pending")),
    licenseStart: info.start || "",
    licenseEnd: info.end || "",
    licenseUnlockCode: info.unlockCode || "",
    licenseRequestCode: info.requestCode || __licenseRequestCodeForUser__(row || {})
  };
}
async function __refreshSessionFromStorage__(identity){
  const row = await __findUserRowByIdentity__(identity || state?.session || loadSession() || {});
  if (!row) return null;
  const sess = Object.assign({}, identity || state?.session || loadSession() || {}, __sessionExtrasFromUserRow__(row));
  try{ if (!sess.user_id) sess.user_id = String(row?.id || row?.user_id || row?.username || "").trim(); }catch(_){ }
  try{ if (!sess.username) sess.username = String(row?.username || "").trim(); }catch(_){ }
  try{ if (!sess.ruolo) sess.ruolo = String(row?.ruolo || row?.role || "admin").trim().toLowerCase().startsWith("op") ? "operatore" : "admin"; }catch(_){ }
  return sess;
}
async function __ensureSessionLicenseOnBoot__(){
  const sess = state?.session || loadSession();
  if (!sess || !sess.user_id) return { valid:false, session:null };
  const row = await __findUserRowByIdentity__(sess);
  if (!row) {
    try{ clearSession(); }catch(_){ }
    try{ if (state) state.session = null; }catch(_){ }
    return { valid:false, session:null };
  }
  const merged = await __refreshSessionFromStorage__(sess);
  try{ saveSession(merged); }catch(_){ }
  try{ if (state) state.session = merged; }catch(_){ }
  const lic = __licenseStateForRow__(row);
  if (!lic.valid){
    __authPendingUser__ = merged;
    __authPendingReason__ = lic.expired ? "Licenza scaduta" : "Inserisci codice sblocco";
  } else {
    __authPendingUser__ = null;
    __authPendingReason__ = "";
  }
  return { valid:true, session:merged };
}

async function __localApiUtenti__(method, body){
  const op = String(body?.op || "").trim();
  const rows0 = await __tblGet__("utenti", []);
  const rows = Array.isArray(rows0) ? rows0 : [];

  const findUser = (username) => {
    const u = String(username || "").trim();
    return rows.find(r => String(r?.username || r?.user || "").trim() === u) || null;
  };

  const saveAll = async () => { await __tblSet__("utenti", rows); return true; };

  const okLogin = (u) => {
    const user_id = String(u?.id || u?.user_id || u?.userId || "").trim() || String(u?.username || "").trim();
    const ruolo = String(u?.ruolo || u?.role || "").trim() || (String(u?.isOperatore||"")==="1" ? "operatore" : "amministratore");
    return Object.assign({
      user_id,
      username: String(u?.username || "").trim(),
      ruolo
    }, __sessionExtrasFromUserRow__(u || {}));
  };

  if (method === "POST" && op === "login"){
    const username = String(body?.username || "").trim();
    const password = String(body?.password || "").trim();
    const u = findUser(username);
    if (!u) throw new Error("Credenziali non valide");
    if (String(u?.password || "").trim() !== password) throw new Error("Credenziali non valide");
    return { user: okLogin(u) };
  }

  if (method === "POST" && op === "create"){
    const username = String(body?.username || "").trim();
    const password = String(body?.password || "").trim();
    const roleIn = String(body?.role || body?.ruolo || "").trim().toLowerCase();
    const ruolo = (roleIn.startsWith("op")) ? "operatore" : "admin";
    if (!username || !password) throw new Error("Username e password obbligatori");
    if (findUser(username)) throw new Error("Username già esistente");
    const u = {
      id: String(body?.id || "") || (typeof genId === "function" ? genId("u") : ("u-"+Date.now())),
      username,
      password,
      ruolo,
      licenseStatus: "pending",
      licenseType: "time",
      createdAt: __nowIso__(),
      updatedAt: __nowIso__(),
    };
    try{ u.licenseRequestCode = __licenseRequestCodeForUser__(u); }catch(_){ }
    rows.push(u);
    await saveAll();
    return { user: okLogin(u) };
  }

  if (method === "POST" && op === "update"){
    const username = String(body?.username || "").trim();
    const password = String(body?.password || "").trim();
    const u = findUser(username);
    if (!u) throw new Error("Account non trovato");
    if (String(u?.password || "").trim() !== password) throw new Error("Credenziali non valide");
    const newPassword = String(body?.newPassword || "").trim();
    if (newPassword) u.password = newPassword;
    u.updatedAt = __nowIso__();
    await saveAll();
    return { user: okLogin(u) };
  }

  if (method === "POST" && op === "delete"){
    const username = String(body?.username || "").trim();
    const password = String(body?.password || "").trim();
    const u = findUser(username);
    if (!u) throw new Error("Account non trovato");
    if (String(u?.password || "").trim() !== password) throw new Error("Credenziali non valide");
    // elimina anche operatori legati (tenant__*) se l'utente è owner/admin? qui: elimina solo quell'account
    const idx = rows.indexOf(u);
    if (idx >= 0) rows.splice(idx, 1);
    await saveAll();
    return { ok: true };
  }

  if (method === "POST" && op === "create_operator"){
    const ownerUsername = String(body?.username || "").trim();
    const ownerPassword = String(body?.password || "").trim();
    const owner = findUser(ownerUsername);
    if (!owner) throw new Error("Owner non trovato");
    if (String(owner?.password || "").trim() !== ownerPassword) throw new Error("Credenziali non valide");
    if (String(owner?.ruolo || "").toLowerCase().includes("oper")) throw new Error("Owner non valido");

    const operator_username = String(body?.operator_username || "").trim();
    const operator_password = String(body?.operator_password || "").trim();
    if (!operator_username || !operator_password) throw new Error("Credenziali operatore mancanti");
    if (findUser(operator_username)) throw new Error("Operatore già esistente");

    const u = {
      id: (typeof genId === "function" ? genId("op") : ("op-"+Date.now())),
      username: operator_username,
      password: operator_password,
      ruolo: "operatore",
      createdAt: __nowIso__(),
      updatedAt: __nowIso__(),
    };
    rows.push(u);
    await saveAll();
    return { ok: true };
  }

  if (method === "POST" && op === "update_operator"){
    const ownerUsername = String(body?.username || "").trim();
    const ownerPassword = String(body?.password || "").trim();
    const owner = findUser(ownerUsername);
    if (!owner) throw new Error("Owner non trovato");
    if (String(owner?.password || "").trim() !== ownerPassword) throw new Error("Credenziali non valide");

    const operator_username = String(body?.operator_username || "").trim();
    const newPassword = String(body?.newPassword || "").trim();
    const u = findUser(operator_username);
    if (!u) throw new Error("Operatore non trovato");
    if (!String(u?.ruolo || "").toLowerCase().includes("oper")) throw new Error("Account non è operatore");
    if (!newPassword) throw new Error("Nuova password mancante");
    u.password = newPassword;
    u.updatedAt = __nowIso__();
    await saveAll();
    return { ok: true };
  }

  if (method === "POST" && op === "delete_operator"){
    const ownerUsername = String(body?.username || "").trim();
    const ownerPassword = String(body?.password || "").trim();
    const owner = findUser(ownerUsername);
    if (!owner) throw new Error("Owner non trovato");
    if (String(owner?.password || "").trim() !== ownerPassword) throw new Error("Credenziali non valide");

    const operator_username = String(body?.operator_username || "").trim();
    const u = findUser(operator_username);
    if (!u) throw new Error("Operatore non trovato");
    if (!String(u?.ruolo || "").toLowerCase().includes("oper")) throw new Error("Account non è operatore");
    const idx = rows.indexOf(u);
    if (idx >= 0) rows.splice(idx, 1);
    await saveAll();
    return { ok: true };
  }

  // fallback: restituisce lista utenti (usata in alcune viste)
  return rows;
}

async function __localApiImpostazioni__(method, body){
  const rows0 = await __tblGet__("impostazioni", []);
  let rows = Array.isArray(rows0) ? rows0 : [];

  if (method === "GET"){
    return { rows };
  }

  if (method === "POST"){
    const now = __nowIso__();
    const upsert = (nextRow) => {
      const key = String(nextRow?.key || "").trim().toLowerCase();
      if (!key) return;
      const idx = rows.findIndex(r => String(r?.key || r?.Key || "").trim().toLowerCase() === key);
      const prev = idx >= 0 ? rows[idx] : null;
      const merged = Object.assign({}, prev || {}, nextRow || {});
      merged.key = nextRow.key;
      merged.createdAt = prev?.createdAt || nextRow.createdAt || now;
      merged.updatedAt = now;
      if (idx >= 0) rows[idx] = merged;
      else rows.push(merged);
    };

    try{
      if (Array.isArray(body?.operatori)){
        const ops = body.operatori;
        upsert({
          key: "operatori",
          operatore_1: String(ops[0] || "").trim(),
          operatore_2: String(ops[1] || "").trim(),
          operatore_3: String(ops[2] || "").trim(),
          createdAt: now,
        });
      }
    }catch(_){}

    const valueKeys = ["tariffa_oraria","costo_benzina","tassa_soggiorno","tassa_soggiorno_max_notti","numero_stanze","app_language","stanze_ui"];
    valueKeys.forEach((k)=>{
      if (!body || body[k] === undefined) return;
      upsert({ key:k, value: String(body[k] ?? "").trim(), createdAt: now });
    });

    if (body && body.operatori_catalogo !== undefined){
      const raw = typeof body.operatori_catalogo === "string"
        ? body.operatori_catalogo
        : JSON.stringify(body.operatori_catalogo ?? []);
      upsert({ key:"operatori_catalogo", value: raw, createdAt: now });
    }

    if (body && body.channel_catalogo !== undefined){
      const raw = typeof body.channel_catalogo === "string"
        ? body.channel_catalogo
        : JSON.stringify(body.channel_catalogo ?? []);
      upsert({ key:"channel_catalogo", value: raw, createdAt: now });
    }

    if (body && body.laundry_catalogo !== undefined){
      const raw = typeof body.laundry_catalogo === "string"
        ? body.laundry_catalogo
        : JSON.stringify(body.laundry_catalogo ?? []);
      upsert({ key:"laundry_catalogo", value: raw, createdAt: now });
    }

    if (body && body.laundry_prices !== undefined){
      const raw = typeof body.laundry_prices === "string"
        ? body.laundry_prices
        : JSON.stringify(body.laundry_prices ?? {});
      upsert({ key:"laundry_prices", value: raw, createdAt: now });
    }

    await __tblSet__("impostazioni", rows);
    return { rows };
  }

  return { rows };
}

async function __localApiSpesaList__(tableName, method, body){
  const tName = String(tableName || "").trim() || "colazione";
  const rows0 = await __tblGet__(tName, []);
  const rows = Array.isArray(rows0) ? rows0 : [];

  const save = async ()=>{ await __tblSet__(tName, rows); };

  if (method === "GET"){
    return rows;
  }

  if (method === "PUT"){
    const id = String(body?.id || "").trim();
    const it = rows.find(r => String(r?.id||"").trim() === id);
    if (!it) return { ok:true };
    Object.keys(body||{}).forEach((k)=>{
      if (k === "id") return;
      it[k] = body[k];
    });
    it.updatedAt = __nowIso__();
    await save();
    return { ok:true };
  }

  if (method === "POST"){
    const op = String(body?.op || "").trim();
    if (op === "create"){
      const prodotto = String(body?.prodotto || "").trim().toUpperCase();
      if (!prodotto) throw new Error("Prodotto mancante");
      const exist = rows.find(r => String(r?.prodotto||"").trim().toUpperCase() === prodotto && !__normBool01(r?.isDeleted));
      if (exist) return { ok:true };
      const prefix = (tName === "prodotti_pulizia") ? "p" : "c";
      rows.push({
        id: (typeof genId === "function" ? genId(prefix) : (prefix+"-"+Date.now())),
        prodotto,
        qty: 0,
        saved: 0,
        checked: 0,
        isDeleted: 0,
        createdAt: __nowIso__(),
        updatedAt: __nowIso__(),
      });
      await save();
      return { ok:true };
    }
    if (op === "resetQty"){
      rows.forEach(r => { if (!__normBool01(r?.isDeleted)) { r.qty = 0; r.saved = 0; r.checked = 0; r.updatedAt = __nowIso__(); } });
      await save();
      return { ok:true };
    }
    if (op === "save"){
      rows.forEach(r => { if (!__normBool01(r?.isDeleted)) { const q = parseInt(String(r.qty||0),10); r.saved = (isNaN(q)?0:(q>0?1:0)); r.updatedAt = __nowIso__(); } });
      await save();
      return { ok:true };
    }
    return { ok:true };
  }

  return { ok:true };
}

async function __localApiColazione__(method, body){
  return __localApiSpesaList__("colazione", method, body);
}

async function __localApiLavanderia__(method, params, body){
  const list0 = await __tblGet__("lavanderia", []);
  const list = Array.isArray(list0) ? list0 : [];

  const save = async ()=>{ await __tblSet__("lavanderia", list); };

  if (method === "GET"){
    return list;
  }

  if (method === "PUT"){
    const id = String((body && body.id) || (params && params.id) || "").trim();
    if (!id) return { ok:true };
    const now = __nowIso__();
    const idx = list.findIndex(it => String(it?.id||"").trim() === id);
    if (idx < 0) return { ok:true };
    const prev = list[idx] || {};
    const patch = Object.assign({}, body || {});
    delete patch.id;
    list[idx] = Object.assign({}, prev, patch, {
      id: (prev.id || id),
      updatedAt: now,
      createdAt: (prev.createdAt || prev.created_at || now)
    });
    if (__normBool01(list[idx]?.isDeleted ?? list[idx]?.is_deleted ?? list[idx]?.deleted)){
      list[idx].isDeleted = true;
      list[idx].is_deleted = true;
      list[idx].deletedAt = String(list[idx].deletedAt || list[idx].deleted_at || now);
      list[idx].deleted_at = String(list[idx].deleted_at || list[idx].deletedAt || now);
    }
    await save();
    return { ok:true };
  }

  if (method === "DELETE"){
    const id = String((body && body.id) || (params && params.id) || "").trim();
    if (!id) return { ok:true };
    const now = __nowIso__();
    const idx = list.findIndex(it => String(it?.id||"").trim() === id);
    if (idx >= 0){
      try{
        const prev = list[idx] || {};
        list[idx] = Object.assign({}, prev, {
          id: (prev.id || id),
          isDeleted: true,
          is_deleted: true,
          deletedAt: now,
          deleted_at: now,
          updatedAt: now,
          createdAt: (prev.createdAt || prev.created_at || now)
        });
      }catch(_){
        list[idx] = { id, isDeleted: true, is_deleted: true, deletedAt: now, deleted_at: now, createdAt: now, updatedAt: now };
      }
    } else {
      list.push({ id, isDeleted: true, is_deleted: true, deletedAt: now, deleted_at: now, createdAt: now, updatedAt: now });
    }
    await save();
    return { ok:true };
  }


if (method === "POST"){
    const startDate = __normIsoDate__(body?.startDate || body?.start_date || body?.from);
    const endDate = __normIsoDate__(body?.endDate || body?.end_date || body?.to);

    if (!startDate || !endDate) throw new Error("Date mancanti");
    if (startDate > endDate) throw new Error("Intervallo non valido");

    // Aggrega da pulizie
    const pul0 = await __tblGet__("pulizie", []);
    const pul = Array.isArray(pul0) ? pul0 : [];
    const cols = getLaundryComponentCodes();
    // Recupera l'elenco delle stanze valide (stanza_num) dalla tabella "stanze" per filtrare
    // eventuali righe con stanza non riconosciuta (es. stanza "7" fantasma).
    let validRooms = null;
    try{
      const stanzeList = await __tblGet__("stanze", []);
      if (Array.isArray(stanzeList)){
        validRooms = new Set();
        stanzeList.forEach((r) => {
          const sn = r?.stanza_num ?? r?.stanzaNum ?? r?.room_number ?? r?.roomNumber ?? r?.stanza ?? r?.room;
          if (sn !== undefined && sn !== null){
            const v = String(sn).trim().toUpperCase();
            if (v) validRooms.add(v);
          }
        });
      }
    }catch(_){ validRooms = null; }

    // Inizializza accumulatori per pezzi e resi.  I resi sono
    // rappresentati nelle pulizie come una riga separata con stanza
    // "RES".  Dobbiamo sommare i valori di tale riga separatamente e
    // non conteggiarli nel totale pagabile.  Inoltre, trattiamo
    // qualsiasi valore negativo come reso.
    const sums = {};
    const resi = {};
    cols.forEach(k => { sums[k] = 0; resi[k] = 0; });

    pul.forEach(r => {
      // Salta le righe eliminate (soft-delete) per evitare conteggi fantasma.
      try{
        const del = __normBool01(r?.isDeleted ?? r?.is_deleted ?? r?.deleted);
        if (del) return;
      }catch(_){ }
      const d = __normIsoDate__(r?.data || r?.date || "");
      if (!d) return;
      if (d < startDate || d > endDate) return;
      const s = String(r?.stanza || r?.room || "").trim().toUpperCase();
      const isResRow = (s === 'RES');
      // Se non è una riga di resi, verifica che la stanza sia valida; altrimenti scarta.
      if (!isResRow && validRooms && validRooms.size > 0){
        if (!validRooms.has(s)) return;
      }
      cols.forEach(k => {
        let n = Number(r?.[k] ?? 0);
        if (!isFinite(n)) return;
        n = Math.floor(n);
        // Se la riga rappresenta i resi o la quantità è negativa,
        // accumula nei resi; altrimenti nei pezzi.
        if (isResRow || n < 0){
          resi[k] += Math.abs(n);
        } else {
          sums[k] += Math.max(0, n);
        }
      });
    });

    const catalog = getLaundryCatalogFromSettings();
    const priceMap = getLaundryPricesFromSettings();
    const laundryPrices = {};
    cols.forEach((k) => {
      const n = Number(priceMap?.[k] || 0) || 0;
      laundryPrices[k] = Math.round(n * 100) / 100;
    });

    const item = {
      id: (typeof genId === "function" ? genId("l") : ("l-"+Date.now())),
      startDate,
      endDate,
      createdAt: __nowIso__(),
      updatedAt: __nowIso__(),
      laundryPrices,
      laundryCatalog: catalog.map((row) => ({
        id: String(row?.id || ''),
        titolo: String(row?.titolo || '').trim(),
        abbreviazione: String(row?.abbreviazione || '').trim(),
        prezzo: Math.round((Number(row?.prezzo || 0) || 0) * 100) / 100,
        colore: __normalizeLaundryColor__(row?.colore || 'blue'),
      })),
    };
    // Copia i pezzi (sums) e i resi nel record.  I resi sono
    // salvati con suffisso "_resi" per ogni colonna.
    cols.forEach(k => {
      item[k] = sums[k] || 0;
      item[`${k}_resi`] = resi[k] || 0;
    });
    // Calcola il costo totale utilizzando solo i pezzi (sums) e non i resi.
    item.totalCost = Math.round(cols.reduce((acc, k) => acc + ((Number(sums[k] || 0) || 0) * (Number(laundryPrices?.[k] || 0) || 0)), 0) * 100) / 100;

    list.push(item);
    await save();
    return item;
  }

  return { ok:true };
}

async function __localApiTable__(action, method, params, body){
  const rows0 = await __tblGet__(action, []);
  let rows = Array.isArray(rows0) ? rows0 : [];

  const save = async ()=>{ await __tblSet__(action, rows); };

  const delById = async (id)=>{
    const x = String(id || "").trim();
    if (!x) return;
    const idx = rows.findIndex(r => String(r?.id || "").trim() === x);
    if (idx >= 0) rows.splice(idx, 1);
    await save();
  };

  if (method === "GET"){
    // filtri comuni
    if (action === "servizi"){
      const gid = String(params?.ospite_id || params?.ospiteId || "").trim();
      if (!gid) return [];
      return rows.filter(r => String(r?.ospite_id ?? r?.ospiteId ?? "").trim() === gid);
    }

    if (action === "ospiti"){
      const from = params?.from || params?.da || "";
      const to = params?.to || params?.a || "";
      return rows.filter(r => __overlapRange__(r?.check_in, r?.check_out, from, to));
    }

    if (action === "ospiti_eliminati"){
      const from = params?.from || params?.da || "";
      const to = params?.to || params?.a || "";
      return rows.filter(r => __dateInRange__(r?.deletedAt || r?.deleted_at || r?.data || "", from, to));
    }

    if (action === "spese"){
      const from = params?.from || "";
      const to = params?.to || "";
      return rows.filter(r => __dateInRange__(r?.dataSpesa || r?.data_spesa || r?.data || "", from, to));
    }

    if (action === "pulizie"){
      const d = __normIsoDate__(params?.data || "");
      if (!d) return rows;
      return rows.filter(r => __normIsoDate__(r?.data || "") === d);
    }

    if (action === "operatori"){
      const d = __normIsoDate__(params?.data || "");
      if (!d) return { rows };
      const dayRows = rows.filter(r => __normIsoDate__(r?.data || "") === d);
      return dayRows;
    }

    return rows;
  }

  // scritture specifiche
  if (action === "motivazioni" && method === "POST"){
    const motivazione = String(body?.motivazione || "").trim();
    if (!motivazione) return { ok:true };
    const key = motivazione.toLowerCase();
    const exist = rows.find(r => String(r?.motivazione||"").trim().toLowerCase() === key);
    if (!exist){
      rows.push({ id: (typeof genId==="function"?genId("m"):("m-"+Date.now())), motivazione, attiva: 1, createdAt: __nowIso__(), updatedAt: __nowIso__() });
      await save();
    }
    return { ok:true };
  }

  if (action === "stanze" && method === "POST"){
    const gid = String(body?.ospite_id || "").trim();
    const list = Array.isArray(body?.stanze) ? body.stanze : [];
    if (!gid) return { ok:true };
    rows = rows.filter(r => String(r?.ospite_id || "").trim() !== gid);
    list.forEach(r => {
      const it = Object.assign({}, r);
      it.id = it.id || (typeof genId==="function"?genId("st"):("st-"+Date.now()+Math.random()));
      it.ospite_id = gid;
      it.createdAt = it.createdAt || __nowIso__();
      it.updatedAt = __nowIso__();
      rows.push(it);
    });
    await __tblSet__("stanze", rows);
    return { ok:true };
  }

  if (action === "servizi" && method === "POST"){
    const gid = String(body?.ospite_id || "").trim();
    const list = Array.isArray(body?.servizi) ? body.servizi : [];
    if (!gid) return { ok:true };
    rows = rows.filter(r => String(r?.ospite_id || "").trim() !== gid);
    list.forEach(r => {
      const it = Object.assign({}, r);
      it.id = it.id || (typeof genId==="function"?genId("sv"):("sv-"+Date.now()+Math.random()));
      it.ospite_id = gid;
      it.createdAt = it.createdAt || __nowIso__();
      it.updatedAt = __nowIso__();
      rows.push(it);
    });
    await __tblSet__("servizi", rows);
    return { ok:true };
  }

  if (action === "pulizie" && method === "POST"){
    const data = __normIsoDate__(body?.data || "");
    const list = Array.isArray(body?.rows) ? body.rows : [];
    if (!data) return { ok:true };
    // upsert per stanza
    const byKey = new Map();
    rows.forEach(r => {
      const k = `${__normIsoDate__(r?.data||"")}|${String(r?.stanza||"").trim()}`;
      byKey.set(k, r);
    });
    list.forEach(r => {
      const stanza = String(r?.stanza || "").trim();
      if (!stanza) return;
      const k = `${data}|${stanza}`;
      const ex = byKey.get(k);
      const it = ex ? ex : {};
      it.id = it.id || (typeof genId==="function"?genId("p"):("p-"+Date.now()+Math.random()));
      it.data = data;
      it.stanza = stanza;
      // copia colonne
      Object.keys(r||{}).forEach((kk)=>{
        if (kk === "id") return;
        it[kk] = r[kk];
      });
      it.updatedAt = __nowIso__();
      if (!it.createdAt) it.createdAt = __nowIso__();
      if (!ex) rows.push(it);
      byKey.set(k, it);
    });
    await __tblSet__("pulizie", rows);
    return { ok:true };
  }

  if (action === "operatori" && method === "POST"){
    const data = __normIsoDate__(body?.data || "");
    const list = Array.isArray(body?.operatori) ? body.operatori : [];
    if (!data) return { ok:true };
    const replaceDay = !!body?.replaceDay;
    if (replaceDay){
      rows = rows.filter(r => __normIsoDate__(r?.data||"") !== data);
    }
    list.forEach(r => {
      const it = Object.assign({}, r);
      it.id = it.id || (typeof genId==="function"?genId("opd"):("opd-"+Date.now()+Math.random()));
      it.data = data;
      it.createdAt = it.createdAt || __nowIso__();
      it.updatedAt = __nowIso__();
      rows.push(it);
    });
    await __tblSet__("operatori", rows);
    return { ok:true };
  }

  if (action === "spese" && method === "POST"){
    const it = Object.assign({}, body || {});
    it.id = it.id || (typeof genId==="function"?genId("s"):("s-"+Date.now()));
    it.createdAt = it.createdAt || __nowIso__();
    it.updatedAt = __nowIso__();
    rows.unshift(it);
    await save();
    return { id: it.id };
  }

  if (action === "spese" && method === "PUT"){
    const it = Object.assign({}, body || {});
    it.id = String(it.id || '').trim() || (typeof genId==="function"?genId("s"):("s-"+Date.now()));
    const idx = rows.findIndex(r => String(r?.id || '').trim() === it.id);
    const prev = idx >= 0 ? rows[idx] : null;
    it.createdAt = it.createdAt || it.created_at || prev?.createdAt || prev?.created_at || __nowIso__();
    it.updatedAt = __nowIso__();
    if (idx >= 0) rows[idx] = Object.assign({}, prev || {}, it);
    else rows.unshift(it);
    await save();
    return { id: it.id };
  }

  if (action === "spese" && method === "DELETE"){
    await delById(params?.id);
    return { ok:true };
  }

  if (action === "ospiti" && (method === "POST" || method === "PUT")){
    const it = Object.assign({}, body || {});
    it.id = String(it.id || "").trim() || (typeof genId==="function"?genId("o"):("o-"+Date.now()));
    // preserve createdAt if provided
    it.createdAt = it.createdAt || it.created_at || __nowIso__();
    it.created_at = it.createdAt;
    it.updatedAt = __nowIso__();
    it.updated_at = it.updatedAt;

    const idx = rows.findIndex(r => String(r?.id||"").trim() === it.id);
    if (idx >= 0) rows[idx] = Object.assign({}, rows[idx], it);
    else rows.unshift(it);
    await save();
    return { id: it.id };
  }

  if (action === "ospiti" && method === "DELETE"){
    const id = String(params?.id || "").trim();
    if (!id) return { ok:true };
    const idx = rows.findIndex(r => String(r?.id||"").trim() === id);
    if (idx >= 0){
      const removed = rows[idx];
      rows.splice(idx, 1);
      await save();

      // sposta in eliminati
      try{
        const del0 = await __tblGet__("ospiti_eliminati", []);
        const delRows = Array.isArray(del0) ? del0 : [];
        delRows.unshift(Object.assign({}, removed, { deletedAt: __nowIso__(), isDeleted: 1 }));
        await __tblSet__("ospiti_eliminati", delRows);
      }catch(_){}

      // pulisci relazioni
      try{
        const st0 = await __tblGet__("stanze", []);
        const st = Array.isArray(st0) ? st0 : [];
        await __tblSet__("stanze", st.filter(r => String(r?.ospite_id||"").trim() !== id));
      }catch(_){}
      try{
        const sv0 = await __tblGet__("servizi", []);
        const sv = Array.isArray(sv0) ? sv0 : [];
        await __tblSet__("servizi", sv.filter(r => String(r?.ospite_id||"").trim() !== id));
      }catch(_){}
    }
    return { ok:true };
  }

  // fallback generico PUT/DELETE per id
  if (method === "PUT"){
    const id = String(body?.id || "").trim();
    if (!id) return { ok:true };
    const idx = rows.findIndex(r => String(r?.id||"").trim() === id);
    if (idx < 0) return { ok:true };
    rows[idx] = Object.assign({}, rows[idx], body, { updatedAt: __nowIso__() });
    await save();
    return { ok:true };
  }

  if (method === "DELETE"){
    await delById((params && params.id) || (body && body.id));
    return { ok:true };
  }

  if (method === "POST"){
    // append generico
    const it = Object.assign({}, body || {});
    it.id = it.id || (typeof genId==="function"?genId("r"):("r-"+Date.now()+Math.random()));
    it.createdAt = it.createdAt || __nowIso__();
    it.updatedAt = __nowIso__();
    rows.push(it);
    await save();
    return { id: it.id };
  }

  return { ok:true };
}

async function __localApi__(action, { method="GET", params={}, body=null } = {}){
  const m = String(method || "GET").toUpperCase();
  const a = String(action || "").trim();
  if (a === "utenti") return __localApiUtenti__(m, body || {});
  if (a === "impostazioni") return __localApiImpostazioni__(m, body || {});
  if (a === "colazione") return __localApiColazione__(m, body || {});
  if (a === "prodotti_pulizia") return __localApiSpesaList__("prodotti_pulizia", m, body || {});
  if (a === "lavanderia") return __localApiLavanderia__(m, params || {}, body || {});
  return __localApiTable__(a, m, params || {}, body || {});
}

// Import/Export DB (JSON unico) — Admin vs Operatore
/* Legacy localStorage DB Import/Export removed (LOCAL build uses IndexedDB) */

// ================================
// __FIREBASE_SYNC__ (Firestore-only, no Storage)
// Sync Admin <-> Operatori tramite Firebase, attivato SOLO su tap Import/Export.
// Nessun blocco della Home se offline.
// ================================
const __FB_STATE__ = { token:null, exp:0, teamId:null, teamKey:null };

function __fbLoadLink__(){
  try{
    __FB_STATE__.teamId = localStorage.getItem("ddae_fb_teamId") || "";
    __FB_STATE__.teamKey = localStorage.getItem("ddae_fb_teamKey") || "";
  }catch(_){}
}
function __fbSaveLink__(teamId, teamKey){
  try{
    localStorage.setItem("ddae_fb_teamId", String(teamId||""));
    localStorage.setItem("ddae_fb_teamKey", String(teamKey||""));
  }catch(_){}
  __FB_STATE__.teamId = String(teamId||"");
  __FB_STATE__.teamKey = String(teamKey||"");
}

function __randStr__(n){
  const a = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
  let s = "";
  for (let i=0;i<n;i++) s += a[(Math.random()*a.length)|0];
  return s;
}
function __qrCodeText__(teamId, teamKey){
  return `DDAE|${teamId}|${teamKey}`;
}
function __parseQr__(txt){
  const s = String(txt||"").trim();
  const m = s.match(/^DDAE\|([^|]+)\|([^|]+)$/i);
  if (!m) return null;
  return { teamId: m[1], teamKey: m[2] };
}

async function __fbGetIdToken__(){
  try{
    if (!FIREBASE_ENABLED || !FIREBASE_CONFIG || !FIREBASE_CONFIG.apiKey) throw new Error("Firebase non configurato");
    const now = Date.now();
    if (__FB_STATE__.token && __FB_STATE__.exp && now < (__FB_STATE__.exp - 15000)) return __FB_STATE__.token;

    // anonymous signUp (REST)
    const url = `https://identitytoolkit.googleapis.com/v1/accounts:signUp?key=${encodeURIComponent(FIREBASE_CONFIG.apiKey)}`;
    const res = await fetch(url, { method:"POST", headers:{ "Content-Type":"application/json" }, body: JSON.stringify({ returnSecureToken:true }) });
    if (!res.ok) throw new Error("Auth Firebase fallita");
    const data = await res.json();
    __FB_STATE__.token = data.idToken;
    const sec = parseInt(String(data.expiresIn||"3600"),10);
    __FB_STATE__.exp = Date.now() + (isNaN(sec)?3600:sec)*1000;
    return __FB_STATE__.token;
  }catch(e){
    throw e;
  }
}

function __fsBase__(){
  const pid = FIREBASE_CONFIG.projectId;
  return `https://firestore.googleapis.com/v1/projects/${pid}/databases/(default)/documents`;
}
function __fsDocUrl__(path){
  return `${__fsBase__()}/${path}`;
}
function __fsEncode__(obj){
  // only simple string/timestamp/array<string>
  const out = { fields:{} };
  const f = out.fields;
  for (const k of Object.keys(obj||{})){
    const v = obj[k];
    if (v === undefined) continue;
    if (v === null){ f[k] = { nullValue: null }; continue; }
    if (typeof v === "string"){ f[k] = { stringValue: v }; continue; }
    if (typeof v === "number"){ f[k] = { doubleValue: v }; continue; }
    if (typeof v === "boolean"){ f[k] = { booleanValue: v }; continue; }
    if (v && v.__ts){ f[k] = { timestampValue: v.__ts }; continue; }
    if (Array.isArray(v)){
      f[k] = { arrayValue: { values: v.map(x => ({ stringValue: String(x) })) } };
      continue;
    }
    // fallback: stringify
    f[k] = { stringValue: JSON.stringify(v) };
  }
  return out;
}
function __fsDecode__(doc){
  try{
    const f = (doc && doc.fields) ? doc.fields : {};
    const out = {};
    for (const k of Object.keys(f)){
      const v = f[k];
      if (v.stringValue !== undefined) out[k] = v.stringValue;
      else if (v.doubleValue !== undefined) out[k] = v.doubleValue;
      else if (v.integerValue !== undefined) out[k] = parseInt(v.integerValue,10);
      else if (v.booleanValue !== undefined) out[k] = !!v.booleanValue;
      else if (v.timestampValue !== undefined) out[k] = v.timestampValue;
      else if (v.arrayValue && Array.isArray(v.arrayValue.values)) out[k] = v.arrayValue.values.map(it => it.stringValue);
    }
    return out;
  }catch(_){ return {}; }
}

async function __fsGet__(path){
  const token = await __fbGetIdToken__();
  __syncLedBegin("GET");
  try{
    const res = await fetch(__fsDocUrl__(path), { headers: { "Authorization": `Bearer ${token}` }, cache:"no-store" });
    if (!res.ok) return null;
    return await res.json();
  }finally{
    __syncLedEnd("GET");
  }
}
async function __fsPatch__(path, data){
  const token = await __fbGetIdToken__();
  __syncLedBegin("POST");
  try{
    const res = await fetch(__fsDocUrl__(path) + "?currentDocument.exists=true", {
      method:"PATCH",
      headers:{ "Authorization": `Bearer ${token}`, "Content-Type":"application/json" },
      body: JSON.stringify(__fsEncode__(data))
    });
    if (res.ok) return await res.json();
    // if missing, create
    const res2 = await fetch(__fsDocUrl__(path), {
      method:"POST",
      headers:{ "Authorization": `Bearer ${token}`, "Content-Type":"application/json" },
      body: JSON.stringify(__fsEncode__(data))
    });
    if (!res2.ok) throw new Error("Scrittura Firebase fallita");
    return await res2.json();
  }finally{
    __syncLedEnd("POST");
  }
}
async function __fsSet__(path, data){
  const token = await __fbGetIdToken__();
  __syncLedBegin("POST");
  try{
    const res = await fetch(__fsDocUrl__(path), {
      method:"PATCH",
      headers:{ "Authorization": `Bearer ${token}`, "Content-Type":"application/json" },
      body: JSON.stringify(__fsEncode__(data))
    });
    if (!res.ok) throw new Error("Scrittura Firebase fallita");
    return await res.json();
  }finally{
    __syncLedEnd("POST");
  }
}

async function __fsList__(collectionPath){
  const token = await __fbGetIdToken__();
  __syncLedBegin("GET");
  try{
    const res = await fetch(__fsBase__() + "/" + collectionPath, { headers:{ "Authorization": `Bearer ${token}` }, cache:"no-store" });
    if (!res.ok) return [];
    const j = await res.json();
    return Array.isArray(j.documents) ? j.documents : [];
  }finally{
    __syncLedEnd("GET");
  }
}

async function __ensureAdminTeam__(){
  __fbLoadLink__();
      // QR modal close
      try{
        const qc = document.getElementById("qrClose");
        const qm = document.getElementById("qrModal");
        if (qc && qm) bindFastTap(qc, ()=>{ qm.hidden = true; try{ qm.setAttribute("aria-hidden","true"); }catch(_){} });
      }catch(_){}

  if (__FB_STATE__.teamId && __FB_STATE__.teamKey) return { teamId: __FB_STATE__.teamId, teamKey: __FB_STATE__.teamKey };

  const teamId = "T" + __randStr__(10);
  const teamKey = __randStr__(24);
  __fbSaveLink__(teamId, teamKey);
  return { teamId, teamKey };
}

async function __adminGenerateCode__(){
  // ensure team
  const { teamId, teamKey } = await __ensureAdminTeam__();

  // update team doc (include operator names from impostazioni if available)
  let ops = [];
  try{
    await ensureSettingsLoaded({ force:false, showLoader:false });
    ops = (getOperatorNamesFromSettings ? getOperatorNamesFromSettings() : []).map(x=>String(x||"").trim()).filter(Boolean);
  }catch(_){}

  const nowIso = new Date().toISOString();
  await __fsSet__(`teams/${teamId}`, { key: teamKey, operators: ops, updatedAt: { __ts: nowIso } });

  const code = __qrCodeText__(teamId, teamKey);
  try{ __showQrModal__(code); }catch(_){ alert(code); }
}

function __showQrModal__(code){
  const modal = document.getElementById("qrModal");
  const txt = document.getElementById("qrCodeText");

  if (txt){
    txt.textContent = code;

    // Tap per copiare (iOS-friendly)
    try{
      txt.onclick = async () => {
        try{
          if (navigator.clipboard && navigator.clipboard.writeText){
            await navigator.clipboard.writeText(String(code||""));
            try{ toast("Codice copiato", "blue"); }catch(_){}
          }else{
            // fallback: seleziona e copia via execCommand
            const r = document.createRange();
            r.selectNodeContents(txt);
            const sel = window.getSelection();
            sel.removeAllRanges();
            sel.addRange(r);
            document.execCommand("copy");
            sel.removeAllRanges();
            try{ toast("Codice copiato", "blue"); }catch(_){}
          }
        }catch(_e){
          try{ toast("Copia non disponibile", "orange"); }catch(_){}
        }
      };
    }catch(_){}
  }

  if (modal){
    modal.hidden = false;
    try{ modal.setAttribute("aria-hidden","false"); }catch(_){}
  }
}

function __openCodeLinkModal__(){
  return new Promise((resolve)=>{
    const modal = document.getElementById("qrScanModal");
    const input = document.getElementById("qrScanInput");
    const ok = document.getElementById("qrScanConfirm");
    const close = document.getElementById("qrScanClose");

    const finish = (val)=>{
      try{
        if (modal){
          modal.hidden = true;
          try{ modal.setAttribute("aria-hidden","true"); }catch(_){}
        }
      }catch(_){}
      resolve(String(val||"").trim());
    };

    // open
    try{
      if (modal){
        modal.hidden = false;
        try{ modal.setAttribute("aria-hidden","false"); }catch(_){}
      }
    }catch(_){}

    try{
      if (input){
        input.value = "";
        setTimeout(()=>{ try{ input.focus(); }catch(_){ } }, 80);
      }
    }catch(_){}

    const onOk = (e)=>{ try{ if(e && e.preventDefault) e.preventDefault(); }catch(_){ } finish(input ? input.value : ""); };
    const onClose = (e)=>{ try{ if(e && e.preventDefault) e.preventDefault(); }catch(_){ } finish(""); };

    if (ok) ok.onclick = onOk;
    if (close) close.onclick = onClose;
    if (input){
      input.onkeydown = (e)=>{ if (e && e.key === "Enter") onOk(e); };
    }
  });
}

async function __qrScanAndLink__(){
  // Ora: inserimento codice (no QR)
  let code = "";
  try{
    code = await __openCodeLinkModal__();
  }catch(_){ code = ""; }

  if (!code){
    try{ code = String(prompt("Incolla codice (DDAE|...)") || "").trim(); }catch(_){ code = ""; }
  }
  if (!code) return;

  const parsed = __parseQr__(code);
  if (!parsed) { try{ toast("Codice non valido", "orange"); }catch(_){ } return; }

  // validate team key matches
  const doc = await __fsGet__(`teams/${parsed.teamId}`);
  if (!doc){ try{ toast("Team non trovato", "orange"); }catch(_){ } return; }
  const data = __fsDecode__(doc);
  if (String(data.key||"") !== String(parsed.teamKey||"")){ try{ toast("Codice non valido", "orange"); }catch(_){ } return; }

  __fbSaveLink__(parsed.teamId, parsed.teamKey);
  try{ toast("Collegato", "green"); }catch(_){ }
}

function __isAdmin__(){
  return !!(state && state.session && !isOperatoreSession(state.session));
}
function __operatorName__(){
  try{ return String(state?.session?.username || "").trim(); }catch(_){ return ""; }
}


// --- Spesa Board (bacheca condivisa) ---
// La bacheca vive su Firebase in: sync/{teamId}/boards/spesa
// Contiene SOLO colazione + prodotti_pulizia e viene aggiornata da admin e operatori.
// Precedenza: ultimo aggiornamento per singolo prodotto (LWW) usando updatedAt/deletedAt.
function __spesaKeyShared__(it){
  const p = String(it?.prodotto || it?.nome || "").trim().toUpperCase();
  return p || String(it?.id || "").trim();
}
function __spesaIsDeleted__(it){
  const d = (it && (it.isDeleted ?? it.is_deleted ?? it.deleted));
  return (d === true) || (String(d) === "1");
}
function __spesaEffTs__(it){
  const u = String(it?.updatedAt || it?.updated_at || it?.createdAt || it?.created_at || "");
  if (__spesaIsDeleted__(it)){
    const d = String(it?.deletedAt || it?.deleted_at || "") || u;
    return d || u;
  }
  return u;
}
function __mergeSpesaLWW__(base, inc){
  const out = [];
  const best = new Map();
  const put = (r)=>{
    if (!r || typeof r !== "object") return;
    const k = __spesaKeyShared__(r);
    if (!k) return;
    const prev = best.get(k);
    if (!prev){ best.set(k, r); return; }
    const ta = __spesaEffTs__(prev);
    const tb = __spesaEffTs__(r);
    if (tb && (!ta || tb > ta)){
      best.set(k, r);
      return;
    }
    if (ta && tb && tb === ta){
      // tie-break: prefer tombstone (evita resurrezioni a parità di timestamp)
      if (__spesaIsDeleted__(r) && !__spesaIsDeleted__(prev)){
        best.set(k, r);
        return;
      }
      if (!__spesaIsDeleted__(r) && __spesaIsDeleted__(prev)){
        return;
      }
      // else keep prev
    }
  };
  (Array.isArray(base)?base:[]).forEach(put);
  (Array.isArray(inc)?inc:[]).forEach(put);
  best.forEach(v=>out.push(v));
  return out;
}

async function __fbExportSpesaBoard__(opts){
  __fbLoadLink__();
  if (!__FB_STATE__.teamId) return false;
  try{
    const colazione = await __tblGet__("colazione", []);
    const prodotti = await __tblGet__("prodotti_pulizia", []);
    const payload = {
      kind:"DDAE_SPESA_BOARD",
      build: BUILD_VERSION,
      at: __nowIso__(),
      datasets:{
        colazione: Array.isArray(colazione)?colazione:[],
        prodotti_pulizia: Array.isArray(prodotti)?prodotti:[]
      }
    };
    await __fsSet__(`sync/${__FB_STATE__.teamId}/boards/spesa`, {
      spesa_json: JSON.stringify(payload),
      updatedAt: { __ts: __nowIso__() }
    });
    return true;
  }catch(_){
    return false;
  }
}

async function __fbReadSpesaBoardPayload__(){
  __fbLoadLink__();
  if (!__FB_STATE__.teamId) return null;
  try{
    const doc = await __fsGet__(`sync/${__FB_STATE__.teamId}/boards/spesa`);
    if (!doc) return null;
    const data = __fsDecode__(doc);
    const raw = String(data?.spesa_json || "");
    if (!raw) return null;
    const p = JSON.parse(raw);
    if (p && p.datasets) return p;
  }catch(_){}
  return null;
}
// --- /Spesa Board ---
async function __fbExportAdmin__(opts){
  __fbLoadLink__();
  if (!__FB_STATE__.teamId) { try{ if(!opts?.silent) toast("Genera prima il codice in Impostazioni", "orange"); }catch(_){ } return false; }

  const tables = __OP_TABLES__.filter(t => t !== 'utenti');
  const datasets = {};
  for (const t of tables){ datasets[t] = await __tblGet__(t, (t==="impostazioni"?[]:[])); }
  const payload = { kind:"DDAE_SYNC_ADMIN", build: BUILD_VERSION, at: __nowIso__(), datasets };

  await __fsSet__(`sync/${__FB_STATE__.teamId}`, { admin_json: JSON.stringify(payload), updatedAt:{ __ts: __nowIso__() } });
  try{ await __fbExportSpesaBoard__({ silent:true }); }catch(_){ }
  try{ if(!opts?.silent) toast("Operazione completata", "blue"); }catch(_){}
  return true;

}

async function __fbImportOperator__(opts){
  __fbLoadLink__();
  if (!__FB_STATE__.teamId) { try{ if(!opts?.silent) toast("Inserisci prima il codice", "orange"); }catch(_){ } return false; }

// Carica dati condivisi SENZA dipendere dall'admin:
// - admin_json (se presente)
// - export di tutti gli operatori (collection operators)
let payloads = [];
try{
  const docAdmin = await __fsGet__(`sync/${__FB_STATE__.teamId}`);
  if (docAdmin){
    const dataA = __fsDecode__(docAdmin);
    const rawA = String(dataA.admin_json||"");
    if (rawA){
      try{
        const pA = JSON.parse(rawA);
        if (pA && pA.datasets) payloads.push(pA);
      }catch(_){}
    }
  }
}catch(_){}

try{
  const docsOps = await __fsList__(`sync/${__FB_STATE__.teamId}/operators`);
  (docsOps||[]).forEach(d=>{
    try{
      const dd = __fsDecode__(d);
      const rawO = String(dd.operator_json||"");
      if (!rawO) return;
      const pO = JSON.parse(rawO);
      if (pO && pO.datasets) payloads.push(pO);
    }catch(_){}
  });
}catch(_){}


// bacheca spesa condivisa (opzionale)
try{
  const b = await __fbReadSpesaBoardPayload__();
  if (b && b.datasets) payloads.push(b);
}catch(_){}
if (!payloads.length){ try{ if(!opts?.silent) toast("Nessun dato disponibile", "orange"); }catch(_){ } return false; }

// Combina tutti i dataset in un unico payload remoto
let payload = { kind:"DDAE_SYNC_COMBINED", build: BUILD_VERSION, at: __nowIso__(), datasets: {} };
try{
  for (const p of payloads){
    const ds = (p && p.datasets && typeof p.datasets === "object") ? p.datasets : {};
    for (const k of Object.keys(ds)){
      const v = ds[k];
      if (Array.isArray(v)){
        if (!Array.isArray(payload.datasets[k])) payload.datasets[k] = [];
        payload.datasets[k] = payload.datasets[k].concat(v);
      } else if (v && typeof v === "object") {
        // oggetti: merge shallow
        payload.datasets[k] = Object.assign({}, payload.datasets[k]||{}, v);
      } else {
        if (payload.datasets[k] === undefined) payload.datasets[k] = v;
      }
    }
  }
}catch(_){}


// Normalizza liste spesa (evita duplicati e applica precedenza cronologica per prodotto)
try{
  if (payload.datasets){
    if (Array.isArray(payload.datasets.colazione)){
      payload.datasets.colazione = __mergeSpesaLWW__([], payload.datasets.colazione);
    }
    if (Array.isArray(payload.datasets.prodotti_pulizia)){
      payload.datasets.prodotti_pulizia = __mergeSpesaLWW__([], payload.datasets.prodotti_pulizia);
    }
  }
}catch(_){}
if (!payload || !payload.datasets){ try{ if(!opts?.silent) toast("Dati non validi", "orange"); }catch(_){ } return false; }

// Import subset operatore
  // Import subset operatore (NON sovrascrivere credenziali locali)
  // - Evita di perdere l'account operatore dopo logout su device che importano da Firebase
  // - Per sicurezza: se nel payload arriva 'utenti', lo mergiamo con quelli già presenti
  try{
    if (payload && payload.datasets && Array.isArray(payload.datasets.utenti)) {
      const existingUsers = await __tblGet__("utenti", []);
      await __tblSet__("utenti", __mergeUsers__(existingUsers, payload.datasets.utenti));
    }
  }catch(_){ }

  // Merge helper: unisce per id scegliendo la versione più recente (updatedAt/createdAt)
  const __mergeByIdLatest__ = (localArr, remoteArr) => {
    try{
      const loc = Array.isArray(localArr) ? localArr : [];
      const rem = Array.isArray(remoteArr) ? remoteArr : [];
      const pickT = (o) => String(o?.updatedAt || o?.updated_at || o?.createdAt || o?.created_at || "");
      const map = new Map();
      let anon = 0;

      const put = (it) => {
        if (!it) return;
        const id = String(it?.id || "").trim();
        if (!id){
          map.set(`__anon_${anon++}`, it);
          return;
        }
        const prev = map.get(id);
        if (!prev){ map.set(id, it); return; }
        const tp = pickT(prev);
        const tn = pickT(it);
        // se "it" è più recente, sovrascrive prev; altrimenti mantiene prev
        if (tn && (!tp || tn > tp)){
          map.set(id, Object.assign({}, prev, it));
        } else {
          map.set(id, Object.assign({}, it, prev));
        }
      };

      loc.forEach(put);
      rem.forEach(put);

      return Array.from(map.values());
    }catch(_){
      return Array.isArray(remoteArr) ? remoteArr : (Array.isArray(localArr) ? localArr : []);
    }
  };

  for (const t of __OP_TABLES__){
    if (t === "utenti") continue;

    // Dati operativi: MERGE smart (key-based) per evitare di perdere lavoro locale
    // e per garantire che ogni operatore veda anche i dati degli altri.
    if (t === "pulizie"){
  if (payload.datasets[t] !== undefined){
    const local = await __tblGet__(t, []);
    const remote = Array.isArray(payload.datasets[t]) ? payload.datasets[t] : [];

    const pickU = (o) => String(o?.updatedAt || o?.updated_at || o?.createdAt || o?.created_at || "");
    const pickD = (o) => String(o?.deletedAt || o?.deleted_at || "");
    const key = (r) => {
      const d = String(r?.data || r?.date || "").slice(0,10);
      const s = String(r?.stanza || r?.room || "").trim();
      return (d && s) ? (d + "|" + s) : "";
    };
    const isNum = (v)=> typeof v === "number" && !Number.isNaN(v);
    const asNum = (v)=> {
      if (isNum(v)) return v;
      if (typeof v === "string" && v.trim()!=="" && !isNaN(Number(v))) return Number(v);
      return null;
    };
    const mergeMax = (a,b)=>{
      // LWW (Last-Write-Wins): vince SEMPRE l'ultima modifica, anche se è una cancellazione (valori a 0).
      const pickU = (o) => String(o?.updatedAt || o?.updated_at || o?.createdAt || o?.created_at || "");
      const pickD = (o) => String(o?.deletedAt || o?.deleted_at || "");
      const isDel = (o) => !!(o && (o.isDeleted || o.deleted));
      const effT = (o) => {
        const u = pickU(o);
        if (isDel(o)){ const d = pickD(o) || u; return d || u; }
        return u;
      };
      const ta = effT(a);
      const tb = effT(b);
      const newerIsB = (!ta && !tb) ? true : (tb && (!ta || tb >= ta));
      const newer = newerIsB ? (b||{}) : (a||{});
      const older = newerIsB ? (a||{}) : (b||{});
      return Object.assign({}, older, newer);
    };

    const best = new Map();
    const put = (it) => {
      if (!it || typeof it !== "object") return;
      const k = key(it);
      if (!k){ best.set("__anon_"+best.size, it); return; }
      const prev = best.get(k);
      if (!prev){ best.set(k, it); return; }
      best.set(k, mergeMax(prev, it));
    };
    (Array.isArray(local)?local:[]).forEach(put);
    remote.forEach(put);
    await __tblSet__(t, Array.from(best.values()));
  }
  continue;
}

    if (t === "operatori"){
  if (payload.datasets[t] !== undefined){
    const local = await __tblGet__(t, []);
    const remote = Array.isArray(payload.datasets[t]) ? payload.datasets[t] : [];
    const pickU = (o) => String(o?.updatedAt || o?.updated_at || o?.createdAt || o?.created_at || "");
    const pickD = (o) => String(o?.deletedAt || o?.deleted_at || "");
    const normOp = (s) => String(s||"").trim().toLowerCase();
    const normD  = (s) => __normIsoDate__(s);
    const key = (r) => {
      const d = normD(r?.data || r?.date || "");
      const o = normOp(r?.operatore || r?.nome || "");
      return (d && o) ? (d + "|" + o) : "";
    };
    const asNum = (v)=> {
      if (typeof v === "number" && !Number.isNaN(v)) return v;
      if (typeof v === "string" && v.trim()!=="" && !isNaN(Number(v))) return Number(v);
      return null;
    };
    const mergeMax = (a,b)=>{
      // LWW (Last-Write-Wins): vince SEMPRE l'ultima modifica, anche se è una cancellazione (valori a 0).
      const pickU = (o) => String(o?.updatedAt || o?.updated_at || o?.createdAt || o?.created_at || "");
      const pickD = (o) => String(o?.deletedAt || o?.deleted_at || "");
      const isDel = (o) => !!(o && (o.isDeleted || o.deleted));
      const effT = (o) => {
        const u = pickU(o);
        if (isDel(o)){ const d = pickD(o) || u; return d || u; }
        return u;
      };
      const ta = effT(a);
      const tb = effT(b);
      const newerIsB = (!ta && !tb) ? true : (tb && (!ta || tb >= ta));
      const newer = newerIsB ? (b||{}) : (a||{});
      const older = newerIsB ? (a||{}) : (b||{});
      return Object.assign({}, older, newer);
    };

    const best = new Map();
    const put = (it) => {
      if (!it || typeof it !== "object") return;
      const k = key(it);
      if (!k){ best.set("__anon_"+best.size, it); return; }
      const prev = best.get(k);
      if (!prev){ best.set(k, it); return; }
      best.set(k, mergeMax(prev, it));
    };
    (Array.isArray(local)?local:[]).forEach(put);
    remote.forEach(put);
    await __tblSet__(t, Array.from(best.values()));
  }
  continue;
}

    if (t === "lavanderia"){
  if (payload.datasets[t] !== undefined){
    const local = await __tblGet__(t, []);
    const remote = Array.isArray(payload.datasets[t]) ? payload.datasets[t] : [];
    const pickU = (o) => String(o?.updatedAt || o?.updated_at || o?.createdAt || o?.created_at || "");
    const pickD = (o) => String(o?.deletedAt || o?.deleted_at || "");
    const key = (it) => {
      const id = String(it?.id || "").trim();
      if (id) return "id:" + id;
      const a = __normIsoDate__(it?.startDate || it?.start_date || it?.from || "");
      const b = __normIsoDate__(it?.endDate || it?.end_date || it?.to || "");
      return (a && b) ? ("rng:" + a + "|" + b) : "";
    };
    const asNum = (v)=> {
      if (typeof v === "number" && !Number.isNaN(v)) return v;
      if (typeof v === "string" && v.trim()!=="" && !isNaN(Number(v))) return Number(v);
      return null;
    };
    const mergeMax = (a,b)=>{
      // LWW (Last-Write-Wins): vince SEMPRE l'ultima modifica, anche se è una cancellazione (valori a 0).
      const pickU = (o) => String(o?.updatedAt || o?.updated_at || o?.createdAt || o?.created_at || "");
      const pickD = (o) => String(o?.deletedAt || o?.deleted_at || "");
      const isDel = (o) => !!(o && (o.isDeleted || o.deleted));
      const effT = (o) => {
        const u = pickU(o);
        if (isDel(o)){ const d = pickD(o) || u; return d || u; }
        return u;
      };
      const ta = effT(a);
      const tb = effT(b);
      const newerIsB = (!ta && !tb) ? true : (tb && (!ta || tb >= ta));
      const newer = newerIsB ? (b||{}) : (a||{});
      const older = newerIsB ? (a||{}) : (b||{});
      return Object.assign({}, older, newer);
    };
    const best = new Map();
    const put = (it) => {
      if (!it || typeof it !== "object") return;
      const k = key(it);
      if (!k){ best.set("__anon_"+best.size, it); return; }
      const prev = best.get(k);
      if (!prev){ best.set(k, it); return; }
      best.set(k, mergeMax(prev, it));
    };
    (Array.isArray(local)?local:[]).forEach(put);
    remote.forEach(put);
    await __tblSet__(t, Array.from(best.values()));
  }
  continue;
}

    if (t === "colazione" || t === "prodotti_pulizia"){
      if (payload.datasets[t] !== undefined){
        const local = await __tblGet__(t, []);
        const remote = Array.isArray(payload.datasets[t]) ? payload.datasets[t] : [];
        await __tblSet__(t, __mergeSpesaLWW__(local, remote));
      }
      continue;
    }

    if (payload.datasets[t] !== undefined){
      await __tblSet__(t, payload.datasets[t]);
    }
  }

    try{ await __fbExportSpesaBoard__({ silent:true }); }catch(_){ }

try{ if(!opts?.silent) toast("Operazione completata", "green"); }catch(_){}
  const __restoreAfterSync = opts?.restoreState || __captureSyncRestoreState();
  try{ await __refreshAfterSync__(__restoreAfterSync); }catch(_){
    if(!opts?.skipReload){ setTimeout(()=>{ try{ __writeRestoreState(__restoreAfterSync); }catch(_){ } try{ location.reload(); }catch(_){ } }, 250); }
  }
  return true;
}

async function __fbExportOperator__(opts){
  __fbLoadLink__();
  if (!__FB_STATE__.teamId) { try{ if(!opts?.silent) toast("Inserisci prima il codice", "orange"); }catch(_){ } return false; }
  const name = (__operatorName__() || "operatore").toLowerCase().replace(/\s+/g,"_").replace(/[^a-z0-9_\-]/g,"");
  if (!name){ try{ if(!opts?.silent) toast("Nome operatore mancante", "orange"); }catch(_){ } return false; }

  const datasets = {
    pulizie: await __tblGet__("pulizie", []),
    operatori: await __tblGet__("operatori", []),
    lavanderia: await __tblGet__("lavanderia", []),
    colazione: await __tblGet__("colazione", []),
    prodotti_pulizia: await __tblGet__("prodotti_pulizia", [])
  };
  const payload = { kind:"DDAE_SYNC_OPERATOR", operator:name, build: BUILD_VERSION, at: __nowIso__(), datasets };
  await __fsSet__(`sync/${__FB_STATE__.teamId}/operators/${name}`, { operator_json: JSON.stringify(payload), updatedAt:{ __ts: __nowIso__() } });
  try{ await __fbExportSpesaBoard__({ silent:true }); }catch(_){ }
  try{ if(!opts?.silent) toast("Operazione completata", "blue"); }catch(_){}
  return true;

}

function __pickLatestLaundry__(list){
  try{
    const arr = Array.isArray(list)?list:[];
    if (!arr.length) return null;
    // pick max updatedAt or endDate
    return arr.slice().sort((a,b)=>{
      const ua = String(a.updatedAt||a.updated_at||a.endDate||a.end_date||"");
      const ub = String(b.updatedAt||b.updated_at||b.endDate||b.end_date||"");
      return ua < ub ? 1 : (ua > ub ? -1 : 0);
    })[0];
  }catch(_){ return null; }
}

async function __fbImportAdmin__(opts){
  __fbLoadLink__();
  if (!__FB_STATE__.teamId) { try{ if(!opts?.silent) toast("Genera prima il codice in Impostazioni", "orange"); }catch(_){ } return false; }

  // get operator list from settings if present
  let ops = [];
  try{
    await ensureSettingsLoaded({ force:false, showLoader:false });
    ops = (getOperatorNamesFromSettings ? getOperatorNamesFromSettings() : []).map(x=>String(x||"").trim()).filter(Boolean);
  }catch(_){}
  // normalize
  ops = ops.map(n => String(n).toLowerCase().replace(/\s+/g,"_").replace(/[^a-z0-9_\-]/g,"")).filter(Boolean);

  // Sempre includi TUTTI i documenti presenti nella collection operators (non dipendere solo dal roster/settings)
  // per evitare che un operatore venga "saltato" e che l'admin esporti solo un sottoinsieme dei dati.
  try{
    const docsAll = await __fsList__(`sync/${__FB_STATE__.teamId}/operators`);
    const fromDocs = (docsAll||[]).map(d => String(d.name||"").split("/").pop()).map(x=>String(x||"").trim()).filter(Boolean);
    if (!ops.length) ops = fromDocs;
    else {
      const set = new Set(ops);
      fromDocs.forEach(n=>{ if(n) set.add(n); });
      ops = Array.from(set);
    }
  }catch(_){ }

  if (!ops.length){
    try{ if(!opts?.silent) toast("Nessun operatore trovato", "orange"); }catch(_){ }
  }

  // merge
  let mergedPulizie = await __tblGet__("pulizie", []);
  const basePulizie = Array.isArray(mergedPulizie) ? mergedPulizie : [];
  mergedPulizie = basePulizie.slice();

  let mergedOperatori = await __tblGet__("operatori", []);
  const baseOperatori = Array.isArray(mergedOperatori) ? mergedOperatori : [];
  mergedOperatori = baseOperatori.slice();


  // merge colazione + prodotti pulizia (lista spesa) da operatori
  let mergedColazione = await __tblGet__("colazione", []);
  mergedColazione = Array.isArray(mergedColazione) ? mergedColazione.slice() : [];

  let mergedProdottiPulizia = await __tblGet__("prodotti_pulizia", []);
  mergedProdottiPulizia = Array.isArray(mergedProdottiPulizia) ? mergedProdottiPulizia.slice() : [];

  // merge bacheca condivisa spesa (se presente)
  try{
    const b = await __fbReadSpesaBoardPayload__();
    if (b && b.datasets){
      mergedColazione = __mergeSpesaLWW__(mergedColazione, b.datasets.colazione);
      mergedProdottiPulizia = __mergeSpesaLWW__(mergedProdottiPulizia, b.datasets.prodotti_pulizia);
    }
  }catch(_){}

  const __spesaKey__ = (it) => {
    const p = String(it?.prodotto || it?.nome || "").trim().toUpperCase();
    return p || String(it?.id || "").trim();
  };
  const __spesaUAt__ = (it) => String(it?.updatedAt || it?.updated_at || it?.createdAt || it?.created_at || "");
  const __mergeSpesaList__ = (base, inc) => {
    const out = Array.isArray(base) ? base.slice() : [];
    const idxByKey = new Map();
    out.forEach((r, i) => { const k = __spesaKey__(r); if (k) idxByKey.set(k, i); });
    (Array.isArray(inc) ? inc : []).forEach((r) => {
      const k = __spesaKey__(r);
      if (!k) return;
      const i = idxByKey.get(k);
      if (i === undefined){
        out.push(r);
        idxByKey.set(k, out.length - 1);
        return;
      }
      const a = out[i];
      const ua = __spesaUAt__(a);
      const ub = __spesaUAt__(r);
      if (ub && (!ua || ub > ua)){
        out[i] = r;
      }else if (!ub && !ua){
        // fallback: conserva qty più alta
        const qa = parseInt(String(a?.qty ?? 0), 10);
        const qb = parseInt(String(r?.qty ?? 0), 10);
        if ((isNaN(qa)?0:qa) < (isNaN(qb)?0:qb)) out[i] = Object.assign({}, a, r);
      }
    });
    return out;
  };

  let mergedLavanderia = await __tblGet__("lavanderia", []);
  mergedLavanderia = Array.isArray(mergedLavanderia) ? mergedLavanderia.slice() : [];
  const __lavKey__ = (it) => {
    const id = String(it?.id || "").trim();
    if (id) return "id:" + id;
    const a = __normIsoDate__(it?.startDate || it?.start_date || it?.from || "");
    const b = __normIsoDate__(it?.endDate || it?.end_date || it?.to || "");
    return (a && b) ? ("rng:" + a + "|" + b) : "";
  };
  const __lavUAt__ = (it) => String(it?.updatedAt || it?.updated_at || it?.createdAt || it?.created_at || "");
  const __lavIdx__ = new Map();
  mergedLavanderia.forEach((it, i) => { const k = __lavKey__(it); if (k && !__lavIdx__.has(k)) __lavIdx__.set(k, i); });

  for (const op of ops){

    const doc = await __fsGet__(`sync/${__FB_STATE__.teamId}/operators/${op}`);
    if (!doc) continue;
    const d = __fsDecode__(doc);
    const raw = String(d.operator_json||"");
    if (!raw) continue;
    let payload=null; try{ payload=JSON.parse(raw); }catch(_){ payload=null; }
    if (!payload || !payload.datasets) continue;

    // merge pulizie entries (merge by id or by key data+stanza; max per-col)
    try{
      const cols = getLaundryComponentCodes();
      const listP = Array.isArray(payload.datasets.pulizie) ? payload.datasets.pulizie : [];
      const byId = new Map();
      mergedPulizie.forEach(r=>{ const id = String(r?.id||"").trim(); if (id) byId.set(id, r); });
      const byKey = new Map();
      mergedPulizie.forEach(r=>{
        const d = String(r?.data||r?.date||"").slice(0,10);
        const s = String(r?.stanza||r?.room||"").trim();
        if (d && s) byKey.set(d+"|"+s, r);
      });
      listP.forEach(r=>{
        if (!r) return;
        const id = String(r?.id||"").trim();
        const d = String(r?.data||r?.date||"").slice(0,10);
        const s = String(r?.stanza||r?.room||"").trim();
        const key = (d && s) ? (d+"|"+s) : "";
        const target = (id && byId.has(id)) ? byId.get(id) : (key && byKey.has(key) ? byKey.get(key) : null);
        if (!target){
          mergedPulizie.push(r);
          if (id) byId.set(id, r);
          if (key) byKey.set(key, r);
          return;
        }
        const ua = String(target.updatedAt||target.updated_at||target.createdAt||target.created_at||"");
        const ub = String(r.updatedAt||r.updated_at||r.createdAt||r.created_at||"");
        const should = (!ua && !ub) ? true : (ub && (!ua || ub > ua));
        if (should){
          try{ Object.keys(r||{}).forEach(k=>{ target[k] = r[k]; }); }catch(_){ }
        }
      });
    }catch(_){ }

    // merge operatori entries (LWW by data+operatore; consente decrementi/cancellazioni)
    try{
      const list = Array.isArray(payload.datasets.operatori) ? payload.datasets.operatori : [];
      const __opKey__ = (it) => {
        const d = __normIsoDate__(it?.data || it?.date || "");
        const op = String(it?.operatore || it?.nome || "").trim().toLowerCase();
        return (d && op) ? (d + "|" + op) : "";
      };
      const __opUAt__ = (it) => String(it?.updatedAt || it?.updated_at || it?.createdAt || it?.created_at || "");
      const idxByKey = new Map();
      // indicizza base (dedupe interno: tieni la più recente)
      mergedOperatori.forEach((it, i) => {
        const k = __opKey__(it);
        if (!k) return;
        if (!idxByKey.has(k)) { idxByKey.set(k, i); return; }
        const j = idxByKey.get(k);
        const a = mergedOperatori[j];
        const ua = __opUAt__(a);
        const ub = __opUAt__(it);
        const should = (!ua && !ub) ? false : (ub && (!ua || ub > ua));
        if (should) idxByKey.set(k, i);
      });

      list.forEach((it) => {
        const k = __opKey__(it);
        if (!k){ mergedOperatori.push(it); return; }
        const i = idxByKey.get(k);
        if (i === undefined){
          mergedOperatori.push(it);
          idxByKey.set(k, mergedOperatori.length - 1);
          return;
        }
        const a = mergedOperatori[i];
        const ua = __opUAt__(a);
        const ub = __opUAt__(it);
        const should = (!ua && !ub) ? true : (ub && (!ua || ub > ua));
        if (should){
          mergedOperatori[i] = Object.assign({}, (mergedOperatori[i]||{}), (it||{}));
        }
      });
    }catch(_){}
// merge lista spesa (colazione + prodotti pulizia)
    try{
      if (payload.datasets.colazione !== undefined){
        mergedColazione = __mergeSpesaLWW__(mergedColazione, payload.datasets.colazione);
      }
      if (payload.datasets.prodotti_pulizia !== undefined){
        mergedProdottiPulizia = __mergeSpesaLWW__(mergedProdottiPulizia, payload.datasets.prodotti_pulizia);
      }
    }catch(_){}

    // merge lavanderia entries (LWW; supporta eliminazioni via isDeleted)
    try{
      const listL = Array.isArray(payload.datasets.lavanderia) ? payload.datasets.lavanderia : [];
      (Array.isArray(listL) ? listL : []).forEach((it) => {
        const k = __lavKey__(it);
        if (!k){ mergedLavanderia.push(it); return; }
        const i = __lavIdx__.get(k);
        if (i === undefined){
          mergedLavanderia.push(it);
          __lavIdx__.set(k, mergedLavanderia.length - 1);
          return;
        }
        const a = mergedLavanderia[i];
        const ua = __lavUAt__(a);
        const ub = __lavUAt__(it);
        const should = (!ua && !ub) ? true : (ub && (!ua || ub > ua));
        if (should){
          mergedLavanderia[i] = it;
        }
      });
    }catch(_){}
  }

  // merge impostazioni dall'admin_json per preservare catalogo operatori (colore/tariffa/benzina) anche dopo sync
  try{
    const docAdminSync = await __fsGet__(`sync/${__FB_STATE__.teamId}`);
    if (docAdminSync){
      const dataAdminSync = __fsDecode__(docAdminSync);
      const rawAdminSync = String(dataAdminSync?.admin_json || "");
      if (rawAdminSync){
        const payloadAdminSync = JSON.parse(rawAdminSync);
        const remoteImp = Array.isArray(payloadAdminSync?.datasets?.impostazioni) ? payloadAdminSync.datasets.impostazioni : [];
        if (remoteImp.length){
          const localImp = await __tblGet__("impostazioni", []);
          const pickKey = (r) => String(r?.key || r?.Key || "").trim().toLowerCase();
          const pickU = (r) => String(r?.updatedAt || r?.updated_at || r?.createdAt || r?.created_at || "");
          const byKey = new Map();
          (Array.isArray(localImp) ? localImp : []).forEach((row) => {
            const k = pickKey(row) || `__local_${byKey.size}`;
            byKey.set(k, row);
          });
          remoteImp.forEach((row) => {
            const k = pickKey(row) || `__remote_${byKey.size}`;
            if ((state?.session && isOperatoreSession(state.session)) && k === "app_language") return;
            const prev = byKey.get(k);
            if (!prev){ byKey.set(k, row); return; }
            if (k === "operatori_catalogo"){
              byKey.set(k, Object.assign({}, prev, row));
              return;
            }
            const up = pickU(prev);
            const ur = pickU(row);
            const takeRemote = (!up && !ur) ? true : (!!ur && (!up || ur >= up));
            byKey.set(k, takeRemote ? Object.assign({}, prev, row) : Object.assign({}, row, prev));
          });
          await __tblSet__("impostazioni", Array.from(byKey.values()));
          try{ state.settings.loaded = false; }catch(_){ }
          try{ await ensureSettingsLoaded({ force:true, showLoader:false }); }catch(_){ }
        }
      }
    }
  }catch(_){ }

  // cleanup: dedupe pulizie/operatori/lavanderia con logica LWW (evita che i vecchi valori "restino appiccicati")
  try{
    const __uAt__ = (it) => String(it?.updatedAt || it?.updated_at || it?.createdAt || it?.created_at || "");
    const __pKey__ = (it) => {
      const d = String(it?.data || it?.date || "").slice(0,10);
      const s = String(it?.stanza || it?.room || "").trim();
      return (d && s) ? (d + "|" + s) : "";
    };
    const bestP = new Map();
    (Array.isArray(mergedPulizie) ? mergedPulizie : []).forEach(it => {
      const k = __pKey__(it);
      if (!k) return;
      const prev = bestP.get(k);
      if (!prev){ bestP.set(k, it); return; }
      const ua = __uAt__(prev);
      const ub = __uAt__(it);
      const should = (!ua && !ub) ? false : (ub && (!ua || ub > ua));
      if (should) bestP.set(k, it);
    });
    mergedPulizie = Array.from(bestP.values());
  }catch(_){}

  try{
    const __uAt__ = (it) => String(it?.updatedAt || it?.updated_at || it?.createdAt || it?.created_at || "");
    const __oKey__ = (it) => {
      const d = __normIsoDate__(it?.data || it?.date || "");
      const op = String(it?.operatore || it?.nome || "").trim().toLowerCase();
      return (d && op) ? (d + "|" + op) : "";
    };
    const bestO = new Map();
    (Array.isArray(mergedOperatori) ? mergedOperatori : []).forEach(it => {
      const k = __oKey__(it);
      if (!k) return;
      const prev = bestO.get(k);
      if (!prev){ bestO.set(k, it); return; }
      const ua = __uAt__(prev);
      const ub = __uAt__(it);
      const should = (!ua && !ub) ? false : (ub && (!ua || ub > ua));
      if (should) bestO.set(k, it);
    });
    mergedOperatori = Array.from(bestO.values());
  }catch(_){}

  try{
    const __uAt__ = (it) => String(it?.updatedAt || it?.updated_at || it?.createdAt || it?.created_at || "");
    const __lKey__ = (it) => {
      const id = String(it?.id || "").trim();
      if (id) return "id:" + id;
      const a = __normIsoDate__(it?.startDate || it?.start_date || it?.from || "");
      const b = __normIsoDate__(it?.endDate || it?.end_date || it?.to || "");
      return (a && b) ? ("rng:" + a + "|" + b) : "";
    };
    const bestL = new Map();
    (Array.isArray(mergedLavanderia) ? mergedLavanderia : []).forEach(it => {
      const k = __lKey__(it);
      if (!k) return;
      const prev = bestL.get(k);
      if (!prev){ bestL.set(k, it); return; }
      const ua = __uAt__(prev);
      const ub = __uAt__(it);
      const should = (!ua && !ub) ? false : (ub && (!ua || ub > ua));
      if (should) bestL.set(k, it);
    });
    mergedLavanderia = Array.from(bestL.values());
  }catch(_){}
// write merged pulizie
  await __tblSet__("pulizie", mergedPulizie);

  // write merged operatori
  await __tblSet__("operatori", mergedOperatori);

  // write merged spesa (colazione + prodotti pulizia)
  await __tblSet__("colazione", mergedColazione);
  await __tblSet__("prodotti_pulizia", mergedProdottiPulizia);

  // write merged lavanderia (include tombstones; UI filtra isDeleted)
  await __tblSet__("lavanderia", mergedLavanderia);


    try{ await __fbExportSpesaBoard__({ silent:true }); }catch(_){ }

try{ if(!opts?.silent) toast("Operazione completata", "green"); }catch(_){}
  const __restoreAfterSync = opts?.restoreState || __captureSyncRestoreState();
  try{ await __refreshAfterSync__(__restoreAfterSync); }catch(_){
    if(!opts?.skipReload){ setTimeout(()=>{ try{ __writeRestoreState(__restoreAfterSync); }catch(_){ } try{ location.reload(); }catch(_){ } }, 250); }
  }
  return true;
}

async function __handleSyncImport__(){
  const restoreState = __captureSyncRestoreState();
  if (__isAdmin__()) return __fbImportAdmin__({ restoreState });
  return __fbImportOperator__({ restoreState });
}
async function __handleSyncExport__(){
  const restoreState = __captureSyncRestoreState();
  if (__isAdmin__()) return __fbExportAdmin__({ restoreState });
  return __fbExportOperator__({ restoreState });
}


async function __handleSyncBoth__(){
  __fbLoadLink__();
  const restoreState = __captureSyncRestoreState();
  try{ toast("Sync in corso...", "blue"); }catch(_){}
  const __sleep__ = (ms) => new Promise(r => setTimeout(r, ms));
  const __attemptSync__ = async (fn, tries = 2, delay = 350) => {
    let last = false;
    for (let i = 0; i < tries; i++){
      try{
        last = await fn();
        if (last !== false) return last;
      }catch(_){
        last = false;
      }
      if (i < (tries - 1)) await __sleep__(delay);
    }
    return last;
  };
  let exported = false, imported = false;
  try{
    if (__isAdmin__()){
      exported = await __attemptSync__(()=>__fbExportAdmin__({ silent:true, restoreState }), 2, 350);
      imported = await __attemptSync__(()=>__fbImportAdmin__({ silent:true, skipReload:true, restoreState }), 3, 450);
    }else{
      exported = await __attemptSync__(()=>__fbExportOperator__({ silent:true, restoreState }), 2, 350);
      imported = await __attemptSync__(()=>__fbImportOperator__({ silent:true, skipReload:true, restoreState }), 3, 450);
    }
  }catch(_){ }
  const ok = (exported !== false) || (imported !== false);
  try{ toast(ok ? "Sync completata" : "Sync non riuscita", ok ? "green" : "orange"); }catch(_){}
  setTimeout(()=>{ try{ __writeRestoreState(restoreState); }catch(_){ } try{ location.reload(); }catch(_){ } }, 900);
  return ok;
}

// Bind sync buttons once DOM is ready
try{
  window.addEventListener("load", ()=>{
    try{ bindStatFiscalModeToggle(); }catch(_){ }
    try{
      __fbLoadLink__();
      const btn = document.getElementById("goDbSync");
      if (btn && !btn.__syncBound){ btn.__syncBound = true; bindFastTap(btn, async ()=>{ try{ await __handleSyncBoth__(); }catch(e){ try{ toast("Sync non disponibile", "orange"); }catch(_){ } } }); }
      try{ setTimeout(()=>{ try{ __fitHomeSyncBtn__(); }catch(_){ } }, 0); }catch(_){ }
      try{
        if (!window.__homeSyncFitBound){
          window.__homeSyncFitBound = true;
          let __homeSyncFitTO__ = null;
          window.addEventListener("resize", ()=>{
            try{ clearTimeout(__homeSyncFitTO__); }catch(_){ }
            __homeSyncFitTO__ = setTimeout(()=>{ try{ __fitHomeSyncBtn__(); }catch(_){ } }, 80);
          }, { passive:true });
        }
      }catch(_){ }
    }catch(_){}
  }, { passive:true });
}catch(_){}


// Dialog a due azioni (riusa il modal Sì/No esistente con label dinamiche)
// Ritorna "yes" o "no".
async function __confirmTwoActions__(message, yesLabel, noLabel){
  try{
    const yesBtn = document.getElementById("confirmYesNoYes");
    const noBtn  = document.getElementById("confirmYesNoNo");
    const prevYes = yesBtn ? yesBtn.textContent : null;
    const prevNo  = noBtn  ? noBtn.textContent  : null;

    if (yesBtn) yesBtn.textContent = String(yesLabel || "Sì");
    if (noBtn)  noBtn.textContent  = String(noLabel  || "No");

    const ok = await confirmYesNo(String(message || "Confermare?"));
    try{ __confirmYesNoForceClose__(); }catch(_){ }

    // restore
    if (yesBtn && prevYes !== null) yesBtn.textContent = prevYes;
    if (noBtn  && prevNo  !== null) noBtn.textContent  = prevNo;

    return ok ? "yes" : "no";
  }catch(_){
    // fallback
    try{ return (confirm(String(message || "Confermare?")) ? "yes" : "no"); }catch(__){ return "no"; }
  }
}


async function __openDbPopup__(kind){
  const k = String(kind||"admin").toLowerCase().startsWith("op") ? "operator" : "admin";
  const label = (k==="admin") ? "DB Amministratore" : "DB Operatore";
  const choice = await __confirmTwoActions__(label + ": scegli operazione", "Importa", "Esporta");
  if (choice === "yes") return __dbImport__(k);
  // best-effort preopen for iOS download
  let w=null; try{ w = window.open("", "_blank"); }catch(_){ w=null; }
  return __dbExport__(k, w);
}


// Nuovo menu DB: un solo popup con selezione Admin/Operatore + Import/Export
function __setDbMenuSelected__(kind){
  const a = document.getElementById("dbMenuOptAdmin");
  const o = document.getElementById("dbMenuOptOperator");
  if (!a || !o) return;
  const k = String(kind||"admin").toLowerCase().startsWith("op") ? "operator" : "admin";
  if (k === "admin"){
    a.classList.add("selected"); o.classList.remove("selected");
    try{ a.setAttribute("aria-pressed","true"); o.setAttribute("aria-pressed","false"); }catch(_){}
  }else{
    o.classList.add("selected"); a.classList.remove("selected");
    try{ o.setAttribute("aria-pressed","true"); a.setAttribute("aria-pressed","false"); }catch(_){}
  }
  try{ window.__dbMenuKind = k; }catch(_){}
}

function __closeDbMenuModal__(){
  try{
    const modal = document.getElementById("dbMenuModal");
    if (modal){ modal.hidden = true; try{ modal.setAttribute("aria-hidden","true"); }catch(_){ } }
  }catch(_){}
}

function __openDbMenuModal__(){
  try{
    const modal = document.getElementById("dbMenuModal");
    const closeBtn = document.getElementById("dbMenuClose");
    const importBtn = document.getElementById("dbMenuImportBtn");
    const exportBtn = document.getElementById("dbMenuExportBtn");
    if (!modal || !importBtn || !exportBtn){
      return __openDbPopup__("admin");
    }

    // bind once
    if (!modal.__bound){
      modal.__bound = true;
      const bind = (el, fn) => { try{ if (el) bindFastTap(el, fn); }catch(_){ try{ el.addEventListener("click", fn); }catch(__){} } };

      bind(closeBtn, ()=>{ __closeDbMenuModal__(); });

      // Import/Export LOCAL (FILE OFFLINE) — solo Admin
      bind(importBtn, async ()=>{ __closeDbMenuModal__(); await __dbImport__("admin"); });
      bind(exportBtn, async ()=>{
        __closeDbMenuModal__();
        // Pre-open window to keep iOS user gesture for download (best-effort)
        let w=null; try{ w = window.open("", "_blank"); }catch(_){ w=null; }
        await __dbExport__("admin", w);
      });
      // click outside to close
      try{
        modal.addEventListener("click", (e)=>{ if (e.target === modal) __closeDbMenuModal__(); });
      }catch(_){}
    }

    modal.hidden = false;
    try{ modal.setAttribute("aria-hidden","false"); }catch(_){}
  }catch(e){
    try{ toast(e.message || String(e)); }catch(_){}
  }
}




// ===== DB Import/Export (LOCAL) =====
const __DB_EXPORT_KIND__ = "dDAE_export";
const __DB_SCHEMA_VERSION__ = 2;

function __dbTablesForKind__(kind){
  const k = String(kind || "").toLowerCase();
  return (k.startsWith("admin") ? __ADMIN_TABLES__ : __OP_TABLES__);
}

function __safeFileName__(base){
  return String(base || "backup").replace(/[^\w\-\.]+/g, "_");
}

function __dbBackupExcludedLocalStorageKeys__(){
  try{
    return new Set([
      "dDAE_pending_build",
      "dDAE_update_attempt_build",
      "dDAE_update_attempt_at",
      "dDAE_session_v2",
      "__ddae_restore_state"
    ]);
  }catch(_){ return new Set(); }
}

function __collectBackupLocalStorage__(){
  const out = {};
  try{
    const excluded = __dbBackupExcludedLocalStorageKeys__();
    for (let i = 0; i < localStorage.length; i++) {
      const k = localStorage.key(i);
      if (!k) continue;
      const key = String(k);
      const lower = key.toLowerCase();
      const isAppKey = key.startsWith("dDAE_") || key.startsWith("ddae_") || key.startsWith("__ddae_") || lower.startsWith("ddae:");
      if (!isAppKey) continue;
      if (excluded.has(key)) continue;
      try{ out[key] = String(localStorage.getItem(key) ?? ""); }catch(_){ }
    }
  }catch(_){ }
  return out;
}

function __restoreBackupLocalStorage__(payload){
  try{
    if (!payload || typeof payload !== "object") return;
    const excluded = __dbBackupExcludedLocalStorageKeys__();
    Object.keys(payload).forEach((key)=>{
      try{
        const k = String(key || "");
        if (!k || excluded.has(k)) return;
        const v = payload[key];
        if (v === null || v === undefined) localStorage.removeItem(k);
        else localStorage.setItem(k, String(v));
      }catch(_){ }
    });
  }catch(_){ }
}

function __collectBackupThemeSlots__(){
  try{
    return {
      slots: (typeof __roomSettingsThemeSlotsRead__ === 'function') ? __roomSettingsThemeSlotsRead__() : {},
      activeSlot: (typeof localStorage !== 'undefined') ? String(localStorage.getItem('dDAE_roomsettings_theme_active_slot_v1') || '') : ''
    };
  }catch(_){
    return { slots:{}, activeSlot:'' };
  }
}

function __restoreBackupThemeSlots__(payload){
  try{
    const src = (payload && typeof payload === 'object') ? payload : {};
    const slots = (src.slots && typeof src.slots === 'object') ? src.slots : {};
    try{
      localStorage.setItem('dDAE_roomsettings_theme_slots_v1', JSON.stringify(slots || {}));
    }catch(_){ }
    try{
      if (src.activeSlot === null || src.activeSlot === undefined || String(src.activeSlot || '').trim() === '') localStorage.removeItem('dDAE_roomsettings_theme_active_slot_v1');
      else localStorage.setItem('dDAE_roomsettings_theme_active_slot_v1', String(src.activeSlot || ''));
    }catch(_){ }
  }catch(_){ }
}

function __dbFmtDateDdMmYy__(){
  try{
    const ts = new Date();
    const d = String(ts.getDate()).padStart(2,"0");
    const m = String(ts.getMonth()+1).padStart(2,"0");
    const y = String(ts.getFullYear()).slice(-2);
    return `${d}-${m}-${y}`;
  }catch(_){ return "00-00-00"; }
}

function __dbAccountNameForKind__(kind){
  // admin/operator name for filenames. Prefer current session username; fallback to app label.
  try{
    const k = String(kind||"").toLowerCase();
    const sess = (typeof loadSession === "function") ? loadSession() : null;
    const uname = String(sess?.username || "").trim();
    if (uname) return uname.toUpperCase();
    if (k.startsWith("admin")) return "DAEDALIUM";
    return "OPERATORE";
  }catch(_){
    return "DAEDALIUM";
  }
}




function __sessionFromUserRow__(u){
  try{
    if (!u || typeof u !== "object") return null;
    const user_id = String(u?.id || u?.user_id || u?.userId || u?.username || "").trim();
    const username = String(u?.username || u?.user || u?.email || "").trim();
    const ruoloRaw = String(u?.ruolo || u?.role || u?.tipo || "").trim().toLowerCase();
    const ruolo = ruoloRaw
      ? (ruoloRaw.startsWith("op") ? "operatore" : "admin")
      : ((String(u?.isOperatore || u?.is_operatore || "").trim() === "1" || u?.isOperatore === true) ? "operatore" : "admin");
    if (!user_id || !username) return null;
    return Object.assign({
      user_id,
      username,
      ruolo,
      name: String(u?.name || u?.nome || username).trim()
    }, __sessionExtrasFromUserRow__(u || {}));
  }catch(_){ return null; }
}

function __adminSessionFromUserRows__(rows){
  try{
    const list = Array.isArray(rows) ? rows : [];
    for (const row of list){
      const sess = __sessionFromUserRow__(row);
      if (sess && !isOperatoreSession(sess)) return sess;
    }
  }catch(_){ }
  return null;
}

async function __forceAdminLoginAfterAuthBackupImport__(rows){
  try{
    let list = Array.isArray(rows) ? rows.slice() : (await __tblGet__("utenti", []));
    if (!Array.isArray(list)) list = [];

    const isAdminRow = (u) => {
      try{
        const raw = String(u?.ruolo || u?.role || u?.tipo || "").trim().toLowerCase();
        if (raw) return !raw.startsWith("op");
        return !(String(u?.isOperatore || u?.is_operatore || "").trim() === "1" || u?.isOperatore === true);
      }catch(_){ return false; }
    };

    let adminRow = list.find(isAdminRow) || null;
    const nowIso = (typeof __nowIso__ === "function") ? __nowIso__() : new Date().toISOString();
    if (!adminRow){
      adminRow = {
        id: (typeof genId === "function") ? genId("u") : ("u_auth_backup_" + Date.now()),
        username: "Amministratore",
        password: "",
        ruolo: "admin",
        createdAt: nowIso
      };
      list.push(adminRow);
    }

    adminRow = Object.assign({}, adminRow, {
      id: String(adminRow.id || adminRow.user_id || adminRow.userId || adminRow.username || ("u_auth_backup_" + Date.now())).trim(),
      username: String(adminRow.username || adminRow.user || adminRow.email || "Amministratore").trim() || "Amministratore",
      ruolo: "admin",
      role: "admin",
      isOperatore: false,
      isMaster: true,
      licenseType: "master",
      licenseStatus: "master",
      licenseStart: String(adminRow.licenseStart || adminRow.license_start || todayISO()),
      licenseEnd: String(adminRow.licenseEnd || adminRow.license_end || "2099-12-31"),
      masterEnabledAt: String(adminRow.masterEnabledAt || nowIso),
      updatedAt: nowIso
    });
    try{ if (!adminRow.licenseRequestCode) adminRow.licenseRequestCode = __licenseRequestCodeForUser__(adminRow); }catch(_){ }

    const adminId = String(adminRow.id || adminRow.user_id || adminRow.userId || adminRow.username || "").trim();
    const adminName = String(adminRow.username || adminRow.user || "").trim().toLowerCase();
    let replaced = false;
    list = list.map((u) => {
      const uid = String(u?.id || u?.user_id || u?.userId || u?.username || "").trim();
      const uname = String(u?.username || u?.user || "").trim().toLowerCase();
      if ((adminId && uid === adminId) || (adminName && uname === adminName)){ replaced = true; return adminRow; }
      return u;
    });
    if (!replaced) list.push(adminRow);
    try{ await __tblSet__("utenti", list); }catch(_){ }

    let sess = __sessionFromUserRow__(adminRow);
    if (!sess){
      sess = {
        user_id: String(adminRow.id || adminRow.user_id || adminRow.userId || adminRow.username || "admin").trim() || "admin",
        username: String(adminRow.username || "Amministratore").trim() || "Amministratore",
        ruolo: "admin",
        name: String(adminRow.name || adminRow.nome || adminRow.username || "Amministratore").trim() || "Amministratore"
      };
    }
    sess = Object.assign({}, sess, {
      ruolo: "admin",
      isMaster: true,
      licenseType: "master",
      licenseStatus: "master",
      licenseStart: String(adminRow.licenseStart || todayISO()),
      licenseEnd: String(adminRow.licenseEnd || "2099-12-31"),
      licenseUnlockCode: String(adminRow.licenseUnlockCode || adminRow.license_unlock_code || ""),
      licenseRequestCode: String(adminRow.licenseRequestCode || "")
    });

    try{ state.session = sess; }catch(_){ }
    try{ saveSession(sess); }catch(_){ }
    try{ __authPendingUser__ = null; __authPendingReason__ = ""; }catch(_){ }
    try{ localStorage.removeItem(__AUTH_BACKUP_FORCE_ADMIN_KEY); }catch(_){ }
    try{ await __kvSet__("auth:lastImportedAccount", { at: nowIso, username: sess.username || "", role: "admin", source: "auth-backup" }); }catch(_){ }
    return sess;
  }catch(_){ return null; }
}

async function __completeForcedAdminLoginAfterAuthBackupImport__(){
  try{
    const flag = String(localStorage.getItem(__AUTH_BACKUP_FORCE_ADMIN_KEY) || "").trim();
    const active = String(localStorage.getItem(__AUTH_BACKUP_IMPORT_ACTIVE_KEY) || "").trim();
    if (!flag && !active) return null;
    const sess = await __forceAdminLoginAfterAuthBackupImport__();
    if (sess && sess.user_id){
      try{ localStorage.removeItem(__AUTH_BACKUP_IMPORT_ACTIVE_KEY); }catch(_){ }
    }
    return sess;
  }catch(_){ return null; }
}

function __mergeUsers__(existing, incoming){
  const out = [];
  const seen = new Set();
  const add = (u) => {
    if (!u || typeof u !== "object") return;
    const id = String(u.id || u.user_id || u.userId || "").trim();
    const un = String(u.username || "").trim().toLowerCase();
    const key = (id ? `id:${id}` : "") + "|" + (un ? `u:${un}` : "");
    if (key === "|" ) return;
    if (seen.has(key)) return;
    seen.add(key);
    out.push(u);
  };
  try{ (Array.isArray(existing)?existing:[]).forEach(add); }catch(_){}
  try{ (Array.isArray(incoming)?incoming:[]).forEach(add); }catch(_){}
  return out;
}

function __pickDbImportFile__(){
  return new Promise((resolve) => {
    try{
      let input = document.getElementById("dbFileInput");
      let created = false;
      if (!input){
        input = document.createElement("input");
        input.type = "file";
        input.accept = "application/json,.json";
        input.id = "dbFileInput";
        input.style.position = "fixed";
        input.style.left = "0";
        input.style.top = "0";
        input.style.width = "1px";
        input.style.height = "1px";
        input.style.opacity = "0";
        input.style.pointerEvents = "none";
        input.style.zIndex = "-1";
        document.body.appendChild(input);
        created = true
      }
      try{ input.setAttribute("accept", "application/json,.json"); }catch(_){ }
      try{ input.style.display = "block"; }catch(_){ }
      try{ input.style.position = "fixed"; input.style.left = "0"; input.style.top = "0"; input.style.width = "1px"; input.style.height = "1px"; input.style.opacity = "0"; input.style.pointerEvents = "none"; input.style.zIndex = "-1"; }catch(_){ }
      try{ input.value = ""; }catch(_){ }
      let settled = false;
      const finish = (file) => {
        if (settled) return;
        settled = true;
        try{ input.onchange = null; }catch(_){ }
        try{ input.oncancel = null; }catch(_){ }
        if (created){ try{ document.body.removeChild(input); }catch(_){ } }
        resolve(file || null);
      };
      input.onchange = () => finish((input.files && input.files[0]) ? input.files[0] : null);
      try{ input.oncancel = () => finish(null); }catch(_){ }
      const openPicker = () => {
        try{
          if (typeof input.showPicker === "function") input.showPicker();
          else input.click();
        }catch(_){
          try{ input.click(); }catch(__){ finish(null); }
        }
      };
      openPicker();
      setTimeout(() => { if (!settled) openPicker(); }, 120);
      setTimeout(() => { if (!settled && (!input.files || !input.files.length)) finish(null); }, 120000);
    }catch(_){ resolve(null); }
  });
}

async function __dbImport__(kind){
  try{
    const label = (String(kind||"").toLowerCase().startsWith("admin")) ? "DB Amministratore" : "DB Operatore";
    const file = await __pickDbImportFile__();

    if (!file){
      try{ toast("Import annullato"); }catch(_){}
      return;
    }

    const __isAuthEntryBackupImport__ = (() => {
      try{
        const pg = String(state?.page || document?.body?.dataset?.page || "").trim().toLowerCase();
        const sess = (typeof loadSession === "function") ? loadSession() : null;
        const topBtn = document.getElementById("authImportBackupTop");
        const topVisible = !!(topBtn && topBtn.hidden !== true && topBtn.getAttribute("aria-hidden") !== "true" && getComputedStyle(topBtn).display !== "none" && getComputedStyle(topBtn).visibility !== "hidden");
        const explicitAuthImport = String(localStorage.getItem(__AUTH_BACKUP_IMPORT_ACTIVE_KEY) || "").trim() === "1";
        return explicitAuthImport || pg === "auth" || topVisible || !(sess && sess.user_id);
      }catch(_){ return false; }
    })();

    if (__isAuthEntryBackupImport__){
      try{ localStorage.setItem(__AUTH_BACKUP_FORCE_ADMIN_KEY, "1"); }catch(_){ }
      try{ localStorage.setItem(__AUTH_BACKUP_IMPORT_ACTIVE_KEY, "1"); }catch(_){ }
    }

    const text = await file.text();
    let data = null;
    try{ data = JSON.parse(text); }catch(e){
      try{ toast("JSON non valido", "orange"); }catch(_){}
      return;
    }

    const kindOk = String(data?.kind || "").trim() === __DB_EXPORT_KIND__;
    const sv = data?.schemaVersion;
    const svNum = (typeof sv === "number") ? sv : parseInt(String(sv||"").trim(), 10);
    const hasDatasets = data && typeof data === "object" && data.datasets && typeof data.datasets === "object";

    if (!kindOk || !hasDatasets || !(svNum >= 1)){
      try{ toast("File non compatibile", "orange"); }catch(_){}
      return;
    }


    const allowedTables = new Set(__dbTablesForKind__(kind));
    const ds = data.datasets || {};
    const tablesToWrite = Object.keys(ds).filter(t => allowedTables.has(t));
    const backupLocalStorage = (data && typeof data.localStorage === "object" && data.localStorage)
      ? data.localStorage
      : ((data?.meta && typeof data.meta.localStorage === "object") ? data.meta.localStorage : null);
    const backupThemeSlots = (data?.meta && typeof data.meta.themeSlots === "object" && data.meta.themeSlots)
      ? data.meta.themeSlots
      : null;

    if (!tablesToWrite.length){
      try{ toast("Nessun dataset da importare", "orange"); }catch(_){}
      return;
    }

    // Precompute auth-page imported session BEFORE writing datasets so the backup
    // gets stored in the correct account context instead of the anonymous login context.
    const __isAuthPageImport__ = __isAuthEntryBackupImport__ || String(state?.page || "").trim() === "auth";
    const __isAdminImport__ = String(kind||"").toLowerCase().startsWith("admin");
    let __authImportedSession__ = null;
    let __forcedAdminSessionAfterBackup__ = null;

    // Import da schermata login/primo accesso: prima di scrivere i dataset
    // inizializza una sessione amministratore reale, così tutte le tabelle
    // non globali vengono salvate direttamente nel contesto admin e non
    // nel contesto anonimo della schermata di login.
    try{
      if (__isAuthEntryBackupImport__){
        __forcedAdminSessionAfterBackup__ = await __forceAdminLoginAfterAuthBackupImport__(Array.isArray(ds?.utenti) ? ds.utenti : null);
        if (__forcedAdminSessionAfterBackup__){
          __authImportedSession__ = __forcedAdminSessionAfterBackup__;
          try{ state.session = __forcedAdminSessionAfterBackup__; }catch(_){ }
          try{ saveSession(__forcedAdminSessionAfterBackup__); }catch(_){ }
        }
      }
    }catch(_){ }

    try{
      const importedUsers = Array.isArray(ds?.utenti) ? ds.utenti : [];
      if (__isAuthPageImport__ && importedUsers.length){
        const preferred = __isAdminImport__
          ? (importedUsers.find(u => !isOperatoreSession(u)) || importedUsers[0])
          : (importedUsers.find(u => isOperatoreSession(u)) || importedUsers[0]);
        __authImportedSession__ = __sessionFromUserRow__(preferred);
        if (__authImportedSession__){
          try{ state.session = __authImportedSession__; }catch(_){ }
          try{ saveSession(__authImportedSession__); }catch(_){ }
          try{ await __kvSet__("auth:lastImportedAccount", { at: __nowIso__(), username: __authImportedSession__.username || "", role: __authImportedSession__.ruolo || (__isAdminImport__ ? "admin" : "operatore") }); }catch(_){ }
        }
      }
    }catch(_){ }

    // Pre-merge "utenti" for operator import to avoid losing existing accounts
    try{
      if (!__isAdminImport__ && Array.isArray(ds?.utenti)){
        const existingUsers = await __tblGet__("utenti", []);
        ds.utenti = __mergeUsers__(existingUsers, ds.utenti);
      }
    }catch(_){ }

    // Write datasets (only allowed tables)
    for (const t of tablesToWrite){
      const v = ds[t];
      await __tblSet__(t, v);
    }

    // Ensure missing tables exist as empty (prevents UI from seeing old leftovers)
    for (const t of allowedTables){
      if (!(t in ds)){
        // non cancellare utenti/impostazioni se non presenti? per sicurezza admin/operator:
        if (t === "utenti" || t === "impostazioni") continue;
        await __tblSet__(t, []);
      }
    }

    
    // IMPORT_SESSION_RECONCILE: solo per import ADMIN (l'import operatore non deve forzare logout)
    try{
      const __isAdminImport__ = String(kind||"").toLowerCase().startsWith("admin");
      if (__isAdminImport__){
        const importedUsers = Array.isArray(ds?.utenti) ? ds.utenti : null;
        if (importedUsers){
          const sess = loadSession();
          if (sess && sess.user_id){
            const stillExists = importedUsers.some(u => String(u?.id||"") === String(sess.user_id)) || importedUsers.some(u => String(u?.username||"").trim() === String(sess?.username||"").trim());
            if (!stillExists){
              clearSession();
            }
          }
        }
      }
    }catch(_){}
    // Login import from auth page: sessione già predisposta prima della scrittura dei dataset.
    // Manteniamo qui solo un fallback difensivo per backup legacy o sessioni incomplete.
    try{
      const isAuthPage = __isAuthEntryBackupImport__ || String(state?.page || "").trim() === "auth";
      const importedUsers = Array.isArray(ds?.utenti) ? ds.utenti : [];
      if (isAuthPage && !__authImportedSession__ && importedUsers.length){
        const preferred = __isAdminImport__
          ? (importedUsers.find(u => !isOperatoreSession(u)) || importedUsers[0])
          : (importedUsers.find(u => isOperatoreSession(u)) || importedUsers[0]);
        const importedSession = __sessionFromUserRow__(preferred);
        if (importedSession){
          try{ state.session = importedSession; }catch(_){ }
          try{ saveSession(importedSession); }catch(_){ }
          try{ await __kvSet__("auth:lastImportedAccount", { at: __nowIso__(), username: importedSession.username || "", role: importedSession.ruolo || (__isAdminImport__ ? "admin" : "operatore") }); }catch(_){ }
        }
      }
    }catch(_){ }

    try{ __restoreBackupLocalStorage__(backupLocalStorage); }catch(_){ }
    if (__isAuthEntryBackupImport__){
      try{ localStorage.setItem(__AUTH_BACKUP_FORCE_ADMIN_KEY, "1"); }catch(_){ }
      try{
        __forcedAdminSessionAfterBackup__ = await __forceAdminLoginAfterAuthBackupImport__(Array.isArray(ds?.utenti) ? ds.utenti : null);
      }catch(_){ }
    }
    try{ __restoreBackupThemeSlots__(backupThemeSlots); }catch(_){ }
    try{ __setupSingleActionButtonPaletteBindings__(); }catch(_){ }

    try{
      const __importedUsersForAdminLogin__ = Array.isArray(ds?.utenti) ? ds.utenti : (await __tblGet__("utenti", []));
      if (__isAuthEntryBackupImport__){
        __forcedAdminSessionAfterBackup__ = await __forceAdminLoginAfterAuthBackupImport__(__importedUsersForAdminLogin__);
      } else {
        __forcedAdminSessionAfterBackup__ = __adminSessionFromUserRows__(__importedUsersForAdminLogin__);
        if (__forcedAdminSessionAfterBackup__){
          try{ state.session = __forcedAdminSessionAfterBackup__; }catch(_){ }
          try{ saveSession(__forcedAdminSessionAfterBackup__); }catch(_){ }
          try{ await __kvSet__("auth:lastImportedAccount", { at: __nowIso__(), username: __forcedAdminSessionAfterBackup__.username || "", role: "admin" }); }catch(_){ }
        }
      }
      const __resolvedImportedSession__ = __forcedAdminSessionAfterBackup__ || __authImportedSession__ || loadSession();
      if (__resolvedImportedSession__){
        try{ state.session = __resolvedImportedSession__; }catch(_){ }
        try{ saveSession(__resolvedImportedSession__); }catch(_){ }
      }
      const __importedYear__ = String((backupLocalStorage && backupLocalStorage['dDAE_exerciseYear']) || '').trim();
      if (__importedYear__ && /^\d{4}$/.test(__importedYear__)){
        try{ saveExerciseYear(__importedYear__); }catch(_){ }
        try{ state.exerciseYear = __importedYear__; }catch(_){ }
      }
    }catch(_){ }

// Mark last import
    await __kvSet__(`db:lastImport:${String(kind||"")}`, { at: __nowIso__(), fileName: file.name || "" });

    let __isAuthAutoLogin__ = false;
    try{
      if (__isAuthEntryBackupImport__){
        try{ await __forceAdminLoginAfterAuthBackupImport__(Array.isArray(ds?.utenti) ? ds.utenti : null); }catch(_){ }
      }
      __isAuthAutoLogin__ = (__isAuthEntryBackupImport__ || String(state?.page || "").trim() === "auth") && !!loadSession();
      toast(__isAuthAutoLogin__ ? "Backup importato: account creato e accesso eseguito" : `${label}: import completato`, "blue");
    }catch(_){ }

    if (__isAuthAutoLogin__){
      setTimeout(async ()=>{
        try{
          if (__isAuthEntryBackupImport__){
            try{ localStorage.setItem(__AUTH_BACKUP_FORCE_ADMIN_KEY, "1"); }catch(_){ }
            try{ localStorage.setItem(__AUTH_BACKUP_IMPORT_ACTIVE_KEY, "1"); }catch(_){ }
            try{ await __forceAdminLoginAfterAuthBackupImport__(Array.isArray(ds?.utenti) ? ds.utenti : null); }catch(_){ }
          }
          state.session = loadSession() || state.session || null;
          state.exerciseYear = loadExerciseYear();
        }catch(_){ }
        try{ updateYearPill(); }catch(_){ }
        try{ __applyContext__({ force:true }); }catch(_){ }
        try{ applyRoleMode(); }catch(_){ }
  try{ __hydrateAppLanguageFromSettings__(); }catch(_){ }
        try{
          const __targetAfterImport__ = __isAuthEntryBackupImport__ ? "home" : ((state.session && isOperatoreSession(state.session)) ? "pulizie" : "home");
          __writeRestoreState({ page: __targetAfterImport__ });
          if (__isAuthEntryBackupImport__ && state.session && state.session.user_id){
            try{ document.body.dataset.page = "home"; }catch(_){ }
            try{ showPage("home"); }catch(_){ }
            try{ refreshAllDataInBackground(); }catch(_){ }
          }
        }catch(_){ }
        try{ localStorage.removeItem(__AUTH_BACKUP_IMPORT_ACTIVE_KEY); }catch(_){ }
        try{ location.reload(); }catch(__){
          try{ showPage((state.session && isOperatoreSession(state.session)) ? "pulizie" : "home"); }catch(___){}
        }
      }, 150);
      return;
    }

    setTimeout(()=>{
      try{ __writeRestoreState(__forcedAdminSessionAfterBackup__ ? { page: "home" } : __captureUiState()); }catch(_){ }
      try{ location.reload(); }catch(_){ }
    }, 400);

  }catch(e){
    try{ toast("Errore import", "orange"); }catch(_){}
  }
}


async function __extractRosterNames__(data){
  try{
    if (!data || typeof data !== "object") return [];
    // direct arrays
    if (Array.isArray(data.operatori)) return data.operatori.map(x=>String(x||"").trim()).filter(Boolean).slice(0,3);
    if (Array.isArray(data.operators)) return data.operators.map(x=>String(x||"").trim()).filter(Boolean).slice(0,3);
    if (Array.isArray(data.roster)) return data.roster.map(x=>String(x||"").trim()).filter(Boolean).slice(0,3);

    // nested: { operatori: {operatore_1,...}}
    if (data.operatori && typeof data.operatori === "object" && !Array.isArray(data.operatori)){
      const o=data.operatori;
      const arr=[o.operatore_1,o.operatore_2,o.operatore_3].map(x=>String(x||"").trim()).filter(Boolean);
      if (arr.length) return arr.slice(0,3);
    }

    // db export: datasets.impostazioni row key=operatori
    const ds = data.datasets || {};
    const imp = ds.impostazioni;
    if (Array.isArray(imp)){
      const row = imp.find(r => String(r?.key || r?.Key || "").trim().toLowerCase() === "operatori");
      if (row){
        const arr=[row.operatore_1,row.operatore_2,row.operatore_3,row.Operatore_1,row.Operatore_2,row.Operatore_3].map(x=>String(x||"").trim()).filter(Boolean);
        // arr contains duplicates; keep first 3 unique in order
        const out=[];
        for (const n of arr){ if (!out.includes(n)) out.push(n); if (out.length>=3) break; }
        if (out.length) return out;
      }
    }

    // settings-like: {operatore_1,...}
    const arr=[data.operatore_1,data.operatore_2,data.operatore_3,data.Operatore_1,data.Operatore_2,data.Operatore_3].map(x=>String(x||"").trim()).filter(Boolean);
    if (arr.length){
      const out=[];
      for (const n of arr){ if (!out.includes(n)) out.push(n); if (out.length>=3) break; }
      return out;
    }
  }catch(_){ }
  return [];
}

async function __importRosterOperators__(){
  const input = document.createElement("input");
  input.type = "file";
  input.accept = "application/json,.json";
  input.style.position = "fixed";
  input.style.left = "-9999px";
  document.body.appendChild(input);

  const file = await new Promise((resolve)=>{
    input.onchange = () => resolve((input.files && input.files[0]) ? input.files[0] : null);
    input.click();
  });

  try{ document.body.removeChild(input); }catch(_){}

  if (!file) return;

  let data = null;
  try{
    const txt = await file.text();
    data = JSON.parse(txt);
  }catch(_){
    try{ toast("File non compatibile", "orange"); }catch(__){}
    return;
  }

  const names = await __extractRosterNames__(data);
  if (!names || !names.length){
    try{ toast("Roster vuoto o non valido", "orange"); }catch(_){}
    return;
  }

  // salva nella tabella impostazioni (row key=operatori)
  try{
    await api("impostazioni", { method:"POST", body:{ operatori: names }, showLoader:true });
    try{ await ensureSettingsLoaded({ force:true, showLoader:false }); }catch(_){}
    try{ toast("Roster operatori importato", "green"); }catch(_){}
    // refresh UI (pulizie ecc.)
    try{ renderSettingsOperatorsNames?.(); }catch(_){}
    try{ if (state.page === "pulizie") renderPuliziePage?.(); }catch(_){}
  }catch(e){
    try{ toast("Errore import roster", "orange"); }catch(_){}
  }
}

async function __exportRosterOperators__(){
  try{
    await ensureSettingsLoaded({ force:false, showLoader:true });
    const names = (getOperatorNamesFromSettings ? getOperatorNamesFromSettings() : []).map(x=>String(x||"").trim()).filter(Boolean);
    if (!names.length){
      try{ toast("Nessun operatore impostato", "orange"); }catch(_){}
      return;
    }

    const payload = {
      kind: "DDAE_ROSTER_OPERATORS",
      schemaVersion: 1,
      exportedAt: __nowIso__(),
      operatori: names
    };

    const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    const ts = new Date();
    const y = ts.getFullYear();
    const m = String(ts.getMonth()+1).padStart(2,"0");
    const d = String(ts.getDate()).padStart(2,"0");
    a.href = url;
    a.download = __safeFileName__(`dDAE_Roster_Operatori_${y}${m}${d}_${BUILD_VERSION}.json`);
    document.body.appendChild(a);
    a.click();
    setTimeout(()=>{ try{ URL.revokeObjectURL(url); }catch(_){}
      try{ document.body.removeChild(a); }catch(_){}
    }, 0);

    try{ toast("Roster operatori: export pronto", "blue"); }catch(_){}
  }catch(e){
    try{ toast("Errore export roster", "orange"); }catch(_){}
  }
}


async function __dbExport__(kind, preopenWin){
  try{
    const label = (String(kind||"").toLowerCase().startsWith("admin")) ? "DB Amministratore" : "DB Operatore";
    const tables = __dbTablesForKind__(kind);
    const datasets = {};
    for (const t of tables){
      datasets[t] = await __tblGet__(t, (t==="impostazioni" ? {} : []));
    }
    try{
      const activeNames = new Set((typeof getActiveOperatorNames === 'function' ? getActiveOperatorNames() : []).map((name) => String(name || '').trim().toLowerCase()).filter(Boolean));
      if (Array.isArray(datasets.operatori)){
        datasets.operatori = datasets.operatori.filter((row) => activeNames.has(String(getCanonicalActiveOperatorName(row?.operatore || row?.nome || '') || '').trim().toLowerCase()));
      }
    }catch(_){ }
    const backupLocalStorage = __collectBackupLocalStorage__();
    const backupThemeSlots = __collectBackupThemeSlots__();
    const payload = {
      kind: __DB_EXPORT_KIND__,
      schemaVersion: __DB_SCHEMA_VERSION__,
      exportedAt: __nowIso__(),
      datasets,
      localStorage: backupLocalStorage,
      meta: {
        localStorage: backupLocalStorage,
        themeSlots: backupThemeSlots
      }
    };

    const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
    const url = URL.createObjectURL(blob);
    // Nome file export: dd-mm-yyyy_nome_account.json
    const dtObj = new Date();
    const dd = String(dtObj.getDate()).padStart(2, "0");
    const mm = String(dtObj.getMonth() + 1).padStart(2, "0");
    const yyyy = String(dtObj.getFullYear());
    const dt = `${dd}-${mm}-${yyyy}`;
    let accountName = "";
    try{
      accountName =
        String(
          state?.session?.account_name ||
          state?.session?.accountName ||
          state?.session?.nome_account ||
          state?.session?.nomeAccount ||
          state?.session?.name ||
          state?.session?.username ||
          ""
        ).trim();
    }catch(_){ accountName = ""; }
    if (!accountName){
      try{
        accountName = String(
          payload?.meta?.account_name ||
          payload?.meta?.accountName ||
          ""
        ).trim();
      }catch(_){ accountName = ""; }
    }
    const accountTag = __safeFileName__(accountName || "nome_account");
    const filename = `${dt}_${accountTag}.json`;

    let usedPreopenDownload = false;

    // iOS/Safari: se abbiamo una finestra aperta nel gesto utente, usiamo solo quella
    // per evitare un secondo popup/secondo tentativo di download dopo il salvataggio.
    if (preopenWin && typeof preopenWin === "object"){
      try{
        const doc = preopenWin.document;
        doc.open();
        doc.write(`<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"></head><body style="font-family:-apple-system,system-ui;padding:16px">
          <a id="dl" download="${filename}" href="${url}" style="display:inline-block;padding:12px 14px;border:1px solid #ccc;border-radius:10px;text-decoration:none">Download</a>
          <script>
            (function(){
              var a=document.getElementById('dl');
              try{ a.click(); }catch(e){}
              setTimeout(function(){ try{ window.close(); }catch(e){} }, 600);
            })();
          </script>
        </body></html>`);
        doc.close();
        usedPreopenDownload = true;
        setTimeout(()=>{ try{ URL.revokeObjectURL(url); }catch(_){ } }, 2000);
        try{ toast("Backup creato", "green"); }catch(_){ }
      }catch(_){ usedPreopenDownload = false; }
    }

    if (!usedPreopenDownload){
      // Fallback: se non stiamo usando la finestra pre-aperta, chiediamo conferma finale
      // e avviamo il download solo sul tap "Salva".
      let doSave = true;
      try{
        const choice = await __confirmTwoActions__(`${label}: backup pronto`, "Salva", "Chiudi");
        doSave = (choice === "yes");
      }catch(_){ doSave = true; }

      if (doSave){
        const a = document.createElement("a");
        a.href = url;
        a.download = filename;
        document.body.appendChild(a);
        try{ a.click(); }catch(_){ }
        setTimeout(()=>{
          try{ document.body.removeChild(a); }catch(_){ }
          try{ URL.revokeObjectURL(url); }catch(_){ }
        }, 800);
        try{ toast("Backup creato", "green"); }catch(_){ }
      } else {
        try{ URL.revokeObjectURL(url); }catch(_){ }
      }
    }

  }catch(e){
    try{ toast("Errore export", "orange"); }catch(_){}
  }
}
// ===== /DB Import/Export (LOCAL) =====


// Utility: parse importi (usato anche in guest list)
function money(v){
  if (v === null || v === undefined) return 0;
  if (typeof v === 'number') return isFinite(v) ? v : 0;
  if (typeof v === 'string'){
    const s = v.trim().replace(/\./g,'').replace(',', '.');
    const n = parseFloat(s);
    return isFinite(n) ? n : 0;
  }
  const n = Number(v);
  return isFinite(n) ? n : 0;
}

/* Audio SFX (iOS-friendly, no assets) */
const AUDIO_PREF_KEY = "ddae_audio_enabled";
let __audioEnabled = false;
let __audioCtx = null;
let __lastTapSfxAt = 0;

function __loadAudioPref(){
  try{
    const stored = localStorage.getItem(AUDIO_PREF_KEY);
    __audioEnabled = (stored === null) ? true : (stored === "1");
  }catch(_){ __audioEnabled = true; }
}
function __setAudioPref(v){
  __audioEnabled = !!v;
  try{ localStorage.setItem(AUDIO_PREF_KEY, __audioEnabled ? "1" : "0"); }catch(_){}
  try{
    ["audioToggle", "opAudioToggle"].forEach((id) => {
      const t = document.getElementById(id);
      if (t) t.checked = __audioEnabled;
    });
  }catch(_){}
}
function __ensureAudioCtx(){
  try{
    const AC = window.AudioContext || window.webkitAudioContext;
    if (!AC) return null;
    if (!__audioCtx) __audioCtx = new AC();
    if (__audioCtx && __audioCtx.state === "suspended"){
      __audioCtx.resume().catch(()=>{});
    }
    return __audioCtx;
  }catch(_){ return null; }
}
function __sfxTap(){
  if (!__audioEnabled) return;
  const ctx = __ensureAudioCtx();
  if (!ctx) return;
  const __now = (typeof performance !== "undefined" && performance.now) ? performance.now() : Date.now();
  if (__now - __lastTapSfxAt < 70) return;
  __lastTapSfxAt = __now;
  try{
    const t = ctx.currentTime;
    const o = ctx.createOscillator();
    const g = ctx.createGain();
    o.type = "sine";
    o.frequency.setValueAtTime(1200, t);
    o.frequency.exponentialRampToValueAtTime(900, t + 0.03);

    g.gain.setValueAtTime(0.0001, t);
    g.gain.exponentialRampToValueAtTime(0.03, t + 0.004);
    g.gain.exponentialRampToValueAtTime(0.0001, t + 0.05);

    o.connect(g);
    g.connect(ctx.destination);
    o.start(t);
    o.stop(t + 0.06);
  }catch(_){} 
}
function __sfxGlass(){
  // "Carta stropicciata" (usato per cancellazioni/long-press)
  if (!__audioEnabled) return;
  const ctx = __ensureAudioCtx();
  if (!ctx) return;
  try{
    const sr = ctx.sampleRate || 44100;
    const dur = 0.28;
    const buf = ctx.createBuffer(1, Math.max(1, (sr * dur)|0), sr);
    const ch = buf.getChannelData(0);

    for (let i=0;i<ch.length;i++){
      const x = i / ch.length;
      const env = Math.pow(1 - x, 1.8);
      const flutter = (Math.random() < 0.18 ? 1.0 : 0.35);
      ch[i] = (Math.random()*2 - 1) * env * flutter;
    }

    const src = ctx.createBufferSource();
    src.buffer = buf;

    const hp = ctx.createBiquadFilter();
    hp.type = "highpass";
    hp.frequency.setValueAtTime(80, ctx.currentTime);

    const lp = ctx.createBiquadFilter();
    lp.type = "lowpass";
    lp.frequency.setValueAtTime(1200, ctx.currentTime);

    const bp = ctx.createBiquadFilter();
    bp.type = "bandpass";
    bp.frequency.setValueAtTime(420, ctx.currentTime);
    bp.Q.setValueAtTime(0.8, ctx.currentTime);

    const g = ctx.createGain();
    const t = ctx.currentTime;
    g.gain.setValueAtTime(0.0001, t);
    g.gain.exponentialRampToValueAtTime(0.11, t + 0.01);
    g.gain.exponentialRampToValueAtTime(0.0001, t + dur);

    src.connect(hp);
    hp.connect(lp);
    lp.connect(bp);
    bp.connect(g);
    g.connect(ctx.destination);

    src.start(t);
    src.stop(t + dur);
  }catch(_){} 
}
function __sfxSave(){
  if (!__audioEnabled) return;
  const ctx = __ensureAudioCtx();
  if (!ctx) return;
  try{
    const t = ctx.currentTime;
    const o1 = ctx.createOscillator();
    const o2 = ctx.createOscillator();
    const g = ctx.createGain();

    o1.type = "sine";
    o2.type = "triangle";
    o1.frequency.setValueAtTime(180, t);
    o2.frequency.setValueAtTime(90, t);

    g.gain.setValueAtTime(0.0001, t);
    g.gain.exponentialRampToValueAtTime(0.18, t + 0.03);
    g.gain.exponentialRampToValueAtTime(0.0001, t + 0.55);

    o1.connect(g);
    o2.connect(g);
    g.connect(ctx.destination);

    o1.start(t);
    o2.start(t);
    o1.stop(t + 0.6);
    o2.stop(t + 0.6);
  }catch(_){}
}

function setupAudioUI(){
  __loadAudioPref();

  // Aggancia toggle audio in Impostazioni admin + operatore
  try{
    ["audioToggle", "opAudioToggle"].forEach((id) => {
      const t = document.getElementById(id);
      if (!t || t.dataset.audioBound === "1") return;
      t.dataset.audioBound = "1";
      t.checked = __audioEnabled;
      t.addEventListener("change", () => {
        __ensureAudioCtx(); // unlock su gesto utente
        __setAudioPref(!!t.checked);
        if (__audioEnabled) __sfxTap();
      }, { passive:true });
    });
  }catch(_){}

  // iOS: sblocca/resume AudioContext sul primo gesto (anche senza suono)
  const unlock = () => { try{ if (__audioEnabled) __ensureAudioCtx(); }catch(_){ } };
  try{ document.addEventListener("pointerdown", unlock, { capture:true, passive:true }); }catch(_){}
  try{ document.addEventListener("touchstart", unlock, { capture:true, passive:true }); }catch(_){}

  // Tap SFX: click su bottoni/icone e focus su input
  try{
    document.addEventListener("click", (e) => {
      if (!__audioEnabled) return;
      try{
        const t = e.target;
        if (!t) return;
        if (t.closest && t.closest("button, a, .icon-btn, [role='button']")) { __sfxTap(); return; }
        if (t.closest && t.closest("input, textarea, select, label")) { __sfxTap(); return; }
      }catch(_){}
    }, true);
  }catch(_){}

  try{
    let __lastFocusTapAt = 0;
    document.addEventListener("focusin", (e) => {
      if (!__audioEnabled) return;
      const now = Date.now();
      if (now - __lastFocusTapAt < 120) return;
      __lastFocusTapAt = now;
      try{
        const t = e.target;
        if (t && t.matches && t.matches("input, textarea, select")) __sfxTap();
      }catch(_){}
    }, true);
  }catch(_){}
}


// Ruoli: "user" (default) | "operatore"
function isOperatoreSession(sess){
  try{ return String(sess?.ruolo || "").trim().toLowerCase() === "operatore"; }
  catch(_){ return false; }
}

function __fitHomeSyncBtn__(){
  try{
    const bar = document.getElementById("homeSyncBar");
    const btn = document.getElementById("goDbSync");
    if (!bar || !btn) return;

    // Layout vincolato: linea sopra + tasto SYNC sotto (stack verticale).
    // Resetta eventuali override inline che potevano mettere gli elementi "affiancati".
    try{
      bar.style.display = "";
      bar.style.alignItems = "";
      bar.style.justifyContent = "";
    }catch(_){}

    try{
      btn.style.width = "";
      btn.style.marginLeft = "";
      btn.style.marginRight = "";
    }catch(_){}
  }catch(_){}
}

function applyRoleMode(){
  const isOp = !!(state && state.session && isOperatoreSession(state.session));
  const isMaster = __isMasterSession__(state && state.session ? state.session : null);
  try{ const wrap = document.getElementById("settingsMasterWrap"); if (wrap){ wrap.hidden = !(!!(state && state.session && !isOp)); try{ wrap.style.display = wrap.hidden ? "none" : ""; }catch(_){ } } }catch(_){ }
  try{ document.body.dataset.role = isOp ? "operatore" : "user"; }catch(_){ }
  try{
    const shoppingBtn = document.getElementById("goProdotti");
    if (shoppingBtn){
      shoppingBtn.setAttribute("aria-label", (typeof __translateText__ === "function") ? __translateText__("Lista spesa") : "Lista spesa");
      const shoppingLbl = shoppingBtn.querySelector(".home-main-label");
      if (shoppingLbl){
        shoppingLbl.textContent = (typeof __translateText__ === "function") ? __translateText__("Lista spesa") : "Lista spesa";
        shoppingLbl.classList.toggle("small", true);
      }
    }
  }catch(_){ }

  // Home: SYNC Firebase — tasto unico (Admin sempre; Operatore solo dopo collegamento Roster)
  try{
    const impTile = document.getElementById("goDbImport");
    const expTile = document.getElementById("goDbExport");
    if (impTile){ try{ impTile.hidden = true; impTile.style.display = "none"; }catch(_){ } }
    if (expTile){ try{ expTile.hidden = true; expTile.style.display = "none"; }catch(_){ } }
    const row = document.getElementById("operatorDbRow");
    if (row) row.hidden = true;

    const bar = document.getElementById("homeSyncBar");
    if (bar){
      try{ bar.hidden = false; bar.style.display = ""; }catch(_){ }
    }
  }catch(_){ }
// HOME operatore: mostra solo Pulizie / Lavanderia / Calendario / Spesa in griglia 2x2
  if (isOp){
    const hideIds = [
      "goOspite",
      "goTassaSoggiorno",
      "goStatistiche",
      "goOrePuliziaHome",
            // icone/shortcuts ospiti duplicati (se presenti)
      "goOspiti",
    ];
    hideIds.forEach((id)=>{
      const el = document.getElementById(id);
      if (!el) return;
      try{ el.hidden = true; }catch(_){ }
      try{ el.style.display = "none"; }catch(_){ }
    });


    // Impostazioni: per Operatore non mostrare il popup Database locale (solo Admin)
    try{
      const sdb = document.getElementById("settingsDbBtn");
      if (sdb){ sdb.hidden = true; try{ sdb.style.display = "none"; }catch(_){ } }
    }catch(_){}
    // Header tools: nascondi tools non consentiti
    try{ const ospitiTopTools = document.getElementById("ospitiTopTools"); if (ospitiTopTools) ospitiTopTools.hidden = true; }catch(_){ }
    try{ const speseTopTools = document.getElementById("speseTopTools"); if (speseTopTools) speseTopTools.hidden = true; }catch(_){ }
    try{ const statGenTopTools = document.getElementById("statGenTopTools"); if (statGenTopTools) statGenTopTools.hidden = true; }catch(_){ }
    try{ const statMensiliTopTools = document.getElementById("statMensiliTopTools"); if (statMensiliTopTools) statMensiliTopTools.hidden = true; }catch(_){ }
    try{ const statSpeseTopTools = document.getElementById("statSpeseTopTools"); if (statSpeseTopTools) statSpeseTopTools.hidden = true; }catch(_){ }
    try{ const statPrenTopTools = document.getElementById("statPrenTopTools"); if (statPrenTopTools) statPrenTopTools.hidden = true; }catch(_){ }
    try{ const statChannelTopTools = document.getElementById("statChannelTopTools"); if (statChannelTopTools) statChannelTopTools.hidden = true; }catch(_){ }
    try{ const statPulizieTopTools = document.getElementById("statPulizieTopTools"); if (statPulizieTopTools) statPulizieTopTools.hidden = true; }catch(_){ }
    try{ const orePuliziaTopTools = document.getElementById("orePuliziaTopTools"); if (orePuliziaTopTools) orePuliziaTopTools.hidden = true; }catch(_){ }
    try{ const statCancTopTools = document.getElementById("statCancTopTools"); if (statCancTopTools) statCancTopTools.hidden = true; }catch(_){ }
  }
}


function __parseBuildVersion(v){
  try{
    const raw = String(v||'').trim();
    const m = raw.match(/(?:dDAE_)?(\d+)\.(\d+)/i);
    if(!m) return null;
    return {maj:Number(m[1]), min:Number(m[2])};
  }catch(_){ return null; }
}
function __isRemoteNewer(remote, local){
  const r = __parseBuildVersion(remote);
  const l = __parseBuildVersion(local);
  if(!r || !l) return String(remote).trim() !== String(local).trim();
  if(r.maj !== l.maj) return r.maj > l.maj;
  return r.min > l.min;
}

// =========================
// AUTH + SESSION (dDAE_1.020)
// =========================

const __SESSION_KEY = "dDAE_session_v2";
const __AUTH_BACKUP_FORCE_ADMIN_KEY = "__ddae_auth_backup_force_admin_login_v1";
const __AUTH_BACKUP_IMPORT_ACTIVE_KEY = "__ddae_auth_backup_import_active_v1";
const __YEAR_KEY = "dDAE_exerciseYear";

function loadSession(){
  try{
    const raw = localStorage.getItem(__SESSION_KEY);
    if (!raw) return null;
    const s = JSON.parse(raw);
    if (!s || !s.user_id) return null;
    return s;
  } catch(_){ return null; }
}

function saveSession(session){
  try{ localStorage.setItem(__SESSION_KEY, JSON.stringify(session || null)); } catch(_){ }
}

function clearSession(){
  try{ localStorage.removeItem(__SESSION_KEY); } catch(_){ }
}

function loadExerciseYear(){
  try{
    const v = String(localStorage.getItem(__YEAR_KEY) || "").trim();
    const n = Number(v);
    if (isFinite(n) && n >= 2000 && n <= 2100) return String(n);
  } catch(_){ }
  return String(new Date().getFullYear());
}

function saveExerciseYear(year){
  try{ localStorage.setItem(__YEAR_KEY, String(year || "")); } catch(_){ }
}

function __applyExerciseYearChange__(nextYear){
  const y = String(nextYear || "").trim();
  if (!y) return false;
  state.exerciseYear = y;
  saveExerciseYear(state.exerciseYear);
  updateYearPill();
  try{ __applyContext__({ force:true }); }catch(_){ }
  try{
    if (y){
      setPresetValue("ytd");
      setPeriod(`${y}-01-01`, `${y}-12-31`);
    }
  }catch(_){ }
  invalidateApiCache();
  try{ refreshHome(); }catch(_){ }
  try{ if (state.page==="spese") ensurePeriodData({showLoader:true,force:true}).then(()=>{ try{ renderSpese(); }catch(_){ } }); }catch(_){ }
  try{ if (state.page==="ospiti") ensureGuestsForPeriod(true); }catch(_){ }
  try{ if (state.page==="statistiche") loadStatistichePage({ force:true }); }catch(_){ }
  try{ if (state.page==="pulizie") loadPuliziePage(); }catch(_){ }
  try{ if (state.page==="lavanderia") loadLavanderiaPage(); }catch(_){ }
  try{ if (state.page==="calendario") renderCalendar(); }catch(_){ }
  try{ updateSettingsAccountName(); }catch(_){ }
  return true;
}

const __SETTINGS_YEAR_WHEEL_ROW__ = 60;
const __SETTINGS_YEAR_WHEEL_PAD_ROWS__ = 2;
let __settingsYearWheelTimer__ = null;
let __settingsYearWheelApplying__ = false;

function __getSettingsYearValues__(){
  const years = [];
  for (let y = 2100; y >= 2000; y -= 1) years.push(String(y));
  return years;
}

function __populateSettingsYearPicker__(selectedYear){
  const wheel = document.getElementById("settingsYearWheel");
  if (!wheel) return;
  const years = __getSettingsYearValues__();
  const selected = String(selectedYear || state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  const pad = '<div class="settings-year-wheel-spacer" aria-hidden="true"></div>'.repeat(__SETTINGS_YEAR_WHEEL_PAD_ROWS__);
  wheel.innerHTML = pad + years.map((year) => `<div class="settings-year-wheel-item${year === selected ? ' is-selected' : ''}" data-year="${year}" role="option" aria-selected="${year === selected ? 'true' : 'false'}">${year}</div>`).join("") + pad;
}

function __highlightSettingsYearWheel__(year){
  const wheel = document.getElementById("settingsYearWheel");
  if (!wheel) return;
  const target = String(year || "").trim();
  wheel.querySelectorAll('.settings-year-wheel-item').forEach((item) => {
    const on = item.getAttribute('data-year') === target;
    item.classList.toggle('is-selected', on);
    item.setAttribute('aria-selected', on ? 'true' : 'false');
  });
}

function __getSettingsYearWheelValue__(){
  const wheel = document.getElementById("settingsYearWheel");
  if (!wheel) return String(state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  const years = __getSettingsYearValues__();
  const rawIndex = Math.round((wheel.scrollTop || 0) / __SETTINGS_YEAR_WHEEL_ROW__);
  const index = Math.max(0, Math.min(years.length - 1, rawIndex));
  return years[index] || years[0];
}

function __scrollSettingsYearWheelTo__(year, behavior){
  const wheel = document.getElementById("settingsYearWheel");
  if (!wheel) return;
  const years = __getSettingsYearValues__();
  const target = String(year || state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  const index = Math.max(0, years.indexOf(target));
  const top = index * __SETTINGS_YEAR_WHEEL_ROW__;
  try{ wheel.scrollTo({ top, behavior: behavior || 'auto' }); }
  catch(_){ wheel.scrollTop = top; }
  __highlightSettingsYearWheel__(target);
}

function __applySettingsYearWheelSelection__(opts){
  if (__settingsYearWheelApplying__) return;
  __settingsYearWheelApplying__ = true;
  const next = __getSettingsYearWheelValue__();
  const current = String(state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  __highlightSettingsYearWheel__(next);
  __scrollSettingsYearWheelTo__(next, (opts && opts.snapOnly) ? 'auto' : 'smooth');
  if (next && next !== current){
    try{ __applyExerciseYearChange__(String(next)); }catch(_){ }
  }
  requestAnimationFrame(() => { __settingsYearWheelApplying__ = false; });
}

function __openSettingsYearModal__(){
  const modal = document.getElementById("settingsYearModal");
  const wheel = document.getElementById("settingsYearWheel");
  if (!modal || !wheel) return;
  const current = String(state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  __populateSettingsYearPicker__(current);
  modal.hidden = false;
  modal.setAttribute("aria-hidden", "false");
  __settingsYearWheelApplying__ = false;
  requestAnimationFrame(() => {
    __scrollSettingsYearWheelTo__(current, 'auto');
    try{ wheel.focus({ preventScroll:true }); }catch(_){ try{ wheel.focus(); }catch(__){ } }
  });
}

function __closeSettingsYearModal__(){
  const modal = document.getElementById("settingsYearModal");
  if (!modal) return;
  try{ clearTimeout(__settingsYearWheelTimer__); }catch(_){ }
  modal.hidden = true;
  modal.setAttribute("aria-hidden", "true");
}

function __saveSettingsYearModal__(){
  __applySettingsYearWheelSelection__({ snapOnly:true });
  __closeSettingsYearModal__();
}

function __pickExerciseYearFromSettings__(){
  __openSettingsYearModal__();
}

const __STATGEN_COMPARE_YEAR_KEY__ = "dDAE_statgen_compare_year_v1";
const __STATGEN_COMPARE_ENABLED_KEY__ = "dDAE_statgen_compare_enabled_v1";
const __STATGEN_COMPARE_BTN_VISUAL_KEY__ = "dDAE_statgen_compare_btn_visual_v1";
let __statGenCompareYearWheelTimer__ = null;
let __statGenCompareYearWheelApplying__ = false;

function __statGenCompareYearDefault__(){
  const current = Number(state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  const fallback = Number.isFinite(current) ? current - 1 : (new Date().getFullYear() - 1);
  return String(Math.max(2000, Math.min(2100, fallback)));
}

function __sanitizeStatGenCompareYear__(year){
  const n = Number(String(year || '').trim());
  if (Number.isFinite(n) && n >= 2000 && n <= 2100) return String(n);
  return __statGenCompareYearDefault__();
}

function loadStatGenCompareYear(){
  try{
    const stored = localStorage.getItem(__STATGEN_COMPARE_YEAR_KEY__);
    if (stored !== null && stored !== undefined && String(stored).trim() !== '') return __sanitizeStatGenCompareYear__(stored);
  }catch(_){ }
  return __statGenCompareYearDefault__();
}

function saveStatGenCompareYear(year){
  try{ localStorage.setItem(__STATGEN_COMPARE_YEAR_KEY__, __sanitizeStatGenCompareYear__(year)); }catch(_){ }
}

function __ensureStatGenCompareYear__(){
  const current = __sanitizeStatGenCompareYear__(state.statGenCompareYear || loadStatGenCompareYear());
  state.statGenCompareYear = current;
  return current;
}

function __statGenCompareEnabledRead__(){
  try{
    const raw = localStorage.getItem(__STATGEN_COMPARE_ENABLED_KEY__);
    if (raw === null || raw === undefined || String(raw).trim() === '') return true;
    return !(String(raw).trim() === '0' || String(raw).trim().toLowerCase() === 'false');
  }catch(_){ return true; }
}

function __statGenCompareEnabledWrite__(enabled){
  try{ localStorage.setItem(__STATGEN_COMPARE_ENABLED_KEY__, enabled ? '1' : '0'); }catch(_){ }
  state.statGenCompareEnabled = !!enabled;
  return !!enabled;
}

function __ensureStatGenCompareEnabled__(){
  const current = (typeof state.statGenCompareEnabled === 'boolean') ? state.statGenCompareEnabled : __statGenCompareEnabledRead__();
  state.statGenCompareEnabled = !!current;
  return !!current;
}

function __statGenCompareBtnVisualDefaultState__(enabled){
  return enabled
    ? { bg:'#2b7cb4', border:'#2b7cb4', fg:'#ffffff', opacity:0.80 }
    : { bg:'#d6dee8', border:'#d6dee8', fg:'#0f172a', opacity:0.80 };
}

function __statGenCompareBtnVisualRead__(){
  const fallback = { on: __statGenCompareBtnVisualDefaultState__(true), off: __statGenCompareBtnVisualDefaultState__(false) };
  try{
    const raw = localStorage.getItem(__STATGEN_COMPARE_BTN_VISUAL_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return {
      on: __tagColorPairFromValue__(parsed?.on || fallback.on, fallback.on.bg),
      off: __tagColorPairFromValue__(parsed?.off || fallback.off, fallback.off.bg)
    };
  }catch(_){ return fallback; }
}

function __statGenCompareBtnVisualWrite__(visuals){
  const current = __statGenCompareBtnVisualRead__();
  const next = {
    on: __tagColorPairFromValue__(visuals?.on || current.on, (visuals?.on && visuals.on.bg) || current.on.bg),
    off: __tagColorPairFromValue__(visuals?.off || current.off, (visuals?.off && visuals.off.bg) || current.off.bg)
  };
  try{ localStorage.setItem(__STATGEN_COMPARE_BTN_VISUAL_KEY__, JSON.stringify(next)); }catch(_){ }
  return next;
}

function __applyStatGenCompareYearButtonVisual__(){
  try{
    const btns = ['statGenCompareToggleBtn','statAmmCompareToggleBtn','statMensiliCompareToggleBtn'].map((id)=>document.getElementById(id)).filter(Boolean);
    if (!btns.length) return;
    const enabled = __ensureStatGenCompareEnabled__();
    const visuals = __statGenCompareBtnVisualRead__();
    const pair = enabled ? visuals.on : visuals.off;
    const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    const bgHex = __graphColorValueToHex__(pair?.bg || (enabled ? '#2b7cb4' : '#d6dee8'), enabled ? '#2b7cb4' : '#d6dee8');
    const borderHex = __graphColorValueToHex__(pair?.border || pair?.bg || bgHex, bgHex);
    const textHex = isDark
      ? __darkModeReadableTextHex__(__graphColorValueToHex__(pair?.fg || __tagColorTextHex__(pair?.bg || bgHex, pair?.fg || '', false), bgHex), enabled ? '#f8fbff' : '#e2e8f0')
      : __tagColorTextHex__(pair?.bg || bgHex, pair?.fg || '', false);
    const opacity = __designBgOpacityNormalize__(pair?.opacity ?? 0.80);
    btns.forEach((btn)=>{
      btn.classList.toggle('is-active', enabled);
      btn.classList.toggle('is-inactive', !enabled);
      btn.style.setProperty('--comparebtn-bg', hexToRgba(bgHex, opacity));
      btn.style.setProperty('--comparebtn-border', borderHex);
      btn.style.setProperty('--comparebtn-fg', textHex);
      btn.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('border', `1px solid ${borderHex}`, 'important');
      btn.style.setProperty('color', textHex, 'important');
      btn.style.setProperty('-webkit-text-fill-color', textHex, 'important');
      btn.setAttribute('aria-pressed', enabled ? 'true' : 'false');
      __applyVisualTextWeight__(btn, pair);
    });
  }catch(_){ }
}

function __openStatGenCompareYearButtonColorPicker__(){
  const enabled = __ensureStatGenCompareEnabled__();
  const visuals = __statGenCompareBtnVisualRead__();
  const stateKey = enabled ? 'on' : 'off';
  const initial = __tagColorPairFromValue__(visuals[stateKey], visuals[stateKey]?.bg || (enabled ? '#2b7cb4' : '#d6dee8'));
  __tagColorPopupOpen__('statgen-compare-toggle-btn', initial, (payload) => {
    const current = __statGenCompareBtnVisualRead__();
    const pair = __tagColorPairFromValue__(current[stateKey] || initial, initial.bg || '#2b7cb4');
    const payloadColors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    pair.bg = __parseOperatoreColorSpec__(payloadColors.bg || pair.bg || initial.bg || '#2b7cb4').spec;
    pair.border = __parseOperatoreColorSpec__(payloadColors.border || pair.border || pair.bg || initial.bg || '#2b7cb4').spec;
    pair.fg = String(payloadColors.fg || '').trim() ? __parseOperatoreColorSpec__(payloadColors.fg).spec : '';
    pair.opacity = __designBgOpacityNormalize__(payload?.opacity ?? pair.opacity ?? initial.opacity ?? 0.80);
    current[stateKey] = { bg: pair.bg, border: pair.border || pair.bg, fg: pair.fg || '', opacity: pair.opacity };
    __statGenCompareBtnVisualWrite__(current);
    __applyStatGenCompareYearButtonVisual__();
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:__designBgOpacityNormalize__(initial.opacity ?? 0.80), defaultMode:'bg', fallbackBg:initial.bg || (enabled ? '#2b7cb4' : '#d6dee8') });
}

function __toggleStatGenCompareEnabled__(){
  const next = !__ensureStatGenCompareEnabled__();
  __statGenCompareEnabledWrite__(next);
  __updateStatGenCompareYearButtonUI__();
  if (next){
    try{ __loadStatGenCompareGuests__({ force:true }); }catch(_){ }
  }
  try{ drawStatGenRegistrationsLineChart('statGenRegChart'); }catch(_){ }
  try{ drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); }catch(_){ }
  try{ drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
}

function __updateStatGenCompareYearButtonUI__(){
  const year = __ensureStatGenCompareYear__();
  const enabled = __ensureStatGenCompareEnabled__();
  try{
    const yearLabel = document.getElementById('statGenCompareYearBtnLabel');
    if (yearLabel) yearLabel.textContent = year;
    const yearLabelAmm = document.getElementById('statAmmCompareYearBtnLabel');
    if (yearLabelAmm) yearLabelAmm.textContent = year;
    const yearLabelMensili = document.getElementById('statMensiliCompareYearBtnLabel');
    if (yearLabelMensili) yearLabelMensili.textContent = year;
  }catch(_){ }
  try{
    const toggleLabel = document.getElementById('statGenCompareToggleBtnLabel');
    if (toggleLabel) toggleLabel.textContent = enabled ? 'ON' : 'OFF';
    const toggleLabelAmm = document.getElementById('statAmmCompareToggleBtnLabel');
    if (toggleLabelAmm) toggleLabelAmm.textContent = enabled ? 'ON' : 'OFF';
    const toggleLabelMensili = document.getElementById('statMensiliCompareToggleBtnLabel');
    if (toggleLabelMensili) toggleLabelMensili.textContent = enabled ? 'ON' : 'OFF';
  }catch(_){ }
  try{
    ['statGenCompareToggleBtn','statAmmCompareToggleBtn','statMensiliCompareToggleBtn'].forEach((id)=>{
      const toggleBtn = document.getElementById(id);
      if (toggleBtn){
        toggleBtn.setAttribute('aria-label', `Confronto grafico ${enabled ? 'attivo' : 'disattivo'}`);
        toggleBtn.title = `Confronto ${enabled ? 'ON' : 'OFF'}`;
      }
    });
  }catch(_){ }
  try{
    ['statGenCompareYearBtn','statMensiliCompareYearBtn'].forEach((id)=>{
      const yearBtn = document.getElementById(id);
      if (yearBtn){
        yearBtn.setAttribute('aria-label', `Seleziona anno confronto grafico ${year}`);
        yearBtn.title = `Anno confronto ${year}`;
      }
    });
  }catch(_){ }
  try{ __applyStatGenCompareYearButtonVisual__(); }catch(_){ }
}

function __populateStatGenCompareYearPicker__(selectedYear){
  const wheel = document.getElementById('statGenCompareYearWheel');
  if (!wheel) return;
  const years = __getSettingsYearValues__();
  const selected = __sanitizeStatGenCompareYear__(selectedYear || __ensureStatGenCompareYear__());
  const pad = '<div class="settings-year-wheel-spacer" aria-hidden="true"></div>'.repeat(__SETTINGS_YEAR_WHEEL_PAD_ROWS__);
  wheel.innerHTML = pad + years.map((year) => `<div class="settings-year-wheel-item${year === selected ? ' is-selected' : ''}" data-year="${year}" role="option" aria-selected="${year === selected ? 'true' : 'false'}">${year}</div>`).join('') + pad;
}

function __highlightStatGenCompareYearWheel__(year){
  const wheel = document.getElementById('statGenCompareYearWheel');
  if (!wheel) return;
  const target = __sanitizeStatGenCompareYear__(year);
  wheel.querySelectorAll('.settings-year-wheel-item').forEach((item) => {
    const on = item.getAttribute('data-year') === target;
    item.classList.toggle('is-selected', on);
    item.setAttribute('aria-selected', on ? 'true' : 'false');
  });
}

function __getStatGenCompareYearWheelValue__(){
  const wheel = document.getElementById('statGenCompareYearWheel');
  if (!wheel) return __ensureStatGenCompareYear__();
  const years = __getSettingsYearValues__();
  const rawIndex = Math.round((wheel.scrollTop || 0) / __SETTINGS_YEAR_WHEEL_ROW__);
  const index = Math.max(0, Math.min(years.length - 1, rawIndex));
  return years[index] || years[0] || __ensureStatGenCompareYear__();
}

function __scrollStatGenCompareYearWheelTo__(year, behavior){
  const wheel = document.getElementById('statGenCompareYearWheel');
  if (!wheel) return;
  const years = __getSettingsYearValues__();
  const target = __sanitizeStatGenCompareYear__(year || __ensureStatGenCompareYear__());
  const index = Math.max(0, years.indexOf(target));
  const top = index * __SETTINGS_YEAR_WHEEL_ROW__;
  try{ wheel.scrollTo({ top, behavior: behavior || 'auto' }); }
  catch(_){ wheel.scrollTop = top; }
  __highlightStatGenCompareYearWheel__(target);
}

function __applyStatGenCompareYearSelection__(opts){
  if (__statGenCompareYearWheelApplying__) return;
  __statGenCompareYearWheelApplying__ = true;
  const next = __sanitizeStatGenCompareYear__(__getStatGenCompareYearWheelValue__());
  const current = __ensureStatGenCompareYear__();
  __highlightStatGenCompareYearWheel__(next);
  __scrollStatGenCompareYearWheelTo__(next, (opts && opts.snapOnly) ? 'auto' : 'smooth');
  if (next && next !== current){
    state.statGenCompareYear = next;
    saveStatGenCompareYear(next);
    __updateStatGenCompareYearButtonUI__();
    state.statGenCompareGuests = [];
    try{ drawStatGenRegistrationsLineChart('statGenRegChart'); }catch(_){ }
    try{ drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); }catch(_){ }
    try{ drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
    try{ __loadStatGenCompareGuests__({ force:true }); }catch(_){ }
  }
  requestAnimationFrame(() => { __statGenCompareYearWheelApplying__ = false; });
}

function __openStatGenCompareYearModal__(){
  const modal = document.getElementById('statGenCompareYearModal');
  const wheel = document.getElementById('statGenCompareYearWheel');
  if (!modal || !wheel) return;
  const current = __ensureStatGenCompareYear__();
  __populateStatGenCompareYearPicker__(current);
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  __statGenCompareYearWheelApplying__ = false;
  requestAnimationFrame(() => {
    __scrollStatGenCompareYearWheelTo__(current, 'auto');
    try{ wheel.focus({ preventScroll:true }); }catch(_){ try{ wheel.focus(); }catch(__){ } }
  });
}

function __closeStatGenCompareYearModal__(){
  const modal = document.getElementById('statGenCompareYearModal');
  if (!modal) return;
  try{ clearTimeout(__statGenCompareYearWheelTimer__); }catch(_){ }
  modal.classList.remove('is-open');
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
}

function __saveStatGenCompareYearModal__(){
  __applyStatGenCompareYearSelection__({ snapOnly:true });
  __closeStatGenCompareYearModal__();
}

function __statGenGuestYearFields__(){
  return [
    'check_in','checkIn','arrivo','dataArrivo','check_out','checkOut','partenza','dataPartenza',
    'createdAt','created_at','updatedAt','updated_at'
  ];
}

function __statGenReadYearGuestsLocalCache__(year){
  try{
    const uid = (state && state.session && state.session.user_id) ? String(state.session.user_id) : '';
    const yy = String(year || '').trim();
    if (!uid || !yy) return [];
    const fullKey = `${__lsPrefixBase}${uid}:${yy}:ospitiALL|${uid}|${yy}|${yy}-01-01|${yy}-12-31`;
    const raw = localStorage.getItem(fullKey);
    if (!raw) return [];
    const parsed = JSON.parse(raw);
    return Array.isArray(parsed?.data) ? parsed.data : [];
  }catch(_){ return []; }
}


async function __statGenReadYearSnapshotFromIndexedDb__(year){
  try{
    const yy = String(year || '').trim();
    if (!/^\d{4}$/.test(yy)) return null;
    const keys = (typeof __kvKeys__ === 'function') ? await __kvKeys__('ctx:') : [];
    const suffix = `:${yy}:tbl:ospiti`;
    let guestKeys = keys.filter((k) => String(k || '').endsWith(suffix));
    if (!guestKeys.length) return null;

    const currentUid = String((state && state.session && (state.session.user_id || state.session.id || state.session.username)) || '').trim();
    guestKeys.sort((a, b) => {
      const au = String(a || '').split(':')[1] || '';
      const bu = String(b || '').split(':')[1] || '';
      const ap = currentUid && au === currentUid ? 0 : 1;
      const bp = currentUid && bu === currentUid ? 0 : 1;
      if (ap !== bp) return ap - bp;
      if (au === 'anon') return 1;
      if (bu === 'anon') return -1;
      return String(a).localeCompare(String(b));
    });

    const fields = [
      'check_in','checkIn','arrivo','dataArrivo','check_out','checkOut','partenza','dataPartenza',
      'createdAt','created_at','updatedAt','updated_at'
    ];

    for (const key of guestKeys){
      const parts = String(key || '').split(':');
      const uid = parts[1] || '';
      if (!uid) continue;
      const guestsRaw = await __kvGet__(`ctx:${uid}:${yy}:tbl:ospiti`);
      const guests = __guestFilterPreventiviRows__(__filterByExerciseYear__(Array.isArray(guestsRaw) ? guestsRaw : [], yy, fields), false);
      if (!Array.isArray(guests) || !guests.length) continue;

      const speseRaw = await __kvGet__(`ctx:${uid}:${yy}:tbl:spese`);
      const serviziRaw = await __kvGet__(`ctx:${uid}:${yy}:tbl:servizi`);
      const stanzeRaw = await __kvGet__(`ctx:${uid}:${yy}:tbl:stanze`);
      const spese = __filterByExerciseYear__(Array.isArray(speseRaw) ? speseRaw : [], yy, ['dataSpesa','data','data_spesa']);
      return {
        guests: guests.slice(),
        spese: Array.isArray(spese) ? spese.slice() : [],
        report: buildReportFromSpese(Array.isArray(spese) ? spese : []),
        servizi: Array.isArray(serviziRaw) ? serviziRaw.slice() : [],
        stanzeRows: Array.isArray(stanzeRaw) ? stanzeRaw.slice() : []
      };
    }
  }catch(_){ }
  return null;
}

async function __statGenLoadGuestsThroughPrimaryPipeline__(year, opts = {}){
  const targetYear = String(year || '').trim();
  if (!targetYear) return [];

  try{
    const directSnapshot = await __statGenReadYearSnapshotFromIndexedDb__(targetYear);
    if (directSnapshot && Array.isArray(directSnapshot.guests) && directSnapshot.guests.length) return directSnapshot;
  }catch(_){ }

  const backup = {
    exerciseYear: state.exerciseYear,
    speseAll: Array.isArray(state.speseAll) ? state.speseAll.slice() : state.speseAll,
    reportAll: state.reportAll,
    statsGuests: Array.isArray(state.statsGuests) ? state.statsGuests.slice() : state.statsGuests,
    _statsDataKey: state._statsDataKey,
    servizi: Array.isArray(state.servizi) ? state.servizi.slice() : state.servizi,
    stanzeRows: Array.isArray(state.stanzeRows) ? state.stanzeRows.slice() : state.stanzeRows
  };

  try{
    state.exerciseYear = targetYear;
    state._statsDataKey = '';
    await ensureStatsAllData({ showLoader:false, force: !!opts.force });
    return {
      guests: Array.isArray(state.statsGuests) ? state.statsGuests.slice() : [],
      spese: Array.isArray(state.speseAll) ? state.speseAll.slice() : [],
      report: state.reportAll ? JSON.parse(JSON.stringify(state.reportAll)) : null,
      servizi: Array.isArray(state.servizi) ? state.servizi.slice() : [],
      stanzeRows: Array.isArray(state.stanzeRows) ? state.stanzeRows.slice() : []
    };
  }catch(_){
    return [];
  }finally{
    state.exerciseYear = backup.exerciseYear;
    state.speseAll = backup.speseAll;
    state.reportAll = backup.reportAll;
    state.statsGuests = backup.statsGuests;
    state._statsDataKey = backup._statsDataKey;
    state.servizi = backup.servizi;
    state.stanzeRows = backup.stanzeRows;
  }
}

async function __loadStatGenCompareGuests__(opts = {}){
  const compareYear = __ensureStatGenCompareYear__();
  const currentYear = String(state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  state.statGenCompareLoading = true;
  if (compareYear === currentYear){
    state.statGenCompareGuests = Array.isArray(state.statsGuests) ? state.statsGuests.slice() : [];
    state.statGenCompareSnapshotYear = compareYear;
    state.statGenCompareSnapshot = {
      guests: Array.isArray(state.statsGuests) ? state.statsGuests.slice() : [],
      spese: Array.isArray(state.speseAll) ? state.speseAll.slice() : [],
      report: state.reportAll ? JSON.parse(JSON.stringify(state.reportAll)) : null,
      servizi: Array.isArray(state.servizi) ? state.servizi.slice() : [],
      stanzeRows: Array.isArray(state.stanzeRows) ? state.stanzeRows.slice() : []
    };
    state.statGenCompareLoading = false;
    try{ if (state.page === 'statgen') drawStatGenRegistrationsLineChart('statGenRegChart'); if (state.page === 'statmensili') drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); if (state.page === 'statamministratore') drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
    return state.statGenCompareGuests;
  }

  try{
    const directSnapshot = await __statGenReadYearSnapshotFromIndexedDb__(compareYear);
    if (directSnapshot && Array.isArray(directSnapshot.guests) && directSnapshot.guests.length){
      if (__ensureStatGenCompareYear__() !== compareYear) return Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : [];
      state.statGenCompareGuests = directSnapshot.guests.slice();
      state.statGenCompareSnapshotYear = compareYear;
      state.statGenCompareSnapshot = {
        guests: directSnapshot.guests.slice(),
        spese: Array.isArray(directSnapshot.spese) ? directSnapshot.spese.slice() : [],
        report: directSnapshot.report ? JSON.parse(JSON.stringify(directSnapshot.report)) : null,
        servizi: Array.isArray(directSnapshot.servizi) ? directSnapshot.servizi.slice() : [],
        stanzeRows: Array.isArray(directSnapshot.stanzeRows) ? directSnapshot.stanzeRows.slice() : []
      };
      try{ if (state.page === 'statgen') drawStatGenRegistrationsLineChart('statGenRegChart'); if (state.page === 'statmensili') drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); if (state.page === 'statamministratore') drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
    }
  }catch(_){ }

  try{
    const cachedRows = __statGenReadYearGuestsLocalCache__(compareYear);
    if (Array.isArray(cachedRows) && cachedRows.length){
      if (__ensureStatGenCompareYear__() !== compareYear) return Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : [];
      state.statGenCompareGuests = cachedRows.slice();
      state.statGenCompareSnapshotYear = compareYear;
      state.statGenCompareSnapshot = {
        guests: cachedRows.slice(),
        spese: Array.isArray(state.statGenCompareSnapshot?.spese) ? state.statGenCompareSnapshot.spese.slice() : [],
        report: state.statGenCompareSnapshot?.report ? JSON.parse(JSON.stringify(state.statGenCompareSnapshot.report)) : null,
        servizi: Array.isArray(state.statGenCompareSnapshot?.servizi) ? state.statGenCompareSnapshot.servizi.slice() : [],
        stanzeRows: Array.isArray(state.statGenCompareSnapshot?.stanzeRows) ? state.statGenCompareSnapshot.stanzeRows.slice() : (Array.isArray(state.stanzeRows) ? state.stanzeRows.slice() : [])
      };
      try{ if (state.page === 'statgen') drawStatGenRegistrationsLineChart('statGenRegChart'); if (state.page === 'statmensili') drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); if (state.page === 'statamministratore') drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
    }
  }catch(_){ }

  try{
    const snapshot = await __statGenLoadGuestsThroughPrimaryPipeline__(compareYear, { force:true });
    if (__ensureStatGenCompareYear__() !== compareYear) return Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : [];
    state.statGenCompareGuests = Array.isArray(snapshot?.guests) ? snapshot.guests : [];
    state.statGenCompareSnapshotYear = compareYear;
    state.statGenCompareSnapshot = {
      guests: Array.isArray(snapshot?.guests) ? snapshot.guests.slice() : [],
      spese: Array.isArray(snapshot?.spese) ? snapshot.spese.slice() : [],
      report: snapshot?.report ? JSON.parse(JSON.stringify(snapshot.report)) : null,
      servizi: Array.isArray(snapshot?.servizi) ? snapshot.servizi.slice() : [],
      stanzeRows: Array.isArray(snapshot?.stanzeRows) ? snapshot.stanzeRows.slice() : (Array.isArray(state.stanzeRows) ? state.stanzeRows.slice() : [])
    };
  }catch(_){
    if (__ensureStatGenCompareYear__() === compareYear) state.statGenCompareGuests = Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : [];
  }finally{
    state.statGenCompareLoading = false;
  }
  try{ if (state.page === 'statgen') drawStatGenRegistrationsLineChart('statGenRegChart'); if (state.page === 'statmensili') drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); if (state.page === 'statamministratore') drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
  return Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : [];
}

function __openStatGenCompareYearPicker__(){
  __openStatGenCompareYearModal__();
}

// =========================
// Context change (account / anno) — reset cache + state
// =========================
function __resetInMemoryData__(){
  try{
    for (const k of Object.keys(state || {})){
      const v = state[k];
      if (!v || typeof v !== "object") continue;
      if (Array.isArray(v.items)) v.items.length = 0;
      if (Array.isArray(v.rows)) v.rows.length = 0;
      if ("loadedAt" in v) try{ v.loadedAt = 0; }catch(_){}
      if ("loaded" in v) try{ v.loaded = false; }catch(_){}
      if ("byId" in v && v.byId && typeof v.byId === "object" && !Array.isArray(v.byId)) try{ v.byId = {}; }catch(_){}
      if ("map" in v && v.map && typeof v.map === "object" && !Array.isArray(v.map)) try{ v.map = {}; }catch(_){}
      if ("cache" in v && v.cache && typeof v.cache === "object") try{ v.cache = {}; }catch(_){}
    }
  }catch(_){}
}

function __applyContext__({ force } = {}){
  try{
    const sig = __ctxSig__();
    if (force || state.__ctxSig !== sig){
      state.__ctxSig = sig;
      try{ __resetInMemoryData__(); }catch(_){}
      try{ invalidateApiCache(); }catch(_){}
    }
  }catch(_){}
}

// Cancella COMPLETAMENTE i dati locali del browser (tutti account/anni)
async function __wipeBrowserDb__(){
  try{ invalidateApiCache(); }catch(_){}
  try{ __lsClearAll(); }catch(_){}

  try{
    const toDel = [];
    for (let i=0; i<localStorage.length; i++){
      const k = localStorage.key(i);
      if (!k) continue;
      if (k.startsWith(__lsPrefixBase) || k.startsWith("ddae_") || k.startsWith("dDAE_")) toDel.push(k);
    }
    toDel.forEach(k => { try{ localStorage.removeItem(k); }catch(_){ }});
  }catch(_){}

  try{ localStorage.removeItem(__SESSION_KEY); }catch(_){}
  try{ localStorage.removeItem(__YEAR_KEY); }catch(_){}

  try{
    await new Promise((resolve)=> {
      try{
        const rq = indexedDB.deleteDatabase(__IDB_NAME__);
        rq.onsuccess = () => resolve(true);
        rq.onerror = () => resolve(false);
        rq.onblocked = () => resolve(false);
      }catch(_){ resolve(false); }
    });
  }catch(_){}

  try{
    if (window.caches && caches.keys){
      const ks = await caches.keys();
      await Promise.all(ks.map(k => {
        if (k && (String(k).toLowerCase().includes("ddae") || String(k).toLowerCase().includes("daed"))) {
          return caches.delete(k);
        }
        return Promise.resolve(false);
      }));
    }
  }catch(_){}

  try{
    if (navigator.serviceWorker && navigator.serviceWorker.getRegistrations){
      const regs = await navigator.serviceWorker.getRegistrations();
      await Promise.all(regs.map(r => {
        try{
          const url = String(r && r.active && r.active.scriptURL ? r.active.scriptURL : "");
          if (!url || url.includes("service-worker")) return r.unregister();
        }catch(_){}
        return Promise.resolve(false);
      }));
    }
  }catch(_){}
}

function formatPulizieTopbarDateIT(d){
  try{
    const dt = (d instanceof Date) ? d : new Date(d);
    if (isNaN(dt)) return "";
    const wd = __capitalizeLocale__(__getWeekdayLongForLocale__(dt));
    const month = __capitalizeLocale__(dt.toLocaleDateString(__getCurrentLocale__(), { month:"long" }));
    return `${wd} ${dt.getDate()} ${month}`.trim();
  }catch(_){ return ""; }
}

function __syncTopbarCenterLayout__(){
  try{
    const brand = document.querySelector('.topbar .brand');
    if (!brand) return;
    const left = document.getElementById('topbarBrandLeft') || brand.querySelector('.brand-left');
    const right = document.getElementById('topbarBrandRight') || brand.querySelector('.brand-right');
    const sideGapRaw = getComputedStyle(brand).getPropertyValue('--topbar-side-gap');
    const sideGap = parseFloat(sideGapRaw || '14') || 14;
    const leftW = left ? Math.ceil(left.getBoundingClientRect().width) : 0;
    const rightW = right ? Math.ceil(right.getBoundingClientRect().width) : 0;
    const reserveLeft = Math.max(56, leftW + sideGap);
    const reserveRight = Math.max(56, rightW + sideGap);
    const full = Math.max(0, Math.ceil(brand.getBoundingClientRect().width) || 0);
    const sideReserve = Math.max(reserveLeft, reserveRight);
    const centerWidth = Math.max(64, full - ((sideReserve * 2) + (sideGap * 2)));
    brand.style.setProperty('--topbar-left-reserve', reserveLeft + 'px');
    brand.style.setProperty('--topbar-right-reserve', reserveRight + 'px');
    brand.style.setProperty('--topbar-center-width', centerWidth + 'px');
  }catch(_){ }
}

function __setTopbarCenterLabel__(){
  try{
    const el = document.getElementById("topbarYear");
    if (!el) return;
    if (state && state.page === "calendario"){
      const a = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();
      const d = (a instanceof Date) ? a : new Date(a);
      const month = monthNameIT(d).toUpperCase();
      const year = (!isNaN(d)) ? String(d.getFullYear()) : String(state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
      el.textContent = `${month} ${year}`.trim();
    } else if (state && state.page === "pulizie"){
      const base = (state && state.session && isOperatoreSession(state.session)) ? new Date() : (state.cleanDay ? new Date(state.cleanDay) : new Date());
      el.textContent = formatPulizieTopbarDateIT(startOfLocalDay(base)) || "Daedalium";
    } else {
      el.textContent = "Daedalium";
    }
  }catch(_){ }
  try{ __syncTopbarCenterLayout__(); }catch(_){ }
}

function updateYearPill(){
  const y = String(state.exerciseYear || loadExerciseYear() || "").trim();
  const pills = [
    document.getElementById("yearPill"),
    document.getElementById("homeYearPill")
  ].filter(Boolean);

  pills.forEach((pill) => {
    if (!y){ pill.hidden = true; }
    else{
      pill.textContent = y;
      pill.hidden = false;
    }
  });

  // Topbar: anno (default) o mese (solo Calendario)
  try{ __setTopbarCenterLabel__(); }catch(_){ }

  try{ updateSettingsTabs(); }catch(_){ }
}

function updateSettingsTabs(){
  try{
    const y = String(state.exerciseYear || loadExerciseYear() || new Date().getFullYear() || "").trim();
    const yLabel = y || "—";
    const s = state.session || {};
    const raw = (s.accountName || s.username || s.user || s.nome || s.name || s.email || "").toString().trim();
    const userLabel = raw ? raw : "—";
    const el = document.getElementById("settingsAccountYearTab");
    if (el) el.textContent = `${userLabel} - ${yLabel}`;
    const yearPill = document.getElementById("settingsYearPill");
    if (yearPill){
      const yearLabelEl = yearPill.querySelector('.settings-btn-year-value');
      if (yearLabelEl) yearLabelEl.textContent = `Anno ${yLabel}`;
      else yearPill.textContent = yLabel;
      try{ yearPill.setAttribute('aria-label', `Anno selezionato ${yLabel}`); }catch(_){ }
    }
    const opYearPill = document.getElementById("opSettingsYearPill");
    if (opYearPill) opYearPill.textContent = userLabel;
  }catch(_){ }
  try{ updateSettingsAccountName(); }catch(_){ }
}

function updateSettingsAccountName(){
  try{
    const s = state.session || {};
    const raw = String(s.accountName || s.username || s.user || s.nome || s.name || s.email || "").trim();
    ["settingsAccountName","opSettingsAccountName"].forEach((id)=>{ const el = document.getElementById(id); if (el) el.textContent = raw || "—"; });
  }catch(_){ }
}


// Mostra la build a runtime (se il JS è vecchio, lo vedi subito)
(function syncBuildLabel(){
  try{
    ["buildText"].forEach((id) => {
      const el = document.getElementById(id);
      if (el) el.textContent = `dDAE_${BUILD_VERSION}`;
    });
    try{
      const box = document.getElementById("homeOperatorDbBox");
      if (box){
        const role = String((state && state.session && (state.session.ruolo || state.session.role || state.session.tipo || state.session.account_type)) || "").toLowerCase();
        box.hidden = !(role.includes("oper"));
      }
    }catch(_){}

  }catch(_){}
  try{
    const pendingBuild = String(sessionStorage.getItem("dDAE_pending_build") || "").trim();
    if (pendingBuild && !__isRemoteNewer(pendingBuild, BUILD_VERSION) && !__isRemoteNewer(BUILD_VERSION, pendingBuild)) {
      sessionStorage.removeItem("dDAE_pending_build");
      sessionStorage.removeItem("dDAE_update_attempt_build");
      sessionStorage.removeItem("dDAE_update_attempt_at");
    }
  }catch(_){ }
})();
try{
  const pendingBuild = String(sessionStorage.getItem("dDAE_pending_build") || "").trim();
  if (pendingBuild) {
    ["buildText"].forEach((id) => {
      const el = document.getElementById(id);
      if (el) el.textContent = pendingBuild;
    });
  }
}catch(_){ }
// Aggiornamento build iOS: niente purge aggressivo di cache/SW.
// Se esiste una build remota più nuova, chiediamo l'update del SW e facciamo al massimo
// un solo reload con cache-bust per build, evitando loop/toast invasivi.
async function hardUpdateCheck(){
  try{
    if (window.__ddaeHardUpdating) return;
    const res = await fetch(`./version.json?ts=${Date.now()}`, { cache: "no-store" });
    if (!res.ok) return;
    const data = await res.json();
    const remote = String((data && (data.build || data.version || data.ver)) || "").trim();
    if (!remote){
      try{ sessionStorage.removeItem("dDAE_pending_build"); }catch(_){ }
      try{ sessionStorage.removeItem("dDAE_update_attempt_build"); }catch(_){ }
      try{ sessionStorage.removeItem("dDAE_update_attempt_at"); }catch(_){ }
      return;
    }
    if (!__isRemoteNewer(remote, BUILD_VERSION)) {
      try{ sessionStorage.removeItem("dDAE_pending_build"); }catch(_){ }
      try{ sessionStorage.removeItem("dDAE_update_attempt_build"); }catch(_){ }
      try{ sessionStorage.removeItem("dDAE_update_attempt_at"); }catch(_){ }
      return;
    }

    const lastBuild = String(sessionStorage.getItem("dDAE_update_attempt_build") || "").trim();
    const lastAt = parseInt(String(sessionStorage.getItem("dDAE_update_attempt_at") || "0"), 10) || 0;
    const tooSoon = (lastBuild === remote) && ((Date.now() - lastAt) < 15000);
    if (tooSoon) return;

    window.__ddaeHardUpdating = true;
    try{ sessionStorage.setItem("dDAE_pending_build", remote); }catch(_){ }
    try{ sessionStorage.setItem("dDAE_update_attempt_build", remote); }catch(_){ }
    try{ sessionStorage.setItem("dDAE_update_attempt_at", String(Date.now())); }catch(_){ }

    try{
      if ("serviceWorker" in navigator){
        const reg = await navigator.serviceWorker.getRegistration();
        if (reg){
          try{ await reg.update(); }catch(_){ }
          try{ if (reg.waiting) reg.waiting.postMessage({ type: "SKIP_WAITING" }); }catch(_){ }
        }
      }
    }catch(_){ }

    const target = `./index.html?v=${encodeURIComponent(remote)}&r=${Date.now()}`;
    const href = String(location.href || "");
    if (!href.includes(`v=${encodeURIComponent(remote)}`)) {
      window.location.replace(target);
      return;
    }

    window.__ddaeHardUpdating = false;
  }catch(_){
    window.__ddaeHardUpdating = false;
  }
}
// ===== Performance mode (iOS/Safari PWA) =====
const IS_IOS = (() => {
  const ua = navigator.userAgent || "";
  const iOS = /iPad|iPhone|iPod/.test(ua);
  const iPadOS = (navigator.platform === "MacIntel" && (navigator.maxTouchPoints || 0) > 1);
  return iOS || iPadOS;
})();

// Marca l'ambiente iOS (utile per CSS mirati)
try{ document.documentElement.classList.toggle("is-ios", IS_IOS); }catch(_){ }

function applyPerfMode(){
  try{
    const saved = localStorage.getItem("ddae_perf_mode"); // "full" | "lite"
    const mode = saved ? saved : (IS_IOS ? "lite" : "full");
    document.body.classList.toggle("perf-lite", mode === "lite");
  } catch(_){
    // fallback: su iOS attiva comunque lite
    if (IS_IOS) document.body.classList.add("perf-lite");
  }
}




// ===== Stato UI: evita "torna in HOME" quando iOS aggiorna il Service Worker =====
const __RESTORE_KEY = "__ddae_restore_state";
const __LAST_PAGE_KEY = "__ddae_last_page";
const __HASH_PREFIX = "#p=";

function __sanitizePage(p){
  try{
    if (!p) return null;
    const page = String(p).trim();
    if (!page) return null;
    const el = document.getElementById(`page-${page}`);
    return el ? page : null;
  } catch(_) { return null; }
}

function __readHashPage(){
  try{
    const h = (location.hash || "").trim();
    if (!h.startsWith(__HASH_PREFIX)) return null;
    const p = decodeURIComponent(h.slice(__HASH_PREFIX.length));
    return __sanitizePage(p);
  } catch(_) { return null; }
}

function __writeHashPage(page){
  try{
    const p = __sanitizePage(page) || "home";
    const newHash = __HASH_PREFIX + encodeURIComponent(p);
    if (location.hash !== newHash){
      history.replaceState(null, document.title, newHash);
    }
  } catch(_) {}
}

function __readRestoreState(){
  try{
    // 1) restore "one-shot" (session -> local)
    let raw = null;
    try { raw = sessionStorage.getItem(__RESTORE_KEY); } catch(_) {}
    if (!raw){
      try { raw = localStorage.getItem(__RESTORE_KEY); } catch(_) {}
    }
    if (raw){
      try { sessionStorage.removeItem(__RESTORE_KEY); } catch(_) {}
      try { localStorage.removeItem(__RESTORE_KEY); } catch(_) {}
      const obj = JSON.parse(raw);
      if (obj && typeof obj === "object"){
        if (!obj.page){
          let last = null;
          try { last = __sanitizePage(localStorage.getItem(__LAST_PAGE_KEY)); } catch(_) {}
          obj.page = __readHashPage() || last || "home";
        } else {
          obj.page = __sanitizePage(obj.page) || "home";
        }
        return obj;
      }
    }

    // 2) fallback: hash / last page (persistente)
    const pHash = __readHashPage();
    if (pHash) return { page: pHash };
    let pLast = null;
    try { pLast = __sanitizePage(localStorage.getItem(__LAST_PAGE_KEY)); } catch(_) {}
    if (pLast) return { page: pLast };
    return null;
  } catch(_) { return null; }
}

function __writeRestoreState(obj){
  const o = (obj && typeof obj === "object") ? obj : {};
  const page = __sanitizePage(o.page) || __sanitizePage(state.page) || "home";
  o.page = page;

  // 1) one-shot restore for SW reload (session + local for iOS reliability)
  try { sessionStorage.setItem(__RESTORE_KEY, JSON.stringify(o)); } catch(_) {}
  try { localStorage.setItem(__RESTORE_KEY, JSON.stringify(o)); } catch(_) {}

  // 2) persistent page memory (so even if iOS drops sessionStorage we stay on page)
  try { localStorage.setItem(__LAST_PAGE_KEY, page); } catch(_) {}
  __writeHashPage(page);
}

function __rememberPage(page){
  const p = __sanitizePage(page) || "home";
  try { localStorage.setItem(__LAST_PAGE_KEY, p); } catch(_) {}
  __writeHashPage(p);
}


// ===== Service Worker reload "safe": non interrompere i caricamenti DB =====
let __SW_RELOAD_PENDING = false;
let __SW_RELOADING = false;

function __performSwReload(){
  if (__SW_RELOADING) return;
  __SW_RELOADING = true;
  try { __writeRestoreState(__captureUiState()); } catch (_) {}
  location.reload();
}

function __requestSwReload(){
  try { __writeRestoreState(__captureUiState()); } catch (_) {}
  // Se stiamo caricando dati (API), rimanda il reload a fine richieste
  if (loadingState && loadingState.requestCount > 0){
    __SW_RELOAD_PENDING = true;
    return;
  }
  __performSwReload();
}

function __captureFormValue(id){
  try {
    const el = document.getElementById(id);
    if (!el) return null;
    return (el.type === "checkbox") ? !!el.checked : (el.value ?? "");
  } catch (_) { return null; }
}

function __applyFormValue(id, v){
  try {
    const el = document.getElementById(id);
    if (!el || v == null) return;
    if (el.type === "checkbox") el.checked = !!v;
    else el.value = String(v);
  } catch (_) {}
}

function __captureSyncRestoreState(){
  try{
    const out = __captureUiState();
    const currentPage = __sanitizePage(state.page)
      || __sanitizePage(document.body?.dataset?.page)
      || __readHashPage()
      || "home";
    out.page = currentPage;
    return out;
  }catch(_){
    return { page: (__sanitizePage(state.page) || "home") };
  }
}

function __captureUiState(){
  // IMPORTANT:
  // Salviamo lo stato della scheda ospite SOLO se l'utente e' davvero nella pagina "ospite".
  // Su iOS/PWA un reload/restore puo' riportare in primo piano una scheda vecchia (mode/view + layout diverso)
  // anche se non e' stata richiamata.
  const shouldPersistGuest = (state.page === "ospite");

  const out = {
    page: state.page || "home",
    period: state.period || { from:"", to:"" },
    preset: state.periodPreset || "this_month",
    guest: shouldPersistGuest ? {
      mode: state.guestMode || "create",
      editId: state.guestEditId || null,
      depositType: state.guestDepositType || "",
      saldoType: state.guestSaldoType || "",
      depositReceipt: !!state.guestDepositReceipt,
      saldoReceipt: !!state.guestSaldoReceipt,
      marriage: !!state.guestMarriage,
      group: !!state.guestGroup,
      colc: !!state.guestColC,
      rooms: Array.from(state.guestRooms || []),
      lettiPerStanza: state.lettiPerStanza || {},
      form: {
        guestName: __captureFormValue("guestName"),
        guestNationality: __captureFormValue("guestNationality"),
        guestAdults: __captureFormValue("guestAdults"),
        guestKidsU10: __captureFormValue("guestKidsU10"),
        guestCheckIn: __captureFormValue("guestCheckIn"),
        guestCheckOut: __captureFormValue("guestCheckOut"),
        guestTotal: __captureFormValue("guestTotal"),
        guestChannel: __captureFormValue("guestChannel"),
        guestChannelCommission: __captureFormValue("guestChannelCommission"),
        guestBooking: __captureFormValue("guestBooking"),
        guestDiscount: __captureFormValue("guestDiscount"),
        guestDeposit: __captureFormValue("guestDeposit"),
        guestSaldo: __captureFormValue("guestSaldo"),
      }
    } : null,
    calendar: {
      anchor: (state.calendar && state.calendar.anchor) ? toISO(state.calendar.anchor) : ""
    }
  };
  return out;
}

function __applyUiState(restore){
  if (!restore || typeof restore !== "object") return;

  try {
    // periodo
    const p = restore.period || null;
    if (p && p.from && p.to) {
      setPeriod(p.from, p.to);
    }

    if (restore.preset) setPresetValue(restore.preset);

    // calendario
    if (restore.calendar?.anchor) {
      if (!state.calendar) state.calendar = { anchor: new Date(), ready:false, guests:[], rangeKey:"" };
      state.calendar.anchor = new Date(restore.calendar.anchor + "T00:00:00");
      state.calendar.ready = false;
    }

    // ospite (solo se eri in quella sezione)
    if (restore.guest) {
      state.guestMode = restore.guest.mode || state.guestMode;
      state.guestEditId = restore.guest.editId || state.guestEditId;
      state.guestDepositType = restore.guest.depositType || state.guestDepositType;
      state.guestSaldoType = restore.guest.saldoType || state.guestSaldoType;
      state.guestDepositReceipt = !!restore.guest.depositReceipt;
      state.guestSaldoReceipt = !!restore.guest.saldoReceipt;
      state.guestMarriage = !!restore.guest.marriage;
      state.guestGroup = !!(restore.guest.group);
      state.guestColC = !!(restore.guest.colc);

      // stanze selezionate
      try {
        state.guestRooms = new Set((restore.guest.rooms || []).map(n=>parseInt(n,10)).filter(n=>isFinite(n)));
        state.lettiPerStanza = restore.guest.lettiPerStanza || {};
      } catch (_) {}

      // campi form
      const f = restore.guest.form || {};
      __applyFormValue("guestName", f.guestName);
      __applyFormValue("guestNationality", f.guestNationality);
      try{ updateGuestNationalityButton(); }catch(_){}
      __applyFormValue("guestAdults", f.guestAdults);
      __applyFormValue("guestKidsU10", f.guestKidsU10);
      __applyFormValue("guestCheckIn", f.guestCheckIn);
      __applyFormValue("guestCheckOut", f.guestCheckOut);
      try{ __updateGuestDateRangeTrigger__(); }catch(_){ }
      __applyFormValue("guestTotal", f.guestTotal);
      __applyFormValue("guestChannel", f.guestChannel);
      __applyFormValue("guestChannelCommission", f.guestChannelCommission);
      __applyFormValue("guestBooking", f.guestBooking);
      try { applySelectedChannelToGuestForm(f.guestChannel, { preserveManual:true }); } catch (_) {}
      __applyFormValue("guestDiscount", f.guestDiscount);
      __applyFormValue("guestDeposit", f.guestDeposit);
      __applyFormValue("guestSaldo", f.guestSaldo);
      try { updateGuestRemaining(); } catch (_) {}

      // UI rooms + pills
      try {
        document.querySelectorAll("#roomsPicker .room-dot").forEach(btn => {
          const n = parseInt(btn.getAttribute("data-room"), 10);
          const on = state.guestRooms.has(n);
          btn.classList.toggle("selected", on);
          btn.setAttribute("aria-pressed", on ? "true" : "false");
        });
      } catch (_) {}
      try { setPayType("depositType", state.guestDepositType); } catch (_) {}
      try { setPayType("saldoType", state.guestSaldoType); } catch (_) {}
      try { setPayReceipt("depositType", state.guestDepositReceipt); } catch (_) {}
      try { setPayReceipt("saldoType", state.guestSaldoReceipt); } catch (_) {}
      try { setMarriage(state.guestMarriage);
    setGroup(state.guestGroup);
    setColC(state.guestColC); } catch (_) {}
      try { setGroup(state.guestGroup); } catch (_) {}
    }

  } catch (_) {}
}


function genId(prefix){
  return `${prefix}_${Date.now()}_${Math.floor(Math.random()*1000000)}`;
}

const $ = (sel) => document.querySelector(sel);

function setMarriage(on){
  state.guestMarriage = !!on;
  const btn = document.getElementById("roomMarriage");
  if (!btn) return;
  btn.classList.toggle("selected", state.guestMarriage);
  btn.setAttribute("aria-pressed", state.guestMarriage ? "true" : "false");
}

function setGroup(on){
  state.guestGroup = !!on;
  const btn = document.getElementById("roomGroup");
  if (!btn) return;
  btn.classList.toggle("selected", state.guestGroup);
  btn.setAttribute("aria-pressed", state.guestGroup ? "true" : "false");
}


function setColC(on){
  state.guestColC = !!on;
  const btn = document.getElementById("roomColC");
  if (!btn) return;
  btn.classList.toggle("selected", state.guestColC);
  btn.setAttribute("aria-pressed", state.guestColC ? "true" : "false");
}



function setPayType(containerId, type){
  const wrap = document.getElementById(containerId);
  if (!wrap) return;
  const t = (type == null ? "" : String(type)).toLowerCase();
  wrap.querySelectorAll(".pay-dot[data-type]").forEach(b => {
    const v = (b.getAttribute("data-type") || "").toLowerCase();
    const on = v === t;
    b.classList.toggle("selected", on);
    b.setAttribute("aria-pressed", on ? "true" : "false");
  });
}


function setPayReceipt(containerId, on){
  const wrap = document.getElementById(containerId);
  if (!wrap) return;
  const btn = wrap.querySelector('.pay-dot[data-receipt]');
  if (!btn) return;
  const active = !!on;
  btn.classList.toggle("selected", active);
  btn.setAttribute("aria-pressed", active ? "true" : "false");
}

function __guestPaymentAmountForKind__(kind){
  try{
    const id = (String(kind || '').toLowerCase() === 'saldo') ? 'guestSaldo' : 'guestDeposit';
    const raw = String(document.getElementById(id)?.value ?? '').replace(',', '.').trim();
    const n = parseFloat(raw || '0');
    return Number.isFinite(n) ? n : 0;
  }catch(_){ return 0; }
}

function __syncPaymentLedsWithAmounts__(){
  try{
    if (__guestPaymentAmountForKind__('deposit') <= 0){
      state.guestDepositType = '';
      state.guestDepositReceipt = false;
      setPayType('depositType', '');
      setPayReceipt('depositType', false);
    }
    if (__guestPaymentAmountForKind__('saldo') <= 0){
      state.guestSaldoType = '';
      state.guestSaldoReceipt = false;
      setPayType('saldoType', '');
      setPayReceipt('saldoType', false);
    }
  }catch(_){ }
}



function setRegFlag(containerId, flag, on){
  const wrap = document.getElementById(containerId);
  if (!wrap) return;
  const btn = wrap.querySelector(`.pay-dot[data-flag="${flag}"]`);
  if (!btn) return;
  const active = !!on;
  btn.classList.toggle("selected", active);
  btn.setAttribute("aria-pressed", active ? "true" : "false");
}

function setRegFlags(containerId, psOn, istatOn){
  setRegFlag(containerId, "ps", psOn);
  setRegFlag(containerId, "istat", istatOn);
}

function truthy(v){
  if (v === true) return true;
  if (v === false || v === undefined || v === null) return false;
  const s = String(v).trim().toLowerCase();
  return (s === "1" || s === "true" || s === "yes" || s === "si" || s === "on");
}

// dDAE_1.020 — error overlay: evita blocchi silenziosi su iPhone PWA
window.addEventListener("error", (e) => {
  try {
    const msg = (e?.message || "Errore JS") + (e?.filename ? ` @ ${e.filename.split("/").pop()}:${e.lineno||0}` : "");
    console.error("JS error", e?.error || e);
    toast(msg);
  } catch (_) {}
});
window.addEventListener("unhandledrejection", (e) => {
  try {
    console.error("Unhandled promise rejection", e?.reason || e);
    const msg = (e?.reason?.message || e?.reason || "Promise rejection").toString();
    toast("Errore: " + msg);
  } catch (_) {}
});

const state = {
  navId: 0,
  cleanDay: null,

  motivazioni: [],
  spese: [],
  report: null,
  _dataKey: "",
  period: { from: "", to: "" },
  deletedGuests: [],
  periodPreset: "this_month",
  page: "home",
  speseView: "list",
  guests: [],
  stanzeRows: [],
  stanzeByKey: {},
  guestRooms: new Set(),
  guestDepositType: "",

  // HOME: ricevute mancanti (check a ogni riavvio)
  pendingReceipts: [],
  pendingReceiptsGuests: [],
  pendingReceiptsCount: 0,
  guestAlerts: { left: [], right: [] },
  guestAlertCounts: { left: 0, right: 0 },
  guestAlertModalSide: "left",

  guestEditId: null,
  guestMode: "create",
  lettiPerStanza: {},
    bedsDirty: false,
  stanzeSnapshotOriginal: "",
guestMarriage: false,
  guestGroup: false,
  guestColC: false,
  guestSaldoType: "",
  guestPSRegistered: false,
  guestISTATRegistered: false,
  guestCheckInDone: false,
  guestArrivalFilter: "today",
  guestListScrollState: null,
  guestListScrollRestorePending: false,
  // Scheda ospite (sola lettura): ultimo ospite aperto
  guestViewItem: null,

  // Servizi (scheda ospite)
  guestServicesItems: [],
  guestServicesComputedTotal: 0,
  guestServicesManualOverride: false,
  guestServicesLoadedFor: null,
  guestServicesCacheById: {},


  // Lavanderia (resoconti settimanali)
  laundry: { list: [], current: null },
  // Impostazioni (foglio "impostazioni")
  settings: { loaded: false, byKey: {}, rows: [], loadedAt: 0 },

  // Colazione (lista spesa permanente)
  colazione: { loaded: false, loadedAt: 0, items: [] },

  // Prodotti pulizia (lista spesa permanente)
  prodotti_pulizia: { loaded: false, loadedAt: 0, items: [] },

  // UI prodotti
  prodottiUI: { list: "colazione", sort: "frequent" },

  // Auth/session + anno esercizio
  session: null,
  exerciseYear: null,
  statGenCompareYear: null,
  statGenCompareGuests: [],
  statGenCompareSnapshot: null,
};

const COLORS = {
  CONTANTI: "#2b7cb4",          // palette
  TASSA_SOGGIORNO: "#bfbea9",   // palette
  IVA_22: "#c9772b",            // palette
  IVA_10: "#6fb7d6",            // palette
  IVA_4: "#4d9cc5",             // palette
  FUORI_BUDGET: "#8b5cf6",      // palette
};

const LS_STAT_FISCAL_MODE = "ddae_stat_fiscal_mode";
const LS_STAT_FORFETTARIO_STARTUP_5 = "ddae_stat_forfettario_startup_5";
const __STAT_FISCAL_BTN_VISUAL_KEY__ = "dDAE_stat_fiscal_btn_visual_v1";
const LS_APP_TEXT_UI = "ddae_app_text_ui_v1";
const APP_TEXT_SCALE_MAP = Object.freeze({ "1": 1, "2": 1.08, "3": 1.16 });
let __appTextUiMutationObserver__ = null;
let __appTextUiApplyTimer__ = null;

function getAppTextUiSettings(){
  const fallback = { bold:false, size:"1" };
  try{
    const raw = localStorage.getItem(LS_APP_TEXT_UI);
    if (!raw) return fallback;
    const parsed = JSON.parse(raw);
    const size = String(parsed?.size || "1");
    return {
      bold: false,
      size: (size === "2" || size === "3") ? size : "1"
    };
  }catch(_){
    return fallback;
  }
}

function __saveAppTextUiSettings__(settings){
  try{
    const size = String(settings?.size || '1');
    localStorage.setItem(LS_APP_TEXT_UI, JSON.stringify({
      bold:false,
      size:(size === '2' || size === '3') ? size : '1'
    }));
  }catch(_){ }
}

function __appTextUiBoldWeight__(baseWeight, isBold){
  const raw = String(baseWeight || "").trim();
  if (!isBold) return raw || "400";
  const numeric = parseInt(raw, 10);
  if (Number.isFinite(numeric)) return String(Math.max(700, numeric));
  return "700";
}

function __isAppTextCandidate__(el){
  try{
    if (!(el instanceof Element)) return false;
    const tag = String(el.tagName || "").toUpperCase();
    if (!tag) return false;
    if (["SCRIPT","STYLE","NOSCRIPT","SVG","PATH","RECT","CIRCLE","LINE","POLYLINE","POLYGON","G","DEFS","CLIPPATH","USE","STOP"].includes(tag)) return false;
    if (el.closest && el.closest('svg')) return false;
    if (tag === 'INPUT'){
      const type = String(el.getAttribute('type') || el.type || 'text').toLowerCase();
      if (["checkbox","radio","range","color","file","hidden","date","time","month","week"].includes(type)) return false;
      return true;
    }
    if (["TEXTAREA","SELECT","OPTION","BUTTON","LABEL","SPAN","A","P","PRE","LI","TD","TH","STRONG","B","EM","I","SMALL","H1","H2","H3","H4","H5","H6","SUMMARY"].includes(tag)) return true;
    for (const node of Array.from(el.childNodes || [])){
      if (node && node.nodeType === 3 && String(node.textContent || '').trim()) return true;
    }
  }catch(_){ }
  return false;
}

function __applyAppTextUiToElement__(el, settings){
  try{
    if (!__isAppTextCandidate__(el)) return;
    const style = getComputedStyle(el);
    const computedSize = parseFloat(style.fontSize || '0');
    if (!Number.isFinite(computedSize) || computedSize <= 0) return;
    if (!el.dataset.appTextBaseSize) el.dataset.appTextBaseSize = String(computedSize);
    if (!el.dataset.appTextBaseWeight) el.dataset.appTextBaseWeight = String(style.fontWeight || '400');
    const baseSize = parseFloat(el.dataset.appTextBaseSize || '0');
    if (!Number.isFinite(baseSize) || baseSize <= 0) return;
    const scale = APP_TEXT_SCALE_MAP[String(settings?.size || '1')] || 1;
    const nextSize = Math.max(9, baseSize * scale);
    el.style.setProperty('font-size', `${nextSize.toFixed(2)}px`, 'important');
    el.style.setProperty('font-weight', __appTextUiBoldWeight__(el.dataset.appTextBaseWeight || '400', !!settings?.bold), 'important');
  }catch(_){ }
}

function __applyAppTextUiToIcons__(root, settings){
  try{
    const targets = [];
    if (root instanceof Element && root.matches && root.matches('svg, svg *, .ui-ico, .ui-ico *')) targets.push(root);
    if (root && root.querySelectorAll){
      root.querySelectorAll('svg, svg *, .ui-ico, .ui-ico *').forEach((node) => targets.push(node));
    }
    const seen = new Set();
    targets.forEach((node) => {
      if (!(node instanceof Element)) return;
      if (seen.has(node)) return;
      seen.add(node);
      try{
        const style = getComputedStyle(node);
        const strokeWidth = parseFloat(style.strokeWidth || node.getAttribute('stroke-width') || '0');
        if (!Number.isFinite(strokeWidth) || strokeWidth <= 0) return;
        if (!node.dataset.appTextBaseStrokeWidth) node.dataset.appTextBaseStrokeWidth = String(strokeWidth);
        const baseStroke = parseFloat(node.dataset.appTextBaseStrokeWidth || '0');
        if (!Number.isFinite(baseStroke) || baseStroke <= 0) return;
        const nextStroke = (!!settings?.bold ? baseStroke * 1.18 : baseStroke);
        node.style.setProperty('stroke-width', `${nextStroke.toFixed(2)}px`, 'important');
      }catch(_){ }
    });
  }catch(_){ }
}

function applyAppTextUi(root){
  const scope = root || document.body;
  if (!scope) return;
  const settings = getAppTextUiSettings();
  try{
    document.body?.setAttribute('data-text-size', String(settings.size || '1'));
    document.body?.setAttribute('data-text-weight', settings.bold ? 'bold' : 'normal');
  }catch(_){ }
  try{
    const elements = [];
    if (scope instanceof Element) elements.push(scope);
    if (scope && scope.querySelectorAll) scope.querySelectorAll('*').forEach((el) => elements.push(el));
    const seen = new Set();
    elements.forEach((el) => {
      if (!(el instanceof Element)) return;
      if (seen.has(el)) return;
      seen.add(el);
      __applyAppTextUiToElement__(el, settings);
    });
  }catch(_){ }
  __applyAppTextUiToIcons__(scope, settings);
}

function scheduleApplyAppTextUi(root){
  try{ if (__appTextUiApplyTimer__) clearTimeout(__appTextUiApplyTimer__); }catch(_){ }
  __appTextUiApplyTimer__ = setTimeout(() => {
    __appTextUiApplyTimer__ = null;
    try{ applyAppTextUi(root || document.body); }catch(_){ }
  }, 16);
}

function setAppTextUiSettings(next){
  const current = getAppTextUiSettings();
  const size = String(next?.size || current.size || '1');
  const settings = {
    bold: false,
    size: (size === '2' || size === '3') ? size : '1'
  };
  __saveAppTextUiSettings__(settings);
  scheduleApplyAppTextUi(document.body);
  try{ __refreshStatSharedLineCharts__(); }catch(_){ }
  try{ renderStatCancellazioni(); }catch(_){ }
  try{ renderRoomSettingsPage(); }catch(_){ }
}

function renderRoomSettingsTextControls(){
  try{
    const settings = getAppTextUiSettings();
    const fallbackVisual = { bg:'blue-4', border:'blue-4', fg:'', opacity:0.80 };
    const boldBtn = document.getElementById('roomSettingsTextBoldBtn');
    if (boldBtn){
      boldBtn.hidden = true;
      boldBtn.style.display = 'none';
      boldBtn.setAttribute('aria-hidden', 'true');
      boldBtn.setAttribute('tabindex', '-1');
    }
    ['1','2','3'].forEach((size) => {
      const btn = document.getElementById(`roomSettingsTextSizeBtn${size}`);
      if (!btn) return;
      const active = String(settings.size || '1') === size;
      btn.classList.toggle('is-active', active);
      btn.setAttribute('aria-pressed', active ? 'true' : 'false');
      const label = active ? `Dimensione testo ${size} attiva. Tap per confermare, pressione lunga per colore` : `Dimensione testo ${size}. Tap per attivare, pressione lunga per colore`;
      btn.setAttribute('aria-label', label);
      btn.title = label;
      __roomSettingsTextButtonVisualApply__(btn, size, fallbackVisual);
    });
  }catch(_){ }
}

function bindRoomSettingsTextControls(){
  const bindControl = (btn, key, onTap) => {
    if (!btn || btn.__boundRoomSettingsTextControl) return;
    btn.__boundRoomSettingsTextControl = true;
    let holdTimer = null;
    let holdTriggered = false;
    let touchAt = 0;
    const clearHold = () => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      holdTimer = null;
    };
    const doTap = () => {
      try{ __sfxTap(); }catch(_){ }
      onTap();
    };
    const doLong = async () => {
      try{ __sfxGlass(); }catch(_){ }
      holdTriggered = true;
      await __openRoomSettingsTextButtonColorPicker__(key);
    };
    btn.addEventListener('touchstart', (e) => {
      touchAt = Date.now();
      holdTriggered = false;
      clearHold();
      holdTimer = setTimeout(() => { doLong(); }, 500);
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, { passive:false, capture:true });
    btn.addEventListener('touchend', (e) => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      if (!holdTriggered) doTap();
      clearHold();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
      setTimeout(()=>{ holdTriggered = false; },0);
    }, { passive:false, capture:true });
    btn.addEventListener('touchcancel', (e) => {
      clearHold();
      holdTriggered = false;
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, { passive:false, capture:true });
    btn.addEventListener('click', (e) => {
      if (Date.now() - touchAt < 450) { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } return; }
      if (holdTriggered){ try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } holdTriggered = false; return; }
      doTap();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, true);
    btn.addEventListener('contextmenu', (e) => { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } });
    btn.addEventListener('mousedown', (e) => {
      if ((e.button || 0) !== 0) return;
      holdTriggered = false;
      clearHold();
      holdTimer = setTimeout(() => { doLong(); }, 500);
    });
    ['mouseup','mouseleave'].forEach((evt) => btn.addEventListener(evt, () => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      clearHold();
    }));
  };
  const boldBtn = document.getElementById('roomSettingsTextBoldBtn');
  bindControl(boldBtn, 'bold', () => {
    const current = getAppTextUiSettings();
    setAppTextUiSettings({ bold: !current.bold, size: current.size });
  });
  ['1','2','3'].forEach((size) => {
    const btn = document.getElementById(`roomSettingsTextSizeBtn${size}`);
    bindControl(btn, size, () => {
      const current = getAppTextUiSettings();
      setAppTextUiSettings({ bold: current.bold, size });
    });
  });
}

function initAppTextUiObserver(){
  try{
    if (__appTextUiMutationObserver__ || !document.body) return;
    __appTextUiMutationObserver__ = new MutationObserver((mutations) => {
      try{
        const roots = [];
        mutations.forEach((mutation) => {
          mutation.addedNodes && mutation.addedNodes.forEach((node) => {
            if (node instanceof Element) roots.push(node);
          });
        });
        if (!roots.length) return;
        scheduleApplyAppTextUi(document.body);
      }catch(_){ }
    });
    __appTextUiMutationObserver__.observe(document.body, { childList:true, subtree:true });
  }catch(_){ }
}


function __statFiscalBtnVisualDefaultState__(isSocieta){
  return isSocieta
    ? { bg:'#2b7cb4', border:'#2b7cb4', fg:'#ffffff', opacity:0.80 }
    : { bg:'#d6dee8', border:'#d6dee8', fg:'#0f172a', opacity:0.80 };
}

function __statFiscalBtnVisualRead__(){
  const fallback = { on: __statFiscalBtnVisualDefaultState__(true), off: __statFiscalBtnVisualDefaultState__(false) };
  try{
    const raw = localStorage.getItem(__STAT_FISCAL_BTN_VISUAL_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return {
      on: __tagColorPairFromValue__(parsed?.on || fallback.on, fallback.on.bg),
      off: __tagColorPairFromValue__(parsed?.off || fallback.off, fallback.off.bg)
    };
  }catch(_){ return fallback; }
}

function __statFiscalBtnVisualWrite__(visuals){
  const current = __statFiscalBtnVisualRead__();
  const next = {
    on: __tagColorPairFromValue__(visuals?.on || current.on, (visuals?.on && visuals.on.bg) || current.on.bg),
    off: __tagColorPairFromValue__(visuals?.off || current.off, (visuals?.off && visuals.off.bg) || current.off.bg)
  };
  try{ localStorage.setItem(__STAT_FISCAL_BTN_VISUAL_KEY__, JSON.stringify(next)); }catch(_){ }
  return next;
}

function __applyStatFiscalModeButtonVisual__(){
  try{
    const isSocieta = getStatFiscalMode() === 'societa';
    const visuals = __statFiscalBtnVisualRead__();
    const pair = isSocieta ? visuals.on : visuals.off;
    const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    const bgHex = __graphColorValueToHex__(pair?.bg || (isSocieta ? '#2b7cb4' : '#d6dee8'), isSocieta ? '#2b7cb4' : '#d6dee8');
    const borderHex = __graphColorValueToHex__(pair?.border || pair?.bg || bgHex, bgHex);
    const textHex = isDark
      ? __darkModeReadableTextHex__(__graphColorValueToHex__(pair?.fg || __tagColorTextHex__(pair?.bg || bgHex, pair?.fg || '', false), bgHex), isSocieta ? '#f8fbff' : '#e2e8f0')
      : __tagColorTextHex__(pair?.bg || bgHex, pair?.fg || '', false);
    const opacity = __designBgOpacityNormalize__(pair?.opacity ?? 0.80);
    ['statFiscalModeBtn','statAmmFiscalModeBtn'].forEach((id)=>{
      const btn = document.getElementById(id);
      if (!btn) return;
      btn.classList.toggle('is-active', isSocieta);
      btn.classList.toggle('is-inactive', !isSocieta);
      btn.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('border', `1px solid ${borderHex}`, 'important');
      btn.style.setProperty('color', textHex, 'important');
      btn.style.setProperty('-webkit-text-fill-color', textHex, 'important');
      btn.setAttribute('aria-pressed', isSocieta ? 'true' : 'false');
      __applyVisualTextWeight__(btn, pair);
    });
    const startupBtn = document.getElementById('statAmmStartupRateBtn');
    if (startupBtn){
      const startupOn = getStatForfettarioStartup5();
      const startupPair = startupOn ? visuals.on : visuals.off;
      const startupBg = __graphColorValueToHex__(startupPair?.bg || (startupOn ? '#2b7cb4' : '#d6dee8'), startupOn ? '#2b7cb4' : '#d6dee8');
      const startupBorder = __graphColorValueToHex__(startupPair?.border || startupPair?.bg || startupBg, startupBg);
      const startupText = isDark
        ? __darkModeReadableTextHex__(__graphColorValueToHex__(startupPair?.fg || __tagColorTextHex__(startupPair?.bg || startupBg, startupPair?.fg || '', false), startupBg), startupOn ? '#f8fbff' : '#e2e8f0')
        : __tagColorTextHex__(startupPair?.bg || startupBg, startupPair?.fg || '', false);
      const startupOpacity = __designBgOpacityNormalize__(startupPair?.opacity ?? 0.80);
      startupBtn.classList.toggle('is-active', startupOn);
      startupBtn.classList.toggle('is-inactive', !startupOn);
      startupBtn.style.setProperty('background', hexToRgba(startupBg, startupOpacity), 'important');
      startupBtn.style.setProperty('background-color', hexToRgba(startupBg, startupOpacity), 'important');
      startupBtn.style.setProperty('border', `1px solid ${startupBorder}`, 'important');
      startupBtn.style.setProperty('color', startupText, 'important');
      startupBtn.style.setProperty('-webkit-text-fill-color', startupText, 'important');
      __applyVisualTextWeight__(startupBtn, startupPair);
    }
  }catch(_){ }
}

function __openStatFiscalModeButtonColorPicker__(){
  const isSocieta = getStatFiscalMode() === 'societa';
  const visuals = __statFiscalBtnVisualRead__();
  const stateKey = isSocieta ? 'on' : 'off';
  const initial = __tagColorPairFromValue__(visuals[stateKey], visuals[stateKey]?.bg || (isSocieta ? '#2b7cb4' : '#d6dee8'));
  __tagColorPopupOpen__('stat-fiscal-mode-btn', initial, (payload) => {
    const current = __statFiscalBtnVisualRead__();
    const pair = __tagColorPairFromValue__(current[stateKey] || initial, initial.bg || '#2b7cb4');
    const payloadColors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    pair.bg = __parseOperatoreColorSpec__(payloadColors.bg || pair.bg || initial.bg || '#2b7cb4').spec;
    pair.border = __parseOperatoreColorSpec__(payloadColors.border || pair.border || pair.bg || initial.bg || '#2b7cb4').spec;
    pair.fg = String(payloadColors.fg || '').trim() ? __parseOperatoreColorSpec__(payloadColors.fg).spec : '';
    pair.opacity = __designBgOpacityNormalize__(payload?.opacity ?? pair.opacity ?? initial.opacity ?? 0.80);
    current[stateKey] = { bg: pair.bg, border: pair.border || pair.bg, fg: pair.fg || '', opacity: pair.opacity };
    __statFiscalBtnVisualWrite__(current);
    __applyStatFiscalModeButtonVisual__();
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:__designBgOpacityNormalize__(initial.opacity ?? 0.80), defaultMode:'bg', fallbackBg:initial.bg || (isSocieta ? '#2b7cb4' : '#d6dee8') });
}

function getStatFiscalMode(){
  try{
    const current = String(state?.fiscalRegime || "").trim().toLowerCase();
    if (current === "forfettario" || current === "societa") return current;
  }catch(_){ }
  try{
    const stored = String(localStorage.getItem(LS_STAT_FISCAL_MODE) || "").trim().toLowerCase();
    if (stored === "forfettario" || stored === "societa") {
      state.fiscalRegime = stored;
      return stored;
    }
  }catch(_){ }
  state.fiscalRegime = "societa";
  return "societa";
}

function updateStatFiscalModeUI(){
  const mode = getStatFiscalMode();
  const isSocieta = mode === "societa";
  try{
    const btn = document.getElementById("statFiscalModeBtn");
    if (btn) {
      btn.setAttribute("aria-pressed", isSocieta ? "true" : "false");
      btn.setAttribute("aria-label", isSocieta ? "Regime società" : "Regime forfettario");
      btn.title = isSocieta ? "Regime società" : "Regime forfettario";
    }
  }catch(_){ }
  try{
    const label = document.getElementById("statFiscalModeLabel");
    if (label) label.textContent = isSocieta ? "Società" : "Forf.";
    const labelAmm = document.getElementById("statAmmFiscalModeLabel");
    if (labelAmm) labelAmm.textContent = isSocieta ? "Società" : "Forf.";
    const startup = getStatForfettarioStartup5();
    const startupBtn = document.getElementById("statAmmStartupRateBtn");
    const startupLabel = document.getElementById("statAmmStartupRateLabel");
    if (startupBtn){
      startupBtn.hidden = isSocieta;
      startupBtn.setAttribute("aria-hidden", isSocieta ? "true" : "false");
      startupBtn.setAttribute("aria-pressed", startup ? "true" : "false");
      startupBtn.setAttribute("aria-label", startup ? "Aliquota startup 5% attiva" : "Aliquota ordinaria forfettario 15% attiva");
      startupBtn.title = startup ? "Startup 5%" : "Forfettario 15%";
    }
    if (startupLabel) startupLabel.textContent = startup ? "5%" : "15%";
    const adminBtn = document.getElementById("btnAdminInputsTop");
    if (adminBtn){
      adminBtn.hidden = !isSocieta;
      adminBtn.setAttribute("aria-hidden", !isSocieta ? "true" : "false");
    }
  }catch(_){ }
  try{ __applyStatFiscalModeButtonVisual__(); }catch(_){ }
}

function setStatFiscalMode(mode, opts){
  const next = String(mode || "").trim().toLowerCase() === "forfettario" ? "forfettario" : "societa";
  state.fiscalRegime = next;
  try{ localStorage.setItem(LS_STAT_FISCAL_MODE, next); }catch(_){ }
  updateStatFiscalModeUI();
  if (!(opts && opts.skipRender)) {
    if (state.page === "statgen") { try{ renderStatGen(); }catch(_){ } }
    if (state.page === "statamministratore") { try{ renderStatAmministratore(); }catch(_){ } }
  }
}

function bindStatFiscalModeToggle(){
  try{
    const btn = document.getElementById("statFiscalModeBtn");
    if (!btn || btn.__boundFiscalMode) {
      updateStatFiscalModeUI();
      return;
    }
    btn.__boundFiscalMode = true;
    let longPressTimer = null;
    let longPressFired = false;
    const clearLong = ()=>{ if (longPressTimer){ clearTimeout(longPressTimer); longPressTimer = null; } };
    const block = (e)=>{ try{ e && e.preventDefault && e.preventDefault(); }catch(_){ } try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){ } return false; };
    const startLong = (e)=>{
      try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
      longPressFired = false;
      clearLong();
      longPressTimer = setTimeout(()=>{
        longPressFired = true;
        __openStatFiscalModeButtonColorPicker__();
      }, 500);
    };
    const stopLong = (e)=>{
      clearLong();
      if (longPressFired){
        block(e);
        setTimeout(()=>{ longPressFired = false; }, 0);
        return;
      }
    };
    btn.addEventListener('click', (e) => {
      if (longPressFired) return block(e);
      setStatFiscalMode(getStatFiscalMode() === 'societa' ? 'forfettario' : 'societa');
    });
    ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt, startLong, { passive:true }); }catch(_){ } });
    ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt, stopLong, { passive:false }); }catch(_){ } });
    try{ btn.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
    updateStatFiscalModeUI();
  }catch(_){ }
}

function _guestHasFiscalReceipt(g){
  try{
    if (_isRicevutaFlag(g, "acconto")) return true;
    if (_isRicevutaFlag(g, "saldo")) return true;
  }catch(_){ }
  return false;
}


// Loader globale (gestisce richieste parallele + anti-flicker)
const loadingState = {
  requestCount: 0,
  showTimer: null,
  hideTimer: null,
  shownAt: 0,
  isVisible: false,
  delayMs: 800,       // evita loader per richieste brevi
  minVisibleMs: 450,  // se compare, resta un minimo (evita “lampeggi”)
  hideGraceMs: 260,   // unisce richieste sequenziali (evita molte comparse)
};


// ===== Alert LED ospiti (topbar) =====
const __guestAlertState = { timer: null, lastTick: 0 };

function _guestAlertDismissKey(side){
  const uid = (state && state.session && state.session.user_id) ? String(state.session.user_id) : 'anon';
  const year = (state && state.exerciseYear) ? String(state.exerciseYear) : 'year';
  return `dDAE_guest_alert_dismissed_${uid}_${year}_${side}`;
}
function _readGuestAlertDismissed(side){
  try{
    const raw = localStorage.getItem(_guestAlertDismissKey(side));
    const arr = JSON.parse(raw || '[]');
    return Array.isArray(arr) ? new Set(arr.map(x => String(x || '').trim()).filter(Boolean)) : new Set();
  }catch(_){ return new Set(); }
}
function _writeGuestAlertDismissed(side, set){
  try{ localStorage.setItem(_guestAlertDismissKey(side), JSON.stringify(Array.from(set || []))); }catch(_){ }
}
function dismissGuestAlert(side, guestId){
  const id = String(guestId || '').trim();
  if (!id) return;
  const set = _readGuestAlertDismissed(side);
  set.add(id);
  _writeGuestAlertDismissed(side, set);
  try{ refreshTopGuestAlerts({ force:true, keepModal:true }); }catch(_){ }
}
function clearDismissedGuestAlert(side, guestId){
  const id = String(guestId || '').trim();
  if (!id) return;
  const set = _readGuestAlertDismissed(side);
  if (set.delete(id)) _writeGuestAlertDismissed(side, set);
}

function __guestRegistrationExtraStoreKey__(kind){
  try{
    const uid = (state && state.session && state.session.user_id) ? String(state.session.user_id) : 'anon';
    const year = (state && state.exerciseYear) ? String(state.exerciseYear) : 'year';
    const k = String(kind || '').toLowerCase() === 'istat' ? 'istat' : 'ps';
    return `dDAE_guest_registration_extra_${uid}_${year}_${k}`;
  }catch(_){ return `dDAE_guest_registration_extra_anon_year_${String(kind||'ps')}`; }
}
function __readGuestRegistrationExtraStore__(kind){
  try{
    const raw = localStorage.getItem(__guestRegistrationExtraStoreKey__(kind));
    const obj = JSON.parse(raw || '{}');
    return (obj && typeof obj === 'object' && !Array.isArray(obj)) ? obj : {};
  }catch(_){ return {}; }
}
function __writeGuestRegistrationExtraStore__(kind, obj){
  try{ localStorage.setItem(__guestRegistrationExtraStoreKey__(kind), JSON.stringify(obj || {})); }catch(_){ }
}
function __setGuestRegistrationExtraStore__(guestId, kind, info){
  try{
    const id = String(guestId || '').trim();
    if (!id) return;
    const obj = __readGuestRegistrationExtraStore__(kind);
    obj[id] = { from:String(info?.from || ''), to:String(info?.to || ''), text:String(info?.text || '') };
    __writeGuestRegistrationExtraStore__(kind, obj);
  }catch(_){ }
}
function __clearGuestRegistrationExtraStore__(guestId, kind){
  try{
    const id = String(guestId || '').trim();
    if (!id) return;
    const obj = __readGuestRegistrationExtraStore__(kind);
    if (Object.prototype.hasOwnProperty.call(obj, id)){
      delete obj[id];
      __writeGuestRegistrationExtraStore__(kind, obj);
    }
  }catch(_){ }
}
function __guestRegistrationExtraDatesList__(fromISO, toISO){
  try{
    const from = formatISODateLocal(fromISO);
    const to = formatISODateLocal(toISO);
    if (!from || !to || !/^\d{4}-\d{2}-\d{2}$/.test(from) || !/^\d{4}-\d{2}-\d{2}$/.test(to)) return [];
    if (to <= from) return [];
    const out = [];
    let cur = from;
    let guard = 0;
    while (cur && cur < to && guard < 62){
      out.push(cur);
      cur = __guestDateAddDays__(cur, 1);
      guard++;
    }
    return out;
  }catch(_){ return []; }
}
function __guestRegistrationExtraDatesText__(fromISO, toISO, fallbackText){
  try{
    const fb = String(fallbackText || '').trim();
    const dates = __guestRegistrationExtraDatesList__(fromISO, toISO);
    if (dates.length){
      return dates.map(d => formatLongDateIT(d) || d).filter(Boolean).join(', ');
    }
    if (fb) return fb;
    const from = formatISODateLocal(fromISO);
    const to = formatISODateLocal(toISO);
    const range = formatRangeCompactIT(from, to);
    return range || '';
  }catch(_){ return String(fallbackText || '').trim(); }
}
function __guestRegistrationExtraInfo__(g, kind){
  try{
    const k = String(kind || '').toLowerCase() === 'istat' ? 'istat' : 'ps';
    const commonFrom = g?.registrazione_extra_da ?? g?.registrazioneExtraDa ?? g?.registrazione_extra_from ?? g?.registration_extra_from ?? '';
    const commonTo = g?.registrazione_extra_a ?? g?.registrazioneExtraA ?? g?.registrazione_extra_to ?? g?.registration_extra_to ?? '';
    const commonDates = g?.registrazione_extra_date ?? g?.registrazioneExtraDate ?? g?.registration_extra_dates ?? '';
    const from = formatISODateLocal(
      k === 'istat'
        ? (g?.istat_registrazione_extra_da ?? g?.istatRegistrazioneExtraDa ?? g?.istat_registration_extra_from ?? g?.istatExtraFrom ?? commonFrom)
        : (g?.ps_registrazione_extra_da ?? g?.psRegistrazioneExtraDa ?? g?.ps_registration_extra_from ?? g?.psExtraFrom ?? commonFrom)
    );
    const to = formatISODateLocal(
      k === 'istat'
        ? (g?.istat_registrazione_extra_a ?? g?.istatRegistrazioneExtraA ?? g?.istat_registration_extra_to ?? g?.istatExtraTo ?? commonTo)
        : (g?.ps_registrazione_extra_a ?? g?.psRegistrazioneExtraA ?? g?.ps_registration_extra_to ?? g?.psExtraTo ?? commonTo)
    );
    const datesText = String(
      k === 'istat'
        ? (g?.istat_registrazione_extra_date ?? g?.istatRegistrazioneExtraDate ?? g?.istat_registration_extra_dates ?? g?.istatExtraDates ?? commonDates)
        : (g?.ps_registrazione_extra_date ?? g?.psRegistrazioneExtraDate ?? g?.ps_registration_extra_dates ?? g?.psExtraDates ?? commonDates)
    || '').trim();
    let text = __guestRegistrationExtraDatesText__(from, to, datesText);
    let outFrom = from;
    let outTo = to;
    if (!text){
      try{
        const gid = guestIdOf(g) || g?.id || '';
        const stored = gid ? (__readGuestRegistrationExtraStore__(k)[String(gid)] || null) : null;
        if (stored){
          outFrom = formatISODateLocal(stored.from || '') || outFrom;
          outTo = formatISODateLocal(stored.to || '') || outTo;
          text = __guestRegistrationExtraDatesText__(outFrom, outTo, stored.text || '');
        }
      }catch(_){ }
    }
    return { from: outFrom, to: outTo, text, has: !!text };
  }catch(_){ return { from:'', to:'', text:'', has:false }; }
}
function __applyGuestRegistrationExtensionOnPayload__(payload, original){
  try{
    if (!payload || !original) return;
    const oldOut = formatISODateLocal(original?.check_out ?? original?.checkOut ?? original?.checkout ?? original?.data_check_out ?? '');
    const newOut = formatISODateLocal(payload?.check_out ?? payload?.checkOut ?? '');
    if (!oldOut || !newOut || !/^\d{4}-\d{2}-\d{2}$/.test(oldOut) || !/^\d{4}-\d{2}-\d{2}$/.test(newOut) || newOut <= oldOut){
      if (truthy(payload.ps_registrato ?? payload.psRegistrato)){
        payload.ps_registrazione_extra_da = '';
        payload.ps_registrazione_extra_a = '';
        payload.ps_registrazione_extra_date = '';
        try{ __clearGuestRegistrationExtraStore__(payload.id, 'ps'); }catch(_){ }
      }
      if (truthy(payload.istat_registrato ?? payload.istatRegistrato)){
        payload.istat_registrazione_extra_da = '';
        payload.istat_registrazione_extra_a = '';
        payload.istat_registrazione_extra_date = '';
        try{ __clearGuestRegistrationExtraStore__(payload.id, 'istat'); }catch(_){ }
      }
      return;
    }
    const extraText = __guestRegistrationExtraDatesText__(oldOut, newOut, '');
    const oldPs = truthy(original?.ps_registrato ?? original?.psRegistrato);
    const oldIstat = truthy(original?.istat_registrato ?? original?.istatRegistrato);
    const nowPs = truthy(payload.ps_registrato ?? payload.psRegistrato);
    const nowIstat = truthy(payload.istat_registrato ?? payload.istatRegistrato);
    if (oldPs && nowPs){
      payload.ps_registrato = '';
      payload.psRegistrato = '';
      payload.ps_registrazione_extra_da = oldOut;
      payload.ps_registrazione_extra_a = newOut;
      payload.ps_registrazione_extra_date = extraText;
      try{ __setGuestRegistrationExtraStore__(payload.id, 'ps', { from: oldOut, to: newOut, text: extraText }); }catch(_){ }
      try{ state.guestPSRegistered = false; }catch(_){ }
    }
    if (oldIstat && nowIstat){
      payload.istat_registrato = '';
      payload.istatRegistrato = '';
      payload.istat_registrazione_extra_da = oldOut;
      payload.istat_registrazione_extra_a = newOut;
      payload.istat_registrazione_extra_date = extraText;
      try{ __setGuestRegistrationExtraStore__(payload.id, 'istat', { from: oldOut, to: newOut, text: extraText }); }catch(_){ }
      try{ state.guestISTATRegistered = false; }catch(_){ }
    }
    if (nowPs && !(oldPs && nowPs)){
      payload.ps_registrazione_extra_da = '';
      payload.ps_registrazione_extra_a = '';
      payload.ps_registrazione_extra_date = '';
      try{ __clearGuestRegistrationExtraStore__(payload.id, 'ps'); }catch(_){ }
    }
    if (nowIstat && !(oldIstat && nowIstat)){
      payload.istat_registrazione_extra_da = '';
      payload.istat_registrazione_extra_a = '';
      payload.istat_registrazione_extra_date = '';
      try{ __clearGuestRegistrationExtraStore__(payload.id, 'istat'); }catch(_){ }
    }
  }catch(_){ }
}
function _guestStayFinancials(g){
  const total = money(g?.importo_prenotazione ?? g?.importo_prenota ?? g?.total ?? g?.importo_booking ?? 0);
  const services = money(g?.servizi_totale ?? g?.serviziTotal ?? g?.importo_servizi ?? 0);
  const dep = money(g?.acconto_importo ?? g?.accontoImporto ?? g?.deposit ?? 0);
  const saldo = money(g?.saldo_pagato ?? g?.saldoPagato ?? g?.saldo ?? 0);
  const discount = money(g?.sconto ?? g?.discount ?? g?.sconto_importo ?? g?.scontoImporto ?? 0);
  const remainingRaw = (total + services) - discount - dep - saldo;
  const remaining = Math.max(0, Math.round((isFinite(remainingRaw) ? remainingRaw : 0) * 100) / 100);
  return { total, services, discount, dep, saldo, remaining };
}
function _guestReceiptMissingNow(g){
  const missing = [];
  const dep = _num(g?.acconto_importo ?? g?.accontoImporto ?? 0);
  const depType = (g?.acconto_tipo ?? g?.accontoTipo ?? '');
  if (dep > 0 && _isElectronicTypeStr_(depType) && !_isRicevutaFlag(g, 'acconto')) {
    missing.push(`Acconto elettronico senza ricevuta — Acconto da registrare: ${euro(dep)}`);
  }
  const saldo = _num(g?.saldo_pagato ?? g?.saldoPagato ?? g?.saldo ?? 0);
  const saldoType = (g?.saldo_tipo ?? g?.saldoTipo ?? '');
  if (saldo > 0 && _isElectronicTypeStr_(saldoType) && !_isRicevutaFlag(g, 'saldo')) {
    missing.push(`Saldo elettronico senza ricevuta — Saldo da registrare: ${euro(saldo)}`);
  }
  return missing;
}

function __guestCheckInDone__(g){
  try{
    return truthy(g?.checkin_effettuato ?? g?.checkInEffettuato ?? g?.check_in_effettuato ?? g?.checkInDone ?? g?.checkinDone ?? g?.check_in_done ?? g?.checked_in ?? g?.checkedIn ?? g?.arrivato ?? g?.arrived);
  }catch(_){ return false; }
}

function __guestCheckInExpectedToday__(g){
  try{
    const todayDay = _dayNumFromISO(todayISO());
    const checkInDay = _dayNumFromISO(g?.check_in ?? g?.checkIn ?? g?.arrivo ?? g?.dataArrivo ?? '');
    return todayDay != null && checkInDay != null && todayDay === checkInDay && !__guestCheckInDone__(g);
  }catch(_){ return false; }
}

function __guestGroupCheckInExpectedToday__(guest){
  try{
    const rows = (Array.isArray(guest?._groupBookings) && guest._groupBookings.length) ? guest._groupBookings : (guest ? [guest] : []);
    return rows.some(g => __guestCheckInExpectedToday__(g));
  }catch(_){ return false; }
}

function __syncGuestCheckInButton__(){
  try{
    const btn = document.querySelector('#ospiteHdActions [data-guest-checkin]');
    if (!btn) return;
    const needsActiveGroup = String(state.guestMode || '') === 'view';
    const active = !needsActiveGroup || !!String(state.guestGroupActiveId || '').trim();
    const on = active && !!state.guestCheckInDone;
    btn.disabled = !active;
    btn.classList.toggle('is-disabled', !active);
    btn.classList.toggle('is-on', on);
    btn.classList.toggle('is-off', active && !on);
    btn.setAttribute('aria-disabled', active ? 'false' : 'true');
    btn.setAttribute('aria-pressed', on ? 'true' : 'false');
    btn.setAttribute('title', !active ? 'Seleziona prima un gruppo stanze' : (on ? 'Check-in effettuato' : 'Check-in non effettuato'));
    btn.setAttribute('aria-label', !active ? 'Check-in non attivo: seleziona un gruppo stanze' : (on ? 'Check-in effettuato' : 'Check-in non effettuato'));
  }catch(_){ }
}

function _guestPaymentMissingNow(g){
  try{
    const fin = _guestStayFinancials(g);
    if (!(isFinite(fin.remaining) && fin.remaining > 0.0001)) return false;
    // dDAE_2.755 — Alert pagamento: subito dopo check-in effettuato
    // e finché la rimanenza da pagare non torna a zero.
    return __guestCheckInDone__(g);
  }catch(_){
    return false;
  }
}

function __guestCardAlertFlags__(guest){
  const rows = (Array.isArray(guest?._groupBookings) && guest._groupBookings.length) ? guest._groupBookings : (guest ? [guest] : []);
  const now = Date.now();
  const twelveHoursMs = 12 * 60 * 60 * 1000;
  const todayDay = _dayNumFromISO(todayISO());
  const flags = { ps:false, istat:false, payment:false, receipt:false };
  try{
    rows.forEach((g) => {
      if (!g) return;
      const checkInTs = parseDateTs(g?.check_in ?? g?.checkIn ?? g?.arrivo ?? g?.dataArrivo ?? '');
      const psReg = truthy(g?.ps_registrato ?? g?.psRegistrato);
      const istatReg = truthy(g?.istat_registrato ?? g?.istatRegistrato);
      const checkInDone = __guestCheckInDone__(g);
      if (checkInDone){
        if (!psReg) flags.ps = true;
        if (!istatReg) flags.istat = true;
      }

      if (_guestPaymentMissingNow(g)){
        flags.payment = true;
      }
      if (_guestReceiptMissingNow(g).length > 0){
        flags.receipt = true;
      }
    });
  }catch(_){ }
  return flags;
}
function buildGuestCardAlertLedsHTML(guest){
  const f = __guestCardAlertFlags__(guest);
  const led = (on, cls, labelOn, labelOff) => `<span class="guest-card-alert-led top-led top-led-alert ${on ? cls : 'is-off'}" aria-label="${escapeHtml(on ? labelOn : labelOff)}" title="${escapeHtml(on ? labelOn : labelOff)}"></span>`;
  return `<div class="guest-card-alert-leds" aria-label="Alert ospite">
    ${led(!!f.ps, 'is-black', 'Polizia di Stato mancante', 'Polizia di Stato ok')}
    ${led(!!f.istat, 'is-sky', 'ISTAT mancante', 'ISTAT ok')}
    ${led(!!f.payment, 'is-yellow', 'Pagamento mancante', 'Pagamento ok')}
    ${led(!!f.receipt, 'is-red', 'Ricevuta mancante', 'Ricevuta ok')}
  </div>`;
}
function computeTopGuestAlerts(guests){
  const now = Date.now();
  const twelveHoursMs = 12 * 60 * 60 * 1000;
  const twentyFourHoursMs = 24 * 60 * 60 * 1000;
  const leftDismissed = _readGuestAlertDismissed('left');
  const rightDismissed = _readGuestAlertDismissed('right');
  const left = [];
  const right = [];
  const src = Array.isArray(guests) ? guests : [];
  for (const g of src){
    const guestId = guestIdOf(g);
    if (!guestId) continue;
    const rawName = g?.nome ?? g?.name ?? '';
    const name = collapseSpaces(String(rawName || '').trim()) || 'Prenotazione';
    const checkInTs = parseDateTs(g?.check_in ?? g?.checkIn ?? g?.arrivo ?? g?.dataArrivo ?? '');
    const checkOutTs = parseDateTs(g?.check_out ?? g?.checkOut ?? g?.checkout ?? g?.data_check_out ?? '');
    const psReg = truthy(g?.ps_registrato ?? g?.psRegistrato);
    const istatReg = truthy(g?.istat_registrato ?? g?.istatRegistrato);
    const checkInDone = __guestCheckInDone__(g);

    if (checkInDone){
      const missingPs = !psReg;
      const missingIstat = !istatReg;
      const psExtra = __guestRegistrationExtraInfo__(g, 'ps');
      const istatExtra = __guestRegistrationExtraInfo__(g, 'istat');
      if (missingPs || missingIstat){
        if (!leftDismissed.has(guestId)){
          left.push({
            id: guestId,
            name,
            guest: g,
            details: [
              missingPs ? (psExtra.has ? `Registrazione Polizia mancante per date extra: ${psExtra.text}` : 'Registrazione Polizia mancante') : '',
              missingIstat ? (istatExtra.has ? `Registrazione ISTAT mancante per date extra: ${istatExtra.text}` : 'Registrazione ISTAT mancante') : ''
            ].filter(Boolean),
            mode: (missingPs && missingIstat) ? 'dual' : (missingPs ? 'black' : 'sky'),
            tags: [
              missingPs ? { label: 'Polizia', cls: 'tag-black' } : null,
              missingIstat ? { label: 'ISTAT', cls: 'tag-sky' } : null
            ].filter(Boolean),
            checkInTs,
            checkOutTs
          });
        }
      }else{
        clearDismissedGuestAlert('left', guestId);
      }
    }else if (psReg && istatReg){
      clearDismissedGuestAlert('left', guestId);
    }

    const fin = _guestStayFinancials(g);
    const receiptMissing = _guestReceiptMissingNow(g);
    const paymentMissing = _guestPaymentMissingNow(g);
    const receiptAlert = receiptMissing.length > 0;
    const canDismissRightAlert = !paymentMissing && rightDismissed.has(guestId);
    if ((paymentMissing || receiptAlert) && !canDismissRightAlert){
      right.push({
        id: guestId,
        name,
        guest: g,
        details: [
          paymentMissing ? `Pagamento mancante — Rimanenza da pagare: ${euro(fin.remaining)}` : '',
          ...receiptMissing
        ].filter(Boolean),
        mode: (paymentMissing && receiptAlert) ? 'dual' : (receiptAlert ? 'red' : 'yellow'),
        tags: [
          paymentMissing ? { label: 'Pagamento', cls: 'tag-yellow' } : null,
          receiptAlert ? { label: 'Ricevuta', cls: 'tag-red' } : null
        ].filter(Boolean),
        checkInTs,
        checkOutTs
      });
    }else{
      clearDismissedGuestAlert('right', guestId);
    }
  }
  left.sort((a,b) => (a.checkInTs || 0) - (b.checkInTs || 0));
  right.sort((a,b) => {
    const ta = (a.checkOutTs == null) ? 1e18 : Number(a.checkOutTs);
    const tb = (b.checkOutTs == null) ? 1e18 : Number(b.checkOutTs);
    return ta - tb;
  });
  return { left, right };
}
let __topGuestDualPhase = 0;
let __topGuestDualTimer = null;
function paintTopGuestDualLed(side){
  const el = document.getElementById(side === 'right' ? 'dbLedWrite' : 'dbLedRead');
  if (!el) return;
  el.classList.remove('is-black','is-sky','is-yellow','is-red','is-blink');
  if (side === 'left'){
    el.classList.add(__topGuestDualPhase ? 'is-sky' : 'is-black');
  }else{
    el.classList.add(__topGuestDualPhase ? 'is-red' : 'is-yellow');
  }
}
function syncTopGuestDualTimer(){
  if (__topGuestDualTimer){ clearInterval(__topGuestDualTimer); __topGuestDualTimer = null; }
  __topGuestDualPhase = 0;
  return;
}
function __setTopLedState__(id, active, cls, label){
  const el = document.getElementById(id);
  if (!el) return;
  el.classList.remove('is-off','is-black','is-sky','is-yellow','is-red','is-blink','is-dual-left','is-dual-right');
  if (active){
    el.classList.add(cls);
  }else{
    el.classList.add('is-off');
  }
  try{ el.setAttribute('aria-label', label || 'Indicatore'); }catch(_){ }
  try{ el.setAttribute('title', label || ''); }catch(_){ }
}
function applyTopGuestAlertLed(side){
  const items = (state.guestAlerts && Array.isArray(state.guestAlerts[side])) ? state.guestAlerts[side] : [];
  const hasBlack = items.some(x => x.mode === 'black' || x.mode === 'dual');
  const hasSky = items.some(x => x.mode === 'sky' || x.mode === 'dual');
  const hasYellow = items.some(x => x.mode === 'yellow' || x.mode === 'dual');
  const hasRed = items.some(x => x.mode === 'red' || x.mode === 'dual');
  if (side === 'left'){
    __setTopLedState__('dbLedRead', hasBlack, 'is-black', hasBlack ? 'Schedine PS mancanti' : 'Nessun alert schedine PS');
    __setTopLedState__('dbLedIstat', hasSky, 'is-sky', hasSky ? 'ISTAT mancante' : 'Nessun alert ISTAT');
  }else{
    __setTopLedState__('dbLedWrite', hasYellow, 'is-yellow', hasYellow ? 'Pagamento mancante' : 'Nessun alert pagamenti');
    __setTopLedState__('dbLedReceipt', hasRed, 'is-red', hasRed ? 'Ricevuta mancante' : 'Nessun alert ricevute');
  }
  try{ syncTopGuestDualTimer(); }catch(_){ }
}
function updateTopGuestAlertLeds(){
  try{ applyTopGuestAlertLed('left'); }catch(_){ }
  try{ applyTopGuestAlertLed('right'); }catch(_){ }
}
function __guestAlertLedConfig__(kind){
  const k = String(kind || '').trim();
  if (k === 'istat') return { side:'left', title:'Alert ISTAT', tag:'ISTAT', tagCls:'tag-sky', detailNeedle:'ISTAT', empty:'Nessun alert ISTAT.' };
  if (k === 'payment') return { side:'right', title:'Alert pagamenti', tag:'Pagamento', tagCls:'tag-yellow', detailNeedle:'pagamento', empty:'Nessun pagamento mancante.' };
  if (k === 'receipt') return { side:'right', title:'Alert ricevute', tag:'Ricevuta', tagCls:'tag-red', detailNeedle:'ricevuta', empty:'Nessuna ricevuta mancante.' };
  if (k === 'right') return { side:'right', title:'Alert pagamenti e ricevute', tag:'', tagCls:'', detailNeedle:'', empty:'Nessun ospite in alert.' };
  if (k === 'left') return { side:'left', title:'Alert registrazioni', tag:'', tagCls:'', detailNeedle:'', empty:'Nessun ospite in alert.' };
  return { side:'left', title:'Alert schedine PS', tag:'Polizia', tagCls:'tag-black', detailNeedle:'Polizia', empty:'Nessun alert schedine PS.' };
}
function __filterGuestAlertItemsForLed__(items, cfg){
  const src = Array.isArray(items) ? items : [];
  if (!cfg || !cfg.tag) return src.map(it => ({ item: it, details: Array.isArray(it.details) ? it.details : [], tags: Array.isArray(it.tags) ? it.tags : [] }));
  const tagLc = String(cfg.tag || '').toLowerCase();
  const needleLc = String(cfg.detailNeedle || '').toLowerCase();
  return src.map((it) => {
    const tags = (Array.isArray(it.tags) ? it.tags : []).filter(t => String(t && t.label || '').toLowerCase() === tagLc);
    const details = (Array.isArray(it.details) ? it.details : []).filter(d => {
      const x = String(d || '').toLowerCase();
      return x.includes(needleLc) || x.includes(tagLc);
    });
    return (tags.length || details.length) ? { item: it, details: details.length ? details : [cfg.title.replace(/^Alert\s+/i, '')], tags: tags.length ? tags : [{ label: cfg.tag, cls: cfg.tagCls }] } : null;
  }).filter(Boolean);
}
function openGuestAlertModal(kind){
  const modal = document.getElementById('guestAlertModal');
  const title = document.getElementById('guestAlertTitle');
  const list = document.getElementById('guestAlertList');
  if (!modal || !title || !list) return;
  const cfg = __guestAlertLedConfig__(kind);
  state.guestAlertModalSide = cfg.side;
  state.guestAlertModalKind = String(kind || (cfg.side === 'right' ? 'right' : 'ps'));
  const allItems = (state.guestAlerts && Array.isArray(state.guestAlerts[cfg.side])) ? state.guestAlerts[cfg.side] : [];
  const rows = __filterGuestAlertItemsForLed__(allItems, cfg);
  title.textContent = `${cfg.title} (${rows.length})`;
  list.innerHTML = '';
  if (!rows.length){
    const empty = document.createElement('div');
    empty.className = 'guest-alert-empty';
    empty.textContent = cfg.empty || 'Nessun ospite in alert.';
    list.appendChild(empty);
  } else {
    rows.forEach((row) => {
      const it = row.item;
      const card = document.createElement('div');
      card.className = 'guest-alert-card';
      const tagsHtml = (row.tags || []).map(tag => `<span class="guest-alert-tag ${escapeHtml(tag.cls || '')}">${escapeHtml(tag.label || '')}</span>`).join('');
      const details = (row.details || []).map(x => escapeHtml(x)).join(' · ');
      const range = formatRangeCompactIT(it.guest?.check_in ?? it.guest?.checkIn ?? '', it.guest?.check_out ?? it.guest?.checkOut ?? '');
      card.innerHTML = `<div class="guest-alert-copy"><div class="guest-alert-name">${escapeHtml(it.name)}</div><div class="guest-alert-meta">${details || 'Alert attivo'}</div>${range ? `<div class="guest-alert-meta">${escapeHtml(range)}</div>` : ''}<div class="guest-alert-tags">${tagsHtml}</div></div><button type="button" class="guest-alert-dismiss" aria-label="Nascondi alert ospite">✕</button>`;
      const closeBtn = card.querySelector('.guest-alert-dismiss');
      if (closeBtn) bindFastTap(closeBtn, () => dismissGuestAlert(cfg.side, it.id));
      list.appendChild(card);
    });
  }
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
}
function closeGuestAlertModal(){
  const modal = document.getElementById('guestAlertModal');
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
}
async function refreshTopGuestAlerts(opts){
  opts = opts || {};
  try{
    if (!(state.session && state.session.user_id)) {
      state.guestAlerts = { left: [], right: [] };
      state.guestAlertCounts = { left: 0, right: 0 };
      updateTopGuestAlertLeds();
      if (!opts.keepModal) closeGuestAlertModal();
      return;
    }
    let rows = [];
    try{
      if (!opts.force){
        const cached = Array.isArray(state.ospiti) && state.ospiti.length ? state.ospiti : (Array.isArray(state.guests) && state.guests.length ? state.guests : null);
        if (cached) rows = cached;
      }
      if (!rows.length){
        const data = await cachedGet('ospiti', {}, { showLoader:false, ttlMs: opts.force ? 0 : 45000, swrMs: 120000, force: !!opts.force });
        rows = Array.isArray(data) ? data : [];
      }
    }catch(_){ rows = []; }
    try{ rows = __guestFilterPreventiviRows__(rows, false); }catch(_){ }
    const alerts = computeTopGuestAlerts(rows);
    state.guestAlerts = alerts;
    state.guestAlertCounts = { left: alerts.left.length, right: alerts.right.length };
    updateTopGuestAlertLeds();
    if (!opts.keepModal){
      try{
        const modal = document.getElementById('guestAlertModal');
        if (modal && !modal.hidden) openGuestAlertModal(state.guestAlertModalKind || state.guestAlertModalSide || 'left');
      }catch(_){ }
    } else {
      try{
        const modal = document.getElementById('guestAlertModal');
        if (modal && !modal.hidden) openGuestAlertModal(state.guestAlertModalKind || state.guestAlertModalSide || 'left');
      }catch(_){ }
    }
  }catch(e){ console.error(e); }
}
function scheduleTopGuestAlertsRefresh(){
  try{
    if (__guestAlertState.timer) return;
    const tick = () => {
      const now = Date.now();
      if ((now - (__guestAlertState.lastTick || 0)) < 20000) return;
      __guestAlertState.lastTick = now;
      try{ refreshTopGuestAlerts({ force:false, keepModal:true }); }catch(_){ }
    };
    __guestAlertState.timer = setInterval(tick, 60000);
    window.addEventListener('focus', tick);
    document.addEventListener('visibilitychange', ()=>{ if (!document.hidden) tick(); });
    setTimeout(tick, 450);
  }catch(_){ }
}
function __syncLedUpdate(){}
function __syncLedBegin(method){}
function __syncLedEnd(method){}

function showLoading(){
  // Loader overlay disabilitato: usiamo LED sync in topbar
  try{ const ov = document.getElementById("loadingOverlay"); if (ov) ov.hidden = true; }catch(_){}
  loadingState.isVisible = false;
}

function hideLoading(){
  try{ const ov = document.getElementById("loadingOverlay"); if (ov) ov.hidden = true; }catch(_){}
  loadingState.isVisible = false;
}

function beginRequest(){
  loadingState.requestCount += 1;
  if (loadingState.requestCount !== 1) return;

  // Se stavamo per nascondere il loader, annulla: richieste ravvicinate = una sola sessione di loading
  if (loadingState.hideTimer){
    clearTimeout(loadingState.hideTimer);
    loadingState.hideTimer = null;
  }

  // Programma la comparsa dopo delayMs
  if (loadingState.showTimer) clearTimeout(loadingState.showTimer);
  loadingState.showTimer = setTimeout(() => {
    if (loadingState.requestCount > 0 && !loadingState.isVisible) {
      showLoading();
    }
  }, loadingState.delayMs);
}

function endRequest(){
  loadingState.requestCount = Math.max(0, loadingState.requestCount - 1);
  if (loadingState.requestCount !== 0) return;

  if (loadingState.showTimer) {
    clearTimeout(loadingState.showTimer);
    loadingState.showTimer = null;
  }

  // Se il SW ha chiesto un reload mentre caricavamo, fallo ora che siamo "idle"
  if (__SW_RELOAD_PENDING && !__SW_RELOADING){
    __SW_RELOAD_PENDING = false;
    // micro-delay: lascia aggiornare UI/loader
    setTimeout(() => __performSwReload(), 50);
    // non serve gestire ulteriormente il loader
    return;
  }

  // Se non è mai comparso, fine.
  if (!loadingState.isVisible) return;

  const elapsed = performance.now() - (loadingState.shownAt || performance.now());
  const minRemain = loadingState.minVisibleMs - elapsed;
  const delay = Math.max(loadingState.hideGraceMs || 0, minRemain > 0 ? minRemain : 0);

  if (loadingState.hideTimer) {
    clearTimeout(loadingState.hideTimer);
    loadingState.hideTimer = null;
  }

  loadingState.hideTimer = setTimeout(() => {
    loadingState.hideTimer = null;
    if (loadingState.requestCount === 0) hideLoading();
  }, delay);
}


const __DATE_RANGE_CALENDAR_THEME_STORAGE_KEY__ = 'dDAE_date_range_calendar_theme_v1';

function __dateRangeCalendarThemeDefault__(){
  return __launcherVisualNormalize__({ bg:'gray-1', border:'gray-2', fg:'blue-6', opacity:0.18 }, 'blue-6');
}

function __dateRangeCalendarThemeNormalize__(visual){
  const fallback = __dateRangeCalendarThemeDefault__();
  const current = __launcherVisualNormalize__(visual || {}, fallback.fg || 'blue-6');
  return {
    bg: current.bg || fallback.bg || 'gray-1',
    border: current.border || current.bg || fallback.border || fallback.bg || 'gray-2',
    fg: current.fg || fallback.fg || 'blue-6',
    opacity: __designBgOpacityNormalize__(current.opacity ?? fallback.opacity ?? 0.18)
  };
}

function __dateRangeCalendarThemeRead__(){
  try{
    const raw = localStorage.getItem(__DATE_RANGE_CALENDAR_THEME_STORAGE_KEY__);
    if (!raw) return __dateRangeCalendarThemeDefault__();
    return __dateRangeCalendarThemeNormalize__(JSON.parse(raw));
  }catch(_){ return __dateRangeCalendarThemeDefault__(); }
}

function __dateRangeCalendarThemeWrite__(visual){
  try{ localStorage.setItem(__DATE_RANGE_CALENDAR_THEME_STORAGE_KEY__, JSON.stringify(__dateRangeCalendarThemeNormalize__(visual))); }catch(_){ }
}

function __setDateRangeCalendarCssVar__(name, value){
  try{
    const root = document.documentElement;
    if (!root || !name) return;
    root.style.setProperty(name, String(value == null ? '' : value));
  }catch(_){ }
}

function __dateRangeCalendarApplyTheme__(){
  try{
    const visual = __dateRangeCalendarThemeRead__();
    const bgHex = __operatoreColorHex__(visual.bg || 'gray-1');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'gray-2');
    const fgHex = __tagColorTextHex__(visual.bg || 'gray-1', visual.fg || 'blue-6', false) || __operatoreColorHex__(visual.fg || 'blue-6');
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.18);
    const dark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    if (dark){
      const text = __darkModeReadableTextHex__(fgHex, '#f8fbff');
      const subtle = hexToRgba(text, 0.82);
      __setDateRangeCalendarCssVar__('--ddae-range-modal-bg', 'rgba(15,23,42,0.98)');
      __setDateRangeCalendarCssVar__('--ddae-range-modal-border', hexToRgba(borderHex, 0.28));
      __setDateRangeCalendarCssVar__('--ddae-range-modal-text', text);
      __setDateRangeCalendarCssVar__('--ddae-range-modal-subtle', subtle);
      __setDateRangeCalendarCssVar__('--ddae-range-nav-bg', hexToRgba(bgHex, Math.max(0.14, Math.min(0.22, opacity))));
      __setDateRangeCalendarCssVar__('--ddae-range-nav-border', hexToRgba(borderHex, 0.30));
      __setDateRangeCalendarCssVar__('--ddae-range-day-bg', hexToRgba(bgHex, Math.max(0.14, Math.min(0.22, opacity))));
      __setDateRangeCalendarCssVar__('--ddae-range-day-border', hexToRgba(borderHex, 0.26));
      __setDateRangeCalendarCssVar__('--ddae-range-day-text', text);
      __setDateRangeCalendarCssVar__('--ddae-range-day-muted', hexToRgba(text, 0.48));
      __setDateRangeCalendarCssVar__('--ddae-range-day-selected-bg', hexToRgba(fgHex, 0.42));
      __setDateRangeCalendarCssVar__('--ddae-range-day-selected-border', hexToRgba(fgHex, 0.78));
      __setDateRangeCalendarCssVar__('--ddae-range-day-selected-text', '#f8fbff');
      __setDateRangeCalendarCssVar__('--ddae-range-day-range-bg', hexToRgba(fgHex, 0.20));
      __setDateRangeCalendarCssVar__('--ddae-range-day-range-border', hexToRgba(fgHex, 0.36));
      __setDateRangeCalendarCssVar__('--ddae-range-day-range-text', '#eaf4ff');
      __setDateRangeCalendarCssVar__('--ddae-range-day-today-ring', hexToRgba(fgHex, 0.38));
      __setDateRangeCalendarCssVar__('--ddae-range-legend-bg', hexToRgba(fgHex, 0.20));
      __setDateRangeCalendarCssVar__('--ddae-range-legend-edge-bg', hexToRgba(fgHex, 0.42));
      __setDateRangeCalendarCssVar__('--ddae-range-accent', fgHex);
      __setDateRangeCalendarCssVar__('--ddae-range-field-border', hexToRgba(fgHex, 0.78));
      __setDateRangeCalendarCssVar__('--ddae-range-field-muted-border', hexToRgba(text, 0.24));
      __setDateRangeCalendarCssVar__('--ddae-range-field-bg', 'rgba(15,23,42,0.52)');
      __setDateRangeCalendarCssVar__('--ddae-range-action-cancel-bg', hexToRgba(bgHex, Math.max(0.18, Math.min(0.36, opacity))));
      __setDateRangeCalendarCssVar__('--ddae-range-action-cancel-text', text);
      __setDateRangeCalendarCssVar__('--ddae-range-action-apply-bg', fgHex);
      __setDateRangeCalendarCssVar__('--ddae-range-action-apply-text', '#f8fbff');
      return;
    }
    __setDateRangeCalendarCssVar__('--ddae-range-modal-bg', 'rgba(255,255,255,0.98)');
    __setDateRangeCalendarCssVar__('--ddae-range-modal-border', hexToRgba(borderHex, 0.16));
    __setDateRangeCalendarCssVar__('--ddae-range-modal-text', fgHex);
    __setDateRangeCalendarCssVar__('--ddae-range-modal-subtle', hexToRgba(fgHex, 0.72));
    __setDateRangeCalendarCssVar__('--ddae-range-nav-bg', hexToRgba(bgHex, Math.max(0.24, Math.min(0.72, opacity + 0.16))));
    __setDateRangeCalendarCssVar__('--ddae-range-nav-border', hexToRgba(borderHex, 0.20));
    __setDateRangeCalendarCssVar__('--ddae-range-day-bg', hexToRgba(bgHex, Math.max(0.18, Math.min(0.72, opacity + 0.10))));
    __setDateRangeCalendarCssVar__('--ddae-range-day-border', hexToRgba(borderHex, 0.14));
    __setDateRangeCalendarCssVar__('--ddae-range-day-text', fgHex);
    __setDateRangeCalendarCssVar__('--ddae-range-day-muted', hexToRgba(fgHex, 0.34));
    __setDateRangeCalendarCssVar__('--ddae-range-day-selected-bg', hexToRgba(fgHex, 0.22));
    __setDateRangeCalendarCssVar__('--ddae-range-day-selected-border', hexToRgba(fgHex, 0.46));
    __setDateRangeCalendarCssVar__('--ddae-range-day-selected-text', fgHex);
    __setDateRangeCalendarCssVar__('--ddae-range-day-range-bg', hexToRgba(fgHex, 0.10));
    __setDateRangeCalendarCssVar__('--ddae-range-day-range-border', hexToRgba(fgHex, 0.18));
    __setDateRangeCalendarCssVar__('--ddae-range-day-range-text', fgHex);
    __setDateRangeCalendarCssVar__('--ddae-range-day-today-ring', hexToRgba(fgHex, 0.30));
    __setDateRangeCalendarCssVar__('--ddae-range-legend-bg', hexToRgba(fgHex, 0.10));
    __setDateRangeCalendarCssVar__('--ddae-range-legend-edge-bg', hexToRgba(fgHex, 0.22));
    __setDateRangeCalendarCssVar__('--ddae-range-accent', fgHex);
    __setDateRangeCalendarCssVar__('--ddae-range-field-border', hexToRgba(fgHex, 0.78));
    __setDateRangeCalendarCssVar__('--ddae-range-field-muted-border', hexToRgba(borderHex, 0.34));
    __setDateRangeCalendarCssVar__('--ddae-range-field-bg', 'rgba(255,255,255,0.94)');
    __setDateRangeCalendarCssVar__('--ddae-range-action-cancel-bg', hexToRgba(bgHex, Math.max(0.18, Math.min(0.48, opacity + 0.08))));
    __setDateRangeCalendarCssVar__('--ddae-range-action-cancel-text', fgHex);
    __setDateRangeCalendarCssVar__('--ddae-range-action-apply-bg', fgHex);
    __setDateRangeCalendarCssVar__('--ddae-range-action-apply-text', '#ffffff');
  }catch(_){ }
}

function __openDateRangeCalendarThemePicker__(){
  try{
    const current = __dateRangeCalendarThemeRead__();
    const applyVisual = (payload, changed = { bg:true, border:true, fg:true, opacity:true }) => {
      const next = __applyDesignPayloadToVisual__(__dateRangeCalendarThemeRead__(), payload, changed, current.bg || 'gray-1');
      __dateRangeCalendarThemeWrite__(next);
      __dateRangeCalendarApplyTheme__();
      try{ __renderGuestDateRangeCalendar__(); }catch(_){ }
      try{ __renderLaundryDateRangeCalendar__(); }catch(_){ }
      try{ __renderSpeseDateRangeCalendar__(); }catch(_){ }
      try{ __renderLicenseDateRangeCalendar__(); }catch(_){ }
    };
    __tagColorPopupOpen__('date-range-calendar-theme', current, (payload) => { applyVisual(payload); }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:true,
      supportsOpacity:true,
      opacity:current.opacity ?? 0.18,
      defaultMode:'bg',
      fallbackBg:(current.bg || 'gray-1'),
      onPreview:(payload, changed) => { applyVisual(payload, changed || { bg:true, border:true, fg:true, opacity:true }); },
      onRevert:() => { __dateRangeCalendarThemeWrite__(current); __dateRangeCalendarApplyTheme__(); try{ __renderGuestDateRangeCalendar__(); }catch(_){ } try{ __renderLaundryDateRangeCalendar__(); }catch(_){ } try{ __renderSpeseDateRangeCalendar__(); }catch(_){ } try{ __renderLicenseDateRangeCalendar__(); }catch(_){ } }
    });
  }catch(_){ }
}

function __bindDateRangeCalendarHold__(grid){
  try{ if (!grid || grid.dataset.rangeCalendarHoldBound === '1') return; grid.dataset.rangeCalendarHoldBound = '1'; }catch(_){ if (!grid) return; }
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } try{ e.stopImmediatePropagation(); }catch(_){ } };
  const start = (e)=>{
    const btn = e?.target?.closest?.('.guest-date-range-day[data-date]');
    if (!btn) return;
    try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    fired = false;
    clear();
    timer = setTimeout(()=>{ fired = true; try{ grid.__rangeCalendarSuppressClickUntil = Date.now() + 900; }catch(_){ } __openDateRangeCalendarThemePicker__(); }, 500);
  };
  const stop = (e)=>{ clear(); if (fired){ block(e); setTimeout(()=>{ fired = false; }, 0); } };
  const swallow = (e)=>{ try{ if ((grid.__rangeCalendarSuppressClickUntil || 0) > Date.now()) block(e); }catch(_){ } };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ grid.addEventListener(evt, start, { passive:true, capture:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ grid.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ } });
  try{ grid.addEventListener('click', swallow, true); }catch(_){ }
  try{ grid.addEventListener('contextmenu', (e)=>{ const btn = e?.target?.closest?.('.guest-date-range-day[data-date]'); if (btn){ block(e); __openDateRangeCalendarThemePicker__(); } }, true); }catch(_){ }
}


function __guestDateRangeFormatDisplay__(checkIn, checkOut){
  const ci = String(checkIn || '').trim();
  const co = String(checkOut || '').trim();
  if (!ci && !co) return __translateExactText__('Seleziona date') || 'Seleziona date';
  const fmt = (v) => {
    try{
      const ts = parseDateTs(v);
      if (ts == null) return String(v || '');
      return new Date(ts).toLocaleDateString(__getCurrentLocale__(), { day:'2-digit', month:'2-digit', year:'numeric' });
    }catch(_){ return String(v || ''); }
  };
  if (ci && co) return `${fmt(ci)} - ${fmt(co)}`;
  return fmt(ci || co);
}
function __updateGuestDateRangeTrigger__(){
  try{
    const value = document.getElementById('guestDateRangeValue');
    const btn = document.getElementById('guestDateRangeTrigger');
    const ci = document.getElementById('guestCheckIn')?.value || '';
    const co = document.getElementById('guestCheckOut')?.value || '';
    if (value) value.textContent = __guestDateRangeFormatDisplay__(ci, co);
    if (btn) btn.setAttribute('aria-label', __translateExactText__('Seleziona periodo pernottamento') || 'Seleziona periodo pernottamento');
  }catch(_){ }
}
const __guestDateRangeState__ = { month:null, start:'', end:'', draftStart:'', draftEnd:'' };
function __guestDateAddDays__(iso, days){
  const ts = parseDateTs(iso);
  if (ts == null) return '';
  const d = new Date(ts);
  d.setDate(d.getDate() + Number(days || 0));
  const y = d.getFullYear();
  const m = String(d.getMonth()+1).padStart(2,'0');
  const da = String(d.getDate()).padStart(2,'0');
  return `${y}-${m}-${da}`;
}
function __guestDateRangeMonthStart__(src){
  const d = src ? new Date(src) : new Date();
  return new Date(d.getFullYear(), d.getMonth(), 1);
}
function __guestDateRangeSameDay__(a,b){ return String(a||'') === String(b||''); }
function __guestDateRangeInBetween__(iso, start, end){ return !!(iso && start && end && iso > start && iso < end); }
function __guestDateRangeWeekdayLabels__(){
  const base=['L','M','M','G','V','S','D'];
  const lang=__getAppLanguage__();
  const maps={ en:['M','T','W','T','F','S','S'], fr:['L','M','M','J','V','S','D'], de:['M','D','M','D','F','S','S'], es:['L','M','M','J','V','S','D'] };
  return maps[lang] || base;
}

function __dateRangeCalendarPrettyDate__(iso){
  try{
    const ts = parseDateTs(iso);
    if (ts == null) return '—';
    const d = new Date(ts);
    const lang = __getAppLanguage__ ? __getAppLanguage__() : 'it';
    const loc = ({ it:'it-IT', en:'en-GB', fr:'fr-FR', de:'de-DE', es:'es-ES' })[lang] || 'it-IT';
    let s = d.toLocaleDateString(loc, { weekday:'short', day:'numeric', month:'short' });
    s = String(s || '').replace(/\.$/, '').replace(',', '');
    return s;
  }catch(_){ return String(iso || '—'); }
}
function __dateRangeCalendarEnsureHeader__(modalId, startIso, endIso, leftLabel, rightLabel){
  try{
    const modal = document.getElementById(modalId);
    if (!modal) return;
    const body = modal.querySelector('.guest-date-range-modal-body');
    if (!body) return;
    let wrap = body.querySelector('.guest-date-range-top-fields');
    const monthbar = body.querySelector('.guest-date-range-monthbar');
    if (!wrap){
      wrap = document.createElement('div');
      wrap.className = 'guest-date-range-top-fields';
      wrap.innerHTML = '<button type="button" class="guest-date-range-top-field is-start"><span class="guest-date-range-top-label"></span><span class="guest-date-range-top-value"></span></button><button type="button" class="guest-date-range-top-field is-end"><span class="guest-date-range-top-label"></span><span class="guest-date-range-top-value"></span></button>';
      if (monthbar) body.insertBefore(wrap, monthbar); else body.insertBefore(wrap, body.firstChild);
      try{ __bindDateRangeCalendarHold__(wrap); }catch(_){ }
    }
    const labels = wrap.querySelectorAll('.guest-date-range-top-label');
    const values = wrap.querySelectorAll('.guest-date-range-top-value');
    if (labels[0]) labels[0].textContent = leftLabel || 'Da';
    if (labels[1]) labels[1].textContent = rightLabel || 'A';
    if (values[0]) values[0].textContent = __dateRangeCalendarPrettyDate__(startIso);
    if (values[1]) values[1].textContent = __dateRangeCalendarPrettyDate__(endIso || startIso);
    wrap.querySelectorAll('.guest-date-range-top-field').forEach((el)=>{
      try{ el.onclick = null; }catch(_){ }
    });
  }catch(_){ }
}
function __bindDateRangeCalendarModalTheme__(modalId){
  try{
    const modal = document.getElementById(modalId);
    if (!modal || modal.__rangeThemeBound) return;
    modal.__rangeThemeBound = '1';
    const card = modal.querySelector('.guest-date-range-modal-card');
    if (!card) return;
    let timer = null;
    let fired = false;
    const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
    const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } try{ e.stopImmediatePropagation(); }catch(_){ } };
    const start = (e)=>{
      try{ if (e.target && e.target.closest && e.target.closest('.operatori-editor-close')) return; }catch(_){ }
      try{ if (e.target && e.target.closest && e.target.closest('.guest-date-range-day[data-date]')) return; }catch(_){ }
      try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
      fired = false; clear();
      timer = setTimeout(()=>{ fired = true; try{ modal.__rangeThemeSuppressClickUntil = Date.now() + 900; }catch(_){ } __openDateRangeCalendarThemePicker__(); }, 520);
    };
    const stop = (e)=>{ clear(); if (fired){ block(e); setTimeout(()=>{ fired = false; }, 0); } };
    const swallow = (e)=>{ try{ if ((modal.__rangeThemeSuppressClickUntil || 0) > Date.now()) block(e); }catch(_){ } };
    ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ card.addEventListener(evt, start, { passive:true, capture:true }); }catch(_){ } });
    ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ card.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ } });
    try{ card.addEventListener('click', swallow, true); }catch(_){ }
    try{ card.addEventListener('contextmenu', (e)=>{ block(e); __openDateRangeCalendarThemePicker__(); }, true); }catch(_){ }
  }catch(_){ }
}
function __renderGuestDateRangeCalendar__(){
  try{
    const grid = document.getElementById('guestDateRangeGrid');
    const monthTitle = document.getElementById('guestDateRangeMonthTitle');
    const rangeTitle = document.getElementById('guestDateRangeModalTitle');
    const weekdaysWrap = document.getElementById('guestDateRangeWeekdays');
    if (!grid || !monthTitle || !weekdaysWrap) return;
    weekdaysWrap.innerHTML = __guestDateRangeWeekdayLabels__().map((d)=>`<span>${d}</span>`).join('');
    const monthDate = __guestDateRangeState__.month || __guestDateRangeMonthStart__();
    __guestDateRangeState__.month = __guestDateRangeMonthStart__(monthDate);
    const year = __guestDateRangeState__.month.getFullYear();
    const month = __guestDateRangeState__.month.getMonth();
    monthTitle.textContent = __guestDateRangeState__.month.toLocaleDateString(__getCurrentLocale__(), { month:'long', year:'numeric' });
    const first = new Date(year, month, 1);
    const offset = (first.getDay() + 6) % 7;
    const start = new Date(year, month, 1 - offset);
    const todayIso = todayISO();
    const startIso = String(__guestDateRangeState__.draftStart || '');
    const endIso = String(__guestDateRangeState__.draftEnd || '');
    __dateRangeCalendarEnsureHeader__('guestDateRangeModal', startIso, endIso, 'Check-in', 'Check-out');
    __bindDateRangeCalendarModalTheme__('guestDateRangeModal');
    const parts = [];
    for(let i=0;i<42;i++) {
      const d = new Date(start.getFullYear(), start.getMonth(), start.getDate()+i);
      const iso = `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
      const inMonth = d.getMonth() === month;
      const classes = ['guest-date-range-day'];
      if (!inMonth) classes.push('is-muted');
      if (__guestDateRangeSameDay__(iso, todayIso)) classes.push('is-today');
      if (__guestDateRangeSameDay__(iso, startIso) || __guestDateRangeSameDay__(iso, endIso)) classes.push('is-selected','is-edge');
      else if (__guestDateRangeInBetween__(iso, startIso, endIso)) classes.push('is-in-range');
      parts.push(`<button class="${classes.join(' ')}" data-date="${iso}" type="button">${d.getDate()}</button>`);
    }
    grid.innerHTML = parts.join('');
    if (rangeTitle) rangeTitle.textContent = __guestDateRangeFormatDisplay__(startIso, endIso);
  }catch(_){ }
}
function __openGuestDateRangeModal__(){
  try{
    const modal = document.getElementById('guestDateRangeModal');
    if (!modal) return;
    const ci = document.getElementById('guestCheckIn')?.value || todayISO();
    const coRaw = document.getElementById('guestCheckOut')?.value || '';
    const co = (coRaw && coRaw >= ci) ? coRaw : ci;
    __guestDateRangeState__.start = ci;
    __guestDateRangeState__.end = co;
    __guestDateRangeState__.draftStart = ci;
    __guestDateRangeState__.draftEnd = co;
    __guestDateRangeState__.month = __guestDateRangeMonthStart__(parseDateTs(ci) != null ? new Date(parseDateTs(ci)) : new Date());
    modal.hidden = false;
    modal.classList.add('is-open');
    modal.setAttribute('aria-hidden','false');
    __renderGuestDateRangeCalendar__();
  }catch(_){ }
}
function __closeGuestDateRangeModal__(restore = false){
  try{
    const modal = document.getElementById('guestDateRangeModal');
    if (!modal) return;
    if (restore){
      __guestDateRangeState__.draftStart = __guestDateRangeState__.start || '';
      __guestDateRangeState__.draftEnd = __guestDateRangeState__.end || '';
    }
    modal.classList.remove('is-open');
    modal.hidden = true;
    modal.setAttribute('aria-hidden','true');
  }catch(_){ }
}
function __applyGuestDateRangeDraft__(){
  try{
    const ci = document.getElementById('guestCheckIn');
    const co = document.getElementById('guestCheckOut');
    if (!ci || !co) return;
    let start = String(__guestDateRangeState__.draftStart || '').trim();
    let end = String(__guestDateRangeState__.draftEnd || '').trim();
    if (!start) start = todayISO();
    if (!end || end < start) end = start;
    ci.value = start;
    co.value = end;
    __guestDateRangeState__.start = start;
    __guestDateRangeState__.end = end;
    try{ syncGuestCheckoutPickerMonth(); }catch(_){ }
    try { const __refresh = () => { try{ window.__ddae_refreshRoomsAvailability && window.__ddae_refreshRoomsAvailability(); }catch(_){ } }; __refresh(); setTimeout(__refresh, 80); setTimeout(__refresh, 260); } catch (_) {}
    try { __updateGuestDateRangeTrigger__(); } catch (_) {}
    try { refreshFloatingLabels(); } catch (_) {}
    __closeGuestDateRangeModal__(false);
  }catch(_){ }
}
function __handleGuestDateRangePick__(iso){
  try{
    const start = String(__guestDateRangeState__.draftStart || '');
    const end = String(__guestDateRangeState__.draftEnd || '');
    if (!start || (start && end)){
      __guestDateRangeState__.draftStart = iso;
      __guestDateRangeState__.draftEnd = '';
    } else if (iso < start){
      __guestDateRangeState__.draftStart = iso;
    } else {
      __guestDateRangeState__.draftEnd = iso;
    }
    __renderGuestDateRangeCalendar__();
  }catch(_){ }
}

function euro(n){
  const x = Number(n || 0);
  try{ return x.toLocaleString(__getCurrentLocale__(), { style:"currency", currency:"EUR" }); }catch(_){ return `${(Math.round(x * 100) / 100).toFixed(2)} €`; }
}

let __toastTimer = null;
function toast(msg, kind){
  const t = $("#toast");
  if (!t) return;
  try{ msg = __translateText__ ? __translateText__(msg) : msg; }catch(_){ }
  t.textContent = msg;
  // kind: "blue" | "orange" | "" (default)
  t.dataset.kind = kind ? String(kind) : "";
  t.classList.add("show");
  try{ if (__toastTimer) clearTimeout(__toastTimer); }catch(_ ){}
  __toastTimer = setTimeout(() => {
    t.classList.remove("show");
    t.dataset.kind = "";
  }, 1700);
}


const __I18N_STORAGE_KEY__ = "ddae:app-language";
function __getAppLanguageStorageKey__(){
  try{
    const base = __I18N_STORAGE_KEY__;
    const sess = state?.session || null;
    if (sess && isOperatoreSession(sess)){
      const user = String(sess?.username || sess?.user_id || "operator").trim().toLowerCase().replace(/[^a-z0-9_\-]+/g, "_");
      return `${base}:operator:${user || "default"}`;
    }
    return `${base}:admin`;
  }catch(_){ return __I18N_STORAGE_KEY__; }
}
function __getSharedAppLanguageFromSettings__(fallback = "it"){
  try{
    const fromSettings = getSettingText ? String(getSettingText("app_language", fallback) || "").trim().toLowerCase() : String(fallback || "it").trim().toLowerCase();
    return __I18N_LOCALES__[fromSettings] ? fromSettings : String(fallback || "it").trim().toLowerCase();
  }catch(_){ return String(fallback || "it").trim().toLowerCase(); }
}
function __canPersistSharedAppLanguage__(){
  try{ return !!(state?.session?.user_id) && !isOperatoreSession(state.session); }catch(_){ return false; }
}
const __I18N_LOCALES__ = { it:"it-IT", en:"en-GB", fr:"fr-FR", de:"de-DE", es:"es-ES" };
let __appLanguage__ = "it";
let __applyingLanguage__ = false;
let __languageObserver__ = null;
let __MONTHS_IT = [];
const __I18N_PHRASES__ = {
  "crea account": {
    "en": "create account",
    "fr": "créer un compte",
    "de": "Konto erstellen",
    "es": "crear cuenta"
  },
  "modifica account": {
    "en": "edit account",
    "fr": "modifier le compte",
    "de": "Konto bearbeiten",
    "es": "editar cuenta"
  },
  "amministratore": {
    "en": "administrator",
    "fr": "administrateur",
    "de": "Administrator",
    "es": "administrador"
  },
  "operatore": {
    "en": "operator",
    "fr": "opérateur",
    "de": "Mitarbeiter",
    "es": "operador"
  },
  "Struttura": {
    "en": "Property",
    "fr": "Structure",
    "de": "Unterkunft",
    "es": "Alojamiento"
  },
  "Ricorda struttura": {
    "en": "Remember property",
    "fr": "Mémoriser la structure",
    "de": "Unterkunft merken",
    "es": "Recordar alojamiento"
  },
  "Username": {
    "en": "Username",
    "fr": "Nom d'utilisateur",
    "de": "Benutzername",
    "es": "Usuario"
  },
  "Password": {
    "en": "Password",
    "fr": "Mot de passe",
    "de": "Passwort",
    "es": "Contraseña"
  },
  "Conferma password": {
    "en": "Confirm password",
    "fr": "Confirmer le mot de passe",
    "de": "Passwort bestätigen",
    "es": "Confirmar contraseña"
  },
  "Nuova password": {
    "en": "New password",
    "fr": "Nouveau mot de passe",
    "de": "Neues Passwort",
    "es": "Nueva contraseña"
  },
  "Conferma nuova password": {
    "en": "Confirm new password",
    "fr": "Confirmer le nouveau mot de passe",
    "de": "Neues Passwort bestätigen",
    "es": "Confirmar nueva contraseña"
  },
  "Nome": {
    "en": "Name",
    "fr": "Nom",
    "de": "Name",
    "es": "Nombre"
  },
  "Inserimento": {
    "en": "Entry",
    "fr": "Insertion",
    "de": "Eingabe",
    "es": "Inserción"
  },
  "Arrivo": {
    "en": "Check-in",
    "fr": "Check-in",
    "de": "Check-in",
    "es": "Check-in"
  },
  "Attivo": {
    "en": "Active",
    "fr": "Actif",
    "de": "Aktiv",
    "es": "Activo"
  },
  "Disattivo": {
    "en": "Inactive",
    "fr": "Inactif",
    "de": "Inaktiv",
    "es": "Inactivo"
  },
  "Trasparenza": {
    "en": "Transparency",
    "fr": "Transparence",
    "de": "Transparenz",
    "es": "Transparencia"
  },
  "Telefono": {
    "en": "Phone",
    "fr": "Téléphone",
    "de": "Telefon",
    "es": "Teléfono"
  },
  "Email": {
    "en": "Email",
    "fr": "E-mail",
    "de": "E-Mail",
    "es": "Correo"
  },
  "indietro": {
    "en": "back",
    "fr": "retour",
    "de": "zurück",
    "es": "volver"
  },
  "continua": {
    "en": "continue",
    "fr": "continuer",
    "de": "weiter",
    "es": "continuar"
  },
  "Ospiti": {
    "en": "Guests",
    "fr": "Clients",
    "de": "Gäste",
    "es": "Huéspedes"
  },
  "Calendario": {
    "en": "Calendar",
    "fr": "Calendrier",
    "de": "Kalender",
    "es": "Calendario"
  },
  "Spese": {
    "en": "Expenses",
    "fr": "Dépenses",
    "de": "Ausgaben",
    "es": "Gastos"
  },
  "Tassa": {
    "en": "Tax",
    "fr": "Taxe",
    "de": "Steuer",
    "es": "Tasa"
  },
  "Pulizie": {
    "en": "Cleaning",
    "fr": "Ménage",
    "de": "Reinigung",
    "es": "Limpieza"
  },
  "Lavanderia": {
    "en": "Laundry",
    "fr": "Blanchisserie",
    "de": "Wäscherei",
    "es": "Lavandería"
  },
  "Ore Pulizia": {
    "en": "Cleaning Hours",
    "fr": "Heures de ménage",
    "de": "Reinigungsstunden",
    "es": "Horas de limpieza"
  },
  "Ore Pulizia Mensili": {
    "en": "Monthly Cleaning Hours",
    "fr": "Heures de ménage mensuelles",
    "de": "Monatliche Reinigungsstunden",
    "es": "Horas de limpieza mensuales"
  },
  "Statistiche": {
    "en": "Stats",
    "fr": "Statistiques",
    "de": "Statistiken",
    "es": "Estadísticas"
  },
  "Spesa": {
    "en": "Shopping",
    "fr": "Achats",
    "de": "Einkauf",
    "es": "Compras"
  },
  "IMPORTA": {
    "en": "IMPORT",
    "fr": "IMPORTER",
    "de": "IMPORT",
    "es": "IMPORTAR"
  },
  "ESPORTA": {
    "en": "EXPORT",
    "fr": "EXPORTER",
    "de": "EXPORT",
    "es": "EXPORTAR"
  },
  "Lista della spesa": {
    "en": "Shopping List",
    "fr": "Liste des achats",
    "de": "Einkaufsliste",
    "es": "Lista de compras"
  },
  "Lista spesa": {
    "en": "Shopping List",
    "fr": "Liste des achats",
    "de": "Einkaufsliste",
    "es": "Lista de compras"
  },
  "Colazione": {
    "en": "Breakfast",
    "fr": "Petit-déjeuner",
    "de": "Frühstück",
    "es": "Desayuno"
  },
  "Prodotti": {
    "en": "Products",
    "fr": "Produits",
    "de": "Produkte",
    "es": "Productos"
  },
  "Aggiungi prodotto": {
    "en": "Add product",
    "fr": "Ajouter un produit",
    "de": "Produkt hinzufügen",
    "es": "Añadir producto"
  },
  "Generali": {
    "en": "General",
    "fr": "Générales",
    "de": "Allgemein",
    "es": "Generales"
  },
  "Mensili": {
    "en": "Monthly",
    "fr": "Mensuelles",
    "de": "Monatlich",
    "es": "Mensuales"
  },
  "Grafici": {
    "en": "Charts",
    "fr": "Graphiques",
    "de": "Diagramme",
    "es": "Gráficos"
  },
  "Piscina": {
    "en": "Pool",
    "fr": "Piscine",
    "de": "Pool",
    "es": "Piscina"
  },
  "Cancellazioni": {
    "en": "Cancellations",
    "fr": "Annulations",
    "de": "Stornierungen",
    "es": "Cancelaciones"
  },
  "Impostazioni": {
    "en": "Settings",
    "fr": "Paramètres",
    "de": "Einstellungen",
    "es": "Ajustes"
  },
  "AUDIO": {
    "en": "Sound",
    "fr": "Son",
    "de": "Ton",
    "es": "Sonido"
  },
  "Anno": {
    "en": "Year",
    "fr": "Année",
    "de": "Jahr",
    "es": "Año"
  },
  "Backup": {
    "en": "Backup",
    "fr": "Sauvegarde",
    "de": "Backup",
    "es": "Copia de seguridad"
  },
  "Stanze": {
    "en": "Rooms",
    "fr": "Chambres",
    "de": "Zimmer",
    "es": "Habitaciones"
  },
  "Operatori": {
    "en": "Operators",
    "fr": "Opérateurs",
    "de": "Mitarbeiter",
    "es": "Operadores"
  },
  "Channel": {
    "en": "Channel",
    "fr": "Canal",
    "de": "Kanal",
    "es": "Canal"
  },
  "Tassa Sogg...": {
    "en": "Tourist Tax",
    "fr": "Taxe séjour",
    "de": "Kurtaxe",
    "es": "Tasa turíst."
  },
  "Codice Ope...": {
    "en": "Operator Code",
    "fr": "Code op.",
    "de": "Mitarbeiter-Code",
    "es": "Código op."
  },
  "Lingua": {
    "en": "Language",
    "fr": "Langue",
    "de": "Sprache",
    "es": "Idioma"
  },
  "Logout": {
    "en": "Logout",
    "fr": "Déconnexion",
    "de": "Abmelden",
    "es": "Cerrar sesión"
  },
  "Tassa di soggiorno": {
    "en": "Tourist Tax",
    "fr": "Taxe de séjour",
    "de": "Kurtaxe",
    "es": "Tasa turística"
  },
  "Tassa soggiorno": {
    "en": "Tourist Tax",
    "fr": "Taxe de séjour",
    "de": "Kurtaxe",
    "es": "Tasa turística"
  },
  "Sconto": {
    "en": "Discount",
    "fr": "Remise",
    "de": "Rabatt",
    "es": "Descuento"
  },
  "Rimanenza da pagare": {
    "en": "Remaining to pay",
    "fr": "Reste a payer",
    "de": "Restbetrag",
    "es": "Pendiente de pago"
  },
  "Stima": {
    "en": "Estimate",
    "fr": "Estimation",
    "de": "Schätzung",
    "es": "Estimación"
  },
  "Da": {
    "en": "From",
    "fr": "De",
    "de": "Von",
    "es": "Desde"
  },
  "A": {
    "en": "To",
    "fr": "À",
    "de": "Bis",
    "es": "Hasta"
  },
  "Totale tassa": {
    "en": "Total tax",
    "fr": "Taxe totale",
    "de": "Gesamtsteuer",
    "es": "Impuesto total"
  },
  "Ospiti paganti": {
    "en": "Paying guests",
    "fr": "Clients payants",
    "de": "Zahlende Gäste",
    "es": "Huéspedes que pagan"
  },
  "Bambini (<10)": {
    "en": "Children (<10)",
    "fr": "Enfants (<10)",
    "de": "Kinder (<10)",
    "es": "Niños (<10)"
  },
  "Altri (ridotti)": {
    "en": "Others (reduced)",
    "fr": "Autres (réduit)",
    "de": "Andere (ermäßigt)",
    "es": "Otros (reducidos)"
  },
  "Inserisci spesa": {
    "en": "Add expense",
    "fr": "Ajouter une dépense",
    "de": "Ausgabe erfassen",
    "es": "Añadir gasto"
  },
  "Motivazione": {
    "en": "Reason",
    "fr": "Motif",
    "de": "Grund",
    "es": "Motivo"
  },
  "Data": {
    "en": "Date",
    "fr": "Date",
    "de": "Datum",
    "es": "Fecha"
  },
  "Seleziona…": {
    "en": "Select…",
    "fr": "Sélectionner…",
    "de": "Auswählen…",
    "es": "Selecciona…"
  },
  "Categoria": {
    "en": "Category",
    "fr": "Catégorie",
    "de": "Kategorie",
    "es": "Categoría"
  },
  "Salva": {
    "en": "Save",
    "fr": "Enregistrer",
    "de": "Speichern",
    "es": "Guardar"
  },
  "Nessun operatore inserito": {
    "en": "No operator added",
    "fr": "Aucun opérateur saisi",
    "de": "Kein Mitarbeiter vorhanden",
    "es": "No hay operadores"
  },
  "Nessun channel inserito": {
    "en": "No channel added",
    "fr": "Aucun canal saisi",
    "de": "Kein Kanal vorhanden",
    "es": "No hay canales"
  },
  "Componenti lavanderia": {
    "en": "Laundry Components",
    "fr": "Composants blanchisserie",
    "de": "Wäscherei-Komponenten",
    "es": "Componentes de lavandería"
  },
  "Nessun componente lavanderia inserito": {
    "en": "No laundry component added",
    "fr": "Aucun composant blanchisserie saisi",
    "de": "Keine Wäscherei-Komponente vorhanden",
    "es": "No hay componentes de lavandería"
  },
  "Ordina: data": {
    "en": "Sort: date",
    "fr": "Trier : date",
    "de": "Sortieren: Datum",
    "es": "Ordenar: fecha"
  },
  "Ordina: inserimento": {
    "en": "Sort: entry",
    "fr": "Trier : saisie",
    "de": "Sortieren: Eingabe",
    "es": "Ordenar: ingreso"
  },
  "Ordina: motivazione": {
    "en": "Sort: reason",
    "fr": "Trier : motif",
    "de": "Sortieren: Grund",
    "es": "Ordenar: motivo"
  },
  "Nuovo ospite": {
    "en": "New guest",
    "fr": "Nouveau client",
    "de": "Neuer Gast",
    "es": "Nuevo huésped"
  },
  "Modifica": {
    "en": "Edit",
    "fr": "Modifier",
    "de": "Bearbeiten",
    "es": "Editar"
  },
  "Elimina": {
    "en": "Delete",
    "fr": "Supprimer",
    "de": "Löschen",
    "es": "Eliminar"
  },
  "Nome ospite": {
    "en": "Guest name",
    "fr": "Nom du client",
    "de": "Name des Gasts",
    "es": "Nombre del huésped"
  },
  "Adulti": {
    "en": "Adults",
    "fr": "Adultes",
    "de": "Erwachsene",
    "es": "Adultos"
  },
  "Bambini < 10": {
    "en": "Children < 10",
    "fr": "Enfants < 10",
    "de": "Kinder < 10",
    "es": "Niños < 10"
  },
  "Check-in": {
    "en": "Check-in",
    "fr": "Arrivée",
    "de": "Check-in",
    "es": "Check-in"
  },
  "Check-out": {
    "en": "Check-out",
    "fr": "Départ",
    "de": "Check-out",
    "es": "Check-out"
  },
  "Note": {
    "en": "Notes",
    "fr": "Notes",
    "de": "Notizen",
    "es": "Notas"
  },
  "Servizi": {
    "en": "Services",
    "fr": "Services",
    "de": "Services",
    "es": "Servicios"
  },
  "Oggi": {
    "en": "Today",
    "fr": "Aujourd'hui",
    "de": "Heute",
    "es": "Hoy"
  },
  "3 giorni": {
    "en": "3 days",
    "fr": "3 jours",
    "de": "3 Tage",
    "es": "3 días"
  },
  "Nessun ospite nei prossimi 3 giorni.": {
    "en": "No guests in the next 3 days.",
    "fr": "Aucun client dans les 3 prochains jours.",
    "de": "Keine Gäste in den nächsten 3 Tagen.",
    "es": "No hay huéspedes en los próximos 3 días."
  },
  "Nessun ospite per oggi.": {
    "en": "No guests for today.",
    "fr": "Aucun client pour aujourd'hui.",
    "de": "Keine Gäste für heute.",
    "es": "No hay huéspedes para hoy."
  },
  "Matrimoniale": {
    "en": "Double",
    "fr": "Double",
    "de": "Doppelbett",
    "es": "Doble"
  },
  "Singolo": {
    "en": "Single",
    "fr": "Simple",
    "de": "Einzelbett",
    "es": "Individual"
  },
  "Culla": {
    "en": "Cot",
    "fr": "Berceau",
    "de": "Kinderbett",
    "es": "Cuna"
  },
  "Chiudi": {
    "en": "Close",
    "fr": "Fermer",
    "de": "Schließen",
    "es": "Cerrar"
  },
  "Annulla": {
    "en": "Cancel",
    "fr": "Annuler",
    "de": "Abbrechen",
    "es": "Cancelar"
  },
  "Conferma": {
    "en": "Confirm",
    "fr": "Confirmer",
    "de": "Bestätigen",
    "es": "Confirmar"
  },
  "Importa": {
    "en": "Import",
    "fr": "Importer",
    "de": "Importieren",
    "es": "Importar"
  },
  "Esporta": {
    "en": "Export",
    "fr": "Exporter",
    "de": "Exportieren",
    "es": "Exportar"
  },
  "Nuovo channel": {
    "en": "New channel",
    "fr": "Nouveau canal",
    "de": "Neuer Kanal",
    "es": "Nuevo canal"
  },
  "Nome channel": {
    "en": "Channel name",
    "fr": "Nom du canal",
    "de": "Kanalname",
    "es": "Nombre del canal"
  },
  "Commissione (%)": {
    "en": "Commission (%)",
    "fr": "Commission (%)",
    "de": "Provision (%)",
    "es": "Comisión (%)"
  },
  "Iniziale": {
    "en": "Initial",
    "fr": "Initiale",
    "de": "Initiale",
    "es": "Inicial"
  },
  "Tag colore": {
    "en": "Color tag",
    "fr": "Étiquette couleur",
    "de": "Farbetikett",
    "es": "Etiqueta de color"
  },
  "Nuovo operatore": {
    "en": "New operator",
    "fr": "Nouvel opérateur",
    "de": "Neuer Mitarbeiter",
    "es": "Nuevo operador"
  },
  "Nome operatore": {
    "en": "Operator name",
    "fr": "Nom de l'opérateur",
    "de": "Name des Mitarbeiters",
    "es": "Nombre del operador"
  },
  "Tariffa (€ / ora)": {
    "en": "Rate (€ / hour)",
    "fr": "Tarif (€ / heure)",
    "de": "Satz (€ / Stunde)",
    "es": "Tarifa (€ / hora)"
  },
  "Benzina (€)": {
    "en": "Fuel (€)",
    "fr": "Carburant (€)",
    "de": "Kraftstoff (€)",
    "es": "Combustible (€)"
  },
  "Nuovo componente lavanderia": {
    "en": "New laundry component",
    "fr": "Nouveau composant blanchisserie",
    "de": "Neue Wäscherei-Komponente",
    "es": "Nuevo componente de lavandería"
  },
  "Titolo componente": {
    "en": "Component title",
    "fr": "Titre du composant",
    "de": "Komponententitel",
    "es": "Título del componente"
  },
  "Abbreviazione": {
    "en": "Abbreviation",
    "fr": "Abréviation",
    "de": "Abkürzung",
    "es": "Abreviatura"
  },
  "Prezzo pulizia (€)": {
    "en": "Cleaning price (€)",
    "fr": "Prix nettoyage (€)",
    "de": "Reinigungspreis (€)",
    "es": "Precio de limpieza (€)"
  },
  "Azzurro": {
    "en": "Sky blue",
    "fr": "Bleu ciel",
    "de": "Hellblau",
    "es": "Azul cielo"
  },
  "Arancione": {
    "en": "Orange",
    "fr": "Orange",
    "de": "Orange",
    "es": "Naranja"
  },
  "Verde": {
    "en": "Green",
    "fr": "Vert",
    "de": "Grün",
    "es": "Verde"
  },
  "Rosso": {
    "en": "Red",
    "fr": "Rouge",
    "de": "Rot",
    "es": "Rojo"
  },
  "Viola": {
    "en": "Purple",
    "fr": "Violet",
    "de": "Lila",
    "es": "Morado"
  },
  "Sabbia": {
    "en": "Sand",
    "fr": "Sable",
    "de": "Sand",
    "es": "Arena"
  },
  "Tariffa": {
    "en": "Rate",
    "fr": "Tarif",
    "de": "Satz",
    "es": "Tarifa"
  },
  "Benzina": {
    "en": "Fuel",
    "fr": "Carburant",
    "de": "Kraftstoff",
    "es": "Combustible"
  },
  "Modifica operatore": {
    "en": "Edit operator",
    "fr": "Modifier l'opérateur",
    "de": "Mitarbeiter bearbeiten",
    "es": "Editar operador"
  },
  "Eliminare questo operatore?": {
    "en": "Delete this operator?",
    "fr": "Supprimer cet opérateur ?",
    "de": "Diesen Mitarbeiter löschen?",
    "es": "¿Eliminar este operador?"
  },
  "Inserisci il nome operatore": {
    "en": "Enter the operator name",
    "fr": "Saisissez le nom de l'opérateur",
    "de": "Namen des Mitarbeiters eingeben",
    "es": "Introduce el nombre del operador"
  },
  "Tariffa non valida": {
    "en": "Invalid rate",
    "fr": "Tarif non valide",
    "de": "Ungültiger Satz",
    "es": "Tarifa no válida"
  },
  "Benzina non valida": {
    "en": "Invalid fuel amount",
    "fr": "Montant carburant non valide",
    "de": "Ungültiger Kraftstoffwert",
    "es": "Importe de combustible no válido"
  },
  "Operatore salvato": {
    "en": "Operator saved",
    "fr": "Opérateur enregistré",
    "de": "Mitarbeiter gespeichert",
    "es": "Operador guardado"
  },
  "Operatore eliminato": {
    "en": "Operator deleted",
    "fr": "Opérateur supprimé",
    "de": "Mitarbeiter gelöscht",
    "es": "Operador eliminado"
  },
  "Nessun dato": {
    "en": "No data",
    "fr": "Aucune donnée",
    "de": "Keine Daten",
    "es": "Sin datos"
  },
  "Ricevute mancanti": {
    "en": "Missing receipts",
    "fr": "Reçus manquants",
    "de": "Fehlende Belege",
    "es": "Recibos faltantes"
  },
  "Calendario settimanale": {
    "en": "Weekly calendar",
    "fr": "Calendrier hebdomadaire",
    "de": "Wochenkalender",
    "es": "Calendario semanal"
  },
  "Calendario mensile": {
    "en": "Monthly calendar",
    "fr": "Calendrier mensuel",
    "de": "Monatskalender",
    "es": "Calendario mensual"
  },
  "Forza lettura database": {
    "en": "Force database refresh",
    "fr": "Forcer la lecture de la base",
    "de": "Datenbank neu laden",
    "es": "Forzar lectura de la base de datos"
  },
  "Reset pulizie": {
    "en": "Reset cleaning",
    "fr": "Réinitialiser le ménage",
    "de": "Reinigung zurücksetzen",
    "es": "Restablecer limpieza"
  },
  "Ore lavoro pulizie": {
    "en": "Cleaning work hours",
    "fr": "Heures de travail ménage",
    "de": "Reinigungsarbeitsstunden",
    "es": "Horas de trabajo de limpieza"
  },
  "Riepilogo ore pulizia": {
    "en": "Cleaning hours summary",
    "fr": "Résumé des heures de ménage",
    "de": "Zusammenfassung Reinigungsstunden",
    "es": "Resumen de horas de limpieza"
  },
  "Filtri ore pulizia": {
    "en": "Cleaning hours filters",
    "fr": "Filtres heures ménage",
    "de": "Filter Reinigungsstunden",
    "es": "Filtros horas de limpieza"
  },
  "Mese": {
    "en": "Month",
    "fr": "Mois",
    "de": "Monat",
    "es": "Mes"
  },
  "Operatore": {
    "en": "Operator",
    "fr": "Opérateur",
    "de": "Mitarbeiter",
    "es": "Operador"
  },
  "Totali ore e benzina": {
    "en": "Hours and fuel totals",
    "fr": "Totaux heures et carburant",
    "de": "Stunden- und Kraftstoffsummen",
    "es": "Totales de horas y combustible"
  },
  "Calendario ore pulizia": {
    "en": "Cleaning hours calendar",
    "fr": "Calendrier des heures de ménage",
    "de": "Kalender Reinigungsstunden",
    "es": "Calendario de horas de limpieza"
  },
  "Genera report lavanderia": {
    "en": "Generate laundry report",
    "fr": "Générer le rapport blanchisserie",
    "de": "Wäscherei-Bericht erstellen",
    "es": "Generar informe de lavandería"
  },
  "Intervallo report lavanderia": {
    "en": "Laundry report range",
    "fr": "Période du rapport blanchisserie",
    "de": "Zeitraum Wäscherei-Bericht",
    "es": "Intervalo del informe de lavandería"
  },
  "Data inizio": {
    "en": "Start date",
    "fr": "Date de début",
    "de": "Startdatum",
    "es": "Fecha de inicio"
  },
  "Data fine": {
    "en": "End date",
    "fr": "Date de fin",
    "de": "Enddatum",
    "es": "Fecha de fin"
  },
  "Condividi": {
    "en": "Share",
    "fr": "Partager",
    "de": "Teilen",
    "es": "Compartir"
  },
  "Caricamento": {
    "en": "Loading",
    "fr": "Chargement",
    "de": "Laden",
    "es": "Cargando"
  },
  "Costi lavanderia": {
    "en": "Laundry costs",
    "fr": "Coûts blanchisserie",
    "de": "Wäschereikosten",
    "es": "Costes de lavandería"
  },
  "Importa database": {
    "en": "Import database",
    "fr": "Importer la base",
    "de": "Datenbank importieren",
    "es": "Importar base de datos"
  },
  "Esporta database": {
    "en": "Export database",
    "fr": "Exporter la base",
    "de": "Datenbank exportieren",
    "es": "Exportar base de datos"
  },
  "Database": {
    "en": "Database",
    "fr": "Base de données",
    "de": "Datenbank",
    "es": "Base de datos"
  },
  "Aggiungi servizio": {
    "en": "Add service",
    "fr": "Ajouter un service",
    "de": "Service hinzufügen",
    "es": "Añadir servicio"
  },
  "Servizio": {
    "en": "Service",
    "fr": "Service",
    "de": "Service",
    "es": "Servicio"
  },
  "Cancella": {
    "en": "Clear",
    "fr": "Effacer",
    "de": "Leeren",
    "es": "Borrar"
  },
  "Eliminare definitivamente questa spesa?": {
    "en": "Delete this expense permanently?",
    "fr": "Supprimer définitivement cette dépense ?",
    "de": "Diese Ausgabe endgültig löschen?",
    "es": "¿Eliminar definitivamente este gasto?"
  },
  "Spesa eliminata": {
    "en": "Expense deleted",
    "fr": "Dépense supprimée",
    "de": "Ausgabe gelöscht",
    "es": "Gasto eliminado"
  },
  "Mese corrente": {
    "en": "Current month",
    "fr": "Mois en cours",
    "de": "Aktueller Monat",
    "es": "Mes actual"
  },
  "Mese precedente": {
    "en": "Previous month",
    "fr": "Mois précédent",
    "de": "Vorheriger Monat",
    "es": "Mes anterior"
  },
  "Mese successivo": {
    "en": "Next month",
    "fr": "Mois suivant",
    "de": "Nächster Monat",
    "es": "Mes siguiente"
  },
  "Giorno precedente": {
    "en": "Previous day",
    "fr": "Jour précédent",
    "de": "Vorheriger Tag",
    "es": "Día anterior"
  },
  "Giorno successivo": {
    "en": "Next day",
    "fr": "Jour suivant",
    "de": "Nächster Tag",
    "es": "Día siguiente"
  },
  "Navigazione giorno": {
    "en": "Day navigation",
    "fr": "Navigation jour",
    "de": "Tagesnavigation",
    "es": "Navegación por día"
  },
  "Navigazione calendario": {
    "en": "Calendar navigation",
    "fr": "Navigation calendrier",
    "de": "Kalendernavigation",
    "es": "Navegación del calendario"
  },
  "Stanze occupate": {
    "en": "Occupied rooms",
    "fr": "Chambres occupées",
    "de": "Belegte Zimmer",
    "es": "Habitaciones ocupadas"
  },
  "Stanze da pulire": {
    "en": "Rooms to clean",
    "fr": "Chambres à nettoyer",
    "de": "Zu reinigende Zimmer",
    "es": "Habitaciones por limpiar"
  },
  "Legenda letti": {
    "en": "Bed legend",
    "fr": "Légende des lits",
    "de": "Bettenlegende",
    "es": "Leyenda de camas"
  },
  "Filtri ospiti": {
    "en": "Guest filters",
    "fr": "Filtres clients",
    "de": "Gästefilter",
    "es": "Filtros de huéspedes"
  },
  "Ordinamento": {
    "en": "Sorting",
    "fr": "Tri",
    "de": "Sortierung",
    "es": "Ordenación"
  },
  "Ordina ospiti per": {
    "en": "Sort guests by",
    "fr": "Trier les clients par",
    "de": "Gäste sortieren nach",
    "es": "Ordenar huéspedes por"
  },
  "Direzione ordinamento": {
    "en": "Sort direction",
    "fr": "Sens du tri",
    "de": "Sortierrichtung",
    "es": "Dirección de ordenación"
  },
  "Registrazioni": {
    "en": "Registrations",
    "fr": "Enregistrements",
    "de": "Registrierungen",
    "es": "Registros"
  },
  "Polizia di Stato": {
    "en": "State Police",
    "fr": "Police d'État",
    "de": "Staatspolizei",
    "es": "Policía del Estado"
  },
  "ISTAT": {
    "en": "ISTAT",
    "fr": "ISTAT",
    "de": "ISTAT",
    "es": "ISTAT"
  },
  "Ricevuta": {
    "en": "Receipt",
    "fr": "Reçu",
    "de": "Beleg",
    "es": "Recibo"
  },
  "Tipo acconto": {
    "en": "Deposit type",
    "fr": "Type d'acompte",
    "de": "Art der Anzahlung",
    "es": "Tipo de depósito"
  },
  "Elettronico": {
    "en": "Electronic",
    "fr": "Électronique",
    "de": "Elektronisch",
    "es": "Electrónico"
  },
  "Saldo": {
    "en": "Balance",
    "fr": "Solde",
    "de": "Saldo",
    "es": "Saldo"
  },
  "SI": {
    "en": "YES",
    "fr": "OUI",
    "de": "JA",
    "es": "SÍ"
  },
  "NO": {
    "en": "NO",
    "fr": "NON",
    "de": "NEIN",
    "es": "NO"
  },
  "Confermare?": {
    "en": "Confirm?",
    "fr": "Confirmer ?",
    "de": "Bestätigen?",
    "es": "¿Confirmar?"
  },
  "Sì": {
    "en": "Yes",
    "fr": "Oui",
    "de": "Ja",
    "es": "Sí"
  },
  "TUTTI": {
    "en": "ALL",
    "fr": "TOUS",
    "de": "ALLE",
    "es": "TODOS"
  },
  "Tutti": {
    "en": "All",
    "fr": "Tous",
    "de": "Alle",
    "es": "Todos"
  },
  "Audio on/off": {
    "en": "Sound on/off",
    "fr": "Activer/désactiver le son",
    "de": "Ton ein/aus",
    "es": "Activar/desactivar sonido"
  },
  "Lingua aggiornata": {
    "en": "Language updated",
    "fr": "Langue mise à jour",
    "de": "Sprache aktualisiert",
    "es": "Idioma actualizado"
  },
  "Seleziona la lingua dell'app": {
    "en": "Select the app language",
    "fr": "Sélectionnez la langue de l'application",
    "de": "App-Sprache auswählen",
    "es": "Selecciona el idioma de la app"
  }
};
const __I18N_WORD_MAPS__ = {
  "en": {
    "Lista": "List",
    "lista": "list",
    "Nuovo": "New",
    "Nuova": "New",
    "Modifica": "Edit",
    "Salva": "Save",
    "Elimina": "Delete",
    "Chiudi": "Close",
    "Annulla": "Cancel",
    "Nome": "Name",
    "operatore": "operator",
    "operatori": "operators",
    "ospite": "guest",
    "ospiti": "guests",
    "channel": "channel",
    "lavanderia": "laundry",
    "componente": "component",
    "componenti": "components",
    "Tariffa": "Rate",
    "tariffa": "rate",
    "Benzina": "Fuel",
    "benzina": "fuel",
    "Prezzo": "Price",
    "prezzo": "price",
    "Titolo": "Title",
    "titolo": "title",
    "Importo": "Amount",
    "importo": "amount",
    "Commissione": "Commission",
    "commissione": "commission",
    "Acconto": "Deposit",
    "Saldo": "Balance",
    "Servizi": "Services",
    "servizi": "services",
    "Spese": "Expenses",
    "spese": "expenses",
    "Spesa": "Shopping",
    "spesa": "shopping",
    "Pulizie": "Cleaning",
    "pulizie": "cleaning",
    "Calendario": "Calendar",
    "calendario": "calendar",
    "Impostazioni": "Settings",
    "Lingua": "Language",
    "lingua": "language",
    "Stanze": "Rooms",
    "stanza": "room",
    "stanze": "rooms",
    "Tassa": "Tax",
    "tassa": "tax",
    "soggiorno": "stay",
    "Data": "Date",
    "Mese": "Month",
    "mese": "month",
    "Anno": "Year",
    "anno": "year",
    "Oggi": "Today",
    "oggi": "today",
    "Giorno": "Day",
    "giorno": "day",
    "ricevuta": "receipt",
    "Ricevuta": "Receipt",
    "Contanti": "Cash",
    "Elettronico": "Electronic",
    "Database": "Database",
    "Codice": "Code",
    "Report": "Report",
    "Filtro": "Filter",
    "Filtri": "Filters",
    "Ordina": "Sort",
    "Operatore": "Operator",
    "Ore": "Hours",
    "ore": "hours",
    "Pulizia": "Cleaning",
    "pulizia": "cleaning",
    "Totale": "Total",
    "totali": "totals",
    "Totali": "Totals"
  },
  "fr": {
    "Lista": "Liste",
    "lista": "liste",
    "Nuovo": "Nouveau",
    "Nuova": "Nouvelle",
    "Modifica": "Modifier",
    "Salva": "Enregistrer",
    "Elimina": "Supprimer",
    "Chiudi": "Fermer",
    "Annulla": "Annuler",
    "Nome": "Nom",
    "operatore": "opérateur",
    "operatori": "opérateurs",
    "ospite": "client",
    "ospiti": "clients",
    "channel": "canal",
    "lavanderia": "blanchisserie",
    "componente": "composant",
    "componenti": "composants",
    "Tariffa": "Tarif",
    "tariffa": "tarif",
    "Benzina": "Carburant",
    "benzina": "carburant",
    "Prezzo": "Prix",
    "prezzo": "prix",
    "Titolo": "Titre",
    "titolo": "titre",
    "Importo": "Montant",
    "importo": "montant",
    "Commissione": "Commission",
    "commissione": "commission",
    "Acconto": "Acompte",
    "Saldo": "Solde",
    "Servizi": "Services",
    "servizi": "services",
    "Spese": "Dépenses",
    "spese": "dépenses",
    "Spesa": "Achats",
    "spesa": "achats",
    "Pulizie": "Ménage",
    "pulizie": "ménage",
    "Calendario": "Calendrier",
    "calendario": "calendrier",
    "Impostazioni": "Paramètres",
    "Lingua": "Langue",
    "lingua": "langue",
    "Stanze": "Chambres",
    "stanza": "chambre",
    "stanze": "chambres",
    "Tassa": "Taxe",
    "tassa": "taxe",
    "soggiorno": "séjour",
    "Data": "Date",
    "Mese": "Mois",
    "mese": "mois",
    "Anno": "Année",
    "anno": "année",
    "Oggi": "Aujourd'hui",
    "oggi": "aujourd'hui",
    "Giorno": "Jour",
    "giorno": "jour",
    "ricevuta": "reçu",
    "Ricevuta": "Reçu",
    "Contanti": "Espèces",
    "Elettronico": "Électronique",
    "Database": "Base de données",
    "Codice": "Code",
    "Report": "Rapport",
    "Filtro": "Filtre",
    "Filtri": "Filtres",
    "Ordina": "Trier",
    "Operatore": "Opérateur",
    "Ore": "Heures",
    "ore": "heures",
    "Pulizia": "Ménage",
    "pulizia": "ménage",
    "Totale": "Total",
    "totali": "totaux",
    "Totali": "Totaux"
  },
  "de": {
    "Lista": "Liste",
    "lista": "Liste",
    "Nuovo": "Neu",
    "Nuova": "Neu",
    "Modifica": "Bearbeiten",
    "Salva": "Speichern",
    "Elimina": "Löschen",
    "Chiudi": "Schließen",
    "Annulla": "Abbrechen",
    "Nome": "Name",
    "operatore": "Mitarbeiter",
    "operatori": "Mitarbeiter",
    "ospite": "Gast",
    "ospiti": "Gäste",
    "channel": "Kanal",
    "lavanderia": "Wäscherei",
    "componente": "Komponente",
    "componenti": "Komponenten",
    "Tariffa": "Satz",
    "tariffa": "Satz",
    "Benzina": "Kraftstoff",
    "benzina": "Kraftstoff",
    "Prezzo": "Preis",
    "prezzo": "Preis",
    "Titolo": "Titel",
    "titolo": "Titel",
    "Importo": "Betrag",
    "importo": "Betrag",
    "Commissione": "Provision",
    "commissione": "Provision",
    "Acconto": "Anzahlung",
    "Saldo": "Saldo",
    "Servizi": "Services",
    "servizi": "Services",
    "Spese": "Ausgaben",
    "spese": "Ausgaben",
    "Spesa": "Einkauf",
    "spesa": "Einkauf",
    "Pulizie": "Reinigung",
    "pulizie": "Reinigung",
    "Calendario": "Kalender",
    "calendario": "Kalender",
    "Impostazioni": "Einstellungen",
    "Lingua": "Sprache",
    "lingua": "Sprache",
    "Stanze": "Zimmer",
    "stanza": "Zimmer",
    "stanze": "Zimmer",
    "Tassa": "Steuer",
    "tassa": "Steuer",
    "soggiorno": "Aufenthalt",
    "Data": "Datum",
    "Mese": "Monat",
    "mese": "Monat",
    "Anno": "Jahr",
    "anno": "Jahr",
    "Oggi": "Heute",
    "oggi": "heute",
    "Giorno": "Tag",
    "giorno": "Tag",
    "ricevuta": "Beleg",
    "Ricevuta": "Beleg",
    "Contanti": "Bar",
    "Elettronico": "Elektronisch",
    "Database": "Datenbank",
    "Codice": "Code",
    "Report": "Bericht",
    "Filtro": "Filter",
    "Filtri": "Filter",
    "Ordina": "Sortieren",
    "Operatore": "Mitarbeiter",
    "Ore": "Stunden",
    "ore": "Stunden",
    "Pulizia": "Reinigung",
    "pulizia": "Reinigung",
    "Totale": "Gesamt",
    "totali": "Summen",
    "Totali": "Summen"
  },
  "es": {
    "Lista": "Lista",
    "lista": "lista",
    "Nuovo": "Nuevo",
    "Nuova": "Nueva",
    "Modifica": "Editar",
    "Salva": "Guardar",
    "Elimina": "Eliminar",
    "Chiudi": "Cerrar",
    "Annulla": "Cancelar",
    "Nome": "Nombre",
    "operatore": "operador",
    "operatori": "operadores",
    "ospite": "huésped",
    "ospiti": "huéspedes",
    "channel": "canal",
    "lavanderia": "lavandería",
    "componente": "componente",
    "componenti": "componentes",
    "Tariffa": "Tarifa",
    "tariffa": "tarifa",
    "Benzina": "Combustible",
    "benzina": "combustible",
    "Prezzo": "Precio",
    "prezzo": "precio",
    "Titolo": "Título",
    "titolo": "título",
    "Importo": "Importe",
    "importo": "importe",
    "Commissione": "Comisión",
    "commissione": "comisión",
    "Acconto": "Depósito",
    "Saldo": "Saldo",
    "Servizi": "Servicios",
    "servizi": "servicios",
    "Spese": "Gastos",
    "spese": "gastos",
    "Spesa": "Compras",
    "spesa": "compras",
    "Pulizie": "Limpieza",
    "pulizie": "limpieza",
    "Calendario": "Calendario",
    "calendario": "calendario",
    "Impostazioni": "Ajustes",
    "Lingua": "Idioma",
    "lingua": "idioma",
    "Stanze": "Habitaciones",
    "stanza": "habitación",
    "stanze": "habitaciones",
    "Tassa": "Tasa",
    "tassa": "tasa",
    "soggiorno": "turística",
    "Data": "Fecha",
    "Mese": "Mes",
    "mese": "mes",
    "Anno": "Año",
    "anno": "año",
    "Oggi": "Hoy",
    "oggi": "hoy",
    "Giorno": "Día",
    "giorno": "día",
    "ricevuta": "recibo",
    "Ricevuta": "Recibo",
    "Contanti": "Efectivo",
    "Elettronico": "Electrónico",
    "Database": "Base de datos",
    "Codice": "Código",
    "Report": "Informe",
    "Filtro": "Filtro",
    "Filtri": "Filtros",
    "Ordina": "Ordenar",
    "Operatore": "Operador",
    "Ore": "Horas",
    "ore": "horas",
    "Pulizia": "Limpieza",
    "pulizia": "limpieza",
    "Totale": "Total",
    "totali": "totales",
    "Totali": "Totales"
  }
};

Object.assign(__I18N_PHRASES__, {
  "Telefono": { "en":"Phone", "fr":"Téléphone", "de":"Telefon", "es":"Teléfono" },
  "Email": { "en":"Email", "fr":"E-mail", "de":"E-Mail", "es":"Correo electrónico" },
  "Numero prenotazione": { "en":"Booking number", "fr":"Numéro de réservation", "de":"Buchungsnummer", "es":"Número de reserva" },
  "Periodo pernottamento": { "en":"Stay period", "fr":"Période de séjour", "de":"Aufenthaltszeitraum", "es":"Periodo de estancia" },
  "Seleziona date": { "en":"Select dates", "fr":"Sélectionnez les dates", "de":"Daten auswählen", "es":"Selecciona las fechas" },
  "Seleziona il range di pernottamento nello stesso calendario": { "en":"Select the stay range in the same calendar", "fr":"Sélectionnez la plage de séjour dans le même calendrier", "de":"Wählen Sie den Aufenthaltszeitraum im selben Kalender", "es":"Selecciona el rango de estancia en el mismo calendario" },
  "Arrivo": { "en":"Check-in", "fr":"Check-in", "de":"Check-in", "es":"Check-in" },
  "Partenza": { "en":"Departure", "fr":"Départ", "de":"Abreise", "es":"Salida" },
  "Conferma": { "en":"Confirm", "fr":"Confirmer", "de":"Bestätigen", "es":"Confirmar" },
  "Mese precedente": { "en":"Previous month", "fr":"Mois précédent", "de":"Vorheriger Monat", "es":"Mes anterior" },
  "Mese successivo": { "en":"Next month", "fr":"Mois suivant", "de":"Nächster Monat", "es":"Mes siguiente" },
  "Seleziona periodo pernottamento": { "en":"Select stay period", "fr":"Sélectionnez la période de séjour", "de":"Aufenthaltszeitraum auswählen", "es":"Selecciona el periodo de estancia" },
  "Seleziona stanze": { "en":"Select rooms", "fr":"Sélectionner les chambres", "de":"Zimmer auswählen", "es":"Seleccionar habitaciones" },
  "Matrimonio / G": { "en":"Wedding / G", "fr":"Mariage / G", "de":"Hochzeit / G", "es":"Boda / G" },
  "Inserisci eventuali note": { "en":"Enter any notes", "fr":"Saisissez d'éventuelles notes", "de":"Geben Sie eventuelle Notizen ein", "es":"Introduce cualquier nota" },
  "Seleziona nazionalità": { "en":"Select nationality", "fr":"Sélectionner la nationalité", "de":"Nationalität auswählen", "es":"Seleccionar nacionalidad" },
  "Modifica ospite": { "en":"Edit guest", "fr":"Modifier le client", "de":"Gast bearbeiten", "es":"Editar huésped" },
  "Elimina ospite": { "en":"Delete guest", "fr":"Supprimer le client", "de":"Gast löschen", "es":"Eliminar huésped" },
  "Crea report ospite": { "en":"Create guest report", "fr":"Créer le rapport client", "de":"Gastbericht erstellen", "es":"Crear informe del huésped" },
  "Vai al calendario": { "en":"Go to calendar", "fr":"Aller au calendrier", "de":"Zum Kalender", "es":"Ir al calendario" },
  "Salva modifiche": { "en":"Save changes", "fr":"Enregistrer les modifications", "de":"Änderungen speichern", "es":"Guardar cambios" },
  "Crea ospite": { "en":"Create guest", "fr":"Créer le client", "de":"Gast erstellen", "es":"Crear huésped" },
  "Prenota": { "en":"Book", "fr":"Réserver", "de":"Buchen", "es":"Reservar" },
  "Preventivo": { "en":"Estimate", "fr":"Devis", "de":"Angebot", "es":"Presupuesto" },
  "Preventivi": { "en":"Estimates", "fr":"Devis", "de":"Angebote", "es":"Presupuestos" },
  "Mostra solo preventivi": { "en":"Show estimates only", "fr":"Afficher seulement les devis", "de":"Nur Angebote anzeigen", "es":"Mostrar solo presupuestos" },
  "Nascondi preventivi": { "en":"Hide estimates", "fr":"Masquer les devis", "de":"Angebote ausblenden", "es":"Ocultar presupuestos" }
});

try{
  Object.assign(__I18N_PHRASES__, {
    "stanza": { "en":"room", "fr":"chambre", "de":"Zimmer", "es":"habitación" },
    "stanze": { "en":"rooms", "fr":"chambres", "de":"Zimmer", "es":"habitaciones" },
    "Stato": { "en":"Status", "fr":"Statut", "de":"Status", "es":"Estado" },
    "Apri scheda ospite": { "en":"Open guest card", "fr":"Ouvrir la fiche client", "de":"Gastkarte öffnen", "es":"Abrir ficha del huésped" },
    "Numero prenotazione inserito": { "en":"Booking number entered", "fr":"Numéro de réservation saisi", "de":"Buchungsnummer eingegeben", "es":"Número de reserva introducido" },
    "Numero prenotazione mancante": { "en":"Booking number missing", "fr":"Numéro de réservation manquant", "de":"Buchungsnummer fehlt", "es":"Falta número de reserva" },
    "Nazionalità": { "en":"Nationality", "fr":"Nationalité", "de":"Nationalität", "es":"Nacionalidad" },
    "Nazionalità non selezionata": { "en":"Nationality not selected", "fr":"Nationalité non sélectionnée", "de":"Nationalität nicht ausgewählt", "es":"Nacionalidad no seleccionada" },
    "Matrimonio": { "en":"Wedding", "fr":"Mariage", "de":"Hochzeit", "es":"Boda" },
    "check-in non effettuato": { "en":"check-in not completed", "fr":"check-in non effectué", "de":"Check-in nicht durchgeführt", "es":"check-in no realizado" },
    "Nessun ospite per oggi.": { "en":"No guests for today.", "fr":"Aucun client pour aujourd’hui.", "de":"Keine Gäste für heute.", "es":"No hay huéspedes para hoy." },
    "Nessun ospite nel periodo.": { "en":"No guests in the period.", "fr":"Aucun client sur la période.", "de":"Keine Gäste im Zeitraum.", "es":"No hay huéspedes en el periodo." },
    "Nessun ospite nei prossimi 3 giorni.": { "en":"No guests in the next 3 days.", "fr":"Aucun client dans les 3 prochains jours.", "de":"Keine Gäste in den nächsten 3 Tagen.", "es":"No hay huéspedes en los próximos 3 días." },
    "Nessun ospite nei prossimi 7 giorni.": { "en":"No guests in the next 7 days.", "fr":"Aucun client dans les 7 prochains jours.", "de":"Keine Gäste in den nächsten 7 Tagen.", "es":"No hay huéspedes en los próximos 7 días." },
    "Nessun ospite negli ultimi 3 giorni.": { "en":"No guests in the last 3 days.", "fr":"Aucun client dans les 3 derniers jours.", "de":"Keine Gäste in den letzten 3 Tagen.", "es":"No hay huéspedes en los últimos 3 días." },
    "Nessun ospite negli ultimi 7 giorni.": { "en":"No guests in the last 7 days.", "fr":"Aucun client dans les 7 derniers jours.", "de":"Keine Gäste in den letzten 7 Tagen.", "es":"No hay huéspedes en los últimos 7 días." }
  });
}catch(_){ }

function __getAppLanguage__(){ const v = String(__appLanguage__ || "it").trim().toLowerCase(); return __I18N_LOCALES__[v] ? v : "it"; }
function __getCurrentLocale__(){ return __I18N_LOCALES__[__getAppLanguage__()] || "it-IT"; }
function __capitalizeLocale__(s){ const raw = String(s || ""); return raw ? (raw.charAt(0).toUpperCase() + raw.slice(1)) : ""; }
function __normalizeI18nWhitespace__(text){ return String(text || "").replace(/\s+/g, " ").trim(); }
function __getMonthNamesForLocale__(locale, capitalize = false){ try{ const out=[]; for(let i=0;i<12;i+=1){ let label=new Date(2026, i, 1).toLocaleDateString(locale || __getCurrentLocale__(), { month:"long" }); out.push(capitalize ? __capitalizeLocale__(label) : label); } return out; }catch(_){ return []; } }
function __refreshMonthNamesCache__(){ __MONTHS_IT = __getMonthNamesForLocale__(__getCurrentLocale__(), true); }
function __getWeekdayShortForLocale__(date){ try{ return new Date(date).toLocaleDateString(__getCurrentLocale__(), { weekday:"short" }); }catch(_){ return ""; } }
function __getWeekdayLongForLocale__(date){ try{ return new Date(date).toLocaleDateString(__getCurrentLocale__(), { weekday:"long" }); }catch(_){ return ""; } }
function __translateExactText__(value){ const lang=__getAppLanguage__(); if(lang==="it") return String(value||""); const key=__normalizeI18nWhitespace__(value); const row=__I18N_PHRASES__[key]; return row && row[lang] ? String(row[lang]) : String(value||""); }
function __escapeRegExp__(s){ return String(s || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&"); }
function __applyWordMap__(value){ let out=String(value||""); const lang=__getAppLanguage__(); const map=__I18N_WORD_MAPS__[lang]; if(!map) return out; const entries=Object.entries(map).sort((a,b)=>String(b[0]).length-String(a[0]).length); entries.forEach(([src,dst])=>{ const rx=new RegExp(`\\b${__escapeRegExp__(src)}\\b`,"gi"); out=out.replace(rx,(match)=>{ if(!match) return dst; if(match===match.toUpperCase()) return String(dst).toUpperCase(); if(match.charAt(0)===match.charAt(0).toUpperCase()) return __capitalizeLocale__(dst); return dst; }); }); return out; }
function __translateText__(value){ const original=String(value??""); const lang=__getAppLanguage__(); if(lang==="it") return original; const trimmed=original.trim(); if(!trimmed) return original; const exact=__translateExactText__(trimmed); const translated=(exact!==trimmed)?exact:__applyWordMap__(trimmed); return (!translated || translated===trimmed) ? original : original.replace(trimmed, translated); }
function __setOriginalAttr__(el, attr){ try{ if(!el.__i18nOriginalAttrs) el.__i18nOriginalAttrs={}; if(!(attr in el.__i18nOriginalAttrs)) el.__i18nOriginalAttrs[attr]=el.getAttribute(attr); }catch(_){} }
function __translateElementAttributes__(el){ try{ if(!el || !el.getAttribute || el.closest?.("[data-no-i18n='1']")) return; ["aria-label","placeholder","title"].forEach((attr)=>{ if(!el.hasAttribute(attr)) return; __setOriginalAttr__(el, attr); const base=el.__i18nOriginalAttrs ? el.__i18nOriginalAttrs[attr] : el.getAttribute(attr); const next=(__getAppLanguage__()==="it") ? String(base ?? "") : __translateText__(base); if(String(next ?? "") !== String(el.getAttribute(attr) ?? "")) el.setAttribute(attr, String(next ?? "")); }); }catch(_){} }
function __translateTextNode__(node){ try{ if(!node || !node.parentElement) return; const parent=node.parentElement; if(parent.closest?.("[data-no-i18n='1']")) return; if(/^(SCRIPT|STYLE|TEXTAREA)$/i.test(parent.tagName)) return; if(parent.closest?.("svg")) return; const current=String(node.nodeValue ?? ""); if(!current.trim()) return; if(typeof node.__i18nOriginal !== "string") node.__i18nOriginal=current; const base=String(node.__i18nOriginal ?? current); const next=(__getAppLanguage__()==="it") ? base : __translateText__(base); if(next !== current) node.nodeValue=next; }catch(_){} }
function __translateTree__(root){ try{ if(!root || __applyingLanguage__) return; __applyingLanguage__=true; if(root.nodeType===Node.TEXT_NODE){ __translateTextNode__(root); return; } const start=(root.nodeType===Node.ELEMENT_NODE)?root:document.body; if(start && start.nodeType===Node.ELEMENT_NODE){ __translateElementAttributes__(start); const walker=document.createTreeWalker(start, NodeFilter.SHOW_TEXT, null); let textNode=walker.nextNode(); while(textNode){ __translateTextNode__(textNode); textNode=walker.nextNode(); } start.querySelectorAll?.("*").forEach((el)=>__translateElementAttributes__(el)); } }catch(_){} finally{ __applyingLanguage__=false; } }
function __applyAppLanguageToDom__(){ try{ document.documentElement.lang=__getAppLanguage__(); }catch(_){} __refreshMonthNamesCache__(); __translateTree__(document.body); try{ document.querySelectorAll?.("#languageGrid .language-option").forEach((btn)=>btn.classList.toggle("is-selected", String(btn?.dataset?.lang || "")===__getAppLanguage__())); }catch(_){} }
window.__applyAppLanguageToDom__ = __applyAppLanguageToDom__;
function __ensureLanguageObserver__(){ try{ if(__languageObserver__ || !document.body) return; __languageObserver__ = new MutationObserver((mutations)=>{ if(__applyingLanguage__) return; mutations.forEach((m)=>{ if(m.type==="characterData"){ __translateTextNode__(m.target); return; } if(m.type==="attributes"){ __translateElementAttributes__(m.target); return; } if(m.type==="childList"){ m.addedNodes.forEach((node)=>{ if(node.nodeType===Node.TEXT_NODE) __translateTextNode__(node); else if(node.nodeType===Node.ELEMENT_NODE) __translateTree__(node); }); } }); }); __languageObserver__.observe(document.body, { childList:true, subtree:true, characterData:true, attributes:true, attributeFilter:["aria-label","placeholder","title"] }); }catch(_){} }
async function __persistAppLanguage__(lang){
  const next = String(lang || "it");
  try{ localStorage.setItem(__getAppLanguageStorageKey__(), next); }catch(_){}
  if (__canPersistSharedAppLanguage__()){
    try{ if(state && state.settings && state.settings.byKey) state.settings.byKey.app_language={ key:"app_language", value:next }; }catch(_){}
    try{ await api("impostazioni", { method:"POST", body:{ app_language:next }, showLoader:false }); }catch(_){}
  }
}
async function __setAppLanguage__(lang, { persist = true, silent = false } = {}){ const next=__I18N_LOCALES__[String(lang || "").trim().toLowerCase()] ? String(lang || "").trim().toLowerCase() : "it"; __appLanguage__=next; if(persist) await __persistAppLanguage__(next); __applyAppLanguageToDom__(); try{ setLaundryLabels_(); }catch(_){} try{ __laundryUpdatePriceHints__(); }catch(_){} try{ const host=document.getElementById("laundryPricesList"); if(host && host.dataset.ready==="1") host.dataset.ready="0"; }catch(_){} try{ if(state?.page==="laundrycatalog") await renderLaundryCatalogPage(); }catch(_){} try{ if(state?.page==="lavanderia") renderLaundry_(state?.laundry?.current || null); }catch(_){} try{ if(state?.page==="ospiti") renderGuestCards(); }catch(_){} try{ window.dispatchEvent(new CustomEvent("ddae:language-change", { detail:{ lang:next } })); }catch(_){} if(!silent){ try{ toast("Lingua aggiornata", "blue"); }catch(_){} } }
async function __hydrateAppLanguageFromSettings__(){
  let next="it";
  const isOp = !!(state?.session && isOperatoreSession(state.session));
  const activeKey = __getAppLanguageStorageKey__();
  let hasScopedLocal = false;
  try{
    const rawLocal = localStorage.getItem(activeKey);
    hasScopedLocal = rawLocal !== null && rawLocal !== undefined && String(rawLocal).trim() !== "";
    const local=String(rawLocal || "").trim().toLowerCase();
    if(__I18N_LOCALES__[local]) next=local;
  }catch(_){}
  if (isOp){
    if (!hasScopedLocal){
      try{
        const legacy = String(localStorage.getItem(__I18N_STORAGE_KEY__) || "").trim().toLowerCase();
        if(__I18N_LOCALES__[legacy]) next = legacy;
      }catch(_){}
      if (!__I18N_LOCALES__[next]) next = "it";
    }
  }else{
    const shared = __getSharedAppLanguageFromSettings__(next);
    if(__I18N_LOCALES__[shared]) next = shared;
  }
  __appLanguage__=next;
  try{ localStorage.setItem(activeKey, next); }catch(_){}
  __applyAppLanguageToDom__();
}
let __languageModalReadyAt__ = 0;
let __languageModalSuppressUntil__ = 0;
function __languageModalGhostTapActive__(){ try{ return Date.now() < (__languageModalSuppressUntil__ || 0); }catch(_){ return false; } }
function __languageModalSwallowGhostTap__(ev){
  try{
    if (!__languageModalGhostTapActive__()) return;
    const modal = document.getElementById("languageModal");
    const insideModal = !!(modal && !modal.hidden && ev && ev.target && modal.contains(ev.target));
    if (insideModal) return;
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    try{ ev.stopImmediatePropagation(); }catch(_){ }
  }catch(_){ }
}
function __openLanguageModal__(){ const modal=document.getElementById("languageModal"); if(!modal) return; __languageModalReadyAt__ = Date.now() + 260; __languageModalSuppressUntil__ = 0; modal.hidden=false; modal.setAttribute("aria-hidden","false"); __applyAppLanguageToDom__(); }
function __closeLanguageModal__(){ const modal=document.getElementById("languageModal"); if(!modal) return; modal.hidden=true; modal.setAttribute("aria-hidden","true"); __languageModalReadyAt__ = 0; __languageModalSuppressUntil__ = Date.now() + 700; }
function setupLanguageModal(){ const modal=document.getElementById("languageModal"); if(!modal || modal.dataset.bound==="1") return; modal.dataset.bound="1"; const closeBtn=document.getElementById("languageModalClose"); const closeFooterBtn=document.getElementById("languageModalCloseBtn"); const card=modal.querySelector?.(".language-modal-card"); if(closeBtn) bindFastTap(closeBtn, __closeLanguageModal__); if(closeFooterBtn) bindFastTap(closeFooterBtn, __closeLanguageModal__); if(card){ ["pointerdown","pointerup","touchstart","touchend","click"].forEach((evt)=>{ try{ card.addEventListener(evt,(ev)=>{ try{ ev.stopPropagation(); }catch(_){} },{ passive:false }); }catch(_){ try{ card.addEventListener(evt,(ev)=>{ try{ ev.stopPropagation(); }catch(__){} }); }catch(__){} } }); } ["pointerdown","pointerup","touchstart","touchend","click"].forEach((evt)=>{ try{ document.addEventListener(evt, __languageModalSwallowGhostTap__, true); }catch(_){ } }); modal.addEventListener("click",(ev)=>{ try{ if(ev.target===modal) __closeLanguageModal__(); }catch(_){} }); document.querySelectorAll?.("#languageGrid .language-option").forEach((btn)=>bindFastTap(btn, async()=>{ try{ if(Date.now() < __languageModalReadyAt__) return; __languageModalSuppressUntil__ = Date.now() + 900; try{ if(document.activeElement && document.activeElement.blur) document.activeElement.blur(); }catch(_){} await __setAppLanguage__(btn.dataset.lang || "it"); setTimeout(()=>{ try{ __closeLanguageModal__(); }catch(_){} }, 80); }catch(_){} })); }
try{ const __nativeConfirm__=(typeof window!=="undefined" && typeof window.confirm==="function") ? window.confirm.bind(window) : null; const __nativeAlert__=(typeof window!=="undefined" && typeof window.alert==="function") ? window.alert.bind(window) : null; if(__nativeConfirm__) window.confirm=(message)=>__nativeConfirm__(__translateText__(message)); if(__nativeAlert__) window.alert=(message)=>__nativeAlert__(__translateText__(message)); }catch(_){}
try{ if(typeof window!=="undefined") window.addEventListener("DOMContentLoaded", ()=>{ try{ __ensureLanguageObserver__(); }catch(_){} try{ __hydrateAppLanguageFromSettings__(); }catch(_){} }); }catch(_){}
__refreshMonthNamesCache__();


// Conferma con modal Sì/No (label esplicite)
let __confirmYesNoResolve = null;
let __confirmYesNoReadyAt__ = 0;
let __confirmYesNoSuppressUntil__ = 0;
function __confirmYesNoGhostTapActive__(){
  try{ return Date.now() < (__confirmYesNoSuppressUntil__ || 0); }catch(_){ return false; }
}
function __confirmYesNoSwallowGhostTap__(ev){
  try{
    if (!__confirmYesNoGhostTapActive__()) return;
    const modal = document.getElementById('confirmYesNoModal');
    const insideModal = !!(modal && !modal.hidden && ev && ev.target && modal.contains(ev.target));
    if (insideModal) return;
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    try{ ev.stopImmediatePropagation(); }catch(_){ }
  }catch(_){ }
}
function __confirmYesNoSetupModal__(){
  try{
    const modal = document.getElementById('confirmYesNoModal');
    if (!modal || modal.dataset.ghostTapBound === '1') return;
    modal.dataset.ghostTapBound = '1';
    ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt) => {
      try{ document.addEventListener(evt, __confirmYesNoSwallowGhostTap__, true); }catch(_){ }
    });
  }catch(_){ }
}
function __confirmYesNoIsActionButton__(target, id){
  try{
    if (!target) return false;
    if (target.id === id) return true;
    const el = target.closest ? target.closest('#' + id) : null;
    return !!el;
  }catch(_){ return false; }
}
function __confirmYesNoStopEvent__(e){
  try{ if (e && e.cancelable !== false) e.preventDefault(); }catch(_){ }
  try{ if (e) e.stopPropagation(); }catch(_){ }
  try{ if (e && e.stopImmediatePropagation) e.stopImmediatePropagation(); }catch(_){ }
}

function __confirmYesNoForceClose__(){
  try{
    const modal = document.getElementById('confirmYesNoModal');
    if (modal){
      try{ modal.hidden = true; }catch(_){ }
      try{ modal.setAttribute('hidden', ''); }catch(_){ }
      try{ modal.setAttribute('aria-hidden', 'true'); }catch(_){ }
      try{ modal.classList.remove('is-open', 'show', 'open', 'active'); }catch(_){ }
      try{ modal.style.removeProperty('z-index'); }catch(_){ }
      try{ modal.style.removeProperty('pointer-events'); }catch(_){ }
      try{ modal.style.removeProperty('display'); }catch(_){ }
    }
    try{ document.body.classList.remove('confirm-yesno-open'); }catch(_){ }
    try{ document.documentElement.classList.remove('confirm-yesno-open'); }catch(_){ }
  }catch(_){ }
}
function confirmYesNo(message){
  return new Promise((resolve)=>{
    let settled = false;
    const done = (value) => {
      if (settled) return;
      settled = true;
      try{ __confirmYesNoResolve = null; }catch(_){ }
      try{ __confirmYesNoForceClose__(); }catch(_){ }
      try{ setTimeout(()=>{ try{ __confirmYesNoForceClose__(); }catch(__){} }, 0); }catch(_){ }
      try{ setTimeout(()=>{ try{ __confirmYesNoForceClose__(); }catch(__){} }, 120); }catch(_){ }
      try{ resolve(!!value); }catch(_){ }
    };
    try{
      const modal = document.getElementById('confirmYesNoModal');
      const textEl = document.getElementById('confirmYesNoText');
      const yesOld = document.getElementById('confirmYesNoYes');
      const noOld  = document.getElementById('confirmYesNoNo');
      if (!modal || !textEl || !yesOld || !noOld){
        try{ done(!!confirm(__translateText__(String(message || 'Confermare?')))); }catch(_){ done(false); }
        return;
      }

      try{ __confirmYesNoSetupModal__(); }catch(_){ }
      try{ if (__confirmYesNoResolve){ const prev = __confirmYesNoResolve; __confirmYesNoResolve = null; try{ prev(false); }catch(__){} } }catch(_){ }
      try{ __confirmYesNoForceClose__(); }catch(_){ }
      __confirmYesNoResolve = done;

      textEl.textContent = __translateText__(String(message || 'Confermare?'));
      try{ if (modal.parentNode !== document.body) document.body.appendChild(modal); }catch(_){ }

      // Clona i pulsanti a ogni apertura: elimina listener vecchi o catturatori ereditati
      // che nei popup Impostazioni potevano intercettare il tap e bloccare la conferma.
      const yesBtn = yesOld.cloneNode(true);
      const noBtn = noOld.cloneNode(true);
      yesOld.parentNode.replaceChild(yesBtn, yesOld);
      noOld.parentNode.replaceChild(noBtn, noOld);

      const onYes = (ev) => { try{ __confirmYesNoStopEvent__(ev); }catch(_){ } done(true); };
      const onNo  = (ev) => { try{ __confirmYesNoStopEvent__(ev); }catch(_){ } done(false); };
      const onBackdrop = (ev) => {
        try{
          if (ev && ev.target === modal){
            __confirmYesNoStopEvent__(ev);
            done(false);
          }
        }catch(_){ }
      };

      ['pointerdown','touchstart','mousedown','click'].forEach((type)=>{
        try{ yesBtn.addEventListener(type, onYes, { capture:false, passive:false }); }catch(_){ try{ yesBtn.addEventListener(type, onYes, false); }catch(__){} }
        try{ noBtn.addEventListener(type, onNo, { capture:false, passive:false }); }catch(_){ try{ noBtn.addEventListener(type, onNo, false); }catch(__){} }
      });
      try{ yesBtn.onclick = onYes; noBtn.onclick = onNo; }catch(_){ }
      try{ modal.onclick = onBackdrop; }catch(_){ }

      __confirmYesNoReadyAt__ = 0;
      __confirmYesNoSuppressUntil__ = 0;
      try{ if (typeof __tagColorPopupSuppressUntil__ !== 'undefined') __tagColorPopupSuppressUntil__ = 0; }catch(_){ }
      try{ document.body.classList.add('confirm-yesno-open'); }catch(_){ }
      try{ document.documentElement.classList.add('confirm-yesno-open'); }catch(_){ }
      modal.hidden = false;
      try{ modal.removeAttribute('hidden'); }catch(_){ }
      try{ modal.setAttribute('aria-hidden', 'false'); }catch(_){ }
      try{ modal.classList.add('is-open'); }catch(_){ }
      try{ modal.style.setProperty('display','flex','important'); }catch(_){ }
      try{ modal.style.setProperty('z-index','2147483647','important'); }catch(_){ }
      try{ modal.style.setProperty('pointer-events','auto','important'); }catch(_){ }
    }catch(_){
      try{ __confirmYesNoForceClose__(); }catch(__){}
      try{ done(!!confirm(__translateText__(String(message || 'Confermare?')))); }catch(__){ done(false); }
    }
  });
}

function todayISO(){
  const d = new Date();
  const yyyy = d.getFullYear();
  const mm = String(d.getMonth()+1).padStart(2,"0");
  const dd = String(d.getDate()).padStart(2,"0");
  return `${yyyy}-${mm}-${dd}`;
}

// --- Guest status LED (scheda ospiti) ---
function _dayNumFromISO(iso){
  if (!iso || typeof iso !== 'string') return null;

  // ISO datetime (es: 2026-01-05T23:00:00.000Z) -> converti in data locale (YYYY-MM-DD)
  if (iso.includes("T")) {
    const dt = new Date(iso);
    if (!isNaN(dt)) {
      return Math.floor(Date.UTC(dt.getFullYear(), dt.getMonth(), dt.getDate()) / 86400000);
    }
    iso = iso.split("T")[0];
  }

  // Support both YYYY-MM-DD and DD/MM/YYYY
  if (iso.includes('/')) {
    const parts = iso.split('/').map(n=>parseInt(n,10));
    if (parts.length === 3 && parts.every(n=>isFinite(n))) {
      const [dd,mm,yy] = parts;
      return Math.floor(Date.UTC(yy, mm-1, dd) / 86400000);
    }
  }

  const parts = iso.split('-').map(n=>parseInt(n,10));
  if (parts.length !== 3 || parts.some(n=>!isFinite(n))) return null;
  const [y,m,d] = parts;
  // day number in UTC to avoid DST issues
  return Math.floor(Date.UTC(y, m-1, d) / 86400000);
}

function __guestRemainingForLed__(item){
  try{
    const explicit = item?.rimanenza_da_pagare ?? item?.rimanenzaDaPagare ?? item?.remaining_to_pay ?? item?.remainingToPay ?? item?.guestRemaining ?? item?.remaining;
    if (explicit !== undefined && explicit !== null && String(explicit).trim() !== '') {
      const v = Number(String(explicit).replace(',', '.'));
      if (isFinite(v)) return Math.max(0, Math.round(v * 100) / 100);
    }
  }catch(_){ }
  try{
    const hasFinancialData = [
      item?.importo_prenotazione, item?.importo_prenota, item?.total, item?.importo_booking,
      item?.servizi_totale, item?.serviziTotal, item?.importo_servizi,
      item?.acconto_importo, item?.accontoImporto, item?.deposit,
      item?.saldo_pagato, item?.saldoPagato, item?.saldo,
      item?.sconto, item?.discount, item?.sconto_importo, item?.scontoImporto
    ].some(v => v !== undefined && v !== null && String(v).trim() !== '');
    if (!hasFinancialData) return null;
    const fin = (typeof _guestStayFinancials === 'function') ? _guestStayFinancials(item) : null;
    const remaining = Number(fin && fin.remaining);
    return isFinite(remaining) ? Math.max(0, Math.round(remaining * 100) / 100) : null;
  }catch(_){ return null; }
}

function guestLedStatus(item){
  const ci = item?.check_in || item?.checkIn || "";
  const co = item?.check_out || item?.checkOut || "";

  const t = _dayNumFromISO(todayISO());
  const dIn = _dayNumFromISO(ci);
  const dOut = _dayNumFromISO(co);
  const checkInDone = __guestCheckInDone__(item);

  if (t == null) return { cls: "led-gray", label: "Nessuna scadenza" };

  // dDAE_2.754 — Nuove regole LED multifunzione guest list:
  // arrivo oggi = verde (lampeggiante finché manca check-in); dopo check-in
  // diventa arancione se il checkout è domani, altrimenti verde fisso.
  // Il giorno prima del checkout = arancione, checkout = rosso, dopo checkout = azzurro.
  if (dOut != null) {
    if (t === dOut) {
      try{
        const remaining = __guestRemainingForLed__(item);
        if (isFinite(remaining) && remaining <= 0.0001) return { cls: "led-blue", label: "Ospite andato via" };
      }catch(_){ }
      return { cls: "led-red", label: "Check-out oggi" };
    }
    if (t > dOut) return { cls: "led-blue", label: "Ospite andato via" };
  }

  if (dIn != null && t === dIn) {
    if (!checkInDone) return { cls: "led-green", label: "Check-in oggi da confermare" };
    if (dOut != null && t === (dOut - 1)) return { cls: "led-orange", label: "Check-out domani" };
    return { cls: "led-green", label: "Check-in effettuato" };
  }

  if (dOut != null && t === (dOut - 1)) {
    return { cls: "led-orange", label: "Check-out domani" };
  }

  if (dIn != null) {
    if (t > dIn) return { cls: "led-green", label: "In soggiorno" };
    return { cls: "led-gray", label: "In arrivo" };
  }

  return { cls: "led-gray", label: "Nessuna data" };
}





function toISO(d){
  const yyyy = d.getFullYear();
  const mm = String(d.getMonth()+1).padStart(2,"0");
  const dd = String(d.getDate()).padStart(2,"0");
  return `${yyyy}-${mm}-${dd}`;
}

function formatISODateLocal(value){
  if (!value) return "";
  const s = String(value);

  // Already YYYY-MM-DD
  if (/^\d{4}-\d{2}-\d{2}$/.test(s)) return s;

  // ISO datetime -> local date
  if (s.includes("T")) {
    const dt = new Date(s);
    if (!isNaN(dt)) return toISO(dt); // toISO usa date locale
    return s.split("T")[0];
  }

  // Fallback: DD/MM/YYYY
  if (s.includes("/")) {
    const parts = s.split("/").map(x=>parseInt(x,10));
    if (parts.length === 3 && parts.every(n=>isFinite(n))) {
      const [dd,mm,yy] = parts;
      const dt = new Date(yy, mm-1, dd);
      return toISO(dt);
    }
  }

  // Last resort: cut
  return s.slice(0,10);
}

// 2026-01-01 -> "1 Gennaio 2026" (mese con iniziale maiuscola)
function formatLongDateIT(value){
  const iso = formatISODateLocal(value);
  if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return "";
  const [y,m,d] = iso.split("-").map(n=>parseInt(n,10));
  const dt = new Date(y, (m-1), d);
  if (isNaN(dt)) return "";
  return __capitalizeLocale__(dt.toLocaleDateString(__getCurrentLocale__(), { day: "numeric", month: "long", year: "numeric" }));
}

function formatArrivalDayIT(value){
  const iso = formatISODateLocal(value);
  if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return "";
  const [y,m,d] = iso.split("-").map(n=>parseInt(n,10));
  const dt = new Date(y, (m-1), d);
  if (isNaN(dt)) return "";
  const locale = (typeof __getCurrentLocale__ === 'function') ? __getCurrentLocale__() : 'it-IT';
  const weekday = __capitalizeLocale__(dt.toLocaleDateString(locale, { weekday: 'long' }));
  const dayMonth = __capitalizeLocale__(dt.toLocaleDateString(locale, { day: 'numeric', month: 'long' }));
  return `${weekday} ${dayMonth}`;
}

function formatRangeCompactIT(checkInValue, checkOutValue){
  const ciIso = formatISODateLocal(checkInValue);
  const coIso = formatISODateLocal(checkOutValue);
  if (!ciIso || !coIso) return "";
  if (!/^\d{4}-\d{2}-\d{2}$/.test(ciIso) || !/^\d{4}-\d{2}-\d{2}$/.test(coIso)) return "";

  const ci = new Date(ciIso + "T00:00:00");
  const co = new Date(coIso + "T00:00:00");
  if (isNaN(ci) || isNaN(co)) return "";

  // Range soggiorno: mostra check-in e check-out
  let end = new Date(co);
  if (end < ci) end = new Date(ci);

  const d1 = ci.getDate();
  const d2 = end.getDate();
  const m1 = ci.getMonth();
  const m2 = end.getMonth();
  const y1 = ci.getFullYear();
  const y2 = end.getFullYear();

  const monthCap = (dt) => {
    const locale = (typeof __getCurrentLocale__ === 'function') ? __getCurrentLocale__() : 'it-IT';
    const s = dt.toLocaleDateString(locale, { month: "long" });
    return s.charAt(0).toUpperCase() + s.slice(1);
  };

  // stesso mese/anno -> "21-24 Agosto"
  if (y1 === y2 && m1 === m2){
    return `${d1}-${d2} ${monthCap(ci)}`;
  }

  // mesi diversi -> "28 Agosto-2 Settembre"
  if (y1 === y2){
    return `${d1} ${monthCap(ci)}-${d2} ${monthCap(end)}`;
  }

  // anni diversi (raro): mantieni compatto senza anno
  return `${d1} ${monthCap(ci)}-${d2} ${monthCap(end)}`;
}

function formatGuestStayRangeLowerIT(checkInValue, checkOutValue){
  const ciIso = formatISODateLocal(checkInValue);
  const coIso = formatISODateLocal(checkOutValue);
  if (!ciIso || !coIso) return "";
  if (!/^\d{4}-\d{2}-\d{2}$/.test(ciIso) || !/^\d{4}-\d{2}-\d{2}$/.test(coIso)) return "";

  const ci = new Date(ciIso + "T00:00:00");
  const co = new Date(coIso + "T00:00:00");
  if (isNaN(ci) || isNaN(co)) return "";

  let end = new Date(co);
  if (end < ci) end = new Date(ci);

  const d1 = ci.getDate();
  const d2 = end.getDate();
  const sameMonth = ci.getFullYear() === end.getFullYear() && ci.getMonth() === end.getMonth();
  const locale = (typeof __getCurrentLocale__ === 'function') ? __getCurrentLocale__() : 'it-IT';
  const monthLower = (dt) => String(dt.toLocaleDateString(locale, { month: "long" }) || "").toLowerCase();

  if (sameMonth) return `${d1}-${d2} ${monthLower(ci)}`;
  return `${d1} ${monthLower(ci)}-${d2} ${monthLower(end)}`;
}

function __guestCardStayRangeLabel__(guest){
  try{
    const bookings = Array.isArray(guest?._groupBookings) && guest._groupBookings.length ? guest._groupBookings : [guest];
    let firstIn = "";
    let lastOut = "";
    for (const b of bookings){
      const ci = formatISODateLocal(b?.check_in ?? b?.checkIn ?? b?.arrivo ?? b?.arrival ?? b?.guestCheckIn ?? "");
      const co = formatISODateLocal(b?.check_out ?? b?.checkOut ?? b?.partenza ?? b?.departure ?? b?.guestCheckOut ?? "");
      if (ci && /^\d{4}-\d{2}-\d{2}$/.test(ci) && (!firstIn || ci < firstIn)) firstIn = ci;
      if (co && /^\d{4}-\d{2}-\d{2}$/.test(co) && (!lastOut || co > lastOut)) lastOut = co;
    }
    return formatGuestStayRangeLowerIT(firstIn, lastOut);
  }catch(_){ return ""; }
}

function formatShortDateIT(input){
  try{
    if (!input) return "";
    const s = String(input).trim();

    // ISO datetime (con T/Z): non usare slice(0,10) perché può "scalare" di 1 giorno
    if (s.includes("T")) {
      const dt = new Date(s);
      if (!isNaN(dt)){
        const dd = String(dt.getDate()).padStart(2,"0");
        const mm = String(dt.getMonth()+1).padStart(2,"0");
        const yy = String(dt.getFullYear()).slice(-2);
        return `${dd}/${mm}/${yy}`;
      }
    }

    // YYYY-MM-DD
    const iso = s.slice(0, 10);
    if (/^\d{4}-\d{2}-\d{2}$/.test(iso)){
      const [y,m,d] = iso.split("-");
      return `${d}/${m}/${y.slice(-2)}`;
    }

    // fallback Date parse
    const dt = new Date(s);
    if (!isNaN(dt)){
      const dd = String(dt.getDate()).padStart(2,"0");
      const mm = String(dt.getMonth()+1).padStart(2,"0");
      const yy = String(dt.getFullYear()).slice(-2);
      return `${dd}/${mm}/${yy}`;
    }
    return iso;
  }catch(_){
    return "";
  }
}


function formatFullDateIT(d){
  try{
    const dt = (d instanceof Date) ? d : new Date(d);
    if (isNaN(dt)) return "";
    return __capitalizeLocale__(dt.toLocaleDateString(__getCurrentLocale__(), { weekday:"long", day:"numeric", month:"long", year:"numeric" }));
  }catch(_){ return ""; }
}

function startOfLocalDay(d){
  const dt = (d instanceof Date) ? new Date(d) : new Date(d);
  dt.setHours(0,0,0,0);
  return dt;
}

function toISODateLocal(d){
  const dt = startOfLocalDay(d);
  const y = dt.getFullYear();
  const m = String(dt.getMonth()+1).padStart(2,"0");
  const da = String(dt.getDate()).padStart(2,"0");
  return `${y}-${m}-${da}`;
}




const __SPESE_BUDGET_MODE_KEY__ = 'dDAE_spese_budget_mode_v1';

function __speseBudgetModeRead__(){
  try{ return localStorage.getItem(__SPESE_BUDGET_MODE_KEY__) === 'fuori' ? 'fuori' : 'budget'; }catch(_){ return 'budget'; }
}
function __speseBudgetModeWrite__(mode){
  try{ localStorage.setItem(__SPESE_BUDGET_MODE_KEY__, String(mode || '') === 'fuori' ? 'fuori' : 'budget'); }catch(_){ }
}
function __speseBudgetModeIsFuori__(){
  try{ return __speseBudgetModeRead__() === 'fuori'; }catch(_){ return false; }
}
function __setSpeseBudgetMode__(mode, opts){
  opts = opts || {};
  const next = String(mode || '') === 'fuori' ? 'fuori' : 'budget';
  __speseBudgetModeWrite__(next);
  try{ state.speseBudgetMode = next; }catch(_){ }
  try{ if (String(state.speseCategoryFilter || '') === 'FUORI_BUDGET') state.speseCategoryFilter = ''; }catch(_){ }
  try{ __syncSpeseBudgetToggles__(); }catch(_){ }
  if (opts.render !== false){
    try{ if (state.page === 'spese') renderSpese(); }catch(_){ }
    try{ if (state.page === 'statspese') renderStatSpese(); }catch(_){ }
    try{ if (state.page === 'statgen') renderStatGen(); }catch(_){ }
    try{ if (state.page === 'statmensili') renderStatMensili(); }catch(_){ }
  }
}
function __syncSpeseBudgetToggles__(){
  const isFuori = __speseBudgetModeIsFuori__();
  ['speseBudgetToggle','statSpeseBudgetTogglePage'].forEach((id)=>{
    const btn = document.getElementById(id);
    if (!btn) return;
    btn.classList.toggle('is-on', isFuori);
    btn.setAttribute('aria-pressed', isFuori ? 'true' : 'false');
    btn.setAttribute('title', isFuori ? 'Fuori Budget' : 'Budget');
  });
}
function __bindSpeseBudgetToggles__(){
  ['speseBudgetToggle','statSpeseBudgetTogglePage'].forEach((id)=>{
    const btn = document.getElementById(id);
    if (!btn || btn.dataset.budgetToggleBound === '1') return;
    btn.dataset.budgetToggleBound = '1';
    bindFastTap(btn, (ev)=>{
      try{ ev && ev.preventDefault && ev.preventDefault(); }catch(_){ }
      __setSpeseBudgetMode__(__speseBudgetModeIsFuori__() ? 'budget' : 'fuori');
    });
  });
  __syncSpeseBudgetToggles__();
}
function __filterSpeseBudgetRows__(rows, mode){
  const wantFuori = String(mode || __speseBudgetModeRead__()) === 'fuori';
  return (Array.isArray(rows) ? rows : []).filter((row)=> __isFuoriBudgetSpesa__(row) === wantFuori);
}
function __spesaTaxKeyForItem__(row){
  const catRaw = (row?.categoria ?? row?.cat ?? '').toString().trim().toLowerCase();
  const aliq = (row?.aliquotaIva ?? row?.aliquota_iva ?? row?.aliquota ?? '').toString().trim();
  if (catRaw.includes('contant')) return 'CONTANTI';
  if (catRaw.includes('tassa') && catRaw.includes('sogg')) return 'TASSA_SOGGIORNO';
  if (catRaw.includes('iva')){
    if (catRaw.includes('22')) return 'IVA_22';
    if (catRaw.includes('10')) return 'IVA_10';
    if (catRaw.includes('4')) return 'IVA_4';
  }
  const n = parseFloat(String(aliq).replace(',', '.'));
  if (!isNaN(n)){
    if (n >= 21.5) return 'IVA_22';
    if (n >= 9.5 && n < 11.5) return 'IVA_10';
    if (n >= 3.5 && n < 5.5) return 'IVA_4';
  }
  return '';
}

function __isFuoriBudgetSpesa__(row){
  try{
    if (!row) return false;
    if (row.fuoriBudget === true || row.fuori_budget === true || row.isFuoriBudget === true || row.outOfBudget === true || row.excludeFromBudget === true) return true;
    const flag = String(row.fuoriBudget ?? row.fuori_budget ?? row.isFuoriBudget ?? row.outOfBudget ?? row.excludeFromBudget ?? '').trim().toLowerCase();
    if (['1','true','yes','si','sì','fuori','fuori_budget'].includes(flag)) return true;
    const catRaw = (row?.categoria ?? row?.cat ?? '').toString().trim().toLowerCase();
    return catRaw.includes('fuori') && catRaw.includes('budget');
  }catch(_){ return false; }
}

function spesaCategoryClass(s){
  const key = spesaGraphKeyForItem(s);
  if (key === "CONTANTI") return "spesa-bg-contanti";
  if (key === "TASSA_SOGGIORNO") return "spesa-bg-tassa";
  if (key === "IVA_22") return "spesa-bg-iva22";
  if (key === "IVA_10") return "spesa-bg-iva10";
  if (key === "IVA_4") return "spesa-bg-iva4";
  if (key === "FUORI_BUDGET") return "spesa-bg-fuoribudget";
  return ""; // nessun colore
}

function spesaGraphKeyForItem(s){
  const catRaw = (s?.categoria ?? s?.cat ?? "").toString().trim().toLowerCase();
  const aliq = (s?.aliquotaIva ?? s?.aliquota_iva ?? "").toString().trim();
  if (catRaw.includes("contant")) return "CONTANTI";
  if (catRaw.includes("tassa") && catRaw.includes("sogg")) return "TASSA_SOGGIORNO";
  if (catRaw.includes("iva")){
    if (catRaw.includes("22")) return "IVA_22";
    if (catRaw.includes("10")) return "IVA_10";
    if (catRaw.includes("4")) return "IVA_4";
  }
  const n = parseFloat(String(aliq).replace(",", "."));
  if (!isNaN(n)){
    if (n >= 21.5) return "IVA_22";
    if (n >= 9.5 && n < 11.5) return "IVA_10";
    if (n >= 3.5 && n < 5.5) return "IVA_4";
  }
  if (__isFuoriBudgetSpesa__(s)) return "CONTANTI";
  return "";
}

function __spesaCategoryNameKey__(row){
  try{
    const raw = (row?.categoria ?? row?.cat ?? '').toString().trim().toLowerCase();
    return raw ? `CAT:${raw}` : '';
  }catch(_){ return ''; }
}

function __spesaVisualKeyForRow__(row){
  try{
    return __spesaCategoryNameKey__(row) || spesaGraphKeyForItem(row) || '';
  }catch(_){ return spesaGraphKeyForItem(row) || ''; }
}

function __spesaVisualFallbackSpecForRow__(row){
  try{
    const graphKey = spesaGraphKeyForItem(row);
    const fallbackMap = {
      CONTANTI: (COLORS.CONTANTI || '#2b7cb4'),
      TASSA_SOGGIORNO: (COLORS.TASSA_SOGGIORNO || '#d8bd97'),
      IVA_22: (COLORS.IVA_22 || '#c9772b'),
      IVA_10: (COLORS.IVA_10 || '#7ac0db'),
      IVA_4: (COLORS.IVA_4 || '#1f2937'),
      FUORI_BUDGET: (COLORS.FUORI_BUDGET || '#8b5cf6')
    };
    if (graphKey && fallbackMap[graphKey]) return fallbackMap[graphKey];
    const cat = (row?.categoria ?? row?.cat ?? '').toString().trim().toLowerCase();
    if (cat.includes('contant')) return fallbackMap.CONTANTI;
    if (cat.includes('tassa') && cat.includes('sogg')) return fallbackMap.TASSA_SOGGIORNO;
    return '#2B7CB4';
  }catch(_){ return '#2B7CB4'; }
}





function calcStayNights(ospite){
  // Calcola le notti tra check-in e check-out (date ISO), robusto per Safari/iOS (usa Date.UTC)
  const inRaw  = ospite?.check_in ?? ospite?.checkIn ?? "";
  const outRaw = ospite?.check_out ?? ospite?.checkOut ?? "";
  const inISO  = formatISODateLocal(inRaw);
  const outISO = formatISODateLocal(outRaw);

  if (!inISO || !outISO) return null;
  if (!/^\d{4}-\d{2}-\d{2}$/.test(inISO) || !/^\d{4}-\d{2}-\d{2}$/.test(outISO)) return null;

  const [yi, mi, di] = inISO.split("-").map(n => parseInt(n, 10));
  const [yo, mo, do_] = outISO.split("-").map(n => parseInt(n, 10));

  const tIn  = Date.UTC(yi, mi - 1, di);
  const tOut = Date.UTC(yo, mo - 1, do_);

  const diff = Math.round((tOut - tIn) / 86400000);
  if (!isFinite(diff) || diff <= 0) return null;
  return diff;
}

function formatEUR(value){
  const n = Number(value || 0);
  try{
    return n.toLocaleString("it-IT", { style: "currency", currency: "EUR" });
  }catch(_){
    // fallback
    return "€" + (Math.round(n * 100) / 100).toFixed(2).replace(".", ",");
  }
}

function calcTouristTax(ospite, nights){
  // Tassa di soggiorno: per persona > 10 anni (usa 'adulti'), max notti configurabile
  const adultsRaw = ospite?.adulti ?? ospite?.adults ?? 0;
  const adults = Math.max(0, parseInt(adultsRaw, 10) || 0);

  const nNights = Math.max(0, parseInt(nights, 10) || 0);
  const maxTaxableNightsRaw = (state.settings && state.settings.loaded) ? getSettingNumber("tassa_soggiorno_max_notti", 3) : 3;
  const maxTaxableNights = Math.max(0, parseInt(maxTaxableNightsRaw, 10) || 0);
  const taxableDays = Math.min(nNights, maxTaxableNights);

  const rate = getSettingNumber("tassa_soggiorno", (typeof TOURIST_TAX_EUR_PPN !== "undefined" ? TOURIST_TAX_EUR_PPN : 0));
  const r = isFinite(rate) ? Math.max(0, rate) : 0;

  const total = adults * taxableDays * r;
  return { total, adults, taxableDays, rate: r };
}

function buildNightsDotHTML(nights){
  const n = Math.max(0, parseInt(nights, 10) || 0);
  if (!n) return ``;
  return `<span class="nights-dot" aria-label="Notti" style="${__roomsUiBadgeStyle__(getRoomsUiConfig().nights || 'indigo-4')}">${n}</span>`;
}

function updateGuestTaxTotalPill(targetGuest){
  try{
    // dDAE: in modalità 'create' non mostrare la tassa di soggiorno
    if (String(state.guestMode||'').toLowerCase()==='create'){
      const el0 = document.getElementById('guestTaxTotal');
      if (el0) el0.hidden = true;
      return;
    }

    const el = document.getElementById("guestTaxTotal");
    if (!el) return;
    const valEl = document.getElementById("guestTaxTotalVal");
    if (!(state.settings && state.settings.loaded) && !updateGuestTaxTotalPill.__settingsRefreshPending){
      updateGuestTaxTotalPill.__settingsRefreshPending = true;
      try{ ensureSettingsLoaded({ force:false, showLoader:false }).then(() => { updateGuestTaxTotalPill.__settingsRefreshPending = false; updateGuestTaxTotalPill(targetGuest); }).catch(() => { updateGuestTaxTotalPill.__settingsRefreshPending = false; }); }catch(_){ updateGuestTaxTotalPill.__settingsRefreshPending = false; }
    }

    const activeGuest = targetGuest || ((String(state.guestMode || '').toLowerCase() === 'view' && String(state.guestGroupActiveId || '').trim()) ? __guestActiveBookingForAction__() : null);
    const list = activeGuest
      ? [activeGuest]
      : ((Array.isArray(state.guestGroupBookings) && state.guestGroupBookings.length)
        ? state.guestGroupBookings
        : (state.guestViewItem ? [state.guestViewItem] : []));

    let sum = 0;
    for (const g of (list || [])){
      const nights = calcStayNights(g);
      if (nights == null) continue;
      const tt = calcTouristTax(g, nights);
      sum += Number(tt?.total || 0) || 0;
    }

    if (!isFinite(sum) || sum <= 0){
      if (valEl) valEl.textContent = "€0,00";
      el.hidden = true;
      return;
    }

    el.hidden = false;
    const txt = formatEUR(sum);
    if (valEl) valEl.textContent = txt;
    else el.textContent = txt;
  }catch(_){}
}


function monthRangeISO(date = new Date()){
  const y = date.getFullYear();
  const m = date.getMonth();
  const start = new Date(y, m, 1);
  const end = new Date(y, m+1, 0);
  return [toISO(start), toISO(end)];
}


// Period preset (scroll picker iOS) — nessuna API extra
let periodSyncLock = 0;
let presetSyncLock = 0;

function addDaysISO(iso, delta){
  const [y,m,d] = iso.split("-").map(n=>parseInt(n,10));
  const dt = new Date(y, (m-1), d);
  dt.setDate(dt.getDate() + delta);
  return toISO(dt);
}

function monthRangeFromYM(ym){
  const [yy,mm] = ym.split("-").map(n=>parseInt(n,10));
  const start = new Date(yy, mm-1, 1);
  const end = new Date(yy, mm, 0);
  return [toISO(start), toISO(end)];
}

function recentMonths(n=8){
  const out = [];
  const d = new Date();
  d.setDate(1);
  for (let i=0;i<n;i++){
    const y = d.getFullYear();
    const m = String(d.getMonth()+1).padStart(2,"0");
    out.push(`${y}-${m}`);
    d.setMonth(d.getMonth()-1);
  }
  return out;
}

function buildPeriodPresetOptions(){
  const opts = [
    { value:"this_month", label:"Questo mese" },
    { value:"last_month", label:"Mese scorso" },
    { value:"last_7", label:"Ultimi 7 giorni" },
    { value:"last_30", label:"Ultimi 30 giorni" },
    { value:"ytd", label:"Anno corrente" },
    { value:"all", label:"Tutto" },
  ];
  for (const ym of recentMonths(8)){
    opts.push({ value:`month:${ym}`, label: ym });
  }
  opts.push({ value:"custom", label:"Personalizzato" });
  return opts;
}

function fillPresetSelect(selectEl){
  if (!selectEl) return;
  const opts = buildPeriodPresetOptions();
  selectEl.innerHTML = "";
  for (const o of opts){
    const opt = document.createElement("option");
    opt.value = o.value;
    opt.textContent = o.label;
    selectEl.appendChild(opt);
  }
}

function setPresetValue(value){
  state.periodPreset = value;
  presetSyncLock += 1;
  try {
    const sels = ["#periodPreset1","#periodPreset2","#periodPreset3"]
      .map(s => document.querySelector(s))
      .filter(Boolean);
    for (const s of sels) s.value = value;
  } finally {
    presetSyncLock -= 1;
  }
}

function presetToRange(value){
  const today = todayISO();
  const ySel = Number(state.exerciseYear || new Date().getFullYear());
  const y = (isFinite(ySel) && ySel >= 2000 && ySel <= 2100) ? ySel : new Date().getFullYear();

  // Helpers: month range for selected exercise year
  const monthRangeForExerciseYear = (dateObj)=>{
    const d = new Date(dateObj);
    try{ d.setFullYear(y); }catch(_){}
    return monthRangeISO(d);
  };

  if (value === "this_month") return monthRangeForExerciseYear(new Date());

  if (value === "last_month"){
    const d = new Date();
    d.setMonth(d.getMonth()-1);
    return monthRangeForExerciseYear(d);
  }

  if (value === "last_7") return [addDaysISO(today, -6), today];
  if (value === "last_30") return [addDaysISO(today, -29), today];

  // "Anno corrente" = anno di esercizio selezionato (tutto l'anno)
  if (value === "ytd" || value === "all"){
    return [`${y}-01-01`, `${y}-12-31`];
  }

  if (value && value.startsWith("month:")){
    const ym = value.split(":")[1]; // YYYY-MM
    return monthRangeFromYM(ym);
  }

  return null;
}

function bindPresetSelect(sel){
  const el = document.querySelector(sel);
  if (!el) return;
  fillPresetSelect(el);
  el.value = state.periodPreset || "this_month";

  el.addEventListener("change", async () => {
    if (presetSyncLock > 0) return;
    const v = el.value;
    const range = presetToRange(v);
    setPresetValue(v);
    if (!range) return;
    const [from,to] = range;

    setPeriod(from,to);

    try { await onPeriodChanged({ showLoader:false }); } catch (e) { toast(e.message); }
  });
}

function categoriaLabel(cat){
  return ({
    CONTANTI: "Contanti",
    TASSA_SOGGIORNO: "Tassa soggiorno",
    IVA_22: "IVA 22%",
    IVA_10: "IVA 10%",
    IVA_4: "IVA 4%",
    FUORI_BUDGET: "Fuori Budget",
  })[cat] || cat;
}

function __spesaCategoriaDisplay__(row){
  try{
    const key = spesaGraphKeyForItem(row);
    if (key) return categoriaLabel(key);
    const raw = (row?.categoria ?? row?.cat ?? '').toString().trim();
    return raw || '';
  }catch(_){ return ''; }
}

function __apiProfile(action, method, body){
  const a = String(action || "").trim().toLowerCase();
  const m = String(method || "").trim().toUpperCase();
  // Default: breve per evitare loader infinito su iOS
  let timeoutMs = 15000;
  let retries = 0;

  // Lista spesa (colazione + prodotti pulizia): più lenta su rete iOS / Apps Script
  if (a === "colazione" || a === "prodotti_pulizia"){
    timeoutMs = (m === "GET") ? 45000 : 60000;
    retries = 2;
  }

  return { timeoutMs, retries };
}

async function api(action, { method="GET", params={}, body=null, showLoader=true } = {}){
  if (showLoader) beginRequest();
  let realMethod = method; // definito subito per evitare ReferenceError nel finally
  try {
  // LOCAL: nessuna chiamata a Google/Apps Script
  if (typeof __LOCAL_MODE__ !== "undefined" && __LOCAL_MODE__) {
    try{ __syncLedBegin(realMethod); }catch(_){ }
    return await __localApi__(action, { method: realMethod, params: (params||{}), body });
  }
  if (!API_BASE_URL || API_BASE_URL.includes("INCOLLA_QUI")) {
    throw new Error("Config mancante: imposta API_BASE_URL in config.js");
  }

  const url = new URL(API_BASE_URL);
  url.searchParams.set("action", action);
  url.searchParams.set("apiKey", API_KEY);
  // Cache-busting for iOS/Safari aggressive caching
  url.searchParams.set("_ts", String(Date.now()));

  // Context multi-account (user + anno)
  try{
    if (state && state.session && state.session.user_id && action !== "utenti" && action !== "ping"){
      if (!params) params = {};
      if (params.user_id === undefined || params.user_id === null || String(params.user_id).trim() === "") {
        params.user_id = String(state.session.user_id);
      }
      if (params.anno === undefined || params.anno === null || String(params.anno).trim() === "") {
        params.anno = String(state.exerciseYear || new Date().getFullYear());
      }
    }
  }catch(_){ }

  Object.entries(params || {}).forEach(([k,v]) => {
    if (v !== undefined && v !== null && String(v).length) url.searchParams.set(k, v);
  });
  if (method === "PUT" || method === "DELETE") {
    url.searchParams.set("_method", method);
    realMethod = "POST";
  }
  const { timeoutMs, retries } = __apiProfile(action, realMethod, body);

  // Sync LED: verde=lettura, rosso=scrittura
  try{ __syncLedBegin(realMethod); }catch(_){}


  const baseFetchOpts = {
    method: realMethod,
    cache: "no-store",
  };

  // Headers/body solo quando serve (riduce rischi di preflight su Safari iOS)
  if (realMethod !== "GET") {
    baseFetchOpts.headers = { "Content-Type": "text/plain;charset=utf-8" };
    let payload = body;
    // Inietta user_id/anno su POST/PUT (se mancano)
    try{
      if (state && state.session && state.session.user_id && action !== "utenti"){
        const uid = String(state.session.user_id);
        const yr = String(state.exerciseYear || new Date().getFullYear());
        const addCtx = (o)=>{
          if (!o || typeof o !== "object") return o;
          if (o.user_id === undefined || o.user_id === null || String(o.user_id).trim() === "") o.user_id = uid;
          if (o.anno === undefined || o.anno === null || String(o.anno).trim() === "") o.anno = yr;
          return o;
        };

        const deep = (x, depth = 0)=>{
          if (!x || typeof x !== "object") return x;
          if (Array.isArray(x)) return x.map(v => deep(v, depth));
          addCtx(x);
          if (depth >= 1) return x;
          // pattern comuni: bulk payloads
          ["rows","items","records","data","list"].forEach((k)=>{
            const v = x[k];
            if (Array.isArray(v)) x[k] = v.map(r => deep(r, depth + 1));
          });
          return x;
        };

        payload = deep(payload, 0);
      }
    }catch(_){ }
    baseFetchOpts.body = payload ? JSON.stringify(payload) : "{}";
  }

  const sleep = (ms) => new Promise(r => setTimeout(r, ms));

  let res;
  for (let attempt = 0; attempt <= retries; attempt++){
    // Timeout concreto: evita loader infinito su iOS quando la rete “si pianta”
    const controller = new AbortController();
    const t = setTimeout(() => controller.abort(), timeoutMs);

    try{
      res = await fetch(url.toString(), Object.assign({}, baseFetchOpts, { signal: controller.signal }));
      break;
    }catch(err){
      const msg = String((err && err.message) || err || "");
      const low = msg.toLowerCase();
      const name = String((err && err.name) || "").toLowerCase();

      const isAbort = name === "aborterror" || low.includes("fetch is aborted") || low.includes("aborted");
      const isNet = low.includes("failed to fetch") || low.includes("networkerror") || low.includes("load failed");
      const retryable = isAbort || isNet;

      if (attempt < retries && retryable){
        const backoff = Math.min(8000, 700 * (2 ** attempt));
        const jitter = Math.floor(Math.random() * 200);
        await sleep(backoff + jitter);
        continue;
      }

      if (isAbort){
        throw new Error("Operazione annullata: connessione lenta o instabile (timeout). Riprova.");
      }

      if (isNet){
        throw new Error("Connessione assente o instabile. Verifica la rete e riprova. Se il problema persiste: 1) Web App Apps Script distribuita come 'Chiunque', 2) URL /exec corretto, 3) hai ridistribuito una nuova versione dello script dopo modifiche.");
      }

      throw err;
    }finally{
      clearTimeout(t);
    }
  }

  let json;
try {
  json = await res.json();
} catch (_) {
  throw new Error("Risposta non valida dal server");
}

if (!json.ok) throw new Error(json.error || "API error");
return json.data;
  } finally { try{ __syncLedEnd(realMethod); }catch(_){}
  if (showLoader) endRequest(); }
}


// =========================
// IMPOSTAZIONI (foglio Google "impostazioni")
// Chiavi usate:
// - operatori  -> colonne operatore_1/2/3
// - tariffa_oraria -> value (number)
// - costo_benzina  -> value (number)
// - tassa_soggiorno -> value (number)
// - tassa_soggiorno_max_notti -> value (integer, max notti tassabili)
// =========================

function __normKey(k) {
  return String(k || "").trim().toLowerCase();
}

function __parseSettingsRows(rows) {
  const byKey = {};
  (Array.isArray(rows) ? rows : []).forEach(r => {
    const key = __normKey(r?.key ?? r?.Key ?? r?.KEY);
    if (!key) return;
    byKey[key] = r;
  });
  return byKey;
}

function getSettingRow(key) {
  const k = __normKey(key);
  return (state.settings && state.settings.byKey && state.settings.byKey[k]) ? state.settings.byKey[k] : null;
}

function getSettingText(key, fallback = "") {
  const row = getSettingRow(key);
  const v = row ? (row.value ?? row.Value ?? row.val ?? "") : "";
  const s = String(v ?? "").trim();
  return s ? s : String(fallback ?? "");
}

function getSettingNumber(key, fallback = 0) {
  const row = getSettingRow(key);
  let v = row ? (row.value ?? row.Value ?? row.val ?? "") : "";
  if (v === null || v === undefined) v = "";
  const s = String(v).trim().replace(",", ".");
  if (!s) return Number(fallback) || 0;
  const n = Number(s);
  return isFinite(n) ? n : (Number(fallback) || 0);
}

const __LAUNDRY_DEFAULT_COMPONENTS__ = [
  { id: "lc-mat", titolo: "Lenzuolo Matrimoniale", abbreviazione: "MAT", prezzo: 0, colore: "blue" },
  { id: "lc-sin", titolo: "Lenzuolo Singolo", abbreviazione: "SIN", prezzo: 0, colore: "blue" },
  { id: "lc-fed", titolo: "Federe", abbreviazione: "FED", prezzo: 0, colore: "blue" },
  { id: "lc-tdo", titolo: "Telo Doccia", abbreviazione: "TDO", prezzo: 0, colore: "orange" },
  { id: "lc-tfa", titolo: "Telo Faccia", abbreviazione: "TFA", prezzo: 0, colore: "orange" },
  { id: "lc-tbi", titolo: "Telo Bidet", abbreviazione: "TBI", prezzo: 0, colore: "orange" },
  { id: "lc-tap", titolo: "Tappeto", abbreviazione: "TAP", prezzo: 0, colore: "sand" },
  { id: "lc-tpi", titolo: "Telo Piscina", abbreviazione: "TPI", prezzo: 0, colore: "purple" },
];

const __LAUNDRY_COMPONENT_TRANSLATIONS__ = {
  MAT: { it: "Lenzuolo Matrimoniale", en: "Double Bed Sheet", fr: "Drap Double", de: "Doppelbettlaken", es: "Sábana Matrimonial" },
  SIN: { it: "Lenzuolo Singolo", en: "Single Bed Sheet", fr: "Drap Simple", de: "Einzelbettlaken", es: "Sábana Individual" },
  FED: { it: "Federe", en: "Pillowcases", fr: "Taies d'oreiller", de: "Kissenbezüge", es: "Fundas de Almohada" },
  TDO: { it: "Telo Doccia", en: "Bath Towel", fr: "Serviette de Douche", de: "Duschtuch", es: "Toalla de Ducha" },
  TFA: { it: "Telo Faccia", en: "Face Towel", fr: "Serviette Visage", de: "Handtuch", es: "Toalla de Cara" },
  TBI: { it: "Telo Bidet", en: "Bidet Towel", fr: "Serviette Bidet", de: "Bidettuch", es: "Toalla de Bidé" },
  TAP: { it: "Tappeto", en: "Bath Mat", fr: "Tapis de Bain", de: "Badematte", es: "Alfombra de Baño" },
  TPI: { it: "Telo Piscina", en: "Pool Towel", fr: "Serviette Piscine", de: "Poolhandtuch", es: "Toalla de Piscina" },
};

function __laundryDisplayTitle__(itemOrCode, fallbackTitle = ""){
  const code = __normalizeLaundryCode__(typeof itemOrCode === 'string' ? itemOrCode : (itemOrCode?.abbreviazione ?? itemOrCode?.code));
  const base = String(typeof itemOrCode === 'string' ? fallbackTitle : (itemOrCode?.titolo ?? itemOrCode?.nome ?? itemOrCode?.label ?? itemOrCode?.title ?? fallbackTitle) || '').trim();
  const lang = __getAppLanguage__();
  const translated = code && __LAUNDRY_COMPONENT_TRANSLATIONS__[code] ? String(__LAUNDRY_COMPONENT_TRANSLATIONS__[code]?.[lang] || __LAUNDRY_COMPONENT_TRANSLATIONS__[code]?.it || '').trim() : '';
  if (translated) return translated;
  if (base){
    try{
      const next = __translateText__(base);
      return String(next || base).trim() || base;
    }catch(_){ return base; }
  }
  return code || '';
}

const __LAUNDRY_RESERVED_KEYS__ = new Set([
  "id","startdate","start_date","enddate","end_date","from","to","createdat","created_at","updatedat","updated_at","deletedat","deleted_at","isdeleted","is_deleted","deleted","totalcost","laundryprices","laundry_prices","laundrycatalog","laundry_catalog"
]);
const __LAUNDRY_CATALOG_CACHE_KEY__ = "ddae_laundry_catalog_v1";

function __persistLaundryCatalogCache__(list){
  try{
    const clean = __sanitizeLaundryCatalogList__(list, { fallbackToDefault: false });
    localStorage.setItem(__LAUNDRY_CATALOG_CACHE_KEY__, JSON.stringify(clean));
    return clean;
  }catch(_){ return []; }
}

function __readLaundryCatalogCache__(){
  try{
    const raw = localStorage.getItem(__LAUNDRY_CATALOG_CACHE_KEY__);
    if (!String(raw || '').trim()) return [];
    const parsed = JSON.parse(String(raw || '[]'));
    return __sanitizeLaundryCatalogList__(parsed, { fallbackToDefault: false });
  }catch(_){ return []; }
}

function __normalizeLaundryColor__(value){
  return __normalizeOperatoreColor__(value);
}

function __normalizeLaundryCode__(value){
  return String(value || '').trim().toUpperCase().replace(/[^A-Z0-9_]/g, '').slice(0, 10);
}

function __laundryLegacyPriceMap__(){
  const row = getSettingRow("laundry_prices");
  const raw = row ? (row.value ?? row.Value ?? "") : "";
  const out = {};
  if (!String(raw || '').trim()) return out;
  try{
    const parsed = JSON.parse(String(raw || '{}'));
    Object.keys(parsed || {}).forEach((key) => {
      const code = __normalizeLaundryCode__(key);
      if (!code) return;
      const n = Number(String(parsed?.[key] ?? '').replace(',', '.'));
      out[code] = (isFinite(n) && n >= 0) ? Math.round(n * 100) / 100 : 0;
    });
  }catch(_){ }
  return out;
}

function __defaultLaundryCatalogFromLegacy__(){
  const priceMap = __laundryLegacyPriceMap__();
  return __LAUNDRY_DEFAULT_COMPONENTS__.map((item, idx) => ({
    id: String(item.id || `lc-${idx+1}`),
    titolo: String(item.titolo || '').trim(),
    abbreviazione: __normalizeLaundryCode__(item.abbreviazione || item.code || item.sigla),
    prezzo: (() => { const n = Number(priceMap?.[__normalizeLaundryCode__(item.abbreviazione)] ?? item.prezzo ?? 0); return isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0; })(),
    colore: __normalizeLaundryColor__(item.colore || 'blue'),
  }));
}

function __sanitizeLaundryCatalogList__(list, { fallbackToDefault = true } = {}){
  const baseMap = new Map(__LAUNDRY_DEFAULT_COMPONENTS__.map(item => [__normalizeLaundryCode__(item.abbreviazione), item]));
  const input = Array.isArray(list) ? list : [];
  const seen = new Set();
  const out = [];
  input.forEach((item, idx) => {
    const code = __normalizeLaundryCode__(item?.abbreviazione ?? item?.code ?? item?.sigla ?? item?.key);
    const legacy = code ? baseMap.get(code) : null;
    const titolo = String(item?.titolo ?? item?.nome ?? item?.label ?? item?.title ?? legacy?.titolo ?? code).trim();
    if (!code || !titolo || seen.has(code)) return;
    const rawPrice = item?.prezzo ?? item?.price ?? item?.prezzo_pulizia ?? item?.unitPrice ?? item?.costo;
    const priceNum = Number(String(rawPrice ?? legacy?.prezzo ?? 0).replace(',', '.'));
    out.push({
      id: String(item?.id || `lc-${Date.now()}-${idx}`),
      titolo,
      abbreviazione: code,
      prezzo: isFinite(priceNum) && priceNum >= 0 ? Math.round(priceNum * 100) / 100 : 0,
      colore: __normalizeLaundryColor__(item?.colore ?? item?.color ?? legacy?.colore ?? 'blue'),
      coloreTesto: __normalizeOptionalOperatoreColor__(item?.coloreTesto ?? item?.textColor ?? legacy?.coloreTesto ?? ''),
    });
    seen.add(code);
  });
  return out.length ? out : (fallbackToDefault ? __defaultLaundryCatalogFromLegacy__() : []);
}

function getLaundryCatalogFromSettings(){
  const row = getSettingRow("laundry_catalogo");
  const raw = row ? (row.value ?? row.Value ?? "") : "";
  if (String(raw || '').trim()){
    try{
      const parsed = JSON.parse(String(raw || '[]'));
      const clean = __sanitizeLaundryCatalogList__(parsed, { fallbackToDefault: true });
      try{ if (clean && clean.length) __persistLaundryCatalogCache__(clean); }catch(_){ }
      return clean;
    }catch(_){ }
  }
  const cached = __readLaundryCatalogCache__();
  if (cached.length) return cached;
  return __defaultLaundryCatalogFromLegacy__();
}

function getLaundryComponentCodes(){
  return getLaundryCatalogFromSettings().map(item => String(item?.abbreviazione || '').trim()).filter(Boolean);
}

function __laundryCatalogMapByCode__(catalog){
  const map = new Map();
  (Array.isArray(catalog) ? catalog : []).forEach((item) => {
    const code = __normalizeLaundryCode__(item?.abbreviazione ?? item?.code);
    if (!code || map.has(code)) return;
    map.set(code, {
      id: String(item?.id || code),
      titolo: String(item?.titolo ?? item?.nome ?? item?.label ?? item?.title ?? code).trim() || code,
      abbreviazione: code,
      prezzo: (() => { const n = Number(String(item?.prezzo ?? item?.price ?? item?.unitPrice ?? 0).replace(',', '.')); return isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0; })(),
      colore: __normalizeLaundryColor__(item?.colore ?? item?.color ?? 'blue'),
      coloreTesto: __normalizeOptionalOperatoreColor__(item?.coloreTesto ?? item?.textColor ?? ''),
    });
  });
  return map;
}

function __detectLaundryCodesFromRecord__(it){
  const set = new Set();
  try{ getLaundryComponentCodes().forEach(code => { if (code) set.add(code); }); }catch(_){ }
  Object.keys(it || {}).forEach((rawKey) => {
    const lowered = String(rawKey || '').trim().toLowerCase();
    if (!lowered || __LAUNDRY_RESERVED_KEYS__.has(lowered)) return;
    let candidate = lowered;
    if (/_resi$/i.test(rawKey)) candidate = lowered.slice(0, -5);
    else if (/_r$/i.test(rawKey)) candidate = lowered.slice(0, -2);
    else if (/r$/i.test(rawKey) && lowered.length > 1) candidate = lowered.slice(0, -1);
    const code = __normalizeLaundryCode__(candidate);
    if (!code) return;
    const rawVal = it?.[rawKey];
    const numeric = Number(rawVal);
    if (isFinite(numeric) && Math.abs(numeric) <= 0) return;
    if (!String(rawVal ?? '').trim()) return;
    set.add(code);
  });
  return Array.from(set).filter(Boolean);
}

function __getLaundryCatalogForRecord__(it){
  const settingsCatalog = getLaundryCatalogFromSettings();
  const settingsMap = __laundryCatalogMapByCode__(settingsCatalog);
  const snapshot = __sanitizeLaundryCatalogList__(Array.isArray(it?.laundryCatalog) ? it.laundryCatalog : (Array.isArray(it?.laundry_catalog) ? it.laundry_catalog : []), { fallbackToDefault: false });
  const snapshotMap = __laundryCatalogMapByCode__(snapshot);
  const sourceList = snapshot.length ? snapshot : settingsCatalog;
  const map = __laundryCatalogMapByCode__(sourceList);
  const extras = __detectLaundryCodesFromRecord__(it);
  extras.forEach((code, idx) => {
    if (map.has(code)) return;
    const fromSnapshot = snapshotMap.get(code);
    const fromSettings = settingsMap.get(code);
    const source = fromSnapshot || fromSettings || null;
    map.set(code, {
      id: String(source?.id || `lc-extra-${idx}`),
      titolo: String(source?.titolo || code).trim() || code,
      abbreviazione: code,
      prezzo: (() => { const n = Number(source?.prezzo ?? 0); return isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0; })(),
      colore: __normalizeLaundryColor__(source?.colore || 'blue'),
    });
  });
  const ordered = [];
  const pushFrom = (arr) => {
    (Array.isArray(arr) ? arr : []).forEach((item) => {
      const code = __normalizeLaundryCode__(item?.abbreviazione ?? item?.code);
      if (!code || !map.has(code) || ordered.some(row => row.abbreviazione === code)) return;
      ordered.push(map.get(code));
    });
  };
  pushFrom(sourceList);
  Array.from(map.keys()).forEach((code) => {
    if (!ordered.some(row => row.abbreviazione === code)) ordered.push(map.get(code));
  });
  return ordered.filter(item => item && item.abbreviazione);
}

function getLaundryPricesFromSettings(){
  const out = {};
  getLaundryCatalogFromSettings().forEach((item) => {
    const code = __normalizeLaundryCode__(item?.abbreviazione);
    if (!code) return;
    const n = Number(item?.prezzo || 0);
    out[code] = isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0;
  });
  return out;
}

async function saveLaundryCatalogToSettings(list){
  const clean = __sanitizeLaundryCatalogList__(list, { fallbackToDefault: false });
  try{ __persistLaundryCatalogCache__(clean); }catch(_){ }
  const priceMap = {};
  clean.forEach((item) => {
    priceMap[item.abbreviazione] = Math.round((Number(item.prezzo || 0) || 0) * 100) / 100;
  });
  await api("impostazioni", {
    method: "POST",
    body: {
      laundry_catalogo: clean,
      laundry_prices: priceMap,
    },
    showLoader: true,
  });
  await ensureSettingsLoaded({ force: true, showLoader: false });
  return clean;
}

async function saveLaundryPricesToSettings(prices){
  const map = prices && typeof prices === 'object' ? prices : {};
  const next = getLaundryCatalogFromSettings().map((item) => {
    const code = __normalizeLaundryCode__(item?.abbreviazione);
    const raw = Number(String(map?.[code] ?? item?.prezzo ?? 0).replace(',', '.'));
    return { ...item, prezzo: isFinite(raw) && raw >= 0 ? Math.round(raw * 100) / 100 : 0 };
  });
  await saveLaundryCatalogToSettings(next);
  return getLaundryPricesFromSettings();
}

function __laundryMoneyFmt__(value){
  const n = Number(value || 0);
  return euro(isFinite(n) ? n : 0);
}

function __laundryComputeTotalCost__(item, prices){
  const priceMap = (prices && typeof prices === 'object') ? prices : getLaundryPricesFromSettings();
  const defs = __getLaundryCatalogForRecord__(item);
  let total = 0;
  defs.forEach((def) => {
    const code = String(def?.abbreviazione || '').trim();
    if (!code) return;
    const qty = Math.max(0, Number(item?.[code] || 0) || 0);
    const unit = Math.max(0, Number(priceMap?.[code] ?? def?.prezzo ?? 0) || 0);
    total += qty * unit;
  });
  return Math.round(total * 100) / 100;
}

function getOperatorNamesFromSettings() {
  try{
    const catalog = getOperatoriCatalogFromSettings ? getOperatoriCatalogFromSettings() : [];
    const names = (Array.isArray(catalog) ? catalog : []).map(item => String(item?.nome || '').trim()).filter(Boolean);
    if (names.length) return names;
  }catch(_){ }
  const row = getSettingRow("operatori");
  const op1 = String(row?.operatore_1 ?? row?.Operatore_1 ?? row?.operatore1 ?? "").trim();
  const op2 = String(row?.operatore_2 ?? row?.Operatore_2 ?? row?.operatore2 ?? "").trim();
  const op3 = String(row?.operatore_3 ?? row?.Operatore_3 ?? row?.operatore3 ?? "").trim();
  return [op1, op2, op3].filter(Boolean);
}



const __OPERATORI_COLOR_KEYS__ = ["red","orange","yellow","acid","green","mint","sky","blue","gray","indigo","violet","pink","beige"];
const __OPERATORI_COLOR_SHADE_COUNT__ = 6;
const __OPERATORI_COLOR_DEFAULT_SHADE__ = 4;
const __OPERATORI_COLOR_TONES__ = {
  red:    ["#fff1ef", "#ffd6d3", "#ffaca5", "#ff6f61", "#e24a3f", "#b92a23"],
  orange: ["#fff0dd", "#ffe0bf", "#ffc27a", "#ff9f33", "#e07f13", "#b96100"],
  yellow: ["#fff9d9", "#fff4b3", "#ffe47a", "#ffd43b", "#e0b800", "#b38f00"],
  acid:   ["#fbffd9", "#f2ffb3", "#ddff66", "#bfff00", "#98cc00", "#6f9900"],
  green:  ["#eefbf1", "#d9f7df", "#9fe7af", "#52c96a", "#2f9e44", "#1b6f31"],
  mint:   ["#effffc", "#cffff8", "#8ff0e8", "#26d9c7", "#13b8a6", "#0b7f74"],
  sky:    ["#eef8ff", "#d7ecff", "#9dd8ff", "#5bbcff", "#2695e8", "#1769aa"],
  blue:   ["#eef6ff", "#cfe4ff", "#8cbcff", "#3d8bfd", "#1f6fe5", "#0a58ca"],
  gray:   ["#FFFFFF", "#F2F2F2", "#D9D9D9", "#A6A6A6", "#4D4D4D", "#000000"],
  indigo: ["#f0efff", "#ddd9ff", "#b4a8ff", "#7c6ff2", "#6357dc", "#4338b8"],
  violet: ["#f7f0ff", "#e9dbff", "#cfb3ff", "#a678ff", "#7a46d1", "#5a2ea6"],
  pink:   ["#fff0f5", "#ffd6e4", "#ff9fc0", "#ff5f93", "#e04377", "#b92b58"],
  beige:  ["#fff7ef", "#f7ead8", "#ead1ad", "#d8b98b", "#c49a65", "#9c754c"],
};

function __parseOperatoreColorSpec__(value){
  const raw = String(value || "").trim().toLowerCase();
  const legacyBaseMap = { purple:'indigo', sand:'beige', lime:'acid', acidgreen:'acid', greenacid:'acid', grey:'gray' };
  const m = raw.match(/^([a-z]+)(?:[-_: ]?([1-6]))?$/i);
  const requestedBase = m ? String(m[1] || '').toLowerCase() : '';
  const normalizedBase = legacyBaseMap[requestedBase] || requestedBase;
  const base = __OPERATORI_COLOR_KEYS__.includes(normalizedBase) ? normalizedBase : 'blue';
  const shade = Math.min(__OPERATORI_COLOR_SHADE_COUNT__, Math.max(1, parseInt((m && m[2]) || String(__OPERATORI_COLOR_DEFAULT_SHADE__), 10) || __OPERATORI_COLOR_DEFAULT_SHADE__));
  return { base, shade, spec: `${base}-${shade}` };
}

function __normalizeOperatoreColor__(value){
  return __parseOperatoreColorSpec__(value).spec;
}

function __operatoreColorBase__(value){
  return __parseOperatoreColorSpec__(value).base;
}

function __operatoreColorShade__(value){
  return __parseOperatoreColorSpec__(value).shade;
}

function __operatoreColorHex__(color){
  const { base, shade } = __parseOperatoreColorSpec__(color);
  const tones = __OPERATORI_COLOR_TONES__[base] || __OPERATORI_COLOR_TONES__.blue;
  return tones[Math.max(0, Math.min(tones.length - 1, shade - 1))] || tones[1] || '#6f84c8';
}

function __normalizeOptionalOperatoreColor__(value){
  const raw = String(value || '').trim();
  return raw ? __normalizeOperatoreColor__(raw) : '';
}

function __tagColorTextAutoHex__(spec, preferWhite = false){
  if (preferWhite) return '#ffffff';
  try{
    const parsed = __parseOperatoreColorSpec__(spec || 'blue-4');
    if ((['yellow','beige','acid','mint','sky','gray','violet'].includes(parsed.base) && parsed.shade <= 4) || (parsed.base === 'orange' && parsed.shade <= 2)) return '#0b1f3a';
  }catch(_){ }
  return '#ffffff';
}

function __tagColorTextHex__(bgSpec, fgSpec, preferWhite = false){
  const custom = __normalizeOptionalOperatoreColor__(fgSpec);
  if (custom) return __operatoreColorHex__(custom);
  return __tagColorTextAutoHex__(bgSpec, preferWhite);
}

function __tagColorInlineStyle__(bgSpec, fgSpec, opts){
  const options = (opts && typeof opts === 'object') ? opts : {};
  const bg = __normalizeOperatoreColor__(bgSpec || options.fallbackBg || 'blue-4');
  const main = __operatoreColorHex__(bg);
  const bgOpacity = isFinite(Number(options.opacity)) ? Math.max(0, Math.min(1, Number(options.opacity))) : 0.80;
  const borderOpacity = isFinite(Number(options.borderOpacity)) ? Math.max(0, Math.min(1, Number(options.borderOpacity))) : 1;
  const fg = __tagColorTextHex__(bg, fgSpec, !!options.preferWhiteText);
  return `background:${hexToRgba(main, bgOpacity)};background-color:${hexToRgba(main, bgOpacity)};border-color:${hexToRgba(main, borderOpacity)};color:${fg};-webkit-text-fill-color:${fg};`;
}

function __tagColorPairFromValue__(input, fallbackBg){
  const fallback = __normalizeOperatoreColor__(fallbackBg || 'blue-4');
  if (input && typeof input === 'object' && !Array.isArray(input)) {
    const bg = __normalizeOperatoreColor__(input.bg || input.background || input.colore || input.color || fallback);
    const border = __normalizeOptionalOperatoreColor__(input.border || input.borderColor || input.stroke || input.outline || '');
    return {
      bg,
      border: border || bg,
      fg: __normalizeOptionalOperatoreColor__(input.fg || input.foreground || input.text || input.icon || input.coloreTesto || input.textColor || ''),
      opacity: __designBgOpacityNormalize__(input.opacity ?? input.alpha ?? 0.80),
      bold: false
    };
  }
  const raw = String(input || '').trim();
  if (!raw) return { bg: fallback, border: fallback, fg: '', opacity:0.80, bold:false };
  const bg = __normalizeOperatoreColor__(raw);
  return { bg, border: bg, fg: '', opacity:0.80, bold:false };
}

function __launcherVisualNormalize__(value, fallbackFg){
  const fallback = __normalizeOperatoreColor__(fallbackFg || 'blue-4');
  if (value && typeof value === 'object' && !Array.isArray(value)) {
    return {
      fg: __normalizeOperatoreColor__(value.fg || value.foreground || value.color || fallback),
      bg: __normalizeOptionalOperatoreColor__(value.bg || value.background || ''),
      border: __normalizeOptionalOperatoreColor__(value.border || value.borderColor || value.stroke || value.outline || ''),
      opacity: __designBgOpacityNormalize__(value.opacity ?? value.alpha ?? 0.80),
      bold: false
    };
  }
  const raw = String(value || '').trim();
  return { fg: __normalizeOperatoreColor__(raw || fallback), bg: '', border: '', opacity:0.80, bold:false };
}


function __applyVisualTextWeight__(el, visual){
  try{
    if (!el) return;
    const weight = '400';
    const shouldSkip = (node) => {
      try{
        if (!(node instanceof Element)) return true;
        const tag = String(node.tagName || '').toUpperCase();
        if (!tag) return true;
        if (['SVG','PATH','RECT','CIRCLE','LINE','POLYLINE','POLYGON','G','DEFS','CLIPPATH','USE','STOP'].includes(tag)) return true;
        if (node.closest && node.closest('svg')) return true;
        if (node.classList && (node.classList.contains('ui-ico') || node.classList.contains('tl-ico') || node.classList.contains('theme-transfer-btn-icon') || node.classList.contains('room-settings-square-preview') || node.classList.contains('home-main-glyph'))) return true;
      }catch(_){ return true; }
      return false;
    };
    const applyWeight = (node) => {
      try{
        if (shouldSkip(node)) return;
        node.style.setProperty('font-weight', weight, 'important');
      }catch(_){ }
    };
    applyWeight(el);
    try{ el.querySelectorAll('*').forEach((node) => { applyWeight(node); }); }catch(_){ }
  }catch(_){ }
}

function __setTagPreviewButtonStyle__(id, bgSpec, fgSpec){

  try{
    const btn = document.getElementById(id);
    if (!btn) return;
    const safeId = String(id || '').trim();
    try{
      const map = __loadSingleActionButtonVisualMap__();
      const saved = map && typeof map === 'object' ? map[safeId] : null;
      if (saved && typeof saved === 'object'){
        __applySingleActionButtonVisual__(btn);
        return;
      }
    }catch(_){ }
    btn.setAttribute('style', __tagColorInlineStyle__(bgSpec || 'blue-4', fgSpec || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }));
  }catch(_){ }
}


const __LAUNCHER_ICON_COLOR_STORAGE_KEY__ = 'dDAE_launcher_icon_colors_v2';
const __LAUNCHER_ICON_LONGPRESS_DELAY__ = 500;
const __LAUNCHER_ICON_TARGET_IDS__ = [
  'goOspite','goCalendario','openLauncher','goTassaSoggiorno','goPulizie','goLavanderia','goOrePuliziaHome','goStatistiche','goProdotti','goDbSync',
  'settingsYearPill','settingsSaveBtn','settingsDbBtn','settingsRoomsBtn','settingsOperatoriBtn','settingsChannelBtn','settingsLaundryCatalogBtn','settingsConfigBtn','settingsExportRosterBtn','settingsLanguageBtn','settingsLogoutBtn','settingsMasterBtn',
  'opSettingsLanguageBtn','opSettingsCodeBtn','opSettingsLogoutBtn','opSettingsYearPill',
  'goStatGen','goStatMensili','goStatSpese','goStatRicevute','goStatChannel','goStatPulizie','goStatPiscina','goStatCancellazioni','goStatAmministratore'
];
const __LAUNCHER_ICON_DEFAULT_SPECS__ = {
  goOspite: 'blue-6',
  goCalendario: 'blue-5',
  openLauncher: 'sky-4',
  goTassaSoggiorno: 'sky-3',
  goPulizie: 'orange-4',
  goLavanderia: 'orange-3',
  goOrePuliziaHome: 'beige-5',
  goStatistiche: 'beige-4',
  goProdotti: 'gray-3',
  goDbSync: 'white',
  settingsSaveBtn: 'sky-3',
  settingsDbBtn: 'green-4',
  settingsRoomsBtn: 'yellow-4',
  settingsOperatoriBtn: 'blue-4',
  settingsChannelBtn: 'orange-4',
  settingsLaundryCatalogBtn: 'indigo-4',
  settingsConfigBtn: 'red-4',
  settingsExportRosterBtn: 'violet-4',
  settingsLanguageBtn: 'sky-4',
  settingsYearPill: 'sky-4',
  settingsLogoutBtn: 'red-4',
  settingsMasterBtn: 'violet-4',
  opSettingsLanguageBtn: 'sky-4',
  opSettingsDarkBtn: 'gray-6',
  opSettingsCodeBtn: 'violet-4',
  opSettingsYearPill: 'sky-4',
  opSettingsLogoutBtn: 'red-4',
  goStatGen: 'blue-5',
  goStatMensili: 'sky-5',
  goStatSpese: 'orange-4',
  goStatRicevute: 'orange-3',
  goStatChannel: 'beige-5',
  goStatPulizie: 'gray-4',
  goStatPiscina: 'beige-4',
  goStatCancellazioni: 'sky-4',
  goStatAmministratore: 'violet-4',
  homeYearPill: 'sky-4'
};

function __launcherIconColorMapRead__(){
  try{
    const raw = localStorage.getItem(__LAUNCHER_ICON_COLOR_STORAGE_KEY__) || localStorage.getItem('dDAE_launcher_icon_colors_v1');
    if (!raw) return {};
    const parsed = JSON.parse(raw);
    return (parsed && typeof parsed === 'object') ? parsed : {};
  }catch(_){ return {}; }
}

function __launcherIconColorMapWrite__(map){
  try{ localStorage.setItem(__LAUNCHER_ICON_COLOR_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

const __LAUNCHER_GRID_THEME_STORAGE_KEY__ = 'dDAE_launcher_grid_theme_v1';
const __STATISTICS_CARD_THEME_STORAGE_KEY__ = 'dDAE_statistics_card_theme_v1';
const __ROOM_SETTINGS_CARD_THEME_STORAGE_KEY__ = 'dDAE_room_settings_card_theme_v1';
const __ROOM_SETTINGS_SHELL_THEME_STORAGE_KEY__ = 'dDAE_room_settings_shell_theme_v1';
const __DESIGN_BG_OPACITY_STORAGE_KEY__ = 'dDAE_design_bg_opacity_v1';
const __DESIGN_BG_OPACITY_ALLOWED__ = [0.00, 0.25, 0.50, 0.75, 1.00];

function __designBgOpacityNormalize__(value){
  const raw = Number(value);
  const allowed = __DESIGN_BG_OPACITY_ALLOWED__.slice();
  if (!isFinite(raw)) return 0.75;
  let best = allowed[0];
  let dist = Math.abs(raw - best);
  allowed.forEach((v) => { const d = Math.abs(raw - v); if (d < dist) { best = v; dist = d; } });
  return best;
}

function __designBgOpacityRead__(){
  try{
    const raw = localStorage.getItem(__DESIGN_BG_OPACITY_STORAGE_KEY__);
    if (!raw) return 0.75;
    return __designBgOpacityNormalize__(parseFloat(raw));
  }catch(_){ return 0.75; }
}

function __designBgOpacityWrite__(value){
  try{ localStorage.setItem(__DESIGN_BG_OPACITY_STORAGE_KEY__, String(__designBgOpacityNormalize__(value))); }catch(_){ }
}

function __designBgOpacityPercentLabel__(value){
  return String(Math.round(__designBgOpacityNormalize__(value) * 100));
}

function __designTranslate__(it, map = null){
  try{
    const lang = __getAppLanguage__();
    if (lang === 'it') return String(it || '');
    if (map && map[lang]) return String(map[lang] || '');
    return __translateText__(String(it || ''));
  }catch(_){ return String(it || ''); }
}

const __HEADER_ACTION_THEME_STORAGE_KEY__ = 'dDAE_header_action_theme_v1';
const __HEADER_ACTION_COLOR_STORAGE_KEY__ = 'dDAE_header_action_colors_v1';

function __headerActionTargetButtons__(){
  try{
    return Array.from(document.querySelectorAll(
      '.topbar .icon-btn[id], ' +
      '.hd-actions .icon-btn[id], ' +
      '.guest-hd-actions .icon-btn[id], ' +
      '.guest-hd-actions .tl-btn[id], ' +
      '.clean-topbar .icon-btn[id], ' +
      '.clean-nav .btn[id], ' +
      '.app-section-head-row .cal-nav-btn[id], ' +
      '.stats-head-actions .piscina-action-btn[id], ' +
      '#btnAdminInputsTop'
    ));
  }catch(_){ return []; }
}

function __headerActionColorMapRead__(){
  try{
    const raw = localStorage.getItem(__HEADER_ACTION_COLOR_STORAGE_KEY__);
    if (!raw) return {};
    const parsed = JSON.parse(raw);
    return (parsed && typeof parsed === 'object') ? parsed : {};
  }catch(_){ return {}; }
}

function __headerActionColorMapWrite__(map){
  try{ localStorage.setItem(__HEADER_ACTION_COLOR_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __headerActionThemeRead__(){
  try{
    const raw = localStorage.getItem(__HEADER_ACTION_THEME_STORAGE_KEY__);
    if (!raw) return { fg:'blue-4', bg:'white', border:'white', opacity:0.80 };
    return __launcherVisualNormalize__(JSON.parse(raw), 'blue-4');
  }catch(_){ return { fg:'blue-4', bg:'white', border:'white', opacity:0.80 }; }
}

function __headerActionThemeWrite__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    localStorage.setItem(__HEADER_ACTION_THEME_STORAGE_KEY__, JSON.stringify({ fg: clean.fg || 'blue-4', bg: clean.bg || 'white', border: clean.border || clean.bg || 'white', opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80), bold: !!clean.bold }));
  }catch(_){ }
}

function __headerActionThemeVisual__(){
  const visual = __headerActionThemeRead__();
  return { fg: visual.fg || 'blue-4', bg: visual.bg || 'white', border: visual.border || visual.bg || 'white', opacity: __designBgOpacityNormalize__(visual.opacity ?? 0.80), bold: !!visual.bold };
}

function __headerActionVisualFor__(id){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return __headerActionThemeVisual__();
  const map = __headerActionColorMapRead__();
  const base = __headerActionThemeVisual__();
  const current = __launcherVisualNormalize__(map[key], base.fg || 'blue-4');
  return {
    fg: current.fg || base.fg || 'blue-4',
    bg: current.bg || base.bg || 'white',
    border: current.border || current.bg || base.border || base.bg || 'white',
    opacity: __designBgOpacityNormalize__(current.opacity ?? base.opacity ?? 0.80),
    bold: false
  };
}

function __headerActionSaveColor__(id, spec, mode = 'fg'){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return;
  const rawMode = String(mode || 'fg').trim().toLowerCase();
  const normalizedMode = rawMode === 'bg' ? 'bg' : (rawMode === 'border' ? 'border' : 'fg');
  const map = __headerActionColorMapRead__();
  const current = __headerActionVisualFor__(key);
  if (normalizedMode === 'bg') current.bg = __normalizeOperatoreColor__(spec || current.bg || 'white');
  else if (normalizedMode === 'border') current.border = __normalizeOperatoreColor__(spec || current.border || current.bg || 'white');
  else current.fg = __normalizeOperatoreColor__(spec || current.fg || 'blue-4');
  map[key] = { fg: current.fg || 'blue-4', bg: current.bg || 'white', border: current.border || current.bg || 'white', opacity: __designBgOpacityNormalize__(current.opacity ?? 0.80), bold: !!current.bold };
  __headerActionColorMapWrite__(map);
}

function __headerActionThemeOverwriteTargets__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    const map = __headerActionColorMapRead__();
    __headerActionTargetButtons__().forEach((btn) => {
      const key = __singleActionButtonSharedKey__(btn);
      if (!key) return;
      map[key] = {
        fg: clean.fg || 'blue-4',
        bg: clean.bg || 'white',
        border: clean.border || clean.bg || 'white',
        opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80),
        bold: !!clean.bold
      };
    });
    __headerActionColorMapWrite__(map);
  }catch(_){ }
}

function __isDarkModeRuntime__(){
  try{ return !!(document && document.body && document.body.classList && document.body.classList.contains('ddae-dark')); }catch(_){ return false; }
}

function __isProtectedHeaderActionButton__(btn){
  try{
    if (!btn || !btn.id) return false;
    return !!(btn.closest('.topbar') || btn.closest('.hd-actions') || btn.closest('.guest-hd-actions') || btn.closest('.clean-topbar') || btn.closest('.app-section-head-row'));
  }catch(_){ return false; }
}

function __isProtectedPillButton__(btn){
  try{
    if (!btn || !btn.id) return false;
    return ['opSettingsYearPill','opSettingsLogoutBtn'].includes(String(btn.id || '').trim());
  }catch(_){ return false; }
}

function __hexToRgbTuple__(hex){
  try{
    let raw = String(hex || '').trim();
    if (!raw) return null;
    if (!raw.startsWith('#')) raw = __operatoreColorHex__(raw || 'blue-4');
    raw = String(raw || '').trim();
    if (!/^#([0-9a-f]{3}|[0-9a-f]{6})$/i.test(raw)) return null;
    if (raw.length === 4) raw = '#' + raw.slice(1).split('').map((ch) => ch + ch).join('');
    return [parseInt(raw.slice(1,3),16), parseInt(raw.slice(3,5),16), parseInt(raw.slice(5,7),16)];
  }catch(_){ return null; }
}

function __colorLuminance__(hex){
  try{
    const rgb = __hexToRgbTuple__(hex);
    if (!rgb) return 0;
    const [r,g,b] = rgb.map((v) => {
      const n = v / 255;
      return n <= 0.03928 ? (n / 12.92) : Math.pow((n + 0.055) / 1.055, 2.4);
    });
    return (0.2126 * r) + (0.7152 * g) + (0.0722 * b);
  }catch(_){ return 0; }
}

function __darkModeReadableTextHex__(preferredHex, fallbackHex){
  try{
    const pref = String(preferredHex || '').trim();
    const fallback = String(fallbackHex || '#f8fbff').trim();
    if (!pref) return fallback;
    return __colorLuminance__(pref) < 0.42 ? fallback : pref;
  }catch(_){ return fallbackHex || '#f8fbff'; }
}

function __isTaxPagePillButton__(btn){
  try{
    if (!btn || !btn.id) return false;
    return ['taxYearBtn','taxEstimateBtn'].includes(String(btn.id || '').trim());
  }catch(_){ return false; }
}

function __applyDarkAdaptiveSurface__(el, visual, options = {}){
  try{
    if (!el) return;
    const bgHex = __operatoreColorHex__(visual?.bg || options.fallbackBg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual?.border || visual?.bg || options.fallbackBg || 'blue-4');
    const requestedFg = visual?.fg ? __operatoreColorHex__(visual.fg) : '';
    const accentHex = requestedFg || __darkModeReadableTextHex__(borderHex, '#8fcbe8');
    const textHex = __darkModeReadableTextHex__(requestedFg, '#f8fbff');
    const bgOpacity = Math.max(0.14, Math.min(0.28, Number(options.bgOpacity != null ? options.bgOpacity : (visual?.opacity ?? 0.20)) || 0.20));
    const borderOpacity = Math.max(0.22, Math.min(0.42, Number(options.borderOpacity != null ? options.borderOpacity : 0.30) || 0.30));
    const bgCss = hexToRgba(bgHex, bgOpacity);
    const borderCss = hexToRgba(borderHex, borderOpacity);
    el.style.setProperty('background', bgCss, 'important');
    el.style.setProperty('background-color', bgCss, 'important');
    el.style.setProperty('border', '1px solid ' + borderCss, 'important');
    el.style.setProperty('border-color', borderCss, 'important');
    el.style.setProperty('box-shadow', 'none', 'important');
    el.style.setProperty('backdrop-filter', 'none', 'important');
    el.style.setProperty('-webkit-backdrop-filter', 'none', 'important');
    el.style.setProperty('background-image', 'none', 'important');
    el.style.setProperty('color', textHex, 'important');
    el.style.setProperty('-webkit-text-fill-color', textHex, 'important');
    return { textHex, accentHex, bgCss, borderCss };
  }catch(_){ return null; }
}

function __applyProtectedDarkButtonStyle__(btn, options = {}, visual = null){
  try{
    if (!btn) return;
    const fgHex = String(options.fg || '#58b7f4');
    const textHex = String(options.text || fgHex);
    const bgCss = String(options.bg || 'rgba(8,18,38,0.96)');
    const borderCss = String(options.border || 'rgba(88,183,244,0.28)');
    btn.style.setProperty('background', bgCss, 'important');
    btn.style.setProperty('background-color', bgCss, 'important');
    btn.style.setProperty('border', '1px solid ' + borderCss, 'important');
    btn.style.setProperty('border-color', borderCss, 'important');
    btn.style.setProperty('border-width', '1px', 'important');
    btn.style.setProperty('border-style', 'solid', 'important');
    btn.style.setProperty('box-shadow', 'none', 'important');
    btn.style.setProperty('opacity', '1', 'important');
    btn.style.setProperty('color', textHex, 'important');
    btn.style.setProperty('-webkit-text-fill-color', textHex, 'important');
    btn.style.setProperty('-webkit-appearance', 'none', 'important');
    btn.style.setProperty('appearance', 'none', 'important');
    const label = btn.querySelector('.settings-btn-label, #taxYearBtnLabel');
    if (label){
      label.style.setProperty('color', textHex, 'important');
      label.style.setProperty('-webkit-text-fill-color', textHex, 'important');
      label.style.setProperty('opacity', '1', 'important');
    }
    const svg = btn.querySelector('svg.ui-ico');
    if (svg){
      svg.style.setProperty('color', fgHex, 'important');
      svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
        node.style.setProperty('stroke', fgHex, 'important');
        node.style.setProperty('fill', 'none', 'important');
      });
    }
    __applyVisualTextWeight__(btn, visual);
    const iconWrap = btn.querySelector('.backchev, .ui-ico, .tl-ico');
    if (iconWrap){
      iconWrap.style.setProperty('color', fgHex, 'important');
      iconWrap.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
    }
    try{
      const headerWeight = visual && visual.bold ? '700' : '400';
      btn.style.setProperty('font-weight', headerWeight, 'important');
      btn.querySelectorAll('.label, .btn-label, .home-sync-text, .topbar-title, span').forEach((node) => {
        try{ node.style.setProperty('font-weight', headerWeight, 'important'); }catch(_){ }
      });
    }catch(_){ }
  }catch(_){ }
}

function __headerActionApplyToButton__(btn, previewVisual){
  try{
    if (!btn || !btn.id) return;
    if (__isDarkModeRuntime__() && __isProtectedHeaderActionButton__(btn)){
      __applyProtectedDarkButtonStyle__(btn, { fg:'#58b7f4', text:'#58b7f4', bg:'rgba(8,18,38,0.96)', border:'rgba(88,183,244,0.28)' }, previewVisual || __headerActionVisualFor__(btn.id));
      return;
    }
    const visual = previewVisual || __headerActionVisualFor__(btn.id);
    const fgHex = __operatoreColorHex__(visual.fg || 'blue-4');
    const bgHex = __operatoreColorHex__(visual.bg || 'white');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'white');
    const bgCss = hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__()));
    btn.style.setProperty('background', bgCss, 'important');
    btn.style.setProperty('background-color', bgCss, 'important');
    btn.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
    btn.style.setProperty('border-width', '1px', 'important');
    btn.style.setProperty('border-style', 'solid', 'important');
    btn.style.setProperty('box-shadow', 'none', 'important');
    btn.style.setProperty('color', fgHex, 'important');
    btn.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
    const svg = btn.querySelector('svg.ui-ico');
    if (svg){
      svg.style.setProperty('color', fgHex, 'important');
      svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
        node.style.setProperty('stroke', fgHex, 'important');
        node.style.setProperty('fill', 'none', 'important');
      });
    }
    const iconWrap = btn.querySelector('.backchev, .ui-ico, .tl-ico');
    if (iconWrap){
      iconWrap.style.setProperty('color', fgHex, 'important');
      iconWrap.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
    }
    __applyVisualTextWeight__(btn, visual);
    try{
      const headerWeight = visual && visual.bold ? '700' : '400';
      btn.style.setProperty('font-weight', headerWeight, 'important');
      btn.querySelectorAll('.label, .btn-label, .home-sync-text, .topbar-title, span').forEach((node) => {
        try{ node.style.setProperty('font-weight', headerWeight, 'important'); }catch(_){ }
      });
    }catch(_){ }
  }catch(_){ }
}

function __headerActionApplyAll__(){
  try{
    __headerActionTargetButtons__().forEach((btn) => {
      try{ __bindHeaderActionLongPress__(btn); }catch(_){ }
      __headerActionApplyToButton__(btn);
    });
  }catch(_){ }
}


function __applyDesignPayloadToVisual__(current, payload, changed, fallback){
  const base = __launcherVisualNormalize__(current || {}, fallback || 'blue-4');
  const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
  if (changed?.bg) base.bg = __normalizeOperatoreColor__(colors.bg || base.bg || fallback || 'blue-4');
  if (changed?.border) base.border = __normalizeOperatoreColor__(colors.border || colors.bg || base.border || base.bg || fallback || 'blue-4');
  if (changed?.fg) base.fg = __normalizeOperatoreColor__(colors.fg || base.fg || fallback || 'blue-4');
  if (changed?.opacity) base.opacity = __designBgOpacityNormalize__(payload?.opacity ?? base.opacity ?? 0.80);
  if (changed?.bold) base.bold = !!payload?.bold;
  if (!base.border) base.border = base.bg || fallback || 'blue-4';
  return base;
}

function __designVisualFromPayload__(current, payload, fallback){
  const base = __launcherVisualNormalize__(current || {}, fallback || 'blue-4');
  const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
  base.bg = __normalizeOperatoreColor__(colors.bg || base.bg || fallback || 'blue-4');
  base.border = __normalizeOperatoreColor__(colors.border || colors.bg || base.border || base.bg || fallback || 'blue-4');
  base.fg = __normalizeOptionalOperatoreColor__(colors.fg || base.fg || '');
  base.opacity = __designBgOpacityNormalize__(payload?.opacity ?? base.opacity ?? 0.80);
  base.bold = !!(payload?.bold ?? base.bold ?? false);
  if (!base.border) base.border = base.bg || fallback || 'blue-4';
  return base;
}

function __writeHeaderActionVisual__(id, visual){
  try{
    const key = __singleActionButtonSharedKey__(id);
    if (!key) return;
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    const map = __headerActionColorMapRead__();
    map[key] = {
      fg: clean.fg || 'blue-4',
      bg: clean.bg || 'white',
      border: clean.border || clean.bg || 'white',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80),
      bold: !!clean.bold
    };
    __headerActionColorMapWrite__(map);
  }catch(_){ }
}

function __writePillVisual__(id, visual){
  try{
    const key = __singleActionButtonSharedKey__(id);
    if (!key) return;
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    const map = __pillColorMapRead__();
    map[key] = {
      fg: clean.fg || 'white',
      bg: clean.bg || 'blue-4',
      border: clean.border || clean.bg || 'blue-4',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80),
      bold: !!clean.bold
    };
    __pillColorMapWrite__(map);
  }catch(_){ }
}

function __writeLauncherIconVisual__(id, visual){
  try{
    const key = __singleActionButtonSharedKey__(id);
    if (!key) return;
    const fallback = __LAUNCHER_ICON_DEFAULT_SPECS__[key] || 'blue-4';
    const clean = __launcherVisualNormalize__(visual || {}, fallback);
    const map = __launcherIconColorMapRead__();
    map[key] = {
      fg: clean.fg || fallback,
      bg: clean.bg || '',
      border: clean.border || clean.bg || '',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80),
      bold: !!clean.bold
    };
    __launcherIconColorMapWrite__(map);
  }catch(_){ }
}

async function __applyHeaderActionChangesToCategory__(payload, changed){
  try{
    const map = __headerActionColorMapRead__();
    __headerActionTargetButtons__().forEach((btn) => {
      if (!btn || !btn.id) return;
      const next = __applyDesignPayloadToVisual__(__headerActionVisualFor__(btn.id), payload, changed, 'blue-4');
      map[__singleActionButtonSharedKey__(btn) || String(btn.id || '').trim()] = { fg: next.fg || 'blue-4', bg: next.bg || 'white', border: next.border || next.bg || 'white', opacity: __designBgOpacityNormalize__(next.opacity ?? 0.80), bold: !!next.bold };
    });
    __headerActionColorMapWrite__(map);
    if (changed?.opacity) __designBgOpacityWrite__(payload?.opacity);
    __headerActionApplyAll__();
    try{ renderRoomSettingsPage(); }catch(_){ }
  }catch(_){ }
}

async function __applyPillChangesToCategory__(payload, changed){
  try{
    const map = __pillColorMapRead__();
    __PILL_THEME_TARGET_IDS__.forEach((id) => {
      const next = __applyDesignPayloadToVisual__(__pillVisualFor__(id), payload, changed, 'white');
      map[__singleActionButtonSharedKey__(id) || String(id || '').trim()] = { fg: next.fg || 'white', bg: next.bg || 'blue-4', border: next.border || next.bg || 'blue-4', opacity: __designBgOpacityNormalize__(next.opacity ?? 0.80), bold: !!next.bold };
    });
    __pillColorMapWrite__(map);
    if (changed?.opacity) __designBgOpacityWrite__(payload?.opacity);
    __pillApplyAll__();
    try{ renderRoomSettingsPage(); }catch(_){ }
  }catch(_){ }
}

async function __applyLauncherIconChangesToCategory__(payload, changed){
  try{
    const map = __launcherIconColorMapRead__();
    __LAUNCHER_ICON_TARGET_IDS__.forEach((id) => {
      const current = __launcherIconVisualFor__(id);
      const next = __applyDesignPayloadToVisual__(current, payload, changed, __LAUNCHER_ICON_DEFAULT_SPECS__[id] || 'blue-4');
      map[String(id || '').trim()] = {
        fg: next.fg || (current.fg || (__LAUNCHER_ICON_DEFAULT_SPECS__[id] || 'blue-4')),
        bg: next.bg || current.bg || '',
        border: next.border || next.bg || current.border || current.bg || '',
        opacity: __designBgOpacityNormalize__(next.opacity ?? 0.80),
        bold: !!next.bold
      };
    });
    __launcherIconColorMapWrite__(map);
    if (changed?.opacity) __designBgOpacityWrite__(payload?.opacity);
    __launcherIconApplyAll__();
    try{ renderRoomSettingsPage(); }catch(_){ }
  }catch(_){ }
}

async function __applyRoomSettingsChangesToCategory__(target, payload, changed){
  try{
    const key = String(target || '').trim().toLowerCase();
    const next = getRoomsUiConfig();
    const applyVisual = (visual, fallback) => {
      const base = __roomsUiVisualNormalize__(visual, fallback || 'blue-4');
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      if (changed?.bg) base.bg = __normalizeOperatoreColor__(colors.bg || base.bg || fallback || 'blue-4');
      if (changed?.border) base.border = __normalizeOperatoreColor__(colors.border || colors.bg || base.border || base.bg || fallback || 'blue-4');
      if (changed?.fg) base.fg = __normalizeOptionalOperatoreColor__(colors.fg || base.fg || '');
      if (changed?.opacity) base.opacity = __designBgOpacityNormalize__(payload?.opacity ?? base.opacity ?? 0.80);
      return { bg: base.bg || fallback || 'blue-4', border: base.border || base.bg || fallback || 'blue-4', fg: base.fg || '', opacity: __designBgOpacityNormalize__(base.opacity ?? 0.80) };
    };
    if (/^room:\d+$/.test(key)){
      const count = getConfiguredRoomsCount(6);
      for (let i = 1; i <= count; i++) next.rooms[String(i)] = applyVisual(next.rooms?.[String(i)] || 'blue-4', 'blue-4');
    } else if (key.startsWith('option:')){
      next.options.m = applyVisual(next.options?.m || 'blue-4', 'blue-4');
      next.options.g = applyVisual(next.options?.g || 'yellow-4', 'yellow-4');
      next.options.c = applyVisual(next.options?.c || 'indigo-6', 'indigo-6');
    } else if (key.startsWith('bed:')){
      next.beds.matrimoniale = applyVisual(next.beds?.matrimoniale || 'red-4', 'red-4');
      next.beds.singolo = applyVisual(next.beds?.singolo || 'blue-4', 'blue-4');
      next.beds.culla = applyVisual(next.beds?.culla || 'yellow-4', 'yellow-4');
    } else {
      return;
    }
    await saveRoomsUiConfigToSettings(next, { showToast:true });
  }catch(_){ }
}

function __headerActionThemeButtonStyle__(){
  try{
    const visual = __headerActionThemeVisual__();
    const bgHex = __operatoreColorHex__(visual.bg || 'white');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'white');
    const fgHex = __operatoreColorHex__(visual.fg || 'blue-4');
    return [
      'background:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'background-color:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'border-color:' + hexToRgba(borderHex, 1),
      'color:' + fgHex,
      '-webkit-text-fill-color:' + fgHex
    ].join(';');
  }catch(_){
    return 'background:rgba(255,255,255,0.80);background-color:rgba(255,255,255,0.80);border-color:rgba(255,255,255,1);color:#4d9cc5;-webkit-text-fill-color:#4d9cc5';
  }
}

function __previewHeaderActionVisual__(id, payload){
  try{
    const current = __headerActionVisualFor__(id);
    const nextVisual = __designVisualFromPayload__(current, payload, current.bg || 'white');
    const btn = document.getElementById(id);
    if (!btn) return;
    __headerActionApplyToButton__(btn, nextVisual);
  }catch(_){ }
}

function __previewHeaderActionTheme__(payload){
  try{
    const current = __headerActionThemeVisual__();
    const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    const nextVisual = {
      bg: colors.bg || current.bg || 'white',
      border: colors.border || current.border || colors.bg || current.bg || 'white',
      fg: colors.fg || current.fg || 'blue-4',
      opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? __designBgOpacityRead__()),
      bold: false
    };
    __headerActionTargetButtons__().forEach((btn) => { try{ __headerActionApplyToButton__(btn, nextVisual); }catch(_){ } });
  }catch(_){ }
}

function __openHeaderActionThemePicker__(){
  const current = __headerActionThemeVisual__();
  __tagColorPopupOpen__('header-action-theme', current, (payload) => {
    try{
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const nextVisual = {
        bg: colors.bg || current.bg || 'white',
        border: colors.border || current.border || colors.bg || current.bg || 'white',
        fg: colors.fg || current.fg || 'blue-4',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? __designBgOpacityRead__()),
        bold: false
      };
      __headerActionThemeWrite__(nextVisual);
      if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
      __headerActionThemeOverwriteTargets__(nextVisual);
      __headerActionApplyAll__();
      try{ renderRoomSettingsPage(); }catch(_){ }
      try{ toast('Design tasti top bar aggiornato'); }catch(_){ }
    }catch(e){ try{ toast(e?.message || 'Errore design tasti top bar'); }catch(_){ } }
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, supportsBold:false, opacity:__designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(current.bg || 'white'), onPreview:(payload) => { __previewHeaderActionTheme__(payload); }, onRevert:() => { __headerActionApplyAll__(); } });
}

const __PILL_THEME_STORAGE_KEY__ = 'dDAE_pill_theme_v1';
const __PILL_COLOR_STORAGE_KEY__ = 'dDAE_pill_colors_v1';
const __PILL_THEME_TARGET_IDS__ = ['opSettingsYearPill','opSettingsLogoutBtn','homeYearPill','taxYearBtn','taxEstimateBtn','setTassaFieldPill','setTassaMaxNottiBtn','settingsConfigCancelBtn','settingsConfigSaveBtn'];
const __PILL_LONGPRESS_SUPPRESS_UNTIL__ = Object.create(null);

function __pillLongPressKey__(btnOrId){
  try{
    if (!btnOrId) return '';
    if (typeof btnOrId === 'string') return String(btnOrId || '').trim();
    return String(btnOrId.id || '').trim();
  }catch(_){ return ''; }
}

function __pillLongPressSuppress__(btnOrId, ms = 1200){
  try{
    const key = __pillLongPressKey__(btnOrId);
    if (!key) return;
    __PILL_LONGPRESS_SUPPRESS_UNTIL__[key] = Date.now() + Math.max(0, Number(ms || 0) || 0);
  }catch(_){ }
}

function __pillLongPressSuppressed__(btnOrId){
  try{
    const key = __pillLongPressKey__(btnOrId);
    if (!key) return false;
    return Date.now() < Number(__PILL_LONGPRESS_SUPPRESS_UNTIL__[key] || 0);
  }catch(_){ return false; }
}

function __pillTargetButtons__(){
  try{
    return __PILL_THEME_TARGET_IDS__
      .map((id) => document.getElementById(id))
      .filter((el) => !!(el && (String(el.tagName || '').toUpperCase() === 'BUTTON' || String(el.id || '').trim() === 'homeYearPill')));
  }catch(_){ return []; }
}

function __pillColorMapRead__(){
  try{
    const raw = localStorage.getItem(__PILL_COLOR_STORAGE_KEY__);
    if (!raw) return {};
    const parsed = JSON.parse(raw);
    return (parsed && typeof parsed === 'object') ? parsed : {};
  }catch(_){ return {}; }
}

function __pillColorMapWrite__(map){
  try{ localStorage.setItem(__PILL_COLOR_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __pillThemeRead__(){
  try{
    const raw = localStorage.getItem(__PILL_THEME_STORAGE_KEY__);
    if (!raw) return { fg:'white', bg:'blue-4', border:'blue-4', opacity:0.80, bold:false };
    return __launcherVisualNormalize__(JSON.parse(raw), 'blue-4');
  }catch(_){ return { fg:'white', bg:'blue-4', border:'blue-4', opacity:0.80, bold:false }; }
}

function __pillThemeWrite__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    localStorage.setItem(__PILL_THEME_STORAGE_KEY__, JSON.stringify({ fg: clean.fg || 'white', bg: clean.bg || 'blue-4', border: clean.border || clean.bg || 'blue-4', opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80), bold: !!clean.bold }));
  }catch(_){ }
}

function __pillThemeVisual__(){
  const visual = __pillThemeRead__();
  return { fg: visual.fg || 'white', bg: visual.bg || 'blue-4', border: visual.border || visual.bg || 'blue-4', opacity: __designBgOpacityNormalize__(visual.opacity ?? 0.80), bold: !!visual.bold };
}


function __pillDefaultVisualForId__(id, base){
  const key = __singleActionButtonSharedKey__(id);
  const defaults = {
    taxYearBtn:{ fg:'sky-5', bg:'gray-1', border:'sky-2', opacity:0.80 },
    taxEstimateBtn:{ fg:'sky-5', bg:'gray-1', border:'sky-2', opacity:0.80 }
  };
  const chosen = defaults[key] || base || {};
  return {
    fg: chosen.fg || base?.fg || 'white',
    bg: chosen.bg || base?.bg || 'blue-4',
    border: chosen.border || chosen.bg || base?.border || base?.bg || 'blue-4',
    opacity: __designBgOpacityNormalize__(chosen.opacity ?? base?.opacity ?? 0.80),
    bold: false
  };
}

function __pillVisualFor__(id){
  const key = __singleActionButtonSharedKey__(id);
  const base = __pillThemeVisual__();
  const fallback = __pillDefaultVisualForId__(key, base);
  if (!key) return fallback;
  const map = __pillColorMapRead__();
  const current = __launcherVisualNormalize__(map[key] || fallback, fallback.fg || base.fg || 'white');
  return {
    fg: current.fg || fallback.fg || base.fg || 'white',
    bg: current.bg || fallback.bg || base.bg || 'blue-4',
    border: current.border || current.bg || fallback.border || fallback.bg || base.border || base.bg || 'blue-4',
    opacity: __designBgOpacityNormalize__(current.opacity ?? fallback.opacity ?? base.opacity ?? 0.80),
    bold: !!current.bold
  };
}

function __pillSaveColor__(id, spec, mode = 'fg'){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return;
  const rawMode = String(mode || 'fg').trim().toLowerCase();
  const normalizedMode = rawMode === 'bg' ? 'bg' : (rawMode === 'border' ? 'border' : 'fg');
  const map = __pillColorMapRead__();
  const current = __pillVisualFor__(key);
  if (normalizedMode === 'bg') current.bg = __normalizeOperatoreColor__(spec || current.bg || 'blue-4');
  else if (normalizedMode === 'border') current.border = __normalizeOperatoreColor__(spec || current.border || current.bg || 'blue-4');
  else current.fg = __normalizeOperatoreColor__(spec || current.fg || 'white');
  map[key] = { fg: current.fg || 'white', bg: current.bg || 'blue-4', border: current.border || current.bg || 'blue-4', opacity: __designBgOpacityNormalize__(current.opacity ?? 0.80), bold: !!current.bold };
  __pillColorMapWrite__(map);
}

function __pillThemeOverwriteTargets__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    const map = __pillColorMapRead__();
    __PILL_THEME_TARGET_IDS__.forEach((id) => {
      map[__singleActionButtonSharedKey__(id) || id] = { fg: clean.fg || 'white', bg: clean.bg || 'blue-4', border: clean.border || clean.bg || 'blue-4', opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80), bold: !!clean.bold };
    });
    __pillColorMapWrite__(map);
  }catch(_){ }
}

function __pillApplyToButton__(btn, previewVisual){
  try{
    if (!btn || !btn.id) return;
    const visual = previewVisual || __pillVisualFor__(btn.id);
    if (__isDarkModeRuntime__() && __isProtectedPillButton__(btn)){
      const isLogout = String(btn.id || '').toLowerCase().includes('logout');
      __applyProtectedDarkButtonStyle__(btn, {
        fg: isLogout ? '#58b7f4' : '#f8fafc',
        text: isLogout ? '#58b7f4' : '#f8fafc',
        bg: 'rgba(8,18,38,0.96)',
        border: 'rgba(88,183,244,0.28)'
      }, visual);
      return;
    }
    if (__isDarkModeRuntime__() && __isTaxPagePillButton__(btn)){
      const resolved = __applyDarkAdaptiveSurface__(btn, visual, { fallbackBg:'gray-1', bgOpacity:0.18, borderOpacity:0.30 }) || {};
      const pillText = resolved.textHex || '#f8fbff';
      const pillAccent = resolved.accentHex || '#8fcbe8';
      const label = btn.querySelector('.settings-btn-label, #taxYearBtnLabel');
      if (label){
        label.style.setProperty('color', pillText, 'important');
        label.style.setProperty('-webkit-text-fill-color', pillText, 'important');
        label.style.setProperty('opacity', '1', 'important');
      }
      const svg = btn.querySelector('svg.ui-ico');
      if (svg){
        svg.style.setProperty('color', pillAccent, 'important');
        svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
          node.style.setProperty('stroke', pillAccent, 'important');
          node.style.setProperty('fill', 'none', 'important');
        });
      }
      __applyVisualTextWeight__(btn, visual);
      return;
    }
    const fgHex = __tagColorTextHex__(visual.bg || 'blue-4', visual.fg || '', false);
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    const bgCss = hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__()));
    btn.style.setProperty('background', bgCss, 'important');
    btn.style.setProperty('background-color', bgCss, 'important');
    btn.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
    btn.style.setProperty('border-width', '1px', 'important');
    btn.style.setProperty('border-style', 'solid', 'important');
    btn.style.setProperty('box-shadow', 'none', 'important');
    btn.style.setProperty('opacity', '1', 'important');
    btn.style.setProperty('color', fgHex, 'important');
    btn.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
    const label = btn.querySelector('.settings-btn-label, #taxYearBtnLabel');
    if (label){
      label.style.setProperty('color', fgHex, 'important');
      label.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
      label.style.setProperty('opacity', '1', 'important');
    }
    const svg = btn.querySelector('svg.ui-ico');
    if (svg){
      svg.style.setProperty('color', fgHex, 'important');
      svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
        node.style.setProperty('stroke', fgHex, 'important');
        node.style.setProperty('fill', 'none', 'important');
      });
    }
    __applyVisualTextWeight__(btn, visual);
    try{
      const pillWeight = visual && visual.bold ? '700' : '400';
      btn.style.setProperty('font-weight', pillWeight, 'important');
      btn.querySelectorAll('.settings-btn-label, .settings-btn-label *, #taxYearBtnLabel, #taxYearBtnLabel *').forEach((node) => {
        try{ node.style.setProperty('font-weight', pillWeight, 'important'); }catch(_){ }
      });
    }catch(_){ }
  }catch(_){ }
}

function __applySettingsTaxPillField__(){
  try{
    const wrap = document.getElementById('setTassaFieldPill');
    const input = document.getElementById('setTassa');
    const label = wrap ? wrap.querySelector('label[for="setTassa"]') : null;
    if (!wrap || !input) return;
    const visual = __pillVisualFor__('setTassaFieldPill');
    if (__isDarkModeRuntime__()){
      const resolved = __applyDarkAdaptiveSurface__(wrap, visual, { fallbackBg:'gray-1', bgOpacity:0.18, borderOpacity:0.30 }) || {};
      const textHex = resolved.textHex || '#f8fbff';
      const accentHex = resolved.accentHex || '#8fcbe8';
      input.style.setProperty('background', 'transparent', 'important');
      input.style.setProperty('background-color', 'transparent', 'important');
      input.style.setProperty('border', '0', 'important');
      input.style.setProperty('box-shadow', 'none', 'important');
      input.style.setProperty('color', textHex, 'important');
      input.style.setProperty('-webkit-text-fill-color', textHex, 'important');
      if (label){
        label.style.setProperty('color', accentHex, 'important');
        label.style.setProperty('-webkit-text-fill-color', accentHex, 'important');
        label.style.setProperty('opacity', '1', 'important');
      }
      return;
    }
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    const fgHex = __tagColorTextHex__(visual.bg || 'blue-4', visual.fg || 'white', false) || __operatoreColorHex__(visual.fg || 'white');
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__());
    wrap.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
    wrap.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
    wrap.style.setProperty('border', '1px solid ' + hexToRgba(borderHex, 1), 'important');
    wrap.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
    wrap.style.setProperty('box-shadow', 'none', 'important');
    wrap.style.setProperty('backdrop-filter', 'none', 'important');
    wrap.style.setProperty('-webkit-backdrop-filter', 'none', 'important');
    input.style.setProperty('background', 'transparent', 'important');
    input.style.setProperty('background-color', 'transparent', 'important');
    input.style.setProperty('border', '0', 'important');
    input.style.setProperty('box-shadow', 'none', 'important');
    input.style.setProperty('color', fgHex, 'important');
    input.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
    if (label){
      label.style.setProperty('color', fgHex, 'important');
      label.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
      label.style.setProperty('opacity', '0.92', 'important');
    }
  }catch(_){ }
}

function __bindSettingsTaxPillFieldLongPress__(){
  try{
    const wrap = document.getElementById('setTassaFieldPill');
    if (!wrap || wrap.dataset.pillColorHoldBound === '1') return;
    wrap.dataset.pillColorHoldBound = '1';
    let timer = null;
    let fired = false;
    const clear = ()=>{ try{ if (timer) clearTimeout(timer); }catch(_){ } timer = null; };
    const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } try{ e.stopImmediatePropagation(); }catch(_){ } };
    const suppressTap = ()=>{ try{ wrap.__pillFieldSuppressTapUntil = Date.now() + 900; }catch(_){ } };
    const openPicker = ()=>{
      fired = true;
      const current = __pillVisualFor__('setTassaFieldPill');
      __tagColorPopupOpen__('pill-single-button', current, (payload) => {
        try{
          const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
          __pillSaveColor__('setTassaFieldPill', colors.bg || current.bg || 'blue-4', 'bg');
          __pillSaveColor__('setTassaFieldPill', colors.border || current.border || colors.bg || current.bg || 'blue-4', 'border');
          __pillSaveColor__('setTassaFieldPill', colors.fg || current.fg || 'white', 'fg');
          {
            const map = __pillColorMapRead__();
            const next = __pillVisualFor__('setTassaFieldPill');
            map['setTassaFieldPill'] = {
              fg: next.fg || 'white',
              bg: next.bg || 'blue-4',
              border: next.border || next.bg || 'blue-4',
              opacity: __designBgOpacityNormalize__(payload?.opacity ?? next.opacity ?? current.opacity ?? __designBgOpacityRead__()),
              bold: false
            };
            __pillColorMapWrite__(map);
            if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
          }
          __pillApplyAll__();
          try{ renderRoomSettingsPage(); }catch(_){ }
        }catch(_){ }
      }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:current.opacity ?? __designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(current.bg || 'blue-4'), applyCategory:{ message:'Applicare le modifiche a tutti i pulsanti pill?', apply: async(payload, changed) => { await __applyPillChangesToCategory__(payload, changed); } } });
    };
    const start = (e)=>{ fired = false; clear(); timer = setTimeout(()=>{ suppressTap(); openPicker(); }, 500); };
    const stop = (e)=>{ clear(); if (fired){ block(e); setTimeout(()=>{ fired = false; }, 0); } };
    ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ wrap.addEventListener(evt, start, { passive:true }); }catch(_){ } });
    ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ wrap.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ try{ wrap.addEventListener(evt, stop, true); }catch(__){ } } });
    ['pointerup','touchend','click','dblclick'].forEach((evt)=>{ try{ wrap.addEventListener(evt, (e)=>{ if ((wrap.__pillFieldSuppressTapUntil || 0) > Date.now()) block(e); }, true); }catch(_){ } });
    try{ wrap.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
  }catch(_){ }
}

function __pillApplyAll__(){
  try{
    __pillTargetButtons__().forEach((btn) => {
      try{ __pillApplyToButton__(btn); }catch(_){ }
      try{ __bindPillLongPress__(btn); }catch(_){ }
    });
    try{ __applySettingsTaxPillField__(); }catch(_){ }
    try{ __bindSettingsTaxPillFieldLongPress__(); }catch(_){ }
  }catch(_){ }
}

function __bindPillLongPress__(btn){
  try{
    if (!btn || !btn.id || btn.dataset.pillColorHoldBound === '1') return;
    btn.dataset.pillColorHoldBound = '1';
    let holdTimer = null;
    let holdTriggered = false;
    let touchAt = 0;
    const clearHold = () => { try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ } holdTimer = null; };
    const canOpenPicker = () => {
      try{ if (__pillLongPressSuppressed__(btn)) return false; }catch(_){ }
      try{ if (__confirmYesNoGhostTapActive__()) return false; }catch(_){ }
      return true;
    };
    const openPicker = () => {
      if (!canOpenPicker()) return;
      holdTriggered = true;
      const current = __pillVisualFor__(btn.id);
      __tagColorPopupOpen__('pill-single-button', current, (payload) => {
        try{
          const nextVisual = __designVisualFromPayload__(current, payload, current.bg || 'blue-4');
          __writePillVisual__(btn.id, nextVisual);
          if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
          __pillApplyAll__();
          try{ renderRoomSettingsPage(); }catch(_){ }
        }catch(_){ }
      }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:current.opacity ?? __designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(current.bg || 'blue-4'), applyCategory:{ message:'Applicare le modifiche a tutti i pulsanti pill?', apply: async(payload, changed) => { await __applyPillChangesToCategory__(payload, changed); } } });
    };
    btn.addEventListener('touchstart', (e) => {
      touchAt = Date.now();
      holdTriggered = false;
      clearHold();
      if (!canOpenPicker()) return;
      holdTimer = setTimeout(() => { if (canOpenPicker()) openPicker(); }, 500);
    }, { passive:true, capture:true });
    btn.addEventListener('touchend', (e) => {
      clearHold();
      if (holdTriggered){ try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } }
    }, { passive:false, capture:true });
    btn.addEventListener('touchcancel', () => { clearHold(); }, { passive:true, capture:true });
    btn.addEventListener('mousedown', (e) => {
      if ((e.button || 0) !== 0) return;
      holdTriggered = false;
      clearHold();
      if (!canOpenPicker()) return;
      holdTimer = setTimeout(() => { if (canOpenPicker()) openPicker(); }, 500);
    }, true);
    ['mouseup','mouseleave'].forEach((evt) => btn.addEventListener(evt, () => { clearHold(); }, true));
    btn.addEventListener('click', (e) => {
      if (holdTriggered || (Date.now() - touchAt < 450 && holdTriggered)){
        try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
      }
    }, true);
    btn.addEventListener('contextmenu', (e) => {
      try{ e.preventDefault(); }catch(_){ }
      if (canOpenPicker()){
        try{ openPicker(); }catch(_){ }
      }
      try{ e.stopPropagation(); }catch(_){ }
    });
  }catch(_){ }
}

function __pillThemeButtonStyle__(){
  try{
    const visual = __pillThemeVisual__();
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    const fgHex = __tagColorTextHex__(visual.bg || 'blue-4', visual.fg || '', false);
    return [
      'background:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'background-color:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'border-color:' + hexToRgba(borderHex, 1),
      'color:' + fgHex,
      '-webkit-text-fill-color:' + fgHex
    ].join(';');
  }catch(_){
    return 'background:rgba(77,156,197,0.80);background-color:rgba(77,156,197,0.80);border-color:rgba(77,156,197,1);color:#ffffff;-webkit-text-fill-color:#ffffff';
  }
}

function __openPillThemePicker__(){
  const current = __pillThemeVisual__();
  __tagColorPopupOpen__('pill-theme', current, (payload) => {
    try{
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const nextVisual = {
        bg: colors.bg || current.bg || 'blue-4',
        border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
        fg: colors.fg || current.fg || '',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? __designBgOpacityRead__()),
        bold: false
      };
      __pillThemeWrite__(nextVisual);
      if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
      __pillThemeOverwriteTargets__(nextVisual);
      __pillApplyAll__();
      try{ renderRoomSettingsPage(); }catch(_){ }
      try{ toast(__designTranslate__('Design pulsanti pill aggiornato', { en:'Pill button design updated', fr:'Design des boutons pill mis à jour', de:'Pill-Button-Design aktualisiert', es:'Diseño de botones pill actualizado' })); }catch(_){ }
    }catch(e){ try{ toast(e?.message || __designTranslate__('Errore design pulsanti pill', { en:'Pill button design error', fr:'Erreur de design des boutons pill', de:'Fehler beim Pill-Button-Design', es:'Error de diseño de botones pill' })); }catch(_){ } }
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, supportsBold:false, opacity:current.opacity ?? __designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(current.bg || 'blue-4'), onPreview:(payload) => {
    try{
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const nextVisual = {
        bg: colors.bg || current.bg || 'blue-4',
        border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
        fg: colors.fg || current.fg || '',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? __designBgOpacityRead__()),
        bold: false
      };
      __PILL_THEME_TARGET_IDS__.forEach((id) => {
        const btn = document.getElementById(id);
        if (!btn) return;
        try{ __pillApplyToButton__(btn, nextVisual); }catch(_){ }
      });
      try{ __applySettingsTaxPillField__(); }catch(_){ }
    }catch(_){ }
  }, onRevert:() => { try{ __pillApplyAll__(); }catch(_){ } } });
}

function __bindHeaderActionLongPress__(btn){
  try{
    if (!btn || (typeof __ddae841IsOperationalNoColorPopupButton__ === 'function' && __ddae841IsOperationalNoColorPopupButton__(btn)) || btn.id === 'piscinaResetMonthBtn' || btn.dataset.headerColorHoldBound === '1') return;
    btn.dataset.headerColorHoldBound = '1';
    let holdTimer = null;
    let holdTriggered = false;
    let touchAt = 0;
    const clearHold = () => { try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ } holdTimer = null; };
    const openPicker = () => {
      holdTriggered = true;
      __tagColorPopupOpen__('header-action-button', __headerActionVisualFor__(btn.id), (payload) => {
        const current = __headerActionVisualFor__(btn.id);
        const nextVisual = __designVisualFromPayload__(current, payload, current.bg || 'white');
        __writeHeaderActionVisual__(btn.id, nextVisual);
        if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
        __headerActionApplyAll__();
        try{ renderRoomSettingsPage(); }catch(_){ }
      }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, supportsBold:false, opacity:__designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(__headerActionVisualFor__(btn.id).bg || 'white'), onPreview:(payload) => { __previewHeaderActionVisual__(btn.id, payload); }, onRevert:() => { __headerActionApplyToButton__(btn); }, applyCategory:{ message:'Applicare le modifiche a tutti i tasti della top bar?', confirmYesLabel:'Sì', confirmNoLabel:'No', apply: async(payload, changed) => { await __applyHeaderActionChangesToCategory__(payload, changed); } } });
    };
    btn.addEventListener('touchstart', (e) => {
      touchAt = Date.now();
      holdTriggered = false;
      clearHold();
      holdTimer = setTimeout(() => { openPicker(); }, 500);
    }, { passive:true, capture:true });
    btn.addEventListener('touchend', (e) => {
      clearHold();
      if (holdTriggered){ try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } }
    }, { passive:false, capture:true });
    btn.addEventListener('touchcancel', () => { clearHold(); }, { passive:true, capture:true });
    btn.addEventListener('mousedown', (e) => {
      if ((e.button || 0) !== 0) return;
      holdTriggered = false;
      clearHold();
      holdTimer = setTimeout(() => { openPicker(); }, 500);
    }, true);
    ['mouseup','mouseleave'].forEach((evt) => btn.addEventListener(evt, () => { clearHold(); }, true));
    btn.addEventListener('click', (e) => {
      if (holdTriggered || (Date.now() - touchAt < 450 && holdTriggered)){
        try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
      }
    }, true);
    btn.addEventListener('contextmenu', (e) => { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } });
  }catch(_){ }
}

function __statisticsCardThemeRead__(){
  try{
    const raw = localStorage.getItem(__STATISTICS_CARD_THEME_STORAGE_KEY__);
    if (!raw) return __launcherVisualNormalize__(__launcherGridThemeRead__(), 'blue-4');
    const parsed = JSON.parse(raw);
    return __launcherVisualNormalize__(parsed, 'blue-4');
  }catch(_){
    try{ return __launcherVisualNormalize__(__launcherGridThemeRead__(), 'blue-4'); }catch(__){ return { fg:'white', bg:'blue-4', border:'blue-4', opacity:0.80, bold:false }; }
  }
}

function __statisticsCardThemeWrite__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    localStorage.setItem(__STATISTICS_CARD_THEME_STORAGE_KEY__, JSON.stringify({ fg: clean.fg || 'white', bg: clean.bg || 'blue-4', border: clean.border || clean.bg || 'blue-4', opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80), bold: !!clean.bold }));
    try{ __statisticsCardThemeOverwriteTargets__(clean); }catch(__){ }
  }catch(_){ }
}

function __statisticsCardThemeVisual__(){
  const visual = __statisticsCardThemeRead__();
  return { fg: visual.fg || 'white', bg: visual.bg || 'blue-4', border: visual.border || visual.bg || 'blue-4', opacity: __designBgOpacityNormalize__(visual.opacity ?? 0.80), bold: !!visual.bold };
}

function __statisticsCardThemeTargetKeys__(){
  const statGenKeys = ['fatturato-totale','spese-totali','senza-ricevuta','con-ricevuta','iva-da-versare','guadagno-totale','giacenza-in-cassa'];
  const statSpeseKeys = ['totale-spese','contanti','tassa-soggiorno','iva-22','iva-10','iva-4'];
  const statMensiliKeys = (Array.isArray(__MONTHS_IT) ? __MONTHS_IT : []).map((m)=>String(m || '').trim()).filter(Boolean);
  const statRicevuteKeys = ['senza-ricevuta','con-ricevuta'];
  const statChannelKeys = ['channel','direct'];
  return {
    statgen: statGenKeys,
    statspese: statSpeseKeys,
    statmensili: statMensiliKeys,
    statprenotazioni: statRicevuteKeys,
    statchannel: statChannelKeys,
    statpulizie: []
  };
}

function __statisticsCardThemeOverwriteTargets__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    const targets = __statisticsCardThemeTargetKeys__();
    Object.keys(targets).forEach((pageKey) => {
      const map = __loadStatCardColorMap__(pageKey);
      (targets[pageKey] || []).forEach((cardKey) => {
        const safeKey = String(cardKey || '').trim();
        if (!safeKey) return;
        const current = __tagColorPairFromValue__(map[safeKey] || {}, clean.bg || 'blue-4');
        map[safeKey] = {
          bg: clean.bg || 'blue-4',
          border: clean.border || clean.bg || 'blue-4',
          fg: clean.fg || '',
          opacity: __designBgOpacityNormalize__(clean.opacity ?? current.opacity ?? __designBgOpacityRead__()),
          bold: !!clean.bold
        };
      });
      __saveStatCardColorMap__(pageKey, map);
    });
  }catch(_){ }
}

function __statisticsCardThemeButtonStyle__(){
  try{
    const visual = __statisticsCardThemeVisual__();
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    return [
      'background:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'background-color:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'border-color:' + hexToRgba(borderHex, 1),
      'color:' + __operatoreColorHex__(visual.fg || 'white'),
      '-webkit-text-fill-color:' + __operatoreColorHex__(visual.fg || 'white')
    ].join(';');
  }catch(_){
    return 'background:rgba(77,156,197,0.80);background-color:rgba(77,156,197,0.80);border-color:rgba(77,156,197,1);color:#ffffff;-webkit-text-fill-color:#ffffff';
  }
}

function __openStatisticsCardThemePicker__(){
  const current = __statisticsCardThemeVisual__();
  __tagColorPopupOpen__('statistics-card-theme', current, (payload) => {
    try{
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const nextVisual = {
        bg: colors.bg || current.bg || 'blue-4',
        border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
        fg: colors.fg || current.fg || '',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? __designBgOpacityRead__()),
        bold: false
      };
      __statisticsCardThemeWrite__(nextVisual);
      if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
      __applyStatisticsCardTheme__();
      try{ renderRoomSettingsPage(); }catch(_){ }
      try{ toast('Design card statistiche aggiornato'); }catch(_){ }
    }catch(e){ try{ toast(e?.message || 'Errore design card statistiche'); }catch(_){ } }
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:__designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(current.bg || 'blue-4') });
}

function __launcherGridThemeRead__(){
  try{
    const raw = localStorage.getItem(__LAUNCHER_GRID_THEME_STORAGE_KEY__);
    if (!raw) return { bg:'', border:'', opacity:0.80 };
    const parsed = JSON.parse(raw);
    return __launcherVisualNormalize__(parsed, 'blue-4');
  }catch(_){ return { bg:'', border:'', opacity:0.80 }; }
}

function __launcherGridThemeWrite__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    localStorage.setItem(__LAUNCHER_GRID_THEME_STORAGE_KEY__, JSON.stringify({ fg: clean.fg || 'white', bg: clean.bg || '', border: clean.border || '', opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.80), bold: !!clean.bold }));
  }catch(_){ }
}

function __launcherGridThemeVisual__(){
  const visual = __launcherGridThemeRead__();
  return { fg: visual.fg || 'white', bg: visual.bg || 'blue-4', border: visual.border || visual.bg || 'blue-4', opacity: __designBgOpacityNormalize__(visual.opacity ?? 0.80), bold: !!visual.bold };
}

function __launcherGridThemeResolveLayer__(mode, fallbackSpec){
  const key = String(mode || '').trim().toLowerCase();
  const fallback = fallbackSpec ? __normalizeOperatoreColor__(fallbackSpec) : '';
  try{
    const visual = __launcherGridThemeRead__();
    if (key === 'border') return visual.border || visual.bg || fallback || '';
    return visual.bg || fallback || '';
  }catch(_){
    return fallback || '';
  }
}

function __launcherGridThemeButtonStyle__(){
  try{
    const visual = __launcherGridThemeVisual__();
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    return [
      'background:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'background-color:' + hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())),
      'border-color:' + hexToRgba(borderHex, 1),
      'color:' + __operatoreColorHex__(visual.fg || 'white'),
      '-webkit-text-fill-color:' + __operatoreColorHex__(visual.fg || 'white')
    ].join(';');
  }catch(_){
    return 'background:rgba(77,156,197,0.80);background-color:rgba(77,156,197,0.80);border-color:rgba(77,156,197,1);color:#ffffff;-webkit-text-fill-color:#ffffff';
  }
}

const __LAUNCHER_GRID_THEME_TARGET_IDS__ = [
  'goOspite','goCalendario','openLauncher','goTassaSoggiorno','goPulizie','goLavanderia','goOrePuliziaHome','goStatistiche','goProdotti',
  'settingsYearPill','settingsSaveBtn','settingsDbBtn','settingsRoomsBtn','settingsOperatoriBtn','settingsChannelBtn','settingsLaundryCatalogBtn','settingsConfigBtn','settingsExportRosterBtn','settingsLanguageBtn','settingsLogoutBtn','settingsMasterBtn','opSettingsLanguageBtn','opSettingsCodeBtn','opSettingsLogoutBtn','opSettingsYearPill',
  'goStatGen','goStatMensili','goStatSpese','goStatRicevute','goStatChannel','goStatPulizie','goStatPiscina','goStatCancellazioni','goStatAmministratore'
];

function __launcherGridThemeOverwriteTargets__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    const map = __launcherIconColorMapRead__();
    __LAUNCHER_GRID_THEME_TARGET_IDS__.forEach((id) => {
      const current = __launcherVisualNormalize__(map[id], __LAUNCHER_ICON_DEFAULT_SPECS__[id] || 'blue-4');
      map[id] = {
        fg: clean.fg || (current.fg || (__LAUNCHER_ICON_DEFAULT_SPECS__[id] || 'blue-4')),
        bg: clean.bg || 'blue-4',
        border: clean.border || clean.bg || 'blue-4',
        opacity: __designBgOpacityNormalize__(clean.opacity ?? current.opacity ?? __designBgOpacityRead__()),
        bold: !!clean.bold
      };
    });
    __launcherIconColorMapWrite__(map);
  }catch(_){ }
}

function __openLauncherGridThemePicker__(){
  const current = __launcherGridThemeVisual__();
  __tagColorPopupOpen__('launcher-grid-theme', current, (payload) => {
    try{
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const nextVisual = {
        bg: colors.bg || current.bg || 'blue-4',
        border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
        fg: colors.fg || current.fg || '',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? __designBgOpacityRead__()),
        bold: false
      };
      __launcherGridThemeWrite__(nextVisual);
      if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
      __launcherGridThemeOverwriteTargets__(nextVisual);
      __launcherIconApplyAll__();
      try{ renderRoomSettingsPage(); }catch(_){ }
      try{ toast('Design tasti aggiornato'); }catch(_){ }
    }catch(e){ try{ toast(e?.message || 'Errore design tasti'); }catch(_){ } }
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:__designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(current.bg || 'blue-4') });
}

function __launcherIconVisualFor__(id){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return { fg:'blue-4', bg:'', border:'', opacity:0.80, bold:false };
  const map = __launcherIconColorMapRead__();
  if (key === 'goDbSync'){
    const raw = map[key];
    if (!raw || typeof raw !== 'object'){
      return { fg:'white', bg:'blue-4', border:'blue-4', opacity:0.80, bold:false };
    }
    const visual = __launcherVisualNormalize__(raw, 'white');
    return {
      fg: visual.fg || 'white',
      bg: visual.bg || 'blue-4',
      border: visual.border || visual.bg || 'blue-4',
      opacity: __designBgOpacityNormalize__(visual.opacity ?? 0.80),
      bold: !!visual.bold
    };
  }
  return __launcherVisualNormalize__(map[key], __LAUNCHER_ICON_DEFAULT_SPECS__[key] || 'blue-4');
}

function __launcherIconSpecFor__(id){
  return __launcherIconVisualFor__(id).fg;
}

function __launcherIconResolveHex__(id, fallbackHex){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return fallbackHex || __operatoreColorHex__('blue-4');
  try{
    return __operatoreColorHex__(__launcherIconVisualFor__(key).fg) || fallbackHex || __operatoreColorHex__('blue-4');
  }catch(_){
    return fallbackHex || __operatoreColorHex__('blue-4');
  }
}

function __applySettingsLauncherIconColors__(){
  try{
    [
      'settingsSaveBtn','settingsDbBtn','settingsRoomsBtn','settingsOperatoriBtn','settingsChannelBtn','settingsLaundryCatalogBtn','settingsConfigBtn','settingsExportRosterBtn','settingsLanguageBtn','settingsLogoutBtn','settingsMasterBtn','settingsYearPill',
      'opSettingsLanguageBtn','opSettingsCodeBtn','opSettingsLogoutBtn','opSettingsYearPill'
    ].forEach((id) => {
      const btn = document.getElementById(id);
      if (!btn) return;
      const hex = __launcherIconResolveHex__(id, '#4d9cc5');
      btn.style.setProperty('color', hex, 'important');
      btn.style.setProperty('-webkit-text-fill-color', hex, 'important');
      const label = btn.querySelector('.settings-btn-label');
      if (label){
        label.style.setProperty('color', hex, 'important');
        label.style.setProperty('-webkit-text-fill-color', hex, 'important');
      }
      const svg = btn.querySelector('svg.ui-ico');
      if (svg){
        svg.style.setProperty('color', hex, 'important');
        svg.style.setProperty('stroke', hex, 'important');
        svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
          node.style.setProperty('stroke', hex, 'important');
          node.style.setProperty('fill', 'none', 'important');
        });
      }
    });
  }catch(_){ }
}

function __launcherIconApplyToButton__(btn){
  try{
    if (!btn || !btn.id) return;
    const visual = __launcherIconVisualFor__(btn.id);
    const hex = __operatoreColorHex__(visual.fg || 'blue-4');
    const allowGridTheme = !!(btn.closest('#page-home') || btn.closest('#page-statistiche') || btn.closest('#page-impostazioni'));
    const resolvedBgSpec = visual.bg || (allowGridTheme ? __launcherGridThemeResolveLayer__('bg', '') : '');
    const resolvedBorderSpec = visual.border || (allowGridTheme ? __launcherGridThemeResolveLayer__('border', resolvedBgSpec || '') : '');
    const bgHex = resolvedBgSpec ? __operatoreColorHex__(resolvedBgSpec) : '';
    const borderHex = resolvedBorderSpec ? __operatoreColorHex__(resolvedBorderSpec) : '';
    const setImp = (node, prop, value) => {
      if (!node) return;
      if (value === undefined || value === null || value === '') node.style.removeProperty(prop);
      else node.style.setProperty(prop, value, 'important');
    };
    if (btn.id === 'homeYearPill'){
      btn.style.color = hex;
      btn.style.webkitTextFillColor = hex;
      btn.style.borderColor = borderHex ? hexToRgba(borderHex, 1) : (bgHex ? hexToRgba(bgHex, 0.80) : hex);
      btn.style.borderWidth = '1px';
      btn.style.borderStyle = 'solid';
      btn.style.background = bgHex ? hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())) : '';
      btn.style.backgroundColor = bgHex ? hexToRgba(bgHex, __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__())) : '';
      return;
    }
    if (btn.id === 'goDbSync'){
      const syncBgHex = __operatoreColorHex__(visual.bg || 'blue-4');
      const syncBorderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
      const syncFgHex = __operatoreColorHex__(visual.fg || 'white');
      const syncOpacity = __designBgOpacityNormalize__(visual.opacity ?? 0.80);
      setImp(btn, 'background', hexToRgba(syncBgHex, syncOpacity));
      setImp(btn, 'background-color', hexToRgba(syncBgHex, syncOpacity));
      setImp(btn, 'border', `1px solid ${hexToRgba(syncBorderHex, 1)}`);
      setImp(btn, 'border-color', hexToRgba(syncBorderHex, 1));
      setImp(btn, 'color', syncFgHex);
      setImp(btn, '-webkit-text-fill-color', syncFgHex);
      const iconWrap = btn.querySelector('.home-sync-ico, .ui-ico');
      if (iconWrap){
        setImp(iconWrap, 'color', syncFgHex);
        setImp(iconWrap, '-webkit-text-fill-color', syncFgHex);
      }
      const textWrap = btn.querySelector('.home-sync-text');
      if (textWrap){
        setImp(textWrap, 'color', syncFgHex);
        setImp(textWrap, '-webkit-text-fill-color', syncFgHex);
      }
      const svg = btn.querySelector('svg.ui-ico');
      if (svg){
        setImp(svg, 'color', syncFgHex);
        setImp(svg, 'stroke', syncFgHex);
        try{
          svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
            node.style.setProperty('stroke', syncFgHex, 'important');
            node.style.setProperty('fill', 'none', 'important');
          });
        }catch(_){ }
      }
      __applyVisualTextWeight__(btn, visual);
      return;
    }
    if (btn.closest('#page-home') || btn.closest('#page-statistiche')){
      const resolvedOpacity = __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__());
      const resolvedBg = bgHex ? hexToRgba(bgHex, resolvedOpacity) : '';
      const resolvedBorder = borderHex ? hexToRgba(borderHex, 1) : (bgHex ? hexToRgba(bgHex, 1) : '');
      const glyph = btn.querySelector('.home-main-glyph');
      setImp(btn, 'background', 'transparent');
      setImp(btn, 'background-color', 'transparent');
      setImp(btn, 'border-color', 'transparent');
      if (glyph){
        setImp(glyph, 'color', hex);
        setImp(glyph, '-webkit-text-fill-color', hex);
        setImp(glyph, 'background', resolvedBg);
        setImp(glyph, 'background-color', resolvedBg);
        setImp(glyph, 'border-color', resolvedBorder);
        setImp(glyph, 'border-width', '1px');
        setImp(glyph, 'border-style', 'solid');
        setImp(glyph, 'box-shadow', 'none');
        setImp(glyph, 'backdrop-filter', bgHex ? 'none' : '');
        setImp(glyph, '-webkit-backdrop-filter', bgHex ? 'none' : '');
      }
      const svg = btn.querySelector('svg.ui-ico');
      if (svg){
        setImp(svg, 'color', hex);
        svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
          try{ node.style.setProperty('stroke', 'currentColor', 'important'); }catch(_){ node.style.stroke = 'currentColor'; }
          try{ node.style.setProperty('fill', 'none', 'important'); }catch(_){ node.style.fill = 'none'; }
        });
      }
      btn.style.setProperty('--ico-color', hex);
      __applyVisualTextWeight__(btn, visual);
      try{
        const launcherWeight = visual && visual.bold ? '700' : '400';
        btn.style.setProperty('font-weight', launcherWeight, 'important');
        btn.querySelectorAll('.home-main-label, .home-main-label *, .home-main-glyph + div, .home-main-label span').forEach((node) => {
          try{ node.style.setProperty('font-weight', launcherWeight, 'important'); }catch(_){ }
        });
      }catch(_){ }
      return;
    }
    if (btn.closest('#page-impostazioni') || btn.closest('#page-opsettings')){
      const resolvedOpacity = __designBgOpacityNormalize__(visual.opacity ?? __designBgOpacityRead__());
      const isDarkSettings = !!(__isDarkModeEnabled__ && __isDarkModeEnabled__());
      const useForcedDarkPalette = isDarkSettings && !!(btn.closest('#page-impostazioni') || btn.closest('#page-opsettings'));
      const resolvedBg = useForcedDarkPalette
        ? 'rgba(15,23,42,0.80)'
        : (bgHex ? hexToRgba(bgHex, resolvedOpacity) : '');
      const resolvedBorder = useForcedDarkPalette
        ? 'rgba(148,163,184,0.24)'
        : (borderHex ? hexToRgba(borderHex, 1) : (bgHex ? hexToRgba(bgHex, 1) : ''));
      const resolvedFg = hex;
      setImp(btn, 'background', resolvedBg);
      setImp(btn, 'background-color', resolvedBg);
      setImp(btn, 'border-color', resolvedBorder);
      setImp(btn, 'border-width', '1px');
      setImp(btn, 'border-style', 'solid');
      setImp(btn, 'box-shadow', 'none');
      setImp(btn, 'color', resolvedFg);
      setImp(btn, '-webkit-text-fill-color', resolvedFg);
      const label = btn.querySelector('.settings-btn-label');
      if (label){
        setImp(label, 'color', resolvedFg);
        setImp(label, '-webkit-text-fill-color', resolvedFg);
      }
      const svg = btn.querySelector('svg.ui-ico');
      if (svg){
        svg.style.setProperty('color', resolvedFg, 'important');
        svg.style.setProperty('stroke', resolvedFg, 'important');
        svg.style.setProperty('fill', 'none', 'important');
        svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
          node.style.setProperty('stroke', resolvedFg, 'important');
          node.style.setProperty('fill', 'none', 'important');
        });
      }
      __applyVisualTextWeight__(btn, visual);
      try{
        const settingsWeight = visual && visual.bold ? '700' : '400';
        btn.style.setProperty('font-weight', settingsWeight, 'important');
        btn.querySelectorAll('.settings-btn-label, .settings-btn-label *, .settings-btn-value, .settings-btn-title').forEach((node) => {
          try{ node.style.setProperty('font-weight', settingsWeight, 'important'); }catch(_){ }
        });
      }catch(_){ }
      return;
    }
  }catch(_){ }
}

function __applyStatisticsCardTheme__(){
  try{
    const configs = [
      ['#page-statgen .stat-row', 'statgen'],
      ['#page-statspese .stat-row', 'statspese'],
      ['#page-statmensili .month-row', 'statmensili'],
      ['#page-statprenotazioni .stat-row', 'statprenotazioni'],
      ['#page-statchannel .stat-row', 'statchannel'],
      ['#page-statpulizie .stat-row', 'statpulizie']
    ];
    configs.forEach(([selector, pageKey]) => {
      try{
        document.querySelectorAll(selector).forEach((el, index) => {
          const cardKey = String(el?.dataset?.statCardKey || '').trim() || `${pageKey}-${index+1}`;
          el.dataset.statCardKey = cardKey;
          const fallback = getComputedStyle(el).getPropertyValue('--mcol') || getComputedStyle(el).getPropertyValue('--statbg') || '#2B7CB4';
          __applyStatCardTextColor__(el, pageKey, cardKey, fallback);
        });
      }catch(_){ }
    });
  }catch(_){ }
}

function __launcherIconApplyAll__(){
  try{
    __LAUNCHER_ICON_TARGET_IDS__.forEach((id) => {
      const btn = document.getElementById(id);
      if (btn) __launcherIconApplyToButton__(btn);
    });
  }catch(_){ }
  try{ __applyStatisticsCardTheme__(); }catch(_){ }
  try{ __headerActionApplyAll__(); }catch(_){ }
  try{ __pillApplyAll__(); }catch(_){ }
}

function __launcherIconSaveColor__(id, spec, mode = 'fg'){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return;
  const rawMode = String(mode || 'fg').trim().toLowerCase();
  const normalizedMode = rawMode === 'bg' ? 'bg' : (rawMode === 'border' ? 'border' : 'fg');
  const map = __launcherIconColorMapRead__();
  const current = __launcherVisualNormalize__(map[key], __LAUNCHER_ICON_DEFAULT_SPECS__[key] || 'blue-4');
  if (normalizedMode === 'bg') current.bg = __normalizeOperatoreColor__(spec || current.bg || 'blue-4');
  else if (normalizedMode === 'border') current.border = __normalizeOperatoreColor__(spec || current.border || current.bg || current.fg || 'blue-4');
  else current.fg = __normalizeOperatoreColor__(spec || current.fg || 'blue-4');
  map[key] = { fg: current.fg, bg: current.bg || '', border: current.border || '', opacity: __designBgOpacityNormalize__(current.opacity ?? 0.80), bold: !!current.bold };
  __launcherIconColorMapWrite__(map);
  const btn = document.getElementById(key);
  if (btn) __launcherIconApplyToButton__(btn);
}

function __ddae841IsOperationalNoColorPopupButton__(btn){
  try{
    const id = String((btn && btn.id) || '').trim();
    const blocked = new Set([
      'settingsOperatoriBtn','settingsChannelBtn','settingsLaundryCatalogBtn','goLavanderia',
      'btnAddOperatoreCard','btnAddChannelCard','btnAddLaundryComponentCard'
    ]);
    if (blocked.has(id)) return true;
    if (btn && btn.closest && btn.closest('#page-operatori,#page-channel,#page-laundrycatalog')) return true;
  }catch(_){ }
  return false;
}

function __bindLauncherIconLongPress__(btn){
  try{
    if (!btn || __ddae841IsOperationalNoColorPopupButton__(btn) || btn.dataset.colorHoldBound === '1') return;
    btn.dataset.colorHoldBound = '1';
    let holdTimer = null;
    let holdTriggered = false;
    let suppressUntil = 0;
    let startX = 0;
    let startY = 0;
    const clearHold = () => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      holdTimer = null;
    };
    const setSuppress = (ms = 900) => {
      suppressUntil = Date.now() + Math.max(0, Number(ms) || 0);
    };
    const isSuppressed = () => {
      try{ return Date.now() < suppressUntil; }catch(_){ return false; }
    };
    const blockEvent = (ev) => {
      try{ ev?.preventDefault?.(); }catch(_){ }
      try{ ev?.stopImmediatePropagation?.(); }catch(_){ }
      try{ ev?.stopPropagation?.(); }catch(_){ }
    };
    const currentPoint = (ev) => {
      const t = ev?.touches?.[0] || ev?.changedTouches?.[0] || ev;
      return { x: Number(t?.clientX || 0), y: Number(t?.clientY || 0) };
    };
    const getReturnPage = () => {
      try{
        if (btn.closest('#page-impostazioni')) return 'impostazioni';
        if (btn.closest('#page-statistiche')) return 'statistiche';
      }catch(_){ }
      return 'home';
    };
    const keepCurrentLauncherPage = () => {
      const targetPage = getReturnPage();
      try{ showPage(targetPage); }catch(_){ }
      if (targetPage !== 'home'){
        try{ hideLauncher(); }catch(_){ }
      }
    };
    const startHold = (ev) => {
      if (btn.disabled || btn.hidden) return;
      clearHold();
      holdTriggered = false;
      suppressUntil = 0;
      const pt = currentPoint(ev);
      startX = pt.x;
      startY = pt.y;
      blockEvent(ev);
      holdTimer = setTimeout(() => {
        holdTriggered = true;
        setSuppress(1800);
        try{ if (document.activeElement && document.activeElement.blur) document.activeElement.blur(); }catch(_){ }
        const inSettingsPage = !!(btn.closest('#page-impostazioni') || btn.closest('#page-opsettings'));
        const popupOptions = { supportsBg:true, supportsBorder:true, supportsFg:true, defaultMode: inSettingsPage ? 'bg' : 'fg', fallbackBg:'blue-4' };
        if (btn.id !== 'goDbSync'){
          popupOptions.applyCategory = { message:'Applicare le modifiche a tutti i tasti della stessa categoria?', apply: async(payload, changed) => { await __applyLauncherIconChangesToCategory__(payload, changed); } };
        }
        __tagColorPopupOpen__('launcher-icon', __launcherIconVisualFor__(btn.id), (payload) => {
          const currentVisual = __launcherIconVisualFor__(btn.id);
          const nextVisual = __designVisualFromPayload__(currentVisual, payload, __LAUNCHER_ICON_DEFAULT_SPECS__[__singleActionButtonSharedKey__(btn.id) || btn.id] || 'blue-4');
          __writeLauncherIconVisual__(btn.id, nextVisual);
          if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
          try{ __launcherIconApplyAll__(); }catch(_){ }
          try{ renderRoomSettingsPage(); }catch(_){ }
          setSuppress(1800);
          keepCurrentLauncherPage();
        }, popupOptions);
      }, __LAUNCHER_ICON_LONGPRESS_DELAY__);
    };
    const cancelHold = (ev) => {
      clearHold();
      if (holdTriggered || isSuppressed()){
        setSuppress(1800);
        blockEvent(ev);
      }
    };
    const endHold = (ev) => {
      const fired = holdTriggered || isSuppressed();
      clearHold();
      if (fired){
        holdTriggered = false;
        setSuppress(1800);
        blockEvent(ev);
        return;
      }
      setTimeout(() => { holdTriggered = false; }, 0);
    };
    const moveHold = (ev) => {
      if (!holdTimer) return;
      const pt = currentPoint(ev);
      if (Math.abs(pt.x - startX) > 12 || Math.abs(pt.y - startY) > 12){
        clearHold();
      }
    };
    const swallowIfSuppressed = (ev) => {
      if (!holdTriggered && !isSuppressed()) return;
      setSuppress(1600);
      blockEvent(ev);
    };
    btn.addEventListener('pointerdown', startHold, { passive:false, capture:true });
    btn.addEventListener('touchstart', startHold, { passive:false, capture:true });
    btn.addEventListener('pointerup', endHold, { passive:false, capture:true });
    btn.addEventListener('touchend', endHold, { passive:false, capture:true });
    btn.addEventListener('pointerleave', cancelHold, { passive:false, capture:true });
    btn.addEventListener('pointercancel', cancelHold, { passive:false, capture:true });
    btn.addEventListener('touchcancel', cancelHold, { passive:false, capture:true });
    btn.addEventListener('pointermove', moveHold, { passive:true, capture:true });
    btn.addEventListener('touchmove', moveHold, { passive:true, capture:true });
    btn.addEventListener('click', swallowIfSuppressed, true);
    btn.addEventListener('contextmenu', (ev) => { if (holdTriggered || isSuppressed()) blockEvent(ev); }, true);
    btn.addEventListener('selectstart', (ev) => { blockEvent(ev); return false; }, true);
    btn.addEventListener('dragstart', (ev) => { blockEvent(ev); return false; }, true);
  }catch(_){ }
}

function setupLauncherIconLongPressPalette(){
  try{
    __LAUNCHER_ICON_TARGET_IDS__.forEach((id) => {
      const btn = document.getElementById(id);
      if (btn) __bindLauncherIconLongPress__(btn);
    });
    __launcherIconApplyAll__();
  }catch(_){ }
}

function __laundryColorTokens__(value, textValue){
  const spec = __normalizeLaundryColor__(value || 'blue');
  const parsed = __parseOperatoreColorSpec__(spec);
  const main = __operatoreColorHex__(spec);
  const light = __operatoreColorHex__(`${parsed.base}-1`);
  const baseText = '#000000';
  const customText = __tagColorTextHex__(spec, textValue || '', false);
  return {
    spec,
    base: parsed.base,
    shade: parsed.shade,
    rowBg: hexToRgba(main, 0.10),
    rowBorder: hexToRgba(main, 0.22),
    badgePrimaryBg: hexToRgba(main, 0.80),
    badgePrimaryText: customText || '#ffffff',
    badgeSecondaryBg: hexToRgba(light, 0.80),
    badgeSecondaryText: customText || '#0b1f3a',
    label: baseText,
    meta: 'rgba(0,0,0,0.72)',
    price: baseText,
  };
}

function __laundryEscapeAttr__(value){
  return String(value || '')
    .replaceAll('&', '&amp;')
    .replaceAll('"', '&quot;')
    .replaceAll('<', '&lt;')
    .replaceAll('>', '&gt;');
}

function __normalizeOperatoreNameKey__(value){
  return String(value || '').trim().toLowerCase().replace(/\s+/g, ' ');
}

function __operatoreCatalogMapByName__(){
  const map = new Map();
  try{
    (getOperatoriCatalogFromSettings() || []).forEach((item) => {
      const name = String(item?.nome || '').trim();
      const key = __normalizeOperatoreNameKey__(name);
      if (!key) return;
      map.set(key, { ...item, nome: name });
    });
  }catch(_){ }
  return map;
}

function getActiveOperatorNames(){
  return Array.from(__operatoreCatalogMapByName__().values()).map(item => String(item?.nome || '').trim()).filter(Boolean);
}

function getCanonicalActiveOperatorName(name){
  const key = __normalizeOperatoreNameKey__(name);
  if (!key) return '';
  try{
    const map = __operatoreCatalogMapByName__();
    if (map.has(key)) return String(map.get(key)?.nome || '').trim();
  }catch(_){ }
  return '';
}

function isActiveOperatorName(name){
  return !!getCanonicalActiveOperatorName(name);
}

function getOperatoreCatalogItemByName(name){
  const key = String(name || '').trim().toLowerCase();
  if (!key) return null;
  try{
    const map = __operatoreCatalogMapByName__();
    if (map.has(key)) return map.get(key) || null;
    for (const [k, item] of map.entries()){
      if (!k) continue;
      if (k === key || k.includes(key) || key.includes(k)) return item || null;
    }
  }catch(_){ }
  return null;
}

function getOperatoreColorHexByName(name, fallbackColor){
  const item = getOperatoreCatalogItemByName(name);
  return __operatoreColorHex__(item?.colore || fallbackColor || 'blue');
}

function getOperatoreTariffaByName(name, fallbackValue = 0){
  const item = getOperatoreCatalogItemByName(name);
  const val = Number(item?.tariffa);
  if (isFinite(val) && val >= 0) return Math.round(val * 100) / 100;
  const fb = Number(fallbackValue || 0);
  return isFinite(fb) && fb >= 0 ? Math.round(fb * 100) / 100 : 0;
}

function getOperatoreBenzinaByName(name, fallbackValue = 0){
  const item = getOperatoreCatalogItemByName(name);
  const val = Number(item?.benzina);
  if (isFinite(val) && val >= 0) return Math.round(val * 100) / 100;
  const fb = Number(fallbackValue || 0);
  return isFinite(fb) && fb >= 0 ? Math.round(fb * 100) / 100 : 0;
}


function __normalizeChannelColor__(color){
  return __normalizeOperatoreColor__(color || 'orange-2');
}

function __normalizeChannelTextColor__(color){
  return __normalizeOptionalOperatoreColor__(color);
}

function __normalizeChannelGraphColor__(color){
  return __normalizeOptionalOperatoreColor__(color);
}

function __channelInitialFromName__(name){
  const clean = String(name || '').trim();
  return clean ? clean.charAt(0).toUpperCase() : 'C';
}

function __parseChannelCommissionValue__(value, fallback = 0){
  const raw = String(value ?? '').trim();
  if (!raw) return fallback;
  const n = Number(raw.replace(',', '.'));
  return isFinite(n) ? Math.round(n * 100) / 100 : fallback;
}

function getChannelCatalogFromSettings(){
  const row = getSettingRow("channel_catalogo");
  const raw = row ? (row.value ?? row.Value ?? "") : "";
  if (!String(raw || "").trim()) return [];
  try{
    const parsed = JSON.parse(String(raw || '[]'));
    if (!Array.isArray(parsed)) return [];
    return parsed.map((item, idx) => ({
      id: String(item?.id || `ch-${idx+1}-${Date.now()}`),
      nome: String(item?.nome || item?.name || '').trim(),
      commissione: __parseChannelCommissionValue__(item?.commissione ?? item?.commission ?? item?.percentuale ?? '', 0),
      iniziale: String(item?.iniziale || item?.initial || '').trim().slice(0,1).toUpperCase(),
      colore: __normalizeChannelColor__(item?.colore),
      coloreTesto: __normalizeChannelTextColor__(item?.coloreTesto ?? item?.textColor),
      coloreGrafico: __normalizeChannelGraphColor__(item?.coloreGrafico ?? item?.graphColor ?? item?.dotColor),
    })).filter(item => item.nome).map(item => ({ ...item, iniziale: item.iniziale || __channelInitialFromName__(item.nome) }));
  }catch(_){
    return [];
  }
}

async function saveChannelCatalogToSettings(list){
  const clean = (Array.isArray(list) ? list : []).map((item, idx) => ({
    id: String(item?.id || `ch-${Date.now()}-${idx}`),
    nome: String(item?.nome || '').trim(),
    commissione: __parseChannelCommissionValue__(item?.commissione ?? item?.commission ?? '', 0),
    iniziale: String(item?.iniziale || item?.initial || '').trim().slice(0,1).toUpperCase() || __channelInitialFromName__(item?.nome),
    colore: __normalizeChannelColor__(item?.colore),
    coloreTesto: __normalizeChannelTextColor__(item?.coloreTesto ?? item?.textColor),
    coloreGrafico: __normalizeChannelGraphColor__(item?.coloreGrafico ?? item?.graphColor ?? item?.dotColor),
  })).filter(item => item.nome);
  await api("impostazioni", { method:"POST", body:{ channel_catalogo: clean }, showLoader:true });
  await ensureSettingsLoaded({ force:true, showLoader:false });
  try{ populateGuestChannelOptions(); }catch(_){ }
  return clean;
}

function getChannelCatalogItemById(id){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return null;
  return getChannelCatalogFromSettings().find(item => String(item.id) === key) || null;
}

function populateGuestChannelOptions(selectedId = null){
  const sel = document.getElementById('guestChannel');
  if (!sel) return;
  const current = (selectedId == null) ? String(sel.value || '').trim() : String(selectedId || '').trim();
  const items = getChannelCatalogFromSettings();
  sel.innerHTML = `<option value=""></option>` + items.map(item => `<option value="${String(item.id).replace(/[&<>"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]||s))}">${String(item.nome || '').replace(/[&<>"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]||s))}</option>`).join('');
  if (current && items.some(item => String(item.id) === current)) sel.value = current;
}

function applySelectedChannelToGuestForm(channelId, { preserveManual=false } = {}){
  const sel = document.getElementById('guestChannel');
  const pctEl = document.getElementById('guestChannelCommission');
  const item = getChannelCatalogItemById(channelId || sel?.value || '');
  if (sel && item) sel.value = String(item.id);
  if (pctEl) pctEl.value = item ? String(item.commissione) : '';
  try{ state.guestChannelId = item ? String(item.id) : ''; }catch(_){ }
  try{ state.guestChannelName = item ? String(item.nome) : ''; }catch(_){ }
  try{ state.guestChannelColor = item ? String(item.colore) : ''; }catch(_){ }
  try{ state.guestChannelTextColor = item ? String(item.coloreTesto || '') : ''; }catch(_){ }
  try{ state.guestChannelInitial = item ? String(item.iniziale || __channelInitialFromName__(item.nome)) : ''; }catch(_){ }
  try{ state.guestChannelCommissionPct = item ? Number(item.commissione || 0) : 0; }catch(_){ }
  try{ recalcGuestCommission(); }catch(_){ }
  if (!preserveManual){ try{ refreshFloatingLabels(); }catch(_){ } }
}

function getGuestChannelBadgeData(item){
  const id = String(item?.channel_id ?? item?.channelId ?? '').trim();
  const catalogItem = id ? getChannelCatalogItemById(id) : null;
  const color = __normalizeChannelColor__(catalogItem?.colore || item?.channel_colore || item?.channelColor || 'orange');
  const textColor = __normalizeChannelTextColor__(catalogItem?.coloreTesto || item?.channel_colore_testo || item?.channelColorText || '');
  const initial = String(catalogItem?.iniziale || item?.channel_iniziale || item?.channelInitial || __channelInitialFromName__(catalogItem?.nome || item?.channel_nome || item?.channelName || '')).trim().slice(0,1).toUpperCase() || 'C';
  const name = String(catalogItem?.nome || item?.channel_nome || item?.channelName || '').trim();
  return { color, textColor, style: __tagColorInlineStyle__(color, textColor, { opacity:0.80, borderOpacity:1, preferWhiteText:false }), initial, name };
}

function __operatoriCatalogDefaultFromLegacy__(){
  const names = getOperatorNamesFromSettings().filter(Boolean);
  if (!names.length) return [];
  const tariffa = getSettingNumber("tariffa_oraria", 0);
  const benzina = getSettingNumber("costo_benzina", 0);
  return names.map((nome, idx) => ({
    id: `legacy-${idx+1}`,
    nome,
    tariffa,
    benzina,
    colore: __OPERATORI_COLOR_KEYS__[idx] || "blue",
  }));
}

function getOperatoriCatalogFromSettings(){
  const row = getSettingRow("operatori_catalogo");
  const raw = row ? (row.value ?? row.Value ?? "") : "";
  if (!String(raw || "").trim()) return __operatoriCatalogDefaultFromLegacy__();
  try{
    const parsed = JSON.parse(String(raw || "[]"));
    if (!Array.isArray(parsed)) return __operatoriCatalogDefaultFromLegacy__();
    return parsed.map((item, idx) => ({
      id: String(item?.id || `op-${idx+1}-${Date.now()}`),
      nome: String(item?.nome || item?.name || "").trim(),
      tariffa: (() => { const n = Number(String(item?.tariffa ?? "").replace(",", ".")); return isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0; })(),
      benzina: (() => { const n = Number(String(item?.benzina ?? "").replace(",", ".")); return isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0; })(),
      colore: __normalizeOperatoreColor__(item?.colore),
      coloreTesto: __normalizeOptionalOperatoreColor__(item?.coloreTesto ?? item?.textColor),
      mostraSaldo: !!(item?.mostraSaldo ?? item?.showBalance ?? item?.saldoCalendario),
    })).filter(item => item.nome);
  }catch(_){
    return __operatoriCatalogDefaultFromLegacy__();
  }
}

async function saveOperatoriCatalogToSettings(list){
  const clean = (Array.isArray(list) ? list : []).map((item, idx) => ({
    id: String(item?.id || `op-${Date.now()}-${idx}`),
    nome: String(item?.nome || "").trim(),
    tariffa: (() => { const n = Number(String(item?.tariffa ?? "").replace(",", ".")); return isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0; })(),
    benzina: (() => { const n = Number(String(item?.benzina ?? "").replace(",", ".")); return isFinite(n) && n >= 0 ? Math.round(n * 100) / 100 : 0; })(),
    colore: __normalizeOperatoreColor__(item?.colore),
    coloreTesto: __normalizeOptionalOperatoreColor__(item?.coloreTesto ?? item?.textColor),
    mostraSaldo: !!(item?.mostraSaldo ?? item?.showBalance ?? item?.saldoCalendario),
  })).filter(item => item.nome);
  const firstThree = clean.slice(0, 3).map(item => item.nome);
  await api("impostazioni", {
    method: "POST",
    body: {
      operatori_catalogo: clean,
      operatori: firstThree,
    },
    showLoader: true,
  });
  await ensureSettingsLoaded({ force: true, showLoader: false });
  try{ if (window.__syncCleanOperators__) window.__syncCleanOperators__(); }catch(_){ }
  return clean;
}

async function ensureSettingsLoaded({ force = false, showLoader = false } = {}) {
  try {
    if (!force && state.settings?.loaded) return state.settings;
    const data = await api("impostazioni", { method: "GET", showLoader });
    const rows = data?.rows || data?.items || [];
    state.settings.rows = Array.isArray(rows) ? rows : [];
    state.settings.byKey = __parseSettingsRows(state.settings.rows);
    try{ __persistLaundryCatalogCache__(getLaundryCatalogFromSettings()); }catch(_){ }
    state.settings.loaded = true;
    state.settings.loadedAt = Date.now();

    try{ if (typeof window.__syncCleanOperators__ === "function") window.__syncCleanOperators__(); }catch(_){ }
    try{ refreshFloatingLabels(); }catch(_){ }
    try{ updateSettingsRoomsButtonLabel(); }catch(_){ }
    try{ ensureRoomsPickerButtons(); }catch(_){ }
    try{ populateGuestChannelOptions(); }catch(_){ }
    try{ __applyRoomsUiConfig__(); }catch(_){ }
    try{ __hydrateAppLanguageFromSettings__(); }catch(_){ }
    try{ if (state && state.page === "pulizie") window.__ddae_refreshPulizieGrid?.({ forceReload:true }); }catch(_){ }
    try{ if (state && state.page === "lavanderia") renderLaundry_(state?.laundry?.current || null); }catch(_){ }
    try{ if (state && state.page === "laundrycatalog") renderLaundryCatalogPage(); }catch(_){ }

    return state.settings;
  } catch (e) {
    // Non bloccare l'app se il foglio non è ancora pronto
    console.warn("Impostazioni: load failed", e);
    return state.settings;
  }
}

const __settingsTaxCapUi = {
  value: 0,
  holdTimer: null,
  holdTriggered: false,
  holdDelay: 500,
  lastTapAt: 0,
  suppressTapUntil: 0,
  recentPointerUpAt: 0,
  pendingTouchClickUntil: 0,
};

function getTouristTaxMaxNightsSetting(fallback = 3) {
  const raw = getSettingNumber("tassa_soggiorno_max_notti", fallback);
  return Math.max(0, parseInt(raw, 10) || 0);
}

function renderTassaMaxNottiButton() {
  const btn = document.getElementById("setTassaMaxNottiBtn");
  const val = document.getElementById("setTassaMaxNottiVal");
  const n = Math.max(0, parseInt(__settingsTaxCapUi.value, 10) || 0);
  if (btn) {
    btn.dataset.value = String(n);
    btn.classList.toggle("is-empty", n <= 0);
  }
  if (val) val.textContent = n > 0 ? String(n) : "—";
}

function setTassaMaxNottiValue(next, { silent = false } = {}) {
  __settingsTaxCapUi.value = Math.max(0, parseInt(next, 10) || 0);
  renderTassaMaxNottiButton();
  if (!silent) refreshFloatingLabels();
}

function bindTassaMaxNottiButton() {
  const btn = document.getElementById("setTassaMaxNottiBtn");
  if (!btn || btn.dataset.bound === "1") return;
  btn.dataset.bound = "1";

  const DOUBLE_TAP_MS = 240;

  const clearHold = () => {
    if (__settingsTaxCapUi.holdTimer) {
      clearTimeout(__settingsTaxCapUi.holdTimer);
      __settingsTaxCapUi.holdTimer = null;
    }
  };

  const increment = () => {
    setTassaMaxNottiValue((__settingsTaxCapUi.value || 0) + 1);
  };

  const reset = () => {
    setTassaMaxNottiValue(0);
    try { toast("Numero notti azzerato"); } catch(_) {}
  };

  const block = (e) => {
    try{ e?.preventDefault?.(); }catch(_){ }
    try{ e?.stopPropagation?.(); }catch(_){ }
    try{ e?.stopImmediatePropagation?.(); }catch(_){ }
  };

  const startHold = (e) => {
    try{
      if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && (e.button || 0) !== 0) return;
      if (e && e.type === 'mousedown' && (e.button || 0) !== 0) return;
    }catch(_){ }
    clearHold();
    __settingsTaxCapUi.holdTriggered = false;
    __settingsTaxCapUi.holdTimer = setTimeout(() => {
      __settingsTaxCapUi.holdTriggered = true;
      __settingsTaxCapUi.suppressTapUntil = Date.now() + 900;
    }, __settingsTaxCapUi.holdDelay);
  };

  const endHold = () => {
    clearHold();
    setTimeout(() => { __settingsTaxCapUi.holdTriggered = false; }, 0);
  };

  const registerTap = (e, source = 'tap') => {
    if (__settingsTaxCapUi.holdTriggered || Date.now() < (__settingsTaxCapUi.suppressTapUntil || 0)) {
      block(e);
      return;
    }
    const now = Date.now();
    const lastTapAt = Number(__settingsTaxCapUi.lastTapAt || 0);
    __settingsTaxCapUi.lastTapAt = now;
    if (lastTapAt && (now - lastTapAt) <= DOUBLE_TAP_MS) {
      __settingsTaxCapUi.lastTapAt = 0;
      reset();
      block(e);
      return;
    }
    increment();
    if (source === 'pointerup') {
      __settingsTaxCapUi.recentPointerUpAt = now;
    }
    if (source === 'touchend') {
      __settingsTaxCapUi.pendingTouchClickUntil = now + 700;
    }
    block(e);
  };

  const handlePointerUp = (e) => {
    __settingsTaxCapUi.recentPointerUpAt = Date.now();
    registerTap(e, 'pointerup');
  };

  const handleTouchEnd = (e) => {
    if ((Date.now() - Number(__settingsTaxCapUi.recentPointerUpAt || 0)) <= 450) {
      block(e);
      return;
    }
    registerTap(e, 'touchend');
  };

  const handleMouseUp = (e) => {
    if ((Date.now() - Number(__settingsTaxCapUi.recentPointerUpAt || 0)) <= 450) {
      block(e);
      return;
    }
    registerTap(e, 'mouseup');
  };

  ['pointerdown','touchstart','mousedown'].forEach((evt) => {
    try{ btn.addEventListener(evt, startHold, { passive:true }); }catch(_){ }
  });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave'].forEach((evt) => {
    try{ btn.addEventListener(evt, endHold, { passive:true }); }catch(_){ }
  });

  if (window.PointerEvent) {
    try{ btn.addEventListener('pointerup', handlePointerUp, true); }catch(_){ }
    try{ btn.addEventListener('touchend', handleTouchEnd, true); }catch(_){ }
  } else {
    try{ btn.addEventListener('touchend', handleTouchEnd, true); }catch(_){ }
    try{ btn.addEventListener('mouseup', handleMouseUp, true); }catch(_){ }
  }

  btn.addEventListener('click', (e) => {
    if (__settingsTaxCapUi.holdTriggered || Date.now() < (__settingsTaxCapUi.suppressTapUntil || 0)) {
      block(e);
      return;
    }
    const now = Date.now();
    if ((now - Number(__settingsTaxCapUi.recentPointerUpAt || 0)) <= 700) {
      block(e);
      return;
    }
    if (now < Number(__settingsTaxCapUi.pendingTouchClickUntil || 0)) {
      block(e);
      return;
    }
    registerTap(e, 'click');
  }, true);

  btn.addEventListener('dblclick', (e) => {
    block(e);
  }, true);
}

function __syncSettingsConfigModalGrid__(){
  const modal = document.getElementById("settingsConfigModal");
  const body = modal ? modal.querySelector(".settings-popup-body") : null;
  if (!body) return;
  try{
    const bodyWidth = Math.max(0, Math.round(body.getBoundingClientRect().width || 0));
    const gap = window.matchMedia('(max-width: 640px)').matches ? 12 : 14;
    const target = Math.max(window.matchMedia('(max-width: 640px)').matches ? 116 : 128, Math.min(160, Math.floor((bodyWidth - gap) / 2)));
    body.style.setProperty('--settings-stay-cell-h', target + 'px');
  }catch(_){ }
}

function __openSettingsConfigModal__(){
  const modal = document.getElementById("settingsConfigModal");
  if (!modal) return;
  modal.hidden = false;
  modal.setAttribute("aria-hidden", "false");
  try{ loadImpostazioniPage({ force:false }); }catch(_){ }
  try{ refreshFloatingLabels(); }catch(_){ }
  try{ __syncSettingsConfigModalGrid__(); }catch(_){ }
  try{ requestAnimationFrame(() => { try{ __syncSettingsConfigModalGrid__(); }catch(_){} }); }catch(_){ }
  try{ __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
}


function __closeSettingsConfigModal__(){
  const modal = document.getElementById("settingsConfigModal");
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute("aria-hidden", "true");
}

let __settingsBackupModalSuppressUntil__ = 0;
function __settingsBackupModalGhostTapActive__(){
  try{ return Date.now() < (__settingsBackupModalSuppressUntil__ || 0); }catch(_){ return false; }
}
function __settingsBackupModalSwallowGhostTap__(ev){
  try{
    if (!__settingsBackupModalGhostTapActive__()) return;
    const modal = document.getElementById("settingsBackupModal");
    const insideModal = !!(modal && !modal.hidden && ev && ev.target && modal.contains(ev.target));
    if (insideModal) return;
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    try{ ev.stopImmediatePropagation(); }catch(_){ }
  }catch(_){ }
}
function __openSettingsBackupModal__(){
  const modal = document.getElementById("settingsBackupModal");
  if (!modal) return;
  if (__settingsBackupModalGhostTapActive__()) return;
  __settingsBackupModalSuppressUntil__ = 0;
  modal.hidden = false;
  modal.setAttribute("aria-hidden", "false");
}

function __closeSettingsBackupModal__(){
  const modal = document.getElementById("settingsBackupModal");
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute("aria-hidden", "true");
  __settingsBackupModalSuppressUntil__ = Date.now() + 900;
  try{
    const opener = document.getElementById("settingsDbBtn");
    if (opener){
      opener.style.pointerEvents = "none";
      setTimeout(() => {
        try{ opener.style.pointerEvents = ""; }catch(_){ }
      }, 950);
    }
  }catch(_){ }
}

function __sanitizeTaxInputRaw__(raw){
  try{
    return String(raw ?? '').replace(/€/g, '').replace(/\s+/g, '').replace(/[^0-9,\.]/g, '').replace(/,(?=.*[,])/g, '').trim();
  }catch(_){ return ''; }
}

function __formatTaxInputDisplay__(raw){
  const clean = __sanitizeTaxInputRaw__(raw).replace(',', '.');
  if (!clean) return '';
  const n = Number(clean);
  if (!isFinite(n) || n < 0) return '';
  const rounded = Math.round(n * 100) / 100;
  let text = (Math.round(rounded * 100) % 100 === 0)
    ? String(Math.round(rounded))
    : ((Math.round(rounded * 100) % 10 === 0) ? rounded.toFixed(1) : rounded.toFixed(2));
  text = text.replace('.', ',');
  return text + ' €';
}

function __bindSettingsTaxInput__(){
  const el = document.getElementById('setTassa');
  if (!el || el.dataset.boundTaxDisplay === '1') return;
  el.dataset.boundTaxDisplay = '1';
  const strip = () => {
    try{
      const clean = __sanitizeTaxInputRaw__(el.value).replace(',', '.');
      el.value = clean;
      try{ el.select(); }catch(_){ }
    }catch(_){ }
  };
  const format = () => {
    try{ el.value = __formatTaxInputDisplay__(el.value); }catch(_){ }
  };
  el.addEventListener('focus', strip);
  el.addEventListener('click', () => { try{ if (document.activeElement === el) el.select(); }catch(_){ } });
  el.addEventListener('blur', format);
}

async function loadImpostazioniPage({ force = false } = {}) {
  await ensureSettingsLoaded({ force, showLoader: true });
  try {
    const ts = document.getElementById("setTassa");
    if (ts) ts.value = __formatTaxInputDisplay__(String(getSettingNumber("tassa_soggiorno", (typeof TOURIST_TAX_EUR_PPN !== "undefined" ? TOURIST_TAX_EUR_PPN : 0)) || ""));

    __bindSettingsTaxInput__();
    bindTassaMaxNottiButton();
    setTassaMaxNottiValue(getTouristTaxMaxNightsSetting(3), { silent: true });
    try{ updateSettingsAccountName(); }catch(_){ }
    try{ updateSettingsRoomsButtonLabel(); }catch(_){ }
    try{ ensureRoomsPickerButtons(); }catch(_){ }

    refreshFloatingLabels();
  } catch (e) {
    toast(e.message);
  }
}

function __readNumInput(id) {
  const el = document.getElementById(id);
  const raw = el ? String(el.value || "").trim() : "";
  if (!raw) return "";
  const n = Number(__sanitizeTaxInputRaw__(raw).replace(",", "."));
  if (!isFinite(n) || n < 0) return "";
  return Math.round(n * 100) / 100;
}

async function saveImpostazioniPage() {
  const tassa = __readNumInput("setTassa");
  const tassaMaxNotti = Math.max(0, parseInt(__settingsTaxCapUi.value, 10) || 0);

  const payload = {
    tassa_soggiorno: tassa,
    tassa_soggiorno_max_notti: tassaMaxNotti,
  };

  await api("impostazioni", { method: "POST", body: payload, showLoader: true });
  await ensureSettingsLoaded({ force: true, showLoader: false });

  try{ __closeSettingsConfigModal__(); }catch(_){ }
  toast("Impostazioni salvate");
}

const __operatoriPageUi = {
  color: "blue-2",
  textColor: "",
  editingId: "",
  tones: {},
};

function __operatoriFormatMoney__(value){
  const n = Number(value || 0);
  const safe = isFinite(n) ? n : 0;
  return `€ ${safe.toFixed(2)}`;
}

function __initColorToneMap__(ui, fallbackBase = 'blue'){
  try{
    if (!ui.tones || typeof ui.tones !== 'object') ui.tones = {};
    __OPERATORI_COLOR_KEYS__.forEach((base) => {
      ui.tones[base] = __OPERATORI_COLOR_DEFAULT_SHADE__;
    });
    const parsed = __parseOperatoreColorSpec__(ui.color || `${fallbackBase}-${__OPERATORI_COLOR_DEFAULT_SHADE__}`);
    ui.tones[parsed.base] = parsed.shade;
    ui.color = parsed.spec;
  }catch(_){ }
}

function __updateColorButtonGrid__(selector, ui){
  try{
    const selected = __parseOperatoreColorSpec__(ui.color || 'blue-2');
    document.querySelectorAll(`${selector} .operatori-color-option`).forEach((btn) => {
      const base = String(btn.dataset.color || 'blue').trim().toLowerCase();
      const shade = Math.min(__OPERATORI_COLOR_SHADE_COUNT__, Math.max(1, parseInt(ui?.tones?.[base] || __OPERATORI_COLOR_DEFAULT_SHADE__, 10) || __OPERATORI_COLOR_DEFAULT_SHADE__));
      const hex = __operatoreColorHex__(`${base}-${shade}`);
      btn.classList.toggle('is-selected', base === selected.base);
      btn.dataset.shade = String(shade);
      btn.dataset.spec = `${base}-${shade}`;
      btn.style.background = hex;
      btn.textContent = String(shade);
    });
  }catch(_){ }
}


function __operatoriSetSelectedColor__(color){
  const parsed = __parseOperatoreColorSpec__(color || 'blue-2');
  __operatoriPageUi.color = parsed.spec;
  if (!__operatoriPageUi.tones || typeof __operatoriPageUi.tones !== 'object') __operatoriPageUi.tones = {};
  __OPERATORI_COLOR_KEYS__.forEach((base) => {
    if (!__operatoriPageUi.tones[base]) __operatoriPageUi.tones[base] = __OPERATORI_COLOR_DEFAULT_SHADE__;
  });
  __operatoriPageUi.tones[parsed.base] = parsed.shade;
  __updateColorButtonGrid__('#operatoriColorGrid', __operatoriPageUi);
  __setTagPreviewButtonStyle__('operatoriEditorTagColor', __operatoriPageUi.color || 'blue-2', __operatoriPageUi.textColor || '');
  __setTagPreviewButtonStyle__('operatoriEditorDotColor', __operatoriPageUi.color || 'blue-2', __operatoriPageUi.textColor || '');
}

function __operatoriSetSelectedTextColor__(color){
  __operatoriPageUi.textColor = __normalizeOptionalOperatoreColor__(color);
  __setTagPreviewButtonStyle__('operatoriEditorTagColor', __operatoriPageUi.color || 'blue-2', __operatoriPageUi.textColor || '');
  __setTagPreviewButtonStyle__('operatoriEditorDotColor', __operatoriPageUi.color || 'blue-2', __operatoriPageUi.textColor || '');
}

const __tagColorPopupState__ = { target: "", onSelect: null, mode:'fg', supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:0.80, colors:{ bg:'blue-4', border:'blue-4', fg:'' }, initial:{ bg:'blue-4', border:'blue-4', fg:'', opacity:0.80 }, applyCategory:null, onPreview:null, onRevert:null, previewDirty:false };
let __tagColorPopupReadyAt__ = 0;
let __tagColorPopupSuppressUntil__ = 0;
let __tagColorPopupLastDualMode__ = 'bg';
function __tagColorPopupGhostTapActive__(){
  try{ return Date.now() < (__tagColorPopupSuppressUntil__ || 0); }catch(_){ return false; }
}
function __tagColorPopupSwallowGhostTap__(ev){
  try{
    if (!__tagColorPopupGhostTapActive__()) return;
    const modal = document.getElementById('tagColorModal');
    const confirmModal = document.getElementById('confirmYesNoModal');
    const insideModal = !!(modal && !modal.hidden && ev && ev.target && modal.contains(ev.target));
    const insideConfirm = !!(confirmModal && !confirmModal.hidden && ev && ev.target && confirmModal.contains(ev.target));
    if (insideModal || insideConfirm) return;
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    try{ ev.stopImmediatePropagation(); }catch(_){ }
  }catch(_){ }
}


function __tagColorPopupChangedFields__(payload){
  try{
    const initial = (__tagColorPopupState__ && __tagColorPopupState__.initial && typeof __tagColorPopupState__.initial === 'object') ? __tagColorPopupState__.initial : { bg:'blue-4', border:'blue-4', fg:'', opacity:0.80 };
    const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    const next = {
      bg: __normalizeOperatoreColor__(colors.bg || initial.bg || 'blue-4'),
      border: __normalizeOperatoreColor__(colors.border || colors.bg || initial.border || initial.bg || 'blue-4'),
      fg: __normalizeOptionalOperatoreColor__(colors.fg || initial.fg || ''),
      opacity: __designBgOpacityNormalize__(payload?.opacity ?? initial.opacity ?? 0.80)
    };
    const changed = {
      bg: String(next.bg || '') !== String(initial.bg || ''),
      border: String(next.border || '') !== String(initial.border || ''),
      fg: String(next.fg || '') !== String(initial.fg || ''),
      opacity: String(next.opacity) !== String(__designBgOpacityNormalize__(initial.opacity ?? 0.80))
    };
    changed.any = !!(changed.bg || changed.border || changed.fg || changed.opacity);
    changed.next = next;
    changed.initial = initial;
    return changed;
  }catch(_){ return { bg:false, border:false, fg:false, opacity:false, any:false, next:null, initial:null }; }
}

async function __tagColorPopupApplyCategoryIfNeeded__(payload, applyCategoryCfg){
  try{
    const cfg = (applyCategoryCfg && typeof applyCategoryCfg === 'object') ? applyCategoryCfg : ((__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? __tagColorPopupState__.applyCategory : null);
    if (!cfg || typeof cfg.apply !== 'function') return;
    const changed = __tagColorPopupChangedFields__(payload);
    if (!changed.any) return;
    const ok = await confirmYesNo(String(cfg.message || 'Applicare le modifiche a tutta la categoria di questo tasto?'));
    if (!ok) return;
    await cfg.apply(payload, changed);
  }catch(_){ }
}

function __tagColorPopupSelectedSpec__(){
  const mode = String(__tagColorPopupState__.mode || 'fg').trim().toLowerCase();
  if (mode === 'opacity') return String(__designBgOpacityNormalize__((__tagColorPopupState__.opacity ?? 0.80)));
  if (mode === 'bg') return __normalizeOperatoreColor__(__tagColorPopupState__.colors?.bg || 'blue-4');
  if (mode === 'border') return __normalizeOperatoreColor__(__tagColorPopupState__.colors?.border || __tagColorPopupState__.colors?.bg || 'blue-4');
  return __normalizeOperatoreColor__(__tagColorPopupState__.colors?.fg || __tagColorPopupState__.colors?.bg || 'blue-4');
}

function __tagColorPopupRefreshModeButtons__(){
  const bar = document.getElementById('tagColorModeBar');
  if (!bar) return;
  const isEnabled = (mode) => {
    if (mode === 'bg') return !!__tagColorPopupState__.supportsBg;
    if (mode === 'border') return !!__tagColorPopupState__.supportsBorder;
    if (mode === 'opacity') return !!__tagColorPopupState__.supportsOpacity;
    return !!__tagColorPopupState__.supportsFg;
  };
  const buttons = Array.from(bar.querySelectorAll('.tag-color-mode-btn'));
  bar.hidden = false;
  try{ bar.style.setProperty('--tag-color-mode-count', String(Math.max(1, buttons.length || 4))); }catch(_){ }
  buttons.forEach((btn) => {
    const mode = String(btn.dataset.mode || '').trim().toLowerCase();
    const enabled = isEnabled(mode);
    btn.hidden = false;
    btn.disabled = !enabled;
    btn.classList.toggle('is-disabled', !enabled);
    btn.classList.toggle('is-active', enabled && mode === __tagColorPopupState__.mode);
    try{ btn.setAttribute('aria-disabled', enabled ? 'false' : 'true'); }catch(_){ }
    try{ btn.tabIndex = enabled ? 0 : -1; }catch(_){ }
  });
}

function __tagColorPopupRefreshSelection__(){
  const grid = document.getElementById('tagColorGrid');
  const opacityWrap = document.getElementById('tagOpacityWrap');
  const opacityGrid = document.getElementById('tagOpacityGrid');
  const selected = __tagColorPopupSelectedSpec__();
  const isOpacityMode = String(__tagColorPopupState__.mode || '').trim().toLowerCase() === 'opacity';
  if (grid){
    grid.hidden = isOpacityMode;
    grid.querySelectorAll('.tag-color-option').forEach((btn) => {
      const spec = __parseOperatoreColorSpec__(btn.dataset.spec || '').spec;
      btn.classList.toggle('is-selected', !isOpacityMode && spec === selected);
    });
  }
  if (opacityWrap) opacityWrap.hidden = !isOpacityMode;
  if (opacityGrid){
    opacityGrid.querySelectorAll('.tag-opacity-option').forEach((btn) => {
      const raw = __designBgOpacityNormalize__(parseFloat(btn.dataset.opacity || '0.80'));
      btn.classList.toggle('is-selected', isOpacityMode && String(raw) === String(selected));
    });
  }
  __tagColorPopupRefreshModeButtons__();
}

function __tagColorPopupApplyViewportLayout__(){
  try{
    const modal = document.getElementById('tagColorModal');
    const card = modal?.querySelector?.('.tag-color-modal-card');
    const body = modal?.querySelector?.('.tag-color-modal-body');
    const header = modal?.querySelector?.('.tag-color-modal-hd');
    const grid = document.getElementById('tagColorGrid');
    const modeBar = document.getElementById('tagColorModeBar');
    const opacityWrap = document.getElementById('tagOpacityWrap');
    const opacityGrid = document.getElementById('tagOpacityGrid');
    if (!modal || modal.hidden || !card || !body || !grid) return;
    const rootStyle = window.getComputedStyle ? window.getComputedStyle(document.documentElement) : null;
    const safeTop = Math.max(0, Math.round(parseFloat(rootStyle?.getPropertyValue('--safe-top') || '0') || 0));
    const safeBottom = Math.max(0, Math.round(parseFloat(rootStyle?.getPropertyValue('--safe-bottom') || '0') || 0));
    const vv = window.visualViewport;
    const viewportHeight = Math.max(320, Math.round((vv && vv.height) || window.innerHeight || document.documentElement.clientHeight || 0));
    const viewportWidth = Math.max(280, Math.round((vv && vv.width) || window.innerWidth || document.documentElement.clientWidth || 0));
    const sidePadding = Math.max(8, Math.min(14, Math.round(viewportWidth * 0.022)));
    const verticalPadding = Math.max(6, Math.min(12, Math.round(viewportHeight * 0.012)));
    const cardWidth = Math.max(300, Math.min(460, viewportWidth - (sidePadding * 2)));
    const cardHeight = Math.max(400, Math.min(720, viewportHeight - safeTop - safeBottom - (verticalPadding * 2)));
    const gridPad = Math.max(7, Math.min(10, Math.round(viewportWidth * 0.018)));
    const gridPadBottom = gridPad + Math.max(8, Math.min(12, Math.round(gridPad * 0.9)));
    const opacityPad = Math.max(5, gridPad - 2);
    modal.style.setProperty('--tag-color-popup-side-pad', `${sidePadding}px`);
    modal.style.setProperty('--tag-color-popup-vertical-pad', `${verticalPadding}px`);
    card.style.setProperty('--tag-color-popup-width', `${cardWidth}px`);
    card.style.setProperty('--tag-color-popup-height', `${cardHeight}px`);
    card.style.width = `${cardWidth}px`;
    card.style.minWidth = `${cardWidth}px`;
    card.style.maxWidth = `${cardWidth}px`;
    card.style.height = `${cardHeight}px`;
    card.style.minHeight = `${cardHeight}px`;
    card.style.maxHeight = `${cardHeight}px`;
    card.style.overflow = 'hidden';
    body.style.overflow = 'auto';
    body.style.overflowY = 'auto';
    body.style.overflowX = 'hidden';
    const cardStyle = window.getComputedStyle ? window.getComputedStyle(card) : null;
    const padTop = Math.round(parseFloat(cardStyle?.paddingTop || '0') || 0);
    const padBottom = Math.round(parseFloat(cardStyle?.paddingBottom || '0') || 0);
    const headerRect = header?.getBoundingClientRect?.() || { height: 0 };
    const availableBodyHeight = Math.max(210, Math.floor(cardHeight - padTop - padBottom - headerRect.height - 4));
    body.style.height = `${availableBodyHeight}px`;
    body.style.minHeight = `${availableBodyHeight}px`;
    body.style.maxHeight = `${availableBodyHeight}px`;
    const bodyStyle = window.getComputedStyle ? window.getComputedStyle(body) : null;
    const bodyInnerWidth = Math.max(260, Math.floor(cardWidth - ((parseFloat(bodyStyle?.paddingLeft || '0') || 0) + (parseFloat(bodyStyle?.paddingRight || '0') || 0))));
    const modeHeight = Math.max(34, Math.min(46, Math.round(availableBodyHeight * 0.088)));
    const opacityButtonHeight = Math.max(40, Math.min(56, Math.round(availableBodyHeight * 0.13)));
    const opacityGridHeight = opacityButtonHeight + (opacityPad * 2);
    const reservedOpacity = (!opacityWrap || opacityWrap.hidden) ? 0 : (opacityGridHeight + 6);
    const reservedMode = (!modeBar || modeBar.hidden) ? 0 : (modeHeight + 8);
    const gap = viewportHeight <= 700 ? 2 : (viewportHeight <= 840 ? 3 : 4);
    const cols = 6;
    const total = grid.querySelectorAll('.tag-color-option').length || 72;
    const rows = Math.max(1, Math.ceil(total / cols));
    const availableHeight = Math.max(170, Math.floor(availableBodyHeight - reservedMode - reservedOpacity - gridPad - gridPadBottom - 4));
    const gridInnerWidth = Math.max(240, bodyInnerWidth - (gridPad * 2));
    const cellWidth = Math.floor((gridInnerWidth - (gap * (cols - 1))) / cols);
    const maxHeightFit = Math.floor((availableHeight - (gap * (rows - 1))) / rows);
    const baseHeightFromWidth = Math.floor(cellWidth * 1.02);
    const targetHeight = Math.floor(baseHeightFromWidth * 1.08);
    const finalHeight = Math.max(20, Math.min(maxHeightFit, targetHeight));
    const radius = Math.max(9, Math.min(18, Math.round(finalHeight * 0.34)));
    const computedGridHeight = Math.max(0, (finalHeight * rows) + (gap * Math.max(0, rows - 1)) + gridPad + gridPadBottom);
    card.style.setProperty('--tag-color-gap', `${gap}px`);
    card.style.setProperty('--tag-color-cell-height', `${finalHeight}px`);
    card.style.setProperty('--tag-color-cell-radius', `${radius}px`);
    card.style.setProperty('--tag-color-grid-height', `${computedGridHeight}px`);
    card.style.setProperty('--tag-color-grid-pad', `${gridPad}px`);
    card.style.setProperty('--tag-color-grid-pad-top', `${gridPad}px`);
    card.style.setProperty('--tag-color-grid-pad-bottom', `${gridPadBottom}px`);
    card.style.setProperty('--tag-opacity-grid-pad', `${opacityPad}px`);
    card.style.setProperty('--tag-color-mode-height', `${modeHeight}px`);
    card.style.setProperty('--tag-opacity-height', `${opacityButtonHeight}px`);
    grid.style.height = `${computedGridHeight}px`;
    grid.style.minHeight = `${computedGridHeight}px`;
    grid.style.maxHeight = `${computedGridHeight}px`;
    if (opacityGrid){
      opacityGrid.style.height = `${opacityGridHeight}px`;
      opacityGrid.style.minHeight = `${opacityGridHeight}px`;
      opacityGrid.style.maxHeight = `${opacityGridHeight}px`;
    }
  }catch(_){ }
}

function __tagColorPopupCurrentPayload__(){
  return {
    mode: __tagColorPopupState__.mode,
    spec: __tagColorPopupSelectedSpec__(),
    opacity: __designBgOpacityNormalize__((__tagColorPopupState__.opacity ?? 0.80)),
    colors: { ...(__tagColorPopupState__.colors || {}) }
  };
}

function __tagColorPopupEmitPreview__(){
  try{
    const cb = __tagColorPopupState__.onPreview;
    if (typeof cb !== 'function') return;
    __tagColorPopupState__.previewDirty = true;
    cb(__tagColorPopupCurrentPayload__());
  }catch(_){ }
}

function __tagColorPopupOpen__(target, currentColor, onSelect, options){
  const modal = document.getElementById('tagColorModal');
  const grid = document.getElementById('tagColorGrid');
  const opts = (options && typeof options === 'object') ? options : {};
  if (!modal || !grid) return;
  __tagColorPopupState__.target = String(target || '').trim();
  __tagColorPopupState__.onSelect = typeof onSelect === 'function' ? onSelect : null;
  __tagColorPopupState__.onPreview = typeof opts.onPreview === 'function' ? opts.onPreview : null;
  __tagColorPopupState__.onRevert = typeof opts.onRevert === 'function' ? opts.onRevert : null;
  __tagColorPopupState__.previewDirty = false;
  __tagColorPopupState__.supportsBg = opts.supportsBg !== false;
  __tagColorPopupState__.supportsBorder = opts.supportsBorder !== false;
  __tagColorPopupState__.supportsFg = opts.supportsFg !== false;
  __tagColorPopupState__.supportsOpacity = opts.supportsOpacity !== false;
  const currentVisual = __tagColorPairFromValue__(currentColor, opts.fallbackBg || 'blue-4');
  __tagColorPopupState__.opacity = __designBgOpacityNormalize__(opts.opacity ?? currentColor?.opacity ?? currentVisual?.opacity ?? __designBgOpacityRead__());
  __tagColorPopupState__.colors = {
    ...currentVisual,
    border: __normalizeOptionalOperatoreColor__(currentColor?.border || currentColor?.borderColor || currentVisual?.border || currentVisual?.bg || opts.fallbackBg || 'blue-4') || currentVisual?.bg || opts.fallbackBg || 'blue-4'
  };
  __tagColorPopupState__.initial = {
    bg: __normalizeOperatoreColor__(__tagColorPopupState__.colors?.bg || opts.fallbackBg || 'blue-4'),
    border: __normalizeOperatoreColor__(__tagColorPopupState__.colors?.border || __tagColorPopupState__.colors?.bg || opts.fallbackBg || 'blue-4'),
    fg: __normalizeOptionalOperatoreColor__(__tagColorPopupState__.colors?.fg || ''),
    opacity: __designBgOpacityNormalize__(__tagColorPopupState__.opacity ?? 0.80)
  };
  __tagColorPopupState__.applyCategory = (opts.applyCategory && typeof opts.applyCategory === 'object') ? opts.applyCategory : null;
  const enabledModes = [];
  if (__tagColorPopupState__.supportsBg) enabledModes.push('bg');
  if (__tagColorPopupState__.supportsBorder) enabledModes.push('border');
  if (__tagColorPopupState__.supportsFg) enabledModes.push('fg');
  if (__tagColorPopupState__.supportsOpacity) enabledModes.push('opacity');
  const requestedMode = String(opts.defaultMode || (__tagColorPopupState__.supportsBg && __tagColorPopupState__.supportsFg ? __tagColorPopupLastDualMode__ : (enabledModes[0] || 'fg')) || 'fg').trim().toLowerCase();
  __tagColorPopupState__.mode = enabledModes.includes(requestedMode) ? requestedMode : (enabledModes[0] || 'fg');
  __tagColorPopupState__.confirmed = false;
  __tagColorPopupRefreshSelection__();
  requestAnimationFrame(() => { try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ } });
  __tagColorPopupReadyAt__ = 0;
  __tagColorPopupSuppressUntil__ = 0;
  try{ if (modal.parentNode !== document.body) document.body.appendChild(modal); }catch(_){ }
  modal.hidden = false;
  modal.classList.add('is-open');
  modal.setAttribute('aria-hidden', 'false');
  try{ document.body.classList.add('tag-color-modal-open'); }catch(_){ }
  try{ document.body.classList.add('modal-open'); }catch(_){ }
}



async function __tagColorPopupConfirm__(){
  const cb = __tagColorPopupState__.onSelect;
  const applyCategoryCfg = (__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? { ...__tagColorPopupState__.applyCategory } : null;
  const payload = __tagColorPopupCurrentPayload__();
  __tagColorPopupState__.confirmed = true;
  __tagColorPopupClose__();
  try{
    if (typeof cb === 'function') await cb(payload);
  }catch(_){ }
  try{ await __tagColorPopupApplyCategoryIfNeeded__(payload, applyCategoryCfg); }catch(_){ }
  try{ toast('Colore aggiornato'); }catch(_){ }
}

function __tagColorPopupClose__(){
  const modal = document.getElementById('tagColorModal');
  if (!modal) return;
  const shouldRevert = !__tagColorPopupState__.confirmed && __tagColorPopupState__.previewDirty;
  const revertCb = __tagColorPopupState__.onRevert;
  const revertPayload = {
    mode: 'bg',
    spec: __normalizeOperatoreColor__(__tagColorPopupState__.initial?.bg || 'blue-4'),
    opacity: __designBgOpacityNormalize__(__tagColorPopupState__.initial?.opacity ?? 0.80),
    colors: {
      bg: __normalizeOperatoreColor__(__tagColorPopupState__.initial?.bg || 'blue-4'),
      border: __normalizeOperatoreColor__(__tagColorPopupState__.initial?.border || __tagColorPopupState__.initial?.bg || 'blue-4'),
      fg: __normalizeOptionalOperatoreColor__(__tagColorPopupState__.initial?.fg || '')
    }
  };
  modal.classList.remove('is-open');
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
  try{ document.body.classList.remove('tag-color-modal-open'); }catch(_){ }
  try{
    const hasOtherOpenModals = !!document.querySelector('.modal.is-open:not([hidden]), .modal:not([hidden])[aria-hidden="false"]');
    if (!hasOtherOpenModals) document.body.classList.remove('modal-open');
  }catch(_){ }
  if (__tagColorPopupState__.supportsBg && __tagColorPopupState__.supportsFg) {
    __tagColorPopupLastDualMode__ = __tagColorPopupState__.mode === 'bg' ? 'bg' : 'fg';
  }
  if (shouldRevert && typeof revertCb === 'function') {
    try{ revertCb(revertPayload); }catch(_){ }
  }
  __tagColorPopupState__.target = '';
  __tagColorPopupState__.onSelect = null;
  __tagColorPopupState__.onPreview = null;
  __tagColorPopupState__.onRevert = null;
  __tagColorPopupState__.previewDirty = false;
  __tagColorPopupState__.mode = 'fg';
  __tagColorPopupState__.confirmed = false;
  __tagColorPopupState__.supportsBg = false;
  __tagColorPopupState__.supportsBorder = false;
  __tagColorPopupState__.supportsFg = true;
  __tagColorPopupState__.supportsOpacity = false;
  __tagColorPopupState__.opacity = 0.80;
  __tagColorPopupState__.colors = { bg:'blue-4', border:'', fg:'' };
  __tagColorPopupReadyAt__ = 0;
  __tagColorPopupSuppressUntil__ = Date.now() + 900;
}

function __openTagColorPickerFor__(target){
  const key = String(target || '').trim().toLowerCase();
  if (key === 'operatore'){
    __tagColorPopupOpen__('operatore', { bg: __operatoriPageUi.color || 'blue-3', fg: __operatoriPageUi.textColor || '' }, (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      __operatoriSetSelectedColor__(colors.bg || __operatoriPageUi.color || 'blue-3');
      __operatoriSetSelectedTextColor__(colors.fg || '');
    }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:0.80, defaultMode:'bg', fallbackBg:'blue-3' });
    return;
  }
  if (key === 'channel'){
    __tagColorPopupOpen__('channel', { bg: __channelPageUi.color || 'orange-3', fg: __channelPageUi.textColor || '' }, (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      __channelSetSelectedColor__(colors.bg || __channelPageUi.color || 'orange-3');
      __channelSetSelectedTextColor__(colors.fg || '');
    }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:0.80, defaultMode:'bg', fallbackBg:'orange-3' });
    return;
  }
  if (key === 'lavanderia'){
    __tagColorPopupOpen__('lavanderia', { bg: __laundryCatalogPageUi.color || 'blue-3', fg: __laundryCatalogPageUi.textColor || '' }, (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      __laundryCatalogSetSelectedColor__(colors.bg || __laundryCatalogPageUi.color || 'blue-3');
      __laundryCatalogSetSelectedTextColor__(colors.fg || '');
    }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:0.80, defaultMode:'bg', fallbackBg:'blue-3' });
  }
}



/* dDAE_2.855 — Popup colore ripristinato: helper non invasivi per i popup impostazioni */
function __suppressSettingsEditorColorPopups__(ms){ return; }
function __settingsEditorColorPopupIsSuppressed__(){ return false; }
function __settingsEditorModalRecentlyOpened__(ms){ return false; }
function __markSettingsEditorModalOpened__(){ return; }
function __settingsEditorModalIsOpen__(){
  try{
    return ['operatoriEditorModal','channelEditorModal','laundryCatalogEditorModal'].some((id) => {
      const el = document.getElementById(id);
      return !!(el && !el.hidden && el.getAttribute('aria-hidden') !== 'true');
    });
  }catch(_){ return false; }
}
function __normalizeSettingsActionOrder__(){ return; }
function __refreshDesignUiImmediately__(){
  try{ __launcherIconApplyAll__(); }catch(_){ }
  try{ __headerActionApplyAll__(); }catch(_){ }
  try{ __pillApplyAll__(); }catch(_){ }
  try{ __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
}
function __applySettingsEditorButtonBoldAndVisuals__(){
  try{
    [
      'operatoriEditorSave','operatoriEditorCancel','operatoriEditorDelete','operatoriEditorSaldoBtn','operatoriEditorTagColor','operatoriEditorDotColor',
      'channelEditorSave','channelEditorCancel','channelEditorDelete','channelEditorGraphColor','channelEditorDotColor',
      'laundryCatalogEditorSave','laundryCatalogEditorCancel','laundryCatalogEditorDelete','laundryCatalogEditorTagColor','laundryCatalogEditorDotColor'
    ].forEach((id)=>{
      const btn = document.getElementById(id);
      if (!btn) return;
      try{ __applySingleActionButtonVisual__(btn); }catch(_){ }
    });
  }catch(_){ }
}
function __operatoriOpenModal__(item){
  const modal = document.getElementById('operatoriEditorModal');
  if (!modal) return;
  const current = item || null;
  __operatoriPageUi.editingId = current?.id ? String(current.id) : '';
  const title = document.getElementById('operatoriEditorTitle');
  const idEl = document.getElementById('operatoriEditorId');
  const nomeEl = document.getElementById('operatoriEditorNome');
  const tariffaEl = document.getElementById('operatoriEditorTariffa');
  const benzinaEl = document.getElementById('operatoriEditorBenzina');
  const delBtn = document.getElementById('operatoriEditorDelete');
  const saldoBtn = document.getElementById('operatoriEditorSaldoBtn');
  if (title) title.textContent = current ? 'Modifica operatore' : 'Nuovo operatore';
  if (idEl) idEl.value = current?.id ? String(current.id) : '';
  if (nomeEl) nomeEl.value = current?.nome ? String(current.nome) : '';
  if (tariffaEl) tariffaEl.value = current && isFinite(Number(current.tariffa)) ? String(Number(current.tariffa)) : '';
  if (benzinaEl) benzinaEl.value = current && isFinite(Number(current.benzina)) ? String(Number(current.benzina)) : '';
  if (!__operatoriPageUi.tones || !Object.keys(__operatoriPageUi.tones).length) __initColorToneMap__(__operatoriPageUi, 'blue');
  if (delBtn) delBtn.hidden = !current;
  if (saldoBtn){
    const on = !!(current && (current.mostraSaldo || current.showBalance || current.saldoCalendario));
    saldoBtn.classList.toggle('is-selected', on);
    saldoBtn.setAttribute('aria-pressed', on ? 'true' : 'false');
    try{ __applySingleActionButtonVisual__(saldoBtn, on ? 'on' : 'off'); }catch(_){}
  }
  __operatoriPageUi.textColor = __normalizeOptionalOperatoreColor__(current?.coloreTesto);
  __operatoriSetSelectedColor__(current?.colore || 'blue-2');
  __operatoriSetSelectedTextColor__(__operatoriPageUi.textColor || '');
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  try{ __markSettingsEditorModalOpened__(); }catch(_){ }
  try{ refreshFloatingLabels(); }catch(_){ }
  try{ __syncSettingsConfigModalGrid__(); }catch(_){ }
  try{ requestAnimationFrame(() => { try{ __syncSettingsConfigModalGrid__(); }catch(_){} }); }catch(_){ }
  try{ __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
}


function __operatoriCloseModal__(){
  const modal = document.getElementById('operatoriEditorModal');
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
  const ids = ['operatoriEditorId','operatoriEditorNome','operatoriEditorTariffa','operatoriEditorBenzina'];
  ids.forEach(id => {
    const el = document.getElementById(id);
    if (el) el.value = '';
  });
  __operatoriPageUi.editingId = '';
  __operatoriPageUi.textColor = '';
  __initColorToneMap__(__operatoriPageUi, 'blue');
  __operatoriSetSelectedColor__('blue-2');
  __operatoriSetSelectedTextColor__('');
  try{ const b=document.getElementById('operatoriEditorSaldoBtn'); if(b){ b.classList.remove('is-selected'); b.setAttribute('aria-pressed','false'); __applySingleActionButtonVisual__(b,'off'); } }catch(_){}
}

async function renderOperatoriPage(){
  await ensureSettingsLoaded({ force:false, showLoader:false });
  const listEl = document.getElementById('operatoriList');
  const emptyEl = document.getElementById('operatoriEmpty');
  if (!listEl) return;
  const items = getOperatoriCatalogFromSettings();
  if (!items.length){
    listEl.innerHTML = '';
    if (emptyEl) emptyEl.hidden = false;
    return;
  }
  if (emptyEl) emptyEl.hidden = true;
  listEl.innerHTML = items.map((item) => `
    <article class="operatori-item" data-id="${item.id}">
      <div class="operatori-item-top">
        <div class="operatori-item-left">
          <span class="operatori-tag color-${item.colore}" style="${__laundryEscapeAttr__(__tagColorInlineStyle__(item.colore || 'blue-4', item.coloreTesto || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }))}"></span>
          <div class="operatori-name">${String(item.nome || '').replace(/[&<>"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]))}</div>
        </div>
        <div class="operatori-item-actions">
          <button aria-label="Modifica operatore" class="operatori-mini-btn" data-action="edit" type="button"><svg aria-hidden="true" class="ui-ico" viewbox="0 0 24 24"><path d="M12 20h9"></path><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z"></path></svg></button>
          <button aria-label="Elimina operatore" class="operatori-mini-btn is-delete" data-action="delete" type="button"><svg aria-hidden="true" class="ui-ico" viewbox="0 0 24 24"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M6 6l1 16h10l1-16"></path><path d="M10 11v6M14 11v6"></path></svg></button>
        </div>
      </div>
      <div class="operatori-metrics">
        <div class="operatori-metric">
          <div class="operatori-metric-label">Tariffa</div>
          <div class="operatori-metric-value">${__operatoriFormatMoney__(item.tariffa)}</div>
        </div>
        <div class="operatori-metric">
          <div class="operatori-metric-label">Benzina</div>
          <div class="operatori-metric-value">${__operatoriFormatMoney__(item.benzina)}</div>
        </div>
        <div class="operatori-metric">
          <div class="operatori-metric-label">Saldo calendario</div>
          <div class="operatori-metric-value">${item.mostraSaldo ? 'ON' : 'OFF'}</div>
        </div>
      </div>
    </article>
  `).join('');
}

async function loadOperatoriPage(){
  __initColorToneMap__(__operatoriPageUi, 'blue');
  await renderOperatoriPage();
}

function setupOperatoriPage(){
  const goBtn = document.getElementById('settingsOperatoriBtn');
  if (goBtn) bindFastTap(goBtn, () => { hideLauncher(); showPage('operatori'); });

  const addBtn = document.getElementById('btnAddOperatoreCard');
  if (addBtn){
    try{ ['pointerdown','touchstart','mousedown'].forEach((evt)=>addBtn.addEventListener(evt, ()=>{ try{ __suppressSettingsEditorColorPopups__(3200); }catch(_){} }, { passive:true, capture:true })); }catch(_){ }
    bindFastTap(addBtn, () => { try{ __suppressSettingsEditorColorPopups__(3200); }catch(_){} __operatoriOpenModal__(null); });
  }

  const closeBtn = document.getElementById('operatoriEditorClose');
  if (closeBtn) bindFastTap(closeBtn, __operatoriCloseModal__);
  const cancelBtn = document.getElementById('operatoriEditorCancel');
  if (cancelBtn) bindFastTap(cancelBtn, __operatoriCloseModal__);
  const colorBtn = document.getElementById('operatoriEditorTagColor');
  if (colorBtn) bindFastTap(colorBtn, () => { __openTagColorPickerFor__('operatore'); });
  const dotColorBtn = document.getElementById('operatoriEditorDotColor');
  if (dotColorBtn) bindFastTap(dotColorBtn, () => { __openSettingsDotColorPickerFor__('operatore'); });
  const saldoToggleBtn = document.getElementById('operatoriEditorSaldoBtn');
  if (saldoToggleBtn) bindFastTap(saldoToggleBtn, () => {
    const on = !saldoToggleBtn.classList.contains('is-selected');
    saldoToggleBtn.classList.toggle('is-selected', on);
    saldoToggleBtn.setAttribute('aria-pressed', on ? 'true' : 'false');
    try{ __applySingleActionButtonVisual__(saldoToggleBtn, on ? 'on' : 'off'); }catch(_){}
  });

  try{
    document.querySelectorAll('#operatoriColorGrid .operatori-color-option').forEach(btn => {
      bindFastTap(btn, () => {
        const base = String(btn.dataset.color || 'blue').trim().toLowerCase();
        const current = __parseOperatoreColorSpec__(__operatoriPageUi.color || 'blue-2');
        if (!__operatoriPageUi.tones || typeof __operatoriPageUi.tones !== 'object') __operatoriPageUi.tones = {};
        if (!__operatoriPageUi.tones[base]) __operatoriPageUi.tones[base] = __OPERATORI_COLOR_DEFAULT_SHADE__;
        if (current.base === base) {
          __operatoriPageUi.tones[base] = (__operatoriPageUi.tones[base] % __OPERATORI_COLOR_SHADE_COUNT__) + 1;
        }
        __operatoriSetSelectedColor__(`${base}-${__operatoriPageUi.tones[base]}`);
      });
    });
  }catch(_){ }

  const saveBtn = document.getElementById('operatoriEditorSave');
  if (saveBtn) bindFastTap(saveBtn, async () => {
    try{
      const nome = String(document.getElementById('operatoriEditorNome')?.value || '').trim();
      if (!nome){ toast('Inserisci il nome operatore'); return; }
      const tariffaRaw = String(document.getElementById('operatoriEditorTariffa')?.value || '').trim().replace(',', '.');
      const benzinaRaw = String(document.getElementById('operatoriEditorBenzina')?.value || '').trim().replace(',', '.');
      const tariffa = tariffaRaw ? Number(tariffaRaw) : 0;
      const benzina = benzinaRaw ? Number(benzinaRaw) : 0;
      if (!isFinite(tariffa) || tariffa < 0){ toast('Tariffa non valida'); return; }
      if (!isFinite(benzina) || benzina < 0){ toast('Benzina non valida'); return; }
      const id = String(document.getElementById('operatoriEditorId')?.value || '').trim();
      const list = getOperatoriCatalogFromSettings();
      const nextItem = {
        id: id || `op-${Date.now()}`,
        nome,
        tariffa: Math.round(tariffa * 100) / 100,
        benzina: Math.round(benzina * 100) / 100,
        colore: __operatoriPageUi.color || 'blue',
        coloreTesto: __operatoriPageUi.textColor || '',
        mostraSaldo: !!document.getElementById('operatoriEditorSaldoBtn')?.classList.contains('is-selected'),
      };
      const idx = list.findIndex(item => String(item.id) === nextItem.id);
      if (idx >= 0) list[idx] = nextItem;
      else list.push(nextItem);
      await saveOperatoriCatalogToSettings(list);
      __operatoriCloseModal__();
      await renderOperatoriPage();
      toast('Operatore salvato');
    }catch(e){
      toast(e?.message || 'Errore');
    }
  });

  const deleteBtn = document.getElementById('operatoriEditorDelete');
  if (deleteBtn) bindFastTap(deleteBtn, async () => {
    try{
      const id = String(document.getElementById('operatoriEditorId')?.value || '').trim();
      if (!id) return;
      const ok = confirm('Eliminare questo operatore?');
      if (!ok) return;
      const list = getOperatoriCatalogFromSettings().filter(item => String(item.id) !== id);
      await saveOperatoriCatalogToSettings(list);
      __operatoriCloseModal__();
      await renderOperatoriPage();
      toast('Operatore eliminato');
    }catch(e){
      toast(e?.message || 'Errore');
    }
  });

  const listEl = document.getElementById('operatoriList');
  if (listEl) listEl.addEventListener('click', async (ev) => {
    const btn = ev.target.closest?.('button[data-action]');
    const card = ev.target.closest?.('.operatori-item');
    if (!card) return;
    const id = String(card.getAttribute('data-id') || '').trim();
    const item = getOperatoriCatalogFromSettings().find(row => String(row.id) === id);
    if (!item) return;
    const action = btn ? String(btn.getAttribute('data-action') || '') : 'edit';
    if (action === 'delete'){
      const ok = confirm('Eliminare questo operatore?');
      if (!ok) return;
      const list = getOperatoriCatalogFromSettings().filter(row => String(row.id) !== id);
      await saveOperatoriCatalogToSettings(list);
      await renderOperatoriPage();
      toast('Operatore eliminato');
      return;
    }
    __operatoriOpenModal__(item);
  });
}


const __channelPageUi = {
  color: "orange-2",
  textColor: "",
  graphColor: "",
  editingId: "",
  tones: {},
};

function __channelFormatPct__(value){
  const n = Number(value || 0);
  const safe = isFinite(n) ? n : 0;
  return `${safe.toFixed(2)}%`;
}

function __channelSetSelectedColor__(color){
  const parsed = __parseOperatoreColorSpec__(color || 'orange-2');
  __channelPageUi.color = parsed.spec;
  __channelPageUi.graphColor = parsed.spec;
  __channelPageUi.graphColor = parsed.spec;
  if (!__channelPageUi.tones || typeof __channelPageUi.tones !== 'object') __channelPageUi.tones = {};
  __OPERATORI_COLOR_KEYS__.forEach((base) => {
    if (!__channelPageUi.tones[base]) __channelPageUi.tones[base] = __OPERATORI_COLOR_DEFAULT_SHADE__;
  });
  __channelPageUi.tones[parsed.base] = parsed.shade;
  __updateColorButtonGrid__('#channelColorGrid', __channelPageUi);
  __setTagPreviewButtonStyle__('channelEditorGraphColor', __channelPageUi.color || 'orange-2', '', false);
  __setTagPreviewButtonStyle__('channelEditorDotColor', __channelPageUi.color || 'orange-2', __channelPageUi.textColor || '', false);
}

function __channelSetSelectedTextColor__(color){
  __channelPageUi.textColor = __normalizeOptionalOperatoreColor__(color);
  __setTagPreviewButtonStyle__('channelEditorGraphColor', __channelPageUi.color || 'orange-2', '', false);
  __setTagPreviewButtonStyle__('channelEditorDotColor', __channelPageUi.color || 'orange-2', __channelPageUi.textColor || '', false);
}

function __channelSetSelectedGraphColor__(color){
  __channelPageUi.graphColor = __normalizeChannelGraphColor__(color);
  __setTagPreviewButtonStyle__('channelEditorGraphColor', __channelPageUi.color || 'orange-2', '', false);
  __setTagPreviewButtonStyle__('channelEditorDotColor', __channelPageUi.color || 'orange-2', __channelPageUi.textColor || '', false);
}

function __channelOpenModal__(item){
  const modal = document.getElementById('channelEditorModal');
  if (!modal) return;
  const current = item || null;
  __channelPageUi.editingId = current?.id ? String(current.id) : '';
  const title = document.getElementById('channelEditorTitle');
  const idEl = document.getElementById('channelEditorId');
  const nomeEl = document.getElementById('channelEditorNome');
  const commEl = document.getElementById('channelEditorCommission');
  const iniEl = document.getElementById('channelEditorInitial');
  const delBtn = document.getElementById('channelEditorDelete');
  if (title) title.textContent = current ? 'Modifica channel' : 'Nuovo channel';
  if (idEl) idEl.value = current?.id ? String(current.id) : '';
  if (nomeEl) nomeEl.value = current?.nome ? String(current.nome) : '';
  if (commEl) commEl.value = current && isFinite(Number(current.commissione)) ? String(Number(current.commissione)) : '';
  if (iniEl) iniEl.value = current?.iniziale ? String(current.iniziale).slice(0,1).toUpperCase() : '';
  if (!__channelPageUi.tones || !Object.keys(__channelPageUi.tones).length) __initColorToneMap__(__channelPageUi, 'orange');
  if (delBtn) delBtn.hidden = !current;
  __channelPageUi.textColor = __normalizeOptionalOperatoreColor__(current?.coloreTesto);
  __channelPageUi.graphColor = __normalizeChannelGraphColor__(current?.colore || current?.coloreGrafico);
  __channelSetSelectedColor__(current?.colore || 'orange-2');
  __channelSetSelectedTextColor__(__channelPageUi.textColor || '');
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  try{ __markSettingsEditorModalOpened__(); }catch(_){ }
  try{ refreshFloatingLabels(); }catch(_){ }
  try{ __syncSettingsConfigModalGrid__(); }catch(_){ }
  try{ requestAnimationFrame(() => { try{ __syncSettingsConfigModalGrid__(); }catch(_){} }); }catch(_){ }
  try{ __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
}


function __channelCloseModal__(){
  const modal = document.getElementById('channelEditorModal');
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
  ['channelEditorId','channelEditorNome','channelEditorCommission','channelEditorInitial'].forEach(id => {
    const el = document.getElementById(id);
    if (el) el.value = '';
  });
  __channelPageUi.editingId = '';
  __channelPageUi.textColor = '';
  __channelPageUi.graphColor = '';
  __initColorToneMap__(__channelPageUi, 'orange');
  __channelSetSelectedColor__('orange-2');
  __channelSetSelectedTextColor__('');
  __channelSetSelectedGraphColor__('');
}

async function renderChannelPage(){
  await ensureSettingsLoaded({ force:false, showLoader:false });
  const listEl = document.getElementById('channelList');
  const emptyEl = document.getElementById('channelEmpty');
  if (!listEl) return;
  const items = getChannelCatalogFromSettings();
  if (!items.length){
    listEl.innerHTML = '';
    if (emptyEl) emptyEl.hidden = false;
    return;
  }
  if (emptyEl) emptyEl.hidden = true;
  listEl.innerHTML = items.map((item) => `
    <article class="operatori-item channel-item" data-id="${item.id}">
      <div class="operatori-item-top">
        <div class="operatori-item-left">
          <span class="operatori-tag color-${item.colore}" style="${__laundryEscapeAttr__(__tagColorInlineStyle__(item.colore || 'orange-4', item.coloreTesto || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }))}"><span class="channel-tag-letter">${String(item.iniziale || __channelInitialFromName__(item.nome)).slice(0,1).toUpperCase()}</span></span>
          <div class="operatori-name">${String(item.nome || '').replace(/[&<>"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]||s))}</div>
          <span class="channel-graph-dot" style="background:${__laundryEscapeAttr__(__graphColorValueToHex__(item.coloreGrafico || item.coloreTesto || item.colore || 'orange-4', '#2b7cb4'))}"></span>
        </div>
        <div class="operatori-item-actions">
          <button aria-label="Modifica channel" class="operatori-mini-btn" data-action="edit" type="button"><svg aria-hidden="true" class="ui-ico" viewbox="0 0 24 24"><path d="M12 20h9"></path><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z"></path></svg></button>
          <button aria-label="Elimina channel" class="operatori-mini-btn is-delete" data-action="delete" type="button"><svg aria-hidden="true" class="ui-ico" viewbox="0 0 24 24"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M6 6l1 16h10l1-16"></path><path d="M10 11v6M14 11v6"></path></svg></button>
        </div>
      </div>
      <div class="operatori-metrics channel-metrics-single">
        <div class="operatori-metric">
          <div class="operatori-metric-label">Commissione</div>
          <div class="operatori-metric-value">${__channelFormatPct__(item.commissione)}</div>
        </div>
      </div>
    </article>
  `).join('');
}

async function loadChannelPage(){
  __initColorToneMap__(__channelPageUi, 'orange');
  await renderChannelPage();
}

function setupChannelPage(){
  const goBtn = document.getElementById('settingsChannelBtn');
  if (goBtn) bindFastTap(goBtn, () => { hideLauncher(); showPage('channel'); });
  const addBtn = document.getElementById('btnAddChannelCard');
  if (addBtn){
    try{ ['pointerdown','touchstart','mousedown'].forEach((evt)=>addBtn.addEventListener(evt, ()=>{ try{ __suppressSettingsEditorColorPopups__(3200); }catch(_){} }, { passive:true, capture:true })); }catch(_){ }
    bindFastTap(addBtn, () => { try{ __suppressSettingsEditorColorPopups__(3200); }catch(_){} __channelOpenModal__(null); });
  }
  const closeBtn = document.getElementById('channelEditorClose');
  if (closeBtn) bindFastTap(closeBtn, __channelCloseModal__);
  const cancelBtn = document.getElementById('channelEditorCancel');
  if (cancelBtn) bindFastTap(cancelBtn, __channelCloseModal__);
  const graphColorBtn = document.getElementById('channelEditorGraphColor');
  if (graphColorBtn) bindFastTap(graphColorBtn, () => {
    __openTagColorPickerFor__('channel');
  });
  const dotColorBtn = document.getElementById('channelEditorDotColor');
  if (dotColorBtn) bindFastTap(dotColorBtn, () => { __openSettingsDotColorPickerFor__('channel'); });
  try{
    document.querySelectorAll('#channelColorGrid .operatori-color-option').forEach(btn => {
      bindFastTap(btn, () => {
        const base = String(btn.dataset.color || 'orange').trim().toLowerCase();
        const current = __parseOperatoreColorSpec__(__channelPageUi.color || 'orange-2');
        if (!__channelPageUi.tones || typeof __channelPageUi.tones !== 'object') __channelPageUi.tones = {};
        if (!__channelPageUi.tones[base]) __channelPageUi.tones[base] = __OPERATORI_COLOR_DEFAULT_SHADE__;
        if (current.base === base) {
          __channelPageUi.tones[base] = (__channelPageUi.tones[base] % __OPERATORI_COLOR_SHADE_COUNT__) + 1;
        }
        __channelSetSelectedColor__(`${base}-${__channelPageUi.tones[base]}`);
      });
    });
  }catch(_){ }
  const saveBtn = document.getElementById('channelEditorSave');
  if (saveBtn) bindFastTap(saveBtn, async () => {
    try{
      const nome = String(document.getElementById('channelEditorNome')?.value || '').trim();
      if (!nome){ toast('Inserisci il nome channel'); return; }
      const commissionRaw = String(document.getElementById('channelEditorCommission')?.value || '').trim().replace(',', '.');
      const commissione = commissionRaw ? Number(commissionRaw) : 0;
      if (!isFinite(commissione)){ toast('Commissione non valida'); return; }
      const id = String(document.getElementById('channelEditorId')?.value || '').trim();
      const initialRaw = String(document.getElementById('channelEditorInitial')?.value || '').trim();
      const list = getChannelCatalogFromSettings();
      const nextItem = {
        id: id || `ch-${Date.now()}`,
        nome,
        commissione: Math.round(commissione * 100) / 100,
        iniziale: (initialRaw || __channelInitialFromName__(nome)).slice(0,1).toUpperCase(),
        colore: __channelPageUi.color || 'orange',
        coloreTesto: __channelPageUi.textColor || '',
        coloreGrafico: __channelPageUi.graphColor || __channelPageUi.color || 'orange-2',
      };
      const idx = list.findIndex(item => String(item.id) === nextItem.id);
      if (idx >= 0) list[idx] = nextItem;
      else list.push(nextItem);
      await saveChannelCatalogToSettings(list);
      __channelCloseModal__();
      await renderChannelPage();
      toast('Channel salvato');
    }catch(e){ toast(e?.message || 'Errore'); }
  });
  const deleteBtn = document.getElementById('channelEditorDelete');
  if (deleteBtn) bindFastTap(deleteBtn, async () => {
    try{
      const id = String(document.getElementById('channelEditorId')?.value || '').trim();
      if (!id) return;
      if (!confirm('Eliminare questo channel?')) return;
      const list = getChannelCatalogFromSettings().filter(item => String(item.id) !== id);
      await saveChannelCatalogToSettings(list);
      __channelCloseModal__();
      await renderChannelPage();
      toast('Channel eliminato');
    }catch(e){ toast(e?.message || 'Errore'); }
  });
  const listEl = document.getElementById('channelList');
  if (listEl) listEl.addEventListener('click', async (ev) => {
    const btn = ev.target.closest?.('button[data-action]');
    const card = ev.target.closest?.('.channel-item');
    if (!card) return;
    const id = String(card.getAttribute('data-id') || '').trim();
    const item = getChannelCatalogFromSettings().find(row => String(row.id) === id);
    if (!item) return;
    const action = btn ? String(btn.getAttribute('data-action') || '') : 'edit';
    if (action === 'delete'){
      if (!confirm('Eliminare questo channel?')) return;
      const list = getChannelCatalogFromSettings().filter(row => String(row.id) !== id);
      await saveChannelCatalogToSettings(list);
      await renderChannelPage();
      toast('Channel eliminato');
      return;
    }
    __channelOpenModal__(item);
  });
}

function setupTagColorPopup(){
  const modal = document.getElementById('tagColorModal');
  if (!modal || modal.dataset.bound === '1') return;
  modal.dataset.bound = '1';
  const closeBtn = document.getElementById('tagColorModalClose');
  const confirmBtn = document.getElementById('tagColorModalConfirm');
  const card = modal.querySelector?.('.tag-color-modal-card');
  try{
    if (!window.__tagColorPopupResizeBound__){
      window.__tagColorPopupResizeBound__ = true;
      window.addEventListener('resize', () => { try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ } }, { passive:true });
    }
  }catch(_){ }
  if (closeBtn) bindFastTap(closeBtn, __tagColorPopupClose__);
  if (confirmBtn) bindFastTap(confirmBtn, __tagColorPopupConfirm__);
  if (card){
    ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt) => {
      try{ card.addEventListener(evt, (ev) => { try{ ev.stopPropagation(); }catch(_){} }, { passive:false }); }
      catch(_){ try{ card.addEventListener(evt, (ev) => { try{ ev.stopPropagation(); }catch(__){} }); }catch(__){} }
    });
  }
  ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt) => {
    try{ document.addEventListener(evt, __tagColorPopupSwallowGhostTap__, true); }catch(_){ }
  });
  try{
    modal.addEventListener('click', (ev) => {
      try{ if (ev.target === modal) { ev.preventDefault(); ev.stopPropagation(); } }catch(_){ }
    });
  }catch(_){ }
  try{
    document.querySelectorAll('#tagColorGrid .tag-color-option').forEach((btn) => {
      bindFastTap(btn, () => {
        if (Date.now() < __tagColorPopupReadyAt__) return;
        __tagColorPopupSuppressUntil__ = Date.now() + 900;
        try{ if (document.activeElement && document.activeElement.blur) document.activeElement.blur(); }catch(_){ }
        const spec = __parseOperatoreColorSpec__(btn.dataset.spec || 'blue-3').spec;
        if (__tagColorPopupState__.mode === 'bg') __tagColorPopupState__.colors.bg = spec;
        else if (__tagColorPopupState__.mode === 'border') __tagColorPopupState__.colors.border = spec;
        else __tagColorPopupState__.colors.fg = spec;
        __tagColorPopupRefreshSelection__();
        __tagColorPopupEmitPreview__();
      });
    });
    document.querySelectorAll('#tagColorModeBar .tag-color-mode-btn').forEach((btn) => {
      bindFastTap(btn, () => {
        const rawMode = String(btn.dataset.mode || '').trim().toLowerCase();
        const mode = rawMode === 'bg' ? 'bg' : (rawMode === 'border' ? 'border' : (rawMode === 'opacity' ? 'opacity' : (rawMode === 'bold' ? 'bold' : 'fg')));
        if (mode === 'bg' && !__tagColorPopupState__.supportsBg) return;
        if (mode === 'border' && !__tagColorPopupState__.supportsBorder) return;
        if (mode === 'fg' && !__tagColorPopupState__.supportsFg) return;
        if (mode === 'opacity' && !__tagColorPopupState__.supportsOpacity) return;
        if (mode === 'bold' && !__tagColorPopupState__.supportsBold) return;
        if (mode === 'bold'){
          __tagColorPopupState__.mode = 'bold';
          __tagColorPopupState__.bold = !__tagColorPopupState__.bold;
          __tagColorPopupRefreshSelection__();
          __tagColorPopupEmitPreview__();
          try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ }
          return;
        }
        __tagColorPopupState__.mode = mode;
        if (__tagColorPopupState__.supportsBg && __tagColorPopupState__.supportsFg) __tagColorPopupLastDualMode__ = mode;
        __tagColorPopupRefreshSelection__();
        try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ }
      });
    });
    document.querySelectorAll('#tagOpacityGrid .tag-opacity-option').forEach((btn) => {
      bindFastTap(btn, () => {
        if (Date.now() < __tagColorPopupReadyAt__) return;
        __tagColorPopupSuppressUntil__ = Date.now() + 900;
        try{ if (document.activeElement && document.activeElement.blur) document.activeElement.blur(); }catch(_){ }
        __tagColorPopupState__.opacity = __designBgOpacityNormalize__(parseFloat(btn.dataset.opacity || '0.80'));
        __tagColorPopupRefreshSelection__();
        __tagColorPopupEmitPreview__();
      });
    });
  }catch(_){ }
}

function __applyHomeIconGradients__(){
  try{
    document.querySelectorAll('#page-home .home-grid .home-main svg.ui-ico').forEach((svg) => {
      svg.querySelectorAll('defs').forEach((d) => d.remove());
      svg.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node) => {
        node.style.stroke = 'currentColor';
        node.style.fill = 'none';
      });
    });
  }catch(_){ }
}

const __laundryCatalogPageUi = {
  color: "blue-2",
  textColor: "",
  editingId: "",
  tones: {},
};

function __laundryCatalogSetSelectedColor__(color){
  const parsed = __parseOperatoreColorSpec__(color || 'blue-2');
  __laundryCatalogPageUi.color = parsed.spec;
  if (!__laundryCatalogPageUi.tones || typeof __laundryCatalogPageUi.tones !== 'object') __laundryCatalogPageUi.tones = {};
  __OPERATORI_COLOR_KEYS__.forEach((base) => {
    if (!__laundryCatalogPageUi.tones[base]) __laundryCatalogPageUi.tones[base] = __OPERATORI_COLOR_DEFAULT_SHADE__;
  });
  __laundryCatalogPageUi.tones[parsed.base] = parsed.shade;
  __updateColorButtonGrid__('#laundryCatalogColorGrid', __laundryCatalogPageUi);
  __setTagPreviewButtonStyle__('laundryCatalogEditorTagColor', __laundryCatalogPageUi.color || 'blue-2', __laundryCatalogPageUi.textColor || '');
  __setTagPreviewButtonStyle__('laundryCatalogEditorDotColor', __laundryCatalogPageUi.color || 'blue-2', __laundryCatalogPageUi.textColor || '');
}

function __laundryCatalogSetSelectedTextColor__(color){
  __laundryCatalogPageUi.textColor = __normalizeOptionalOperatoreColor__(color);
  __setTagPreviewButtonStyle__('laundryCatalogEditorTagColor', __laundryCatalogPageUi.color || 'blue-2', __laundryCatalogPageUi.textColor || '');
  __setTagPreviewButtonStyle__('laundryCatalogEditorDotColor', __laundryCatalogPageUi.color || 'blue-2', __laundryCatalogPageUi.textColor || '');
}

function __laundryCatalogOpenModal__(item){
  const modal = document.getElementById('laundryCatalogEditorModal');
  if (!modal) return;
  const current = item || null;
  __laundryCatalogPageUi.editingId = current?.id ? String(current.id) : '';
  const title = document.getElementById('laundryCatalogEditorTitle');
  const idEl = document.getElementById('laundryCatalogEditorId');
  const titleEl = document.getElementById('laundryCatalogEditorTitleInput');
  const codeEl = document.getElementById('laundryCatalogEditorCode');
  const priceEl = document.getElementById('laundryCatalogEditorPrice');
  const delBtn = document.getElementById('laundryCatalogEditorDelete');
  if (title) title.textContent = current ? 'Modifica componente lavanderia' : 'Nuovo componente lavanderia';
  if (idEl) idEl.value = current?.id ? String(current.id) : '';
  if (titleEl) titleEl.value = current?.titolo ? String(current.titolo) : ''; // editor keeps base title
  if (codeEl) codeEl.value = current?.abbreviazione ? String(current.abbreviazione) : '';
  if (priceEl) priceEl.value = current && isFinite(Number(current.prezzo)) ? String(Number(current.prezzo)) : '';
  if (!__laundryCatalogPageUi.tones || !Object.keys(__laundryCatalogPageUi.tones).length) __initColorToneMap__(__laundryCatalogPageUi, 'blue');
  if (delBtn) delBtn.hidden = !current;
  __laundryCatalogPageUi.textColor = __normalizeOptionalOperatoreColor__(current?.coloreTesto);
  __laundryCatalogSetSelectedColor__(current?.colore || 'blue-2');
  __laundryCatalogSetSelectedTextColor__(__laundryCatalogPageUi.textColor || '');
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  try{ __markSettingsEditorModalOpened__(); }catch(_){ }
  try{ refreshFloatingLabels(); }catch(_){ }
  try{ __syncSettingsConfigModalGrid__(); }catch(_){ }
  try{ requestAnimationFrame(() => { try{ __syncSettingsConfigModalGrid__(); }catch(_){} }); }catch(_){ }
}


function __laundryCatalogCloseModal__(){
  const modal = document.getElementById('laundryCatalogEditorModal');
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
  ['laundryCatalogEditorId','laundryCatalogEditorTitleInput','laundryCatalogEditorCode','laundryCatalogEditorPrice'].forEach((id) => {
    const el = document.getElementById(id);
    if (el) el.value = '';
  });
  __laundryCatalogPageUi.editingId = '';
  __laundryCatalogPageUi.textColor = '';
  __initColorToneMap__(__laundryCatalogPageUi, 'blue');
  __laundryCatalogSetSelectedColor__('blue-2');
  __laundryCatalogSetSelectedTextColor__('');
}

async function renderLaundryCatalogPage(){
  await ensureSettingsLoaded({ force:false, showLoader:false });
  const listEl = document.getElementById('laundryCatalogList');
  const emptyEl = document.getElementById('laundryCatalogEmpty');
  if (!listEl) return;
  const items = getLaundryCatalogFromSettings();
  if (!items.length){
    listEl.innerHTML = '';
    if (emptyEl) emptyEl.hidden = false;
    return;
  }
  if (emptyEl) emptyEl.hidden = true;
  listEl.innerHTML = items.map((item) => `
    <article class="operatori-item laundry-component-item" data-id="${item.id}">
      <div class="operatori-item-top">
        <div class="operatori-item-left">
          <span class="operatori-tag color-${item.colore}" style="${__laundryEscapeAttr__(__tagColorInlineStyle__(item.colore || 'blue-4', item.coloreTesto || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }))}"></span>
          <div class="operatori-name">${String(__laundryDisplayTitle__(item, item?.titolo || '') || '').replace(/[&<>"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]))}</div>
        </div>
        <div class="operatori-item-actions">
          <button aria-label="Modifica componente lavanderia" class="operatori-mini-btn" data-action="edit" type="button"><svg aria-hidden="true" class="ui-ico" viewbox="0 0 24 24"><path d="M12 20h9"></path><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z"></path></svg></button>
          <button aria-label="Elimina componente lavanderia" class="operatori-mini-btn is-delete" data-action="delete" type="button"><svg aria-hidden="true" class="ui-ico" viewbox="0 0 24 24"><path d="M3 6h18"></path><path d="M8 6V4h8v2"></path><path d="M6 6l1 16h10l1-16"></path><path d="M10 11v6M14 11v6"></path></svg></button>
        </div>
      </div>
      <div class="operatori-metrics">
        <div class="operatori-metric">
          <div class="operatori-metric-label">Abbreviazione</div>
          <div class="operatori-metric-value">${String(item.abbreviazione || '').replace(/[&<>"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]))}</div>
        </div>
        <div class="operatori-metric">
          <div class="operatori-metric-label">Prezzo pulizia</div>
          <div class="operatori-metric-value">${__laundryMoneyFmt__(item.prezzo)}</div>
        </div>
      </div>
    </article>
  `).join('');
}

async function loadLaundryCatalogPage(){
  __initColorToneMap__(__laundryCatalogPageUi, 'blue');
  await renderLaundryCatalogPage();
}

function setupLaundryCatalogPage(){
  const goBtn = document.getElementById('settingsLaundryCatalogBtn');
  if (goBtn) bindFastTap(goBtn, () => { hideLauncher(); showPage('laundrycatalog'); });
  const addBtn = document.getElementById('btnAddLaundryComponentCard');
  if (addBtn){
    try{ ['pointerdown','touchstart','mousedown'].forEach((evt)=>addBtn.addEventListener(evt, ()=>{ try{ __suppressSettingsEditorColorPopups__(3200); }catch(_){} }, { passive:true, capture:true })); }catch(_){ }
    bindFastTap(addBtn, () => { try{ __suppressSettingsEditorColorPopups__(3200); }catch(_){} __laundryCatalogOpenModal__(null); });
  }
  const closeBtn = document.getElementById('laundryCatalogEditorClose');
  if (closeBtn) bindFastTap(closeBtn, __laundryCatalogCloseModal__);
  const cancelBtn = document.getElementById('laundryCatalogEditorCancel');
  if (cancelBtn) bindFastTap(cancelBtn, __laundryCatalogCloseModal__);
  const colorBtn = document.getElementById('laundryCatalogEditorTagColor');
  if (colorBtn) bindFastTap(colorBtn, () => { __openTagColorPickerFor__('lavanderia'); });
  const dotColorBtn = document.getElementById('laundryCatalogEditorDotColor');
  if (dotColorBtn) bindFastTap(dotColorBtn, () => { __openSettingsDotColorPickerFor__('lavanderia'); });
  try{
    document.querySelectorAll('#laundryCatalogColorGrid .operatori-color-option').forEach((btn) => {
      bindFastTap(btn, () => {
        const base = String(btn.dataset.color || 'blue').trim().toLowerCase();
        const current = __parseOperatoreColorSpec__(__laundryCatalogPageUi.color || 'blue-2');
        if (!__laundryCatalogPageUi.tones || typeof __laundryCatalogPageUi.tones !== 'object') __laundryCatalogPageUi.tones = {};
        if (!__laundryCatalogPageUi.tones[base]) __laundryCatalogPageUi.tones[base] = __OPERATORI_COLOR_DEFAULT_SHADE__;
        if (current.base === base) {
          __laundryCatalogPageUi.tones[base] = (__laundryCatalogPageUi.tones[base] % __OPERATORI_COLOR_SHADE_COUNT__) + 1;
        }
        __laundryCatalogSetSelectedColor__(`${base}-${__laundryCatalogPageUi.tones[base]}`);
      });
    });
  }catch(_){ }
  const saveBtn = document.getElementById('laundryCatalogEditorSave');
  if (saveBtn) bindFastTap(saveBtn, async () => {
    try{
      const titolo = String(document.getElementById('laundryCatalogEditorTitleInput')?.value || '').trim();
      const abbreviazione = __normalizeLaundryCode__(document.getElementById('laundryCatalogEditorCode')?.value || '');
      const priceRaw = String(document.getElementById('laundryCatalogEditorPrice')?.value || '').trim().replace(',', '.');
      const prezzo = priceRaw ? Number(priceRaw) : 0;
      if (!titolo){ toast('Inserisci il titolo componente'); return; }
      if (!abbreviazione){ toast("Inserisci l'abbreviazione"); return; }
      if (!isFinite(prezzo) || prezzo < 0){ toast('Prezzo non valido'); return; }
      const id = String(document.getElementById('laundryCatalogEditorId')?.value || '').trim();
      const list = getLaundryCatalogFromSettings();
      const duplicate = list.find((item) => String(item.id) !== id && String(item.abbreviazione || '').trim().toUpperCase() === abbreviazione);
      if (duplicate){ toast('Abbreviazione già presente'); return; }
      const nextItem = {
        id: id || `lc-${Date.now()}`,
        titolo,
        abbreviazione,
        prezzo: Math.round(prezzo * 100) / 100,
        colore: __laundryCatalogPageUi.color || 'blue',
        coloreTesto: __laundryCatalogPageUi.textColor || '',
      };
      const idx = list.findIndex((item) => String(item.id) === nextItem.id);
      if (idx >= 0) list[idx] = nextItem;
      else list.push(nextItem);
      await saveLaundryCatalogToSettings(list);
      __laundryCatalogCloseModal__();
      await renderLaundryCatalogPage();
      try{ window.__ddae_refreshPulizieGrid?.({ forceReload:true }); }catch(_){ }
      try{ if (state && state.page === 'lavanderia') await loadLavanderia(); }catch(_){ }
      toast('Componente lavanderia salvato');
    }catch(e){ toast(e?.message || 'Errore'); }
  });
  const deleteBtn = document.getElementById('laundryCatalogEditorDelete');
  if (deleteBtn) bindFastTap(deleteBtn, async () => {
    try{
      const id = String(document.getElementById('laundryCatalogEditorId')?.value || '').trim();
      if (!id) return;
      if (!confirm('Eliminare questo componente lavanderia?')) return;
      const list = getLaundryCatalogFromSettings().filter((item) => String(item.id) !== id);
      await saveLaundryCatalogToSettings(list);
      __laundryCatalogCloseModal__();
      await renderLaundryCatalogPage();
      try{ window.__ddae_refreshPulizieGrid?.({ forceReload:true }); }catch(_){ }
      try{ if (state && state.page === 'lavanderia') await loadLavanderia(); }catch(_){ }
      toast('Componente lavanderia eliminato');
    }catch(e){ toast(e?.message || 'Errore'); }
  });
  const listEl = document.getElementById('laundryCatalogList');
  if (listEl) listEl.addEventListener('click', async (ev) => {
    const btn = ev.target.closest?.('button[data-action]');
    const card = ev.target.closest?.('.laundry-component-item');
    if (!card) return;
    const id = String(card.getAttribute('data-id') || '').trim();
    const item = getLaundryCatalogFromSettings().find((row) => String(row.id) === id);
    if (!item) return;
    const action = btn ? String(btn.getAttribute('data-action') || '') : 'edit';
    if (action === 'delete'){
      if (!confirm('Eliminare questo componente lavanderia?')) return;
      const list = getLaundryCatalogFromSettings().filter((row) => String(row.id) !== id);
      await saveLaundryCatalogToSettings(list);
      await renderLaundryCatalogPage();
      try{ window.__ddae_refreshPulizieGrid?.({ forceReload:true }); }catch(_){ }
      try{ if (state && state.page === 'lavanderia') await loadLavanderia(); }catch(_){ }
      toast('Componente lavanderia eliminato');
      return;
    }
    __laundryCatalogOpenModal__(item);
  });
}

const __DARK_MODE_KEY__ = "dDAE_dark_mode";

function __isDarkModeEnabled__(){
  try{
    return localStorage.getItem(__DARK_MODE_KEY__) === "1";
  }catch(_){
    return false;
  }
}

function __updateThemeMeta__(isDark){
  try{
    const meta = document.querySelector('meta[name="theme-color"]');
    if (meta) meta.setAttribute('content', isDark ? '#0f172a' : '#ffffff');
  }catch(_){ }
}

function __syncDarkModeButtons__(){
  const enabled = __isDarkModeEnabled__();
  ["settingsSaveBtn"].forEach((id) => {
    const btn = document.getElementById(id);
    if (!btn) return;
    btn.setAttribute('aria-pressed', enabled ? 'true' : 'false');
    btn.classList.toggle('is-active', enabled);
    const label = btn.querySelector('.settings-btn-label');
    if (label) label.textContent = 'Dark Mode';
    btn.title = enabled ? 'Dark Mode attiva' : 'Dark Mode disattivata';
  });
  ["settingsDarkToggle","opSettingsDarkToggle"].forEach((id) => {
    const toggle = document.getElementById(id);
    if (!toggle) return;
    try{ toggle.checked = !!enabled; }catch(_){ }
    try{ toggle.setAttribute('aria-checked', enabled ? 'true' : 'false'); }catch(_){ }
    try{ toggle.title = enabled ? 'Dark Mode attiva' : 'Dark Mode disattivata'; }catch(_){ }
  });
}

function __applyDarkMode__(enabled){
  try{
    document.body.classList.toggle('ddae-dark', !!enabled);
    document.documentElement.classList.toggle('ddae-dark', !!enabled);
  }catch(_){ }
  try{ __updateThemeMeta__(!!enabled); }catch(_){ }
  try{ __syncDarkModeButtons__(); }catch(_){ }
  try{ __launcherIconApplyAll__(); }catch(_){ }
  try{ __headerActionApplyAll__(); }catch(_){ }
  try{ __pillApplyAll__(); }catch(_){ }
  try{ __dateRangeCalendarApplyTheme__(); }catch(_){ }
  try{ __applyGuestListCardAll__(); }catch(_){ }
}

function __setDarkMode__(enabled){
  try{ localStorage.setItem(__DARK_MODE_KEY__, enabled ? '1' : '0'); }catch(_){ }
  __applyDarkMode__(!!enabled);
}

function __toggleDarkMode__(){
  const next = !__isDarkModeEnabled__();
  __setDarkMode__(next);
  try{ toast(next ? 'Dark Mode attivata' : 'Dark Mode disattivata'); }catch(_){ }
}

function setupImpostazioni() {
  try{ setupLanguageModal(); }catch(_){ }
  const back = document.getElementById("settingsBackBtn");
  if (back) back.addEventListener("click", () => showPage("home"));

  const save = document.getElementById("settingsSaveBtn");
  if (save) bindFastTap(save, () => { __toggleDarkMode__(); });
  const settingsDarkToggle = document.getElementById("settingsDarkToggle");
  if (settingsDarkToggle && settingsDarkToggle.dataset.boundDarkToggle !== '1'){
    settingsDarkToggle.dataset.boundDarkToggle = '1';
    settingsDarkToggle.checked = __isDarkModeEnabled__();
    settingsDarkToggle.addEventListener('change', () => { __setDarkMode__(!!settingsDarkToggle.checked); try{ toast(settingsDarkToggle.checked ? 'Dark Mode attivata' : 'Dark Mode disattivata'); }catch(_){ } });
  }
  const opSettingsDarkToggle = document.getElementById("opSettingsDarkToggle");
  if (opSettingsDarkToggle && opSettingsDarkToggle.dataset.boundDarkToggle !== '1'){
    opSettingsDarkToggle.dataset.boundDarkToggle = '1';
    opSettingsDarkToggle.checked = __isDarkModeEnabled__();
    opSettingsDarkToggle.addEventListener('change', () => { __setDarkMode__(!!opSettingsDarkToggle.checked); try{ toast(opSettingsDarkToggle.checked ? 'Dark Mode attivata' : 'Dark Mode disattivata'); }catch(_){ } });
  }
  try{ __syncDarkModeButtons__(); }catch(_){ }

  const settingsYearPill = document.getElementById("settingsYearPill");
  if (settingsYearPill && !settingsYearPill.__boundYearTap){
    settingsYearPill.__boundYearTap = true;
    bindFastTap(settingsYearPill, () => { __openSettingsYearModal__(); });
    settingsYearPill.addEventListener("keydown", (e) => {
      if (e.key === "Enter" || e.key === " "){
        e.preventDefault();
        __openSettingsYearModal__();
      }
    });
  }


  const operatoriGo = document.getElementById("settingsOperatoriBtn");
  if (operatoriGo) bindFastTap(operatoriGo, () => { hideLauncher(); showPage("operatori"); });
  const laundryCatalogGo = document.getElementById("settingsLaundryCatalogBtn");
  if (laundryCatalogGo) bindFastTap(laundryCatalogGo, () => { hideLauncher(); showPage("laundrycatalog"); });
  const channelGo = document.getElementById("settingsChannelBtn");
  if (channelGo) bindFastTap(channelGo, () => { hideLauncher(); showPage("channel"); });
  const languageBtn = document.getElementById("settingsLanguageBtn");
  if (languageBtn) bindFastTap(languageBtn, () => { try{ __openLanguageModal__(); }catch(_){ } });
  const opLanguageBtn = document.getElementById("opSettingsLanguageBtn");
  if (opLanguageBtn) bindFastTap(opLanguageBtn, () => { try{ __openLanguageModal__(); }catch(_){ } });
  const opLangBtn = document.getElementById("opSettingsLanguageBtn");
  if (opLangBtn && !opLangBtn.__boundLangTap){ opLangBtn.__boundLangTap = true; bindFastTap(opLangBtn, () => { try{ __openLanguageModal__(); }catch(_){ } }); }
  const opCodeBtn = document.getElementById("opSettingsCodeBtn");
  if (opCodeBtn) bindFastTap(opCodeBtn, async () => {
    try{ if (__languageModalGhostTapActive__()) return; }catch(_){ }
    try{ await __qrScanAndLink__(); }catch(e){
      try{ toast(String((e && e.message) ? e.message : "Errore codice operatore"), "orange"); }catch(_){ }
    }
  });
  const opLogoutPageBtn = document.getElementById("opSettingsLogoutBtn");
  if (opLogoutPageBtn) bindFastTap(opLogoutPageBtn, async () => {
    try{ __pillLongPressSuppress__(opLogoutPageBtn, 1800); }catch(_){ }
    let ok = false;
    try{ ok = await confirmYesNo("Vuoi uscire?"); }catch(_){ ok = false; }
    if (!ok) return;
    try{ clearSession(); }catch(_){ }
    try{ state.session = null; }catch(_){ }
    try{ applyRoleMode(); }catch(_){ }
    try{ __resetInMemoryData__(); }catch(_){ }
    try{ invalidateApiCache(); }catch(_){ }
    try{ showPage("auth"); }catch(_){ }
  });


  // DB Import/Export (LOCAL) - nuovo accesso unico dal pulsante Database (icona verde)
  try{
    const dbBtn = document.getElementById("settingsDbBtn");
    if (dbBtn) bindFastTap(dbBtn, async () => { try{ if (__isAdmin__()) { __openSettingsBackupModal__(); } }catch(e){ try{ toast("Errore backup", "orange"); }catch(_){ } } });
    const rosterBtn = document.getElementById("settingsExportRosterBtn");
    if (rosterBtn) bindFastTap(rosterBtn, async () => {
      try{
        if (__isAdmin__()) await __adminGenerateCode__();
        else await __qrScanAndLink__();
      }catch(e){
        try{ toast(String((e && e.message) ? e.message : "Errore codice"), "orange"); }catch(_){ }
        try{ console.error("Roster/Firebase error:", e); }catch(_){ }
      }
    });
    const masterBtn = document.getElementById("settingsMasterBtn");
    if (masterBtn) bindFastTap(masterBtn, async () => {
      try{ await __openSettingsLicenseModalForSession__(); }catch(e){
        try{ toast(String((e && e.message) ? e.message : "Errore licenza"), "orange"); }catch(_){ }
      }
    });
    try{ __initLicenseGeneratorBindings__(); }catch(_){ }
    try{ __initSettingsLicenseBindings__(); }catch(_){ }

    // fallback (se presenti in DOM, ma di norma nascosti)
    const dbA = document.getElementById("dbAdminBtn");
    if (dbA) bindFastTap(dbA, () => { __openDbPopup__("admin"); });
    const dbO = document.getElementById("dbOperatorBtn");
    if (dbO) bindFastTap(dbO, () => { __openDbPopup__("operator"); });
  }catch(_){ }
const cfg = document.getElementById("settingsConfigBtn");
  if (cfg) bindFastTap(cfg, () => { __openSettingsConfigModal__(); });

  const roomsBtn = document.getElementById("settingsRoomsBtn");
  const langBtn = document.getElementById("settingsLanguageBtn");
  if (langBtn && !langBtn.__boundLangTap){
    langBtn.__boundLangTap = true;
    bindFastTap(langBtn, () => { try{ __openLanguageModal__(); }catch(_){ } });
  }
  if (roomsBtn && !roomsBtn.__boundRoomsTap){
    roomsBtn.__boundRoomsTap = true;
    bindFastTap(roomsBtn, () => {
      try{ hideLauncher(); }catch(_){ }
      try{ showPage("roomsettings"); }catch(_){ }
    });
  }

  const cfgClose = document.getElementById("settingsConfigClose");
  if (cfgClose) bindFastTap(cfgClose, __closeSettingsConfigModal__);
  const cfgCancel = document.getElementById("settingsConfigCancelBtn");
  if (cfgCancel) bindFastTap(cfgCancel, __closeSettingsConfigModal__);
  const cfgSave = document.getElementById("settingsConfigSaveBtn");
  if (cfgSave) bindFastTap(cfgSave, async () => {
    try { await saveImpostazioniPage(); __closeSettingsConfigModal__(); } catch (e) { toast(e.message || "Errore"); }
  });
  const cfgModal = document.getElementById("settingsConfigModal");
  if (!window.__boundStayGridResize){
    window.__boundStayGridResize = true;
    window.addEventListener("resize", () => { try{ __syncSettingsConfigModalGrid__(); }catch(_){} }, { passive:true });
  }
  if (cfgModal && !cfgModal.__boundClose){
    cfgModal.__boundClose = true;
    cfgModal.addEventListener("click", (e) => { if (e.target === cfgModal) __closeSettingsConfigModal__(); });
  }

  const yearModal = document.getElementById("settingsYearModal");
  if (yearModal && !yearModal.__boundClose){
    yearModal.__boundClose = true;
    yearModal.addEventListener("click", (e) => { if (e.target === yearModal) __closeSettingsYearModal__(); });
  }
  const yearWheel = document.getElementById("settingsYearWheel");
  if (yearWheel && !yearWheel.__boundWheel){
    yearWheel.__boundWheel = true;
    yearWheel.addEventListener("scroll", () => {
      try{ clearTimeout(__settingsYearWheelTimer__); }catch(_){ }
      const liveYear = __getSettingsYearWheelValue__();
      __highlightSettingsYearWheel__(liveYear);
      __settingsYearWheelTimer__ = setTimeout(() => { __applySettingsYearWheelSelection__({ snapOnly:true }); }, 140);
    }, { passive:true });
    yearWheel.addEventListener("click", (e) => {
      const item = e.target && e.target.closest ? e.target.closest('.settings-year-wheel-item') : null;
      if (!item) return;
      const year = item.getAttribute('data-year');
      __scrollSettingsYearWheelTo__(year, 'smooth');
      try{ clearTimeout(__settingsYearWheelTimer__); }catch(_){ }
      __settingsYearWheelTimer__ = setTimeout(() => { __applySettingsYearWheelSelection__({ snapOnly:false }); }, 150);
    });
    yearWheel.addEventListener("touchend", () => {
      try{ clearTimeout(__settingsYearWheelTimer__); }catch(_){ }
      __settingsYearWheelTimer__ = setTimeout(() => { __applySettingsYearWheelSelection__({ snapOnly:false }); }, 170);
    }, { passive:true });
    yearWheel.addEventListener("keydown", (e) => {
      if (e.key === "Escape"){
        e.preventDefault();
        __closeSettingsYearModal__();
        return;
      }
      if (e.key === "Enter"){
        e.preventDefault();
        __saveSettingsYearModal__();
        return;
      }
      if (e.key === "ArrowDown" || e.key === "ArrowUp"){
        e.preventDefault();
        const years = __getSettingsYearValues__();
        const current = __getSettingsYearWheelValue__();
        let index = years.indexOf(current);
        if (index < 0) index = 0;
        if (e.key === "ArrowDown") index = Math.min(years.length - 1, index + 1);
        else index = Math.max(0, index - 1);
        const year = years[index];
        __scrollSettingsYearWheelTo__(year, 'smooth');
        try{ clearTimeout(__settingsYearWheelTimer__); }catch(_){ }
        __settingsYearWheelTimer__ = setTimeout(() => { __applySettingsYearWheelSelection__({ snapOnly:false }); }, 150);
      }
    });
  }
  const yearCard = document.querySelector('#settingsYearModal .settings-year-wheel-card');
  if (yearCard && !yearCard.__boundContain){
    yearCard.__boundContain = true;
    ['click','touchstart','touchend','pointerdown','pointerup'].forEach((evt) => {
      yearCard.addEventListener(evt, (e) => { try{ e.stopPropagation(); }catch(_){ } }, { passive:false });
    });
  }
  const statGenCompareToggleBtn = document.getElementById('statGenCompareToggleBtn');
  if (statGenCompareToggleBtn && !statGenCompareToggleBtn.__boundAdvanced){
    statGenCompareToggleBtn.__boundAdvanced = true;
    let longPressTimer = null;
    let longPressFired = false;
    const clearLong = ()=>{ if (longPressTimer){ clearTimeout(longPressTimer); longPressTimer = null; } };
    const block = (e)=>{ try{ e && e.preventDefault && e.preventDefault(); }catch(_){ } try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){ } return false; };
    const startLong = (e)=>{
      try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
      longPressFired = false;
      clearLong();
      longPressTimer = setTimeout(()=>{
        longPressFired = true;
        try{ statGenCompareToggleBtn.classList.add('is-pressing'); }catch(_){ }
        __openStatGenCompareYearButtonColorPicker__();
      }, 500);
    };
    const stopLong = (e)=>{
      clearLong();
      if (longPressFired){
        block(e);
        setTimeout(()=>{ longPressFired = false; try{ statGenCompareToggleBtn.classList.remove('is-pressing'); }catch(_){ } }, 0);
        return;
      }
      try{ statGenCompareToggleBtn.classList.remove('is-pressing'); }catch(_){ }
    };
    statGenCompareToggleBtn.addEventListener('click', (e) => {
      if (longPressFired) return block(e);
      __toggleStatGenCompareEnabled__();
    });
    ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ statGenCompareToggleBtn.addEventListener(evt, startLong, { passive:true }); }catch(_){ } });
    ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ statGenCompareToggleBtn.addEventListener(evt, stopLong, { passive:false }); }catch(_){ } });
    try{ statGenCompareToggleBtn.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
  }
  const statGenCompareYearBtn = document.getElementById('statGenCompareYearBtn');
  if (statGenCompareYearBtn && !statGenCompareYearBtn.__boundOpen){
    statGenCompareYearBtn.__boundOpen = true;
    const openPicker = (e) => {
      try{ e && e.preventDefault && e.preventDefault(); }catch(_){ }
      try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){ }
      __openStatGenCompareYearPicker__();
    };
    statGenCompareYearBtn.addEventListener('click', openPicker);
    statGenCompareYearBtn.addEventListener('keydown', (e) => {
      if (!e) return;
      if (e.key === 'Enter' || e.key === ' '){
        openPicker(e);
      }
    });
  }

  const statMensiliCompareToggleBtn = document.getElementById('statMensiliCompareToggleBtn');
  if (statMensiliCompareToggleBtn && !statMensiliCompareToggleBtn.__boundAdvanced){
    statMensiliCompareToggleBtn.__boundAdvanced = true;
    statMensiliCompareToggleBtn.addEventListener('click', (e) => {
      try{ e && e.preventDefault && e.preventDefault(); }catch(_){ }
      try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){ }
      __toggleStatGenCompareEnabled__();
    });
    try{ statMensiliCompareToggleBtn.addEventListener('contextmenu', (e)=>{ try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } return false; }, true); }catch(_){ }
  }
  const statMensiliCompareYearBtn = document.getElementById('statMensiliCompareYearBtn');
  if (statMensiliCompareYearBtn && !statMensiliCompareYearBtn.__boundOpen){
    statMensiliCompareYearBtn.__boundOpen = true;
    const openMensiliPicker = (e) => {
      try{ e && e.preventDefault && e.preventDefault(); }catch(_){ }
      try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){ }
      __openStatGenCompareYearPicker__();
    };
    statMensiliCompareYearBtn.addEventListener('click', openMensiliPicker);
    statMensiliCompareYearBtn.addEventListener('keydown', (e) => {
      if (!e) return;
      if (e.key === 'Enter' || e.key === ' '){ openMensiliPicker(e); }
    });
  }
  const statGenCompareModal = document.getElementById('statGenCompareYearModal');
  if (statGenCompareModal && !statGenCompareModal.__boundClose){
    statGenCompareModal.__boundClose = true;
    statGenCompareModal.addEventListener('click', (e) => { if (e.target === statGenCompareModal) __closeStatGenCompareYearModal__(); });
  }
  const statGenCompareWheel = document.getElementById('statGenCompareYearWheel');
  if (statGenCompareWheel && !statGenCompareWheel.__boundWheel){
    statGenCompareWheel.__boundWheel = true;
    statGenCompareWheel.addEventListener('scroll', () => {
      try{ clearTimeout(__statGenCompareYearWheelTimer__); }catch(_){ }
      const liveYear = __getStatGenCompareYearWheelValue__();
      __highlightStatGenCompareYearWheel__(liveYear);
      __statGenCompareYearWheelTimer__ = setTimeout(() => { __applyStatGenCompareYearSelection__({ snapOnly:true }); }, 140);
    }, { passive:true });
    statGenCompareWheel.addEventListener('click', (e) => {
      const item = e.target && e.target.closest ? e.target.closest('.settings-year-wheel-item') : null;
      if (!item) return;
      const year = item.getAttribute('data-year');
      __scrollStatGenCompareYearWheelTo__(year, 'smooth');
      try{ clearTimeout(__statGenCompareYearWheelTimer__); }catch(_){ }
      __statGenCompareYearWheelTimer__ = setTimeout(() => { __applyStatGenCompareYearSelection__({ snapOnly:false }); }, 150);
    });
    statGenCompareWheel.addEventListener('touchend', () => {
      try{ clearTimeout(__statGenCompareYearWheelTimer__); }catch(_){ }
      __statGenCompareYearWheelTimer__ = setTimeout(() => { __applyStatGenCompareYearSelection__({ snapOnly:false }); }, 170);
    }, { passive:true });
    statGenCompareWheel.addEventListener('keydown', (e) => {
      if (e.key === 'Escape'){
        e.preventDefault();
        __closeStatGenCompareYearModal__();
        return;
      }
      if (e.key === 'Enter'){
        e.preventDefault();
        __saveStatGenCompareYearModal__();
        return;
      }
      if (e.key === 'ArrowDown' || e.key === 'ArrowUp'){
        e.preventDefault();
        const years = __getSettingsYearValues__();
        const current = __getStatGenCompareYearWheelValue__();
        let index = years.indexOf(current);
        if (index < 0) index = 0;
        if (e.key === 'ArrowDown') index = Math.min(years.length - 1, index + 1);
        else index = Math.max(0, index - 1);
        const year = years[index];
        __scrollStatGenCompareYearWheelTo__(year, 'smooth');
        try{ clearTimeout(__statGenCompareYearWheelTimer__); }catch(_){ }
        __statGenCompareYearWheelTimer__ = setTimeout(() => { __applyStatGenCompareYearSelection__({ snapOnly:false }); }, 150);
      }
    });
  }
  const statGenCompareCard = document.querySelector('#statGenCompareYearModal .settings-year-wheel-card');
  if (statGenCompareCard && !statGenCompareCard.__boundContain){
    statGenCompareCard.__boundContain = true;
    ['click','touchstart','touchend','pointerdown','pointerup'].forEach((evt) => {
      statGenCompareCard.addEventListener(evt, (e) => { try{ e.stopPropagation(); }catch(_){ } }, { passive:false });
    });
  }

  const backupClose = document.getElementById("settingsBackupClose");
  if (backupClose) bindFastTap(backupClose, __closeSettingsBackupModal__);
  const backupCancel = document.getElementById("settingsBackupCancel");
  if (backupCancel) bindFastTap(backupCancel, __closeSettingsBackupModal__);
  const backupImport = document.getElementById("settingsBackupImport");
  if (backupImport) bindFastTap(backupImport, async () => {
    try{
      __closeSettingsBackupModal__();
      await __dbImport__("admin");
    }catch(e){
      try{ toast("Errore backup", "orange"); }catch(_){ }
    }
  });
  const backupExport = document.getElementById("settingsBackupExport");
  if (backupExport) bindFastTap(backupExport, async () => {
    try{
      __closeSettingsBackupModal__();
      let w = null; try{ w = window.open("", "_blank"); }catch(_){ w = null; }
      await __dbExport__("admin", w);
    }catch(e){
      try{ toast("Errore backup", "orange"); }catch(_){ }
    }
  });
  const backupModal = document.getElementById("settingsBackupModal");
  if (backupModal && !backupModal.__boundClose){
    backupModal.__boundClose = true;
    const backupCard = backupModal.querySelector?.('.modal-card');
    if (backupCard){
      ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt) => {
        try{ backupCard.addEventListener(evt, (ev) => { try{ ev.stopPropagation(); }catch(_){} }, { passive:false }); }
        catch(_){ try{ backupCard.addEventListener(evt, (ev) => { try{ ev.stopPropagation(); }catch(__){} }); }catch(__){} }
      });
    }
    ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt) => {
      try{ document.addEventListener(evt, __settingsBackupModalSwallowGhostTap__, true); }catch(_){ }
    });
    backupModal.addEventListener("click", (e) => { if (e.target === backupModal) __closeSettingsBackupModal__(); });
  }


  const logout = document.getElementById("settingsLogoutBtn");
  if (logout) bindFastTap(logout, async () => {
    try{ __pillLongPressSuppress__(logout, 1800); }catch(_){ }
    let ok = false;
    try{ ok = await confirmYesNo("Vuoi uscire?"); }catch(_){ ok = false; }
    if (!ok) return;
    try{ clearSession(); }catch(_){ }
    try{ state.session = null; }catch(_){ }
    try{ applyRoleMode(); }catch(_){ }
    try{ __resetInMemoryData__(); }catch(_){ }
    try{ invalidateApiCache(); }catch(_){ }
    try{ showPage("auth"); }catch(_){ }
  });


  // Anno di esercizio (gestito dal pulsante calendario in pagina Impostazioni)
  const selAnno = document.getElementById("setAnno");
  if (selAnno){
    const cy = new Date().getFullYear();
    const years = [];
    for (let y = cy - 3; y <= cy + 2; y++) years.push(String(y));
    selAnno.innerHTML = years.map(y => `<option value="${y}">${y}</option>`).join("");
    selAnno.value = String(state.exerciseYear || loadExerciseYear());
    selAnno.addEventListener("change", () => { __applyExerciseYearChange__(String(selAnno.value || "")); });
  }

  // =========================
  // OPERATORE: popup crea / modifica
  // =========================
  const opBtn = document.getElementById("settingsCreateOperatorBtn");
  const opModal = document.getElementById("operatorModal");
  const opModalClose = document.getElementById("operatorModalClose");
  const opMenu = document.getElementById("operatorModalMenu");
  const opForm = document.getElementById("operatorModalForm");
  const opCreate = document.getElementById("operatorCreateBtn");
  const opEdit = document.getElementById("operatorEditBtn");
  const opDelete = document.getElementById("operatorDeleteBtn");
  const opBack = document.getElementById("opFormBack");
  const opConfirm = document.getElementById("opFormConfirm");
  const opUser = document.getElementById("opFormUsername");
  const opPass = document.getElementById("opFormPassword");
  const opOwnerPass = document.getElementById("opFormOwnerPassword");
  const opPassLabel = document.getElementById("opFormPasswordLabel");
  const opMsg = document.getElementById("operatorModalMsg");
  const opPassWrap = opPass ? (opPass.closest?.(".field") || opPass.parentElement) : null;

  let __opMode = ""; // "create" | "edit" | "delete"

  function __opSetMsg(msg, kind){
    try{
      if (!opMsg) return;
      opMsg.textContent = String(msg || "");
      opMsg.classList.remove("is-ok","is-err");
      if (kind === "ok") opMsg.classList.add("is-ok");
      if (kind === "err") opMsg.classList.add("is-err");
      opMsg.hidden = !opMsg.textContent;
    }catch(_){ }
  }

  function __opShowMenu(){
    __opMode = "";
    if (opMenu) opMenu.hidden = false;
    if (opForm) opForm.hidden = true;
    if (opUser) opUser.value = "";
    if (opPass) opPass.value = "";
    if (opOwnerPass) opOwnerPass.value = "";
    if (opPassWrap) opPassWrap.hidden = false;
    __opSetMsg("", null);
    try{ refreshFloatingLabels(); }catch(_){ }
  }

  function __opShowForm(mode){
    __opMode = mode;
    if (opMenu) opMenu.hidden = true;
    if (opForm) opForm.hidden = false;
    if (opUser) opUser.value = "";
    if (opPass) opPass.value = "";
    if (opOwnerPass) opOwnerPass.value = "";
    if (opPassLabel) opPassLabel.textContent = (mode === "edit") ? "Nuova password" : (mode === "delete" ? "Password operatore" : "Password operatore");
    if (opConfirm) opConfirm.textContent = (mode === "edit") ? "Salva" : (mode === "delete" ? "Elimina" : "Crea");
    if (opPassWrap) opPassWrap.hidden = (mode === "delete");
    __opSetMsg("", null);
    try{ refreshFloatingLabels(); }catch(_){ }
    try{ setTimeout(()=>{ try{ opUser && opUser.focus(); }catch(_){ } }, 30); }catch(_){ }
  }

  function __opOpen(){
    const s = state.session || loadSession();
    if (!s || !s.username){ toast("Nessun account"); return; }
    if (isOperatoreSession(s)){ toast("Non disponibile per operatori"); return; }
    if (!opModal) return;
    __opShowMenu();
    try{ opModal.hidden = false; opModal.setAttribute("aria-hidden","false"); }catch(_){ }
  }

  function __opClose(){
    if (!opModal) return;
    try{ opModal.hidden = true; opModal.setAttribute("aria-hidden","true"); }catch(_){ }
  }

  async function __opSubmit(){
    try{
      const s = state.session || loadSession();
      if (!s || !s.username){ __opSetMsg("Nessun account", "err"); return; }
      if (isOperatoreSession(s)){ __opSetMsg("Non disponibile per operatori", "err"); return; }

      const ownerUsername = String(s.username || "").trim();
      const operatorUsername = String(opUser?.value || "").trim();
      const __composeOpUser = (tenant, opu)=>{
        const t = String(tenant || '').trim();
        const o = String(opu || '').trim();
        if (!t || !o) return o;
        if (o.includes('__')) return o;
        return `${t}__${o}`;
      };
      const operatorFullUsername = __composeOpUser(ownerUsername, operatorUsername);
      const operatorPassword = String(opPass?.value || "");
      const ownerPassword = String(opOwnerPass?.value || "");

      __opSetMsg("", null);
      if (!operatorUsername) { __opSetMsg("Username operatore mancante", "err"); return; }
      if (__opMode !== "delete" && !operatorPassword) { __opSetMsg((__opMode === "edit") ? "Nuova password mancante" : "Password operatore mancante", "err"); return; }
      if (!ownerPassword) { __opSetMsg("Password owner mancante", "err"); return; }

      if (__opMode === "create"){
        await api("utenti", {
          method:"POST",
          body:{
            op:"create_operator",
            username: ownerUsername,
            password: ownerPassword,
            operator_username: operatorFullUsername,
            operator_password: operatorPassword,
          },
          showLoader:true,
        });
        __opSetMsg("Operatore creato", "ok");
        try{ if (opPass) opPass.value = ""; }catch(_){ }
        try{ if (opOwnerPass) opOwnerPass.value = ""; }catch(_){ }
        try{ refreshFloatingLabels(); }catch(_){ }
        return;
      }

      if (__opMode === "edit"){
        await api("utenti", {
          method:"POST",
          body:{
            op:"update_operator",
            username: ownerUsername,
            password: ownerPassword,
            operator_username: operatorFullUsername,
            newPassword: operatorPassword,
          },
          showLoader:true,
        });
        __opSetMsg("Operatore aggiornato", "ok");
        try{ if (opPass) opPass.value = ""; }catch(_){ }
        try{ if (opOwnerPass) opOwnerPass.value = ""; }catch(_){ }
        try{ refreshFloatingLabels(); }catch(_){ }
        return;
      }

      if (__opMode === "delete"){
        const ok = confirm(`Eliminare l'operatore "${operatorUsername}"?`);
        if (!ok) return;
        await api("utenti", {
          method:"POST",
          body:{
            op:"delete_operator",
            username: ownerUsername,
            password: ownerPassword,
            operator_username: operatorFullUsername,
          },
          showLoader:true,
        });
        __opSetMsg("Operatore eliminato", "ok");
        try{ if (opOwnerPass) opOwnerPass.value = ""; }catch(_){ }
        try{ refreshFloatingLabels(); }catch(_){ }
        return;
      }

      __opSetMsg("Operazione non valida", "err");
    }catch(e){
      const msg = String(e && e.message ? e.message : "Errore");
      if (msg.toLowerCase().includes("username operatore già esistente")){
        __opSetMsg(`Nella struttura "${ownerUsername}" questo username operatore è già in uso. Scegli un nome diverso.`, "err");
      } else {
        __opSetMsg(msg, "err");
      }
    }
  }

  if (opBtn) bindFastTap(opBtn, __opOpen);
  if (opModalClose) bindFastTap(opModalClose, __opClose);
  if (opCreate) bindFastTap(opCreate, ()=>__opShowForm("create"));
  if (opEdit) bindFastTap(opEdit, ()=>__opShowForm("edit"));
  if (opDelete) bindFastTap(opDelete, ()=>__opShowForm("delete"));
  if (opBack) bindFastTap(opBack, __opShowMenu);
  if (opConfirm) bindFastTap(opConfirm, __opSubmit);
}


function setupAuth(){
  const menu = document.getElementById("authMenu");
  const form = document.getElementById("authForm");
  const activation = document.getElementById("authActivation");

  const btnCreate = document.getElementById("btnMenuCreate");
  const btnUpdate = document.getElementById("btnMenuUpdate");
  const btnLoginAdmin = document.getElementById("btnMenuLoginAdmin");
  const btnLoginOperator = document.getElementById("btnMenuLoginOperator");

  const btnBack = document.getElementById("btnAuthBack");
  const btnSubmit = document.getElementById("btnAuthSubmit");
  const btnRequestCode = document.getElementById("btnAuthRequestCode");
  const btnUnlockCode = document.getElementById("btnAuthUnlockCode");
  const btnActivationBack = document.getElementById("btnAuthActivationBack");
  const activationTitle = document.getElementById("authActivationTitle");
  const activationUser = document.getElementById("authActivationUser");
  const activationText = document.getElementById("authActivationText");
  const activationHint = document.getElementById("authActivationHint");

  const createRoleWrap = document.getElementById("authCreateRoleWrap");
  const createRoleAdmin = document.getElementById("authCreateRoleAdmin");
  const createRoleOperator = document.getElementById("authCreateRoleOperator");
  const createTypeTag = document.getElementById("authCreateTypeTag");

  const tenantWrap = document.getElementById("authOperatorTenant");
  const credsWrap = document.getElementById("authCredsWrap");
  const u = document.getElementById("authUsername");
  const p = document.getElementById("authPassword");
  const uLabel = document.getElementById("authUsernameLabel");
  const pLabel = document.getElementById("authPasswordLabel");

  const extra = document.getElementById("authExtra");
  const p2Wrap = document.getElementById("authConfirmPasswordWrap");
  const npWrap = document.getElementById("authNewPasswordWrap");

  const p2 = document.getElementById("authPassword2");
  const np1 = document.getElementById("authNewPassword");
  const np2 = document.getElementById("authNewPassword2");

  const hint = document.getElementById("authHint");
  const setHint = (msg)=>{ try{ if (hint) hint.textContent = msg || ""; }catch(_ ){} };
  const setActivationHint = (msg)=>{ try{ if (activationHint) activationHint.textContent = msg || ""; }catch(_ ){} };

  let mode = "menu";

  const getCreateRole = ()=>{
    try{ if (createRoleOperator && createRoleOperator.checked) return "operatore"; }catch(_ ){}
    return "admin";
  };

  const syncCreateTag = ()=>{
    try{
      if (!createTypeTag) return;
      const role = getCreateRole();
      if (role === "operatore"){
        createTypeTag.textContent = "OPERATORE";
        createTypeTag.classList.add("is-operator");
      } else {
        createTypeTag.textContent = "ADMIN";
        createTypeTag.classList.remove("is-operator");
      }
    }catch(_ ){}
  };

  const clearFields = ()=>{
    try{
      if (u) u.value = "";
      if (p) p.value = "";
      if (p2) p2.value = "";
      if (np1) np1.value = "";
      if (np2) np2.value = "";
      try{ if (createRoleAdmin) createRoleAdmin.checked = true; }catch(_ ){}
      try{ if (createRoleOperator) createRoleOperator.checked = false; }catch(_ ){}
      try{ refreshFloatingLabels(); }catch(_ ){}
    }catch(_ ){}
  };

  const hideActivation = ()=>{
    try{ if (activation) activation.hidden = true; }catch(_ ){}
    setActivationHint("");
  };

  const showMenu = ()=>{
    mode = "menu";
    try{ if (menu) menu.hidden = false; }catch(_ ){}
    try{ if (form) form.hidden = true; }catch(_ ){}
    hideActivation();
    setHint("");
    try{ if (createRoleWrap) createRoleWrap.hidden = true; }catch(_ ){}
    try{ if (tenantWrap) tenantWrap.hidden = true; }catch(_ ){}
    try{ if (extra) extra.hidden = true; }catch(_ ){}
    try{ if (p2Wrap) p2Wrap.hidden = true; }catch(_ ){}
    try{ if (npWrap) npWrap.hidden = true; }catch(_ ){}
    try{ if (credsWrap) credsWrap.hidden = false; }catch(_ ){}
    clearFields();
    __authPendingUser__ = null;
    __authPendingReason__ = "";
  };

  const setMode = (m)=>{
    mode = m;
    try{ if (menu) menu.hidden = true; }catch(_ ){}
    try{ if (form) form.hidden = false; }catch(_ ){}
    hideActivation();
    setHint("");

    try{ if (createRoleWrap) createRoleWrap.hidden = true; }catch(_ ){}
    try{ if (tenantWrap) tenantWrap.hidden = true; }catch(_ ){}
    try{ if (extra) extra.hidden = true; }catch(_ ){}
    try{ if (p2Wrap) p2Wrap.hidden = true; }catch(_ ){}
    try{ if (npWrap) npWrap.hidden = true; }catch(_ ){}
    try{ if (credsWrap) credsWrap.hidden = false; }catch(_ ){}
    try{ if (uLabel) uLabel.textContent = "User"; }catch(_ ){}
    try{ if (pLabel) pLabel.textContent = "Password"; }catch(_ ){}

    clearFields();

    if (m === "create"){
      try{ if (btnSubmit) btnSubmit.textContent = "crea account"; }catch(_ ){}
      try{ if (createRoleWrap) createRoleWrap.hidden = false; }catch(_ ){}
      try{ if (p2Wrap) p2Wrap.hidden = false; }catch(_ ){}
      try{ syncCreateTag(); }catch(_ ){}
      try{ u && u.focus(); }catch(_ ){}
      return;
    }

    if (m === "update"){
      try{ if (btnSubmit) btnSubmit.textContent = "modifica account"; }catch(_ ){}
      try{ if (npWrap) npWrap.hidden = false; }catch(_ ){}
      try{ if (pLabel) pLabel.textContent = "Password attuale"; }catch(_ ){}
      try{ u && u.focus(); }catch(_ ){}
      return;
    }

    if (m === "login_admin" || m === "login_operator"){
      try{ if (btnSubmit) btnSubmit.textContent = "accedi"; }catch(_ ){}
      try{ u && u.focus(); }catch(_ ){}
      return;
    }
  };

  const goAfterLogin = ()=>{
    try{ state.exerciseYear = loadExerciseYear(); }catch(_ ){}
    try{ updateYearPill(); }catch(_ ){}
    try{ __applyContext__({ force:true }); }catch(_ ){}
    try{ applyRoleMode(); }catch(_ ){}
    try{ __hydrateAppLanguageFromSettings__(); }catch(_ ){}
    try{ if (window.__syncCleanOperators__) window.__syncCleanOperators__(); }catch(_ ){}
    showPage(isOperatoreSession(state.session) ? "pulizie" : "home");
  };

  const mapAuthError = (msg)=>{
    const m = String(msg || "").trim();
    return m || "Errore";
  };

  const showActivationForUser = async (user, reason)=>{
    const merged = await __refreshSessionFromStorage__(user || {}) || user || null;
    __authPendingUser__ = merged;
    __authPendingReason__ = String(reason || "").trim();
    try{ if (menu) menu.hidden = true; }catch(_ ){}
    try{ if (form) form.hidden = true; }catch(_ ){}
    try{ if (activation) activation.hidden = false; }catch(_ ){}
    try{ if (activationTitle) activationTitle.textContent = (__authPendingReason__ === "Licenza scaduta") ? "Licenza scaduta" : "Attivazione account"; }catch(_ ){}
    try{ if (activationUser) activationUser.textContent = String(merged?.username || user?.username || "—").trim() || "—"; }catch(_ ){}
    try{
      if (activationText){
        activationText.textContent = (__authPendingReason__ === "Licenza scaduta")
          ? "Inserisci un nuovo codice sblocco oppure genera il codice richiesta."
          : "Se hai già il codice sblocco inseriscilo. Altrimenti genera il codice richiesta e invialo via email.";
      }
    }catch(_ ){}
    setActivationHint("");
  };

  const finishLicensedLogin = async (user)=>{
    const merged = await __refreshSessionFromStorage__(user || {}) || user || null;
    if (!merged) throw new Error("Account non trovato");
    state.session = merged;
    saveSession(state.session);
    __authPendingUser__ = null;
    __authPendingReason__ = "";
    setHint("");
    setActivationHint("");
    goAfterLogin();
  };

  __authUiApi__ = {
    showMenu,
    showActivationForUser,
    finishLicensedLogin
  };

  if (btnCreate) bindFastTap(btnCreate, ()=>setMode("create"));
  if (btnUpdate) bindFastTap(btnUpdate, ()=>setMode("update"));
  if (btnLoginAdmin) bindFastTap(btnLoginAdmin, ()=>setMode("login_admin"));
  if (btnLoginOperator) bindFastTap(btnLoginOperator, ()=>setMode("login_operator"));
  if (btnBack) bindFastTap(btnBack, showMenu);
  if (btnActivationBack) bindFastTap(btnActivationBack, showMenu);

  try{
    if (createRoleAdmin) createRoleAdmin.addEventListener("change", syncCreateTag);
    if (createRoleOperator) createRoleOperator.addEventListener("change", syncCreateTag);
  }catch(_ ){}

  if (btnRequestCode) bindFastTap(btnRequestCode, async ()=>{
    try{
      const user = __authPendingUser__;
      if (!user) throw new Error("Nessun account selezionato");
      const row = await __updateUserLicense__(user, { licenseRequestCode: __licenseRequestCodeForUser__(user), licenseStatus: "pending", licenseType: __isMasterSession__(user) ? "master" : "time" });
      const refreshed = await __refreshSessionFromStorage__(row);
      __authPendingUser__ = refreshed || user;
      __showLicenseRequestModal__(__licenseRequestCodeForUser__(row), refreshed || row, { email:true });
      setActivationHint("Codice richiesta generato");
    }catch(e){ setActivationHint(mapAuthError(e && e.message ? e.message : e)); }
  });

  if (btnUnlockCode) bindFastTap(btnUnlockCode, async ()=>{
    try{
      const user = __authPendingUser__;
      if (!user) throw new Error("Nessun account selezionato");
      const code = await __openLicenseUnlockModal__();
      if (!code) return;
      if (String(code).trim() === __MASTER_CODE__){
        if (isOperatoreSession(user)) throw new Error("Il master code è consentito solo agli account admin");
        const row = await __updateUserLicense__(user, {
          isMaster: true,
          licenseType: "master",
          licenseStatus: "master",
          licenseStart: todayISO(),
          licenseEnd: "2099-12-31",
          licenseUnlockCode: __MASTER_CODE__,
          masterEnabledAt: __nowIso__()
        });
        await finishLicensedLogin(row);
        return;
      }
      const parsed = __licenseParseUnlockCodeForUser__(code, user);
      if (!parsed || parsed.type !== "license") throw new Error("Codice sblocco non valido");
      const row = await __updateUserLicense__(user, {
        isMaster: false,
        licenseType: "time",
        licenseStatus: "active",
        licenseStart: parsed.startDate,
        licenseEnd: parsed.endDate,
        licenseUnlockCode: parsed.code,
        licenseRequestCode: __licenseRequestCodeForUser__(user),
        masterEnabledAt: ""
      });
      await finishLicensedLogin(row);
    }catch(e){ setActivationHint(mapAuthError(e && e.message ? e.message : e)); }
  });

  if (btnSubmit) bindFastTap(btnSubmit, async ()=>{
    try{
      const username = String(u ? u.value : "").trim();
      const password = String(p ? p.value : "");
      if (!username || !password){ setHint("Inserisci user e password"); return; }

      if (mode === "create"){
        const confirm = String(p2 ? p2.value : "");
        if (!confirm){ setHint("Conferma password"); return; }
        if (confirm !== password){ setHint("Le password non coincidono"); return; }
        setHint("...");
        const role = getCreateRole();
        const data = await api("utenti", { method:"POST", body:{ op:"create", role, username, password } });
        if (!data || !data.user) throw new Error("Errore creazione account");
        setHint("");
        const row = await __updateUserLicense__(data.user, { licenseRequestCode: __licenseRequestCodeForUser__(data.user), licenseStatus: "pending", licenseType: "time", isMaster:false, masterEnabledAt:"" });
        state.session = await __refreshSessionFromStorage__(row || data.user) || data.user;
        saveSession(state.session);
        setHint("");
        goAfterLogin();
        return;
      }

      if (mode === "update"){
        const newPassword = String(np1 ? np1.value : "");
        const newPassword2 = String(np2 ? np2.value : "");
        if (!newPassword || !newPassword2){ setHint("Inserisci e conferma la nuova password"); return; }
        if (newPassword !== newPassword2){ setHint("Le nuove password non coincidono"); return; }
        setHint("...");
        const data = await api("utenti", { method:"POST", body:{ op:"update", username, password, newPassword } });
        if (!data || !data.user) throw new Error("Errore modifica account");
        state.session = data.user;
        saveSession(state.session);
        setHint("");
        goAfterLogin();
        return;
      }

      if (mode === "login_admin" || mode === "login_operator"){
        setHint("...");
        const data = await api("utenti", { method:"POST", body:{ op:"login", username, password } });
        if (!data || !data.user) throw new Error("Credenziali non valide");
        if (mode === "login_admin" && isOperatoreSession(data.user)) { setHint("Questo account è un operatore. Accedi come operatore."); return; }
        if (mode === "login_operator" && !isOperatoreSession(data.user)) { setHint("Questo account è un admin. Accedi come admin."); return; }
        const row = await __findUserRowByIdentity__(data.user);
        state.session = await __refreshSessionFromStorage__(row || data.user) || data.user;
        saveSession(state.session);
        setHint("");
        goAfterLogin();
        return;
      }

      showMenu();
    }catch(e){
      setHint(mapAuthError(e && e.message ? e.message : e));
    }
  });

  showMenu();
}

function __modalShowById__(id){
  try{
    const modal = document.getElementById(id);
    if (!modal) return;
    modal.hidden = false;
    modal.classList.add('is-open');
    modal.setAttribute('aria-hidden', 'false');
    document.body.classList.add('modal-open');
  }catch(_){ }
}
function __modalHideById__(id){
  try{
    const modal = document.getElementById(id);
    if (!modal) return;
    modal.classList.remove('is-open');
    modal.hidden = true;
    modal.setAttribute('aria-hidden', 'true');
    const hasOtherOpenModals = !!document.querySelector('.modal.is-open:not([hidden])');
    if (!hasOtherOpenModals) document.body.classList.remove('modal-open');
  }catch(_){ }
}

const __licenseModalIds__ = ['settingsLicenseModal','licenseRequestModal','licenseUnlockModal','licenseGeneratorModal','licenseDateRangeModal'];
let __licenseModalSuppressUntil__ = 0;
function __isLicenseModalOpen__(){
  try{
    return __licenseModalIds__.some((id)=>{
      const modal = document.getElementById(id);
      return !!(modal && !modal.hidden && modal.classList.contains('is-open'));
    });
  }catch(_){ return false; }
}
function __isInsideOpenLicenseModal__(node){
  try{
    const target = node && node.nodeType === 1 ? node : (node && node.parentElement ? node.parentElement : null);
    if (!target) return false;
    return __licenseModalIds__.some((id)=>{
      const modal = document.getElementById(id);
      if (!modal || modal.hidden || !modal.classList.contains('is-open')) return false;
      const card = modal.querySelector('.modal-card, .guest-date-range-modal-card, .settings-license-card');
      return !!(card && card.contains(target));
    });
  }catch(_){ return false; }
}
function __licenseModalSwallowGhostTap__(ev){
  try{
    const now = Date.now();
    const shouldSuppress = now < (__licenseModalSuppressUntil__ || 0);
    const modalOpen = __isLicenseModalOpen__();
    if (!shouldSuppress && !modalOpen) return;
    const inside = __isInsideOpenLicenseModal__(ev.target);
    if (!inside || shouldSuppress){
      try{ ev.preventDefault(); }catch(_){ }
      try{ ev.stopPropagation(); }catch(_){ }
      try{ ev.stopImmediatePropagation(); }catch(_){ }
    }
  }catch(_){ }
}
function __bindLicenseModalShield__(){
  try{
    if (window.__licenseModalShieldBound) return;
    window.__licenseModalShieldBound = true;
  }catch(_){ }
  try{
    __licenseModalIds__.forEach((id)=>{
      const modal = document.getElementById(id);
      if (!modal || modal.__licenseShieldBound) return;
      modal.__licenseShieldBound = true;
      const card = modal.querySelector('.modal-card, .guest-date-range-modal-card, .settings-license-card');
      if (card){
        ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt)=>{
          try{ card.addEventListener(evt, (ev)=>{ try{ ev.stopPropagation(); }catch(_){ } }, { passive:false }); }
          catch(_){ try{ card.addEventListener(evt, (ev)=>{ try{ ev.stopPropagation(); }catch(__){ } }); }catch(__){ } }
        });
      }
      ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt)=>{
        try{ modal.addEventListener(evt, (ev)=>{
          const target = ev.target;
          if (target === modal){
            try{ ev.preventDefault(); }catch(_){ }
            try{ ev.stopPropagation(); }catch(_){ }
            try{ ev.stopImmediatePropagation(); }catch(_){ }
          }
        }, { passive:false }); }catch(_){ }
      });
    });
    ['pointerdown','pointerup','touchstart','touchend','click'].forEach((evt)=>{
      try{ document.addEventListener(evt, __licenseModalSwallowGhostTap__, true); }catch(_){ }
    });
  }catch(_){ }
}
function __showLicenseRequestModal__(code, user, opts){
  try{
    const title = document.getElementById('licenseRequestModalTitle');
    const txt = document.getElementById('licenseRequestCodeText');
    const hint = document.getElementById('licenseRequestModalHint');
    const emailBtn = document.getElementById('licenseRequestEmailBtn');
    if (title) title.textContent = (opts && opts.title) ? String(opts.title) : 'CODICE RICHIESTA';
    if (txt){
      txt.textContent = String(code || '');
      txt.onclick = async () => {
        try{
          if (navigator.clipboard && navigator.clipboard.writeText) await navigator.clipboard.writeText(String(code || ''));
          else {
            const r = document.createRange();
            r.selectNodeContents(txt);
            const sel = window.getSelection();
            sel.removeAllRanges();
            sel.addRange(r);
            document.execCommand('copy');
            sel.removeAllRanges();
          }
          try{ toast('Codice copiato', 'blue'); }catch(_){ }
        }catch(_){ try{ toast('Copia non disponibile', 'orange'); }catch(__){ } }
      };
    }
    if (hint) hint.textContent = (opts && opts.hint) ? String(opts.hint) : 'Tocca il codice per copiarlo';
    if (emailBtn){
      const visible = !!(opts && opts.email);
      emailBtn.hidden = !visible;
      emailBtn.style.display = visible ? '' : 'none';
      emailBtn.onclick = () => {
        try{
          const uname = String(user?.username || 'account').trim();
          const subject = encodeURIComponent(`dDAE codice richiesta ${uname}`);
          const body = encodeURIComponent(`Account: ${uname}
Codice richiesta: ${String(code || '')}
Build: ${BUILD_VERSION}`);
          window.location.href = `mailto:${encodeURIComponent(__MASTER_EMAIL__)}?subject=${subject}&body=${body}`;
        }catch(_){ }
      };
    }
    ['licenseRequestClose','licenseRequestDoneBtn'].forEach((id)=>{ const el = document.getElementById(id); if (el) el.onclick = ()=>__modalHideById__('licenseRequestModal'); });
    __modalShowById__('licenseRequestModal');
  }catch(_){ }
}
function __openLicenseUnlockModal__(){
  return new Promise((resolve)=>{
    try{
      const input = document.getElementById('licenseUnlockInput');
      const finish = (val)=>{ __modalHideById__('licenseUnlockModal'); resolve(String(val || '').trim()); };
      ['licenseUnlockClose','licenseUnlockCancel'].forEach((id)=>{ const el = document.getElementById(id); if (el) el.onclick = ()=>finish(''); });
      const ok = document.getElementById('licenseUnlockConfirm');
      if (ok) ok.onclick = ()=>finish(input ? input.value : '');
      if (input){
        input.value = '';
        input.onkeydown = (e)=>{ if (e && e.key === 'Enter'){ try{ e.preventDefault(); }catch(_){ } finish(input.value); } };
        setTimeout(()=>{ try{ input.focus(); }catch(_){ } }, 80);
      }
      __modalShowById__('licenseUnlockModal');
    }catch(_){ resolve(''); }
  });
}
const __licenseDateRangeState__ = { month:null, start:'', end:'', draftStart:'', draftEnd:'' };
function __licenseDateRangeFormatDisplay__(startDate, endDate){
  const a = String(startDate || '').trim();
  const b = String(endDate || '').trim();
  if (!a && !b) return __designTranslate__('Seleziona date', { en:'Select dates', fr:'Sélectionnez les dates', de:'Daten auswählen', es:'Selecciona fechas' });
  if (a && !b) return formatShortDateIT(a);
  if (a && b) return `${formatShortDateIT(a)} → ${formatShortDateIT(b)}`;
  return __designTranslate__('Seleziona date', { en:'Select dates', fr:'Sélectionnez les dates', de:'Daten auswählen', es:'Selecciona fechas' });
}
function __syncLicenseDateRangeUi__(){
  try{
    const start = String(document.getElementById('licenseStartDate')?.value || '').trim();
    const end = String(document.getElementById('licenseEndDate')?.value || '').trim();
    const value = document.getElementById('licenseDateRangeValue');
    if (value) value.textContent = __licenseDateRangeFormatDisplay__(start, end);
  }catch(_){ }
}
function __renderLicenseDateRangeCalendar__(){
  try{
    const grid = document.getElementById('licenseDateRangeGrid');
    const monthTitle = document.getElementById('licenseDateRangeMonthTitle');
    const rangeTitle = document.getElementById('licenseDateRangeModalTitle');
    const weekdaysWrap = document.getElementById('licenseDateRangeWeekdays');
    if (!grid || !monthTitle || !weekdaysWrap) return;
    weekdaysWrap.innerHTML = __guestDateRangeWeekdayLabels__().map((d)=>`<span>${d}</span>`).join('');
    const monthDate = __licenseDateRangeState__.month || __guestDateRangeMonthStart__();
    __licenseDateRangeState__.month = __guestDateRangeMonthStart__(monthDate);
    const year = __licenseDateRangeState__.month.getFullYear();
    const month = __licenseDateRangeState__.month.getMonth();
    monthTitle.textContent = __licenseDateRangeState__.month.toLocaleDateString(__getCurrentLocale__(), { month:'long', year:'numeric' });
    const first = new Date(year, month, 1);
    const offset = (first.getDay() + 6) % 7;
    const start = new Date(year, month, 1 - offset);
    const todayIso = todayISO();
    const startIso = String(__licenseDateRangeState__.draftStart || '');
    const endIso = String(__licenseDateRangeState__.draftEnd || '');
    __dateRangeCalendarEnsureHeader__('licenseDateRangeModal', startIso, endIso, 'Da', 'A');
    __bindDateRangeCalendarModalTheme__('licenseDateRangeModal');
    const parts = [];
    for(let i=0;i<42;i++){
      const d = new Date(start.getFullYear(), start.getMonth(), start.getDate()+i);
      const iso = `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
      const inMonth = d.getMonth() === month;
      const classes = ['guest-date-range-day'];
      if (!inMonth) classes.push('is-muted');
      if (__guestDateRangeSameDay__(iso, todayIso)) classes.push('is-today');
      if (__guestDateRangeSameDay__(iso, startIso) || __guestDateRangeSameDay__(iso, endIso)) classes.push('is-selected','is-edge');
      else if (__guestDateRangeInBetween__(iso, startIso, endIso)) classes.push('is-in-range');
      parts.push(`<button class="${classes.join(' ')}" data-date="${iso}" type="button">${d.getDate()}</button>`);
    }
    grid.innerHTML = parts.join('');
    if (rangeTitle) rangeTitle.textContent = __licenseDateRangeFormatDisplay__(startIso, endIso);
  }catch(_){ }
}
function __selectLicenseDateRangeDay__(iso){
  const start = String(__licenseDateRangeState__.draftStart || '');
  const end = String(__licenseDateRangeState__.draftEnd || '');
  if (!start || (start && end)){
    __licenseDateRangeState__.draftStart = iso;
    __licenseDateRangeState__.draftEnd = '';
  } else if (iso < start){
    __licenseDateRangeState__.draftStart = iso;
  } else {
    __licenseDateRangeState__.draftEnd = iso;
  }
  __renderLicenseDateRangeCalendar__();
}
function __openLicenseDateRangeModal__(){
  try{
    const start = String(document.getElementById('licenseStartDate')?.value || '').trim() || todayISO();
    const endRaw = String(document.getElementById('licenseEndDate')?.value || '').trim();
    const end = (endRaw && endRaw >= start) ? endRaw : start;
    __licenseDateRangeState__.start = start;
    __licenseDateRangeState__.end = end;
    __licenseDateRangeState__.draftStart = start;
    __licenseDateRangeState__.draftEnd = end;
    __licenseDateRangeState__.month = __guestDateRangeMonthStart__(parseDateTs(start) != null ? new Date(parseDateTs(start)) : new Date());
    __renderLicenseDateRangeCalendar__();
    __modalShowById__('licenseDateRangeModal');
  }catch(_){ }
}
function __closeLicenseDateRangeModal__(restore){
  try{
    if (restore){
      __licenseDateRangeState__.draftStart = __licenseDateRangeState__.start || '';
      __licenseDateRangeState__.draftEnd = __licenseDateRangeState__.end || '';
    }
    __modalHideById__('licenseDateRangeModal');
  }catch(_){ }
}
function __applyLicenseDateRangeModal__(){
  try{
    let start = String(__licenseDateRangeState__.draftStart || '').trim();
    let end = String(__licenseDateRangeState__.draftEnd || '').trim();
    if (!start && !end) return __closeLicenseDateRangeModal__(true);
    if (start && !end) end = start;
    if (end && !start) start = end;
    if (end < start){ const swap = start; start = end; end = swap; }
    const startEl = document.getElementById('licenseStartDate');
    const endEl = document.getElementById('licenseEndDate');
    if (startEl) startEl.value = start;
    if (endEl) endEl.value = end;
    __licenseDateRangeState__.start = start;
    __licenseDateRangeState__.end = end;
    __syncLicenseDateRangeUi__();
    __closeLicenseDateRangeModal__(false);
  }catch(_){ }
}
function __bindLicenseUi__(){
  try{
    const trigger = document.getElementById('licenseDateRangeTrigger');
    if (trigger && !trigger.__boundLicenseRange){ trigger.__boundLicenseRange = true; bindFastTap(trigger, ()=>{ try{ __openLicenseDateRangeModal__(); }catch(_){ } }); }
    const modal = document.getElementById('licenseDateRangeModal');
    if (modal && !modal.__boundLicenseRange){
      modal.__boundLicenseRange = true;
      ['licenseDateRangeCancel'].forEach((id)=>{ const el = document.getElementById(id); if (el) bindFastTap(el, ()=>__closeLicenseDateRangeModal__(true)); });
      const applyBtn = document.getElementById('licenseDateRangeApply');
      if (applyBtn) bindFastTap(applyBtn, ()=>__applyLicenseDateRangeModal__());
      const prevBtn = document.getElementById('licenseDateRangePrev');
      if (prevBtn) bindFastTap(prevBtn, ()=>{ try{ const m = __licenseDateRangeState__.month || __guestDateRangeMonthStart__(); __licenseDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()-1, 1); __renderLicenseDateRangeCalendar__(); }catch(_){ } });
      const nextBtn = document.getElementById('licenseDateRangeNext');
      if (nextBtn) bindFastTap(nextBtn, ()=>{ try{ const m = __licenseDateRangeState__.month || __guestDateRangeMonthStart__(); __licenseDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()+1, 1); __renderLicenseDateRangeCalendar__(); }catch(_){ } });
      modal.addEventListener('click', (ev)=>{ try{ if (ev.target === modal) __closeLicenseDateRangeModal__(true); }catch(_){ } });
      const grid = document.getElementById('licenseDateRangeGrid');
      try{ __bindDateRangeCalendarHold__(grid); }catch(_){ }
      if (grid) grid.addEventListener('click', (ev)=>{
        const btn = ev.target.closest?.('.guest-date-range-day[data-date]');
        if (!btn) return;
        const iso = String(btn.dataset.date || '').trim();
        if (!iso) return;
        __selectLicenseDateRangeDay__(iso);
      });
    }
    __syncLicenseDateRangeUi__();
  }catch(_){ }
}
function __openLicenseGeneratorModal__(){
  try{
    const req = document.getElementById('licenseRequestInput');
    const start = document.getElementById('licenseStartDate');
    const end = document.getElementById('licenseEndDate');
    const hint = document.getElementById('licenseGeneratorHint');
    if (req) req.value = '';
    if (start) start.value = todayISO();
    if (end) end.value = todayISO();
    if (hint) hint.textContent = 'Inserisci il codice richiesta e scegli il periodo';
    __syncLicenseDateRangeUi__();
    __modalShowById__('licenseGeneratorModal');
    setTimeout(()=>{ try{ req && req.focus(); }catch(_){ } }, 80);
  }catch(_){ }
}
function __closeLicenseGeneratorModal__(){ __modalHideById__('licenseGeneratorModal'); }
function __initLicenseGeneratorBindings__(){
  try{
    __bindLicenseUi__();
    const closeIds = ['licenseGeneratorCancel'];
    closeIds.forEach((id)=>{ const el = document.getElementById(id); if (el && !el.__boundLicenseClose){ el.__boundLicenseClose = true; bindFastTap(el, __closeLicenseGeneratorModal__); } });
    const ok = document.getElementById('licenseGeneratorConfirm');
    if (ok && !ok.__boundLicenseGenerate){
      ok.__boundLicenseGenerate = true;
      bindFastTap(ok, ()=>{
        try{
          const requestCode = String(document.getElementById('licenseRequestInput')?.value || '').trim();
          const start = String(document.getElementById('licenseStartDate')?.value || '').trim();
          const end = String(document.getElementById('licenseEndDate')?.value || '').trim();
          const code = __licenseGenerateUnlockCode__(requestCode, start, end);
          __closeLicenseGeneratorModal__();
          __showLicenseRequestModal__(code, null, { title:'CODICE SBLOCCO', hint:'Tocca il codice per copiarlo', email:false });
        }catch(e){
          const hint = document.getElementById('licenseGeneratorHint');
          if (hint) hint.textContent = String((e && e.message) ? e.message : 'Errore codice');
        }
      });
    }
  }catch(_){ }
}


function __initSettingsLicenseBindings__(){
  try{
    try{ __bindLicenseModalShield__(); }catch(_){ }
    const closeBtn = document.getElementById('settingsLicenseCloseBtn');
    if (closeBtn && !closeBtn.__boundSettingsLicenseClose){ closeBtn.__boundSettingsLicenseClose = true; bindFastTap(closeBtn, ()=>__modalHideById__('settingsLicenseModal')); }
    const unlockBtn = document.getElementById('settingsLicenseUnlockBtn');
    if (unlockBtn && !unlockBtn.__boundSettingsLicenseUnlock){ unlockBtn.__boundSettingsLicenseUnlock = true; bindFastTap(unlockBtn, async()=>{
      try{
        const sess = state?.session || loadSession();
        if (!sess) throw new Error('Accedi prima');
        const code = await __openLicenseUnlockModal__();
        if (!code) return;
        if (String(code).trim() === __MASTER_CODE__){
          if (isOperatoreSession(sess)) throw new Error('Il codice master è consentito solo agli account admin');
          const row = await __updateUserLicense__(sess, { isMaster:true, licenseType:'master', licenseStatus:'master', licenseStart:todayISO(), licenseEnd:'2099-12-31', licenseUnlockCode:__MASTER_CODE__, masterEnabledAt:__nowIso__() });
          const merged = await __refreshSessionFromStorage__(row) || row;
          state.session = merged; saveSession(merged);
          try{ applyRoleMode(); }catch(_){ }
          try{ __modalHideById__('settingsLicenseModal'); }catch(_){ }
          try{ toast('Account master attivato', 'green'); }catch(_){ }
        }else{
          const parsed = __licenseParseUnlockCodeForUser__(code, sess);
          if (!parsed || parsed.type !== 'license') throw new Error('Codice sblocco non valido');
          const row = await __updateUserLicense__(sess, { isMaster:false, licenseType:'time', licenseStatus:'active', licenseStart:parsed.startDate, licenseEnd:parsed.endDate, licenseUnlockCode:parsed.code, licenseRequestCode:__licenseRequestCodeForUser__(sess), masterEnabledAt:'' });
          const merged = await __refreshSessionFromStorage__(row) || row;
          state.session = merged; saveSession(merged);
          try{ applyRoleMode(); }catch(_){ }
          try{ __modalHideById__('settingsLicenseModal'); }catch(_){ }
          try{ toast('Licenza attivata', 'green'); }catch(_){ }
        }
      }catch(e){ const hint = document.getElementById('settingsLicenseHint'); if (hint) hint.textContent = String((e&&e.message)?e.message:e); else try{ toast(String((e&&e.message)?e.message:e), 'orange'); }catch(_){ } }
    }); }
    const reqBtn = document.getElementById('settingsLicenseRequestBtn');
    if (reqBtn && !reqBtn.__boundSettingsLicenseReq){ reqBtn.__boundSettingsLicenseReq = true; bindFastTap(reqBtn, async()=>{
      try{
        const sess = state?.session || loadSession();
        if (!sess) throw new Error('Accedi prima');
        const row = await __updateUserLicense__(sess, { licenseRequestCode: __licenseRequestCodeForUser__(sess), licenseStatus:'pending' });
        __showLicenseRequestModal__(__licenseRequestCodeForUser__(row), row, { email:true, title:'CODICE RICHIESTA', hint:'Tocca il codice per copiarlo' });
      }catch(e){ const hint = document.getElementById('settingsLicenseHint'); if (hint) hint.textContent = String((e&&e.message)?e.message:e); }
    }); }
    const payBtn = document.getElementById('settingsLicensePayBtn');
    if (payBtn && !payBtn.__boundSettingsLicensePay){ payBtn.__boundSettingsLicensePay = true; bindFastTap(payBtn, async()=>{
      try{
        if (typeof DDAE_LICENSE_PAYMENT_URL === 'undefined' || !String(DDAE_LICENSE_PAYMENT_URL||'').trim()) throw new Error('Pagamento non configurato');
        window.open(String(DDAE_LICENSE_PAYMENT_URL).trim(), '_blank', 'noopener');
      }catch(e){ const hint = document.getElementById('settingsLicenseHint'); if (hint) hint.textContent = String((e&&e.message)?e.message:e); }
    }); }
    const genBtn = document.getElementById('settingsLicenseGeneratorBtn');
    if (genBtn && !genBtn.__boundSettingsLicenseGen){ genBtn.__boundSettingsLicenseGen = true; bindFastTap(genBtn, ()=>{ try{ __modalHideById__('settingsLicenseModal'); __openLicenseGeneratorModal__(); }catch(_){ } }); }
  }catch(_){ }
}

// ===== API Cache (speed + dedupe richieste) =====
const __apiCache = new Map();      // key -> { t:number, data:any }
const __apiInflight = new Map();   // key -> Promise

function __applyCtxToParams(action, params){
  const p = Object.assign({}, params || {});
  try{
    if (state && state.session && state.session.user_id && action !== "utenti" && action !== "ping"){
      if (p.user_id === undefined || p.user_id === null || String(p.user_id).trim() === "") {
        p.user_id = String(state.session.user_id);
      }
      if (p.anno === undefined || p.anno === null || String(p.anno).trim() === "") {
        p.anno = String(state.exerciseYear || "");
      }
    }
  }catch(_){ }
  return p;
}

function __cacheKey(action, params){
  try { return action + "|" + JSON.stringify(params || {}); }
  catch (_) { return action + "|{}"; }
}

function invalidateApiCache(prefix){
  try{
    for (const k of Array.from(__apiCache.keys())){
      if (!prefix || k.startsWith(prefix)) __apiCache.delete(k);
    }
  } catch (_) {}
  try{ __lsClearCtx(); }catch(_){ }
}

function __invalidateSyncCaches__(opts = {}){
  try{ invalidateApiCache(); }catch(_){}
  try{ __apiInflight.clear(); }catch(_){}
  try{ if (state && state.calendar) state.calendar.ready = false; }catch(_){}
  try{
    if (opts && opts.resetHomeRefresh){
      __homeRefreshInFlight = false;
      __homeRefreshLastAt = 0;
    }
  }catch(_){}
}

async function __refreshAfterSync__(restoreState){
  const targetPage = __sanitizePage(restoreState?.page) || __sanitizePage(state?.page) || "home";
  try{ __invalidateSyncCaches__({ resetHomeRefresh:true }); }catch(_){}
  try{ __writeRestoreState(Object.assign({}, restoreState || {}, { page: targetPage })); }catch(_){}

  try{ showPage(targetPage); }catch(_){}

  try{
    if (targetPage === "pulizie"){
      try{ if (typeof loadPulizieForDay === "function") await loadPulizieForDay({ clearFirst:false }); }catch(_){}
      try{ if (typeof loadOperatoriForDay === "function") await loadOperatoriForDay({ clearFirst:false }); }catch(_){}
      try{ if (typeof renderPuliziePage === "function") renderPuliziePage(); }catch(_){}
      return true;
    }

    if (targetPage === "home"){
      try{ refreshAllDataInBackground(); }catch(_){}
      try{ updateHomeReceiptsIndicator(); }catch(_){}
      return true;
    }

    if (targetPage === "orepulizia"){
      try{ await initOrePuliziaPage(); }catch(_){}
      return true;
    }

    if (targetPage === "calendario"){
      try{ if (state && state.calendar) state.calendar.ready = false; }catch(_){}
      try{ await ensureCalendarData({ force:true, showLoader:false }); }catch(_){}
      try{ renderCalendario(); }catch(_){}
      return true;
    }

    if (targetPage === "lavanderia"){
      try{ await loadLavanderia(); }catch(_){}
      return true;
    }

    return true;
  }catch(_){ }
  return true;
}


// ===== HOME: refresh totale dati in background (non blocca UI) =====
let __homeRefreshInFlight = false;
let __homeRefreshLastAt = 0;
function refreshAllDataInBackground(){
  try{
    if (!state || !state.session || !state.session.user_id) return;
    const now = Date.now();
    // evita loop/trigger ravvicinati
    if (__homeRefreshInFlight) return;
    if (now - __homeRefreshLastAt < 1500) return;
    __homeRefreshLastAt = now;
    __homeRefreshInFlight = true;

    // Avvio async non bloccante
    setTimeout(() => {
      (async () => {
        try{
          // Svuota cache dati (in-memory + localStorage cache)
          try{ invalidateApiCache(); }catch(_){}

          // Caricamenti principali (tutti senza loader) — ognuno protetto
          try{ await ensureSettingsLoaded({ force:true, showLoader:false }); }catch(_){}
          try{ await loadMotivazioni(); }catch(_){}
          try{ await load({ showLoader:false }); }catch(_){}

          try{ await ensurePeriodData({ showLoader:false, force:true }); }catch(_){}

          try{ await loadOspiti({ from:"", to:"", force:true }); }catch(_){}
          try{ await loadSpesaAll({ force:true, showLoader:false }); }catch(_){ }

          try{ if (typeof loadPulizieForDay === "function") await loadPulizieForDay({ clearFirst:false }); }catch(_){}
          try{ if (typeof loadOperatoriForDay === "function") await loadOperatoriForDay({ clearFirst:false }); }catch(_){}
          try{ await loadLavanderia(); }catch(_){}

          // Check ricevute (solo acconto+saldo)
          try{ checkReceiptsOnStartup(); }catch(_){}
        }catch(_){}
        finally{
          __homeRefreshInFlight = false;
          try{ __syncLedUpdate(); }catch(_){}
        }
      })();
    }, 0);
  }catch(_){ __homeRefreshInFlight = false; }
}

// ===== LocalStorage cache (perceived speed on iOS) =====
const __lsPrefixBase = "ddae_local_cache_v2:";

// Context (account + anno esercizio) — serve per isolare cache e DB per anno/account
function __ctxUid__(){
  try{
    const s = (state && state.session) ? state.session : loadSession();
    const uid = s && (s.user_id !== undefined ? s.user_id : s.id);
    if (uid !== undefined && uid !== null && String(uid).trim() !== "") return String(uid);
  }catch(_ ){}
  return "anon";
}

function __ctxYear__(){
  try{
    const y = String((state && state.exerciseYear) ? state.exerciseYear : loadExerciseYear()).trim();
    if (y) return y;
  }catch(_ ){}
  return String(new Date().getFullYear());
}

function __ctxSig__(){ return `${__ctxUid__()}|${__ctxYear__()}`; }



// ===== Year filtering (client-side) =====
// Some backend endpoints may ignore anno/from/to; enforce exercise year on the client.
function __yearFromAnyDate__(v){
  try{
    if (v === undefined || v === null) return null;
    const s = String(v).trim();
    if (!s) return null;
    // ISO: YYYY-MM-DD or YYYY-MM-DDTHH:mm:ss...
    const m1 = s.match(/^([0-9]{4})[-\/]/);
    if (m1) return m1[1];
    // IT: DD/MM/YYYY
    const m2 = s.match(/^[0-9]{1,2}[-\/]([0-9]{1,2})[-\/]([0-9]{4})$/);
    if (m2) return m2[2];
    // fallback: Date parse
    const d = new Date(s);
    if (!isNaN(d.getTime())) return String(d.getFullYear());
  }catch(_){ }
  return null;
}

function __filterByExerciseYear__(rows, year, candidateFields){
  try{
    const y = String(year||"").trim();
    if (!y) return Array.isArray(rows) ? rows : [];
    const list = Array.isArray(rows) ? rows : [];
    const fields = Array.isArray(candidateFields) && candidateFields.length ? candidateFields : [];
    if (!fields.length) return list;
    return list.filter(r => {
      if (!r || typeof r !== "object") return false;
      for (const f of fields){
        if (!f) continue;
        const val = r[f];
        const yr = __yearFromAnyDate__(val);
        if (yr === y) return true;
      }
      return false;
    });
  }catch(_){ }
  return Array.isArray(rows) ? rows : [];
}

function __lsPrefixNow__(){ return `${__lsPrefixBase}${__ctxUid__()}:${__ctxYear__()}:`; }

function __lsClearAll(){
  // cancella TUTTE le cache dell'app (tutti account/anni)
  try{
    const keys = [];
    for (let i=0; i<localStorage.length; i++) {
      const k = localStorage.key(i);
      if (k && k.startsWith(__lsPrefixBase)) keys.push(k);
    }
    keys.forEach(k => { try{ localStorage.removeItem(k); }catch(_ ){} });
  } catch(_ ){ }
}

function __lsClearCtx(){
  // cancella solo la cache del contesto corrente (account+anno)
  try{
    const p = __lsPrefixNow__();
    const keys = [];
    for (let i=0; i<localStorage.length; i++) {
      const k = localStorage.key(i);
      if (k && k.startsWith(p)) keys.push(k);
    }
    keys.forEach(k => { try{ localStorage.removeItem(k); }catch(_ ){} });
  } catch(_ ){ }
}

function __lsGet(key){
  try{
    const raw = localStorage.getItem(__lsPrefixNow__() + key);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch(_ ){ return null; }
}

function __lsSet(key, data){
  try{
    localStorage.setItem(__lsPrefixNow__() + key, JSON.stringify({ t: Date.now(), data }));
  } catch(_ ){}
}



// GET con cache in-memory (non tocca SW): evita chiamate duplicate e loader continui
async function cachedGet(action, params = {}, { ttlMs = 30000, swrMs = null, showLoader = true, force = false } = {}){
  const ctxParams = __applyCtxToParams(action, params);
  const key = __cacheKey(action, ctxParams);
  const now = Date.now();
  const swrWindow = (swrMs === null || swrMs === undefined) ? ttlMs : swrMs;

  if (!force) {
    const hit = __apiCache.get(key);
    if (hit) {
      const age = now - hit.t;
      if (age < ttlMs) return hit.data;

      // Stale-while-revalidate: torna subito il dato “stale” e aggiorna in background
      if (age < swrWindow) {
        if (!__apiInflight.has(key)) {
          const bg = (async () => {
            const data = await api(action, { params: ctxParams, showLoader:false });
            __apiCache.set(key, { t: Date.now(), data });
            return data;
          })();
          __apiInflight.set(key, bg);
          bg.finally(() => { try{ __apiInflight.delete(key); }catch(_){ } });
        }
        return hit.data;
      }
    }
  }

  if (__apiInflight.has(key)) return __apiInflight.get(key);

  const p = (async () => {
    const data = await api(action, { params: ctxParams, showLoader });
    __apiCache.set(key, { t: Date.now(), data });
    return data;
  })();

  __apiInflight.set(key, p);

  try {
    return await p;
  } finally {
    __apiInflight.delete(key);
  }
}

/* Launcher modal (popup) */



// iOS/PWA: elimina i “tap” persi (click non sempre affidabile su Safari PWA)
function bindFastTap(el, fn){
  if (!el) return;
  let last = 0;
  const handler = (e)=>{
    const now = Date.now();
    if (now - last < 450) return;
    last = now;
    try{ e.preventDefault(); }catch(_){ }
    try{ e.stopPropagation(); }catch(_){ }
    try{
      const target = e && e.currentTarget ? e.currentTarget : el;
      const inLicenseModal = !!(target && target.closest && target.closest('#settingsLicenseModal,#licenseRequestModal,#licenseUnlockModal,#licenseGeneratorModal,#licenseDateRangeModal'));
      if (inLicenseModal) __licenseModalSuppressUntil__ = Date.now() + 900;
    }catch(_){ }
    try{ __sfxTap(); }catch(_){ }
    fn();
  };

  // In PWA iOS/Safari: evita doppi trigger (touch/pointer/click)
  const usePointer = (typeof window !== "undefined") && ("PointerEvent" in window);
  const events = usePointer ? ["pointerup", "click"] : ["touchend", "click"];

  for (const evt of events){
    try{ el.addEventListener(evt, handler, { passive:false }); }
    catch(_){ el.addEventListener(evt, handler); }
  }
}

function __forceCloseEditorModal__(btnId){
  try{
    if (btnId === 'laundryCatalogEditorCancel') return __laundryCatalogCloseModal__();
    if (btnId === 'operatoriEditorCancel') return __operatoriCloseModal__();
    if (btnId === 'channelEditorCancel') return __channelCloseModal__();
  }catch(_){ }
}

(function __bindEditorCancelDelegation__(){
  if (typeof document === 'undefined') return;
  try{ if (window.__editorCancelDelegationBound) return; window.__editorCancelDelegationBound = true; }catch(_){ }
  const selector = '#laundryCatalogEditorCancel,#operatoriEditorCancel,#channelEditorCancel';
  const handler = (e) => {
    const btn = e.target && e.target.closest ? e.target.closest(selector) : null;
    if (!btn) return;
    try{ e.preventDefault(); }catch(_){ }
    try{ e.stopPropagation(); }catch(_){ }
    try{ e.stopImmediatePropagation(); }catch(_){ }
    try{ __sfxTap(); }catch(_){ }
    try{ __forceCloseEditorModal__(btn.id); }catch(_){ }
    return false;
  };
  try{ document.addEventListener('pointerup', handler, true); }catch(_){ }
  try{ document.addEventListener('touchend', handler, true); }catch(_){ }
  try{ document.addEventListener('click', handler, true); }catch(_){ }
})();


/* dDAE_1.020 — iOS hardening: Home icons always tappable (fallback binding) */
function bindHomeStrongTap(){
  // evita doppio binding
  try{
    if (bindHomeStrongTap.__bound) return;
    bindHomeStrongTap.__bound = true;
  }catch(_){ }

  const go = (id, page, { before } = {}) => {
    const el = document.getElementById(id);
    if (!el) return;

    const handler = (e) => {
      try{ e && e.preventDefault && e.preventDefault(); }catch(_){}
      try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){}
      try{ hideLauncher(); }catch(_){}

      try{ if (typeof before === "function") before(); }catch(_){}
      try{ showPage(page); }catch(_){}
    };

    // dDAE_2.347: il tap breve deve navigare, ma la pressione lunga deve poter aprire il popup colore
    // senza far partire la pagina dedicata gia su pointerdown/touchstart.
    const opts = { passive:false };
    try{ el.addEventListener("click", handler, opts); }catch(_){ try{ el.addEventListener("click", handler); }catch(__){} }
  };

  go("goOspite", "ospiti");
  go("goOspiti", "ospiti");
  go("goCalendario", "calendario");
  go("goTassaSoggiorno", "tassa");
  go("goPulizie", "pulizie");
  go("goLavanderia", "lavanderia");
  go("goStatistiche", "statistiche");
  go("goStatPiscina", "orepulizia");
  go("openLauncher", "spese", { before: ()=>{ try{ setSpeseView("list"); }catch(_){} } });
}


/* dDAE_1.020 — Tap counters: Adulti / Bambini <10 (tap increment, long press 0.5s = reset) */
function bindGuestTapCounters(){
  const ids = ["guestAdults","guestKidsU10"];
  const fireRecalc = ()=>{ try{ updateGuestRemaining(); }catch(_){ } try{ updateGuestTaxTotalPill(); }catch(_){ } };
  const setVal = (el, v)=>{
    try{
      const vv = Math.max(0, parseInt(String(v||"0"),10)||0);
      el.value = String(vv);
      try{ el.dispatchEvent(new Event("input", { bubbles:true })); }catch(_){
        try{ const ev=document.createEvent("Event"); ev.initEvent("input", true, true); el.dispatchEvent(ev); }catch(_){}
      }
      try{ el.dispatchEvent(new Event("change", { bubbles:true })); }catch(_){
        try{ const ev2=document.createEvent("Event"); ev2.initEvent("change", true, true); el.dispatchEvent(ev2); }catch(_){}
      }
      fireRecalc();
    }catch(_){}
  };

  ids.forEach((id)=>{
    const el = document.getElementById(id);
    if (!el) return;
    if (el.__tapCounterBound) return;
    el.__tapCounterBound = true;

    try{ el.setAttribute("readonly","readonly"); }catch(_){}
    try{ el.setAttribute("inputmode","none"); }catch(_){}
    try{ el.classList.add("tap-counter"); }catch(_){}

    let t = null;
    let longFired = false;
    const clearT = ()=>{ if (t){ clearTimeout(t); t=null; } };

    const onDown = (e)=>{
      longFired = false;
      clearT();
      try{ e.preventDefault(); }catch(_){}
      try{ e.stopPropagation(); }catch(_){}
      t = setTimeout(()=>{
        longFired = true;
        try{ __sfxGlass(); }catch(_){ }
        setVal(el, 0);
      }, 500);
    };
    const onUp = (e)=>{
      clearT();
      try{ e.preventDefault(); }catch(_){}
      try{ e.stopPropagation(); }catch(_){}
      if (longFired){ longFired=false; return; }
      const cur = parseInt(el.value || "0", 10) || 0;
      setVal(el, cur + 1);
    };
    const onCancel = ()=>{ clearT(); };

    const usePointer = (typeof window !== "undefined") && ("PointerEvent" in window);
    if (usePointer){
      try{ el.addEventListener("pointerdown", onDown, { passive:false }); }catch(_){ el.addEventListener("pointerdown", onDown); }
      try{ el.addEventListener("pointerup", onUp, { passive:false }); }catch(_){ el.addEventListener("pointerup", onUp); }
      try{ el.addEventListener("pointercancel", onCancel, { passive:true }); }catch(_){ el.addEventListener("pointercancel", onCancel); }
    } else {
      try{ el.addEventListener("touchstart", onDown, { passive:false }); }catch(_){ el.addEventListener("touchstart", onDown); }
      try{ el.addEventListener("touchend", onUp, { passive:false }); }catch(_){ el.addEventListener("touchend", onUp); }
      try{ el.addEventListener("touchcancel", onCancel, { passive:true }); }catch(_){ el.addEventListener("touchcancel", onCancel); }
      try{ el.addEventListener("click", (e)=>{ e.preventDefault(); e.stopPropagation(); onUp(e); }, { passive:false }); }catch(_){ el.addEventListener("click", (e)=>{ e.preventDefault(); e.stopPropagation(); onUp(e); }); }
    }

    // Evita doppio incremento (click dopo pointerup)
    try{ el.addEventListener("click", (e)=>{ e.preventDefault(); e.stopPropagation(); }, { passive:false }); }catch(_){ el.addEventListener("click", (e)=>{ e.preventDefault(); e.stopPropagation(); }); }
    try{ el.addEventListener("contextmenu", (e)=>{ e.preventDefault(); }); }catch(_){}
    try{ el.addEventListener("focus", ()=>{ try{ el.blur(); }catch(_){ } }); }catch(_){}
  });
}


let launcherDelegationBound = false;
let homeDelegationBound = false;
function bindHomeDelegation(){
  if (homeDelegationBound) return;
  homeDelegationBound = true;
  document.addEventListener("click", (e)=>{
    const o = e.target.closest && e.target.closest("#goOspite");
    if (o){ hideLauncher(); showPage("ospiti"); return; }
    const cal = e.target.closest && e.target.closest("#goCalendario");
    if (cal){ hideLauncher(); showPage("calendario"); return; }
    const tassa = e.target.closest && e.target.closest("#goTassaSoggiorno");
    if (tassa){
      hideLauncher();
      (async ()=>{ try{ await ensureSettingsLoaded({ force:false, showLoader:false }); }catch(_){} showPage("tassa"); try{ initTassaPage(); }catch(_){} })();
      return;
    }
    const pul = e.target.closest && e.target.closest("#goPulizie");
    if (pul){ hideLauncher(); showPage("pulizie"); return; }
        const opcal = e.target.closest && e.target.closest("#goOrePulizia") || e.target.closest("#goOrePuliziaTop");
    if (opcal){ hideLauncher(); showPage("orepulizia"); return; }

const lav = e.target.closest && e.target.closest("#goLavanderia") || e.target.closest("#goLavanderiaTop");
    if (lav){ hideLauncher(); showPage("lavanderia"); return; }

    const imp = e.target.closest && e.target.closest("#goImpostazioni");
    if (imp){ hideLauncher(); showPage((state.session && isOperatoreSession(state.session)) ? "opsettings" : "impostazioni"); return; }

    const g = e.target.closest && e.target.closest("#goStatistiche");
    if (g){ hideLauncher(); showPage("statistiche"); return; }

    // STATISTICHE (icone)
    const s1 = e.target.closest && e.target.closest("#goStatGen");
    if (s1){ hideLauncher(); showPage("statgen"); return; }
    const s2 = e.target.closest && e.target.closest("#goStatMensili");
    if (s2){ hideLauncher(); showPage("statmensili"); return; }
    const s3 = e.target.closest && e.target.closest("#goStatSpese");
    if (s3){ hideLauncher(); showPage("statspese"); return; }
    const s4 = e.target.closest && e.target.closest("#goStatRicevute");
    if (s4){ hideLauncher(); showPage("statprenotazioni"); return; }
    const s4b = e.target.closest && e.target.closest("#goStatChannel");
    if (s4b){ hideLauncher(); showPage("statchannel"); return; }
    const s4c = e.target.closest && e.target.closest("#goStatPulizie");
    if (s4c){ hideLauncher(); showPage("statpulizie"); return; }

  
    const s5 = e.target.closest && e.target.closest("#goStatAzienda");
    if (s5){ hideLauncher(); showPage("statazienda"); return; }
    const s6 = e.target.closest && e.target.closest("#goStatAmministratore");
    if (s6){ hideLauncher(); showPage("statamministratore"); return; }
    const s7 = e.target.closest && e.target.closest("#goStatPiscina");
    if (s7){ hideLauncher(); showPage("orepulizia"); return; }
    const s8 = e.target.closest && e.target.closest("#goStatCancellazioni");
    if (s8){ hideLauncher(); showPage("statcancellazioni"); return; }
});
}

function bindLauncherDelegation(){
  if (launcherDelegationBound) return;
  launcherDelegationBound = true;

  document.addEventListener("click", (e) => {
    const goBtn = e.target.closest && e.target.closest("#launcherModal [data-go]");
    if (goBtn){
      const page = goBtn.getAttribute("data-go");
      hideLauncher();
      showPage(page);
      return;
    }
    const close = e.target.closest && e.target.closest("#launcherModal [data-close], #closeLauncher");
    if (close){
      hideLauncher();
    }
  });
}

function showLauncher(){
  const m = document.getElementById("launcherModal");
  if (!m) return;
  m.hidden = false;
  m.setAttribute("aria-hidden", "false");
}
function hideLauncher(){
  const m = document.getElementById("launcherModal");
  if (!m) return;
  m.hidden = true;
  m.setAttribute("aria-hidden", "true");
}


function setSpeseView(view, { render=false } = {}){
  state.speseView = "list";
  const list = document.getElementById("speseViewList");
  const ins = document.getElementById("speseViewInsights");
  if (list) list.hidden = false;
  if (ins) ins.hidden = true;

  const btn = document.getElementById("btnSpeseInsights");
  if (btn){
    btn.setAttribute("aria-pressed", "false");
    btn.classList.remove("is-active");
    btn.hidden = true;
  }

  if (render){
    try{ renderSpese(); }catch(_){}
  }
}

/* NAV pages (5 pagine interne: home + 4 funzioni) */


// dDAE_1.020 — Fix contrast icone topbar: se un tasto appare bianco su iOS, l'icona bianca diventa invisibile.
// Applichiamo una classe .is-light ai pulsanti con background chiaro, così CSS forza icone scure.
function __parseRGBA__(s){
  try{
    const m = String(s||"").match(/rgba?\(([^)]+)\)/i);
    if (!m) return null;
    const parts = m[1].split(",").map(x=>x.trim());
    const r = parseFloat(parts[0]);
    const g = parseFloat(parts[1]);
    const b = parseFloat(parts[2]);
    let a = null;
    if (parts.length > 3) a = parseFloat(parts[3]);
    return {
      r: (isFinite(r) ? r : 0),
      g: (isFinite(g) ? g : 0),
      b: (isFinite(b) ? b : 0),
      a: (a != null && isFinite(a) ? a : null),
    };
  }catch(_){ return null; }
}

function ensureTopbarIconContrast(){
  try{
    const btns = document.querySelectorAll('.topbar .icon-btn');
    btns.forEach((btn)=>{
      if (!btn || btn.hidden) return;
      const cs = getComputedStyle(btn);
      const bg = cs.backgroundColor || '';
      const rgba = __parseRGBA__(bg);
      let isLight = false;
      if (!rgba){
        // con backdrop/trasparenza Safari può dare valori inattesi: trattiamo come "chiaro" (glass)
        isLight = true;
      } else {
        const r = rgba.r, g = rgba.g, b = rgba.b;
        const a = (rgba.a == null ? 1 : rgba.a);
        const lum = 0.2126*r + 0.7152*g + 0.0722*b;
        isLight = (lum >= 210) || (a <= 0.25);
      }
      btn.classList.toggle('is-light', !!isLight);
    });
  }catch(_){ }
}


const __VERTICAL_LOCK_PAGES__ = new Set(["home","tassa","orepulizia","statistiche","impostazioni","opsettings","prodotti"]);
function __isVerticalLockPage__(page){
  return __VERTICAL_LOCK_PAGES__.has(String(page || "").trim().toLowerCase());
}
function __updateVerticalLockViewportVars__(){
  try{
    const root = document.documentElement;
    const body = document.body;
    const vv = window.visualViewport;
    const vh = Math.max(1, Math.round((vv && vv.height) || window.innerHeight || document.documentElement.clientHeight || 0));
    const vw = Math.max(1, Math.round((vv && vv.width) || window.innerWidth || document.documentElement.clientWidth || 0));
    root.style.setProperty('--app-viewport-height', `${vh}px`);
    root.style.setProperty('--app-viewport-width', `${vw}px`);
    const topbar = document.querySelector('.topbar');
    const topbarH = Math.max(64, Math.round((topbar && topbar.getBoundingClientRect && topbar.getBoundingClientRect().height) || 64));
    root.style.setProperty('--app-topbar-offset', `${topbarH}px`);
    const syncBar = document.getElementById('homeSyncBar');
    const syncVisible = !!(body && body.dataset && body.dataset.page === 'home' && syncBar && !syncBar.hidden);
    const syncH = syncVisible ? Math.max(0, Math.ceil((syncBar.getBoundingClientRect && syncBar.getBoundingClientRect().height) || 0) + 12) : 0;
    root.style.setProperty('--app-fixed-bottom-offset', `${syncH}px`);
  }catch(_){ }
}
function __applyVerticalPageLock__(page){
  try{
    const root = document.documentElement;
    const body = document.body;
    if (!root || !body) return;
    const locked = __isVerticalLockPage__(page);
    root.classList.toggle('page-lock-vertical', !!locked);
    body.classList.toggle('page-lock-vertical', !!locked);
    body.classList.toggle('page-lock-sync-footer', !!(locked && String(page) === 'home'));
    body.classList.toggle('page-lock-no-sync-footer', !!(locked && String(page) !== 'home'));
    __updateVerticalLockViewportVars__();
    try{ window.scrollTo(0, 0); }catch(_){ }
    try{ document.documentElement.scrollTop = 0; }catch(_){ }
    try{ document.body.scrollTop = 0; }catch(_){ }
  }catch(_){ }
}
(function(){
  try{
    if (window.__ddaeVerticalLockBound) return;
    window.__ddaeVerticalLockBound = true;
    const refresh = () => {
      try{ __updateVerticalLockViewportVars__(); }catch(_){ }
      try{ if (document.body && document.body.classList && document.body.classList.contains('page-lock-vertical')) { window.scrollTo(0, 0); } }catch(_){ }
    };
    window.addEventListener('resize', refresh, { passive:true });
    window.addEventListener('orientationchange', refresh, { passive:true });
    window.addEventListener('pageshow', refresh, { passive:true });
    try{
      if (window.visualViewport){
        window.visualViewport.addEventListener('resize', refresh, { passive:true });
        window.visualViewport.addEventListener('scroll', refresh, { passive:true });
      }
    }catch(_){ }
    const allowVerticalMove = (target) => {
      try{
        if (!target || !target.closest) return false;
        return !!target.closest('.prodotti-list, .settings-year-wheel, .modal:not([hidden]) .laundry-detail-list, .modal:not([hidden]) .stat-graph-modal-legend, .modal:not([hidden]) [data-allow-vertical-scroll="true"]');
      }catch(_){
        return false;
      }
    };
    document.addEventListener('touchmove', (ev) => {
      try{
        if (!document.body || !document.body.classList || !document.body.classList.contains('page-lock-vertical')) return;
        if (ev.touches && ev.touches.length > 1) return;
        if (allowVerticalMove(ev.target)) return;
        ev.preventDefault();
      }catch(_){ }
    }, { passive:false, capture:true });
    document.addEventListener('scroll', () => {
      try{ if (document.body && document.body.classList && document.body.classList.contains('page-lock-vertical')) window.scrollTo(0, 0); }catch(_){ }
    }, { passive:true, capture:true });
    setTimeout(refresh, 0);
  }catch(_){ }

})();

function __getDocumentScrollTop__(){
  try{
    return Math.max(
      window.scrollY || 0,
      (document.scrollingElement && document.scrollingElement.scrollTop) || 0,
      document.documentElement?.scrollTop || 0,
      document.body?.scrollTop || 0
    );
  }catch(_){ return 0; }
}
function __setDocumentScrollTop__(top){
  const value = Math.max(0, Number(top) || 0);
  try{ window.scrollTo(0, value); }catch(_){ }
  try{ if (document.scrollingElement) document.scrollingElement.scrollTop = value; }catch(_){ }
  try{ document.documentElement.scrollTop = value; }catch(_){ }
  try{ document.body.scrollTop = value; }catch(_){ }
}
function __captureGuestListScrollState__(){
  try{
    const wrap = document.getElementById('guestCards');
    const cards = wrap ? Array.from(wrap.querySelectorAll('.guest-card')) : [];
    const viewportTop = 0;
    let anchorKey = '';
    let anchorOffset = 0;
    for (const card of cards){
      const rect = card.getBoundingClientRect();
      if (rect.bottom > viewportTop + 8){
        anchorKey = String(card.dataset.guestVisualKey || card.dataset.guestId || '').trim();
        anchorOffset = rect.top;
        break;
      }
    }
    state.guestListScrollState = {
      top: __getDocumentScrollTop__(),
      anchorKey,
      anchorOffset,
      ts: Date.now()
    };
    state.guestListScrollRestorePending = true;
  }catch(_){ }
}
function __restoreGuestListScrollState__(){
  try{
    if (state.page !== 'ospiti' || !state.guestListScrollRestorePending) return false;
    const saved = state.guestListScrollState || null;
    if (!saved) {
      state.guestListScrollRestorePending = false;
      return true;
    }

    let targetTop = Math.max(0, Number(saved.top) || 0);
    const wrap = document.getElementById('guestCards');
    const cards = wrap ? Array.from(wrap.querySelectorAll('.guest-card')) : [];
    if (cards.length && saved.anchorKey){
      const anchor = cards.find((card) => String(card.dataset.guestVisualKey || card.dataset.guestId || '').trim() === String(saved.anchorKey || '').trim());
      if (anchor){
        const anchorTop = anchor.getBoundingClientRect().top + __getDocumentScrollTop__();
        const desiredTop = anchorTop - (Number(saved.anchorOffset) || 0);
        if (isFinite(desiredTop)) targetTop = Math.max(0, desiredTop);
      }
    }

    const scrollingEl = document.scrollingElement || document.documentElement || document.body;
    const maxTop = Math.max(0, ((scrollingEl && scrollingEl.scrollHeight) || document.documentElement?.scrollHeight || document.body?.scrollHeight || 0) - (window.innerHeight || document.documentElement?.clientHeight || 0));
    targetTop = Math.min(targetTop, maxTop);
    __setDocumentScrollTop__(targetTop);

    const currentTop = __getDocumentScrollTop__();
    const delta = Math.abs(currentTop - targetTop);
    if (delta <= 2){
      state.guestListScrollRestorePending = false;
      return true;
    }
  }catch(_){ }
  return false;
}
function __scheduleGuestListScrollRestore__(attempt = 0){
  try{
    if (state.page !== 'ospiti' || !state.guestListScrollRestorePending) return;
    requestAnimationFrame(() => {
      const ok = __restoreGuestListScrollState__();
      if (!ok && attempt < 12){
        setTimeout(() => { try{ __scheduleGuestListScrollRestore__(attempt + 1); }catch(_){ } }, 40);
      }
    });
  }catch(_){ }
}

function __licenseAllowsAccess__(){
  try{
    const row = __findUserRowByIdentity__;
  }catch(_){ }
  try{
    const sess = state?.session || loadSession();
    if (!sess || !sess.user_id) return false;
    const info = __licenseStateForRow__(sess || {});
    if (info.valid) return true;
  }catch(_){ }
  try{
    const sess = state?.session || loadSession();
    return !!(sess && (__normBool01(sess?.isMaster) || String(sess?.licenseStatus||'').toLowerCase()==='active' || String(sess?.licenseStatus||'').toLowerCase()==='master') && (!sess.licenseEnd || String(sess.licenseEnd) >= todayISO()));
  }catch(_){ }
  return false;
}
function __pageNeedsLicense__(page){
  try{
    const sess = state?.session || loadSession();
    if (sess && isOperatoreSession(sess)) return false;
  }catch(_){ }
  const allowed = new Set(["auth","home","impostazioni","opsettings"]);
  return !allowed.has(String(page || '').trim().toLowerCase());
}
async function __openSettingsLicenseModalForSession__(){
  try{
    const sess = state?.session || loadSession();
    if (!sess || !sess.user_id) { try{ toast('Accedi prima', 'orange'); }catch(_){ } return; }
    const row = await __findUserRowByIdentity__(sess) || sess;
    const merged = await __refreshSessionFromStorage__(row) || row;
    try{ state.session = merged; saveSession(merged); }catch(_){ }
    const lic = __licenseStateForRow__(row);
    const statusEl = document.getElementById('settingsLicenseStatus');
    const hintEl = document.getElementById('settingsLicenseHint');
    const genBtn = document.getElementById('settingsLicenseGeneratorBtn');
    const reqBtn = document.getElementById('settingsLicenseRequestBtn');
    const payBtn = document.getElementById('settingsLicensePayBtn');
    if (statusEl){
      statusEl.textContent = lic.master ? 'Account master attivo' : (lic.valid ? `Licenza attiva ${lic.start ? formatShortDateIT(lic.start) : ''}${lic.end ? ' → ' + formatShortDateIT(lic.end) : ''}`.trim() : (lic.expired ? 'Licenza scaduta' : 'Licenza non attiva'));
    }
    if (hintEl){ hintEl.textContent = ''; hintEl.hidden = true; }
    if (genBtn) genBtn.hidden = !__isMasterSession__(merged);
    if (reqBtn) reqBtn.hidden = false;
    if (payBtn) payBtn.hidden = false;
    __modalShowById__('settingsLicenseModal');
  }catch(e){ try{ toast(String((e&&e.message)?e.message:e), 'orange'); }catch(_){ } }
}
function showPage(page){
  // Back-compat: vecchia pagina "colazione" ora è "prodotti"
  if (page === "colazione") page = "prodotti";

  // Redirect: grafico/riepilogo ora sono dentro "Spese" (videata unica)
  if (page === "riepilogo" || page === "grafico"){
    page = "spese";
    state.speseView = "list";
  }
  if (page === "spese" && !state.speseView) state.speseView = "list";

  // Gate: senza sessione si rimane in AUTH
  try{
    if (page !== "auth" && (!state.session || !state.session.user_id)) {
      page = "auth";
    }
  }catch(_){ page = "auth"; }

  try{
    if (__pageNeedsLicense__(page) && state.session && !__licenseAllowsAccess__()) {
      try{ toast('Sblocca da Impostazioni → Licenza', 'orange'); }catch(_){ }
      try{ setTimeout(()=>{ __openSettingsLicenseModalForSession__(); }, 40); }catch(_){ }
      page = isOperatoreSession(state.session) ? 'opsettings' : 'impostazioni';
    }
  }catch(_){ }

  // Gate ruolo: operatore vede solo Pulizie / Lavanderia / Calendario
  try{
    if (state.session && isOperatoreSession(state.session)){
      const allowed = new Set(["home","pulizie","lavanderia","calendario","auth","prodotti","colazione","statistiche","statpiscina","laundrycatalog","opsettings"]);
      if (!allowed.has(page)) page = "pulizie";
    }
  }catch(_){ }


  // Token navigazione: impedisce render/loader fuori contesto quando cambi pagina durante fetch
  const navId = ++state.navId;

  const prevPage = state.page;
  if (prevPage === "ospiti" && page === "ospite") {
    try{ __captureGuestListScrollState__(); }catch(_){ }
  }
  if (page === "calendario" && prevPage && prevPage !== "calendario") {
    state._calendarPrev = prevPage;
  }

  try{ closeIrapModal(); }catch(_){ }

state.page = page;
  document.body.dataset.page = page;
  try{ __applyVerticalPageLock__(page); }catch(_){ }

  // Sync footer: nascosto SOLO in Calendario (admin + operatore)
  try{
    const sb = document.getElementById("homeSyncBar");
    const hideSync = (page === "calendario") || (page === "impostazioni") || (page === "opsettings") || (page === "operatori") || (page === "channel") || (page === "laundrycatalog") || (page === "roomsettings") || (page === "tassa") || (page === "orepulizia") || (page === "ospite") || String(page || "").startsWith("stat");
    if (sb) sb.hidden = !!hideSync;
  }catch(_){ }


  try{ __setTopbarCenterLabel__(); }catch(_){}

  try { __rememberPage(page); } catch (_) {}
  document.querySelectorAll(".page").forEach(s => s.hidden = true);
  const el = $(`#page-${page}`);
  if (el) el.hidden = false;
  try{ __scheduleStatLandscapeGraphOnlyRefresh__(); }catch(_){ }
  if (page === "home"){
    // HOME: ricalcola sempre la visibilità del SYNC dopo operazioni in Impostazioni (es. generazione codice Roster)
    try{ __fbLoadLink__(); }catch(_){ }
    try{ applyRoleMode(); }catch(_){ }
    try{ setTimeout(()=>{ try{ __fitHomeSyncBtn__(); }catch(_){ } }, 0); }catch(_){ }
  }

  // Init pagine dinamiche (listener)
  if (page === "tassa"){
    try{ setTimeout(() => { try{ initTassaPage(); }catch(_){} }, 0); }catch(_){ }
  }

  // Impostazioni: aggiorna tabs (account + anno)
  if (page === "impostazioni" || page === "opsettings"){
    try{ updateSettingsTabs(); }catch(_){ }
    try{ __normalizeSettingsActionOrder__(); }catch(_){ }
    if (page === "impostazioni"){ try{ loadImpostazioniPage({ force:true }); }catch(_){ } }
    try{ requestAnimationFrame(() => { try{ __normalizeSettingsActionOrder__(); }catch(_){ } }); }catch(_){ }
  }
  if (page === "operatori"){
    try{ loadOperatoriPage(); }catch(_){ }
  }
  if (page === "channel"){
    try{ loadChannelPage(); }catch(_){ }
  }
  if (page === "laundrycatalog"){
    try{ loadLaundryCatalogPage(); }catch(_){ }
  }
  if (page === "roomsettings"){
    try{ renderRoomSettingsPage(); }catch(_){ }
  }

  // Sotto-viste della pagina Spese (lista ↔ grafico+riepilogo)
  if (page === "spese") {
    try{ __bindSpeseBudgetToggles__(); }catch(_){ }
    try { setSpeseView(state.speseView || "list"); } catch (_) {}
    try{ __bindSpeseDateRangeUi__(); }catch(_){ }
    try{ __syncSpeseDateRangeUi__(); }catch(_){ }
  }

  // Period chip: nascosto in HOME (per rispettare "nessun altro testo" sulla home)
  const chip = $("#periodChip");
  if (chip){
    if (page === "home" || page === "ospite" || page === "ospiti") {
      chip.hidden = true;
    } else {
      chip.hidden = false;
      chip.textContent = `${state.period.from} → ${state.period.to}`;
    }
  }

  try{ setTimeout(() => { try{ __applyAppLanguageToDom__(); }catch(_){ } try{ __updateGuestDateRangeTrigger__(); }catch(_){ } }, 0); }catch(_){ }

  // Topbar: in HOME il tasto "Home" non serve → mostra Impostazioni
  try{
    const hb2 = document.getElementById("hamburgerBtn");
    const hs2 = document.getElementById("homeSettingsTop");
    const leds2 = document.getElementById("topLedGrid") || document.getElementById("prodTopLeds");
    const authImportTop = document.getElementById("authImportBackupTop");
    const isHome = (page === "home");
    const isAuth = (page === "auth");
    const isOp = !!(state.session && isOperatoreSession(state.session));
    if (leds2){
      leds2.hidden = isAuth;
      leds2.classList.toggle('is-operator-leds', !!isOp);
      leds2.classList.toggle('is-admin-leds', !isOp);
    }
    if (hb2) hb2.hidden = isHome || isAuth;
    if (hs2){
      hs2.hidden = !isHome;
      hs2.classList.remove("icon-btn-whiteorange");
      hs2.classList.add("icon-btn-whiteblue");
    }
    if (authImportTop) authImportTop.hidden = !isAuth;
    if (leds2) leds2.hidden = isAuth;
    try{
      ['dbLedRead','dbLedIstat','dbLedWrite','dbLedReceipt'].forEach((id)=>{ const el=document.getElementById(id); if(el) el.hidden = !!isOp; });
      ['prodLedColazione','prodLedPulizia'].forEach((id)=>{ const el=document.getElementById(id); if(el) el.hidden = false; });
    }catch(_){ }
    try{ const opImpTop = document.getElementById("opImportRosterTop"); if (opImpTop) opImpTop.hidden = true; }catch(_){ }
    try{ const opLogoutTopBtn = document.getElementById("opLogoutTop"); if (opLogoutTopBtn) opLogoutTopBtn.hidden = true; }catch(_){ }

    // HOME: refresh totale dati in background (non blocca UI)
    try{ if (isHome){ try{ updateProdottiHomeBlink(); }catch(_){ } refreshAllDataInBackground(); } }catch(_){}

    const ir = document.getElementById("btnIrapTop");
    if (ir) ir.hidden = (page !== "statazienda");

  }catch(_){ }

  // HOME: ricevute indicator
  try{ updateHomeReceiptsIndicator(); }catch(_){ }
  try{ refreshTopGuestAlerts({ force:false, keepModal:true }); }catch(_){ }

  // Top back button (Ore pulizia + Calendario)
  const backBtnTop = $("#backBtnTop");
  if (backBtnTop){
    backBtnTop.hidden = !(page === "operatori" || page === "channel" || page === "laundrycatalog" || page === "roomsettings");
    backBtnTop.classList.toggle("icon-btn-whiteblue", page === "operatori" || page === "channel" || page === "laundrycatalog" || page === "roomsettings");
    backBtnTop.classList.toggle("icon-btn-whiteorange", false);
    try{ backBtnTop.setAttribute("aria-label", "Torna a Impostazioni"); }catch(_){ }
  }

  // Top guest list button (solo scheda Ospite) — torna alla lista ospiti accanto al tasto Home
  const guestBackTop = $("#guestBackTop");
  if (guestBackTop){
    guestBackTop.hidden = (page !== "ospite");
  }

  // HOME operatore: top bar con solo il tasto Impostazioni
  try{
    const opLogout = document.getElementById("opLogoutTop");
    if (opLogout) opLogout.hidden = true;
  }catch(_){ }

  try{
    const opImp = document.getElementById("opImportRosterTop");
    if (opImp) opImp.hidden = true;
  }catch(_){ }

  // Top tools (solo Pulizie) — lavanderia + ore lavoro accanto al tasto Home
  const pulizieTopTools = $("#pulizieTopTools");
  if (pulizieTopTools){
    pulizieTopTools.hidden = true;
  }
  const goOrePulizia = $("#goOrePulizia");
  if (goOrePulizia){
    goOrePulizia.hidden = (page !== "pulizie" || (state && state.session && isOperatoreSession(state.session)));
  }

  // Top tools (Lavanderia) — genera report accanto al tasto Home
  const lavanderiaTopTools = $("#lavanderiaTopTools");
  if (lavanderiaTopTools){
    lavanderiaTopTools.hidden = (page !== "lavanderia");
  }


  // Top tools (Ospiti) — nuovo ospite + calendario accanto al tasto Home
  const ospitiTopTools = $("#ospitiTopTools");
  if (ospitiTopTools){
    ospitiTopTools.hidden = (page !== "ospiti");
  }

  
  // Top tools (Spese) — + e grafico accanto al tasto Home
  const speseTopTools = $("#speseTopTools");
  if (speseTopTools){
    speseTopTools.hidden = (page !== "spese");
  }

  

  // Top tools (Prodotti) — inserisci/azzera/salva accanto al tasto Home
  const prodottiTopTools = $("#prodottiTopTools");
  if (prodottiTopTools){
    prodottiTopTools.hidden = (page !== "prodotti");
  }

// Top tools (Statistiche → Conteggio generale)
  const statGenTopTools = $("#statGenTopTools");
  if (statGenTopTools){
    statGenTopTools.hidden = (page !== "statgen");
  }

  // Top tools (Statistiche → Fatturati mensili)
  const statMensiliTopTools = $("#statMensiliTopTools");
  if (statMensiliTopTools){
    statMensiliTopTools.hidden = (page !== "statmensili");
  }

  // Top tools (Statistiche → Spese generali)
  const statSpeseTopTools = $("#statSpeseTopTools");
  if (statSpeseTopTools){
    statSpeseTopTools.hidden = (page !== "statspese");
  }

  const statPrenTopTools = $("#statPrenTopTools");
  if (statPrenTopTools){
    statPrenTopTools.hidden = (page !== "statprenotazioni");
  }

  const statChannelTopTools = $("#statChannelTopTools");
  if (statChannelTopTools){
    statChannelTopTools.hidden = (page !== "statchannel");
  }

  const statPulizieTopTools = $("#statPulizieTopTools");
  if (statPulizieTopTools){
    statPulizieTopTools.hidden = (page !== "statpulizie");
  }

  const orePuliziaTopTools = $("#orePuliziaTopTools");
  if (orePuliziaTopTools){
    orePuliziaTopTools.hidden = (page !== "orepulizia");
  }

  const statCancTopTools = $("#statCancTopTools");
  if (statCancTopTools){
    statCancTopTools.hidden = (page !== "statcancellazioni");
  }


  const statAziendaTopTools = $("#statAziendaTopTools");
  if (statAziendaTopTools){
    statAziendaTopTools.hidden = (page !== "statazienda");
  }

  const statAmmTopTools = $("#statAmmTopTools");
  if (statAmmTopTools){
    statAmmTopTools.hidden = (page !== "statamministratore");
  }

  const statPiscinaTopTools = $("#statPiscinaTopTools");
  if (statPiscinaTopTools){
    statPiscinaTopTools.hidden = (page !== "statpiscina");
  }

  try{ __syncTopbarCenterLayout__(); }catch(_){ }
  try{ __updateGuestArrivoTodayButtonVisibility__(); }catch(_){ }

  try{ ensureTopbarIconContrast(); }catch(_){ }

// render on demand
  if (page === "prodotti") {
    const _nav = navId;
    loadProdotti({ force:false, showLoader:true })
      .then(()=>{ if (state.navId !== _nav || state.page !== "prodotti") return; renderProdotti(); })
      .catch(e=>toast(e.message));
  }

  if (page === "spese") {
    const _nav = navId;
    ensurePeriodData({ showLoader:true })
      .then(()=>{ if (state.navId !== _nav || state.page !== "spese") return; renderSpese(); })
      .catch(e=>toast(e.message));
  }
  if (page === "riepilogo") {
    const _nav = navId;
    ensurePeriodData({ showLoader:true })
      .then(()=>{ if (state.navId !== _nav || state.page !== "riepilogo") return; renderRiepilogo(); })
      .catch(e=>toast(e.message));
  }
  if (page === "grafico") {
    const _nav = navId;
    ensurePeriodData({ showLoader:true })
      .then(()=>{ if (state.navId !== _nav || state.page !== "grafico") return; renderGrafico(); })
      .catch(e=>toast(e.message));
  }
  if (page === "calendario") {
    const _nav = navId;
    // Entrando in Calendario vogliamo SEMPRE dati freschi.
    // 1) invalida lo stato "ready" e bypassa la cache in-memory (ttl) con force:true.
    try{ if (state.calendar) state.calendar.ready = false; }catch(_){ }
    ensureCalendarData({ force:true, showLoader:false })
      .then(()=>{ if (state.navId !== _nav || state.page !== "calendario") return; renderCalendario(); })
      .catch(e=>toast(e.message));
  }
  if (page === "ospiti") {
    // Difesa anti-stato sporco: quando torno alla lista, la scheda ospite NON deve restare in "view"
    // (layout diverso) o con valori vecchi.
    try { enterGuestCreateMode(); } catch (_) {}
    loadOspiti(state.period || {}).catch(e => toast(e.message));
    try{ if (prevPage === "ospite" && state.guestListScrollRestorePending) __scheduleGuestListScrollRestore__(); }catch(_){ }
  }
  if (page === "lavanderia") loadLavanderia().catch(e => toast(e.message));

  if (page === "statistiche") {
    try{ closeStatPieModal(); }catch(_){ }
    try{ closeStatSpesePieModal(); }catch(_){ }
    try{ closeStatMensiliPieModal(); }catch(_){ }
  }

  if (page === "statgen") {
    const _nav = navId;
    Promise.all([
      ensureStatsAllData({ showLoader:true }),
      loadOspiti({ ...(state.period || {}), force:false }),
      cachedGet("servizi", {}, { showLoader:false, ttlMs: 2*60*1000, swrMs: 10*60*1000, force:false }),
    ])
      .then(([, , servizi])=>{ if (state.navId !== _nav || state.page !== "statgen") return; try{ state.servizi = normalizeServiziResponse(servizi); }catch(_){ state.servizi = []; } renderStatGen(); })
      .catch(e=>toast(e.message));
  }

  if (page === "statmensili") {
    const _nav = navId;
    Promise.all([
      ensureStatsAllData({ showLoader:true }),
      loadOspiti({ ...(state.period || {}), force:false }),
      cachedGet("servizi", {}, { showLoader:false, ttlMs: 2*60*1000, swrMs: 10*60*1000, force:false }),
    ])
      .then(([, , servizi])=>{ if (state.navId !== _nav || state.page !== "statmensili") return; try{ state.servizi = normalizeServiziResponse(servizi); }catch(_){ state.servizi = []; } renderStatMensili(); })
      .catch(e=>toast(e.message));
  }


  if (page === "statspese") {
    const _nav = navId;
    ensureStatsAllData({ showLoader:true })
      .then(()=>{ if (state.navId !== _nav || state.page !== "statspese") return; renderStatSpese(); })
      .catch(e=>toast(e.message));
  }

  if (page === "statprenotazioni") {
    const _nav = navId;
    Promise.all([
      ensureStatsAllData({ showLoader:true }),
      loadOspiti({ ...(state.period || {}), force:false })
    ])
      .then(()=>{ if (state.navId !== _nav || state.page !== "statprenotazioni") return; renderStatRicevute(); })
      .catch(e=>toast(e.message));
  }

  if (page === "statchannel") {
    const _nav = navId;
    Promise.all([
      ensureStatsAllData({ showLoader:true }),
      loadOspiti({ ...(state.period || {}), force:false })
    ])
      .then(()=>{ if (state.navId !== _nav || state.page !== "statchannel") return; renderStatChannel(); })
      .catch(e=>toast(e.message));
  }

  if (page === "statpulizie") {
    const _nav = navId;
    Promise.all([
      ensureStatsAllData({ showLoader:true }),
      __loadOperatoriRows_().catch(()=>[])
    ])
      .then((res)=>{ if (state.navId !== _nav || state.page !== "statpulizie") return; renderStatPulizie(Array.isArray(res && res[1]) ? res[1] : []); })
      .catch(e=>toast(e.message));
  }

  if (page === "statcancellazioni") {
    const _nav = navId;
    Promise.all([
      ensureStatsAllData({ showLoader:true }),
      loadOspiti({ ...(state.period || {}), force:false }),
      loadOspitiEliminati({ ...(state.period || {}), force:false }),
    ])
      .then(()=>{ if (state.navId !== _nav || state.page !== "statcancellazioni") return; renderStatCancellazioni(); })
      .catch(e=>toast(e.message));
  }

  if (page === "statazienda") {
    const _nav = navId;
    Promise.all([
      ensureStatsAllData({ showLoader:true }),
      loadOspiti({ ...(state.period || {}), force:false }),
      loadOspitiEliminati({ ...(state.period || {}), force:false }),
    ])
      .then(()=>{ if (state.navId !== _nav || state.page !== "statazienda") return; renderStatAzienda(); })
      .catch(e=>toast(e.message));
  }

  if (page === "statamministratore") {
    const _nav = navId;
    Promise.resolve()
      .then(()=>{ if (state.navId !== _nav || state.page !== "statamministratore") return; renderStatAmministratore(); })
      .catch(e=>toast(e.message));
  }

  if (page === "statpiscina") {
    const _nav = navId;
    // Render immediato (anche se il fetch fallisce, la pagina deve aprirsi)
    try { renderPiscinaCalendar(); } catch (_) {}
    Promise.resolve(loadPiscinaAll({ force:false, showLoader:true }))
      .then(()=>{ if (state.navId !== _nav || state.page !== "statpiscina") return; renderPiscinaCalendar(); })
      .catch(e=>toast(e.message));
  }


if (page === "orepulizia") { initOrePuliziaPage().catch(e=>toast(e.message)); }


  // dDAE_1.020: fallback visualizzazione Pulizie
  try{
    if (page === "pulizie"){
      const el = document.getElementById("page-pulizie");
      if (el) el.style.display = "block";
      try{ if (window.__syncCleanOperators__) window.__syncCleanOperators__(); }catch(_){ }
      try{
        setTimeout(() => {
          try{ if (window.__ddae_refreshPulizieGrid) window.__ddae_refreshPulizieGrid({ forceReload:false, includeOperatori:true }); }catch(_){ }
          try{ if (window.__ddae_refreshPulizieOperatori) window.__ddae_refreshPulizieOperatori({ clearFirst:false }); }catch(_){ }
        }, 0);
        setTimeout(() => {
          try{ if (state && state.page === "pulizie" && window.__ddae_refreshPulizieOperatori) window.__ddae_refreshPulizieOperatori({ clearFirst:false }); }catch(_){ }
        }, 250);
      }catch(_){ }
    }
  }catch(_){}

  // Palette icone (launcher)
  applyIconPalette();
  try{ __launcherIconApplyAll__(); }catch(_){}


}

function setupHeader(){
  const hb = $("#hamburgerBtn");
  if (hb) hb.addEventListener("click", () => { hideLauncher(); showPage("home"); });


  const authImportTop = document.getElementById("authImportBackupTop");
  if (authImportTop) bindFastTap(authImportTop, async () => {
    try{ localStorage.setItem(__AUTH_BACKUP_IMPORT_ACTIVE_KEY, "1"); }catch(_){ }
    try{ localStorage.setItem(__AUTH_BACKUP_FORCE_ADMIN_KEY, "1"); }catch(_){ }
    try{ await __dbImport__("admin"); }catch(e){ try{ toast("Errore import", "orange"); }catch(_){ } }
  });

  const opImpRoster = document.getElementById("opImportRosterTop");
  if (opImpRoster) bindFastTap(opImpRoster, async () => { try{ await __qrScanAndLink__(); }catch(e){ try{ toast("Codice non disponibile", "orange"); }catch(_){ } } });

// HOME: ricevute mancanti (solo in HOME)
  const btnRec = document.getElementById("homeReceiptsTop");
  if (btnRec) bindFastTap(btnRec, () => { openReceiptDueModal(); });

  const recClose = document.getElementById("receiptDueClose");
  if (recClose) bindFastTap(recClose, () => closeReceiptDueModal());
  const recModal = document.getElementById("receiptDueModal");
  if (recModal) recModal.addEventListener("click", (e)=>{ if (e.target === recModal) closeReceiptDueModal(); });

  const guestLedLeft = document.getElementById("dbLedRead");
  if (guestLedLeft) bindFastTap(guestLedLeft, () => openGuestAlertModal('ps'));
  const guestLedIstat = document.getElementById("dbLedIstat");
  if (guestLedIstat) bindFastTap(guestLedIstat, () => openGuestAlertModal('istat'));
  const guestLedRight = document.getElementById("dbLedWrite");
  if (guestLedRight) bindFastTap(guestLedRight, () => openGuestAlertModal('payment'));
  const guestLedReceipt = document.getElementById("dbLedReceipt");
  if (guestLedReceipt) bindFastTap(guestLedReceipt, () => openGuestAlertModal('receipt'));
  const guestAlertClose = document.getElementById("guestAlertClose");
  if (guestAlertClose) bindFastTap(guestAlertClose, () => closeGuestAlertModal());
  const guestAlertModal = document.getElementById("guestAlertModal");
  if (guestAlertModal) guestAlertModal.addEventListener("click", (e)=>{ if (e.target === guestAlertModal) closeGuestAlertModal(); });

  // AMMINISTRATORE: popup dati + grafico
  const btnAdminInputsTop = document.getElementById("btnAdminInputsTop");
  if (btnAdminInputsTop) bindFastTap(btnAdminInputsTop, () => { openAdminInputsModal(); });
  const btnAdminChartTop = document.getElementById("btnAdminChartTop");
  if (btnAdminChartTop) bindFastTap(btnAdminChartTop, () => { openAdminChartModal(); });
  const adminInputsClose = document.getElementById("adminInputsClose");
  if (adminInputsClose) bindFastTap(adminInputsClose, () => closeAdminInputsModal());
  const adminChartClose = document.getElementById("adminChartClose");
  if (adminChartClose) bindFastTap(adminChartClose, () => closeAdminChartModal());
  const adminInputsModal = document.getElementById("adminInputsModal");
  if (adminInputsModal) adminInputsModal.addEventListener("click", (e)=>{ if (e.target === adminInputsModal) closeAdminInputsModal(); });
  const adminChartModal = document.getElementById("adminChartModal");
  if (adminChartModal) adminChartModal.addEventListener("click", (e)=>{ if (e.target === adminChartModal) closeAdminChartModal(); });

  const roomsReadOnly = document.getElementById("roomsReadOnly");
  if (roomsReadOnly && !roomsReadOnly.__boundGuestGroupInfo){
    roomsReadOnly.__boundGuestGroupInfo = true;
    roomsReadOnly.addEventListener("click", (e) => {
      try{
        if (String(state.guestMode || '') !== 'view') return;
        const block = e.target && e.target.closest ? e.target.closest('.guest-booking-block[data-booking-id]') : null;
        if (!block || !roomsReadOnly.contains(block)) return;
        e.preventDefault();
        e.stopPropagation();
        __toggleGuestGroupInfoForBooking__(block.getAttribute('data-booking-id') || '');
      }catch(_){ }
    });
    roomsReadOnly.addEventListener("keydown", (e) => {
      try{
        if (e.key !== 'Enter' && e.key !== ' ' && e.key !== 'Spacebar') return;
        const block = e.target && e.target.closest ? e.target.closest('.guest-booking-block[data-booking-id]') : null;
        if (!block || !roomsReadOnly.contains(block)) return;
        e.preventDefault();
        __toggleGuestGroupInfoForBooking__(block.getAttribute('data-booking-id') || '');
      }catch(_){ }
    });
  }

  const btnIrapTop = document.getElementById("btnIrapTop");
  if (btnIrapTop) bindFastTap(btnIrapTop, () => { openIrapModal(); });



  const opLogout = document.getElementById("opLogoutTop");
  if (opLogout) bindFastTap(opLogout, async () => {
    let ok = false;
    try{ ok = await confirmYesNo("Vuoi uscire?"); }catch(_){ ok = false; }
    if (!ok) return;
    try{ clearSession(); }catch(_){ }
    try{ state.session = null; }catch(_){ }
    try{ applyRoleMode(); }catch(_){ }
    try{ __resetInMemoryData__(); }catch(_){ }
    try{ invalidateApiCache(); }catch(_){ }
    try{ showPage("auth"); }catch(_){ }
  });

  // Back (ore pulizia + calendario + operatori)
  const bb = $("#backBtnTop");
  if (bb) bb.addEventListener("click", () => {
    if (state.page === "orepulizia") { showPage("pulizie"); return; }
    if (state.page === "operatori" || state.page === "channel" || state.page === "laundrycatalog" || state.page === "roomsettings") { showPage("impostazioni"); return; }
    if (state.page === "calendario") {
      if (state.session && isOperatoreSession(state.session)) { showPage("pulizie"); return; }
      showPage("ospiti");
      return;
    }
    showPage("home");
  });
}
function setupHome(){
  bindLauncherDelegation();
  bindHomeDelegation();
  // iOS hardening: fallback binding for Home icons
  try{ bindHomeStrongTap(); }catch(_){ }
  // stampa build
  const build = $("#buildText");
  if (build) build.textContent = `dDAE_${BUILD_VERSION}`;

  
  // Home: Import/Export SYNC (Firebase) — silente (no file)
  try{
    const imp = document.getElementById("goDbImport");
    if (imp && !imp.__syncBound){ imp.__syncBound = true; bindFastTap(imp, async ()=>{ try{ await __handleSyncImport__(); }catch(e){ try{ toast("Sync non disponibile", "orange"); }catch(_){ } } }); }
    const exp = document.getElementById("goDbExport");
    if (exp && !exp.__syncBound){ exp.__syncBound = true; bindFastTap(exp, async ()=>{ try{ await __handleSyncExport__(); }catch(e){ try{ toast("Sync non disponibile", "orange"); }catch(_){ } } }); }
  }catch(_){ }
// SPESE: pulsante + (nuova spesa) e pulsante grafico+riepilogo e pulsante grafico+riepilogo
  const btnAdd = $("#btnAddSpesa");
  if (btnAdd){
    bindFastTap(btnAdd, () => { try{ __exitSpesaEditMode__(); resetInserisci(); __syncSpeseBudgetToggles__(); }catch(_){} hideLauncher(); showPage("inserisci"); });
  }
  try{ __bindSpeseBudgetToggles__(); }catch(_){ }
  const btnInsights = $("#btnSpeseInsights");
  if (btnInsights){
    btnInsights.hidden = true;
  }

  // HOME: tasto Spese apre direttamente la pagina "spese" (senza launcher)
  const openBtn = $("#openLauncher");
  if (openBtn){
    bindFastTap(openBtn, () => { try{ setSpeseView("list"); }catch(_){} hideLauncher(); showPage("spese"); });
  }

  // HOME: icona Ospite va alla pagina ospite
  const goO = $("#goOspite");
  if (goO){
    bindFastTap(goO, () => { hideLauncher(); showPage("ospiti"); });
  }
  // HOME: icona Ospiti va alla pagina elenco ospiti
  const goOs = $("#goOspiti");
  if (goOs){
    bindFastTap(goOs, () => { hideLauncher(); showPage("ospiti"); });
  }


// OSPITI: pulsante + (nuovo ospite)
const btnNewGuestOspiti = $("#btnNewGuestOspiti");
if (btnNewGuestOspiti){
  btnNewGuestOspiti.addEventListener("click", () => { enterGuestCreateMode(); showPage("ospite"); });
}


// OSPITI: topbar — nuovo ospite + calendario
const btnNewGuestTop = $("#btnNewGuestTop");
if (btnNewGuestTop){
  btnNewGuestTop.addEventListener("click", () => { enterGuestCreateMode(); showPage("ospite"); });
}
const guestScrollTodayBtn = document.getElementById("guestScrollTodayBtn");
if (guestScrollTodayBtn){
  guestScrollTodayBtn.addEventListener("click", () => __scrollGuestCardsToToday__());
}



  

  // OSPITE: topbar — torna alla lista ospiti (guest list)
  const guestBackTop = $("#guestBackTop");
  if (guestBackTop){
    const goGuestBack = () => {
      // Se stai modificando un ospite: torna alla scheda in sola lettura dello stesso record
      try{
        if (state.page === "ospite" && String(state.guestMode || "") === "edit"){
          const src = state.guestEditSourceItem || null;
          if (src){
            try{ enterGuestViewMode(src); }catch(_){ }
            try{ showPage("ospite"); }catch(_){ }
            return;
          }

          // fallback: prova a recuperare il record da multi-booking o lista ospiti
          const id = String(state.guestEditId || "").trim();
          let found = null;
          if (id){
            try{
              const arr = Array.isArray(state.guestGroupBookings) ? state.guestGroupBookings : [];
              found = arr.find(x => String(guestIdOf(x) || x?.id || "") === id) || null;
            }catch(_){ }
            if (!found){
              try{
                const arr2 = Array.isArray(state.ospiti) ? state.ospiti : [];
                found = arr2.find(x => String(x?.id || "") === id) || null;
              }catch(_){ }
            }
          }
          if (found){
            try{ enterGuestViewMode(found); }catch(_){ }
            try{ showPage("ospite"); }catch(_){ }
            return;
          }
        }
      }catch(_){ }

      // Default: torna alla lista ospiti
      try{ state.guestGroupBookings = null; state.guestGroupActiveId = null; state.guestGroupKey = null; clearGuestMulti(); }catch(_){ }
      showPage("ospiti");
    };
    bindFastTap(guestBackTop, goGuestBack);
    guestBackTop.addEventListener("click", (e) => { try{ e.preventDefault(); }catch(_){ } });
  }
// HOME: icona Colazione
  const goCol = $("#goProdotti");
  if (goCol){
    bindFastTap(goCol, () => { hideLauncher(); showPage("prodotti"); });
  }


  // HOME: Impostazioni (top)
  const hsTop = document.getElementById("homeSettingsTop");
  if (hsTop){
    const goHomeSettings = (ev) => {
      try{ if (ev){ ev.preventDefault(); ev.stopPropagation(); } }catch(_){ }
      try{ hsTop.disabled = false; }catch(_){ }
      try{ hsTop.style.pointerEvents = "auto"; }catch(_){ }
      hideLauncher();
      showPage((state.session && isOperatoreSession(state.session)) ? "opsettings" : "impostazioni");
    };
    bindFastTap(hsTop, goHomeSettings);
    if (!hsTop.__settingsStrongBound){
      hsTop.__settingsStrongBound = true;
      ["click","touchend","pointerup"].forEach((evtName) => {
        try{ hsTop.addEventListener(evtName, goHomeSettings, { passive:false, capture:true }); }catch(_){ try{ hsTop.addEventListener(evtName, goHomeSettings, true); }catch(__){} }
      });
    }
  }

  // HOME: icona Calendario (tap-safe su iOS PWA)
  const goCal = $("#goCalendario");
  if (goCal){
    goCal.disabled = false;
    goCal.removeAttribute("aria-disabled");
    bindFastTap(goCal, () => { hideLauncher(); showPage("calendario"); });
  }

  // HOME: icona Pulizie
  const goPul = $("#goPulizie");
  if (goPul){
    bindFastTap(goPul, () => { hideLauncher(); showPage("pulizie"); });
  }

  // HOME: icona Lavanderia (anche pulsante top)
  const goLav = $("#goLavanderia");
  if (goLav){
    bindFastTap(goLav, () => { hideLauncher(); showPage("lavanderia"); });
  }
  const goOrePulHome = $("#goOrePuliziaHome");
  if (goOrePulHome){
    bindFastTap(goOrePulHome, () => { hideLauncher(); showPage("statpiscina"); });
  }
  try{ __applyHomeIconGradients__(); }catch(_){ }
  try{
    const laundryDetailClose = document.getElementById('laundryDetailClose');
    const laundryDetailModal = document.getElementById('laundryDetailModal');
    if (laundryDetailClose && !laundryDetailClose.__boundLaundryDetail){
      laundryDetailClose.__boundLaundryDetail = true;
      bindFastTap(laundryDetailClose, () => { __closeLaundryDetailModal__(); });
    }
    if (laundryDetailModal && !laundryDetailModal.__boundLaundryBackdrop){
      laundryDetailModal.__boundLaundryBackdrop = true;
      laundryDetailModal.addEventListener('click', (ev) => { if (ev.target === laundryDetailModal) __closeLaundryDetailModal__(); });
    }
    if (!document.body.__boundLaundryDetailEsc){
      document.body.__boundLaundryDetailEsc = true;
      document.addEventListener('keydown', (ev) => { if (ev.key === 'Escape') { __closeLaundryDetailModal__(); __closeLaundryPricesModal__(); } });
    }
  }catch(_){ }
  const goLavTop = $("#goLavanderiaTop");
  if (goLavTop){
    bindFastTap(goLavTop, () => { hideLauncher(); showPage("lavanderia"); });
  }

  // HOME: ore pulizie (se presente)
  const goOrePul = $("#goOrePulizia");
  if (goOrePul){
    bindFastTap(goOrePul, () => { hideLauncher(); showPage("orepulizia"); });
  }
  const goOrePulTop = $("#goOrePuliziaTop");
  if (goOrePulTop){
    bindFastTap(goOrePulTop, () => { hideLauncher(); showPage("orepulizia"); });
  }

  // HOME: tassa soggiorno (se presente)
  const goTassa = $("#goTassaSoggiorno");
  if (goTassa){
    bindFastTap(goTassa, async () => {
      hideLauncher();
      try{ await ensureSettingsLoaded({ force:false, showLoader:false }); }catch(_){ }
      showPage("tassa");
      try{ initTassaPage(); }catch(_){ }
    });
  }

  // HOME: Statistiche
  const goG = $("#goStatistiche");
  if (goG){
    bindFastTap(goG, () => { hideLauncher(); showPage("statistiche"); });
  }

  // STATISTICHE: icone
  const s1 = $("#goStatGen");
  if (s1){ bindFastTap(s1, () => { hideLauncher(); showPage("statgen"); }); }
  const s2 = $("#goStatMensili");
  if (s2){ bindFastTap(s2, () => { hideLauncher(); showPage("statmensili"); }); }
  const s3 = $("#goStatSpese");
  if (s3){ bindFastTap(s3, () => { hideLauncher(); showPage("statspese"); }); }
  const s4 = $("#goStatRicevute");
  if (s4){ bindFastTap(s4, () => { hideLauncher(); showPage("statprenotazioni"); }); }
  const s4b = $("#goStatChannel");
  if (s4b){ bindFastTap(s4b, () => { hideLauncher(); showPage("statchannel"); }); }
  const s4c = $("#goStatPulizie");
  if (s4c){ bindFastTap(s4c, () => { hideLauncher(); showPage("statpulizie"); }); }

  
  const s5 = $("#goStatAzienda");
  if (s5){ bindFastTap(s5, () => { hideLauncher(); showPage("statazienda"); }); }
  const s6 = $("#goStatAmministratore");
  if (s6){ bindFastTap(s6, () => { hideLauncher(); showPage("statamministratore"); }); }
  const s7 = $("#goStatPiscina");
  if (s7){ bindFastTap(s7, () => { hideLauncher(); showPage("orepulizia"); }); }
  const s8 = $("#goStatCancellazioni");
  if (s8){ bindFastTap(s8, () => { hideLauncher(); showPage("statcancellazioni"); }); }
// STATGEN: topbar tools
  const btnBackStats = $("#btnBackStatistiche");
  if (btnBackStats){ bindFastTap(btnBackStats, () => { closeStatPieModal(); showPage("statistiche"); }); }
  // STATMENSILI: topbar tools
  const btnBackStatsMensili = $("#btnBackStatisticheMensili");
  if (btnBackStatsMensili){ bindFastTap(btnBackStatsMensili, () => { closeStatMensiliPieModal(); showPage("statistiche"); }); }
  const btnBackStatsRicevute = $("#btnBackStatistichePrenotazioni");
  if (btnBackStatsRicevute){ bindFastTap(btnBackStatsRicevute, () => { showPage("statistiche"); }); }
  const btnBackStatsChannel = $("#btnBackStatisticheChannel");
  if (btnBackStatsChannel){ bindFastTap(btnBackStatsChannel, () => { showPage("statistiche"); }); }
  const btnBackStatsPulizie = $("#btnBackStatistichePulizie");
  if (btnBackStatsPulizie){ bindFastTap(btnBackStatsPulizie, () => { showPage("statistiche"); }); }
  const btnBackStatsOrePulizia = $("#btnBackStatisticheOrePulizia");
  if (btnBackStatsOrePulizia){ bindFastTap(btnBackStatsOrePulizia, () => { showPage("statistiche"); }); }

  const btnPieMensili = $("#btnStatMensiliPie");
  if (btnPieMensili){ bindFastTap(btnPieMensili, () => { openStatMensiliPieModal(); }); }
  const statMensiliPieClose = $("#statMensiliPieClose");
  if (statMensiliPieClose){ bindFastTap(statMensiliPieClose, () => closeStatMensiliPieModal()); }
  const statMensiliPieModal = $("#statMensiliPieModal");
  if (statMensiliPieModal){
    statMensiliPieModal.addEventListener("click", (e)=>{
      if (e.target === statMensiliPieModal) closeStatMensiliPieModal();
    });
  }




  const btnPie = $("#btnStatPie");
  if (btnPie){ bindFastTap(btnPie, () => { openStatPieModal(); }); }
  const statPieClose = $("#statPieClose");
  if (statPieClose){ bindFastTap(statPieClose, () => closeStatPieModal()); }
  const statPieModal = $("#statPieModal");
  if (statPieModal){
    statPieModal.addEventListener("click", (e)=>{
      if (e.target === statPieModal) closeStatPieModal();
    });
  }

  // STATISTICHE: Spese generali topbar tools
  const btnBackStatsSpese = $("#btnBackStatisticheSpese");
  if (btnBackStatsSpese){ bindFastTap(btnBackStatsSpese, () => { closeStatSpesePieModal(); showPage("statistiche"); }); }
  const btnBackStatsPren = $("#btnBackStatistichePrenotazioni");
  if (btnBackStatsPren){ bindFastTap(btnBackStatsPren, () => { showPage("statistiche"); }); }
  const btnBackStatsCanc = $("#btnBackStatisticheCancellazioni");
  if (btnBackStatsCanc){ bindFastTap(btnBackStatsCanc, () => { showPage("statistiche"); }); }
  const btnBackStatsAzienda = $("#btnBackStatisticheAzienda");
  if (btnBackStatsAzienda){ bindFastTap(btnBackStatsAzienda, () => { showPage("statistiche"); }); }
  const btnBackStatsAmm = $("#btnBackStatisticheAmministratore");
  if (btnBackStatsAmm){ bindFastTap(btnBackStatsAmm, () => { showPage("statistiche"); }); }

  // STATPISCINA: topbar tools
  const btnBackStatsPiscina = $("#btnBackStatistichePiscina");
  if (btnBackStatsPiscina){ bindFastTap(btnBackStatsPiscina, () => { showPage("statistiche"); }); }
  const btnPiscinaBackfillTop = $("#btnPiscinaBackfillTop");
  const btnPiscinaResetMonthTop = $("#piscinaResetMonthBtn");
  const btnPiscinaSimToday = $("#piscinaSimTodayBtn");
  if (btnPiscinaSimToday){
    bindFastTap(btnPiscinaSimToday, () => {
      try{ piscinaOpenEditTodayModal(); }catch(e){ toast(e?.message || "Errore"); }
    });
  }


  if (btnPiscinaBackfillTop){ bindFastTap(btnPiscinaBackfillTop, () => { try{ piscinaBackfillCurrentMonth(); }catch(e){ toast(e.message||"Errore"); } }); }

const btnPieSpese = $("#btnStatSpesePie");
  if (btnPieSpese){ bindFastTap(btnPieSpese, () => { openStatSpesePieModal(); }); }

  const statSpesePieClose = $("#statSpesePieClose");
  if (statSpesePieClose){ bindFastTap(statSpesePieClose, () => closeStatSpesePieModal()); }
  const statSpesePieModal = $("#statSpesePieModal");
  if (statSpesePieModal){
    statSpesePieModal.addEventListener("click", (e)=>{
      if (e.target === statSpesePieModal) closeStatSpesePieModal();
    });
  }


  // Escape chiude il launcher
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") hideLauncher();
  });
}



/* dDAE_2.863 — Filtri Ospiti ridotti a: Tutti / Oggi */
function __ddae2862NormalizeFilterMode__(value, scope){
  const raw = String(value || '').trim().toLowerCase();
  const mode = (raw === 'all') ? 'all' : 'today';
  try{
    if (scope === 'guest'){
      state.guestArrivalFilter = mode;
      try{ localStorage.setItem('dDAE_guestArrivalFilter', mode); }catch(_){}
      try{ localStorage.removeItem('dDAE_guestTodayOnly'); }catch(_){}
    }
  }catch(_){}
  return mode;
}

function setupGuestListControls(){
  const sortSel = $("#guestSortBy");
  const dirBtn = $("#guestSortDir");
  const todayBtn = $("#guestToday");
  const preventiviBtn = $("#guestPreventivi");
  const sortButtons = Array.from(document.querySelectorAll(".gf-sort-btn[data-sort-by]"));
  if (!sortSel) return;

  const savedBy = localStorage.getItem("dDAE_guestSortBy");
  const savedDir = localStorage.getItem("dDAE_guestSortDir");
  state.guestSortBy = savedBy || state.guestSortBy || "arrivo";
  state.guestSortDir = savedDir || state.guestSortDir || "asc";

  const syncSortSelect = () => {
    try { sortSel.value = state.guestSortBy; } catch(_) {}
  };

  let lastSortTapBy = "";
  let lastSortTapTs = 0;
  const SORT_DOUBLE_TAP_MS = 360;

  const paintSortButtons = () => {
    sortButtons.forEach((btn) => {
      const active = String(btn.dataset.sortBy || "") === String(state.guestSortBy || "");
      btn.classList.toggle("is-active", active);
      btn.setAttribute("aria-pressed", active ? "true" : "false");
      try { __translateTree__(btn); } catch(_) {}
    });
    try{ __applyGuestFilterButtonVisuals__(); }catch(_){ }
    try{ __updateGuestArrivoTodayButtonVisibility__(); }catch(_){ }
  };

  const paintDir = () => {
    if (!dirBtn) return;
    const asc = (state.guestSortDir !== "desc");
    dirBtn.textContent = asc ? "↑" : "↓";
    dirBtn.setAttribute("aria-pressed", asc ? "false" : "true");
  };
  paintDir();

  const savedArrivalFilter = localStorage.getItem("dDAE_guestArrivalFilter");
  const savedToday = localStorage.getItem("dDAE_guestTodayOnly");
  state.guestArrivalFilter = __ddae2862NormalizeFilterMode__(
    (savedArrivalFilter === "today" || savedArrivalFilter === "all")
      ? savedArrivalFilter
      : ((savedToday === "1") ? "today" : (state.guestArrivalFilter || "today")),
    'guest'
  );

  try{ state.guestShowPreventiviOnly = (localStorage.getItem("dDAE_guestShowPreventiviOnly") === "1"); }catch(_){ state.guestShowPreventiviOnly = false; }

  try{ __ensureGuestArrivalFilterVisualStates__(); }catch(_){ }

  const guestArrivalFilterLabelKey = () => {
    const mode = __ddae2862NormalizeFilterMode__(state.guestArrivalFilter || 'today', 'guest');
    return mode === 'all' ? 'Tutti' : 'Oggi';
  };

  const paintToday = () => {
    if (!todayBtn) return;
    const mode = __ddae2862NormalizeFilterMode__(state.guestArrivalFilter || 'today', 'guest');
    const labelKey = guestArrivalFilterLabelKey();
    todayBtn.classList.toggle("is-active", mode !== 'all');
    todayBtn.dataset.filterState = mode;
    todayBtn.dataset.i18n = labelKey;
    todayBtn.setAttribute("aria-pressed", mode === 'all' ? "false" : "true");
    todayBtn.setAttribute('aria-label', labelKey);
    todayBtn.setAttribute('title', labelKey);
    todayBtn.innerHTML = (mode === 'all')
      ? '<svg aria-hidden="true" class="gf-filter-ico" viewBox="0 0 24 24"><path d="M4 7h16"></path><path d="M4 12h16"></path><path d="M4 17h16"></path></svg><span class="sr-only">Tutti</span>'
      : '<svg aria-hidden="true" class="gf-filter-ico" viewBox="0 0 24 24"><rect x="4" y="5" width="16" height="15" rx="2"></rect><path d="M8 3v4"></path><path d="M16 3v4"></path><path d="M4 10h16"></path><path d="M9 15l2 2 4-5"></path></svg><span class="sr-only">Oggi</span>';
    try { __translateTree__(todayBtn); } catch(_) {}
    try{ __applyGuestFilterButtonVisuals__(); }catch(_){ }
  };

  const paintPreventivi = () => {
    if (!preventiviBtn) return;
    const on = !!state.guestShowPreventiviOnly;
    preventiviBtn.classList.toggle("is-active", on);
    preventiviBtn.setAttribute("aria-pressed", on ? "true" : "false");
    preventiviBtn.dataset.filterState = on ? "active" : "distractive";
    preventiviBtn.setAttribute("aria-label", on ? "Nascondi preventivi" : "Mostra solo preventivi");
    preventiviBtn.setAttribute("title", "Preventivi");
    preventiviBtn.innerHTML = '<svg aria-hidden="true" class="gf-filter-ico" viewBox="0 0 24 24"><path d="M7 3h7l4 4v14H7z"></path><path d="M14 3v5h5"></path><path d="M9 13h6"></path><path d="M9 17h4"></path></svg><span class="sr-only">Preventivi</span>';
    try { __translateTree__(preventiviBtn); } catch(_) {}
    try{ __applyGuestFilterButtonVisuals__(); }catch(_){ }
  };

  syncSortSelect();
  paintSortButtons();
  paintToday();
  paintPreventivi();

  if (todayBtn){
    todayBtn.addEventListener("click", () => {
      const current = __ddae2862NormalizeFilterMode__(state.guestArrivalFilter || 'today', 'guest');
      state.guestArrivalFilter = current === 'today' ? 'all' : 'today';
      try { localStorage.setItem("dDAE_guestArrivalFilter", state.guestArrivalFilter); } catch(_){}
      try { localStorage.removeItem("dDAE_guestTodayOnly"); } catch(_){}
      paintToday();
      renderGuestCards();
    });
  }

  if (preventiviBtn){
    preventiviBtn.addEventListener("click", () => {
      state.guestShowPreventiviOnly = !state.guestShowPreventiviOnly;
      try{ localStorage.setItem("dDAE_guestShowPreventiviOnly", state.guestShowPreventiviOnly ? "1" : "0"); }catch(_){ }
      paintPreventivi();
      renderGuestCards();
    });
  }

  const __shouldResetGuestListScrollForSort__ = (sortBy) => {
    const key = String(sortBy || state.guestSortBy || "").trim().toLowerCase();
    return key === "inserimento" || key === "nome";
  };

  const __prepareGuestListScrollBeforeSortRender__ = (sortBy) => {
    if (!__shouldResetGuestListScrollForSort__(sortBy)) return;
    try{ state.guestListScrollRestorePending = false; }catch(_){ }
    try{
      const wrap = document.getElementById("guestCards");
      if (!wrap) return;
      wrap.hidden = false;
      wrap.style.display = "";
      wrap.style.visibility = "visible";
      wrap.style.webkitOverflowScrolling = "auto";
      wrap.scrollTop = 0;
      if (typeof wrap.scrollTo === "function") wrap.scrollTo(0, 0);
      void wrap.offsetHeight;
    }catch(_){ }
  };

  const __hardRefreshGuestCardsScrollerForIOS__ = () => {
    try{
      const wrap = document.getElementById("guestCards");
      if (!wrap || !wrap.parentNode) return wrap || null;

      // dDAE_2.801 — iOS/Safari: quando si passa da una lista già scrollata
      // ai filtri Inserimento/Nome, il vecchio layer composito può rimanere
      // invisibile ma ancora cliccabile. Sostituiamo quindi solo lo scroller
      // esterno, spostando dentro le stesse card già renderizzate: gli handler
      // delle card restano vivi, ma WebKit ricrea il layer visivo da zero.
      const fresh = wrap.cloneNode(false);
      fresh.hidden = false;
      fresh.style.display = "";
      fresh.style.visibility = "visible";
      fresh.style.opacity = "";
      fresh.style.transform = "none";
      fresh.style.webkitTransform = "none";
      fresh.style.contain = "none";
      fresh.style.webkitOverflowScrolling = "auto";
      fresh.scrollTop = 0;
      while (wrap.firstChild) fresh.appendChild(wrap.firstChild);
      wrap.parentNode.replaceChild(fresh, wrap);
      void fresh.offsetHeight;
      try{ fresh.getBoundingClientRect(); }catch(_){ }
      return fresh;
    }catch(_){
      try{ return document.getElementById("guestCards"); }catch(__){ return null; }
    }
  };

  const __resetGuestListScrollAfterSort__ = (sortBy) => {
    if (!__shouldResetGuestListScrollForSort__(sortBy)) return;
    try{ state.guestListScrollRestorePending = false; }catch(_){ }

    let refreshedOnce = false;
    const reset = () => {
      try{
        let wrap = document.getElementById("guestCards");
        if (!wrap) return;

        if (!refreshedOnce){
          wrap = __hardRefreshGuestCardsScrollerForIOS__() || wrap;
          refreshedOnce = true;
        }

        wrap.hidden = false;
        wrap.style.display = "";
        wrap.style.visibility = "visible";
        wrap.style.opacity = "";
        wrap.style.transform = "none";
        wrap.style.webkitTransform = "none";
        wrap.style.webkitOverflowScrolling = "auto";
        wrap.scrollTop = 0;
        if (typeof wrap.scrollTo === "function") wrap.scrollTo({ top: 0, left: 0, behavior: "auto" });
        void wrap.offsetHeight;

        setTimeout(() => {
          try{
            const w = document.getElementById("guestCards");
            if (!w) return;
            w.scrollTop = 0;
            if (typeof w.scrollTo === "function") w.scrollTo({ top: 0, left: 0, behavior: "auto" });
            w.style.webkitOverflowScrolling = "touch";
            void w.offsetHeight;
          }catch(_){ }
        }, 40);
      }catch(_){ }
    };

    try{ requestAnimationFrame(() => requestAnimationFrame(reset)); }catch(_){ reset(); }
    try{ setTimeout(reset, 0); }catch(_){ }
    try{ setTimeout(reset, 90); }catch(_){ }
    try{ setTimeout(reset, 220); }catch(_){ }
  };

  sortButtons.forEach((btn) => {
    btn.addEventListener("click", () => {
      const nextBy = String(btn.dataset.sortBy || "").trim();
      if (!nextBy) return;

      const sameButton = String(state.guestSortBy || "") === nextBy;
      const now = Date.now();
      const repeatedTap = sameButton || (lastSortTapBy === nextBy && ((now - lastSortTapTs) <= SORT_DOUBLE_TAP_MS));
      lastSortTapBy = nextBy;
      lastSortTapTs = now;

      if (sameButton && repeatedTap) {
        if (nextBy === "arrivo" || nextBy === "checkout") {
          state.guestSortDir = "asc";
          try { localStorage.setItem("dDAE_guestSortDir", state.guestSortDir); } catch(_){}
          paintDir();
          __prepareGuestListScrollBeforeSortRender__(nextBy);
          renderGuestCards();
          __resetGuestListScrollAfterSort__(nextBy);
          return;
        }
        toggleGuestSortDir();
        return;
      }

      state.guestSortBy = nextBy;
      state.guestSortDir = "asc";
      try { localStorage.setItem("dDAE_guestSortBy", state.guestSortBy); } catch(_){}
      try { localStorage.setItem("dDAE_guestSortDir", state.guestSortDir); } catch(_){}
      syncSortSelect();
      paintSortButtons();
      paintDir();
      __prepareGuestListScrollBeforeSortRender__(nextBy);
      renderGuestCards();
      __resetGuestListScrollAfterSort__(nextBy);
    });
  });

  sortSel.addEventListener("change", () => {
    state.guestSortBy = sortSel.value;
    state.guestSortDir = "asc";
    try { localStorage.setItem("dDAE_guestSortBy", state.guestSortBy); } catch(_){}
    try { localStorage.setItem("dDAE_guestSortDir", state.guestSortDir); } catch(_){}
    syncSortSelect();
    paintSortButtons();
    paintDir();
    __prepareGuestListScrollBeforeSortRender__(state.guestSortBy);
    renderGuestCards();
    __resetGuestListScrollAfterSort__(state.guestSortBy);
  });

  const toggleGuestSortDir = () => {
    const by = String(state.guestSortBy || "").trim();
    if (by === "arrivo" || by === "checkout") {
      state.guestSortDir = "asc";
    } else {
      state.guestSortDir = (state.guestSortDir === "desc") ? "asc" : "desc";
    }
    try { localStorage.setItem("dDAE_guestSortDir", state.guestSortDir); } catch(_){}
    paintDir();
    __prepareGuestListScrollBeforeSortRender__(state.guestSortBy);
    renderGuestCards();
    __resetGuestListScrollAfterSort__(state.guestSortBy);
  };

  if (dirBtn){
    dirBtn.addEventListener("click", () => {
      toggleGuestSortDir();
    });
  }


  // IRAP modal buttons
  const irapModal = document.getElementById("irapModal");
  if (irapModal){
    irapModal.addEventListener("click", (e)=>{ if (e.target === irapModal) closeIrapModal(); });
  }
  const btnIrapCancel = document.getElementById("btnIrapCancel");
  if (btnIrapCancel) bindFastTap(btnIrapCancel, () => closeIrapModal());
  const btnIrapSave = document.getElementById("btnIrapSave");
  if (btnIrapSave) bindFastTap(btnIrapSave, () => saveIrapModal());

  // Amministratore: salva compenso/TFM
  const btnAdminSave = document.getElementById("btnAdminSave");
  if (btnAdminSave) bindFastTap(btnAdminSave, () => saveStatAmministratore());

}


function __guestFilterButtonStateLabel__(stateKey){
  const key = String(stateKey || '').trim().toLowerCase();
  if (key === 'today') return __translateExactText__('Oggi') || 'Oggi';
  if (key === '3days') return __translateExactText__('3 giorni') || '3 giorni';
  if (key === '7days') return __translateExactText__('7 giorni') || '7 giorni';
  if (key === '-3days') return __translateExactText__('-3 giorni') || '-3 giorni';
  if (key === '-7days') return __translateExactText__('-7 giorni') || '-7 giorni';
  if (key === 'all') return __translateExactText__('Tutti') || 'Tutti';
  return __translateExactText__(key === 'active' ? 'Attivo' : 'Disattivo');
}

function __guestFilterButtonLocalizedLabel__(btn){
  try{
    const raw = String(btn?.textContent || btn?.getAttribute?.('aria-label') || '').replace(/\s+/g, ' ').trim();
    return raw || __translateExactText__('Tasto') || 'Tasto';
  }catch(_){
    return __translateExactText__('Tasto') || 'Tasto';
  }
}

function __guestFilterButtonEditStatePrompt__(label){
  const safeLabel = String(label || '').trim() || (__translateExactText__('Tasto') || 'Tasto');
  switch (__getAppLanguage__()){
    case 'en': return `Which state do you want to edit for ${safeLabel}?`;
    case 'fr': return `Quel état voulez-vous modifier pour ${safeLabel} ?`;
    case 'de': return `Welchen Status möchten Sie für ${safeLabel} bearbeiten?`;
    case 'es': return `¿Qué estado quieres modificar para ${safeLabel}?`;
    default: return `Quale stato vuoi modificare per ${safeLabel}?`;
  }
}

function __guestFilterButtonStateUpdatedMessage__(label, stateKey){
  const safeLabel = String(label || '').trim() || (__translateExactText__('Tasto') || 'Tasto');
  const stateLabel = __guestFilterButtonStateLabel__(stateKey);
  switch (__getAppLanguage__()){
    case 'en': return `${safeLabel} button: ${stateLabel.toLowerCase()} state updated`;
    case 'fr': return `Bouton ${safeLabel} : état ${String(stateLabel || '').toLowerCase()} mis à jour`;
    case 'de': return `Taste ${safeLabel}: Status ${String(stateLabel || '').toLowerCase()} aktualisiert`;
    case 'es': return `Botón ${safeLabel}: estado ${String(stateLabel || '').toLowerCase()} actualizado`;
    default: return `Tasto ${safeLabel}: stato ${String(stateLabel || '').toLowerCase()} aggiornato`;
  }
}

function __guestFilterButtonCategoryPrompt__(){
  switch (__getAppLanguage__()){
    case 'en': return 'Apply the changes to all guest filter buttons?';
    case 'fr': return 'Appliquer les modifications à tous les boutons de filtre des invités ?';
    case 'de': return 'Änderungen auf alle Gastfilter-Tasten anwenden?';
    case 'es': return '¿Aplicar los cambios a todos los botones de filtro de huéspedes?';
    default: return 'Applicare le modifiche a tutti i tasti filtro ospiti?';
  }
}

function __guestFilterButtonTargetIds__(){
  return ['guestPreventivi','guestSortByArrivo','guestSortByCheckout','guestSortByInserimento','guestSortByNome'];
}

function __guestAuxButtonTargetIds__(){
  return ['guestScrollTodayBtn'];
}

function __loadGuestFilterButtonVisualMap__(){
  try{
    const raw = localStorage.getItem(__GUEST_FILTER_BUTTON_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveGuestFilterButtonVisualMap__(map){
  try{ localStorage.setItem(__GUEST_FILTER_BUTTON_VISUAL_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __guestFilterButtonDefaultVisual__(btnOrId, stateKey){
  const id = String((btnOrId && btnOrId.id) || btnOrId || '').trim();
  const rawMode = String(stateKey || 'distractive').trim().toLowerCase();
  const isToday = id === 'guestToday';
  const isPreventivi = id === 'guestPreventivi';
  const isAuxTodayScroll = id === 'guestScrollTodayBtn';
  if (isAuxTodayScroll){
    const mode = rawMode === 'distractive' ? 'distractive' : 'active';
    if (mode === 'distractive') return __launcherVisualNormalize__({ bg:'gray-1', border:'gray-3', fg:'sky-6', opacity:0.75 }, 'gray-1');
    return __launcherVisualNormalize__({ bg:'sky-5', border:'sky-6', fg:'white', opacity:0.90 }, 'sky-5');
  }
  if (isToday){
    if (rawMode === 'today') return __launcherVisualNormalize__({ bg:'red-4', border:'red-4', fg:'white', opacity:0.80 }, 'red-4');
    return __launcherVisualNormalize__({ bg:'sky-4', border:'sky-4', fg:'white', opacity:0.80 }, 'sky-4');
  }
  if (isPreventivi){
    if (rawMode === 'active') return __launcherVisualNormalize__({ bg:'yellow-4', border:'orange-4', fg:'gray-6', opacity:0.90 }, 'yellow-4');
    return __launcherVisualNormalize__({ bg:'sky-4', border:'sky-4', fg:'white', opacity:0.80 }, 'sky-4');
  }
  const mode = rawMode === 'active' ? 'active' : 'distractive';
  if (mode === 'active'){
    return __launcherVisualNormalize__({ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 }, 'green-4');
  }
  return __launcherVisualNormalize__({ bg:'sky-4', border:'sky-4', fg:'white', opacity:0.80 }, 'sky-4');
}

function __guestFilterButtonVisualForState__(btnOrId, stateKey){
  const id = String((btnOrId && btnOrId.id) || btnOrId || '').trim();
  const rawMode = String(stateKey || 'distractive').trim().toLowerCase();
  const mode = (id === 'guestToday')
    ? ((rawMode === '3days' || rawMode === '7days' || rawMode === '-3days' || rawMode === '-7days' || rawMode === 'all' || rawMode === 'today') ? rawMode : 'today')
    : (id === 'guestScrollTodayBtn' ? (rawMode === 'distractive' ? 'distractive' : 'active') : (rawMode === 'active' ? 'active' : 'distractive'));
  const fallback = __guestFilterButtonDefaultVisual__(id, mode);
  if (!id) return fallback;
  const map = __loadGuestFilterButtonVisualMap__();
  const raw = (map[id] && typeof map[id] === 'object') ? map[id][mode] : null;
  const clean = __launcherVisualNormalize__(raw || fallback, fallback.bg || 'sky-4');
  return {
    bg: clean.bg || fallback.bg || 'sky-4',
    border: clean.border || clean.bg || fallback.border || fallback.bg || 'sky-4',
    fg: clean.fg || fallback.fg || 'white',
    opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.80)
  };
}

function __saveGuestFilterButtonVisualState__(btnOrId, stateKey, visual){
  try{
    const id = String((btnOrId && btnOrId.id) || btnOrId || '').trim();
    if (!id) return;
    const rawMode = String(stateKey || 'distractive').trim().toLowerCase();
    const mode = (id === 'guestToday')
      ? ((rawMode === '3days' || rawMode === '7days' || rawMode === '-3days' || rawMode === '-7days' || rawMode === 'all' || rawMode === 'today') ? rawMode : 'today')
      : (id === 'guestScrollTodayBtn' ? (rawMode === 'distractive' ? 'distractive' : 'active') : (rawMode === 'active' ? 'active' : 'distractive'));
    const map = __loadGuestFilterButtonVisualMap__();
    const entry = (map[id] && typeof map[id] === 'object') ? map[id] : {};
    const fallback = __guestFilterButtonDefaultVisual__(id, mode);
    const clean = __launcherVisualNormalize__(visual || {}, fallback.bg || 'sky-4');
    entry[mode] = {
      bg: clean.bg || fallback.bg || 'sky-4',
      border: clean.border || clean.bg || fallback.border || fallback.bg || 'sky-4',
      fg: clean.fg || fallback.fg || 'white',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.80)
    };
    map[id] = entry;
    __saveGuestFilterButtonVisualMap__(map);
  }catch(_){ }
}

function __guestFilterButtonCurrentState__(btn){
  try{
    if (btn && btn.id === 'guestToday') return String(state.guestArrivalFilter || btn?.dataset?.filterState || 'today');
    if (btn && btn.id === 'guestPreventivi') return state.guestShowPreventiviOnly ? 'active' : 'distractive';
    if (btn && btn.id === 'guestScrollTodayBtn') return (btn.hidden || btn.getAttribute('aria-hidden') === 'true') ? 'distractive' : 'active';
    return btn && btn.classList && btn.classList.contains('is-active') ? 'active' : 'distractive';
  }catch(_){ return (btn && btn.id === 'guestToday') ? 'today' : ((btn && btn.id === 'guestScrollTodayBtn') ? 'active' : 'distractive'); }
}

function __applyGuestFilterButtonVisual__(btn, forcedState, forcedVisual){
  try{
    if (!btn || !btn.id) return;
    const rawState = String(forcedState || __guestFilterButtonCurrentState__(btn) || (btn.id === 'guestToday' ? 'today' : 'distractive')).trim().toLowerCase();
    const stateKey = btn.id === 'guestToday'
      ? ((rawState === 'all') ? 'all' : 'today')
      : (btn.id === 'guestScrollTodayBtn' ? (rawState === 'distractive' ? 'distractive' : 'active') : (rawState === 'active' ? 'active' : 'distractive'));
    const visual = forcedVisual
      ? __launcherVisualNormalize__(forcedVisual, (forcedVisual && forcedVisual.bg) || __guestFilterButtonDefaultVisual__(btn, stateKey).bg || 'sky-4')
      : __guestFilterButtonVisualForState__(btn, stateKey);
    const bgHex = __operatoreColorHex__(visual.bg || 'sky-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'sky-4');
    const fgHex = __tagColorTextHex__(visual.bg || 'sky-4', visual.fg || 'white', false) || __operatoreColorHex__(visual.fg || 'white');
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.80);
    btn.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
    btn.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
    btn.style.setProperty('border', '1px solid ' + hexToRgba(borderHex, 1), 'important');
    btn.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
    btn.style.setProperty('border-width', '1px', 'important');
    btn.style.setProperty('border-style', 'solid', 'important');
    btn.style.setProperty('box-shadow', 'none', 'important');
    btn.style.setProperty('backdrop-filter', 'none', 'important');
    btn.style.setProperty('-webkit-backdrop-filter', 'none', 'important');
    btn.style.setProperty('color', fgHex, 'important');
    btn.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
    try{
      btn.querySelectorAll('svg, .ui-ico').forEach((node)=>{
        node.style.setProperty('color', fgHex, 'important');
        node.style.setProperty('stroke', fgHex, 'important');
      });
      btn.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node)=>{
        node.style.setProperty('stroke', fgHex, 'important');
        node.style.setProperty('fill', 'none', 'important');
      });
    }catch(_){ }
    btn.style.setProperty('opacity', '1', 'important');
    __applyVisualTextWeight__(btn, visual);
  }catch(_){ }
}

function __applyGuestFilterButtonVisuals__(){
  try{
    const ids = ['guestToday', ...__guestFilterButtonTargetIds__(), ...__guestAuxButtonTargetIds__()];
    ids.forEach((id) => {
      const btn = document.getElementById(id);
      if (!btn) return;
      __applyGuestFilterButtonVisual__(btn);
      __bindGuestFilterButtonColorHold__(btn);
    });
  }catch(_){ }
}

async function __applyGuestFilterButtonChangesToCategory__(stateKey, payload, changed){
  try{
    const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    __guestFilterButtonTargetIds__().forEach((id) => {
      const current = __guestFilterButtonVisualForState__(id, mode);
      const fallback = __guestFilterButtonDefaultVisual__(id, mode).bg || 'sky-4';
      const next = __applyDesignPayloadToVisual__(current, payload, changed, fallback);
      __saveGuestFilterButtonVisualState__(id, mode, next);
    });
    __applyGuestFilterButtonVisuals__();
    try{ renderRoomSettingsPage(); }catch(_){ }
  }catch(_){ }
}

async function __openGuestFilterButtonColorPicker__(btn){
  try{
    if (!btn || !btn.id) return;
    const label = __guestFilterButtonLocalizedLabel__(btn);
    const stateKey = String(__guestFilterButtonCurrentState__(btn) || (btn.id === 'guestToday' ? 'today' : 'distractive')).trim().toLowerCase();
    if (!stateKey) return;
    const current = __guestFilterButtonVisualForState__(btn, stateKey);
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = {
        bg: colors.bg || current.bg || 'sky-4',
        border: colors.border || current.border || colors.bg || current.bg || 'sky-4',
        fg: colors.fg || current.fg || 'white',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.80),
        bold: false
      };
      __saveGuestFilterButtonVisualState__(btn, stateKey, next);
      __applyGuestFilterButtonVisual__(btn, stateKey, next);
    };
    const revertVisual = () => {
      __saveGuestFilterButtonVisualState__(btn, stateKey, current);
      __applyGuestFilterButtonVisuals__();
    };
    __tagColorPopupOpen__('guest-filter-button', current, (payload) => {
      try{
        applyVisual(payload);
        __applyGuestFilterButtonVisuals__();
        try{ renderRoomSettingsPage(); }catch(_){ }
        try{ toast(__guestFilterButtonStateUpdatedMessage__(label, stateKey)); }catch(_){ }
      }catch(_){ }
    }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:true,
      supportsOpacity:true,
      opacity:(current.opacity ?? 0.80),
      defaultMode:'bg',
      fallbackBg:(current.bg || 'sky-4'),
      onPreview:applyVisual,
      onRevert:revertVisual,
      applyCategory: (btn.id === 'guestToday' || btn.id === 'guestScrollTodayBtn') ? null : {
        message: __guestFilterButtonCategoryPrompt__(),
        apply: async(payload, changed) => { await __applyGuestFilterButtonChangesToCategory__(stateKey, payload, changed); }
      }
    });
  }catch(_){ }
}


function __bindGuestFilterButtonColorHold__(btn){
  try{ if (!btn || btn.dataset.guestFilterColorBound === '1') return; btn.dataset.guestFilterColorBound = '1'; }catch(_){ if (!btn) return; }
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } };
  const start = (e)=>{
    try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    try{ if (__settingsEditorModalRecentlyOpened__(2400) && __isSettingsEditorDesignTarget__(e && e.target)) return; }catch(_){ }
    fired = false;
    clear();
    timer = setTimeout(()=>{ fired = true; try{ btn.classList.add('is-pressing'); }catch(_){ } __openGuestFilterButtonColorPicker__(btn); }, 500);
  };
  const stop = (e)=>{
    clear();
    if (fired){ block(e); setTimeout(()=>{ fired = false; try{ btn.classList.remove('is-pressing'); }catch(_){ } }, 0); return; }
    try{ btn.classList.remove('is-pressing'); }catch(_){ }
  };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt, stop, { passive:false }); }catch(_){ } });
  try{ btn.addEventListener('click', (e)=>{ if (fired){ block(e); fired = false; } }, true); }catch(_){ }
  try{ btn.addEventListener('contextmenu', (e)=>{ block(e); __openGuestFilterButtonColorPicker__(btn); }, true); }catch(_){ }
}

function guestIdOf(g){
  return String(g?.id ?? g?.ID ?? g?.ospite_id ?? g?.ospiteId ?? g?.guest_id ?? g?.guestId ?? "").trim();
}

function findCalendarGuestById(id){
  try{
    const needle = String(id ?? '').trim();
    if (!needle) return null;

    const sources = [
      state?.calendar?.guests,
      state?.guestRows,
      state?.guests,
      state?.ospitiRows,
      state?.ospiti
    ];

    for (const src of sources){
      if (!Array.isArray(src) || !src.length) continue;
      const hit = src.find((g) => guestIdOf(g) === needle);
      if (hit) return hit;
    }
  }catch(_){ }
  return null;
}

function parseDateTs(v){
  const s = String(v ?? "").trim();
  if (!s) return null;
  const t = Date.parse(s);
  return Number.isFinite(t) ? t : null;
}

function computeInsertionMap(guests){
  const pickSeq = (g) => {
    const v = Number(g?.seq);
    return Number.isFinite(v) && v > 0 ? v : null;
  };
  const pickIns = (g) => {
    const vals = [g?.insertion_no, g?.insertionNo, g?.ordineInserimento, g?.ins_no];
    for (const raw of vals){
      const v = Number(raw);
      if (Number.isFinite(v) && v > 0) return v;
    }
    return null;
  };

  const arr = (guests || []).map((g, idx) => {
    const id = guestIdOf(g);
    const seq = pickSeq(g);
    const ins = pickIns(g);
    const c = g?.created_at ?? g?.createdAt ?? "";
    const t = parseDateTs(c);
    const bucket = (seq != null) ? 0 : (ins != null ? 1 : 2);
    return { id, idx, seq, ins, t, bucket };
  }).filter(x => !!x.id);

  // Ordine misto stabile:
  // 1) seq quando presente
  // 2) insertion_no / ordineInserimento / ins_no come fallback
  // 3) createdAt solo come ulteriore fallback
  // updatedAt non influenza mai l'ordine di inserimento.
  arr.sort((a,b) => {
    if (a.bucket !== b.bucket) return a.bucket - b.bucket;

    if (a.bucket === 0){
      if (a.seq !== b.seq) return a.seq - b.seq;
    }else if (a.bucket === 1){
      if (a.ins !== b.ins) return a.ins - b.ins;
    }

    const at = a.t;
    const bt = b.t;
    if (at != null && bt != null && at !== bt) return at - bt;
    if (at == null && bt != null) return 1;
    if (at != null && bt == null) return -1;
    return a.idx - b.idx;
  });

  const map = {};
  let n = 1;
  for (const x of arr){
    map[x.id] = n++;
  }
  return map;
}



function __isGuestArrivoSortActive__(){
  try{ const by = String(state && state.guestSortBy || '').trim(); return by === 'arrivo' || by === 'checkout'; }catch(_){ return false; }
}

function __updateGuestArrivoTodayButtonVisibility__(){
  try{
    const btn = document.getElementById('guestScrollTodayBtn');
    if (!btn) return;
    const show = (state && state.page === 'ospiti' && __isGuestArrivoSortActive__());
    btn.hidden = !show;
    btn.classList.toggle('is-visible', !!show);
    btn.classList.toggle('is-active', !!show);
    btn.setAttribute('aria-hidden', show ? 'false' : 'true');
    try{ __applyGuestFilterButtonVisual__(btn, show ? 'active' : 'distractive'); }catch(_){}
    try{ __bindGuestFilterButtonColorHold__(btn); }catch(_){}
  }catch(_){ }
}

function __guestCardArrivalISO__(g){
  try{
    const bookings = Array.isArray(g && g._groupBookings) && g._groupBookings.length ? g._groupBookings : [g];
    const vals = bookings.map((x)=>__parseDateFlexibleToISO(x && (x.check_in || x.checkIn || x.arrivo || x.dataArrivo || x.arrival || x.guestCheckIn)))
      .filter(Boolean)
      .sort();
    return vals[0] || '';
  }catch(_){ return ''; }
}

function __guestCardCheckoutISO__(g){
  try{
    const bookings = Array.isArray(g && g._groupBookings) && g._groupBookings.length ? g._groupBookings : [g];
    const vals = bookings.map((x)=>__parseDateFlexibleToISO(x && (x.check_out || x.checkOut || x.checkout || x.data_check_out || x.partenza || x.departure || x.guestCheckOut)))
      .filter(Boolean)
      .sort();
    return vals[0] || '';
  }catch(_){ return ''; }
}

function __scrollGuestCardsToToday__(){
  try{
    const wrap = document.getElementById('guestCards');
    if (!wrap) return;
    const today = todayISO();
    const by = String(state && state.guestSortBy || '').trim();
    const dateKey = by === 'checkout' ? 'checkoutIso' : 'arrivalIso';
    const cards = Array.from(wrap.querySelectorAll('.guest-card')).filter((card)=>String(card.dataset[dateKey] || '').trim());
    if (!cards.length) return;
    let target = cards.find((card)=>String(card.dataset[dateKey] || '').slice(0,10) === today);
    if (!target){
      target = cards.find((card)=>String(card.dataset[dateKey] || '').slice(0,10) >= today) || null;
    }
    if (!target){
      try{ toast(by === 'checkout' ? 'Nessun check-out da oggi in poi.' : 'Nessun check-in da oggi in poi.'); }catch(_){ }
      return;
    }
    const top = Math.max(0, target.offsetTop - wrap.offsetTop);
    try{ wrap.scrollTo({ top, behavior:'smooth' }); }
    catch(_){ wrap.scrollTop = top; }
  }catch(_){ }
}

function sortGuestsList(items){
  const by = state.guestSortBy || "arrivo";
  const dir = (state.guestSortDir === "desc") ? -1 : 1;
  const nameKey = (s) => String(s ?? "").trim().toLowerCase();

  const out = items.slice();
  out.sort((a,b) => {
    if (by === "nome") {
      return nameKey(a.nome).localeCompare(nameKey(b.nome), "it") * dir;
    }
    if (by === "inserimento") {
      const aa = Number(a._insNo) || 1e18;
      const bb = Number(b._insNo) || 1e18;
      return (aa - bb) * dir;
    }
    if (by === "checkout") {
      const ta = parseDateTs(a.check_out ?? a.checkOut ?? a.checkout ?? a.data_check_out ?? a.partenza ?? a.departure);
      const tb = parseDateTs(b.check_out ?? b.checkOut ?? b.checkout ?? b.data_check_out ?? b.partenza ?? b.departure);
      if (ta == null && tb == null) return 0;
      if (ta == null) return 1;
      if (tb == null) return -1;
      return (ta - tb) * dir;
    }
    const ta = parseDateTs(a.check_in ?? a.checkIn);
    const tb = parseDateTs(b.check_in ?? b.checkIn);
    if (ta == null && tb == null) return 0;
    if (ta == null) return 1;
    if (tb == null) return -1;
    return (ta - tb) * dir;
  });
  return out;
}

/* PERIOD SYNC */
function setPeriod(from, to){
  state.period = { from, to };

  periodSyncLock += 1;
  try {
    const map = [
      ["#fromDate", "#toDate"],
      ["#fromDate2", "#toDate2"],
      ["#fromDate3", "#toDate3"],
      ["#fromDate4", "#toDate4"],
    ];
    for (const [fSel,tSel] of map){
      const f = $(fSel), t = $(tSel);
      if (f) f.value = from;
      if (t) t.value = to;
    }
  } finally {
    periodSyncLock -= 1;
  }

  const chip = $("#periodChip");
  if (chip && state.page !== "home") chip.textContent = `${from} → ${to}`;
  try{ __syncSpeseDateRangeUi__(); }catch(_){ }
}


async function onPeriodChanged({ showLoader=false } = {}){
  // Quando cambia il periodo, i dati “period-based” vanno considerati obsoleti
  state._dataKey = "";

  // Aggiorna solo ciò che serve (evita chiamate inutili e loader continui)
  if (state.page === "ospiti") {
    await loadOspiti({ ...(state.period || {}), force:true });
    return;
  }
  if (state.page === "calendario") {
    if (state.calendar) state.calendar.ready = false;
    await ensureCalendarData();
    renderCalendario();
    return;
  }
  if (state.page === "spese") {
    await ensurePeriodData({ showLoader });
    // Se siamo nella sotto-vista "grafico+riepilogo", aggiorna anche quella
    if (state.speseView === "insights") {
      renderRiepilogo();
      renderGrafico();
    } else {
      renderSpese();
    }
    return;
  }
  if (state.page === "riepilogo") {
    await ensurePeriodData({ showLoader });
    renderRiepilogo();
    return;
  }
  if (state.page === "grafico") {
    await ensurePeriodData({ showLoader });
    renderGrafico();
    return;
  }
}

/* DATA LOAD */
async function loadMotivazioni(){
  const data = await cachedGet("motivazioni", {}, { showLoader:false, ttlMs: 5*60*1000 });
  state.motivazioni = data;

  const list = $("#motivazioniList");
  if (list) {
    list.innerHTML = "";
    data.forEach(m => {
      const opt = document.createElement("option");
      opt.value = m.motivazione;
      list.appendChild(opt);
    });
  }
}


async function load({ showLoader=true } = {}){
  // Prefill rapido da cache locale (aiuta dopo reload PWA)
  if (!state.stanzeRows || !state.stanzeRows.length){
    const hit = __lsGet("stanze");
    if (hit && Array.isArray(hit.data) && hit.data.length){
      try{
        const rows0 = hit.data;
        state.stanzeRows = rows0;
        // ricostruisci indicizzazione
        const map0 = {};
        for (const r of rows0){
          const gid = String(r.ospite_id ?? r.ospiteId ?? r.guest_id ?? r.guestId ?? "").trim();
          const sn = String(r.stanza_num ?? r.stanzaNum ?? r.room_number ?? r.roomNumber ?? r.stanza ?? r.room ?? "").trim();
          if (!gid || !sn) continue;
          const key = `${gid}:${sn}`;
          map0[key] = {
            letto_m: Number(r.letto_m ?? r.lettoM ?? 0) || 0,
            letto_s: Number(r.letto_s ?? r.lettoS ?? 0) || 0,
            culla: Number(r.culla ?? r.crib ?? 0) || 0,
          };
        }
        state.stanzeByKey = map0;
      } catch(_){}
    }
  }
  const data = await cachedGet("stanze", {}, { showLoader, ttlMs: 60*1000 });
  const rows = Array.isArray(data) ? data : [];
  state.stanzeRows = rows;

  // indicizza per ospite_id + stanza_num
  const map = {};
  for (const r of rows){
    const gid = String(r.ospite_id ?? r.ospiteId ?? r.guest_id ?? r.guestId ?? "").trim();
    const sn = String(r.stanza_num ?? r.stanzaNum ?? r.room_number ?? r.roomNumber ?? r.stanza ?? r.room ?? "").trim();
    if (!gid || !sn) continue;
    const key = `${gid}:${sn}`;
    map[key] = {
      letto_m: Number(r.letto_m ?? r.lettoM ?? 0) || 0,
      letto_s: Number(r.letto_s ?? r.lettoS ?? 0) || 0,
      culla: Number(r.culla ?? r.crib ?? 0) || 0,
    };
  }
  state.stanzeByKey = map;
  __lsSet("stanze", rows);
}

async function loadOspiti({ from="", to="", force=false } = {}){
  // Prefill rapido da cache locale (poi refresh in background)
  const lsKey = `ospiti|${from}|${to}`;
  const hit = __lsGet(lsKey);
  if (hit && Array.isArray(hit.data) && hit.data.length){
    state.guests = hit.data;
    // render subito (perceived speed)
    try{ requestAnimationFrame(renderGuestCards); } catch(_){ renderGuestCards(); }
  }

  // ✅ Necessario per mostrare i "pallini letti" stanza-per-stanza nelle schede ospiti
  const p = load({ showLoader:false });
  const hasLocal = !!(hit && Array.isArray(hit.data) && hit.data.length);

  // Se ho già dati locali, aggiorna in background (senza loader e senza bloccare la navigazione)
  const refresh = async () => {
    const data = await cachedGet("ospiti", { from, to }, {
      showLoader: !hasLocal,
      ttlMs: 2*60*1000,
      swrMs: 10*60*1000,
      force,
    });
    return data;
  };

  if (hasLocal && !force) {
    // fire-and-forget
    Promise.all([p, refresh()])
      .then(([ , data ]) => {
        // aggiorna solo se l'utente è ancora nella lista ospiti
        if (state.page !== "ospiti") return;
        const nextRaw = Array.isArray(data) ? data : [];
        const next = __filterByExerciseYear__(nextRaw, state.exerciseYear || loadExerciseYear(), [
          "check_in","checkIn","arrivo","dataArrivo","check_out","checkOut","partenza","dataPartenza",
          "createdAt","created_at","updatedAt","updated_at"
        ]);
        // Evita di sovrascrivere con vuoto se ho già dati (flash → scomparsa)
        if (next.length || !(Array.isArray(state.guests) && state.guests.length)) {
          state.guests = next;
          __lsSet(lsKey, state.guests);
        }
        try{ requestAnimationFrame(renderGuestCards); }catch(_){ renderGuestCards(); }
      })
      .catch((err) => {
        try{
          if (state && (state.page === "spese" || state.page === "statspese")) {
            toast((err && err.message) ? err.message : "Errore lettura spese");
          }
        }catch(_){ }
        try{ state._dataKey = ""; }catch(_){ }
      });
    return;
  }

  const [ , data ] = await Promise.all([p, refresh()]);
  const nextRaw = Array.isArray(data) ? data : [];
        const next = __filterByExerciseYear__(nextRaw, state.exerciseYear || loadExerciseYear(), [
          "check_in","checkIn","arrivo","dataArrivo","check_out","checkOut","partenza","dataPartenza",
          "createdAt","created_at","updatedAt","updated_at"
        ]);
  // Evita di sovrascrivere con vuoto se ho già dati (flash → scomparsa)
  if (next.length || !(Array.isArray(state.guests) && state.guests.length)) {
    state.guests = next;
    __lsSet(lsKey, state.guests);
  }
  renderGuestCards();
}


async function ensurePeriodData({ showLoader=true, force=false } = {}){
  const { from, to } = state.period;
  const uid = (state && state.session && state.session.user_id) ? String(state.session.user_id) : "";
  const anno = (state && state.exerciseYear) ? String(state.exerciseYear) : "";
  const key = `${uid}|${anno}|${from}|${to}`;

  if (!force && state._dataKey === key && state.report && Array.isArray(state.spese)) {
    return;
  }

  // Prefill immediato da cache locale (perceived speed) — poi refresh SWR
  const lsSpeseKey = `spese|${uid}|${anno}|${from}|${to}`;
  const lsReportKey = `report|${uid}|${anno}|${from}|${to}`;
  const lsGuestsKey = `ospiti|${uid}|${anno}|${from}|${to}`;
  const hitS = !force ? __lsGet(lsSpeseKey) : null;
  const hitR = !force ? __lsGet(lsReportKey) : null;
  const hitG = !force ? __lsGet(lsGuestsKey) : null;
  const hasLocal = !!((hitS && hitS.data) || (hitR && hitR.data) || (hitG && hitG.data));

  if (!force) {
    if (hitS && Array.isArray(hitS.data)) {
      state.spese = __filterByExerciseYear__(__filterSpeseByCardDateRange__(hitS.data, from, to), state.exerciseYear || loadExerciseYear(), ["dataSpesa","data","data_spesa"]);
      state.report = buildReportFromSpese(state.spese);
    } else if (hitR && hitR.data) {
      state.report = hitR.data;
    }
    if (hasLocal) state._dataKey = key;
  }

  const fetchAll = () => Promise.all([
    cachedGet("spese", { from, to }, { showLoader: showLoader && !hasLocal, ttlMs: 2*60*1000, swrMs: 10*60*1000, force }),
    cachedGet("ospiti", { from, to }, { showLoader:false, ttlMs: 2*60*1000, swrMs: 10*60*1000, force }),
  ]);

  // Se ho cache locale e non forzo, non bloccare la navigazione: aggiorna in background
  if (hasLocal && !force) {
    fetchAll()
      .then(([spese]) => {
                const uidNow = (state && state.session && state.session.user_id) ? String(state.session.user_id) : "";
        const annoNow = (state && state.exerciseYear) ? String(state.exerciseYear) : "";
        const kNow = `${uidNow}|${annoNow}|${state.period.from}|${state.period.to}`;
        if (kNow !== key) return;
        state.spese = __filterByExerciseYear__(__filterSpeseByCardDateRange__(Array.isArray(spese) ? spese : [], state.period.from, state.period.to), state.exerciseYear || loadExerciseYear(), ["dataSpesa","data","data_spesa"]);
        state.report = buildReportFromSpese(state.spese);
        state._dataKey = key;
        __lsSet(lsReportKey, state.report);
        __lsSet(lsSpeseKey, state.spese);

        // refresh UI se siamo su pagine che dipendono da questi dati
        try{
          if (state.page === "spese") {
            if (state.speseView === "list") renderSpese();
            else { renderRiepilogo(); renderGrafico(); }
          }
          if (state.page === "statgen") renderStatGen();
          if (state.page === "statmensili") renderStatMensili();
          if (state.page === "statspese") renderStatSpese();
          if (state.page === "statprenotazioni") renderStatRicevute();
          if (state.page === "statchannel") renderStatChannel();
          if (state.page === "statpulizie") renderStatPulizie(Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []);
        }catch(_){ }
      })
      .catch(() => {});
    return;
  }

  const [spese, ospiti] = await fetchAll();
  state.spese = __filterByExerciseYear__(__filterSpeseByCardDateRange__(Array.isArray(spese) ? spese : [], state.period.from, state.period.to), state.exerciseYear || loadExerciseYear(), ["dataSpesa","data","data_spesa"]);
  state.report = buildReportFromSpese(state.spese);
  state._dataKey = key;
  __lsSet(lsReportKey, state.report);
  __lsSet(lsSpeseKey, state.spese);
}


// =========================
// STATISTICHE: dati NON filtrati dai periodi di "Spese"
// (carica tutte le spese dell'anno esercizio, indipendente da state.period)
// =========================
function __getExerciseYearRangeISO(){
  const y = Number(state.exerciseYear || new Date().getFullYear());
  const yy = String(isFinite(y) ? y : new Date().getFullYear());
  return { from: `${yy}-01-01`, to: `${yy}-12-31` };
}

async function ensureStatsAllData({ showLoader=true, force=false } = {}){
  const { from, to } = __getExerciseYearRangeISO();
  const uid = (state && state.session && state.session.user_id) ? String(state.session.user_id) : "";
  const anno = (state && state.exerciseYear) ? String(state.exerciseYear) : "";
  const key = `${uid}|${anno}|ALL|${from}|${to}`;

  if (!force && state._statsDataKey === key && Array.isArray(state.speseAll) && state.reportAll) {
    return;
  }

  const lsSpeseKey = `speseALL|${uid}|${anno}|${from}|${to}`;
  const lsReportKey = `reportALL|${uid}|${anno}|${from}|${to}`;
  const lsGuestsKey = `ospitiALL|${uid}|${anno}|${from}|${to}`;
  const hitS = !force ? __lsGet(lsSpeseKey) : null;
  const hitR = !force ? __lsGet(lsReportKey) : null;
  const hitG = !force ? __lsGet(lsGuestsKey) : null;
  const hasLocal = !!((hitS && hitS.data) || (hitR && hitR.data) || (hitG && hitG.data));

  if (!force) {
    if (hitS && Array.isArray(hitS.data)) {
      state.speseAll = __filterByExerciseYear__(__filterSpeseByCardDateRange__(hitS.data, from, to), state.exerciseYear || loadExerciseYear(), ["dataSpesa","data","data_spesa"]);
      state.reportAll = buildReportFromSpese(state.speseAll);
    } else if (hitR && hitR.data) {
      state.reportAll = hitR.data;
    }
    if (hitG && Array.isArray(hitG.data)) {
      state.statsGuests = __guestFilterPreventiviRows__(__filterByExerciseYear__(hitG.data, state.exerciseYear || loadExerciseYear(), [
        "check_in","checkIn","arrivo","dataArrivo","check_out","checkOut","partenza","dataPartenza",
        "createdAt","created_at","updatedAt","updated_at"
      ]), false);
    }
    if (hasLocal) state._statsDataKey = key;
  }

  const fetchAll = () => Promise.all([
    cachedGet("spese", { from, to }, { showLoader: showLoader && !hasLocal, ttlMs: 2*60*1000, swrMs: 10*60*1000, force }),
    cachedGet("ospiti", { from, to }, { showLoader: false, ttlMs: 2*60*1000, swrMs: 10*60*1000, force }),
  ]);

  if (hasLocal && !force) {
    fetchAll()
      .then(([spese, ospiti]) => {
        const uidNow = (state && state.session && state.session.user_id) ? String(state.session.user_id) : "";
        const annoNow = (state && state.exerciseYear) ? String(state.exerciseYear) : "";
        const kNow = `${uidNow}|${annoNow}|ALL|${from}|${to}`;
        if (kNow !== key) return;
        state.speseAll = __filterByExerciseYear__(__filterSpeseByCardDateRange__(Array.isArray(spese) ? spese : [], from, to), state.exerciseYear || loadExerciseYear(), ["dataSpesa","data","data_spesa"]);
        state.reportAll = buildReportFromSpese(state.speseAll);
        state._statsDataKey = key;
        __lsSet(lsReportKey, state.reportAll);
        __lsSet(lsSpeseKey, state.speseAll);
        state.statsGuests = __guestFilterPreventiviRows__(__filterByExerciseYear__(Array.isArray(ospiti) ? ospiti : [], state.exerciseYear || loadExerciseYear(), [
          "check_in","checkIn","arrivo","dataArrivo","check_out","checkOut","partenza","dataPartenza",
          "createdAt","created_at","updatedAt","updated_at"
        ]), false);
        __lsSet(lsGuestsKey, state.statsGuests);

        try{
          if (state.page === "statgen") renderStatGen();
          if (state.page === "statmensili") renderStatMensili();
          if (state.page === "statspese") renderStatSpese();
          if (state.page === "statprenotazioni") renderStatRicevute();
          if (state.page === "statchannel") renderStatChannel();
          if (state.page === "statpulizie") renderStatPulizie(Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []);
          if (state.page === "statazienda") renderStatAzienda();
          if (state.page === "statamministratore") renderStatAmministratore();
        }catch(_){ }
      })
      .catch(() => {});
    return;
  }

  const [spese, ospiti] = await fetchAll();
  state.speseAll = __filterByExerciseYear__(__filterSpeseByCardDateRange__(Array.isArray(spese) ? spese : [], from, to), state.exerciseYear || loadExerciseYear(), ["dataSpesa","data","data_spesa"]);
  state.reportAll = buildReportFromSpese(state.speseAll);
  state._statsDataKey = key;
  __lsSet(lsReportKey, state.reportAll);
  __lsSet(lsSpeseKey, state.speseAll);
  state.statsGuests = __guestFilterPreventiviRows__(__filterByExerciseYear__(Array.isArray(ospiti) ? ospiti : [], state.exerciseYear || loadExerciseYear(), [
    "check_in","checkIn","arrivo","dataArrivo","check_out","checkOut","partenza","dataPartenza",
    "createdAt","created_at","updatedAt","updated_at"
  ]), false);
  __lsSet(lsGuestsKey, state.statsGuests);
}


function loadOspitiEliminati({ from="", to="", force=false } = {}){
  const lsKey = `ospiti_eliminati|${from}|${to}`;
  const hit = __lsGet(lsKey);
  if (hit && Array.isArray(hit.data)){
    state.deletedGuests = hit.data;
  }

  if (!force && hit && Array.isArray(hit.data) && hit.data.length){
    // refresh in background
    api("ospiti_eliminati", { method:"GET", params:{ from, to }, showLoader:false })
      .then((rows)=>{
        if (Array.isArray(rows)){
          state.deletedGuests = rows;
          __lsSet(lsKey, { data: rows, ts: Date.now() });
        }
      })
      .catch(()=>{});
    return Promise.resolve(state.deletedGuests);
  }

  return api("ospiti_eliminati", { method:"GET", params:{ from, to }, showLoader:false })
    .then((rows)=>{
      if (Array.isArray(rows)){
        state.deletedGuests = rows;
        __lsSet(lsKey, { data: rows, ts: Date.now() });
        return rows;
      }
      state.deletedGuests = [];
      __lsSet(lsKey, { data: [], ts: Date.now() });
      return [];
    })
    .catch((e)=>{
      state.deletedGuests = state.deletedGuests || [];
      return state.deletedGuests;
    });
}


function __getStatsReport(){
  return (state && state.report) ? state.report : state.reportAll;
}
function __getStatsSpese(){
  const rows = Array.isArray(state && state.spese) ? state.spese : (Array.isArray(state && state.speseAll) ? state.speseAll : []);
  const pg = String(state && state.page || '');
  const mode = (pg === 'spese' || pg === 'statspese') ? __speseBudgetModeRead__() : 'budget';
  return __filterSpeseBudgetRows__(rows, mode);
}

// Compat: vecchi call-site
async function loadData({ showLoader=true } = {}){
  return ensurePeriodData({ showLoader });
}


/* 1) INSERISCI */
function resetInserisci(){
  __exitSpesaEditMode__();
  $("#spesaImporto").value = "";
  $("#spesaMotivazione").value = "";
  __setSpesaCategoriaValue__("");
  $("#spesaData").value = todayISO();
  try{ __setupSpesaCategoryButtons__(); }catch(_){ }
  try{ __setupSpeseCategoryFilterButtons__(); }catch(_){ }
  try{ __bindSpeseDateRangeUi__(); }catch(_){ }

  // Motivazione: se l'utente scrive una variante già esistente, usa la versione canonica
  const mot = $("#spesaMotivazione");
  if (mot) {
    mot.addEventListener("blur", () => {
      const v = collapseSpaces((mot.value || "").trim());
      if (!v) return;
      const canonical = findCanonicalMotivazione(v);
      if (canonical) mot.value = canonical;
      else mot.value = v; // pulizia spazi multipli
    });
  } // lascia oggi
}


function collapseSpaces(s){
  return String(s || "").replace(/\s+/g, " ");
}

// Normalizza SOLO per confronto (non altera la stringa salvata se già esistente)
function normalizeMotivazioneForCompare(s){
  let x = collapseSpaces(String(s || "").trim()).toLowerCase();
  // rimuove accenti SOLO per confronto
  try {
    x = x.normalize("NFD").replace(/[\u0300-\u036f]/g, "");
  } catch (_) {}
  return x;
}

function findCanonicalMotivazione(input){
  const needle = normalizeMotivazioneForCompare(input);
  for (const m of (state.motivazioni || [])){
    const val = m?.motivazione ?? "";
    if (normalizeMotivazioneForCompare(val) === needle) return val;
  }
  return null;
}

function __getSpesaById__(id){
  const safeId = String(id || '').trim();
  if (!safeId) return null;
  const pools = [Array.isArray(state?.speseAll) ? state.speseAll : [], Array.isArray(state?.spese) ? state.spese : []];
  for (const list of pools){
    const found = list.find((row) => String(row?.id || '').trim() === safeId);
    if (found) return found;
  }
  return null;
}

function __setSpesaEditorUiMode__(mode){
  try{
    const titleEl = document.querySelector('#page-inserisci .page-title');
    if (titleEl) titleEl.textContent = (mode === 'edit') ? 'Modifica spesa' : 'Inserisci spesa';
  }catch(_){ }
  try{
    const btn = document.getElementById('btnSaveSpesa');
    if (btn) btn.textContent = 'Salva';
  }catch(_){ }
}

function __exitSpesaEditMode__(){
  try{ delete state.spesaEditingId; }catch(_){ state.spesaEditingId = null; }
  __setSpesaEditorUiMode__('create');
}

function __enterSpesaEditMode__(row){
  if (!row) return;
  try{ state.spesaEditingId = String(row?.id || '').trim(); }catch(_){ }
  __setSpesaEditorUiMode__('edit');
  try{ $("#spesaImporto").value = String(Number(row?.importoLordo || row?.importo_lordo || row?.importo || 0) || ''); }catch(_){ }
  try{ $("#spesaMotivazione").value = String(row?.motivazione || row?.motivo || '').trim(); }catch(_){ }
  try{ __setSpeseBudgetMode__(__isFuoriBudgetSpesa__(row) ? 'fuori' : 'budget', { render:false }); }catch(_){ }
  try{
    const rawCat = String(row?.categoria || row?.cat || '').trim();
    __setSpesaCategoriaValue__(rawCat === 'FUORI_BUDGET' ? '' : rawCat);
  }catch(_){ }
  try{ $("#spesaData").value = String(row?.dataSpesa || row?.data || row?.data_spesa || '').slice(0,10) || todayISO(); }catch(_){ }
  try{ __setupSpesaCategoryButtons__(); }catch(_){ }
  try{ showPage('inserisci'); }catch(_){ }
  try{ window.scrollTo({ top:0, behavior:'smooth' }); }catch(_){ }
}

async function saveSpesa(){
  const dataSpesa = $("#spesaData").value;
  const categoria = $("#spesaCategoria").value;
  const importoLordo = Number($("#spesaImporto").value);
  const motivazione = ($("#spesaMotivazione").value || "").trim();
  const editingId = String(state?.spesaEditingId || '').trim();

  if (!isFinite(importoLordo) || importoLordo <= 0) return toast("Importo non valido");
  if (!motivazione) return toast("Motivazione obbligatoria");
  if (!dataSpesa) return toast("Data obbligatoria");
  if (!categoria) return toast("Categoria obbligatoria");

  // se motivazione nuova => salva per futuro
  const canonical = findCanonicalMotivazione(motivazione);
  // Se esiste già (spazi/case/accenti diversi), non salvare duplicati
  if (canonical) {
    $("#spesaMotivazione").value = canonical; // versione canonica
  } else {
    try {
      await api("motivazioni", { method:"POST", body:{ motivazione }, showLoader:false });
      await loadMotivazioni();
    } catch (_) {}
  }

  const existing = editingId ? (__getSpesaById__(editingId) || {}) : null;
  const payload = {
    id: editingId || undefined,
    dataSpesa,
    categoria,
    motivazione,
    importoLordo,
    fuoriBudget: __speseBudgetModeIsFuori__(),
    fuori_budget: __speseBudgetModeIsFuori__() ? 1 : 0,
    budgetMode: __speseBudgetModeIsFuori__() ? 'fuori' : 'budget',
    note: String(existing?.note || ''),
    createdAt: String(existing?.createdAt || existing?.created_at || '') || undefined
  };

  // Salva spesa
  const res = await api("spese", { method:(editingId ? "PUT" : "POST"), body: payload });

  // UX: ritorno IMMEDIATO alla lista spese (senza attendere refresh dati)
  try { setSpeseView("list"); } catch (_) {}
  try { showPage("spese"); } catch (_) {}

  // Aggiornamento ottimistico: mostra subito il record aggiornato in lista
  try{
    const effectiveId = editingId || (res && (res.id || res.spesaId || (res.data && (res.data.id || res.data.spesaId)))) || ("tmp-" + Date.now());
    const nextItem = Object.assign({}, existing || {}, { id: effectiveId, dataSpesa, categoria, motivazione, importoLordo, fuoriBudget: __speseBudgetModeIsFuori__(), fuori_budget: __speseBudgetModeIsFuori__() ? 1 : 0, budgetMode: __speseBudgetModeIsFuori__() ? 'fuori' : 'budget', updatedAt: (typeof __nowIso__ === 'function' ? __nowIso__() : new Date().toISOString()) });
    const upsert = (rows) => {
      const list = Array.isArray(rows) ? rows.slice() : [];
      const idx = list.findIndex((row) => String(row?.id || '') === String(effectiveId));
      if (idx >= 0) list[idx] = Object.assign({}, list[idx], nextItem);
      else list.unshift(nextItem);
      return list;
    };
    state.spese = upsert(state.spese);
    try{
      state.speseAll = upsert(state.speseAll);
      state.reportAll = buildReportFromSpese(state.speseAll);
    }catch(_){ }
    try{
      state.report = buildReportFromSpese(Array.isArray(state.spese) ? state.spese : []);
    }catch(_){ }
    if (state.page === "spese" && state.speseView === "list") {
      try{ renderSpese(); }catch(_){ }
    }
  }catch(_){ }

  toast(editingId ? "Spesa aggiornata" : "Salvato");
  __exitSpesaEditMode__();
  resetInserisci();

  // refresh dati in background (non blocca il ritorno alla lista)
  try {
    invalidateApiCache("spese|");
    invalidateApiCache("report|");
    ensurePeriodData({ showLoader:false, force:true })
      .then(() => {
        try{
          if (state.page === "spese" && state.speseView === "list") renderSpese();
          if (state.page === "riepilogo") renderRiepilogo();
          if (state.page === "grafico") renderGrafico();
        }catch(_){ }
      })
      .catch(()=>{});
  } catch(_) {}

}

/* 2) SPESE */
function renderSpese(){
  try{ __syncSpeseBudgetToggles__(); }catch(_){ }
  const list = document.getElementById("speseList");
  if (!list) return;
  list.innerHTML = "";

  const items = __getStatsSpese();
  if (!items.length){
    list.innerHTML = `<div style="font-size:13px; opacity:.75; padding:8px 2px;">Nessuna spesa nel periodo.</div>`;
    return;
  }

  items.forEach(s => {
    const el = document.createElement("div");
    el.className = "item";

    const importo = Number(s.importoLordo || 0);
    const data = formatShortDateIT(s.dataSpesa || s.data || s.data_spesa || "");
    const motivo = escapeHtml((s.motivazione || s.motivo || "").toString());

    el.innerHTML = `
      <div class="spesa-card-content">
        <div class="item-top">
          <div class="spesa-line" title="${motivo}">
            <span class="spesa-imp">${euro(importo)}</span>
            <span class="spesa-sep">·</span>
            <span class="spesa-date">${data}</span>
            <span class="spesa-sep">·</span>
            <span class="spesa-motivo">${motivo}</span>
          </div>
          <button class="delbtn delbtn-x" type="button" aria-label="Elimina record" data-del="${s.id}">Elimina</button>
        </div>
      </div>
    `;

    const btn = el.querySelector("[data-del]");
    if (btn){
      // iOS/Safari: tap affidabile anche su PWA (Operatore)
      bindFastTap(btn, async (ev) => {
        try{
          ev && ev.preventDefault && ev.preventDefault();
          ev && ev.stopPropagation && ev.stopPropagation();
          ev && ev.stopImmediatePropagation && ev.stopImmediatePropagation();
        }catch(_){}
        if (!confirm("Eliminare definitivamente questa spesa?")) return;
        await api("spese", { method:"DELETE", params:{ id: s.id } });
        toast("Spesa eliminata");
        invalidateApiCache("spese|");
        invalidateApiCache("report|");
        await ensurePeriodData({ showLoader:false, force:true });
        renderSpese();
      });
    }

    list.appendChild(el);
  });
}


/* 3) RIEPILOGO */
function renderRiepilogo(){
  const r = buildReportFromSpese(state.spese);
  state.report = r;

  $("#kpiTotSpese").textContent = euro(r.totals.importoLordo);
  $("#kpiIvaDetraibile").textContent = euro(r.totals.ivaDetraibile);
  $("#kpiImponibile").textContent = euro(r.totals.imponibile);

  // Lista semplice: 5 righe (categoria + totale lordo)
  const container = $("#byCat");
  if (!container) return;

  const by = r.byCategoria || {};
  const order = ["CONTANTI","TASSA_SOGGIORNO","IVA_22","IVA_10","IVA_4"];

  container.innerHTML = "";
  for (const k of order){
    const o = by[k] || { importoLordo: 0 };
    const row = document.createElement("div");
    row.className = "catitem";
    row.innerHTML = `
      <div class="catitem-left">
        <span class="badge" style="background:${hexToRgba(COLORS[k] || "#d8bd97", 0.20)}">${categoriaLabel(k)}</span>
        <div class="catitem-name">Totale</div>
      </div>
      <div class="catitem-total">${euro(o.importoLordo)}</div>
    `;
    container.appendChild(row);
  }
}

/* 4) GRAFICO */
function renderGrafico(){
  // Usa report locale calcolato dalle spese correnti (evita mix multi-account)
  const r = buildReportFromSpese(state.spese);
  state.report = r;

  const by = r.byCategoria || {};
  const order = ["CONTANTI","TASSA_SOGGIORNO","IVA_22","IVA_10","IVA_4"];
  const values = order.map(k => Number(by[k]?.importoLordo || 0));
  const total = values.reduce((a,b)=>a+b,0);

  drawPie("pieCanvas", order.map((k,i)=>({
    key: k,
    label: categoriaLabel(k),
    value: values[i],
    color: COLORS[k] || "#999999"
  })));

  const leg = $("#pieLegend");
  if (!leg) return;
  leg.innerHTML = "";

  order.forEach((k,i) => {
    const v = values[i];
    const pct = total > 0 ? (v/total*100) : 0;
    const row = document.createElement("div");
    row.className = "legrow";
    row.innerHTML = `
      <div class="legleft">
        <div class="dot" style="background:${COLORS[k] || "#999"}"></div>
        <div class="legname">${categoriaLabel(k)}</div>
      </div>
      <div class="legright">${pct.toFixed(1)}% · ${euro(v)}</div>
    `;
    leg.appendChild(row);
  });
}

/* PIE DRAW (no librerie) */
function drawPie(canvasId, slices, opts){
  const canvas = document.getElementById(canvasId);
  if (!canvas) return;
  opts = opts || {};
  const centerTitle = (opts.centerTitle != null ? String(opts.centerTitle) : "Totale");
  const centerFormatter = (typeof opts.centerFormatter === "function") ? opts.centerFormatter : euro;
  const showCenter = (opts.showCenter !== false);

  const parentW = (canvas.parentElement && canvas.parentElement.clientWidth) ? canvas.parentElement.clientWidth : 0;
  const parentH = (canvas.parentElement && canvas.parentElement.clientHeight) ? canvas.parentElement.clientHeight : 0;
  const baseW = parentW || Math.floor(window.innerWidth * 0.78);
  const baseSize = (opts.size || (parentH > 0 ? Math.min(baseW, parentH) : baseW));
  const cssSize = Math.min((opts.maxSize || 320), Math.max((opts.minSize || 120), Math.floor(baseSize - 8)));
  const dpr = window.devicePixelRatio || 1;
  canvas.style.width = cssSize + "px";
  canvas.style.height = cssSize + "px";
  canvas.width = Math.floor(cssSize * dpr);
  canvas.height = Math.floor(cssSize * dpr);

  const ctx = canvas.getContext("2d");
  ctx.setTransform(dpr,0,0,dpr,0,0);
  ctx.clearRect(0,0,cssSize,cssSize);

  const total = slices.reduce((a,s)=>a+Math.max(0,Number(s.value||0)),0);
  const cx = cssSize/2, cy = cssSize/2;
  const r = cssSize/2 - 10;

  const isDark = !!(document && document.body && document.body.classList && document.body.classList.contains('ddae-dark'));
  const ringStroke = isDark ? "rgba(148,163,184,0.22)" : "rgba(15,23,42,0.08)";
  const holeTextSoft = isDark ? "rgba(226,232,240,0.82)" : "rgba(15,23,42,0.75)";
  const holeTextStrong = isDark ? "rgba(248,250,252,0.98)" : "rgba(15,23,42,0.92)";

  let ang = -Math.PI/2;
  if (total <= 0){
    ctx.save();
    ctx.globalCompositeOperation = "destination-out";
    ctx.beginPath();
    ctx.arc(cx, cy, r*0.58, 0, Math.PI*2);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = holeTextSoft;
    ctx.font = "600 12px system-ui";
    ctx.textAlign = "center";
    ctx.fillText("Nessun dato", cx, cy+4);
    return;
  }

  slices.forEach(s => {
    const v = Math.max(0, Number(s.value||0));
    const a = (v/total) * Math.PI*2;
    ctx.beginPath();
    ctx.moveTo(cx,cy);
    ctx.arc(cx,cy,r-8,ang,ang+a);
    ctx.closePath();
    ctx.fillStyle = s.color;
    ctx.fill();

    ctx.strokeStyle = "rgba(255,255,255,0.65)";
    ctx.lineWidth = 2;
    ctx.stroke();

    ang += a;
  });

  // inner hole: truly transparent, so no residual surface color remains under the graph
  ctx.save();
  ctx.globalCompositeOperation = "destination-out";
  ctx.beginPath();
  ctx.arc(cx, cy, r*0.58, 0, Math.PI*2);
  ctx.fill();
  ctx.restore();

  if (showCenter){
    ctx.fillStyle = holeTextSoft;
    ctx.font = "900 12px system-ui";
    ctx.textAlign = "center";
    ctx.fillText(centerTitle, cx, cy-4);
    ctx.fillStyle = holeTextStrong;
    ctx.font = "950 14px system-ui";
    ctx.fillText(centerFormatter(total), cx, cy+14);
  }
}


function drawMonthlyPctBars(canvasId, pctValues, colors, opts){
  const canvas = document.getElementById(canvasId);
  if (!canvas) return;
  opts = opts || {};
  const parentW = (canvas.parentElement && canvas.parentElement.clientWidth) ? canvas.parentElement.clientWidth : 0;
  const baseW = parentW || Math.floor(window.innerWidth * 0.86);
  const cssW = Math.min(340, Math.max(140, Math.floor(baseW - 4)));
  const cssH = 180;
  const dpr = window.devicePixelRatio || 1;

  canvas.style.width = cssW + "px";
  canvas.style.height = cssH + "px";
  canvas.width = Math.floor(cssW * dpr);
  canvas.height = Math.floor(cssH * dpr);

  const ctx = canvas.getContext("2d");
  ctx.setTransform(dpr,0,0,dpr,0,0);
  ctx.clearRect(0,0,cssW,cssH);

  const isDark = !!(document && document.body && document.body.classList && document.body.classList.contains('ddae-dark'));
  const holeTextSoft = isDark ? "rgba(226,232,240,0.82)" : "rgba(15,23,42,0.75)";

  const vals = Array.isArray(pctValues) ? pctValues : [];
  const v12 = new Array(12).fill(0).map((_,i)=>Math.max(0, Math.min(100, Number(vals[i]||0)||0)));
  const cols = Array.isArray(colors) ? colors : [];
  const padL = 22, padR = 10, padT = 12, padB = 26;

  // background panel
  ctx.fillStyle = "rgba(255,255,255,0.55)";
  ctx.strokeStyle = "rgba(15,23,42,0.08)";
  ctx.lineWidth = 1;
  roundRect(ctx, 6, 6, cssW-12, cssH-12, 16);
  ctx.fill();
  ctx.stroke();

  const chartW = cssW - padL - padR;
  const chartH = cssH - padT - padB;

  // baseline
  ctx.beginPath();
  ctx.moveTo(padL, padT + chartH);
  ctx.lineTo(padL + chartW, padT + chartH);
  ctx.strokeStyle = "rgba(15,23,42,0.12)";
  ctx.lineWidth = 1;
  ctx.stroke();

  const barGap = 6;
  const barW = Math.max(6, Math.floor((chartW - barGap*11) / 12));

  for (let i=0;i<12;i++){
    const x = padL + i*(barW + barGap);
    const h = (v12[i] / 100) * chartH;
    const y = padT + (chartH - h);

    ctx.fillStyle = cols[i] || "#2b7cb4";
    roundRect(ctx, x, y, barW, h, Math.min(10, barW/2));
    ctx.fill();

    // month label (1 letter)
    ctx.fillStyle = holeTextSoft;
    ctx.font = "700 10px system-ui";
    ctx.textAlign = "center";
    ctx.textBaseline = "alphabetic";
    const label = (opts.labels && opts.labels[i]) ? String(opts.labels[i]) : String((__MONTHS_IT[i]||"").slice(0,1)).toUpperCase();
    ctx.fillText(label, x + barW/2, padT + chartH + 18);
  }

  // 100% label
  ctx.fillStyle = holeTextSoft;
  ctx.font = "800 10px system-ui";
  ctx.textAlign = "right";
  ctx.fillText("100%", padL + chartW, padT + 10);
}

function roundRect(ctx, x, y, w, h, r){
  const rr = Math.max(0, Math.min(r, Math.min(w,h)/2));
  ctx.beginPath();
  ctx.moveTo(x+rr, y);
  ctx.arcTo(x+w, y, x+w, y+h, rr);
  ctx.arcTo(x+w, y+h, x, y+h, rr);
  ctx.arcTo(x, y+h, x, y, rr);
  ctx.arcTo(x, y, x+w, y, rr);
  ctx.closePath();
}


function __graphColorStoreKey__(graphKey){
  const year = String(state && (state.exerciseYear || loadExerciseYear && loadExerciseYear()) || new Date().getFullYear());
  return `ddae_graph_colors_${year}_${String(graphKey || "generic")}`;
}

function __normalizeHexColor__(value, fallback){
  const v = String(value || "").trim();
  if (/^#[0-9a-fA-F]{6}$/.test(v)) return v.toUpperCase();
  return String(fallback || "#2B7CB4").toUpperCase();
}

function __graphColorValueToHex__(value, fallback){
  const raw = String(value || '').trim();
  if (!raw) return __normalizeHexColor__(fallback, '#2B7CB4');
  if (/^#[0-9a-fA-F]{6}$/.test(raw)) return raw.toUpperCase();
  try{ return __operatoreColorHex__(__parseOperatoreColorSpec__(raw).spec) || __normalizeHexColor__(fallback, '#2B7CB4'); }catch(_){ }
  return __normalizeHexColor__(fallback, '#2B7CB4');
}

function __closestGraphColorSpec__(value){
  const target = __graphColorValueToHex__(value, '#2B7CB4').toUpperCase();
  let bestSpec = 'blue-4';
  let bestDist = Number.POSITIVE_INFINITY;
  try{
    (__OPERATORI_COLOR_KEYS__ || []).forEach((base) => {
      for (let shade = 1; shade <= __OPERATORI_COLOR_SHADE_COUNT__; shade++){
        const spec = `${base}-${shade}`;
        const hex = __graphColorValueToHex__(spec, '#2B7CB4');
        const r1 = parseInt(target.slice(1,3), 16), g1 = parseInt(target.slice(3,5), 16), b1 = parseInt(target.slice(5,7), 16);
        const r2 = parseInt(hex.slice(1,3), 16), g2 = parseInt(hex.slice(3,5), 16), b2 = parseInt(hex.slice(5,7), 16);
        const dist = Math.pow(r1-r2,2) + Math.pow(g1-g2,2) + Math.pow(b1-b2,2);
        if (dist < bestDist){ bestDist = dist; bestSpec = spec; }
      }
    });
  }catch(_){ }
  return bestSpec;
}

function __loadGraphColorMap__(graphKey){
  try{
    const raw = localStorage.getItem(__graphColorStoreKey__(graphKey));
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveGraphColorMap__(graphKey, map){
  try{ localStorage.setItem(__graphColorStoreKey__(graphKey), JSON.stringify(map || {})); }catch(_){ }
}

function __applyGraphCustomColors__(graphKey, slices){
  const arr = Array.isArray(slices) ? slices.map(s => ({...s})) : [];
  const map = __loadGraphColorMap__(graphKey);
  arr.forEach((sl)=>{
    const key = String(sl && sl.label || '');
    if (key && map[key]) sl.color = __graphColorValueToHex__(map[key], sl.color);
    else sl.color = __graphColorValueToHex__(sl && sl.color, '#2B7CB4');
  });
  return arr;
}

function __statCardTextColorStoreKey__(pageKey){
  const year = String(state && (state.exerciseYear || (typeof loadExerciseYear === "function" && loadExerciseYear())) || new Date().getFullYear());
  return `ddae_stat_card_text_${year}_${String(pageKey || "generic")}`;
}

function __statCardStoreKey__(pageKey){
  return `dDAE_statcard_colors_${String(pageKey || '').trim().toLowerCase()}`;
}

function __loadStatCardColorMap__(pageKey){
  try{
    const raw = localStorage.getItem(__statCardStoreKey__(pageKey));
    return raw ? (JSON.parse(raw) || {}) : {};
  }catch(_){ return {}; }
}

function __saveStatCardColorMap__(pageKey, map){
  try{ localStorage.setItem(__statCardStoreKey__(pageKey), JSON.stringify(map || {})); }catch(_){ }
}

function __loadStatCardTextColorMap__(pageKey){
  const pairMap = __loadStatCardColorMap__(pageKey);
  const out = {};
  try{ Object.keys(pairMap || {}).forEach((key)=>{ const item = pairMap[key]; if (item && typeof item === 'object' && item.fg) out[key] = item.fg; else if (item) out[key] = item; }); }catch(_){ }
  return out;
}

function __saveStatCardTextColorMap__(pageKey, map){
  const current = __loadStatCardColorMap__(pageKey);
  Object.keys(map || {}).forEach((key) => {
    const prev = __tagColorPairFromValue__(current[key], map[key] || 'blue-4');
    current[key] = { bg: prev.bg, border: prev.border || prev.bg, fg: map[key] || prev.fg || '', opacity: __designBgOpacityNormalize__(prev.opacity ?? 0.80) };
  });
  __saveStatCardColorMap__(pageKey, current);
}

function __getStatCardColorPair__(pageKey, cardKey, fallback){
  let baseFallback = fallback || 'blue-4';
  try{
    const themedPages = new Set(['statgen','statspese','statmensili','statprenotazioni','statchannel','statpulizie']);
    if (themedPages.has(String(pageKey || '').trim().toLowerCase())){
      const visual = __statisticsCardThemeRead__();
      baseFallback = { bg: visual.bg || 'blue-4', border: visual.border || visual.bg || 'blue-4', fg: '', bold: !!visual.bold };
    }
  }catch(_){ }
  const normalizedFallback = __tagColorPairFromValue__(baseFallback || 'blue-4', (baseFallback && baseFallback.bg) || fallback || 'blue-4');
  try{
    const map = __loadStatCardColorMap__(pageKey);
    const saved = map[String(cardKey || '')];
    if (saved) return __tagColorPairFromValue__(saved, normalizedFallback.bg || 'blue-4');
  }catch(_){ }
  return normalizedFallback;
}

function __getStatCardTextColorHex__(pageKey, cardKey, fallback){
  const pair = __getStatCardColorPair__(pageKey, cardKey, fallback || '#2B7CB4');
  return __tagColorTextHex__(pair.bg || fallback || '#2B7CB4', pair.fg || '', false);
}

function __applyStatCardTextColor__(el, pageKey, cardKey, fallback){
  if (!el) return;
  try{
    const pair = __getStatCardColorPair__(pageKey, cardKey, fallback || '#2B7CB4');
    const bgHex = __graphColorValueToHex__(pair.bg || fallback || '#2B7CB4', fallback || '#2B7CB4');
    const borderHex = __graphColorValueToHex__(pair.border || pair.bg || fallback || '#2B7CB4', pair.bg || fallback || '#2B7CB4');
    const safePageKey = String(pageKey || '').trim().toLowerCase();
    const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    const useForcedDarkSurface = isDark && ['statgen','statspese','statmensili','statprenotazioni','statchannel','statpulizie','statcancellazioni'].includes(safePageKey);
    const fgHex = useForcedDarkSurface
      ? __graphColorValueToHex__(pair.fg || bgHex || fallback || '#2B7CB4', bgHex || fallback || '#2B7CB4')
      : __tagColorTextHex__(pair.bg || bgHex, pair.fg || '', false);
    const bgOpacity = __designBgOpacityNormalize__(pair.opacity ?? 0.80);
    const borderOpacity = 1;
    const resolvedBg = useForcedDarkSurface ? 'rgba(12,23,46,0.96)' : hexToRgba(bgHex, bgOpacity);
    const resolvedBorder = useForcedDarkSurface ? 'rgba(148,163,184,0.28)' : hexToRgba(borderHex, borderOpacity);
    el.style.setProperty('--statbg', bgHex);
    el.style.setProperty('--mcol', bgHex);
    el.style.setProperty('--cardtext', fgHex);
    el.style.setProperty('--statborder', borderHex);
    el.style.setProperty('color', fgHex, 'important');
    el.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
    el.style.setProperty('background', resolvedBg, 'important');
    el.style.setProperty('background-color', resolvedBg, 'important');
    el.style.setProperty('border', `1px solid ${resolvedBorder}`, 'important');
    try{
      el.querySelectorAll('.stat-name, .stat-val, .month-name, .month-val, .month-occ, .month-fill, .stat-ico-wrap, .kpi-label, .kpi-value, .fin-head, .fin-head div, .fin-label, .fin-val, .stats-graph-card-title, .stats-graph-note, .stats-graph-legend, .piscina-nav-center, .piscina-today').forEach((node)=>{
        try{
          if (node.classList && node.classList.contains('month-fill')){
            node.style.setProperty('background', fgHex, 'important');
          } else if (node.classList && node.classList.contains('stat-ico-wrap')){
            node.style.setProperty('background', useForcedDarkSurface ? 'rgba(12,23,46,0.96)' : hexToRgba(bgHex, bgOpacity), 'important');
            node.style.setProperty('background-color', useForcedDarkSurface ? 'rgba(12,23,46,0.96)' : hexToRgba(bgHex, bgOpacity), 'important');
            node.style.setProperty('border-color', resolvedBorder, 'important');
            node.style.setProperty('color', fgHex, 'important');
            node.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
          } else {
            node.style.setProperty('color', fgHex, 'important');
            node.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
          }
        }catch(_){ }
      });
    }catch(_){ }
    __applyVisualTextWeight__(el, pair);
    try{
      const statWeight = pair && pair.bold ? '700' : '400';
      el.style.setProperty('font-weight', statWeight, 'important');
      el.querySelectorAll('.stat-name, .stat-val, .month-name, .month-val, .month-occ, .kpi-label, .kpi-value, .fin-head, .fin-head div, .fin-label, .fin-val, .stats-graph-card-title, .stats-graph-note, .stats-graph-legend, .piscina-nav-center, .piscina-today').forEach((node) => {
        try{ node.style.setProperty('font-weight', statWeight, 'important'); }catch(_){ }
      });
    }catch(_){ }
  }catch(_){ }
}

function __refreshStatCardsPage__(pageKey){
  try{
    if (pageKey === 'statgen') return renderStatGen();
    if (pageKey === 'statspese') return renderStatSpese();
    if (pageKey === 'statmensili') return renderStatMensili();
    if (pageKey === 'statprenotazioni') return renderStatRicevute();
    if (pageKey === 'statchannel') return renderStatChannel();
    if (pageKey === 'statpulizie') return renderStatPulizie(Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []);
    if (pageKey === 'statcancellazioni') return renderStatCancellazioni();
    if (pageKey === 'statazienda') return renderStatAzienda();
    if (pageKey === 'statamministratore') return renderStatAmministratore();
  }catch(_){ }
}

function __monthlyStatCardGraphSpecFromPair__(pair, fallback){
  const normalizedPair = __tagColorPairFromValue__(pair, fallback || 'blue-4');
  const textSource = normalizedPair.fg || __tagColorTextHex__(normalizedPair.bg || fallback || '#2B7CB4', normalizedPair.fg || '', false);
  return __closestGraphColorSpec__(textSource || fallback || '#2B7CB4');
}

function __statCardCategoryTargetKeys__(pageKey, fallbackKey){
  const safePageKey = String(pageKey || '').trim().toLowerCase();
  const out = [];
  const seen = new Set();
  const pushKey = (value) => {
    const safe = String(value || '').trim();
    if (!safe || seen.has(safe)) return;
    seen.add(safe);
    out.push(safe);
  };
  try{
    const presets = __statisticsCardThemeTargetKeys__();
    (Array.isArray(presets?.[safePageKey]) ? presets[safePageKey] : []).forEach(pushKey);
  }catch(_){ }
  try{ Object.keys(__loadStatCardColorMap__(safePageKey) || {}).forEach(pushKey); }catch(_){ }
  try{
    document.querySelectorAll(`[data-stat-card-key]`).forEach((node) => {
      try{
        const owner = node.closest ? node.closest(`[id="page-${safePageKey}"]`) : null;
        if (owner) pushKey(node.dataset.statCardKey || '');
      }catch(_){ }
    });
  }catch(_){ }
  pushKey(fallbackKey);
  return out;
}

function __statCardCategoryLabel__(pageKey){
  const safePageKey = String(pageKey || '').trim().toLowerCase();
  if (safePageKey === 'statgen') return 'Generali';
  if (safePageKey === 'statspese') return 'Spese';
  if (safePageKey === 'statmensili') return 'Mensili';
  if (safePageKey === 'statprenotazioni') return 'Con / senza ricevuta';
  if (safePageKey === 'statchannel') return 'PMS';
  if (safePageKey === 'statpulizie') return 'Ore pulizia';
  if (safePageKey === 'statcancellazioni') return 'Cancellazioni';
  if (safePageKey === 'statazienda') return 'Azienda';
  if (safePageKey === 'statamministratore') return 'Ricavi';
  return 'stessa tipologia';
}

async function __applyStatCardChangesToCategory__(pageKey, cardKey, payload, changed){
  try{
    const safePageKey = String(pageKey || '').trim().toLowerCase();
    const keys = __statCardCategoryTargetKeys__(safePageKey, cardKey);
    if (!keys.length) return;
    const map = __loadStatCardColorMap__(safePageKey);
    keys.forEach((targetKey) => {
      const currentPair = __getStatCardColorPair__(safePageKey, targetKey, 'blue-4');
      const nextPair = __tagColorPairFromValue__(map[targetKey] || currentPair, currentPair.bg || 'blue-4');
      const next = (changed && changed.next && typeof changed.next === 'object') ? changed.next : {};
      if (changed?.bg) nextPair.bg = __normalizeOperatoreColor__(next.bg || nextPair.bg || currentPair.bg || 'blue-4');
      if (changed?.border) nextPair.border = __normalizeOperatoreColor__(next.border || nextPair.border || nextPair.bg || currentPair.border || currentPair.bg || 'blue-4');
      if (changed?.fg) nextPair.fg = __normalizeOptionalOperatoreColor__(next.fg || '');
      if (changed?.opacity) nextPair.opacity = __designBgOpacityNormalize__(next.opacity ?? nextPair.opacity ?? currentPair.opacity ?? __designBgOpacityRead__());
      nextPair.border = __normalizeOperatoreColor__(nextPair.border || nextPair.bg || 'blue-4');
      map[targetKey] = {
        bg: __normalizeOperatoreColor__(nextPair.bg || currentPair.bg || 'blue-4'),
        border: __normalizeOperatoreColor__(nextPair.border || nextPair.bg || currentPair.border || currentPair.bg || 'blue-4'),
        fg: __normalizeOptionalOperatoreColor__(nextPair.fg || ''),
        opacity: __designBgOpacityNormalize__(nextPair.opacity ?? currentPair.opacity ?? __designBgOpacityRead__()),
        bold: changed?.bold ? !!next.bold : !!currentPair.bold
      };
    });
    __saveStatCardColorMap__(safePageKey, map);
    if (safePageKey === 'statmensili'){
      try{
        const graphMap = __loadGraphColorMap__('occupazione-mensile');
        keys.forEach((targetKey) => {
          const pair = __tagColorPairFromValue__(map[targetKey] || {}, 'blue-4');
          graphMap[targetKey] = __monthlyStatCardGraphSpecFromPair__(pair, pair.bg || 'blue-4');
        });
        __saveGraphColorMap__('occupazione-mensile', graphMap);
      }catch(_){ }
      try{ __refreshStatGraphPreviews__(); }catch(_){ }
    }
    try{ __refreshStatCardsPage__(safePageKey); }catch(_){ }
  }catch(_){ }
}

function __openStatCardTextColorPicker__(pageKey, cardKey, currentColor, onDone){
  const currentPair = __getStatCardColorPair__(pageKey, cardKey, currentColor || '#2B7CB4');
  __tagColorPopupOpen__('stat-card-text', currentPair, (payload) => {
    const map = __loadStatCardColorMap__(pageKey);
    const safeKey = String(cardKey || '').trim();
    const pair = __tagColorPairFromValue__(map[safeKey] || currentPair, currentPair.bg || '#2B7CB4');
    const payloadColors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    const nextBg = __parseOperatoreColorSpec__(payloadColors.bg || pair.bg || currentPair.bg || '#2B7CB4').spec;
    const nextBorder = __parseOperatoreColorSpec__(payloadColors.border || pair.border || nextBg || currentPair.bg || '#2B7CB4').spec;
    const nextFgRaw = String(payloadColors.fg || '').trim();
    pair.bg = nextBg;
    pair.border = nextBorder || nextBg;
    pair.fg = nextFgRaw ? __parseOperatoreColorSpec__(nextFgRaw).spec : '';
    pair.opacity = __designBgOpacityNormalize__(payload?.opacity ?? pair.opacity ?? currentPair.opacity ?? __designBgOpacityRead__());
    pair.bold = !!(payload?.bold ?? pair.bold ?? currentPair.bold ?? false);
    if (safeKey) map[safeKey] = { bg: pair.bg, border: pair.border || pair.bg, fg: pair.fg || '', opacity: pair.opacity, bold: !!pair.bold };
    __saveStatCardColorMap__(pageKey, map);
    if (pageKey === 'statmensili' && safeKey){
      try{
        const graphMap = __loadGraphColorMap__('occupazione-mensile');
        graphMap[safeKey] = __monthlyStatCardGraphSpecFromPair__(pair, currentPair.bg || '#2B7CB4');
        __saveGraphColorMap__('occupazione-mensile', graphMap);
      }catch(_){ }
      try{ __refreshStatGraphPreviews__(); }catch(_){ }
    }
    const nextHex = __tagColorTextHex__(pair.bg || currentColor || '#2B7CB4', pair.fg || '', false);
    if (typeof onDone === 'function') onDone(nextHex);
    __refreshStatCardsPage__(pageKey);
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:__designBgOpacityNormalize__(currentPair.opacity ?? __designBgOpacityRead__()), defaultMode:'bg', fallbackBg:currentPair.bg || '#2B7CB4', applyCategory:{ message:(__statCardCategoryLabel__(pageKey) === 'stessa tipologia' ? 'Applicare le modifiche a tutte le card della stessa tipologia?' : `Applicare le modifiche a tutte le card ${__statCardCategoryLabel__(pageKey)}?`), apply: async(payload, changed) => { await __applyStatCardChangesToCategory__(pageKey, cardKey, payload, changed); } } });
}

function __bindStatCardColorLongPress__(el, pageKey, cardKey, fallback){
  if (!el || el.dataset.statCardColorBound === '1') return;
  el.dataset.statCardColorBound = '1';
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const clearSelection = ()=>{
    try{
      const sel = window.getSelection && window.getSelection();
      if (sel && sel.removeAllRanges) sel.removeAllRanges();
    }catch(_){ }
  };
  const blockEvent = (e)=>{
    try{ if (e && e.preventDefault) e.preventDefault(); }catch(_){ }
    try{ if (e && e.stopPropagation) e.stopPropagation(); }catch(_){ }
    clearSelection();
    return false;
  };
  const start = (e)=>{
    try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    fired = false;
    clear();
    clearSelection();
    timer = setTimeout(()=>{
      fired = true;
      try{ el.classList.add('is-pressing'); }catch(_){ }
      try{ if (document.activeElement && document.activeElement.blur) document.activeElement.blur(); }catch(_){ }
      clearSelection();
      const current = getComputedStyle(el).getPropertyValue('--cardtext') || fallback || '#2B7CB4';
      __openStatCardTextColorPicker__(pageKey, cardKey, current, (hex)=>{
        try{ if (hex) el.style.setProperty('--cardtext', hex); }catch(_){ }
        try{ el.classList.remove('is-pressing'); }catch(_){ }
        clearSelection();
      });
    }, 500);
  };
  const stop = (e)=>{
    clear();
    if (fired){
      blockEvent(e);
      setTimeout(()=>{ fired = false; try{ el.classList.remove('is-pressing'); }catch(_){ } clearSelection(); }, 0);
      return;
    }
    try{ el.classList.remove('is-pressing'); }catch(_){ }
    clearSelection();
  };
  const swallowClick = (e)=>{
    if (!fired) return;
    blockEvent(e);
    fired = false;
  };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ el.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ el.addEventListener(evt, stop, { passive:false }); }catch(_){ } });
  try{ el.addEventListener('click', swallowClick, true); }catch(_){ }
  try{ el.addEventListener('contextmenu', (e)=>{ blockEvent(e); }, true); }catch(_){ }
  try{ el.addEventListener('selectstart', (e)=>{ blockEvent(e); }, true); }catch(_){ }
}

function __refreshStatGraphPreviews__(){
  try{
    if ((state && state.page) !== 'grafici') return;
    renderStatGrafici(Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []);
  }catch(_){ }
}

function __speseGraphLabelForKey__(key){
  try{
    const safeKey = String(key || '').trim();
    return safeKey ? categoriaLabel(safeKey) : '';
  }catch(_){ return String(key || '').trim(); }
}

function __updateSpeseGraphColorByKey__(key, spec, fallback){
  try{
    const safeKey = String(key || '').trim();
    if (!safeKey) return;
    const normalized = __parseOperatoreColorSpec__(spec || fallback || 'blue-4').spec;
    const label = __speseGraphLabelForKey__(safeKey);
    const map = __loadGraphColorMap__('spese');
    if (label) map[label] = normalized;
    __saveGraphColorMap__('spese', map);
  }catch(_){ }
}

function __refreshSpeseColorLinkedViews__(opts){
  const options = opts || {};
  try{ __refreshStatGraphPreviews__(); }catch(_){ }
  try{
    if (!options.skipSpeseList && state && state.page === 'spese' && state.speseView === 'list') renderSpese();
  }catch(_){ }
  try{
    if (state && state.page === 'statspese') renderStatSpese();
  }catch(_){ }
  try{
    const modal = document.getElementById('statSpesePieModal');
    if (modal && !modal.hidden) openStatSpesePieModal();
  }catch(_){ }
}

const __SPESA_CARD_OPACITY_STORAGE_KEY__ = 'dDAE_spese_card_opacity_v1';
const __SPESA_CARD_VISUAL_STORAGE_KEY__ = 'dDAE_spese_card_visual_v1';
const __TAX_QUARTER_VISUAL_STORAGE_KEY__ = 'dDAE_tax_quarter_visual_v1';
const __GUEST_FILTER_BUTTON_VISUAL_STORAGE_KEY__ = 'dDAE_guest_filter_button_visual_v1';
const __SINGLE_ACTION_BUTTON_VISUAL_STORAGE_KEY__ = 'dDAE_single_action_button_visual_v1';
const __TAX_PAGE_CARD_VISUAL_STORAGE_KEY__ = 'dDAE_tax_page_card_visual_v1';
const __TAX_PAGE_CARD_TARGET_IDS__ = ['taxTotalRow','taxPayingCard','taxKidsCard','taxReducedCard'];


function __loadSpesaCardVisualMap__(){
  try{
    const raw = localStorage.getItem(__SPESA_CARD_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveSpesaCardVisualMap__(map){
  try{ localStorage.setItem(__SPESA_CARD_VISUAL_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __setSpesaCardVisual__(key, visual, fallback){
  try{
    const safeKey = String(key || '').trim();
    if (!safeKey) return;
    const map = __loadSpesaCardVisualMap__();
    const current = __tagColorPairFromValue__(map[safeKey] || {}, fallback || 'blue-4');
    const opacity = __designBgOpacityNormalize__(visual?.opacity ?? current.opacity ?? __getSpesaCardOpacity__(safeKey, 0.22));
    map[safeKey] = {
      bg: __normalizeOperatoreColor__(visual?.bg || current.bg || fallback || 'blue-4'),
      border: __normalizeOperatoreColor__(visual?.border || current.border || visual?.bg || current.bg || fallback || 'blue-4'),
      fg: __normalizeOptionalOperatoreColor__(visual?.fg || current.fg || ''),
      opacity
    };
    __saveSpesaCardVisualMap__(map);
    __setSpesaCardOpacity__(safeKey, opacity);
  }catch(_){ }
}

function __loadTaxQuarterVisualMap__(){
  try{
    const raw = localStorage.getItem(__TAX_QUARTER_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveTaxQuarterVisualMap__(map){
  try{ localStorage.setItem(__TAX_QUARTER_VISUAL_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __defaultTaxQuarterVisual__(btn){
  const safeId = String(btn?.id || '').trim();
  const defaults = {
    taxQ1Btn:{ bg:'blue-4', border:'blue-4', fg:'', opacity:0.80 },
    taxQ2Btn:{ bg:'gray-4', border:'gray-4', fg:'', opacity:0.80 },
    taxQ3Btn:{ bg:'orange-4', border:'orange-4', fg:'', opacity:0.80 },
    taxQ4Btn:{ bg:'gray-5', border:'gray-5', fg:'', opacity:0.80 }
  };
  return __launcherVisualNormalize__(defaults[safeId] || { bg:'blue-4', border:'blue-4', fg:'white', opacity:0.80 }, 'blue-4');
}

function __taxQuarterVisualFor__(btn){
  const safeId = String(btn?.id || '').trim();
  const map = __loadTaxQuarterVisualMap__();
  return __launcherVisualNormalize__(map[safeId] || __defaultTaxQuarterVisual__(btn), __defaultTaxQuarterVisual__(btn).bg || 'blue-4');
}

function __saveTaxQuarterVisual__(btn, visual){
  try{
    const safeId = String(btn?.id || '').trim();
    if (!safeId) return;
    const map = __loadTaxQuarterVisualMap__();
    const fallback = __defaultTaxQuarterVisual__(btn);
    const clean = __launcherVisualNormalize__(visual || {}, fallback.bg || 'blue-4');
    map[safeId] = { bg: clean.bg || fallback.bg, border: clean.border || clean.bg || fallback.border || fallback.bg, fg: clean.fg || fallback.fg || '', opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.80) };
    __saveTaxQuarterVisualMap__(map);
  }catch(_){ }
}

function __applyTaxQuarterVisual__(btn){
  try{
    if (!btn) return;
    const visual = __taxQuarterVisualFor__(btn);
    const dark = !!(__isDarkModeEnabled__ && __isDarkModeEnabled__());
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    const fgHex = __tagColorTextHex__(visual.bg || 'blue-4', visual.fg || '', false);
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.80);
    if (dark){
      const resolved = __applyDarkAdaptiveSurface__(btn, visual, { fallbackBg:'blue-4', bgOpacity:Math.max(0.16, Math.min(0.26, opacity)), borderOpacity:0.32 }) || {};
      const darkText = resolved.textHex || __darkModeReadableTextHex__(fgHex, '#f8fbff');
      btn.style.setProperty('color', darkText, 'important');
      btn.style.setProperty('-webkit-text-fill-color', darkText, 'important');
    } else {
      btn.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('border', '1px solid ' + hexToRgba(borderHex, 1), 'important');
      btn.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
      btn.style.setProperty('color', fgHex, 'important');
      btn.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
      btn.style.setProperty('box-shadow', 'none', 'important');
      btn.style.setProperty('backdrop-filter', 'none', 'important');
      btn.style.setProperty('-webkit-backdrop-filter', 'none', 'important');
    }
  }catch(_){ }
}

function __applyTaxQuarterChangesToCategory__(payload, changed){
  try{
    document.querySelectorAll('.btn.tax-quarter').forEach((node)=>{
      try{
        const current = __taxQuarterVisualFor__(node);
        const next = __applyDesignPayloadToVisual__(current, payload, changed, current?.bg || 'blue-4');
        __saveTaxQuarterVisual__(node, next);
        __applyTaxQuarterVisual__(node);
      }catch(_){ }
    });
  }catch(_){ }
}

function __openTaxQuarterColorPicker__(btn){
  try{
    if (!btn) return;
    const current = __taxQuarterVisualFor__(btn);
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = {
        bg: colors.bg || current.bg || 'blue-4',
        border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
        fg: colors.fg || current.fg || '',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.80),
        bold: false
      };
      __saveTaxQuarterVisual__(btn, next);
      __applyTaxQuarterVisual__(btn);
    };
    const revertVisual = () => { __saveTaxQuarterVisual__(btn, current); __applyTaxQuarterVisual__(btn); };
    __tagColorPopupOpen__('tax-quarter', current, (payload) => { applyVisual(payload); }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:current.opacity ?? 0.80, defaultMode:'bg', fallbackBg:(current.bg || 'blue-4'), onPreview:applyVisual, onRevert:revertVisual, applyCategory:{ message:'Applicare il design a tutti i quadrimestri?', apply: async(payload, changed) => { __applyTaxQuarterChangesToCategory__(payload, changed); } } });
  }catch(_){ }
}

function __bindTaxQuarterColorHold__(btn){
  try{ if (!btn || btn.dataset.taxQuarterColorBound === '1') return; btn.dataset.taxQuarterColorBound = '1'; }catch(_){ if (!btn) return; }
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } };
  const start = (e)=>{ try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ } fired = false; clear(); timer = setTimeout(()=>{ fired = true; __openTaxQuarterColorPicker__(btn); }, 500); };
  const stop = (e)=>{ clear(); if (fired){ block(e); setTimeout(()=>{ fired = false; }, 0); } };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt, stop, { passive:false }); }catch(_){ } });
  try{ btn.addEventListener('click', (e)=>{ if (fired){ block(e); fired = false; } }, true); }catch(_){ }
  try{ btn.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
}



function __loadTaxPageCardVisualMap__(){
  try{
    const raw = localStorage.getItem(__TAX_PAGE_CARD_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveTaxPageCardVisualMap__(map){
  try{ localStorage.setItem(__TAX_PAGE_CARD_VISUAL_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __defaultTaxPageCardVisualById__(id){
  const defaults = {
    taxTotalRow:{ bg:'gray-1', border:'gray-2', fg:'blue-6', opacity:0.80 },
    taxPayingCard:{ bg:'sky-2', border:'sky-3', fg:'blue-6', opacity:0.80 },
    taxKidsCard:{ bg:'gray-2', border:'gray-3', fg:'blue-6', opacity:0.80 },
    taxReducedCard:{ bg:'beige-2', border:'beige-3', fg:'blue-6', opacity:0.80 }
  };
  return __launcherVisualNormalize__(defaults[String(id || '').trim()] || { bg:'gray-1', border:'gray-2', fg:'blue-6', opacity:0.80 }, 'blue-6');
}

function __taxPageCardVisualFor__(card){
  const id = String((card && card.id) || card || '').trim();
  const map = __loadTaxPageCardVisualMap__();
  const fallback = __defaultTaxPageCardVisualById__(id);
  const current = __launcherVisualNormalize__(map[id] || fallback, fallback.fg || 'blue-6');
  return {
    bg: current.bg || fallback.bg || 'gray-1',
    border: current.border || current.bg || fallback.border || fallback.bg || 'gray-2',
    fg: current.fg || fallback.fg || 'blue-6',
    opacity: __designBgOpacityNormalize__(current.opacity ?? fallback.opacity ?? 0.80)
  };
}

function __saveTaxPageCardVisual__(card, visual){
  try{
    const id = String((card && card.id) || card || '').trim();
    if (!id) return;
    const map = __loadTaxPageCardVisualMap__();
    const fallback = __defaultTaxPageCardVisualById__(id);
    const clean = __launcherVisualNormalize__(visual || {}, fallback.fg || 'blue-6');
    map[id] = {
      bg: clean.bg || fallback.bg || 'gray-1',
      border: clean.border || clean.bg || fallback.border || fallback.bg || 'gray-2',
      fg: clean.fg || fallback.fg || 'blue-6',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.80)
    };
    __saveTaxPageCardVisualMap__(map);
  }catch(_){ }
}

function __applyTaxPageCardVisual__(card){
  try{
    if (!card) return;
    const visual = __taxPageCardVisualFor__(card);
    const bgHex = __operatoreColorHex__(visual.bg || 'gray-1');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'gray-2');
    const fgHex = __tagColorTextHex__(visual.bg || 'gray-1', visual.fg || 'blue-6', false);
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.80);
    const dark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    let valueText = fgHex;
    let subText = fgHex;
    if (dark){
      const resolved = __applyDarkAdaptiveSurface__(card, visual, { fallbackBg:'gray-1', bgOpacity:Math.max(0.16, Math.min(0.24, opacity)), borderOpacity:0.28 }) || {};
      valueText = resolved.textHex || __darkModeReadableTextHex__(fgHex, '#f8fbff');
      subText = resolved.accentHex || '#cbd5e1';
    } else {
      card.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
      card.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
      card.style.setProperty('border', '1px solid ' + hexToRgba(borderHex, 1), 'important');
      card.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
      card.style.setProperty('box-shadow', 'none', 'important');
      card.style.setProperty('backdrop-filter', 'none', 'important');
      card.style.setProperty('-webkit-backdrop-filter', 'none', 'important');
      card.style.setProperty('background-image', 'none', 'important');
    }
    card.querySelectorAll('.tax-total-label,.tax-total-value,.tax-result-title,.tax-result-value').forEach((node) => {
      try{
        node.style.setProperty('color', valueText, 'important');
        node.style.setProperty('-webkit-text-fill-color', valueText, 'important');
      }catch(_){ }
    });
    card.querySelectorAll('.tax-result-sub').forEach((node) => {
      try{
        node.style.setProperty('color', subText, 'important');
        node.style.setProperty('-webkit-text-fill-color', subText, 'important');
      }catch(_){ }
    });
    __applyVisualTextWeight__(card, visual);
  }catch(_){ }
}

function __applyTaxPageCardChangesToCategory__(payload, changed){
  try{
    __TAX_PAGE_CARD_TARGET_IDS__.forEach((id) => {
      try{
        const next = __applyDesignPayloadToVisual__(__taxPageCardVisualFor__(id), payload, changed, __defaultTaxPageCardVisualById__(id).bg || 'gray-1');
        __saveTaxPageCardVisual__(id, next);
        const card = document.getElementById(id);
        if (card) __applyTaxPageCardVisual__(card);
      }catch(_){ }
    });
  }catch(_){ }
}

function __openTaxPageCardColorPicker__(card){
  try{
    if (!card || !card.id) return;
    const current = __taxPageCardVisualFor__(card);
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = {
        bg: colors.bg || current.bg || 'gray-1',
        border: colors.border || current.border || colors.bg || current.bg || 'gray-2',
        fg: colors.fg || current.fg || 'blue-6',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.80),
        bold: false
      };
      __saveTaxPageCardVisual__(card, next);
      __applyTaxPageCardVisual__(card);
    };
    const revertVisual = () => {
      __saveTaxPageCardVisual__(card, current);
      __applyTaxPageCardVisual__(card);
    };
    __tagColorPopupOpen__('tax-page-card', current, (payload) => { applyVisual(payload); }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:true,
      supportsOpacity:true,
      opacity:current.opacity ?? 0.80,
      defaultMode:'bg',
      fallbackBg:(current.bg || 'gray-1'),
      onPreview:applyVisual,
      onRevert:revertVisual,
      applyCategory:{
        message:'Applicare le modifiche a tutte le card tassa soggiorno?',
        apply: async(payload, changed) => { __applyTaxPageCardChangesToCategory__(payload, changed); }
      }
    });
  }catch(_){ }
}

function __bindTaxPageCardColorHold__(card){
  try{ if (!card || card.dataset.taxPageCardColorBound === '1') return; card.dataset.taxPageCardColorBound = '1'; }catch(_){ if (!card) return; }
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } };
  const start = (e)=>{ try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ } fired = false; clear(); timer = setTimeout(()=>{ fired = true; __openTaxPageCardColorPicker__(card); }, 500); };
  const stop = (e)=>{ clear(); if (fired){ block(e); setTimeout(()=>{ fired = false; }, 0); } };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ card.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ card.addEventListener(evt, stop, { passive:false }); }catch(_){ } });
  try{ card.addEventListener('click', (e)=>{ if (fired){ block(e); fired = false; } }, true); }catch(_){ }
  try{ card.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
}

function __applyTaxPageCardAll__(){
  try{
    __TAX_PAGE_CARD_TARGET_IDS__.forEach((id) => {
      const card = document.getElementById(id);
      if (!card) return;
      __applyTaxPageCardVisual__(card);
      __bindTaxPageCardColorHold__(card);
    });
  }catch(_){ }
}

const __GUEST_LIST_CARD_VISUAL_STORAGE_KEY__ = 'dDAE_guest_list_card_visual_v1';

function __loadGuestListCardVisualMap__(){
  try{
    const raw = localStorage.getItem(__GUEST_LIST_CARD_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveGuestListCardVisualMap__(map){
  try{ localStorage.setItem(__GUEST_LIST_CARD_VISUAL_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __guestListCardVisualDefault__(){
  return __launcherVisualNormalize__({ bg:'gray-1', border:'gray-2', fg:'blue-6', opacity:0.22 }, 'blue-6');
}

function __guestListCardVisualKeyFor__(value){
  try{
    const raw = String(value == null ? '' : value).trim();
    if (!raw) return '';
    return raw.replace(/[^\w\-:.]+/g, '_');
  }catch(_){ return ''; }
}

function __guestListCardVisualDataKey__(card){
  try{
    return __guestListCardVisualKeyFor__(card?.dataset?.guestVisualKey || card?.dataset?.guestId || card?.dataset?.guestKey || '');
  }catch(_){ return ''; }
}

function __guestListCardVisualNormalizeEntry__(visual){
  const fallback = __guestListCardVisualDefault__();
  const current = __launcherVisualNormalize__(visual || {}, fallback.fg || 'blue-6');
  return {
    bg: current.bg || fallback.bg || 'gray-1',
    border: current.border || current.bg || fallback.border || fallback.bg || 'gray-2',
    fg: current.fg || fallback.fg || 'blue-6',
    opacity: __designBgOpacityNormalize__(current.opacity ?? fallback.opacity ?? 0.22),
    bold: !!current.bold
  };
}

function __guestListCardVisualRead__(cardOrKey = null){
  const map = __loadGuestListCardVisualMap__();
  const fallback = __guestListCardVisualDefault__();
  const key = (cardOrKey && typeof cardOrKey === 'object') ? __guestListCardVisualDataKey__(cardOrKey) : __guestListCardVisualKeyFor__(cardOrKey);
  const base = __guestListCardVisualNormalizeEntry__(map.guestListCard || fallback);
  if (key && map[key] && typeof map[key] === 'object') return __guestListCardVisualNormalizeEntry__(map[key]);
  return base;
}

function __guestListCardVisualWrite__(cardOrKey, visual){
  try{
    const map = __loadGuestListCardVisualMap__();
    const key = (cardOrKey && typeof cardOrKey === 'object') ? __guestListCardVisualDataKey__(cardOrKey) : __guestListCardVisualKeyFor__(cardOrKey);
    const clean = __guestListCardVisualNormalizeEntry__(visual);
    if (key) map[key] = clean;
    else map.guestListCard = clean;
    __saveGuestListCardVisualMap__(map);
  }catch(_){ }
}

function __guestListCardApplyVisualToAllKeys__(visual){
  try{
    const map = __loadGuestListCardVisualMap__();
    const clean = __guestListCardVisualNormalizeEntry__(visual);
    map.guestListCard = clean;
    document.querySelectorAll('#page-ospiti .guest-card').forEach((card) => {
      const key = __guestListCardVisualDataKey__(card);
      if (key) map[key] = clean;
    });
    __saveGuestListCardVisualMap__(map);
  }catch(_){ }
}

function __applyGuestListCardVisual__(card){
  try{
    if (!card) return;
    const visual = __guestListCardVisualRead__(card);
    const bgHex = __operatoreColorHex__(visual.bg || 'gray-1');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'gray-2');
    const fgHex = __tagColorTextHex__(visual.bg || 'gray-1', visual.fg || 'blue-6', false) || __operatoreColorHex__(visual.fg || 'blue-6');
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.22);
    const dark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    let mainText = fgHex;
    let secondaryText = fgHex;
    if (dark){
      const resolved = __applyDarkAdaptiveSurface__(card, visual, { fallbackBg:'gray-1', bgOpacity:Math.max(0.14, Math.min(0.24, opacity)), borderOpacity:0.28 }) || {};
      mainText = resolved.textHex || __darkModeReadableTextHex__(fgHex, '#f8fbff');
      secondaryText = __darkModeReadableTextHex__(resolved.accentHex || mainText, '#dbe7f4');
    } else {
      card.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
      card.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
      card.style.setProperty('border', '1px solid ' + hexToRgba(borderHex, 1), 'important');
      card.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
      card.style.setProperty('box-shadow', 'none', 'important');
      card.style.setProperty('backdrop-filter', 'none', 'important');
      card.style.setProperty('-webkit-backdrop-filter', 'none', 'important');
      card.style.setProperty('background-image', 'none', 'important');
    }
    card.style.setProperty('--guest-list-card-text', mainText, 'important');
    card.style.setProperty('--guest-list-card-subtext', secondaryText, 'important');
    card.querySelectorAll('.guest-name-text').forEach((node) => {
      try{
        node.style.setProperty('color', mainText, 'important');
        node.style.setProperty('-webkit-text-fill-color', mainText, 'important');
      }catch(_){ }
    });
    card.querySelectorAll('.guest-arrivo,.guest-contact').forEach((node) => {
      try{
        node.style.setProperty('color', secondaryText, 'important');
        node.style.setProperty('-webkit-text-fill-color', secondaryText, 'important');
      }catch(_){ }
    });
    __applyVisualTextWeight__(card, visual);
  }catch(_){ }
}

function __applyGuestListCardAll__(){
  try{
    document.querySelectorAll('#page-ospiti .guest-card').forEach((card) => {
      try{ __applyGuestListCardVisual__(card); }catch(_){ }
    });
  }catch(_){ }
}

function __openGuestListCardColorPicker__(card){
  try{
    if (!card) return;
    const current = __guestListCardVisualRead__(card);
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = {
        bg: colors.bg || current.bg || 'gray-1',
        border: colors.border || current.border || colors.bg || current.bg || 'gray-2',
        fg: colors.fg || current.fg || 'blue-6',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.22),
        bold: false
      };
      __guestListCardVisualWrite__(card, next);
      __applyGuestListCardAll__();
    };
    const revertVisual = () => {
      __guestListCardVisualWrite__(card, current);
      __applyGuestListCardAll__();
    };
    __tagColorPopupOpen__('guest-list-card', current, (payload) => { applyVisual(payload); }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:true,
      supportsOpacity:true,
      opacity:current.opacity ?? 0.22,
      defaultMode:'bg',
      fallbackBg:(current.bg || 'gray-1'),
      onPreview:applyVisual,
      onRevert:revertVisual,
      applyCategory:{
        message:'Applicare le modifiche a tutte le schede guest list?',
        apply: async(payload, changed) => {
          const next = __applyDesignPayloadToVisual__(__guestListCardVisualRead__(card), payload, changed, 'gray-1');
          __guestListCardApplyVisualToAllKeys__(next);
          __applyGuestListCardAll__();
        }
      }
    });
  }catch(_){ }
}

function __bindGuestListCardColorHold__(card){
  try{ if (!card || card.dataset.guestListCardColorBound === '1') return; card.dataset.guestListCardColorBound = '1'; }catch(_){ if (!card) return; }
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const clearSelection = ()=>{ try{ const sel = window.getSelection && window.getSelection(); if (sel && sel.removeAllRanges) sel.removeAllRanges(); }catch(_){ } };
  const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } try{ e.stopImmediatePropagation(); }catch(_){ } clearSelection(); };
  const suppressTap = ()=>{ try{ card.__guestListCardSuppressTapUntil = Date.now() + 900; }catch(_){ } };
  const start = (e)=>{ try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ } fired = false; clear(); clearSelection(); timer = setTimeout(()=>{ fired = true; suppressTap(); __openGuestListCardColorPicker__(card); }, 500); };
  const stop = (e)=>{ clear(); if (fired){ block(e); setTimeout(()=>{ fired = false; }, 0); } };
  const swallow = (e)=>{ try{ if ((card.__guestListCardSuppressTapUntil || 0) > Date.now()) block(e); }catch(_){ } };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ card.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ card.addEventListener(evt, stop, { passive:false }); }catch(_){ } });
  try{ card.addEventListener('click', swallow, true); }catch(_){ }
  try{ card.addEventListener('keydown', swallow, true); }catch(_){ }
  try{ card.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
}

const __SINGLE_ACTION_BUTTON_TARGET_IDS__ = [
  'confirmYesNoYes','confirmYesNoNo',
  'rc_cancel','rc_save',
  'settingsConfigCancel','settingsConfigSave',
  'settingsBackupCancel','settingsBackupImport','settingsBackupExport',
  'channelEditorDelete','channelEditorCancel','channelEditorGraphColor','channelEditorDotColor','channelEditorSave',
  'operatoriEditorDelete','operatoriEditorCancel','operatoriEditorSaldoBtn','operatoriEditorTagColor','operatoriEditorDotColor','operatoriEditorSave',
  'laundryCatalogEditorDelete','laundryCatalogEditorCancel','laundryCatalogEditorTagColor','laundryCatalogEditorDotColor','laundryCatalogEditorSave',
  'guestPhoneActionCall','guestPhoneActionWhatsApp','guestPhoneActionSms','guestGenderMale','guestGenderFemale',
  'spesaCatBtnContanti','spesaCatBtnTassa','spesaCatBtnIva22','spesaCatBtnIva10','spesaCatBtnIva4',
  'speseFilterCatBtnContanti','speseFilterCatBtnTassa','speseFilterCatBtnIva22','speseFilterCatBtnIva10','speseFilterCatBtnIva4','speseFilterCatBtnFuoriBudget',
  'licenseDateRangeTrigger','licenseGeneratorCancel','licenseGeneratorConfirm','licenseDateRangePrev','licenseDateRangeNext','licenseDateRangeCancel','licenseDateRangeApply','licenseRequestEmailBtn','licenseRequestDoneBtn','licenseUnlockCancel','licenseUnlockConfirm','settingsLicenseUnlockBtn','settingsLicensePayBtn','settingsLicenseRequestBtn','settingsLicenseGeneratorBtn','settingsLicenseCloseBtn',
  'themeTransferImport','themeTransferExport','themeTransferCancel',
  'calTodayOccupancyBadge','calTomorrowCheckoutBadge','createGuestBookingBtn','createGuestEstimateBtn'
];

function __loadSingleActionButtonVisualMap__(){
  try{
    const raw = localStorage.getItem(__SINGLE_ACTION_BUTTON_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveSingleActionButtonVisualMap__(map){
  try{ localStorage.setItem(__SINGLE_ACTION_BUTTON_VISUAL_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __singleActionButtonSharedKey__(btnOrId){
  try{
    if (btnOrId && typeof btnOrId === 'object') return String(btnOrId.dataset?.singleActionKey || btnOrId.id || '').trim();
    return String(btnOrId || '').trim();
  }catch(_){ return String(btnOrId || '').trim(); }
}

function __singleActionButtonsForSharedKey__(btnOrId){
  try{
    const key = __singleActionButtonSharedKey__(btnOrId);
    if (!key) return [];
    return Array.from(document.querySelectorAll(`[data-single-action-key="${key}"], #${key}`)).filter(Boolean);
  }catch(_){ return []; }
}

function __refreshSingleActionButtonSharedKey__(btnOrId){
  try{
    __singleActionButtonsForSharedKey__(btnOrId).forEach((node) => { try{ __applySingleActionButtonVisual__(node); }catch(_){ } });
  }catch(_){ }
}

function __defaultSingleActionButtonVisual__(btn){
  const id = __singleActionButtonSharedKey__(btn);
  const defaults = {
    operatoriEditorDelete:{ bg:'red-4', border:'red-4', fg:'white', opacity:0.80 },
    channelEditorDelete:{ bg:'red-4', border:'red-4', fg:'white', opacity:0.80 },
    laundryCatalogEditorDelete:{ bg:'red-4', border:'red-4', fg:'white', opacity:0.80 },
    operatoriEditorCancel:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    channelEditorCancel:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    laundryCatalogEditorCancel:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    operatoriEditorTagColor:{ bg:'violet-5', border:'violet-5', fg:'white', opacity:0.80 },
    operatoriEditorDotColor:{ bg:'blue-4', border:'blue-4', fg:'white', opacity:0.80 },
    operatoriEditorSaldoBtn:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.72 },
    laundryCatalogEditorTagColor:{ bg:'violet-5', border:'violet-5', fg:'white', opacity:0.80 },
    channelEditorDotColor:{ bg:'orange-4', border:'orange-4', fg:'white', opacity:0.80 },
    laundryCatalogEditorDotColor:{ bg:'blue-4', border:'blue-4', fg:'white', opacity:0.80 },
    operatoriEditorSave:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    channelEditorSave:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    laundryCatalogEditorSave:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    guestPhoneActionCall:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    guestPhoneActionWhatsApp:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    guestPhoneActionSms:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    guestGenderMale:{ bg:'sky-4', border:'sky-4', fg:'white', opacity:0.80 },
    guestGenderFemale:{ bg:'pink-5', border:'pink-5', fg:'white', opacity:0.80 },
    spesaCatBtnContanti:{ bg:'blue-4', border:'blue-4', fg:'white', opacity:0.80 },
    spesaCatBtnTassa:{ bg:'sand-4', border:'sand-4', fg:'white', opacity:0.80 },
    spesaCatBtnIva22:{ bg:'orange-4', border:'orange-4', fg:'white', opacity:0.80 },
    spesaCatBtnIva10:{ bg:'sky-4', border:'sky-4', fg:'white', opacity:0.80 },
    spesaCatBtnIva4:{ bg:'blue-5', border:'blue-5', fg:'white', opacity:0.80 },
    licenseDateRangeTrigger:{ bg:'azure-4', border:'azure-4', fg:'white', opacity:0.80 },
    licenseGeneratorCancel:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    licenseGeneratorConfirm:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    licenseDateRangePrev:{ bg:'azure-4', border:'azure-4', fg:'white', opacity:0.80 },
    licenseDateRangeNext:{ bg:'azure-4', border:'azure-4', fg:'white', opacity:0.80 },
    licenseDateRangeCancel:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    licenseDateRangeApply:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    licenseRequestEmailBtn:{ bg:'azure-4', border:'azure-4', fg:'white', opacity:0.80 },
    licenseRequestDoneBtn:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    licenseUnlockCancel:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    licenseUnlockConfirm:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    settingsLicenseUnlockBtn:{ bg:'green-4', border:'green-4', fg:'white', opacity:0.80 },
    settingsLicensePayBtn:{ bg:'azure-4', border:'azure-4', fg:'white', opacity:0.80 },
    settingsLicenseRequestBtn:{ bg:'orange-4', border:'orange-4', fg:'white', opacity:0.80 },
    settingsLicenseGeneratorBtn:{ bg:'orange-4', border:'orange-4', fg:'white', opacity:0.80 },
    settingsLicenseCloseBtn:{ bg:'gray-4', border:'gray-4', fg:'white', opacity:0.80 },
    calTodayOccupancyBadge:{ bg:'red-5', border:'red-6', fg:'white', opacity:1 },
    calTomorrowCheckoutBadge:{ bg:'sky-5', border:'sky-6', fg:'white', opacity:1 }
  };
  const fallback = defaults[id] || { bg:'blue-4', border:'blue-4', fg:'white', opacity:0.80 };
  return __launcherVisualNormalize__(fallback, fallback.bg || 'blue-4');
}

function __singleActionButtonSupportsDualState__(btn){
  try{ return !!(btn && btn.classList && (btn.classList.contains('spesa-category-btn') || btn.classList.contains('operatori-saldo-toggle') || btn.classList.contains('guest-gender-tab'))); }catch(_){ return false; }
}

function __defaultSingleActionButtonStateVisuals__(btn){
  const base = __defaultSingleActionButtonVisual__(btn);
  try{ if (btn && btn.id === 'operatoriEditorSaldoBtn') return { off:{ ...base, bg:'gray-4', border:'gray-4', fg:'white', opacity:0.72 }, on:{ ...base, bg:'green-5', border:'green-5', fg:'white', opacity:0.90 } }; }catch(_){}
  if (!__singleActionButtonSupportsDualState__(btn)) return { off:{ ...base }, on:{ ...base } };
  const off = { ...base, opacity:0.52 };
  const on = { ...base, opacity:0.88 };
  return { off, on };
}

function __singleActionButtonVisualStateMapFor__(btn){
  const id = __singleActionButtonSharedKey__(btn);
  const map = __loadSingleActionButtonVisualMap__();
  const raw = map[id];
  const defaults = __defaultSingleActionButtonStateVisuals__(btn);
  if (!__singleActionButtonSupportsDualState__(btn)){
    const single = __launcherVisualNormalize__(raw || defaults.off, defaults.off.bg || 'blue-4');
    return { off: single, on: single };
  }
  if (raw && typeof raw === 'object' && (raw.off || raw.on)){
    const off = __launcherVisualNormalize__(raw.off || defaults.off, defaults.off.bg || 'blue-4');
    const on = __launcherVisualNormalize__(raw.on || raw.off || defaults.on, defaults.on.bg || defaults.off.bg || 'blue-4');
    return { off, on };
  }
  const legacy = __launcherVisualNormalize__(raw || defaults.off, defaults.off.bg || 'blue-4');
  return { off: { ...defaults.off, ...legacy, opacity: __designBgOpacityNormalize__(legacy.opacity ?? defaults.off.opacity ?? 0.52) }, on: { ...defaults.on, ...legacy, opacity: __designBgOpacityNormalize__(defaults.on.opacity ?? 0.88) } };
}

function __singleActionButtonVisualFor__(btn, forcedStateKey, forcedVisual){
  const states = __singleActionButtonVisualStateMapFor__(btn);
  const fallback = __defaultSingleActionButtonVisual__(btn);
  const safeStateKey = String(forcedStateKey || '').trim().toLowerCase() === 'on'
    ? 'on'
    : ((forcedStateKey === 'off') ? 'off' : (((btn && btn.classList && btn.classList.contains('is-selected') && __singleActionButtonSupportsDualState__(btn))) ? 'on' : 'off'));
  const resolved = forcedVisual
    ? __launcherVisualNormalize__(forcedVisual, (forcedVisual && forcedVisual.bg) || fallback.bg || 'blue-4')
    : (safeStateKey === 'on' ? states.on : states.off);
  return __launcherVisualNormalize__(resolved || fallback, fallback.bg || 'blue-4');
}

function __saveSingleActionButtonVisual__(btn, visual, stateKey){
  try{
    const id = __singleActionButtonSharedKey__(btn);
    if (!id) return;
    const map = __loadSingleActionButtonVisualMap__();
    const fallback = __defaultSingleActionButtonVisual__(btn);
    const clean = __launcherVisualNormalize__(visual || {}, fallback.bg || 'blue-4');
    if (__singleActionButtonSupportsDualState__(btn)){
      const states = __singleActionButtonVisualStateMapFor__(btn);
      const safeStateKey = String(stateKey || ((btn && btn.classList && btn.classList.contains('is-selected')) ? 'on' : 'off')).trim().toLowerCase() === 'on' ? 'on' : 'off';
      states[safeStateKey] = {
        bg: clean.bg || fallback.bg || 'blue-4',
        border: clean.border || clean.bg || fallback.border || fallback.bg || 'blue-4',
        fg: clean.fg || fallback.fg || 'white',
        opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? (safeStateKey === 'on' ? 0.88 : 0.52)),
        bold: !!clean.bold
      };
      map[id] = { off: states.off, on: states.on };
    } else {
      map[id] = {
        bg: clean.bg || fallback.bg || 'blue-4',
        border: clean.border || clean.bg || fallback.border || fallback.bg || 'blue-4',
        fg: clean.fg || fallback.fg || 'white',
        opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.80),
        bold: !!clean.bold
      };
    }
    __saveSingleActionButtonVisualMap__(map);
  }catch(_){ }
}


function __singleActionButtonCategoryForId__(id){
  const key = __singleActionButtonSharedKey__(id);
  if (!key) return '';
  const map = {
    operatoriEditorDelete:'delete',
    channelEditorDelete:'delete',
    laundryCatalogEditorDelete:'delete',
    operatoriEditorCancel:'cancel',
    channelEditorCancel:'cancel',
    laundryCatalogEditorCancel:'cancel',
    operatoriEditorTagColor:'tag',
    operatoriEditorDotColor:'tag',
    operatoriEditorSaldoBtn:'operator-balance',
    channelEditorDotColor:'tag',
    laundryCatalogEditorTagColor:'tag',
    laundryCatalogEditorDotColor:'tag',
    operatoriEditorSave:'save',
    channelEditorSave:'save',
    laundryCatalogEditorSave:'save',
    licenseGeneratorCancel:'cancel',
    licenseDateRangeCancel:'cancel',
    licenseGeneratorConfirm:'save',
    licenseDateRangeApply:'save',
    licenseDateRangeTrigger:'trigger',
    licenseDateRangePrev:'nav',
    licenseDateRangeNext:'nav',
    licenseRequestEmailBtn:'trigger',
    licenseRequestDoneBtn:'cancel',
    licenseUnlockCancel:'cancel',
    licenseUnlockConfirm:'save',
    settingsLicenseCloseBtn:'cancel',
    settingsLicenseUnlockBtn:'save',
    settingsLicensePayBtn:'trigger',
    settingsLicenseRequestBtn:'trigger',
    settingsLicenseGeneratorBtn:'trigger',
    guestGenderMale:'guest-gender',
    guestGenderFemale:'guest-gender',
    themeTransferImport:'themeimport',
    themeTransferExport:'themeexport',
    themeTransferCancel:'themecancel',
    calTodayOccupancyBadge:'calendar-today-occupancy',
    calTomorrowCheckoutBadge:'calendar-tomorrow-checkout'
  };
  return map[key] || '';
}

function __singleActionButtonIdsForCategory__(category){
  const key = String(category || '').trim().toLowerCase();
  if (!key) return [];
  return __SINGLE_ACTION_BUTTON_TARGET_IDS__.filter((id) => __singleActionButtonCategoryForId__(id) === key);
}

function __applySingleActionButtonVisualToCategory__(btn, payload, changed, forcedStateKey){
  try{
    const category = __singleActionButtonCategoryForId__(btn);
    if (!category) return;
    const ids = __singleActionButtonIdsForCategory__(category);
    if (!ids.length) return;
    const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    const editState = String(forcedStateKey || '').trim().toLowerCase() === 'on'
      ? 'on'
      : (((__singleActionButtonSupportsDualState__(btn) && btn.classList && btn.classList.contains('is-selected'))) ? 'on' : 'off');
    ids.forEach((id) => {
      try{
        const node = document.getElementById(id);
        const current = __singleActionButtonVisualFor__(node || btn || { id }, editState);
        const next = {
          bg: changed?.bg ? (colors.bg || current.bg || 'blue-4') : current.bg,
          border: changed?.border ? (colors.border || current.border || colors.bg || current.bg || 'blue-4') : current.border,
          fg: changed?.fg ? (colors.fg || current.fg || 'white') : current.fg,
          opacity: changed?.opacity ? __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.80) : __designBgOpacityNormalize__(current.opacity ?? 0.80),
          bold: changed?.bold ? !!payload?.bold : !!current.bold
        };
        __saveSingleActionButtonVisual__(node || { id }, next, editState);
        __refreshSingleActionButtonSharedKey__(node || { id });
      }catch(_){ }
    });
  }catch(_){ }
}


function __forceSettingsEditorActionButtonBold__(btn){
  try{
    if (!btn || !btn.id) return;
    const ids = new Set([
      'operatoriEditorDelete','operatoriEditorSaldoBtn','operatoriEditorTagColor','operatoriEditorSave',
      'channelEditorDelete','channelEditorGraphColor','channelEditorSave',
      'laundryCatalogEditorDelete','laundryCatalogEditorTagColor','laundryCatalogEditorSave'
    ]);
    if (!ids.has(btn.id)) return;
    const apply = (node) => {
      try{
        if (!node || !(node instanceof Element)) return;
        if (node.closest && node.closest('svg')) return;
        const tag = String(node.tagName || '').toUpperCase();
        if (['SVG','PATH','RECT','CIRCLE','LINE','POLYLINE','POLYGON','G','DEFS','CLIPPATH','USE','STOP'].includes(tag)) return;
        node.style.setProperty('font-weight', '900', 'important');
        node.style.setProperty('-webkit-font-smoothing', 'antialiased');
      }catch(_){ }
    };
    apply(btn);
    try{ btn.querySelectorAll('*').forEach(apply); }catch(_){ }
  }catch(_){ }
}

function __applySingleActionButtonVisual__(btn, forcedStateKey, forcedVisual){
  try{
    if (!btn || !btn.id) return;
    const selected = String(forcedStateKey || '').trim().toLowerCase() === 'on'
      ? true
      : (String(forcedStateKey || '').trim().toLowerCase() === 'off'
          ? false
          : !!(btn.classList && btn.classList.contains('is-selected') && __singleActionButtonSupportsDualState__(btn)));
    const stateKey = selected ? 'on' : 'off';
    const visual = __singleActionButtonVisualFor__(btn, stateKey, forcedVisual);
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    const fgHex = __tagColorTextHex__(visual.bg || 'blue-4', visual.fg || 'white', false) || __operatoreColorHex__(visual.fg || 'white');
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.80);
    btn.dataset.designStandaloneButton = '1';
    btn.dataset.designState = stateKey;
    if (__isDarkModeEnabled__ && __isDarkModeEnabled__()){
      const resolved = __applyDarkAdaptiveSurface__(btn, visual, { fallbackBg: visual.bg || 'blue-4', bgOpacity: Math.max(0.14, Math.min(0.28, opacity)), borderOpacity: selected ? 0.40 : 0.28 }) || {};
      const darkText = resolved.textHex || __darkModeReadableTextHex__(fgHex, '#f8fbff');
      btn.style.setProperty('color', darkText, 'important');
      btn.style.setProperty('-webkit-text-fill-color', darkText, 'important');
    } else {
      btn.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
      btn.style.setProperty('border', '1px solid ' + hexToRgba(borderHex, 1), 'important');
      btn.style.setProperty('border-color', hexToRgba(borderHex, 1), 'important');
      btn.style.setProperty('color', fgHex, 'important');
      btn.style.setProperty('-webkit-text-fill-color', fgHex, 'important');
      btn.style.setProperty('box-shadow', 'none', 'important');
      btn.style.setProperty('backdrop-filter', 'none', 'important');
      btn.style.setProperty('-webkit-backdrop-filter', 'none', 'important');
    }
    __applyVisualTextWeight__(btn, visual);
    __forceSettingsEditorActionButtonBold__(btn);
    btn.querySelectorAll('svg, .ui-ico, .tl-ico, .tag-color-mode-ico').forEach((node) => {
      try{
        const applied = (btn.style.getPropertyValue('color') || fgHex || '').trim() || fgHex;
        node.style.setProperty('color', applied, 'important');
        node.style.setProperty('stroke', applied, 'important');
        node.style.setProperty('fill', 'none', 'important');
      }catch(_){ }
      try{
        node.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((child) => {
          const applied = (btn.style.getPropertyValue('color') || fgHex || '').trim() || fgHex;
          child.style.setProperty('stroke', applied, 'important');
          if (String(child.getAttribute('fill') || '').toLowerCase() !== 'none') child.style.setProperty('fill', 'none', 'important');
        });
      }catch(_){ }
    });
  }catch(_){ }
}

async function __openSingleActionButtonColorPicker__(btn){
  try{
    if (!btn || !btn.id) return;
    const __insideSettingsEditorAction__ = !!(btn.closest && btn.closest('#operatoriEditorModal,#channelEditorModal,#laundryCatalogEditorModal'));
    if ((__settingsEditorColorPopupIsSuppressed__() || __settingsEditorModalRecentlyOpened__(2400)) && !__insideSettingsEditorAction__) return;
    const label = __guestFilterButtonLocalizedLabel__(btn);
    const editState = __singleActionButtonSupportsDualState__(btn)
      ? await (async()=>{
          const choice = await __confirmTwoActions__(
            __guestFilterButtonEditStatePrompt__(label),
            __guestFilterButtonStateLabel__('active'),
            __guestFilterButtonStateLabel__('distractive')
          );
          if (choice !== 'yes' && choice !== 'no') return '';
          return choice === 'yes' ? 'on' : 'off';
        })()
      : ((__singleActionButtonSupportsDualState__(btn) && btn.classList && btn.classList.contains('is-selected')) ? 'on' : 'off');
    if (!editState) return;
    const current = __singleActionButtonVisualFor__(btn, editState);
    const category = __singleActionButtonCategoryForId__(btn);
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = {
        bg: colors.bg || current.bg || 'blue-4',
        border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
        fg: colors.fg || current.fg || 'white',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.80),
        bold: false
      };
      __saveSingleActionButtonVisual__(btn, next, editState);
      __refreshSingleActionButtonSharedKey__(btn);
    };
    const revertVisual = () => { __saveSingleActionButtonVisual__(btn, current, editState); __refreshSingleActionButtonSharedKey__(btn); };
    const popupOptions = { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:current.opacity ?? 0.80, defaultMode:'bg', fallbackBg:(current.bg || 'blue-4'), onPreview:applyVisual, onRevert:revertVisual };
    if (category){
      popupOptions.applyCategory = {
        message:'Applicare le modifiche a tutti i tasti della stessa categoria?',
        apply: async(payload, changed) => { __applySingleActionButtonVisualToCategory__(btn, payload, changed, editState); }
      };
    }
    __tagColorPopupOpen__('single-action-button', current, (payload) => { applyVisual(payload); }, popupOptions);
  }catch(_){ }
}

function __bindSingleActionButtonColorHold__(btn){
  try{ if (!btn || btn.dataset.singleActionButtonColorBound === '1') return; btn.dataset.singleActionButtonColorBound = '1'; }catch(_){ if (!btn) return; }
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const clearSelection = ()=>{ try{ const sel = window.getSelection && window.getSelection(); if (sel && sel.removeAllRanges) sel.removeAllRanges(); }catch(_){ } };
  const block = (e)=>{
    try{ e.preventDefault(); }catch(_){ }
    try{ e.stopPropagation(); }catch(_){ }
    try{ e.stopImmediatePropagation(); }catch(_){ }
    clearSelection();
  };
  const suppressTap = ()=>{
    try{ btn.__singleActionButtonSuppressTapUntil = Date.now() + 900; }catch(_){ }
  };
  const swallowSuppressedTap = (e)=>{
    try{
      if ((btn.__singleActionButtonSuppressTapUntil || 0) > Date.now()) block(e);
    }catch(_){ }
  };
  const start = (e)=>{
    try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    fired = false;
    clear();
    clearSelection();
    timer = setTimeout(()=>{
      fired = true;
      suppressTap();
      if (__settingsEditorColorPopupIsSuppressed__()) return;
      try{ btn.classList.add('is-pressing'); }catch(_){ }
      __openSingleActionButtonColorPicker__(btn);
    }, 500);
  };
  const stop = (e)=>{
    clear();
    if (fired){
      suppressTap();
      block(e);
      setTimeout(()=>{ fired = false; try{ btn.classList.remove('is-pressing'); }catch(_){ } }, 0);
      return;
    }
    try{ btn.classList.remove('is-pressing'); }catch(_){ }
    clearSelection();
  };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ try{ btn.addEventListener(evt, stop, true); }catch(__){ } } });
  ['pointerup','touchend','click'].forEach((evt)=>{ try{ btn.addEventListener(evt, swallowSuppressedTap, true); }catch(_){ } });
  try{ btn.addEventListener('click', (e)=>{ if (fired){ suppressTap(); block(e); fired = false; } }, true); }catch(_){ }
  try{ btn.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
  try{ btn.addEventListener('selectstart', (e)=>{ block(e); }, true); }catch(_){ }
}

function __setupSingleActionButtonPaletteBindings__(){
  try{
    __SINGLE_ACTION_BUTTON_TARGET_IDS__.forEach((id) => {
      const btn = document.getElementById(id);
      if (!btn) return;
      __applySingleActionButtonVisual__(btn);
      __bindSingleActionButtonColorHold__(btn);
    });
  }catch(_){ }
}


function __spesaCategoryButtons__(){
  return Array.from(document.querySelectorAll('#spesaCategoryButtons .spesa-category-btn[data-value]'));
}

function __syncSpesaCategoryButtons__(selectedValue){
  const safe = String(selectedValue || '').trim();
  __spesaCategoryButtons__().forEach((btn) => {
    const on = String(btn.dataset.value || '') === safe;
    btn.classList.toggle('is-selected', on);
    btn.setAttribute('aria-pressed', on ? 'true' : 'false');
    try{ __applySingleActionButtonVisual__(btn); }catch(_){ }
  });
}

function __setSpesaCategoriaValue__(value){
  try{
    const select = document.getElementById('spesaCategoria');
    if (select) select.value = String(value || '');
  }catch(_){ }
  __syncSpesaCategoryButtons__(value);
}

function __setupSpesaCategoryButtons__(){
  try{
    const select = document.getElementById('spesaCategoria');
    const buttons = __spesaCategoryButtons__();
    if (!select || !buttons.length) return;
    try{ const field = document.getElementById('spesaCategoryField'); if (field) field.classList.add('spesa-category-field-ready'); }catch(_){ }
    buttons.forEach((btn) => {
      if (btn.dataset.spesaCategoryBound === '1') return;
      btn.dataset.spesaCategoryBound = '1';
      bindFastTap(btn, (ev) => {
        try{
          if ((btn.__singleActionButtonSuppressTapUntil || 0) > Date.now()) return;
        }catch(_){ }
        try{ ev && ev.preventDefault && ev.preventDefault(); }catch(_){ }
        __setSpesaCategoriaValue__(btn.dataset.value || '');
      });
    });
    try{
      if (!select.dataset.spesaCategorySyncBound){
        select.dataset.spesaCategorySyncBound = '1';
        select.addEventListener('change', () => { __syncSpesaCategoryButtons__(select.value || ''); });
      }
    }catch(_){ }
    __syncSpesaCategoryButtons__(select.value || '');
    try{ __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
  }catch(_){ }
}

function __speseCategoryFilterButtons__(){
  return Array.from(document.querySelectorAll('#speseCategoryFilterButtons .spesa-category-btn[data-value]'));
}

function __syncSpeseCategoryFilterButtons__(selectedValue){
  const safe = String(selectedValue || '').trim();
  __speseCategoryFilterButtons__().forEach((btn) => {
    const on = String(btn.dataset.value || '') === safe;
    btn.classList.toggle('is-selected', on);
    btn.classList.toggle('is-muted', !on);
    btn.setAttribute('aria-pressed', on ? 'true' : 'false');
    btn.dataset.filterSelected = on ? '1' : '0';
    try{ __applySingleActionButtonVisual__(btn); }catch(_){ }
  });
}

function __setSpeseCategoryFilter__(value){
  try{ state.speseCategoryFilter = String(value || '').trim(); }catch(_){ }
  __syncSpeseCategoryFilterButtons__(value);
  try{ if (state.page === 'spese' && state.speseView === 'list') renderSpese(); }catch(_){ }
}

function __setupSpeseCategoryFilterButtons__(){
  try{
    const buttons = __speseCategoryFilterButtons__();
    if (!buttons.length) return;
    buttons.forEach((btn) => {
      if (btn.dataset.speseFilterBound === '1') return;
      btn.dataset.speseFilterBound = '1';
      bindFastTap(btn, (ev) => {
        try{ if ((btn.__singleActionButtonSuppressTapUntil || 0) > Date.now()) return; }catch(_){ }
        try{ ev && ev.preventDefault && ev.preventDefault(); }catch(_){ }
        const value = String(btn.dataset.value || '').trim();
        const current = String(state.speseCategoryFilter || '').trim();
        __setSpeseCategoryFilter__(current === value ? '' : value);
      });
    });
    __syncSpeseCategoryFilterButtons__(state.speseCategoryFilter || '');
    try{ __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
  }catch(_){ }
}

function __loadSpesaCardOpacityMap__(){
  try{
    const raw = localStorage.getItem(__SPESA_CARD_OPACITY_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return parsed && typeof parsed === 'object' ? parsed : {};
  }catch(_){ return {}; }
}

function __saveSpesaCardOpacityMap__(map){
  try{ localStorage.setItem(__SPESA_CARD_OPACITY_STORAGE_KEY__, JSON.stringify(map || {})); }catch(_){ }
}

function __getSpesaCardOpacity__(key, fallback){
  try{
    const map = __loadSpesaCardOpacityMap__();
    const safeKey = String(key || '').trim();
    const value = safeKey ? map[safeKey] : null;
    return __designBgOpacityNormalize__(value ?? fallback ?? 0.22);
  }catch(_){ return __designBgOpacityNormalize__(fallback ?? 0.22); }
}

function __setSpesaCardOpacity__(key, opacity){
  try{
    const safeKey = String(key || '').trim();
    if (!safeKey) return;
    const map = __loadSpesaCardOpacityMap__();
    map[safeKey] = __designBgOpacityNormalize__(opacity ?? 0.22);
    __saveSpesaCardOpacityMap__(map);
  }catch(_){ }
}

function __spesaCardVisualForKey__(key, fallbackColor){
  const safeKey = String(key || '').trim();
  const graphSpec = __closestGraphColorSpec__(__getSpeseGraphSliceColor__(safeKey, fallbackColor || '#2B7CB4'));
  try{
    const map = __loadSpesaCardVisualMap__();
    const saved = __tagColorPairFromValue__(map[safeKey] || {}, graphSpec);
    return {
      bg: __normalizeOperatoreColor__(saved.bg || graphSpec),
      border: __normalizeOperatoreColor__(saved.border || saved.bg || graphSpec),
      fg: __normalizeOptionalOperatoreColor__(saved.fg || ''),
      opacity: __designBgOpacityNormalize__(saved.opacity ?? __getSpesaCardOpacity__(safeKey, 0.22))
    };
  }catch(_){
    return { bg: graphSpec, border: graphSpec, fg: '', opacity: __getSpesaCardOpacity__(safeKey, 0.22) };
  }
}

function __applySpesaCardChangesToCategory__(categoryKey, payload, changed, fallbackSpec, matchFn){
  try{
    const safeKey = String(categoryKey || '').trim();
    if (!safeKey) return;
    const matcher = (typeof matchFn === 'function') ? matchFn : ((row) => String(__spesaVisualKeyForRow__(row) || '').trim() === safeKey);
    const currentVisual = __spesaCardVisualForKey__(safeKey, fallbackSpec || '#2B7CB4');
    const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    const nextBg = __parseOperatoreColorSpec__(colors.bg || changed?.next?.bg || currentVisual.bg || fallbackSpec || '#2B7CB4').spec;
    const nextVisual = {
      bg: changed?.bg ? nextBg : (currentVisual.bg || nextBg),
      border: changed?.border ? __normalizeOperatoreColor__(colors.border || changed?.next?.border || currentVisual.border || nextBg) : (currentVisual.border || currentVisual.bg || nextBg),
      fg: changed?.fg ? __normalizeOptionalOperatoreColor__(colors.fg || changed?.next?.fg || '') : __normalizeOptionalOperatoreColor__(currentVisual.fg || ''),
      opacity: changed?.opacity ? __designBgOpacityNormalize__(payload?.opacity ?? changed?.next?.opacity ?? currentVisual.opacity ?? 0.22) : __designBgOpacityNormalize__(currentVisual.opacity ?? 0.22)
    };
    __setSpesaCardVisual__(safeKey, nextVisual, fallbackSpec || currentVisual.bg || nextVisual.bg);
    document.querySelectorAll('#speseList .item.spesa-bg, #statSpeseList .item.spesa-bg').forEach((node) => {
      try{
        const row = node.__spesaRow || null;
        if (!row || !matcher(row)) return;
        __applySpesaCardColor__(node, row);
      }catch(_){ }
    });
    __refreshSpeseColorLinkedViews__({ skipSpeseList:false });
  }catch(_){ }
}

function __bindSpesaCardEditLongPress__(el, row){
  if (!el || el.dataset.spesaEditBound === '1') return;
  el.dataset.spesaEditBound = '1';
  const DOUBLE_TAP_MS = 360;
  let lastTapAt = 0;
  const gesture = el.__spesaGestureState || (el.__spesaGestureState = {});
  const block = (e)=>{
    try{ if (e && e.preventDefault) e.preventDefault(); }catch(_){ }
    try{ if (e && e.stopPropagation) e.stopPropagation(); }catch(_){ }
    try{ if (e && e.stopImmediatePropagation) e.stopImmediatePropagation(); }catch(_){ }
    return false;
  };
  const ignoreTarget = (e) => {
    try{
      const t = e && e.target;
      return !!(t && t.closest && t.closest('[data-del]'));
    }catch(_){ return false; }
  };
  const triggerEdit = (e) => {
    gesture.suppressTapUntil = Date.now() + 650;
    try{ el.classList.add('is-pressing'); }catch(_){ }
    try{ __enterSpesaEditMode__(row); }catch(_){ }
    setTimeout(()=>{ try{ el.classList.remove('is-pressing'); }catch(_){ } }, 0);
    return block(e);
  };
  const registerTap = (e, source) => {
    if (ignoreTarget(e)) return;
    const now = Date.now();
    if (gesture.longPressFired || now < Number(gesture.suppressTapUntil || 0)) return block(e);
    if (source === 'click'){
      if ((now - Number(gesture.recentPointerTapAt || 0)) <= 700) return block(e);
    } else {
      gesture.recentPointerTapAt = now;
    }
    if ((now - lastTapAt) <= DOUBLE_TAP_MS){
      lastTapAt = 0;
      return triggerEdit(e);
    }
    lastTapAt = now;
  };
  try{ el.addEventListener('pointerup', (e)=>{ registerTap(e, 'pointerup'); }, true); }catch(_){ }
  try{ el.addEventListener('touchend', (e)=>{ registerTap(e, 'touchend'); }, true); }catch(_){ }
  try{ el.addEventListener('mouseup', (e)=>{ registerTap(e, 'mouseup'); }, true); }catch(_){ }
  try{ el.addEventListener('click', (e)=>{ registerTap(e, 'click'); }, true); }catch(_){ }
  try{ el.addEventListener('dblclick', (e)=>{ if (ignoreTarget(e)) return; if (gesture.longPressFired || Date.now() < Number(gesture.suppressTapUntil || 0)) return block(e); lastTapAt = 0; triggerEdit(e); }, true); }catch(_){ }
  try{ el.addEventListener('contextmenu', (e)=>{ if (gesture.longPressFired || Date.now() < Number(gesture.suppressTapUntil || 0)) block(e); }, true); }catch(_){ }
}

function __openSpesaCardColorPicker__(el, row){
  if (!el) return;
  try{
    const visualKey = __spesaVisualKeyForRow__(row);
    const graphKey = spesaGraphKeyForItem(row);
    if (!visualKey) return;
    const fallbackSpec = __spesaVisualFallbackSpecForRow__(row);
    const currentVisual = __spesaCardVisualForKey__(visualKey, fallbackSpec);
    const currentSpec = __parseOperatoreColorSpec__(currentVisual.bg || __closestGraphColorSpec__(fallbackSpec)).spec;
    const sameCategoryMatcher = (candidate) => {
      const sourceCat = (row?.categoria ?? row?.cat ?? '').toString().trim().toLowerCase();
      const candidateCat = (candidate?.categoria ?? candidate?.cat ?? '').toString().trim().toLowerCase();
      return !!sourceCat && sourceCat === candidateCat;
    };
    const applyVisual = (payload) => {
      const changed = __tagColorPopupChangedFields__(payload);
      __applySpesaCardChangesToCategory__(visualKey, payload, changed, currentSpec, sameCategoryMatcher);
      try{ __applySpesaCardColor__(el, row); }catch(_){ }
    };
    const revertVisual = () => {
      __setSpesaCardVisual__(visualKey, currentVisual, currentSpec);
      try{ __applySpesaCardColor__(el, row); }catch(_){ }
      __refreshSpeseColorLinkedViews__({ skipSpeseList:false });
    };
    __tagColorPopupOpen__('spese-card', currentVisual, (payload) => {
      try{
        const changed = __tagColorPopupChangedFields__(payload);
        const currentRowVisual = __spesaCardVisualForKey__(__spesaVisualKeyForRow__(row), currentSpec);
        const rowNext = {
          bg: changed?.bg ? __normalizeOperatoreColor__(payload?.colors?.bg || changed?.next?.bg || currentRowVisual.bg || currentSpec) : (currentRowVisual.bg || currentSpec),
          border: changed?.border ? __normalizeOperatoreColor__(payload?.colors?.border || changed?.next?.border || currentRowVisual.border || currentRowVisual.bg || currentSpec) : (currentRowVisual.border || currentRowVisual.bg || currentSpec),
          fg: changed?.fg ? __normalizeOptionalOperatoreColor__(payload?.colors?.fg || changed?.next?.fg || '') : __normalizeOptionalOperatoreColor__(currentRowVisual.fg || ''),
          opacity: changed?.opacity ? __designBgOpacityNormalize__(payload?.opacity ?? changed?.next?.opacity ?? currentRowVisual.opacity ?? 0.22) : __designBgOpacityNormalize__(currentRowVisual.opacity ?? 0.22)
        };
        __setSpesaCardVisual__(visualKey, rowNext, currentSpec);
        __applySpesaCardColor__(el, row);
      }catch(_){ }
    }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:currentVisual.opacity ?? 0.22, defaultMode:'bg', fallbackBg:currentSpec, onPreview:applyVisual, onRevert:revertVisual, applyCategory:{ message:'Applicare sfondo, bordo, testo e trasparenza a tutte le card spese della stessa categoria?', apply: async(payload, changed) => {
      await __applySpesaCardChangesToCategory__(visualKey, payload, changed, currentSpec, sameCategoryMatcher);
    } } });
  }catch(_){ }
}

function __bindSpesaCardColorLongPress__(el, row){
  if (!el || el.dataset.spesaColorBound === '1') return;
  el.dataset.spesaColorBound = '1';
  const gesture = el.__spesaGestureState || (el.__spesaGestureState = {});
  let timer = null;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const block = (e)=>{
    try{ if (e && e.preventDefault) e.preventDefault(); }catch(_){ }
    try{ if (e && e.stopPropagation) e.stopPropagation(); }catch(_){ }
    try{ if (e && e.stopImmediatePropagation) e.stopImmediatePropagation(); }catch(_){ }
    return false;
  };
  const ignoreTarget = (e) => {
    try{
      const t = e && e.target;
      return !!(t && t.closest && t.closest('[data-del]'));
    }catch(_){ return false; }
  };
  const start = (e)=>{
    if (ignoreTarget(e)) return;
    try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    gesture.longPressFired = false;
    clear();
    timer = setTimeout(()=>{
      gesture.longPressFired = true;
      gesture.suppressTapUntil = Date.now() + 900;
      try{ el.classList.add('is-pressing'); }catch(_){ }
      __openSpesaCardColorPicker__(el, row);
      setTimeout(()=>{ try{ el.classList.remove('is-pressing'); }catch(_){}; gesture.longPressFired = false; }, 0);
    }, 500);
  };
  const stop = (e)=>{
    clear();
    if (gesture.longPressFired){
      block(e);
      setTimeout(()=>{ gesture.longPressFired = false; try{ el.classList.remove('is-pressing'); }catch(_){ } }, 0);
      return;
    }
    try{ el.classList.remove('is-pressing'); }catch(_){ }
  };
  const swallow = (e)=>{ if (gesture.longPressFired || Date.now() < Number(gesture.suppressTapUntil || 0)) block(e); };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ el.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ el.addEventListener(evt, stop, { passive:false }); }catch(_){ } });
  try{ el.addEventListener('click', swallow, true); }catch(_){ }
  try{ el.addEventListener('contextmenu', (e)=>{ if (ignoreTarget(e)) return; block(e); }, true); }catch(_){ }
}

function __openGraphColorPicker__(graphKey, label, currentColor, onDone){
  const selectedSpec = __closestGraphColorSpec__(currentColor);
  const safeGraphKey = String(graphKey || '').trim();
  const safeLabel = String(label || '').trim();
  const savedMap = __loadGraphColorMap__(safeGraphKey);
  const initialSpec = __parseOperatoreColorSpec__(savedMap[safeLabel] || selectedSpec).spec;
  const initialVisual = { bg: initialSpec, border: initialSpec, fg: '', opacity: 0.80 };
  const syncMonthlyStatCard = (spec) => {
    if (safeGraphKey !== 'occupazione-mensile' || !safeLabel) return;
    try{
      const statMap = __loadStatCardColorMap__('statmensili');
      const fallbackPair = __getStatCardColorPair__('statmensili', safeLabel, spec);
      statMap[safeLabel] = { bg: fallbackPair.bg || 'blue-4', border: fallbackPair.border || fallbackPair.bg || 'blue-4', fg: spec, opacity: __designBgOpacityNormalize__(fallbackPair.opacity ?? 0.80) };
      __saveStatCardColorMap__('statmensili', statMap);
    }catch(_){ }
  };
  const refreshLiveViews = () => {
    try{ __refreshSpeseColorLinkedViews__({ skipSpeseList:false }); }catch(_){ }
    try{ if (safeGraphKey === 'occupazione-mensile') __refreshStatCardsPage__('statmensili'); }catch(_){ }
    try{
      const modal = document.getElementById('statGraphModal');
      if (modal && !modal.hidden) __openStatGraphPopup__({ graphKey: safeGraphKey, title: modal.querySelector('#statGraphModalTitle')?.textContent || '', detail: modal.querySelector('#statGraphModalDetail')?.textContent || '', slices: Array.isArray(state && state.statGraphModalSlices) ? state.statGraphModalSlices : [], valueFormatter: state && state.statGraphModalValueFormatter, legendMode: state && state.statGraphModalLegendMode, centerTitle: state && state.statGraphModalCenterTitle, centerFormatter: state && state.statGraphModalCenterFormatter });
    }catch(_){ }
  };
  const applySpec = (payload) => {
    const colorSource = payload?.colors?.bg || payload?.colors?.border || payload?.colors?.fg || payload?.spec || selectedSpec;
    const normalized = __parseOperatoreColorSpec__(colorSource).spec;
    const map = __loadGraphColorMap__(safeGraphKey);
    if (safeLabel) map[safeLabel] = normalized;
    __saveGraphColorMap__(safeGraphKey, map);
    if (safeGraphKey === 'spese' && safeLabel){
      try{
        const keyMap = __speseGraphSlicesBase__();
        const found = (Array.isArray(keyMap) ? keyMap : []).find((slice) => String(slice?.label || '') === safeLabel);
        const safeExpenseKey = String(found?.key || '').trim();
        if (safeExpenseKey){
          const currentVisual = __spesaCardVisualForKey__(safeExpenseKey, normalized);
          __setSpesaCardVisual__(safeExpenseKey, { bg: normalized, border: payload?.colors?.border || currentVisual.border || normalized, fg: payload?.colors?.fg || currentVisual.fg || '', opacity: __designBgOpacityNormalize__(payload?.opacity ?? currentVisual.opacity ?? 0.22) }, normalized);
        }
      }catch(_){ }
    }
    syncMonthlyStatCard(normalized);
    refreshLiveViews();
    return normalized;
  };
  const revertSpec = () => {
    const map = __loadGraphColorMap__(safeGraphKey);
    if (safeLabel) map[safeLabel] = initialSpec;
    __saveGraphColorMap__(safeGraphKey, map);
    if (safeGraphKey === 'spese' && safeLabel){
      try{
        const keyMap = __speseGraphSlicesBase__();
        const found = (Array.isArray(keyMap) ? keyMap : []).find((slice) => String(slice?.label || '') === safeLabel);
        const safeExpenseKey = String(found?.key || '').trim();
        if (safeExpenseKey) __setSpesaCardVisual__(safeExpenseKey, { bg: initialSpec, border: initialVisual.border, fg: initialVisual.fg, opacity: 0.22 }, initialSpec);
      }catch(_){ }
    }
    syncMonthlyStatCard(initialSpec);
    refreshLiveViews();
  };
  const applyCategoryCfg = (safeGraphKey === 'spese' && safeLabel) ? {
    message:'Applicare sfondo, bordo, testo e trasparenza a tutte le card spese della stessa categoria?',
    apply: async(payload, changed) => {
      try{
        const keyMap = __speseGraphSlicesBase__();
        const found = (Array.isArray(keyMap) ? keyMap : []).find((slice) => String(slice?.label || '') === safeLabel);
        const safeExpenseKey = String(found?.key || '').trim();
        if (!safeExpenseKey) return;
        await __applySpesaCardChangesToCategory__(safeExpenseKey, payload, changed, selectedSpec);
      }catch(_){ }
    }
  } : null;
  __tagColorPopupOpen__('stat-graph', initialVisual, (payload) => {
    const normalized = applySpec(payload);
    if (typeof onDone === 'function') onDone(__graphColorValueToHex__(normalized, currentColor));
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, defaultMode:'bg', fallbackBg:selectedSpec, onPreview:applySpec, onRevert:revertSpec, applyCategory: applyCategoryCfg });
}

function __bindGraphLegendLongPress__(row, payload, slice){
  if (!row || !payload || !slice) return;
  let timer = null;
  let fired = false;
  const start = (e)=>{
    try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    fired = false;
    clearTimeout(timer);
    timer = setTimeout(()=>{
      fired = true;
      row.classList.add('is-pressing');
      __openGraphColorPicker__(payload.graphKey || payload.title || 'generic', slice.label, slice.color, ()=>{
        row.classList.remove('is-pressing');
        try{ __openStatGraphPopup__(payload); }catch(_){ }
      });
    }, 500);
  };
  const stop = ()=>{ clearTimeout(timer); timer = null; if (!fired) row.classList.remove('is-pressing'); };
  ['pointerdown','touchstart','mousedown'].forEach(evt=>row.addEventListener(evt, start, {passive:true}));
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave'].forEach(evt=>row.addEventListener(evt, stop, {passive:true}));
  row.addEventListener('contextmenu', (e)=>{ e.preventDefault(); });
}

function __renderLegendRows__(containerId, slices, valueFormatter, opts){
  const leg = document.getElementById(containerId);
  if (!leg) return;
  const fmt = (typeof valueFormatter === "function") ? valueFormatter : ((v)=>String(v));
  const options = opts || {};
  const mode = String(options.mode || "share").toLowerCase();
  const total = (Array.isArray(slices) ? slices : []).reduce((a,x)=>a+Math.max(0, Number((x && x.value) || 0)), 0);
  leg.innerHTML = "";
  (Array.isArray(slices) ? slices : []).forEach((sl)=>{
    const v = Math.max(0, Number((sl && sl.value) || 0));
    const pct = total > 0 ? (v / total * 100) : 0;
    const right = (mode === "absolute") ? fmt(v) : `${pct.toFixed(1)}% · ${fmt(v)}`;
    const row = document.createElement("div");
    row.className = "legrow";
    row.innerHTML = `
      <div class="legleft">
        <div class="dot" style="background:${sl.color}"></div>
        <div class="legname">${escapeHtml(sl.label)}</div>
      </div>
      <div class="legright">${right}</div>
    `;
    if (options.enableColorEdit) {
      row.classList.add('is-color-editable');
      row.title = 'Pressione lunga per cambiare colore';
      __bindGraphLegendLongPress__(row, options.payload || null, sl);
    }
    leg.appendChild(row);
  });
}

function __getStatGraphCard__(canvasId){
  const canvas = document.getElementById(canvasId);
  return canvas && canvas.closest ? canvas.closest(".stats-graph-card") : null;
}

function __bindStatGraphPopup__(canvasId, payload){
  const card = __getStatGraphCard__(canvasId);
  const canvas = document.getElementById(canvasId);
  const target = card || canvas;
  if (!target || !payload) return;
  try{
    target.classList.add("is-tappable");
    target.setAttribute("role", "button");
    target.setAttribute("tabindex", "0");
    target.setAttribute("aria-label", payload.title || "Grafico");
  }catch(_){ }
  const open = ()=>{ try{ __openStatGraphPopup__(payload); }catch(_){ } };
  target.onclick = open;
  target.onkeydown = (e)=>{
    const k = e && (e.key || e.code);
    if (k === "Enter" || k === " " || k === "Spacebar"){
      try{ e.preventDefault(); }catch(_){ }
      open();
    }
  };
}

function __openStatGraphPopup__(payload){
  const modal = document.getElementById("statGraphModal");
  if (!modal || !payload) return;
  const titleEl = document.getElementById("statGraphModalTitle");
  const detailEl = document.getElementById("statGraphModalDetail");
  const legendEl = document.getElementById("statGraphModalLegend");
  const canvasWrap = document.querySelector("#statGraphModal .stat-graph-modal-canvas");
  const workingPayload = { ...payload };
  try{
    state.statGraphModalSlices = Array.isArray(payload.slices) ? payload.slices.map((slice)=>({ ...slice })) : [];
    state.statGraphModalValueFormatter = payload.valueFormatter;
    state.statGraphModalLegendMode = payload.legendMode || 'share';
    state.statGraphModalCenterTitle = payload.centerTitle != null ? payload.centerTitle : 'Totale';
    state.statGraphModalCenterFormatter = (typeof payload.centerFormatter === 'function') ? payload.centerFormatter : euro;
  }catch(_){ }
  workingPayload.slices = __applyGraphCustomColors__(workingPayload.graphKey || workingPayload.title || 'generic', Array.isArray(payload.slices) ? payload.slices : []);
  if (titleEl) titleEl.textContent = String(workingPayload.title || "Grafico");
  if (detailEl) detailEl.textContent = String(workingPayload.detail || "");
  if (legendEl) __renderLegendRows__("statGraphModalLegend", workingPayload.slices, workingPayload.valueFormatter, { mode: workingPayload.legendMode || "share", enableColorEdit: true, payload: workingPayload });
  if (canvasWrap){
    try{ canvasWrap.classList.remove("is-bars"); }catch(_){ }
  }
  drawPie("statGraphModalCanvas", workingPayload.slices, {
    centerTitle: workingPayload.centerTitle != null ? workingPayload.centerTitle : "Totale",
    centerFormatter: (typeof workingPayload.centerFormatter === "function") ? workingPayload.centerFormatter : euro,
    showCenter: true,
    maxSize: 340,
    minSize: 180
  });
  modal.hidden = false;
  try{ modal.setAttribute("aria-hidden", "false"); }catch(_){ }
}

function __closeStatGraphPopup__(){
  const modal = document.getElementById("statGraphModal");
  if (!modal) return;
  modal.hidden = true;
  try{ modal.setAttribute("aria-hidden", "true"); }catch(_){ }
}

function __ensureStatGraphPopupBound__(){
  const modal = document.getElementById("statGraphModal");
  const closeBtn = document.getElementById("statGraphModalClose");
  if (!modal || modal.__bound) return;
  modal.__bound = true;
  if (closeBtn) closeBtn.addEventListener("click", __closeStatGraphPopup__);
  modal.addEventListener("click", (e)=>{ if (e.target === modal) __closeStatGraphPopup__(); });
}

function __syncOccupazioneMensileGraphColorsFromStatMensili__(){
  try{
    const colors = __mensiliPalette12();
    const statMap = __loadStatCardColorMap__('statmensili');
    const graphMap = __loadGraphColorMap__('occupazione-mensile');
    let changed = false;
    for (let i = 0; i < 12; i++){
      const label = String(__MONTHS_IT[i] || `Mese ${i+1}`);
      const fallback = colors[i % colors.length] || '#2B7CB4';
      const pair = __tagColorPairFromValue__(statMap[label], fallback);
      const next = __monthlyStatCardGraphSpecFromPair__(pair, fallback);
      if (graphMap[label] !== next){
        graphMap[label] = next;
        changed = true;
      }
    }
    if (changed) __saveGraphColorMap__('occupazione-mensile', graphMap);
  }catch(_){ }
}

function __occupazioneMensileSlices__(mensili){
  const vals = Array.isArray(mensili && mensili.occPctByMonth) ? mensili.occPctByMonth : [];
  const colors = __mensiliPalette12();
  return new Array(12).fill(0).map((_,i)=>{
    const label = String(__MONTHS_IT[i] || `Mese ${i+1}`);
    const fallback = colors[i % colors.length] || "#2b7cb4";
    const pair = __getStatCardColorPair__('statmensili', label, fallback);
    const syncedColor = __tagColorTextHex__(pair.bg || fallback, pair.fg || '', false);
    return {
      label,
      value: Math.max(0, Math.min(100, Number(vals[i] || 0) || 0)),
      color: syncedColor
    };
  });
}

function __operatorGraphColors__(){
  try{
    const catalog = getOperatoriCatalogFromSettings ? (getOperatoriCatalogFromSettings() || []) : [];
    const palette = catalog.map(item => getOperatoreColorHexByName(item?.nome || '', item?.colore || 'blue')).filter(Boolean);
    if (palette.length) return palette;
  }catch(_){ }
  return ['#6fb7d6', '#f29c50', '#4caf7d', '#e25d4b', '#8f78d4', '#d6b276'];
}
function __operatorGraphColors(){
  return __operatorGraphColors__();
}
// Compatibilità difensiva: alcune build/client possono richiamare un alias errato
// e bloccare il rendering dei grafici operatori con "... is not defined".
function operatorgraphicColor(){
  return __operatorGraphColors__();
}
function operatorGraphicColor(){
  return __operatorGraphColors__();
}

function computeStatOrePuliziaGrafico(rows){
  const listRaw = Array.isArray(rows) ? rows : [];
  const list = __filterByExerciseYear__(listRaw, state.exerciseYear || loadExerciseYear(), ["data","date","Data","createdAt","created_at","updatedAt","updated_at"]);
  const totals = new Map();
  for (const r of list){
    const name = getCanonicalActiveOperatorName(r?.operatore || r?.nome || '');
    if (!name) continue;
    const ore = Number(String(r?.ore ?? r?.Ore ?? 0).replace(",", "."));
    if (!isFinite(ore) || ore <= 0) continue;
    totals.set(name, (totals.get(name) || 0) + ore);
  }
  let fromSet = [];
  try{ fromSet = getActiveOperatorNames ? (getActiveOperatorNames() || []) : []; }catch(_){ fromSet = []; }
  const ordered = [];
  const pushUnique = (v)=>{ const s = String(v||"").trim(); if (s && !ordered.includes(s)) ordered.push(s); };
  fromSet.forEach(pushUnique);
  const palette = __operatorGraphColors();
  const slices = ordered.map((name, i)=>({
    label: name,
    value: Number(totals.get(name) || 0),
    color: getOperatoreColorHexByName(name, palette[i % palette.length] || 'blue')
  }));
  return slices;
}

function __statPageSliceHexFromCard__(pageKey, cardKey, fallback){
  try{
    const pair = __getStatCardColorPair__(pageKey, cardKey, fallback || '#2B7CB4');
    return __graphColorValueToHex__(pair.bg || fallback || '#2B7CB4', fallback || '#2B7CB4');
  }catch(_){ return __graphColorValueToHex__(fallback || '#2B7CB4', '#2B7CB4'); }
}

function __statPieOptionsForPage__(pageKey){
  try{
    const wrap = document.querySelector(`#page-${pageKey} .statgen-line-chart-wrap`);
    const isLandscape = !!(String((state && state.page) || '') === String(pageKey || '') && __isLandscapeOrientation__());
    const rect = wrap ? wrap.getBoundingClientRect() : null;
    const available = rect ? Math.max(180, Math.min(rect.width - 16, rect.height - 16)) : (isLandscape ? 420 : 240);
    return {
      size: available,
      maxSize: available,
      minSize: Math.max(160, Math.min(available, isLandscape ? 240 : 180)),
      showCenter: false
    };
  }catch(_){ return { maxSize: 240, minSize: 180, showCenter:false }; }
}

function __prepareStatPiePage__(pageKey){
  try{
    const wrap = document.querySelector(`#page-${pageKey} .statgen-line-chart-wrap`);
    if (wrap){
      __applyStatGenRegChartWrapVisual__(wrap);
      __bindStatSharedLineChartLongPress__(wrap);
      try{ wrap.title = 'Pressione lunga per cambiare design grafico'; }catch(_){ }
    }
  }catch(_){ }
}

function __statGuestMoney__(value){
  if (value === null || value === undefined) return 0;
  if (typeof value === 'number') return isFinite(value) ? value : 0;
  let s = String(value).trim();
  if (!s) return 0;
  if (s.includes(',') && s.includes('.')) s = s.replace(/\./g, '').replace(',', '.');
  else if (s.includes(',')) s = s.replace(',', '.');
  const n = Number(s);
  return isFinite(n) ? n : 0;
}

function __statGuestMonthIso__(row){
  const candidates = [
    row?.data_prenotazione, row?.dataPrenotazione,
    row?.booking_date, row?.bookingDate,
    row?.createdAt, row?.created_at,
    row?.updatedAt, row?.updated_at,
    row?.data, row?.date,
    row?.check_in, row?.checkIn, row?.arrivo, row?.data_arrivo, row?.dataArrivo,
    row?.check_out, row?.checkOut, row?.partenza, row?.data_partenza, row?.dataPartenza
  ];
  for (const raw of candidates){
    let iso = '';
    try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(raw || '') : ''; }catch(_){ iso = ''; }
    if (iso && /^\d{4}-\d{2}-\d{2}$/.test(iso)) return iso;
    try{
      const d = new Date(raw || '');
      if (!Number.isNaN(d.getTime())) return d.toISOString().slice(0, 10);
    }catch(_){ }
  }
  return '';
}

function __statGuestDualMonthlySeries__(resolver){
  const guests = Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []);
  const primary = new Array(12).fill(0);
  const secondary = new Array(12).fill(0);
  guests.forEach((guest) => {
    if (!guest) return;
    const iso = __statGuestMonthIso__(guest);
    if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return;
    const month = parseInt(iso.slice(5, 7), 10);
    if (!Number.isFinite(month) || month < 1 || month > 12) return;
    const resolved = (typeof resolver === 'function') ? (resolver(guest) || {}) : {};
    primary[month - 1] += Math.max(0, Number(resolved.primary || 0) || 0);
    secondary[month - 1] += Math.max(0, Number(resolved.secondary || 0) || 0);
  });
  for (let i = 0; i < 12; i += 1){
    primary[i] = Math.round((Number(primary[i] || 0) || 0) * 100) / 100;
    secondary[i] = Math.round((Number(secondary[i] || 0) || 0) * 100) / 100;
  }
  return { primary, secondary };
}


function __statGuestDualMonthlySeriesForRows__(rows, resolver){
  const guests = Array.isArray(rows) ? rows : [];
  const primary = new Array(12).fill(0);
  const secondary = new Array(12).fill(0);
  guests.forEach((guest) => {
    if (!guest) return;
    const iso = __statGuestMonthIso__(guest);
    if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return;
    const month = parseInt(iso.slice(5, 7), 10);
    if (!Number.isFinite(month) || month < 1 || month > 12) return;
    const resolved = (typeof resolver === 'function') ? (resolver(guest) || {}) : {};
    primary[month - 1] += Math.max(0, Number(resolved.primary || 0) || 0);
    secondary[month - 1] += Math.max(0, Number(resolved.secondary || 0) || 0);
  });
  for (let i = 0; i < 12; i += 1){
    primary[i] = Math.round((Number(primary[i] || 0) || 0) * 100) / 100;
    secondary[i] = Math.round((Number(secondary[i] || 0) || 0) * 100) / 100;
  }
  return { primary, secondary };
}

function __statSpeseMonthlyBreakdownForRows__(rows){
  const items = Array.isArray(rows) ? rows : [];
  const out = {
    totale: new Array(12).fill(0),
    contanti: new Array(12).fill(0),
    tassa: new Array(12).fill(0),
    iva22: new Array(12).fill(0),
    iva10: new Array(12).fill(0),
    iva4: new Array(12).fill(0)
  };
  const toNumber = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === 'number') return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    if (s.includes(',') && s.includes('.')) s = s.replace(/\./g, '').replace(',', '.');
    else if (s.includes(',')) s = s.replace(',', '.');
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };
  items.forEach((row) => {
    const raw = row?.dataSpesa || row?.data || row?.data_spesa || '';
    let iso = '';
    try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(raw) : ''; }catch(_){ iso = ''; }
    if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return;
    const month = parseInt(iso.slice(5,7), 10);
    if (!Number.isFinite(month) || month < 1 || month > 12) return;
    const idx = month - 1;
    const lordo = Math.max(0, toNumber(row?.importoLordo || row?.importo_lordo || row?.importo));
    if (__isFuoriBudgetSpesa__(row)) return;
    const categoria = String(row?.categoria || row?.cat || '').trim().toLowerCase();
    const aliquota = toNumber(row?.aliquotaIva ?? row?.aliquota_iva ?? row?.aliquota);
    out.totale[idx] += lordo;
    if (categoria.includes('contant')) { out.contanti[idx] += lordo; return; }
    if (categoria.includes('tassa') && categoria.includes('sogg')) { out.tassa[idx] += lordo; return; }
    if (categoria.includes('iva')){
      if (categoria.includes('22')) { out.iva22[idx] += lordo; return; }
      if (categoria.includes('10')) { out.iva10[idx] += lordo; return; }
      if (categoria.includes('4')) { out.iva4[idx] += lordo; return; }
    }
    if (!Number.isNaN(aliquota)){
      if (aliquota >= 21.5) out.iva22[idx] += lordo;
      else if (aliquota >= 9.5 && aliquota < 11.5) out.iva10[idx] += lordo;
      else if (aliquota >= 3.5 && aliquota < 5.5) out.iva4[idx] += lordo;
      else if (__isFuoriBudgetSpesa__(row)) out.contanti[idx] += lordo;
    } else if (__isFuoriBudgetSpesa__(row)) {
      out.contanti[idx] += lordo;
    }
  });
  return out;
}

function __computeStatGenFromData__(data){
  const guests = Array.isArray(data?.guests) ? data.guests : [];
  const speseRows = Array.isArray(data?.spese) ? data.spese : [];
  const report = data?.report || null;
  const fiscalMode = getStatFiscalMode();
  let fatturato = 0;
  let totalePrenotazioniConRicevuta = 0;
  let giacenza = 0;
  let conRicevuta = 0;
  let senzaRicevuta = 0;
  guests.forEach((g) => {
    const pren = __statGuestMoney__(g?.importo_prenotazione ?? g?.importo_prenota ?? g?.importoPrenotazione ?? g?.importoPrenota ?? 0);
    fatturato += pren;
    if (pren > 0 && _guestHasFiscalReceipt(g)) totalePrenotazioniConRicevuta += pren;
    const dep = __statGuestMoney__(g?.acconto_importo ?? g?.accontoImporto ?? 0);
    const saldo = __statGuestMoney__(g?.saldo_pagato ?? g?.saldoPagato ?? g?.saldo ?? 0);
    giacenza += (dep + saldo);
    const depRec = _isRicevutaFlag(g, 'acconto');
    const saldoRec = _isRicevutaFlag(g, 'saldo');
    if (dep > 0) depRec ? (conRicevuta += dep) : (senzaRicevuta += dep);
    if (saldo > 0) saldoRec ? (conRicevuta += saldo) : (senzaRicevuta += saldo);
  });
  const speseTot = speseRows.reduce((sum, it) => {
    if (__isFuoriBudgetSpesa__(it)) return sum;
    return sum + __statGuestMoney__(it?.importoLordo ?? it?.lordo ?? it?.importo ?? 0);
  }, 0);
  giacenza = (__statGuestMoney__(conRicevuta) + __statGuestMoney__(senzaRicevuta)) - __statGuestMoney__(speseTot);
  let ivaSpese = __statGuestMoney__(report?.totals?.ivaDetraibile ?? 0);
  if (!isFinite(ivaSpese) || ivaSpese === 0){
    ivaSpese = speseRows.reduce((sum, row) => {
      const ivaField = __statGuestMoney__(row?.iva ?? row?.IVA ?? 0);
      if (ivaField > 0) return sum + ivaField;
      const lordo = __statGuestMoney__(row?.importoLordo ?? row?.lordo ?? row?.importo ?? 0);
      if (!isFinite(lordo) || lordo <= 0) return sum;
      const catRaw = String(row?.categoria ?? row?.cat ?? '').trim().toLowerCase();
      let rate = 0;
      if (catRaw.includes('iva')){
        if (catRaw.includes('22')) rate = 22;
        else if (catRaw.includes('10')) rate = 10;
        else if (catRaw.includes('4')) rate = 4;
      } else {
        const n = parseFloat(String(row?.aliquotaIva ?? row?.aliquota_iva ?? '').replace(',', '.'));
        if (!isNaN(n)){
          if (n >= 21.5) rate = 22;
          else if (n >= 9.5 && n < 11.5) rate = 10;
          else if (n >= 3.5 && n < 5.5) rate = 4;
        }
      }
      if (rate <= 0) return sum;
      const imponibile = lordo / (1 + rate/100);
      const iva = lordo - imponibile;
      return sum + (isFinite(iva) ? iva : 0);
    }, 0);
  }
  const ivaPrenotazioni = totalePrenotazioniConRicevuta * 0.10;
  const ivaDaVersareSocieta = ivaPrenotazioni - (__statGuestMoney__(ivaSpese) || 0);
  const ivaDaVersare = fiscalMode === 'societa' ? ivaDaVersareSocieta : 0;
  const guadagno = fatturato - speseTot - (fiscalMode === 'societa' ? ivaDaVersareSocieta : 0);
  return { fiscalMode, fatturatoTotale: fatturato, speseTotali: speseTot, senzaRicevuta, conRicevuta, ivaDaVersare, guadagnoTotale: guadagno, giacenzaCassa: giacenza };
}

function __statGenSeriesListFromData__(data){
  const statGen = __computeStatGenFromData__(data || {});
  const revenueData = __statGenRegistrationsByMonth__(Array.isArray(data?.guests) ? data.guests : []);
  const spese = __statSpeseMonthlyBreakdownForRows__(Array.isArray(data?.spese) ? data.spese : []);
  const ricevute = __statGuestDualMonthlySeriesForRows__(Array.isArray(data?.guests) ? data.guests : [], (guest) => {
    const dep = __statGuestMoney__(guest?.acconto_importo ?? guest?.accontoImporto ?? 0);
    const saldo = __statGuestMoney__(guest?.saldo_pagato ?? guest?.saldoPagato ?? guest?.saldo ?? 0);
    const depRec = _isRicevutaFlag(guest, 'acconto');
    const saldoRec = _isRicevutaFlag(guest, 'saldo');
    return { primary: (depRec ? dep : 0) + (saldoRec ? saldo : 0), secondary: (depRec ? 0 : dep) + (saldoRec ? 0 : saldo) };
  });
  const revenueCum = __statMonthlyCumulative__(revenueData.monthlyRevenue || new Array(12).fill(0));
  const speseCum = __statMonthlyCumulative__(spese.totale || new Array(12).fill(0));
  const withRecCum = __statMonthlyCumulative__(ricevute.primary || new Array(12).fill(0));
  const withoutRecCum = __statMonthlyCumulative__(ricevute.secondary || new Array(12).fill(0));
  const ivaTotal = Number(statGen?.ivaDaVersare || 0) || 0;
  const revenueTotal = Math.max(1, Number(statGen?.fatturatoTotale || 0) || 0);
  const ivaCum = revenueCum.map((value) => Math.round(((value / revenueTotal) * ivaTotal) * 100) / 100);
  const gainCum = revenueCum.map((value, idx) => Math.round(((value - (Number(speseCum[idx] || 0) || 0)) * 100)) / 100);
  const cashCum = gainCum.slice();
  return {
    'fatturato-totale': revenueCum,
    'spese-totali': speseCum,
    'senza-ricevuta': withoutRecCum,
    'con-ricevuta': withRecCum,
    'iva-da-versare': ivaCum,
    'guadagno-totale': gainCum,
    'giacenza-in-cassa': cashCum
  };
}


function __statChartFiltersEnsure__(){
  if (!state.statChartFilters || typeof state.statChartFilters !== 'object') state.statChartFilters = {};
  return state.statChartFilters;
}

function __getStatChartFilter__(pageKey){
  const safePageKey = String(pageKey || '').trim().toLowerCase();
  const filters = __statChartFiltersEnsure__();
  return String(filters[safePageKey] || '').trim();
}

function __setStatChartFilter__(pageKey, cardKey){
  const safePageKey = String(pageKey || '').trim().toLowerCase();
  const safeCardKey = String(cardKey || '').trim();
  const filters = __statChartFiltersEnsure__();
  filters[safePageKey] = safeCardKey;
  return safeCardKey;
}

function __clearStatChartFilter__(pageKey){
  const safePageKey = String(pageKey || '').trim().toLowerCase();
  const filters = __statChartFiltersEnsure__();
  delete filters[safePageKey];
}

function __statChartSeriesIsVisible__(pageKey, cardKey){
  const selected = __getStatChartFilter__(pageKey);
  if (!selected) return true;
  return String(selected) === String(cardKey || '').trim();
}

function __statChartLineColorFromStatCard__(pageKey, cardKey, fallback){
  try{
    const pair = __getStatCardColorPair__(pageKey, cardKey, fallback || 'blue-4');
    const preferred = pair?.fg || fallback || pair?.bg || '#2B7CB4';
    return __graphColorValueToHex__(preferred, fallback || pair?.bg || '#2B7CB4');
  }catch(_){ return __graphColorValueToHex__(fallback || '#2B7CB4', '#2B7CB4'); }
}

function __statChartLineColorFromOperatore__(name, fallback){
  try{
    const item = getOperatoreCatalogItemByName(name);
    const bgSpec = item?.colore || fallback || 'blue-4';
    const preferred = item?.coloreTesto || __tagColorTextHex__(bgSpec, item?.coloreTesto || '', false) || fallback || bgSpec;
    return __graphColorValueToHex__(preferred || fallback || bgSpec, fallback || bgSpec || '#2B7CB4');
  }catch(_){ return __graphColorValueToHex__(fallback || '#2B7CB4', '#2B7CB4'); }
}

function __statChartLineColorFromRenderedCard__(pageKey, cardKey, fallback){
  try{
    const pair = __getStatCardColorPair__(pageKey, cardKey, fallback || 'blue-4');
    const preferredPairFg = String(pair?.fg || '').trim();
    if (preferredPairFg) return __graphColorValueToHex__(preferredPairFg, fallback || pair?.bg || '#2B7CB4');
    const selector = `#page-${String(pageKey || '').trim().toLowerCase()} [data-stat-card-key="${String(cardKey || '').replace(/"/g, '\"')}"]`;
    const el = document.querySelector(selector);
    if (el){
      const cssVar = String(el.style.getPropertyValue('--cardtext') || getComputedStyle(el).getPropertyValue('--cardtext') || '').trim();
      if (cssVar) return __graphColorValueToHex__(cssVar, fallback || '#2B7CB4');
      const textNode = el.querySelector('.stat-name, .stat-val, .month-name, .month-val, .month-occ, .kpi-label, .kpi-value, .fin-label, .fin-val, .stats-graph-card-title, .stats-graph-note');
      if (textNode){
        const textCss = getComputedStyle(textNode).color || '';
        if (textCss) return __graphColorValueToHex__(textCss, fallback || '#2B7CB4');
      }
      const css = getComputedStyle(el).color || '';
      if (css) return __graphColorValueToHex__(css, fallback || '#2B7CB4');
    }
  }catch(_){ }
  return __graphColorValueToHex__(fallback || '#2B7CB4', '#2B7CB4');
}

function __statChartSelectedLineColor__(pageKey, fallback){
  try{
    const selected = __getStatChartFilter__(pageKey);
    if (selected) return __statChartLineColorFromRenderedCard__(pageKey, selected, fallback || '#2B7CB4');
  }catch(_){ }
  return __graphColorValueToHex__(fallback || '#2B7CB4', '#2B7CB4');
}

function __refreshSpecificStatChart__(pageKey){
  const safePageKey = String(pageKey || '').trim().toLowerCase();
  try{
    if (safePageKey === 'statgen') return renderStatGen();
    if (safePageKey === 'statmensili') return renderStatMensili();
    if (safePageKey === 'statspese') return renderStatSpese();
    if (safePageKey === 'statcancellazioni') return renderStatCancellazioni();
    if (safePageKey === 'statprenotazioni') return renderStatRicevute();
    if (safePageKey === 'statchannel') return renderStatChannel();
    if (safePageKey === 'statpulizie') return renderStatPulizie(Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []);
    if (safePageKey === 'statamministratore') return renderStatAmministratore();
  }catch(_){ }
}

function __setStatCardSelectionState__(el, pageKey, cardKey){
  if (!el) return;
  const selected = __getStatChartFilter__(pageKey) === String(cardKey || '').trim();
  try{ el.classList.toggle('is-selected', !!selected); }catch(_){ }
  try{ el.setAttribute('aria-pressed', selected ? 'true' : 'false'); }catch(_){ }
}

function __bindStatChartCardToggle__(el, pageKey, cardKey){
  if (!el || el.dataset.statChartToggleBound === '1') return;
  el.dataset.statChartToggleBound = '1';
  el.addEventListener('click', (e) => {
    try{ e.preventDefault(); }catch(_){ }
    try{ e.stopPropagation(); }catch(_){ }
    __setStatChartFilter__(pageKey, cardKey);
    __refreshSpecificStatChart__(pageKey);
  });
}

function __bindStatChartWrapReset__(pageKey){
  const wrap = document.querySelector(`#page-${String(pageKey || '').trim().toLowerCase()} .statgen-line-chart-wrap`);
  if (!wrap || wrap.dataset.statChartResetBound === '1') return;
  wrap.dataset.statChartResetBound = '1';
  let lastTouchAt = 0;
  const runReset = (e) => {
    if (!__getStatChartFilter__(pageKey)) return;
    try{ e && e.preventDefault && e.preventDefault(); }catch(_){ }
    try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){ }
    __clearStatChartFilter__(pageKey);
    __refreshSpecificStatChart__(pageKey);
  };
  wrap.addEventListener('dblclick', runReset);
  wrap.addEventListener('touchend', (e) => {
    const now = Date.now();
    if (now - lastTouchAt <= 360) runReset(e);
    lastTouchAt = now;
  }, { passive:false });
}

function __statCurrentMonthIndex__(){
  try{
    const now = new Date();
    const idx = Number(now.getMonth());
    return Number.isFinite(idx) ? Math.max(0, Math.min(11, idx)) : 11;
  }catch(_){ return 11; }
}

function __statZeroFutureMonths__(values, opts){
  const list = (Array.isArray(values) ? values : []).slice(0, 12);
  while (list.length < 12) list.push(0);
  const includeCurrent = !!(opts && opts.includeCurrent);
  const currentIdx = __statCurrentMonthIndex__();
  const now = new Date();
  const currentYear = Number(now.getFullYear());
  let selectedYear = Number.NaN;
  try{ selectedYear = Number(opts && opts.year); }catch(_){ selectedYear = Number.NaN; }
  if (!Number.isFinite(selectedYear)){
    try{ selectedYear = Number(state && state.exerciseYear); }catch(_){ selectedYear = Number.NaN; }
  }
  if (!Number.isFinite(selectedYear)){
    try{ selectedYear = Number((typeof loadExerciseYear === 'function') ? loadExerciseYear() : ''); }catch(_){ selectedYear = Number.NaN; }
  }
  const shouldTrimFuture = Number.isFinite(selectedYear) && Number.isFinite(currentYear) && selectedYear === currentYear;
  return list.map((value, idx) => {
    if (shouldTrimFuture && (idx > currentIdx || (includeCurrent && idx >= currentIdx))) return 0;
    return Math.round((Number(value || 0) || 0) * 100) / 100;
  });
}

function __statMonthlyCumulative__(values){
  let acc = 0;
  return (Array.isArray(values) ? values : []).map((value) => {
    acc += (Number(value || 0) || 0);
    return Math.round(acc * 100) / 100;
  });
}

function __statMaskMonthlyValuesByCard__(values, cardKey){
  const selected = String(cardKey || '').trim();
  if (!selected) return (Array.isArray(values) ? values : []).slice();
  const monthNames = Array.isArray(__MONTHS_IT) ? __MONTHS_IT : [];
  const idx = monthNames.findIndex((label) => String(label || '').trim() === selected);
  if (idx < 0) return (Array.isArray(values) ? values : []).slice();
  return (Array.isArray(values) ? values : []).map((value, i) => (i === idx ? value : 0));
}

function __statSpeseMonthlyBreakdown__(){
  const items = __getStatsSpese();
  const out = {
    totale: new Array(12).fill(0),
    contanti: new Array(12).fill(0),
    tassa: new Array(12).fill(0),
    iva22: new Array(12).fill(0),
    iva10: new Array(12).fill(0),
    iva4: new Array(12).fill(0)
  };
  const toNumber = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === 'number') return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    if (s.includes(',') && s.includes('.')) s = s.replace(/\./g, '').replace(',', '.');
    else if (s.includes(',')) s = s.replace(',', '.');
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };
  items.forEach((row) => {
    const raw = row?.dataSpesa || row?.data || row?.data_spesa || '';
    let iso = '';
    try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(raw) : ''; }catch(_){ iso = ''; }
    if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return;
    const month = parseInt(iso.slice(5,7), 10);
    if (!Number.isFinite(month) || month < 1 || month > 12) return;
    const idx = month - 1;
    const lordo = Math.max(0, toNumber(row?.importoLordo || row?.importo_lordo || row?.importo));
    const categoria = String(row?.categoria || row?.cat || '').trim().toLowerCase();
    const aliquota = toNumber(row?.aliquotaIva ?? row?.aliquota_iva ?? row?.aliquota);
    out.totale[idx] += lordo;
    if (categoria.includes('contant')) { out.contanti[idx] += lordo; return; }
    if (categoria.includes('tassa') && categoria.includes('sogg')) { out.tassa[idx] += lordo; return; }
    if (categoria.includes('iva')){
      if (categoria.includes('22')) { out.iva22[idx] += lordo; return; }
      if (categoria.includes('10')) { out.iva10[idx] += lordo; return; }
      if (categoria.includes('4')) { out.iva4[idx] += lordo; return; }
    }
    if (!Number.isNaN(aliquota)){
      if (aliquota >= 21.5) out.iva22[idx] += lordo;
      else if (aliquota >= 9.5 && aliquota < 11.5) out.iva10[idx] += lordo;
      else if (aliquota >= 3.5 && aliquota < 5.5) out.iva4[idx] += lordo;
      else if (__isFuoriBudgetSpesa__(row)) out.contanti[idx] += lordo;
    } else if (__isFuoriBudgetSpesa__(row)) {
      out.contanti[idx] += lordo;
    }
  });
  return out;
}

function __statGenSeriesList__(){
  const statGen = computeStatGen();
  const revenueData = __statGenRegistrationsByMonth__();
  const spese = __statSpeseMonthlyBreakdown__();
  const ricevute = __statRicevuteMonthlySeries__();
  const revenueCum = __statZeroFutureMonths__(__statMonthlyCumulative__(revenueData.monthlyRevenue || new Array(12).fill(0)));
  const speseCum = __statZeroFutureMonths__(__statMonthlyCumulative__(spese.totale || new Array(12).fill(0)));
  const withRecCum = __statZeroFutureMonths__(__statMonthlyCumulative__(ricevute.primary || new Array(12).fill(0)));
  const withoutRecCum = __statZeroFutureMonths__(__statMonthlyCumulative__(ricevute.secondary || new Array(12).fill(0)));
  const ivaTotal = Number(statGen?.ivaDaVersare || 0) || 0;
  const revenueTotal = Math.max(1, Number(statGen?.fatturatoTotale || 0) || 0);
  const ivaCum = __statZeroFutureMonths__(revenueCum.map((value) => Math.round(((value / revenueTotal) * ivaTotal) * 100) / 100));
  const gainCum = __statZeroFutureMonths__(revenueCum.map((value, idx) => Math.round(((value - (Number(speseCum[idx] || 0) || 0)) * 100)) / 100));
  const cashCum = gainCum.slice();
  const defs = [
    { key:'fatturato-totale', label:'Fatturato totale', values: revenueCum, fallback:'#2b7cb4' },
    { key:'spese-totali', label:'Spese totali', values: speseCum, fallback:'#e05a4f' },
    { key:'senza-ricevuta', label:'Senza ricevuta', values: withoutRecCum, fallback:'#9aa3af' },
    { key:'con-ricevuta', label:'Con ricevuta', values: withRecCum, fallback:'#6aa0b3' },
    { key:'iva-da-versare', label:'IVA da versare', values: ivaCum, fallback:'#d39b3b' },
    { key:'guadagno-totale', label:'Guadagno totale', values: gainCum, fallback:'#c9772b' },
    { key:'giacenza-in-cassa', label:'Giacenza in cassa', values: cashCum, fallback:'#2b7cb4' }
  ];
  return defs.map((item) => ({
    ...item,
    color: __statChartLineColorFromRenderedCard__('statgen', item.key, item.fallback)
  }));
}

function __statRicevuteMonthlySeries__(){
  return __statGuestDualMonthlySeries__((guest) => {
    const dep = __statGuestMoney__(guest?.acconto_importo ?? guest?.accontoImporto ?? 0);
    const saldo = __statGuestMoney__(guest?.saldo_pagato ?? guest?.saldoPagato ?? guest?.saldo ?? 0);
    const depRec = _isRicevutaFlag(guest, 'acconto');
    const saldoRec = _isRicevutaFlag(guest, 'saldo');
    const withReceipt = (depRec ? dep : 0) + (saldoRec ? saldo : 0);
    const withoutReceipt = (depRec ? 0 : dep) + (saldoRec ? 0 : saldo);
    return { primary: withReceipt, secondary: withoutReceipt };
  });
}

function drawStatRicevuteLineChart(canvasId){
  const series = __statRicevuteMonthlySeries__();
  __drawSharedMonthlyLineChart__(canvasId, series.primary, {
    seriesList: [
      { key:'con-ricevuta', label:'Con ricevuta', values: series.primary, color: __statChartLineColorFromRenderedCard__('statprenotazioni', 'con-ricevuta', '#6aa0b3') },
      { key:'senza-ricevuta', label:'Senza ricevuta', values: series.secondary, color: __statChartLineColorFromRenderedCard__('statprenotazioni', 'senza-ricevuta', '#9aa3af') }
    ].filter((item) => __statChartSeriesIsVisible__('statprenotazioni', item.key)),
    bubbleFormatter: (value) => __statLineChartCompactEuro__(value),
    yTickFormatter: (value) => __statLineChartCompactEuro__(value)
  });
}

function renderStatRicevute(){
  const statGen = computeStatGen();
  state.statGen = statGen;
  const rows = [
    { cardKey:'senza-ricevuta', label:'Senza ricevuta', value: Number(statGen.senzaRicevuta || 0), fallback:'#9aa3af', valueId:'srNoRicevuta' },
    { cardKey:'con-ricevuta', label:'Con ricevuta', value: Number(statGen.conRicevuta || 0), fallback:'#6aa0b3', valueId:'srRicevuta' }
  ];
  rows.forEach((row) => {
    const valueEl = document.getElementById(row.valueId);
    if (valueEl) valueEl.textContent = euro(row.value || 0);
    const card = valueEl ? valueEl.closest('.stat-row') : null;
    if (card){
      __applyStatCardTextColor__(card, 'statprenotazioni', row.cardKey, row.fallback);
      __bindStatCardColorLongPress__(card, 'statprenotazioni', row.cardKey, row.fallback);
      __bindStatChartCardToggle__(card, 'statprenotazioni', row.cardKey);
      __setStatCardSelectionState__(card, 'statprenotazioni', row.cardKey);
    }
  });
  __prepareStatPiePage__('statprenotazioni');
  __bindStatChartWrapReset__('statprenotazioni');
  try{ drawStatRicevuteLineChart('statRicevuteCanvas'); }catch(_){ }
}

function __statChannelCommissionPct__(guest){
  const directValue = guest?.channel_commissione ?? guest?.channelCommissione ?? guest?.channel_commission_pct ?? guest?.channelCommissionPct;
  if (directValue !== null && directValue !== undefined && String(directValue).trim() !== ''){
    return __parseChannelCommissionValue__(directValue, 0);
  }
  const channelId = String(guest?.channel_id ?? guest?.channelId ?? '').trim();
  if (channelId){
    const catalogItem = getChannelCatalogItemById(channelId);
    if (catalogItem) return __parseChannelCommissionValue__(catalogItem?.commissione, 0);
  }
  const bookingVal = __statGuestMoney__(guest?.importo_booking ?? guest?.importoBooking ?? guest?.booking ?? null);
  return bookingVal > 0 ? 1 : 0;
}

function __statChannelBucketLabelFromGuest__(guest){
  const candidates = [guest?.channel_nome, guest?.channelNome, guest?.channel_name, guest?.channelName, guest?.pms, guest?.fonte];
  for (const raw of candidates){
    const clean = String(raw || '').trim();
    if (clean) return clean;
  }
  return 'PMS';
}

function __statChannelSeriesBundle__(){
  const guests = Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []);
  const catalog = getChannelCatalogFromSettings();
  const bucketMap = new Map();
  const ensureBucket = (key, cfg = {}) => {
    const safeKey = String(key || '').trim();
    if (!safeKey) return null;
    if (!bucketMap.has(safeKey)){
      bucketMap.set(safeKey, {
        key: safeKey,
        label: String(cfg.label || '').trim() || 'PMS',
        monthly: new Array(12).fill(0),
        total: 0,
        fallback: {
          bg: cfg.bg || 'blue-4',
          border: cfg.border || cfg.bg || 'blue-4',
          fg: cfg.fg || ''
        }
      });
    }
    const bucket = bucketMap.get(safeKey);
    if (cfg.label) bucket.label = String(cfg.label).trim() || bucket.label;
    if (cfg.bg) bucket.fallback.bg = cfg.bg;
    if (cfg.border || cfg.bg) bucket.fallback.border = cfg.border || cfg.bg || bucket.fallback.border;
    if (cfg.fg !== undefined) bucket.fallback.fg = cfg.fg || '';
    return bucket;
  };

  catalog.forEach((item) => {
    ensureBucket(`channel:${String(item.id || '').trim()}`, {
      label: item?.nome || 'PMS',
      bg: item?.colore || 'blue-4',
      border: item?.colore || 'blue-4',
      fg: item?.colore || item?.coloreGrafico || item?.coloreTesto || ''
    });
  });

  guests.forEach((guest) => {
    const pren = __statGuestMoney__(guest?.importo_prenotazione ?? guest?.importo_prenota ?? guest?.importoPrenotazione ?? guest?.importoPrenota ?? 0);
    if (!isFinite(pren) || pren <= 0) return;
    const iso = __statGuestMonthIso__(guest);
    const monthIdx = iso ? Math.max(0, Math.min(11, Number(String(iso).slice(5,7)) - 1)) : -1;
    if (monthIdx < 0 || monthIdx > 11) return;
    const channelId = String(guest?.channel_id ?? guest?.channelId ?? '').trim();
    const item = channelId ? getChannelCatalogItemById(channelId) : null;
    const fallbackLabel = item?.nome || __statChannelBucketLabelFromGuest__(guest) || 'PMS';
    const bucketKey = channelId ? `channel:${channelId}` : `channel-name:${String(fallbackLabel).trim().toLowerCase()}`;
    const bucket = ensureBucket(bucketKey, {
      label: fallbackLabel,
      bg: item?.colore || guest?.channel_colore || guest?.channelColor || 'blue-4',
      border: item?.colore || guest?.channel_colore || guest?.channelColor || 'blue-4',
      fg: item?.colore || item?.coloreGrafico || item?.coloreTesto || guest?.channel_colore || guest?.channelColor || guest?.channel_colore_testo || guest?.channelColorText || ''
    });
    if (!bucket) return;
    bucket.monthly[monthIdx] = Math.round((Number(bucket.monthly[monthIdx] || 0) + pren) * 100) / 100;
    bucket.total = Math.round((Number(bucket.total || 0) + pren) * 100) / 100;
  });

  const rows = Array.from(bucketMap.values()).map((item) => ({
    key: item.key,
    label: item.label,
    values: item.monthly.slice(0, 12),
    value: item.total,
    fallback: item.fallback || { bg:'blue-4', border:'blue-4', fg:'' }
  }));

  const catalogOrder = new Map();
  catalog.forEach((item, idx) => {
    const key = `channel:${String(item?.id || '').trim()}`;
    if (!catalogOrder.has(key)) catalogOrder.set(key, idx);
  });
  rows.sort((a, b) => {
    const aOrder = catalogOrder.has(a.key) ? catalogOrder.get(a.key) : Number.MAX_SAFE_INTEGER;
    const bOrder = catalogOrder.has(b.key) ? catalogOrder.get(b.key) : Number.MAX_SAFE_INTEGER;
    if (aOrder !== bOrder) return aOrder - bOrder;
    return String(a.label || '').localeCompare(String(b.label || ''), 'it', { sensitivity:'base' });
  });
  return rows;
}

function __statChannelMonthlySeries__(){
  return __statChannelSeriesBundle__();
}

function drawStatChannelLineChart(canvasId){
  const seriesList = __statChannelMonthlySeries__().map((item) => ({
    key: item.key,
    label: item.label,
    values: item.values,
    color: __statChartLineColorFromRenderedCard__('statchannel', item.key, item.fallback || '#2b7cb4')
  })).filter((item) => __statChartSeriesIsVisible__('statchannel', item.key));
  const baseValues = seriesList.length ? (seriesList[0].values || new Array(12).fill(0)) : new Array(12).fill(0);
  __drawSharedMonthlyLineChart__(canvasId, baseValues, {
    seriesList,
    bubbleFormatter: (value) => __statLineChartCompactEuro__(value),
    yTickFormatter: (value) => __statLineChartCompactEuro__(value)
  });
}

function renderStatChannel(){
  const rows = __statChannelMonthlySeries__();
  const stack = document.getElementById('statPmsRows');
  if (stack){
    stack.innerHTML = rows.map((row) => `
      <button class="stat-row" data-stat-card-key="${String(row.key || '').replace(/"/g, '&quot;')}" type="button">
        <span class="stat-name">${escapeHtml(row.label || 'PMS')}</span>
        <span class="stat-val">${euro(row.value || 0)}</span>
      </button>
    `).join('');
    stack.querySelectorAll('.stat-row').forEach((card) => {
      const row = rows.find((item) => String(item.key) === String(card.dataset.statCardKey || ''));
      if (!row) return;
      __applyStatCardTextColor__(card, 'statchannel', row.key, row.fallback || '#2b7cb4');
      __bindStatCardColorLongPress__(card, 'statchannel', row.key, row.fallback || '#2b7cb4');
      __bindStatChartCardToggle__(card, 'statchannel', row.key);
      __setStatCardSelectionState__(card, 'statchannel', row.key);
    });
  }
  __prepareStatPiePage__('statchannel');
  __bindStatChartWrapReset__('statchannel');
  try{ drawStatChannelLineChart('statChannelCanvas'); }catch(_){ }
}

function __statPulizieCardKey__(label){
  try{
    return `operatore-${String(label || '').normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || 'generico'}`;
  }catch(_){
    return `operatore-${String(label || '').toLowerCase().replace(/[^a-z0-9]+/g, '-') || 'generico'}`;
  }
}

function __statPulizieOperatorNames__(rows){
  const ordered = [];
  const seen = new Set();
  const pushName = (value) => {
    const clean = String(value || '').trim();
    if (!clean) return;
    const key = clean.toLowerCase();
    if (seen.has(key)) return;
    seen.add(key);
    ordered.push(clean);
  };
  try{ (getActiveOperatorNames ? (getActiveOperatorNames() || []) : []).forEach(pushName); }catch(_){ }
  return ordered;
}

function __statPulizieMonthlySeriesByOperator__(rows){
  const orderedNames = __statPulizieOperatorNames__(rows);
  const listRaw = Array.isArray(rows) ? rows : [];
  const list = __filterByExerciseYear__(listRaw, state.exerciseYear || loadExerciseYear(), ['data','date','Data','createdAt','created_at','updatedAt','updated_at']);
  const map = new Map();
  orderedNames.forEach((name) => { map.set(name, new Array(12).fill(0)); });
  list.forEach((row) => {
    if (!row) return;
    let iso = '';
    try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(row?.data ?? row?.date ?? row?.Data ?? row?.createdAt ?? row?.created_at ?? '') : ''; }catch(_){ iso = ''; }
    if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return;
    const month = parseInt(iso.slice(5, 7), 10);
    if (!Number.isFinite(month) || month < 1 || month > 12) return;
    const hours = Number(String(row?.ore ?? row?.Ore ?? 0).replace(',', '.'));
    if (!isFinite(hours) || hours < 0) return;
    const canonical = getCanonicalActiveOperatorName(row?.operatore || row?.nome || '');
    const operatorName = String(canonical || '').trim();
    if (!operatorName) return;
    if (!map.has(operatorName)) map.set(operatorName, new Array(12).fill(0));
    map.get(operatorName)[month - 1] += hours;
  });
  return Array.from(map.entries()).map(([name, values]) => ({
    key: __statPulizieCardKey__(name),
    label: name,
    values: values.map((value) => Math.round((Number(value || 0) || 0) * 100) / 100),
    color: __statChartLineColorFromRenderedCard__('statpulizie', __statPulizieCardKey__(name), __statChartLineColorFromOperatore__(name, '#2b7cb4'))
  }));
}

function drawStatPulizieLineChart(canvasId, rows){
  const seriesList = __statPulizieMonthlySeriesByOperator__(rows).filter((item) => __statChartSeriesIsVisible__('statpulizie', item.key));
  __drawSharedMonthlyLineChart__(canvasId, new Array(12).fill(0), {
    seriesList,
    bubbleFormatter: (value) => `${__fmtHours_(value) || '0'}h`,
    yTickFormatter: (value) => `${Math.round(Number(value || 0) || 0)}h`
  });
}

function renderStatPulizie(operatoriRows){
  try{ state.statGraficiOperatoriRows = Array.isArray(operatoriRows) ? operatoriRows.slice() : []; }catch(_){ }
  const seriesList = __statPulizieMonthlySeriesByOperator__(Array.isArray(operatoriRows) ? operatoriRows : []);
  const container = document.getElementById('statPulizieRows');
  if (container) container.innerHTML = '';
  const rows = (Array.isArray(seriesList) && seriesList.length)
    ? seriesList.map((series) => ({
        cardKey: series.key,
        label: String(series.label || 'Operatore'),
        value: Math.round((series.values || []).reduce((sum, value) => sum + (Number(value || 0) || 0), 0) * 100) / 100,
        fallback: __closestGraphColorSpec__(series.color || '#2b7cb4')
      }))
    : [{ cardKey:'nessun-dato', label:'Nessun dato', value:0, fallback:'blue-4' }];

  if (container){
    rows.forEach((row) => {
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'stat-row';
      btn.dataset.statCardKey = row.cardKey;
      btn.style.setProperty('--statbg', __graphColorValueToHex__(row.fallback, '#2B7CB4'));
      btn.innerHTML = `
        <span aria-hidden="true" class="stat-ico-wrap"><svg class="ui-ico" viewbox="0 0 24 24"><path d="M6 3h12"></path><path d="M8 3v4"></path><path d="M16 3v4"></path><path d="M12 12v4"></path><path d="M12 12l3-2"></path><path d="M12 21a7 7 0 1 0 0-14 7 7 0 0 0 0 14Z"></path></svg></span>
        <span class="stat-name">${escapeHtml(row.label)}</span>
        <span class="stat-val">${row.value > 0 ? `${__fmtHours_(row.value) || '0'}h` : '0h'}</span>`;
      container.appendChild(btn);
      __applyStatCardTextColor__(btn, 'statpulizie', row.cardKey, row.fallback);
      __bindStatCardColorLongPress__(btn, 'statpulizie', row.cardKey, row.fallback);
      __bindStatChartCardToggle__(btn, 'statpulizie', row.cardKey);
      __setStatCardSelectionState__(btn, 'statpulizie', row.cardKey);
    });
  }

  __prepareStatPiePage__('statpulizie');
  __bindStatChartWrapReset__('statpulizie');
  try{ drawStatPulizieLineChart('statPulizieCanvas', Array.isArray(operatoriRows) ? operatoriRows : []); }catch(_){ }
}

function renderStatGrafici(operatoriRows){
  return renderStatRicevute(operatoriRows);
}


/* Helpers */
function hexToRgba(hex, a){
  const h = (hex || "").replace("#","");
  if (h.length !== 6) return `rgba(0,0,0,${a})`;
  const r = parseInt(h.slice(0,2),16);
  const g = parseInt(h.slice(2,4),16);
  const b = parseInt(h.slice(4,6),16);
  return `rgba(${r},${g},${b},${a})`;
}
function escapeHtml(s){
  return String(s || "")
    .replaceAll("&","&amp;")
    .replaceAll("<","&lt;")
    .replaceAll(">","&gt;")
    .replaceAll('"',"&quot;");
}

// =========================
// STATISTICHE (dDAE_1.020)
// =========================

function computeStatGen(){
  const guests = Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []);
  const report = __getStatsReport() || null;
  const fiscalMode = getStatFiscalMode();

  const money = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === "number") return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    // Normalizza numeri tipo "1.234,56" o "1234,56"
    if (s.includes(",") && s.includes(".")) {
      s = s.replace(/\./g, "").replace(",", ".");
    } else if (s.includes(",")) {
      s = s.replace(",", ".");
    }
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };

  // Fatturato totale = somma di tutte le voci "importo prenotazione"
  let fatturato = 0;

  // Totale prenotazioni con ricevuta fiscale: base IVA società
  let totalePrenotazioniConRicevuta = 0;

  // Giacenza in cassa = somma di tutti gli importi "acconto + saldo"
  let giacenza = 0;

  // (Restano utili per le righe con/senza ricevuta)
  let conRicevuta = 0;
  let senzaRicevuta = 0;

  // Servizi (foglio "servizi"): somma importi (importo * qty) per gli ospiti del periodo
  const servizi = Array.isArray(state.servizi) ? state.servizi : [];
  const guestIdsSet = new Set();
  for (const g of guests){
    const _gid = guestIdOf(g) ?? g?.id ?? g?.ID ?? g?.Id ?? "";
    if (_gid !== null && _gid !== undefined && String(_gid).trim() !== "") guestIdsSet.add(String(_gid));

    const pren = money(g?.importo_prenotazione ?? g?.importo_prenota ?? g?.importoPrenotazione ?? g?.importoPrenota ?? 0);
    fatturato += pren;
    if (pren > 0 && _guestHasFiscalReceipt(g)) {
      totalePrenotazioniConRicevuta += pren;
    }

    const dep = money(g?.acconto_importo ?? g?.accontoImporto ?? 0);
    const saldo = money(g?.saldo_pagato ?? g?.saldoPagato ?? g?.saldo ?? 0);

    giacenza += (dep + saldo);

    // receipt flags
    const depRec = _isRicevutaFlag(g, 'acconto');
    const saldoRec = _isRicevutaFlag(g, 'saldo');

    if (dep > 0){
      if (depRec) conRicevuta += dep;
      else senzaRicevuta += dep;
    }
    if (saldo > 0){
      if (saldoRec) conRicevuta += saldo;
      else senzaRicevuta += saldo;
    }
  }

  // Aggiungi servizi al fatturato totale
  try{
    let sumServizi = 0;
    for (const s of (servizi || [])){
      if (!s) continue;
      const del = s.isDeleted ?? s.is_deleted ?? s.deleted;
      if (String(del) === "1" || del === true) continue;
      const ospiteId = s.ospite_id ?? s.ospiteId ?? s.guest_id ?? s.guestId ?? "";
      if (ospiteId === null || ospiteId === undefined || String(ospiteId).trim() === "") continue;
      if (!guestIdsSet.has(String(ospiteId))) continue;

      const qtyRaw = money(s.qty ?? 1);
      const qty = (isFinite(qtyRaw) && qtyRaw > 0) ? qtyRaw : 1;
      const amt = money(s.importo ?? s.amount ?? 0);
      if (!isFinite(amt) || amt === 0) continue;

      sumServizi += (qty * amt);
    }
    if (isFinite(sumServizi) && sumServizi !== 0){
      fatturato += sumServizi;
    }
  }catch(_){ }

  let speseTot = 0;

  try{
    const items = __getStatsSpese();
    let sum = 0;
    for (const it of items){
      if (__isFuoriBudgetSpesa__(it)) continue;
      sum += money(it?.importoLordo ?? it?.lordo ?? 0);
    }
    speseTot = sum;
  }catch(_){
    speseTot = 0;
  }

  // dDAE_1.020+ — Giacenza in cassa = (con ricevuta + senza ricevuta) - spese totali
  try{
    giacenza = (money(conRicevuta) + money(senzaRicevuta)) - money(speseTot);
  }catch(_){ }

  let ivaSpese = 0;
  if (!isFinite(ivaSpese) || ivaSpese === 0){
    ivaSpese = money(report?.totals?.ivaDetraibile ?? 0);
  }

  if (!isFinite(ivaSpese) || ivaSpese === 0){
    try{
      const items = __getStatsSpese();
      let sum = 0;
      for (const s of items){
        // Se c'e' gia' un campo iva, usa quello
        const ivaField = money(s?.iva ?? s?.IVA ?? 0);
        if (ivaField > 0){
          sum += ivaField;
          continue;
        }

        const lordo = money(s?.importoLordo ?? s?.lordo ?? 0);
        if (!isFinite(lordo) || lordo <= 0) continue;

        const catRaw = (s?.categoria ?? s?.cat ?? "").toString().trim().toLowerCase();
        let rate = 0;
        if (catRaw.includes("iva")) {
          if (catRaw.includes("22")) rate = 22;
          else if (catRaw.includes("10")) rate = 10;
          else if (catRaw.includes("4")) rate = 4;
        } else {
          const n = parseFloat(String(s?.aliquotaIva ?? s?.aliquota_iva ?? "").replace(",", "."));
          if (!isNaN(n)) {
            if (n >= 21.5) rate = 22;
            else if (n >= 9.5 && n < 11.5) rate = 10;
            else if (n >= 3.5 && n < 5.5) rate = 4;
          }
        }

        if (rate > 0){
          const imponibile = lordo / (1 + rate/100);
          const iva = lordo - imponibile;
          if (isFinite(iva)) sum += iva;
        }
      }
      if (sum > 0) ivaSpese = sum;
    }catch(_){ }
  }

  const ivaPrenotazioni = totalePrenotazioniConRicevuta * 0.10;
  const ivaDaVersareSocieta = ivaPrenotazioni - (money(ivaSpese) || 0);
  const ivaDaVersare = fiscalMode === "societa" ? ivaDaVersareSocieta : 0;
  const guadagno = fatturato - speseTot - (fiscalMode === "societa" ? ivaDaVersareSocieta : 0);

  return {
    fiscalMode,
    fatturatoTotale: fatturato,
    speseTotali: speseTot,
    senzaRicevuta,
    conRicevuta,
    totalePrenotazioniConRicevuta,
    ivaDaVersare,
    guadagnoTotale: guadagno,
    giacenzaCassa: giacenza,
  };
}

function __statGenRegistrationsByMonth__(sourceRows){
  const guests = Array.isArray(sourceRows) ? sourceRows : (Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []));
  const monthlyRevenue = new Array(12).fill(0);
  const yearlyCounts = Object.create(null);
  const now = new Date();
  const currentYear = Number.isFinite(now.getFullYear()) ? now.getFullYear() : null;
  const currentMonthIndex = Number.isFinite(now.getMonth()) ? now.getMonth() : 11;

  const money = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === 'number') return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    if (s.includes(',') && s.includes('.')) s = s.replace(/\./g, '').replace(',', '.');
    else if (s.includes(',')) s = s.replace(',', '.');
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };

  const pickIso = (row) => {
    const candidates = [
      row?.data_prenotazione, row?.dataPrenotazione,
      row?.booking_date, row?.bookingDate,
      row?.createdAt, row?.created_at,
      row?.updatedAt, row?.updated_at,
      row?.data, row?.date,
      row?.check_in, row?.checkIn,
      row?.arrivo, row?.dataArrivo,
      row?.check_out, row?.checkOut,
      row?.partenza, row?.dataPartenza
    ];
    for (const raw of candidates){
      let iso = '';
      try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(raw || '') : ''; }catch(_){ iso = ''; }
      if (iso && /^\d{4}-\d{2}-\d{2}$/.test(iso)) return iso;
      try{
        const d = new Date(raw || '');
        if (!Number.isNaN(d.getTime())) return d.toISOString().slice(0,10);
      }catch(_){ }
    }
    return '';
  };

  guests.forEach((g)=>{
    if (!g) return;
    const iso = pickIso(g);
    if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return;
    const yy = parseInt(iso.slice(0,4), 10);
    if (Number.isFinite(yy)) yearlyCounts[yy] = (Number(yearlyCounts[yy] || 0) || 0) + 1;
    const mm = parseInt(iso.slice(5,7), 10);
    if (!Number.isFinite(mm) || mm < 1 || mm > 12) return;
    const pren = money(g?.importo_prenotazione ?? g?.importo_prenota ?? g?.importoPrenotazione ?? g?.importoPrenota ?? 0);
    if (!isFinite(pren) || pren <= 0) return;
    monthlyRevenue[mm - 1] += pren;
  });

  for (let i = 0; i < monthlyRevenue.length; i += 1){
    monthlyRevenue[i] = Math.round((Number(monthlyRevenue[i] || 0) || 0) * 100) / 100;
  }

  let sourceYear = null;
  Object.keys(yearlyCounts).forEach((key)=>{
    const year = parseInt(key, 10);
    if (!Number.isFinite(year)) return;
    if (!Number.isFinite(sourceYear) || (Number(yearlyCounts[key] || 0) > Number(yearlyCounts[sourceYear] || 0)) || (Number(yearlyCounts[key] || 0) === Number(yearlyCounts[sourceYear] || 0) && year > sourceYear)){
      sourceYear = year;
    }
  });

  const cumulativeRevenue = monthlyRevenue.map((_, idx)=>{
    if (currentYear !== null && sourceYear === currentYear && idx > currentMonthIndex) return 0;
    const partial = monthlyRevenue.slice(0, idx + 1).reduce((sum, value)=> sum + (Number(value || 0) || 0), 0);
    return Math.round(partial * 100) / 100;
  });
  const totalRevenue = cumulativeRevenue.reduce((max, value)=> Math.max(max, Number(value || 0) || 0), 0);
  return { monthlyRevenue, cumulativeRevenue, totalRevenue, sourceYear };
}

const __STAT_SHARED_LINE_CHART_VISUAL_STORAGE_KEY__ = 'dDAE_stat_shared_line_chart_visual_v1';
const __STAT_SHARED_LINE_CHART_LOCAL_STORAGE_PREFIX__ = 'dDAE_stat_shared_line_chart_local_v1_';
const __STATGEN_REG_CHART_VISUAL_STORAGE_KEY__ = __STAT_SHARED_LINE_CHART_VISUAL_STORAGE_KEY__;

function __statSharedLineChartDefaultVisual__(){
  const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
  return {
    bg: (isDark ? '#0f172a' : '#ffffff'),
    border: (isDark ? '#94a3b8' : '#0f172a'),
    fg: '#c9772b',
    opacity: 0
  };
}

function __statSharedLineChartPageKeyNormalize__(pageKey){
  const safe = String(pageKey || '').trim().toLowerCase();
  if (safe === 'statspese') return 'statspese';
  if (safe === 'statmensili') return 'statmensili';
  if (safe === 'statcancellazioni') return 'statcancellazioni';
  if (safe === 'statricevute' || safe === 'statprenotazioni') return 'statricevute';
  if (safe === 'statchannel') return 'statchannel';
  if (safe === 'statpulizie') return 'statpulizie';
  if (safe === 'statamministratore') return 'statamministratore';
  return 'statgen';
}

function __statSharedLineChartLocalStorageKey__(pageKey){
  return `${__STAT_SHARED_LINE_CHART_LOCAL_STORAGE_PREFIX__}${__statSharedLineChartPageKeyNormalize__(pageKey)}`;
}

function __statSharedLineChartNormalizeVisual__(visual, fallback){
  const base = fallback || __statSharedLineChartDefaultVisual__();
  return {
    bg: __normalizeOperatoreColor__(visual?.bg || base.bg || '#ffffff'),
    border: __normalizeOperatoreColor__(visual?.border || base.border || visual?.bg || base.bg || '#0f172a'),
    fg: __normalizeOptionalOperatoreColor__(visual?.fg || base.fg || '#c9772b') || (base.fg || '#c9772b'),
    opacity: __designBgOpacityNormalize__(visual?.opacity ?? base.opacity ?? 0)
  };
}

function __statSharedLineChartVisualRead__(){
  try{
    const raw = localStorage.getItem(__STAT_SHARED_LINE_CHART_VISUAL_STORAGE_KEY__) || localStorage.getItem('dDAE_statgen_reg_chart_visual_v1');
    const parsed = raw ? JSON.parse(raw) : {};
    return __statSharedLineChartNormalizeVisual__(parsed, __statSharedLineChartDefaultVisual__());
  }catch(_){
    return __statSharedLineChartDefaultVisual__();
  }
}

function __statSharedLineChartVisualReadFor__(pageKey){
  const shared = __statSharedLineChartVisualRead__();
  try{
    const raw = localStorage.getItem(__statSharedLineChartLocalStorageKey__(pageKey));
    if (!raw) return shared;
    const parsed = JSON.parse(raw);
    return __statSharedLineChartNormalizeVisual__(parsed, shared);
  }catch(_){
    return shared;
  }
}

function __statSharedLineChartVisualWrite__(visual){
  try{
    const next = __statSharedLineChartNormalizeVisual__(visual, __statSharedLineChartVisualRead__());
    localStorage.setItem(__STAT_SHARED_LINE_CHART_VISUAL_STORAGE_KEY__, JSON.stringify(next));
    return next;
  }catch(_){ return __statSharedLineChartVisualRead__(); }
}

function __statSharedLineChartVisualWriteFor__(pageKey, visual){
  try{
    const next = __statSharedLineChartNormalizeVisual__(visual, __statSharedLineChartVisualReadFor__(pageKey));
    localStorage.setItem(__statSharedLineChartLocalStorageKey__(pageKey), JSON.stringify(next));
    return next;
  }catch(_){ return __statSharedLineChartVisualReadFor__(pageKey); }
}

function __statSharedLineChartVisualResetFor__(pageKey){
  try{ localStorage.removeItem(__statSharedLineChartLocalStorageKey__(pageKey)); }catch(_){ }
}

function __statSharedLineChartVisualResetAllLocals__(){
  ['statgen','statspese','statmensili','statcancellazioni','statricevute','statchannel','statpulizie','statamministratore'].forEach((pageKey)=>{ try{ __statSharedLineChartVisualResetFor__(pageKey); }catch(_){ } });
}

function __statGenRegChartVisualRead__(){
  return __statSharedLineChartVisualRead__();
}

function __statGenRegChartVisualWrite__(visual){
  return __statSharedLineChartVisualWrite__(visual);
}

function __statLineChartWrapSelector__(key){
  const safe = String(key || '').trim().toLowerCase();
  if (safe === 'statspese') return '#page-statspese .statgen-line-chart-wrap';
  if (safe === 'statmensili') return '#page-statmensili .statgen-line-chart-wrap';
  if (safe === 'statcancellazioni') return '#page-statcancellazioni .statgen-line-chart-wrap';
  if (safe === 'statricevute') return '#page-statprenotazioni .statgen-line-chart-wrap';
  if (safe === 'statchannel') return '#page-statchannel .statgen-line-chart-wrap';
  if (safe === 'statpulizie') return '#page-statpulizie .statgen-line-chart-wrap';
  if (safe === 'statamministratore') return '#page-statamministratore .statgen-line-chart-wrap';
  return '#page-statgen .statgen-line-chart-wrap';
}

function __applyStatSharedLineChartWrapVisualToAll__(){
  ['statgen','statspese','statmensili','statcancellazioni','statricevute','statchannel','statpulizie','statamministratore'].forEach((pageKey)=>{
    try{ __applyStatGenRegChartWrapVisual__(document.querySelector(__statLineChartWrapSelector__(pageKey))); }catch(_){ }
  });
}

function __refreshStatSharedLineCharts__(){
  try{ drawStatGenRegistrationsLineChart('statGenRegChart'); }catch(_){ }
  try{ drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); }catch(_){ }
  try{ drawStatSpesePercentLineChart('statSpeseLineChart'); }catch(_){ }
  try{ drawStatCancellazioniPercentLineChart('statCancellazioniLineChart'); }catch(_){ }
  try{ renderStatRicevute(); }catch(_){ }
  try{ renderStatChannel(); }catch(_){ }
  try{ renderStatPulizie(Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []); }catch(_){ }
  try{ renderStatAmministratore(); }catch(_){ }
}

function __applyStatSharedLineChartChangesToCategory__(payload, changed, sourcePageKey){
  try{
    const safeSource = __statSharedLineChartPageKeyNormalize__(sourcePageKey || 'statgen');
    const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    const pageKeys = ['statgen','statspese','statmensili','statcancellazioni','statricevute','statchannel','statpulizie','statamministratore'];
    const snapshots = {};

    pageKeys.forEach((pageKey) => {
      try{ snapshots[pageKey] = __statSharedLineChartVisualReadFor__(pageKey); }catch(_){ }
    });

    const mergeChanged = (baseVisual) => {
      const current = __statSharedLineChartNormalizeVisual__(baseVisual || {}, __statSharedLineChartDefaultVisual__());
      return {
        bg: changed?.bg
          ? (Object.prototype.hasOwnProperty.call(colors, 'bg') ? colors.bg : current.bg || '#ffffff')
          : current.bg,
        border: changed?.border
          ? (Object.prototype.hasOwnProperty.call(colors, 'border') ? colors.border : current.border || '#0f172a')
          : current.border,
        fg: changed?.fg
          ? (Object.prototype.hasOwnProperty.call(colors, 'fg') ? colors.fg : current.fg || '#c9772b')
          : current.fg,
        opacity: changed?.opacity
          ? ((payload && Object.prototype.hasOwnProperty.call(payload, 'opacity'))
              ? __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0)
              : __designBgOpacityNormalize__(current.opacity ?? 0))
          : __designBgOpacityNormalize__(current.opacity ?? 0)
      };
    };

    pageKeys.forEach((pageKey) => {
      try{
        if (pageKey === safeSource) return;
        const targetNext = mergeChanged(snapshots[pageKey]);
        __statSharedLineChartVisualWriteFor__(pageKey, targetNext);
      }catch(_){ }
    });

    __applyStatSharedLineChartWrapVisualToAll__();
    __refreshStatSharedLineCharts__();
  }catch(_){ }
}

function __statSharedLineChartResolvedSurface__(visual, isDark){
  try{
    const bgHex = __graphColorValueToHex__(visual?.bg || (isDark ? '#0f172a' : '#ffffff'), isDark ? '#0f172a' : '#ffffff');
    const opacity = __designBgOpacityNormalize__(visual?.opacity ?? 0);
    if (!isDark) return { bg: hexToRgba(bgHex, opacity), border: 'transparent', bgHex, opacity };
    const lum = __colorLuminance__(bgHex);
    const useForcedDarkSurface = opacity < 0.18 || lum > 0.58;
    const resolvedBg = useForcedDarkSurface ? 'rgba(12,23,46,0.96)' : hexToRgba(bgHex, Math.max(0.20, opacity));
    const resolvedBorder = useForcedDarkSurface ? 'rgba(148,163,184,0.22)' : hexToRgba(__graphColorValueToHex__(visual?.border || bgHex, bgHex), 0.38);
    return { bg: resolvedBg, border: resolvedBorder, bgHex, opacity, useForcedDarkSurface };
  }catch(_){
    return { bg: isDark ? 'rgba(12,23,46,0.96)' : 'transparent', border: isDark ? 'rgba(148,163,184,0.22)' : 'transparent', bgHex: isDark ? '#0f172a' : '#ffffff', opacity: 0, useForcedDarkSurface: !!isDark };
  }
}

function __statSharedLineChartResolvedStrokeHex__(visual, isDark){
  const preferred = __graphColorValueToHex__(visual?.fg || visual?.border || (isDark ? '#8fcbe8' : '#c9772b'), isDark ? '#8fcbe8' : '#c9772b');
  if (!isDark) return preferred;
  return __darkModeReadableTextHex__(preferred, '#8fcbe8');
}

function __statSharedLineChartResolvedTextHex__(visual, isDark){
  const preferred = __graphColorValueToHex__(visual?.border || visual?.fg || (isDark ? '#e2e8f0' : '#0f172a'), isDark ? '#e2e8f0' : '#0f172a');
  if (!isDark) return preferred;
  return __darkModeReadableTextHex__(preferred, '#f8fbff');
}

function __applyStatGenRegChartWrapVisual__(wrap, visual){
  const el = wrap || document.querySelector('#page-statgen .statgen-line-chart-wrap');
  if (!el) return;
  const pageKey = __statSharedLineChartPageKeyNormalize__(el?.dataset?.statLineChartKey || 'statgen');
  const current = visual || __statSharedLineChartVisualReadFor__(pageKey);
  const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
  const surface = __statSharedLineChartResolvedSurface__(current, isDark);
  try{ el.style.setProperty('--statgen-chart-bg', surface.bg); }catch(_){ }
  try{ el.style.setProperty('background', surface.bg, 'important'); }catch(_){ }
  try{ el.style.setProperty('background-color', surface.bg, 'important'); }catch(_){ }
  try{ el.style.setProperty('border', `1px solid ${surface.border || 'transparent'}`, 'important'); }catch(_){ }
  try{ el.style.setProperty('border-color', surface.border || 'transparent', 'important'); }catch(_){ }
  try{ el.style.setProperty('box-shadow', 'none', 'important'); }catch(_){ }
}

function __openStatSharedLineChartColorPicker__(wrap){
  const pageKey = __statSharedLineChartPageKeyNormalize__(wrap?.dataset?.statLineChartKey || 'statgen');
  const initial = __statSharedLineChartVisualReadFor__(pageKey);
  const applyVisual = (payload) => {
    const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
    const next = __statSharedLineChartVisualWriteFor__(pageKey, {
      bg: (Object.prototype.hasOwnProperty.call(colors, 'bg') ? colors.bg : initial.bg),
      border: (Object.prototype.hasOwnProperty.call(colors, 'border') ? colors.border : initial.border),
      fg: (Object.prototype.hasOwnProperty.call(colors, 'fg') ? colors.fg : initial.fg),
      opacity: (payload && Object.prototype.hasOwnProperty.call(payload, 'opacity'))
        ? __designBgOpacityNormalize__(payload?.opacity ?? initial.opacity ?? 0)
        : __designBgOpacityNormalize__(initial.opacity ?? 0)
    });
    __applyStatSharedLineChartWrapVisualToAll__();
    __refreshStatSharedLineCharts__();
    return next;
  };
  const revertVisual = () => {
    __statSharedLineChartVisualWriteFor__(pageKey, initial);
    __applyStatSharedLineChartWrapVisualToAll__();
    __refreshStatSharedLineCharts__();
  };
  __tagColorPopupOpen__('stat-shared-line-chart', initial, (payload) => {
    applyVisual(payload);
    try{ toast('Design grafico statistiche aggiornato'); }catch(_){ }
  }, {
    supportsBg:true,
    supportsBorder:true,
    supportsFg:true,
    supportsOpacity:true,
    opacity:initial.opacity ?? 0,
    defaultMode:'bg',
    fallbackBg:(initial.bg || '#ffffff'),
    onPreview:applyVisual,
    onRevert:revertVisual,
    applyCategory:{
      message:'Applicare il design a tutti i grafici statistiche della stessa tipologia?',
      apply: async(payload, changed) => { __applyStatSharedLineChartChangesToCategory__(payload, changed, pageKey); }
    }
  });
}

function __openStatGenRegChartColorPicker__(wrap){
  return __openStatSharedLineChartColorPicker__(wrap);
}

function __bindStatSharedLineChartLongPress__(wrap){
  const el = wrap;
  if (!el || el.dataset.statgenChartLongPressBound === '1') return;
  el.dataset.statgenChartLongPressBound = '1';
  try{ if (!el.dataset.statLineChartKey){
    el.dataset.statLineChartKey = el.closest('#page-statspese') ? 'statspese' : (el.closest('#page-statmensili') ? 'statmensili' : (el.closest('#page-statcancellazioni') ? 'statcancellazioni' : 'statgen'));
  } }catch(_){ }
  let timer = null;
  let fired = false;
  const clear = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
  const block = (e)=>{
    try{ if (e?.preventDefault) e.preventDefault(); }catch(_){ }
    try{ if (e?.stopPropagation) e.stopPropagation(); }catch(_){ }
    return false;
  };
  const start = (e)=>{
    try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    fired = false;
    clear();
    timer = setTimeout(()=>{
      fired = true;
      try{ el.classList.add('is-pressing'); }catch(_){ }
      __openStatSharedLineChartColorPicker__(el);
    }, 500);
  };
  const stop = (e)=>{
    clear();
    if (fired){
      block(e);
      setTimeout(()=>{ fired = false; try{ el.classList.remove('is-pressing'); }catch(_){ } }, 0);
      return;
    }
    try{ el.classList.remove('is-pressing'); }catch(_){ }
  };
  const swallow = (e)=>{ if (!fired) return; block(e); fired = false; };
  ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ el.addEventListener(evt, start, { passive:true }); }catch(_){ } });
  ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ el.addEventListener(evt, stop, { passive:false }); }catch(_){ } });
  try{ el.addEventListener('click', swallow, true); }catch(_){ }
  try{ el.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
}

function __bindStatGenRegChartLongPress__(wrap){
  return __bindStatSharedLineChartLongPress__(wrap || document.querySelector('#page-statgen .statgen-line-chart-wrap'));
}

function __statLineChartCompactEuro__(value){
  const n = Number(value || 0) || 0;
  const abs = Math.abs(n);
  if (abs >= 1000000) return `${(n / 1000000).toFixed(abs >= 10000000 ? 0 : 1).replace(/\.0$/, '')}M`;
  if (abs >= 1000) return `${(n / 1000).toFixed(abs >= 10000 ? 0 : 1).replace(/\.0$/, '')}k`;
  return `${Math.round(n)}`;
}

function __statLineChartNiceMax__(value){
  const v = Math.max(0, Number(value || 0) || 0);
  if (v <= 0) return 100;
  const exponent = Math.floor(Math.log10(v));
  const fraction = v / Math.pow(10, exponent);
  let niceFraction = 1;
  if (fraction <= 1) niceFraction = 1;
  else if (fraction <= 2) niceFraction = 2;
  else if (fraction <= 5) niceFraction = 5;
  else niceFraction = 10;
  return niceFraction * Math.pow(10, exponent);
}


const __STAT_LANDSCAPE_GRAPH_ONLY_PAGES__ = new Set(['statgen','statmensili','statspese','statcancellazioni','statprenotazioni','statchannel','statpulizie']);
let __statLandscapeGraphOnlyTimer__ = null;
let __statLandscapeGraphOnlyResizeBound__ = false;

function __isLandscapeOrientation__(){
  try{ return !!(window.matchMedia && window.matchMedia('(orientation: landscape)').matches); }catch(_){ return false; }
}

function __applyStatLandscapeGraphOnlyLayout__(){
  const currentPage = String((state && state.page) || document.body?.dataset?.page || '');
  const viewportHeight = Math.max(window.innerHeight || 0, document.documentElement?.clientHeight || 0, 0);
  __STAT_LANDSCAPE_GRAPH_ONLY_PAGES__.forEach((pageKey) => {
    const wrap = document.querySelector(`#page-${pageKey} .statgen-line-chart-wrap`);
    if (!wrap) return;
    if (!(currentPage === pageKey && __isLandscapeOrientation__())){
      try{ wrap.style.removeProperty('--stat-landscape-chart-height'); }catch(_){ }
      return;
    }
    const compactByViewport = Math.round(viewportHeight * 0.20);
    const nextHeight = Math.max(76, Math.min(104, compactByViewport || 88));
    try{ wrap.style.setProperty('--stat-landscape-chart-height', `${nextHeight}px`); }catch(_){ }
  });
}

function __refreshCurrentStatLandscapeGraphOnlyPage__(){
  const currentPage = String((state && state.page) || document.body?.dataset?.page || '');
  try{ __applyStatLandscapeGraphOnlyLayout__(); }catch(_){ }
  if (currentPage === 'statgen'){
    try{ drawStatGenRegistrationsLineChart('statGenRegChart'); }catch(_){ }
  }else if (currentPage === 'statmensili'){
    try{ drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); }catch(_){ }
  }else if (currentPage === 'statspese'){
    try{ drawStatSpesePercentLineChart('statSpeseLineChart'); }catch(_){ }
  }else if (currentPage === 'statcancellazioni'){
    try{ drawStatCancellazioniPercentLineChart('statCancellazioniLineChart'); }catch(_){ }
  }else if (currentPage === 'statprenotazioni'){
    try{ renderStatRicevute(); }catch(_){ }
  }else if (currentPage === 'statchannel'){
    try{ renderStatChannel(); }catch(_){ }
  }else if (currentPage === 'statpulizie'){
    try{ renderStatPulizie(Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []); }catch(_){ }
  }
}

function __scheduleStatLandscapeGraphOnlyRefresh__(){
  try{ if (__statLandscapeGraphOnlyTimer__) clearTimeout(__statLandscapeGraphOnlyTimer__); }catch(_){ }
  __statLandscapeGraphOnlyTimer__ = setTimeout(() => {
    __statLandscapeGraphOnlyTimer__ = null;
    __refreshCurrentStatLandscapeGraphOnlyPage__();
  }, 40);
}

function __ensureStatLandscapeGraphOnlyResizeBinding__(){
  if (__statLandscapeGraphOnlyResizeBound__) return;
  __statLandscapeGraphOnlyResizeBound__ = true;
  const refresh = () => { try{ __scheduleStatLandscapeGraphOnlyRefresh__(); }catch(_){ } };
  try{ window.addEventListener('resize', refresh, { passive:true }); }catch(_){ }
  try{ window.addEventListener('orientationchange', refresh, { passive:true }); }catch(_){ }
  try{
    if (window.visualViewport){
      window.visualViewport.addEventListener('resize', refresh, { passive:true });
    }
  }catch(_){ }
}

__ensureStatLandscapeGraphOnlyResizeBinding__();

function __drawSharedMonthlyLineChart__(canvasId, values, options){
  const canvas = document.getElementById(canvasId);
  if (!canvas) return;
  const parent = canvas.parentElement || canvas;
  const wrap = (canvas.closest && canvas.closest('.statgen-line-chart-wrap')) ? canvas.closest('.statgen-line-chart-wrap') : parent;
  const pageKey = __statSharedLineChartPageKeyNormalize__(wrap?.dataset?.statLineChartKey || 'statgen');
  try{ __applyStatLandscapeGraphOnlyLayout__(); }catch(_){ }
  const parentRect = parent.getBoundingClientRect ? parent.getBoundingClientRect() : { width: parent.clientWidth || 0, height: parent.clientHeight || 0 };
  const width = Math.max(280, Math.round(parentRect.width || parent.clientWidth || 0));
  const minChartHeight = (__isLandscapeOrientation__() && __STAT_LANDSCAPE_GRAPH_ONLY_PAGES__.has(pageKey)) ? 72 : 118;
  const height = Math.max(minChartHeight, Math.round(parentRect.height || parent.clientHeight || canvas.clientHeight || minChartHeight));
  const dpr = Math.max(1, Math.min(3, window.devicePixelRatio || 1));
  canvas.width = Math.round(width * dpr);
  canvas.height = Math.round(height * dpr);
  canvas.style.width = `${width}px`;
  canvas.style.height = `${height}px`;

  const ctx = canvas.getContext('2d');
  if (!ctx) return;
  ctx.setTransform(1, 0, 0, 1, 0, 0);
  ctx.scale(dpr, dpr);
  ctx.clearRect(0, 0, width, height);

  const visual = __statSharedLineChartVisualReadFor__(pageKey);
  const isDark = !!document.body?.classList?.contains('ddae-dark');
  const surface = __statSharedLineChartResolvedSurface__(visual, isDark);
  const textHex = __statSharedLineChartResolvedTextHex__(visual, isDark);
  const defaultLineColor = __statSharedLineChartResolvedStrokeHex__(visual, isDark);
  const opts = options || {};
  const requestedLineColor = String(opts.lineColor || '').trim();
  const lineColor = requestedLineColor || defaultLineColor;
  const compareLineColor = String((options && options.compareLineColor) || (isDark ? '#7dd3fc' : '#2563eb'));
  const gridBaseHex = __graphColorValueToHex__(visual.border || textHex || (isDark ? '#94a3b8' : '#0f172a'), textHex || (isDark ? '#94a3b8' : '#0f172a'));
  const gridColor = hexToRgba(gridBaseHex, isDark ? 0.22 : 0.18);
  const axisColor = hexToRgba(gridBaseHex, isDark ? 0.36 : 0.28);
  const textColor = hexToRgba(textHex, isDark ? 0.92 : 0.66);
  const pointFill = isDark ? '#0c172e' : '#ffffff';
  const comparePointFill = isDark ? '#081226' : '#ffffff';
  const textUiSettings = (typeof getAppTextUiSettings === 'function') ? getAppTextUiSettings() : { bold:false, size:'1' };
  const isBoldTrend = !!textUiSettings?.bold;
  const trendLineWidth = isBoldTrend ? (2 * 1.70) : 2;

  const seriesListRaw = Array.isArray(opts.seriesList) ? opts.seriesList.filter(Boolean) : [];
  const vals = new Array(12).fill(0).map((_, i)=> Math.max(0, Number((values || [])[i] || 0) || 0));
  const compareVals = new Array(12).fill(0).map((_, i)=> Math.max(0, Number((opts.compareValues || [])[i] || 0) || 0));
  const seriesList = seriesListRaw.length
    ? seriesListRaw.map((item, idx) => ({
        key: String(item?.key || `series-${idx+1}`),
        label: String(item?.label || item?.key || `Serie ${idx+1}`),
        values: new Array(12).fill(0).map((_, i)=> Math.max(0, Number((item?.values || [])[i] || 0) || 0)),
        color: String(item?.color || lineColor),
        dash: Array.isArray(item?.dash) ? item.dash : [],
        pointFill: String(item?.pointFill || pointFill),
        lineWidth: Number(item?.lineWidth || 0) || trendLineWidth,
        radius: Number(item?.radius || 0) || 3,
        pointLineWidth: Number(item?.pointLineWidth || 0) || 2,
        visible: item?.visible !== false
      })).filter((item) => item.visible !== false)
    : [{
        key: 'primary',
        label: 'Serie principale',
        values: vals,
        color: lineColor,
        dash: [],
        pointFill,
        lineWidth: trendLineWidth,
        radius: 3,
        pointLineWidth: 2,
        visible: true
      }].concat(compareVals.some((value) => value > 0) ? [{
        key: 'compare',
        label: 'Confronto',
        values: compareVals,
        color: compareLineColor,
        dash: [5, 4],
        pointFill: comparePointFill,
        lineWidth: Math.max(1.5, trendLineWidth - 0.2),
        radius: 2.5,
        pointLineWidth: 1.5,
        visible: true
      }] : []);

  const activeSeries = seriesList.length ? seriesList : [{ key:'primary', label:'Serie principale', values:new Array(12).fill(0), color:lineColor, dash:[], pointFill, lineWidth:trendLineWidth, radius:3, pointLineWidth:2, visible:true }];
  const maxValue = activeSeries.reduce((best, item) => {
    return Math.max(best, ...(item.values || []).map((value) => Number(value || 0) || 0));
  }, 0);
  const yMax = Math.max(1, Number(opts.yMax || 0) || __statLineChartNiceMax__(maxValue));
  const yTickFormatter = (typeof opts.yTickFormatter === 'function') ? opts.yTickFormatter : ((value)=> String(Math.round(Number(value || 0) || 0)));
  const bubbleFormatter = (typeof opts.bubbleFormatter === 'function') ? opts.bubbleFormatter : ((value)=> String(Math.round(Number(value || 0) || 0)));
  const pad = { top: 8, right: 10, bottom: 20, left: 24 };
  const chartW = Math.max(10, width - pad.left - pad.right);
  const chartH = Math.max(10, height - pad.top - pad.bottom);
  const baseY = pad.top + chartH;
  const gridSteps = 4;

  ctx.lineWidth = 1;
  ctx.strokeStyle = gridColor;
  ctx.fillStyle = textColor;
  ctx.font = '700 9px system-ui';
  ctx.textAlign = 'left';
  ctx.textBaseline = 'middle';
  for (let i = 0; i <= gridSteps; i += 1){
    const y = pad.top + (chartH / gridSteps) * i;
    ctx.beginPath();
    ctx.moveTo(pad.left, y);
    ctx.lineTo(pad.left + chartW, y);
    ctx.stroke();
    const tickValue = yMax - ((yMax / gridSteps) * i);
    ctx.fillText(yTickFormatter(tickValue), 1, y);
  }

  for (let i = 0; i < 12; i += 1){
    const x = pad.left + (chartW / 11) * i;
    ctx.beginPath();
    ctx.moveTo(x, pad.top);
    ctx.lineTo(x, baseY);
    ctx.stroke();
  }

  ctx.strokeStyle = axisColor;
  ctx.beginPath();
  ctx.moveTo(pad.left, baseY);
  ctx.lineTo(pad.left + chartW, baseY);
  ctx.stroke();

  const makePoints = (series) => series.map((value, idx)=>{
    const x = pad.left + (chartW / 11) * idx;
    const y = baseY - ((value / yMax) * chartH);
    return { x, y, value, idx };
  });

  const drawSeries = (seriesPoints, cfg = {}) => {
    if (!Array.isArray(seriesPoints) || !seriesPoints.length) return;
    ctx.save();
    ctx.beginPath();
    seriesPoints.forEach((pt, idx)=>{
      if (idx === 0) ctx.moveTo(pt.x, pt.y);
      else ctx.lineTo(pt.x, pt.y);
    });
    ctx.strokeStyle = cfg.color || lineColor;
    ctx.lineWidth = cfg.lineWidth || trendLineWidth;
    ctx.setLineDash(Array.isArray(cfg.dash) ? cfg.dash : []);
    ctx.stroke();
    ctx.setLineDash([]);
    seriesPoints.forEach((pt)=>{
      ctx.beginPath();
      ctx.arc(pt.x, pt.y, cfg.radius || 3, 0, Math.PI * 2);
      ctx.fillStyle = cfg.pointFill || pointFill;
      ctx.fill();
      ctx.lineWidth = cfg.pointLineWidth || 2;
      ctx.strokeStyle = cfg.color || lineColor;
      ctx.stroke();
    });
    ctx.restore();
  };

  const renderedSeries = activeSeries.map((item) => ({ ...item, points: makePoints(item.values || []) }));
  renderedSeries.forEach((item) => {
    drawSeries(item.points, {
      color: item.color || lineColor,
      dash: item.dash || [],
      lineWidth: item.lineWidth || trendLineWidth,
      pointFill: item.pointFill || pointFill,
      radius: item.radius || 3,
      pointLineWidth: item.pointLineWidth || 2
    });
  });

  ctx.fillStyle = textColor;
  ctx.font = '800 10px system-ui';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'alphabetic';
  const labels = (__MONTHS_IT || []).map((m)=> String(m || '').slice(0, 1).toUpperCase());
  const labelPoints = renderedSeries[0]?.points || makePoints(new Array(12).fill(0));
  labelPoints.forEach((pt, idx)=>{
    ctx.fillText(labels[idx] || String(idx + 1), pt.x, height - 5);
  });

  let peak = null;
  renderedSeries.forEach((item) => {
    (item.points || []).forEach((pt) => {
      if (!peak || pt.value > peak.value) peak = { ...pt, color: item.color || lineColor };
    });
  });
  if (peak && peak.value > 0){
    const bubbleText = bubbleFormatter(peak.value);
    const bubbleW = Math.max(38, Math.min(64, 18 + (String(bubbleText || '').length * 7)));
    const bubbleH = 15;
    const bx = Math.max(pad.left, Math.min(peak.x - bubbleW / 2, pad.left + chartW - bubbleW));
    const by = Math.max(2, peak.y - 20);
    ctx.fillStyle = isDark ? surface.bg : 'rgba(255,255,255,0.96)';
    ctx.strokeStyle = axisColor;
    ctx.lineWidth = 1;
    ctx.beginPath();
    const rr = 8;
    ctx.moveTo(bx + rr, by);
    ctx.arcTo(bx + bubbleW, by, bx + bubbleW, by + bubbleH, rr);
    ctx.arcTo(bx + bubbleW, by + bubbleH, bx, by + bubbleH, rr);
    ctx.arcTo(bx, by + bubbleH, bx, by, rr);
    ctx.arcTo(bx, by, bx + bubbleW, by, rr);
    ctx.closePath();
    ctx.fill();
    ctx.stroke();
    ctx.fillStyle = textColor;
    ctx.font = '800 10px system-ui';
    ctx.textAlign = 'center';
    ctx.textBaseline = 'middle';
    ctx.fillText(bubbleText, bx + bubbleW / 2, by + bubbleH / 2 + 0.5);
  }
}

function drawStatGenRegistrationsLineChart(canvasId){
  const selected = __getStatChartFilter__('statgen');
  let seriesList = __statGenSeriesList__().filter((item) => __statChartSeriesIsVisible__('statgen', item.key));
  if (!seriesList.length){
    try{ __clearStatChartFilter__('statgen'); }catch(_){ }
    seriesList = __statGenSeriesList__();
  }
  if (__ensureStatGenCompareEnabled__()){
    try{
      const compareYear = __ensureStatGenCompareYear__();
      const hasValidSnapshot = String(state.statGenCompareSnapshotYear || '') === String(compareYear) && state.statGenCompareSnapshot;
      if (!hasValidSnapshot && !state.statGenCompareLoading){
        try{ __loadStatGenCompareGuests__({ force:true }); }catch(_){ }
      }
      const compareSource = hasValidSnapshot
        ? state.statGenCompareSnapshot
        : { guests: Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : [], spese: [], report: null, servizi: [] };
      const compareMap = __statGenSeriesListFromData__(compareSource || {});
      const currentByKey = {};
      seriesList.forEach((item) => { currentByKey[String(item?.key || '')] = item; });
      const compareKeys = selected ? [selected] : seriesList.map((item) => String(item?.key || '')).filter(Boolean);
      compareKeys.forEach((key) => {
        const compareValues = Array.isArray(compareMap?.[key]) ? compareMap[key] : [];
        if (!Array.isArray(compareValues) || !compareValues.length) return;
        if (!compareValues.some((value) => Math.abs(Number(value || 0) || 0) > 0.0001)) return;
        const base = currentByKey[key] || {};
        seriesList.push({
          key: String(key) + '-compare-year',
          label: String(base.label || 'Confronto') + ' ' + String(compareYear),
          values: compareValues,
          color: base.color || '#9aa3af',
          dash: [6,4],
          pointFill: '#ffffff',
          lineWidth: 1.8,
          radius: 2.5,
          pointLineWidth: 1.4
        });
      });
    }catch(_){ }
  }
  __drawSharedMonthlyLineChart__(canvasId, (seriesList[0] && seriesList[0].values) ? seriesList[0].values : new Array(12).fill(0), {
    mode: 'currency',
    seriesList,
    bubbleFormatter: (value) => __statLineChartCompactEuro__(value),
    yTickFormatter: (value) => __statLineChartCompactEuro__(value),
    pointAriaLabel: 'Serie generali'
  });
}

function __statSpeseMonthlyPercentages__(){
  const items = __getStatsSpese();
  const byMonth = new Array(12).fill(0);
  const toNumber = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === 'number') return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    if (s.includes(',') && s.includes('.')) s = s.replace(/\./g, '').replace(',', '.');
    else if (s.includes(',')) s = s.replace(',', '.');
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };
  items.forEach((row)=>{
    const raw = row?.dataSpesa || row?.data || row?.data_spesa || '';
    let iso = '';
    try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(raw) : ''; }catch(_){ iso = ''; }
    if (!iso || !/^\d{4}-\d{2}-\d{2}$/.test(iso)) return;
    const month = parseInt(iso.slice(5,7), 10);
    if (!Number.isFinite(month) || month < 1 || month > 12) return;
    byMonth[month - 1] += Math.max(0, toNumber(row?.importoLordo || row?.importo_lordo || row?.importo));
  });
  const total = byMonth.reduce((a, b)=> a + (Number(b || 0) || 0), 0);
  const pctByMonth = byMonth.map((value)=> total > 0 ? ((value / total) * 100) : 0);
  return { byMonth, pctByMonth, total };
}

function drawStatSpesePercentLineChart(canvasId){
  const data = __statSpeseMonthlyBreakdown__();
  let seriesList = [
    { key:'totale-spese', label:'Totale spese', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.totale)), color: __statChartLineColorFromRenderedCard__('statspese', 'totale-spese', '#2b7cb4') },
    { key:'contanti', label:'Contanti', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.contanti)), color: __statChartLineColorFromRenderedCard__('statspese', 'contanti', '#2563eb') },
    { key:'tassa-soggiorno', label:'Tassa soggiorno', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.tassa)), color: __statChartLineColorFromRenderedCard__('statspese', 'tassa-soggiorno', '#c7b198') },
    { key:'iva-22', label:'IVA 22%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.iva22)), color: __statChartLineColorFromRenderedCard__('statspese', 'iva-22', '#f29c50') },
    { key:'iva-10', label:'IVA 10%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.iva10)), color: __statChartLineColorFromRenderedCard__('statspese', 'iva-10', '#6aa0b3') },
    { key:'iva-4', label:'IVA 4%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.iva4)), color: __statChartLineColorFromRenderedCard__('statspese', 'iva-4', '#2b7cb4') }
  ].filter((item) => __statChartSeriesIsVisible__('statspese', item.key));
  if (!seriesList.length){
    try{ __clearStatChartFilter__('statspese'); }catch(_){ }
    seriesList = [
      { key:'totale-spese', label:'Totale spese', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.totale)), color: __statChartLineColorFromRenderedCard__('statspese', 'totale-spese', '#2b7cb4') },
      { key:'contanti', label:'Contanti', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.contanti)), color: __statChartLineColorFromRenderedCard__('statspese', 'contanti', '#2563eb') },
      { key:'tassa-soggiorno', label:'Tassa soggiorno', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.tassa)), color: __statChartLineColorFromRenderedCard__('statspese', 'tassa-soggiorno', '#c7b198') },
      { key:'iva-22', label:'IVA 22%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.iva22)), color: __statChartLineColorFromRenderedCard__('statspese', 'iva-22', '#f29c50') },
      { key:'iva-10', label:'IVA 10%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.iva10)), color: __statChartLineColorFromRenderedCard__('statspese', 'iva-10', '#6aa0b3') },
      { key:'iva-4', label:'IVA 4%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(data.iva4)), color: __statChartLineColorFromRenderedCard__('statspese', 'iva-4', '#2b7cb4') }
    ];
  }
  __drawSharedMonthlyLineChart__(canvasId, (seriesList[0] && seriesList[0].values) ? seriesList[0].values : new Array(12).fill(0), {
    mode: 'currency',
    seriesList,
    bubbleFormatter: (value) => __statLineChartCompactEuro__(value),
    yTickFormatter: (value) => __statLineChartCompactEuro__(value)
  });
}


function __computeStatMensiliFromSnapshot__(snapshot){
  const backup = {
    statsGuests: Array.isArray(state.statsGuests) ? state.statsGuests.slice() : state.statsGuests,
    guests: Array.isArray(state.guests) ? state.guests.slice() : state.guests,
    servizi: Array.isArray(state.servizi) ? state.servizi.slice() : state.servizi,
    stanzeRows: Array.isArray(state.stanzeRows) ? state.stanzeRows.slice() : state.stanzeRows
  };
  try{
    state.statsGuests = Array.isArray(snapshot && snapshot.guests) ? snapshot.guests.slice() : [];
    state.guests = state.statsGuests;
    state.servizi = Array.isArray(snapshot && snapshot.servizi) ? snapshot.servizi.slice() : [];
    if (Array.isArray(snapshot && snapshot.stanzeRows)) state.stanzeRows = snapshot.stanzeRows.slice();
    return computeStatMensili();
  }catch(_){
    return { byMonth:new Array(12).fill(0), occPctByMonth:new Array(12).fill(0), occupiedRoomsByMonth:new Array(12).fill(0) };
  }finally{
    state.statsGuests = backup.statsGuests;
    state.guests = backup.guests;
    state.servizi = backup.servizi;
    state.stanzeRows = backup.stanzeRows;
  }
}

function drawStatMensiliOccupazioneLineChart(canvasId){
  const stats = (state && state.statMensili) ? state.statMensili : computeStatMensili();
  const values = Array.isArray(stats && stats.byMonth) ? stats.byMonth : new Array(12).fill(0);
  const selectedKey = __getStatChartFilter__('statmensili');
  const filtered = __statMaskMonthlyValuesByCard__(values, selectedKey);
  let selectedColor = '#2B7CB4';
  try{
    if (selectedKey) {
      selectedColor = __statChartLineColorFromRenderedCard__('statmensili', selectedKey, '#2B7CB4');
    }
  }catch(_){ selectedColor = '#2B7CB4'; }
  const seriesList = [{
    key: 'mensili',
    label: 'Mensili ' + String(state.exerciseYear || loadExerciseYear() || ''),
    values: filtered,
    color: selectedColor
  }];
  if (__ensureStatGenCompareEnabled__()){
    try{
      const compareYear = __ensureStatGenCompareYear__();
      const hasValidSnapshot = String(state.statGenCompareSnapshotYear || '') === String(compareYear) && state.statGenCompareSnapshot;
      if (!hasValidSnapshot && !state.statGenCompareLoading){
        try{ __loadStatGenCompareGuests__({ force:true }); }catch(_){ }
      }
      const compareSource = hasValidSnapshot
        ? state.statGenCompareSnapshot
        : { guests: Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : [], servizi: [], stanzeRows: Array.isArray(state.stanzeRows) ? state.stanzeRows : [] };
      const compareStats = __computeStatMensiliFromSnapshot__(compareSource || {});
      const compareValues = __statMaskMonthlyValuesByCard__(Array.isArray(compareStats && compareStats.byMonth) ? compareStats.byMonth : new Array(12).fill(0), selectedKey);
      if (compareValues.some((value) => Math.abs(Number(value || 0) || 0) > 0.0001)){
        seriesList.push({
          key: 'mensili-compare-year',
          label: 'Mensili ' + String(compareYear),
          values: compareValues,
          color: selectedColor,
          dash: [6,4],
          pointFill: '#ffffff',
          lineWidth: 1.8,
          radius: 2.5,
          pointLineWidth: 1.4
        });
      }
    }catch(_){ }
  }
  __drawSharedMonthlyLineChart__(canvasId, filtered, {
    mode: 'currency',
    seriesList,
    lineColor: selectedColor,
    bubbleFormatter: (value) => __statLineChartCompactEuro__(value),
    yTickFormatter: (value) => __statLineChartCompactEuro__(value),
    pointAriaLabel: 'Serie mensili'
  });
}
function __statCancellazioniMonthlyCounts__(){
  const year = String((state && state.year) || new Date().getFullYear());
  const delRows = Array.isArray(state.deletedGuests) ? state.deletedGuests : [];
  const cancRows = delRows.filter(r => String(r.delete_reason || '').toLowerCase() === 'cancellazione' || String(r.delete_reason || '').trim() === '');
  const cancByMonth = new Array(12).fill(0);
  const pickIso = (row) => {
    const candidates = [
      row?.deletedAt, row?.deleted_at,
      row?.updatedAt, row?.updated_at,
      row?.dataCancellazione, row?.data_cancellazione,
      row?.delete_date, row?.deleteDate,
      row?.data, row?.date
    ];
    for (const raw of candidates){
      let iso = '';
      try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(raw || '') : ''; }catch(_){ iso = ''; }
      if (iso && /^\d{4}-\d{2}-\d{2}$/.test(iso)) return iso;
    }
    return '';
  };
  cancRows.forEach((row)=>{
    const iso = pickIso(row);
    if (!iso) return;
    if (String(iso.slice(0,4)) !== year) return;
    const mm = parseInt(iso.slice(5,7), 10);
    if (!Number.isFinite(mm) || mm < 1 || mm > 12) return;
    cancByMonth[mm - 1] += 1;
  });
  return { cancByMonth };
}

function drawStatCancellazioniPercentLineChart(canvasId){
  const data = __statCancellazioniMonthlyCounts__();
  __drawSharedMonthlyLineChart__(canvasId, data.cancByMonth, {
    lineColor: __statChartSelectedLineColor__('statcancellazioni', '#2B7CB4')
  });
}
function __scheduleStatsLineChartRedraw__(pageKey){
  const safe = String(pageKey || '').trim().toLowerCase();
  const draw = ()=>{
    try{
      if (safe === 'statgen') drawStatGenRegistrationsLineChart('statGenRegChart');
      else if (safe === 'statspese') drawStatSpesePercentLineChart('statSpeseLineChart');
      else if (safe === 'statamministratore') drawStatAmministratoreLineChart('statAmmRegChart');
    }catch(_){ }
  };
  try{ requestAnimationFrame(()=>requestAnimationFrame(draw)); }catch(_){ setTimeout(draw, 0); }
  try{ setTimeout(draw, 120); }catch(_){ }
  try{ setTimeout(draw, 320); }catch(_){ }
}

function renderStatGen(){
  bindStatFiscalModeToggle();
  updateStatFiscalModeUI();
  __updateStatGenCompareYearButtonUI__();

  const s = computeStatGen();
  state.statGen = s;

  const set = (id, v) => {
    const el = document.getElementById(id);
    if (el) el.textContent = euro(Number(v || 0));
  };

  set("sgFatturato", s.fatturatoTotale);
  set("sgSpese", s.speseTotali);
  set("sgNoRicevuta", s.senzaRicevuta);
  set("sgRicevuta", s.conRicevuta);
  set("sgIva", s.ivaDaVersare);
  set("sgGuadagno", s.guadagnoTotale);
  set("sgCassa", s.giacenzaCassa);

  try{
    const ivaRow = document.getElementById("sgIvaRow");
    if (ivaRow) ivaRow.hidden = (s.fiscalMode !== "societa");
  }catch(_){ }

  try{
    const rows = Array.from(document.querySelectorAll('#page-statgen .stat-row'));
    rows.forEach((row, idx)=>{
      const cardKey = String(row.dataset.statCardKey || `statgen-${idx+1}`);
      row.dataset.statCardKey = cardKey;
      const fallback = getComputedStyle(row).getPropertyValue('--mcol') || getComputedStyle(row).getPropertyValue('--statbg') || '#2B7CB4';
      __applyStatCardTextColor__(row, 'statgen', cardKey, fallback);
      __bindStatCardColorLongPress__(row, 'statgen', cardKey, fallback);
      __bindStatChartCardToggle__(row, 'statgen', cardKey);
      __setStatCardSelectionState__(row, 'statgen', cardKey);
      row.title = 'Pressione lunga per cambiare colori card';
    });
  }catch(_){ }
  try{ __applyStatisticsCardTheme__(); }catch(_){ }
  try{ __headerActionApplyAll__(); }catch(_){ }
  try{ __applyStatFiscalModeButtonVisual__(); }catch(_){ }
  try{ __applyStatGenCompareYearButtonVisual__(); }catch(_){ }
  try{
    const wrap = document.querySelector('#page-statgen .statgen-line-chart-wrap');
    if (wrap){
      __applyStatGenRegChartWrapVisual__(wrap);
      __bindStatGenRegChartLongPress__(wrap);
      __bindStatChartWrapReset__('statgen');
      wrap.title = 'Pressione lunga per cambiare design grafico';
    }
  }catch(_){ }
  try{ drawStatGenRegistrationsLineChart('statGenRegChart'); }catch(_){ }
  try{ __scheduleStatsLineChartRedraw__('statgen'); }catch(_){ }
  try{ __scheduleStatLandscapeGraphOnlyRefresh__(); }catch(_){ }
  try{ __loadStatGenCompareGuests__({ force:false }); }catch(_){ }
}



// ===== Statistiche: Fatturati mensili =====
function __hslToHex(h, s, l){
  h = ((h % 360) + 360) % 360;
  s = Math.max(0, Math.min(100, s)) / 100;
  l = Math.max(0, Math.min(100, l)) / 100;

  const c = (1 - Math.abs(2*l - 1)) * s;
  const hh = h / 60;
  const x = c * (1 - Math.abs((hh % 2) - 1));

  let r1 = 0, g1 = 0, b1 = 0;
  if (0 <= hh && hh < 1){ r1 = c; g1 = x; b1 = 0; }
  else if (1 <= hh && hh < 2){ r1 = x; g1 = c; b1 = 0; }
  else if (2 <= hh && hh < 3){ r1 = 0; g1 = c; b1 = x; }
  else if (3 <= hh && hh < 4){ r1 = 0; g1 = x; b1 = c; }
  else if (4 <= hh && hh < 5){ r1 = x; g1 = 0; b1 = c; }
  else { r1 = c; g1 = 0; b1 = x; }

  const m = l - c/2;
  const r = Math.round((r1 + m) * 255);
  const g = Math.round((g1 + m) * 255);
  const b = Math.round((b1 + m) * 255);
  const toHex = (n) => {
    const v = Math.max(0, Math.min(255, n|0));
    return v.toString(16).padStart(2, "0");
  };
  return `#${toHex(r)}${toHex(g)}${toHex(b)}`;
}

function __mensiliPalette12(){
  if (__mensiliPalette12._cache) return __mensiliPalette12._cache;
  const root = document.documentElement;
  const cs = getComputedStyle(root);
  const pick = (name, fallback) => {
    try{
      const v = (cs.getPropertyValue(name) || "").trim();
      return v || fallback;
    }catch(_){ return fallback; }
  };

  // Palette coerente con l'app (CSS variables in :root)
  const base = [
    pick("--p1", "#2B7CB4"),
    pick("--p2", "#4D9CC5"),
    pick("--p3", "#6FB7D6"),
    pick("--p4", "#96BFC7"),
    pick("--p5", "#BFBEA9"),
    pick("--p6", "#D6B286"),
    pick("--p7", "#CF9458"),
    pick("--p8", "#C9772B"),
  ];

  const out = [];
  for (let i = 0; i < 12; i++) out.push(base[i % base.length]);
  __mensiliPalette12._cache = out;
  return out;
}

// month labels are refreshed by i18n
__refreshMonthNamesCache__();

function computeStatMensili(){
  const guests = Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []);
  const servizi = Array.isArray(state.servizi) ? state.servizi : [];
  const byMonth = new Array(12).fill(0);

  const money = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === "number") return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    // Normalizza numeri tipo "1.234,56" o "1234,56"
    if (s.includes(",") && s.includes(".")) {
      s = s.replace(/\./g, "").replace(",", ".");
    } else if (s.includes(",")) {
      s = s.replace(",", ".");
    }
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };

  // Mappa ospite_id -> mese (basato su check-in/check-out)
  const guestMonthById = {};

  for (const gg of guests){
    let iso = "";
    try{
      if (typeof __parseDateFlexibleToISO === "function"){
        iso = __parseDateFlexibleToISO(gg?.check_in ?? gg?.checkIn ?? gg?.arrivo ?? gg?.data_arrivo ?? gg?.checkin ?? "");
      }
    }catch(_){ iso = ""; }

    if (!iso){
      // fallback: prova check-out se manca check-in
      try{
        if (typeof __parseDateFlexibleToISO === "function"){
          iso = __parseDateFlexibleToISO(gg?.check_out ?? gg?.checkOut ?? gg?.partenza ?? gg?.data_partenza ?? "");
        }
      }catch(_){ iso = ""; }
    }

    if (!iso || !/^(\d{4})-(\d{2})-(\d{2})$/.test(iso)) continue;
    const mm = parseInt(iso.slice(5,7), 10);
    if (!Number.isFinite(mm) || mm < 1 || mm > 12) continue;

    const gid = guestIdOf(gg) ?? gg?.id ?? gg?.ID ?? gg?.Id ?? "";
    if (gid !== null && gid !== undefined && String(gid).trim() !== "") {
      guestMonthById[String(gid)] = mm;
    }

    // Base: importo prenotazione
    const pren = money(gg?.importo_prenotazione ?? gg?.importo_prenota ?? gg?.importoPrenotazione ?? gg?.importoPrenota ?? 0);
    if (isFinite(pren) && pren !== 0) byMonth[mm - 1] += pren;
  }

  // Aggiungi servizi al mese dell'ospite (ospite_id -> mese)
  for (const s of (servizi || [])){
    if (!s) continue;
    const del = s.isDeleted ?? s.is_deleted ?? s.deleted;
    if (String(del) === "1" || del === true) continue;

    const ospiteId = s.ospite_id ?? s.ospiteId ?? s.guest_id ?? s.guestId ?? "";
    if (ospiteId === null || ospiteId === undefined || String(ospiteId).trim() === "") continue;
    const mm = guestMonthById[String(ospiteId)];
    if (!Number.isFinite(mm) || mm < 1 || mm > 12) continue;

    const qtyRaw = money(s.qty ?? 1);
    const qty = (isFinite(qtyRaw) && qtyRaw > 0) ? qtyRaw : 1;
    const amt = money(s.importo ?? s.amount ?? 0);
    if (!isFinite(amt) || amt === 0) continue;

    byMonth[mm - 1] += (qty * amt);
  }

  // Normalizza a 2 decimali
  for (let i = 0; i < byMonth.length; i++){
    const v = byMonth[i];
    byMonth[i] = isFinite(v) ? (Math.round(v * 100) / 100) : 0;
  }

  
  // ===== Occupazione mensile (% room-nights) =====
  // 100% = (giorni del mese) * (numero stanze totali)
  // Numeratore = room-nights occupati (notti * stanze) calcolati dai soggiorni (check-in/check-out)
  const occRoomNights = new Array(12).fill(0);

  // Prova a ricavare numero stanze dalla tabella "stanze"; fallback 6 (esempio struttura)
  const __getTotalRooms = () => {
    try{
      const rows = Array.isArray(state.stanzeRows) ? state.stanzeRows : [];
      const set = new Set();
      for (const r of rows){
        const sn = r?.stanza_num ?? r?.stanzaNum ?? r?.room_number ?? r?.roomNumber ?? r?.stanza ?? r?.room ?? "";
        const n = parseInt(String(sn).trim(),10);
        if (Number.isFinite(n) && n > 0) set.add(n);
      }
      if (set.size > 0) return set.size;
    }catch(_){}
    return getConfiguredRoomsCount(6);
  };

  const totalRooms = __getTotalRooms();

  // Mappa ospite_id -> numero stanze associate (da foglio stanze); fallback a campo "stanze" nell'ospite
  const roomsCountByGuestId = {};
  try{
    const rows = Array.isArray(state.stanzeRows) ? state.stanzeRows : [];
    const map = {};
    for (const r of rows){
      const gid = String(r?.ospite_id ?? r?.ospiteId ?? r?.guest_id ?? r?.guestId ?? "").trim();
      if (!gid) continue;
      const sn = String(r?.stanza_num ?? r?.stanzaNum ?? r?.room_number ?? r?.roomNumber ?? r?.stanza ?? r?.room ?? "").trim();
      const n = parseInt(sn,10);
      if (!Number.isFinite(n) || n<=0) continue;
      if (!map[gid]) map[gid] = new Set();
      map[gid].add(n);
    }
    for (const k in map){
      roomsCountByGuestId[k] = map[k].size || 0;
    }
  }catch(_){}

  const __nightsByMonthFromStay = (checkInISO, checkOutISO, roomsCount) => {
    if (!checkInISO || !checkOutISO) return;
    if (!/^\d{4}-\d{2}-\d{2}$/.test(checkInISO) || !/^\d{4}-\d{2}-\d{2}$/.test(checkOutISO)) return;
    const ci = new Date(checkInISO + "T00:00:00Z");
    const co = new Date(checkOutISO + "T00:00:00Z");
    if (!isFinite(ci.getTime()) || !isFinite(co.getTime())) return;
    if (co <= ci) return;

    let d = new Date(ci.getTime());
    const rooms = Math.max(1, Number(roomsCount||0) || 0);
    while (d < co){
      const m = d.getUTCMonth(); // 0..11
      if (m>=0 && m<12) occRoomNights[m] += rooms;
      d.setUTCDate(d.getUTCDate() + 1);
    }
  };

  // Somma room-nights da soggiorni
  for (const gg of guests){
    const gid = String(guestIdOf(gg) ?? gg?.id ?? gg?.ID ?? gg?.Id ?? "").trim();
    let roomsCount = 0;
    if (gid && roomsCountByGuestId[gid]) roomsCount = roomsCountByGuestId[gid];
    if (!roomsCount){
      const raw = Number(gg?.stanze ?? gg?.rooms ?? gg?.roomCount ?? 0);
      roomsCount = (isFinite(raw) && raw>0) ? Math.round(raw) : 0;
    }
    if (!roomsCount) roomsCount = 1;

    let ciISO = "";
    let coISO = "";
    try{
      ciISO = (typeof __parseDateFlexibleToISO === "function") ? __parseDateFlexibleToISO(gg?.check_in ?? gg?.checkIn ?? gg?.arrivo ?? gg?.data_arrivo ?? gg?.checkin ?? "") : "";
    }catch(_){ ciISO=""; }
    try{
      coISO = (typeof __parseDateFlexibleToISO === "function") ? __parseDateFlexibleToISO(gg?.check_out ?? gg?.checkOut ?? gg?.partenza ?? gg?.data_partenza ?? "") : "";
    }catch(_){ coISO=""; }

    if (ciISO && coISO) __nightsByMonthFromStay(ciISO, coISO, roomsCount);
  }

  // Capacita per mese: usa sempre l'anno di esercizio selezionato
  const y = Number(state.exerciseYear || loadExerciseYear() || new Date().getFullYear());
  const capacityRoomNights = new Array(12).fill(0).map((_,i)=>{
    const days = new Date(Date.UTC(y, i+1, 0)).getUTCDate(); // giorni nel mese
    return days * totalRooms;
  });

  const occPctByMonth = new Array(12).fill(0).map((_,i)=>{
    const cap = Number(capacityRoomNights[i]||0) || 0;
    const occ = Number(occRoomNights[i]||0) || 0;
    if (cap <= 0) return 0;
    return Math.max(0, Math.min(100, (occ / cap) * 100));
  });


  const occupiedRoomsByMonth = occRoomNights.map((v)=>{
    const n = Number(v || 0) || 0;
    return isFinite(n) ? Math.max(0, Math.round(n)) : 0;
  });

  return { byMonth, occPctByMonth, occupiedRoomsByMonth };

}



function renderStatMensili(){
  const wrap = document.getElementById("smList");
  if (!wrap) return;

  const s = computeStatMensili();
  state.statMensili = s;

  const months = s.byMonth || new Array(12).fill(0);
  const max = Math.max(0, ...months.map(v => Number(v || 0)));
  const totalMensile = months.reduce((sum, v) => sum + (Number(v || 0) || 0), 0);
  const colors = __mensiliPalette12();
  const occupiedRooms = (s.occupiedRoomsByMonth && Array.isArray(s.occupiedRoomsByMonth)) ? s.occupiedRoomsByMonth : new Array(12).fill(0);

  const monthlyShareDisplay = (() => {
    if (!(totalMensile > 0)) return new Array(12).fill(0);
    const raw = months.map(v => Math.max(0, ((Number(v || 0) || 0) / totalMensile) * 100));
    const base = raw.map(v => Math.floor(v));
    let remainder = 100 - base.reduce((sum, v) => sum + v, 0);
    const order = raw.map((v, i) => ({ i, frac: v - Math.floor(v) })).sort((a, b) => b.frac - a.frac);
    for (let n = 0; n < order.length && remainder > 0; n++, remainder--) base[order[n].i] += 1;
    return base;
  })();

  wrap.innerHTML = "";

  const fills = [];
  for (let i = 0; i < 12; i++){
    const val = Number(months[i] || 0) || 0;
    const pct = (max > 0) ? Math.max(0, Math.min(100, (val / max) * 100)) : 0;

    const row = document.createElement("div");
    row.className = "month-row";
    row.style.setProperty("--mcol", colors[i] || "#ff3b30");
    const occDisp = Math.max(0, Math.min(100, Number(monthlyShareDisplay[i] || 0) || 0));
    const occRoomsDisp = Math.max(0, Math.round(Number(occupiedRooms[i] || 0) || 0));
    const avgRoomPrice = occRoomsDisp > 0 ? (val / occRoomsDisp) : 0;

    row.innerHTML = `
      <div class="month-left">
        <div class="month-head">
          <div class="month-name">${escapeHtml(__MONTHS_IT[i] || ("Mese " + (i+1)))}</div>
          <div class="month-val">${euro(val)}</div>
        </div>
        <div class="month-bar">
          <div class="month-fill" style="width:0%"></div>
        </div>
      </div>
      <div class="month-occ"><div class="month-occ-pct">${occDisp}%</div><div class="month-occ-rooms">${occRoomsDisp} stanze</div><div class="month-occ-avg">${euro(avgRoomPrice)} media/stanza</div></div>
    `;

    wrap.appendChild(row);
    const fill = row.querySelector(".month-fill");
    if (fill) fills.push({ el: fill, pct });
    try{
      const cardKey = String(__MONTHS_IT[i] || ("Mese " + (i+1))).trim();
      row.dataset.statCardKey = cardKey;
      const fallback = colors[i] || '#2B7CB4';
      __applyStatCardTextColor__(row, 'statmensili', cardKey, fallback);
      __bindStatCardColorLongPress__(row, 'statmensili', cardKey, fallback);
      __bindStatChartCardToggle__(row, 'statmensili', cardKey);
      __setStatCardSelectionState__(row, 'statmensili', cardKey);
      row.title = 'Pressione lunga per cambiare colori card';
    }catch(_){ }
  }

  // animazione riempimento
  requestAnimationFrame(() => {
    for (const f of fills){
      try{ f.el.style.width = `${f.pct.toFixed(2)}%`; }catch(_){ }
    }
  });
  try{ __applyStatisticsCardTheme__(); }catch(_){ }
  try{ __headerActionApplyAll__(); }catch(_){ }
  try{
    const wrapChart = document.querySelector('#page-statmensili .statgen-line-chart-wrap');
    if (wrapChart){
      __applyStatGenRegChartWrapVisual__(wrapChart);
      __bindStatSharedLineChartLongPress__(wrapChart);
      __bindStatChartWrapReset__('statmensili');
      wrapChart.title = 'Pressione lunga per cambiare design grafico';
    }
  }catch(_){ }
  try{ drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); }catch(_){ }
  try{ __scheduleStatLandscapeGraphOnlyRefresh__(); }catch(_){ }
}


// ===== Statistiche: Prenotazioni (booking compilato vs non compilato) =====
function computeStatPrenotazioni(){
  const guests = Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []);

  const money = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === "number") return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    if (s.includes(",") && s.includes(".")) {
      s = s.replace(/\./g, "").replace(",", ".");
    } else if (s.includes(",")) {
      s = s.replace(",", ".");
    }
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };

  let withBooking = 0;
  let withoutBooking = 0;

  for (const g of guests){
    const pren = money(g?.importo_prenotazione ?? g?.importo_prenota ?? g?.importoPrenotazione ?? g?.importoPrenota ?? 0);
    const isDirect = __statGuestIsDirectChannel__(g);
    if (isDirect) withoutBooking += pren;
    else withBooking += pren;
  }

  return { withBooking, withoutBooking };
}

function renderStatPrenotazioni(){
  return renderStatChannel();
}
function openStatPieModal(){
  try{
    if (!state.statGen) state.statGen = computeStatGen();
  }catch(_){ state.statGen = state.statGen || null; }

  const m = document.getElementById("statPieModal");
  if (!m) return;
  m.hidden = false;
  m.setAttribute("aria-hidden", "false");

  const s = state.statGen || computeStatGen();
  const slices = [
    { label: "Senza ricevuta", value: s.senzaRicevuta, color: "#bfbea9" },
    { label: "Con ricevuta", value: s.conRicevuta, color: "#6fb7d6" },
  ];

  drawPie("statPieCanvas", slices);

  const leg = document.getElementById("statPieLegend");
  if (leg){
    const total = slices.reduce((a,x)=>a+Math.max(0,Number(x.value||0)),0);
    leg.innerHTML = "";
    slices.forEach((sl)=>{
      const v = Math.max(0, Number(sl.value || 0));
      const pct = total > 0 ? (v/total*100) : 0;
      const row = document.createElement("div");
      row.className = "legrow";
      row.innerHTML = `
        <div class="legleft">
          <div class="dot" style="background:${sl.color}"></div>
          <div class="legname">${escapeHtml(sl.label)}</div>
        </div>
        <div class="legright">${pct.toFixed(1)}% · ${euro(v)}</div>
      `;
      leg.appendChild(row);
    });
  }
}

function closeStatPieModal(){
  const m = document.getElementById("statPieModal");
  if (!m) return;
  m.hidden = true;
  m.setAttribute("aria-hidden", "true");
}



function openStatMensiliPieModal(){
  try{
    if (!state.statMensili) state.statMensili = computeStatMensili();
  }catch(_){ state.statMensili = state.statMensili || null; }

  const m = document.getElementById("statMensiliPieModal");
  if (!m) return;
  m.hidden = false;
  m.setAttribute("aria-hidden", "false");

  const s = state.statMensili || computeStatMensili();
  const months = (s.byMonth && Array.isArray(s.byMonth)) ? s.byMonth : new Array(12).fill(0);
  const colors = __mensiliPalette12();
  const slices = months.map((v,i)=>({
    label: __MONTHS_IT[i] || ("Mese " + (i+1)),
    value: Number(v || 0) || 0,
    color: colors[i] || "#2b7cb4"
  }));

  drawPie("statMensiliPieCanvas", slices);

  const occPcts = (s.occPctByMonth && Array.isArray(s.occPctByMonth)) ? s.occPctByMonth : new Array(12).fill(0);
  drawMonthlyPctBars("statMensiliOccCanvas", occPcts, colors, { labels: (__MONTHS_IT || []).map(m=>String(m||"").slice(0,1)) });

  const leg = document.getElementById("statMensiliPieLegend");
  if (leg){
    leg.innerHTML = "";
    leg.style.display = "none";
    leg.setAttribute("aria-hidden", "true");
  }
}

function closeStatMensiliPieModal(){
  const m = document.getElementById("statMensiliPieModal");
  if (!m) return;
  m.hidden = true;
  m.setAttribute("aria-hidden", "true");
}


/* =========================
 * Statistiche: Azienda / Amministratore (tassazione stimata)
 * Regole:
 * - Fatturato netto IVA: somma incassi CON ricevuta (acconto+saldo), al netto del 10% IVA
 * - Spese operative: esclude categorie "contanti" (e "tassa soggiorno"), al netto dell'IVA
 * - IRAP: percentuale editabile (stima)
 * ========================= */

const LS_ADMIN_COMPENSO = "ddae_admin_compenso";
const LS_ADMIN_TFM = "ddae_admin_tfm";
const LS_IRAP_PCT = "ddae_irap_pct";

function _num(v){
  if (v === null || v === undefined) return 0;
  if (typeof v === "number") return isFinite(v) ? v : 0;
  let s = String(v).trim();
  if (!s) return 0;
  if (s.includes(",") && s.includes(".")) s = s.replace(/\./g, "").replace(",", ".");
  else if (s.includes(",")) s = s.replace(",", ".");
  const n = Number(s);
  return isFinite(n) ? n : 0;
}
function _lsGetNum(key, fallback){
  try{
    const v = localStorage.getItem(key);
    if (v === null || v === undefined || v === "") return fallback;
    const n = _num(v);
    return isFinite(n) ? n : fallback;
  }catch(_){ return fallback; }
}
function _lsSetNum(key, value){
  try{ localStorage.setItem(key, String(value)); }catch(_){}
}

function __ricevutaValueTruthy__(v){
  try{
    if (v === true) return true;
    if (v === false || v === undefined || v === null) return false;
    if (typeof v === "number") return isFinite(v) && v !== 0;
    const s = String(v).trim().toLowerCase();
    if (!s) return false;
    if (["0","false","no","non","none","null","undefined","off","n","f","ko"].includes(s)) return false;
    if (s.includes("senza") || s.includes("mancant") || s.includes("non emess") || s.includes("no ricev")) return false;
    return true;
  }catch(_){ }
  return false;
}

function __ricevutaDynamicFieldTruthy__(g, kind){
  try{
    if (!g || typeof g !== "object") return false;
    const knd = String(kind || "").trim().toLowerCase();
    const kindNeedles = knd === "acconto"
      ? ["acconto","deposit","deposito","caparra"]
      : ["saldo","balance","finale","restante"];
    for (const key of Object.keys(g)){
      const lk = String(key || "").toLowerCase();
      if (!lk.includes("ricev")) continue;
      if (!kindNeedles.some((needle) => lk.includes(needle))) continue;
      if (__ricevutaValueTruthy__(g[key])) return true;
    }
  }catch(_){ }
  return false;
}

function _isRicevutaFlag(g, kind){
  try{
    if (!g || typeof g !== "object") return false;
    const hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);
    const firstExplicit = (keys) => {
      for (const key of keys){
        if (!hasOwn(g, key)) continue;
        const v = g[key];
        if (v === undefined || v === null) continue;
        return __ricevutaValueTruthy__(v);
      }
      return null;
    };

    if (kind === "acconto"){
      // La spunta della scheda ospite è il dato autorevole: se esiste ed è OFF,
      // eventuali campi legacy/numero/file ricevuta non devono più conteggiare la ricevuta.
      const explicit = firstExplicit([
        "acconto_ricevuta", "accontoRicevuta", "ricevuta_acconto", "ricevutaAcconto",
        "acconto_ricevutain", "accontoRicevutaIn", "ricevutaAccontoIn"
      ]);
      if (explicit !== null) return explicit;

      const values = [
        g?.numero_ricevuta_acconto, g?.numeroRicevutaAcconto, g?.acconto_numero_ricevuta,
        g?.accontoNumeroRicevuta, g?.ricevuta_acconto_numero, g?.ricevutaAccontoNumero,
        g?.url_ricevuta_acconto, g?.pdf_ricevuta_acconto, g?.file_ricevuta_acconto
      ];
      if (values.some(__ricevutaValueTruthy__)) return true;
      if (__ricevutaDynamicFieldTruthy__(g, "acconto")) return true;
      const t = (g?.acconto_tipo ?? g?.accontoTipo ?? "").toString().toLowerCase();
      if (t.includes("ricev")) return true;
      if (t.includes("contant")) return false;
    } else {
      // La spunta della scheda ospite è il dato autorevole: se esiste ed è OFF,
      // eventuali campi legacy/numero/file ricevuta non devono più conteggiare la ricevuta.
      const explicit = firstExplicit([
        "saldo_ricevuta", "saldoRicevuta", "ricevuta_saldo", "ricevutaSaldo",
        "saldo_ricevutain", "saldoRicevutaIn", "ricevutaSaldoIn"
      ]);
      if (explicit !== null) return explicit;

      const values = [
        g?.numero_ricevuta_saldo, g?.numeroRicevutaSaldo, g?.saldo_numero_ricevuta,
        g?.saldoNumeroRicevuta, g?.ricevuta_saldo_numero, g?.ricevutaSaldoNumero,
        g?.url_ricevuta_saldo, g?.pdf_ricevuta_saldo, g?.file_ricevuta_saldo
      ];
      if (values.some(__ricevutaValueTruthy__)) return true;
      if (__ricevutaDynamicFieldTruthy__(g, "saldo")) return true;
      const t = (g?.saldo_tipo ?? g?.saldoTipo ?? "").toString().toLowerCase();
      if (t.includes("ricev")) return true;
      if (t.includes("contant")) return false;
    }
  }catch(_){}
  return false;
}



/* RICEVUTE MANCANTI (HOME) */
function _isElectronicTypeStr_(s){
  const t = String(s ?? "").toLowerCase();
  return t.includes("elet");
}

function computePendingReceipts(guests){
  const now = Date.now();
  const out = [];
  const src = Array.isArray(guests) ? guests : [];
  for (const g of src){
    const checkout = parseDateTs(g?.check_out ?? g?.checkOut ?? g?.checkout ?? g?.data_check_out ?? "");
    if (!checkout) continue;
    if (now < (checkout + 24*60*60*1000)) continue;

    const dep = _num(g?.acconto_importo ?? g?.accontoImporto ?? 0);
    const depType = (g?.acconto_tipo ?? g?.accontoTipo ?? "");
    const depRec = truthy(g?.acconto_ricevuta ?? g?.accontoRicevuta ?? g?.ricevuta_acconto ?? g?.ricevutaAcconto ?? false);

    const saldo = _num(g?.saldo_pagato ?? g?.saldoPagato ?? g?.saldo ?? 0);
    const saldoType = (g?.saldo_tipo ?? g?.saldoTipo ?? "");
    const saldoRec = truthy(g?.saldo_ricevuta ?? g?.saldoRicevuta ?? g?.ricevuta_saldo ?? g?.ricevutaSaldo ?? false);

    const missing = [];
    if (dep > 0 && _isElectronicTypeStr_(depType) && !depRec) missing.push("Acconto");
    if (saldo > 0 && _isElectronicTypeStr_(saldoType) && !saldoRec) missing.push("Saldo");

    if (!missing.length) continue;

    const rawName = g?.nome ?? g?.name ?? "";
    const name = collapseSpaces(String(rawName || "").trim()) || "Prenotazione";
    out.push({
      id: guestIdOf(g),
      name,
      missing,
      checkout,
      guest: g
    });
  }

  out.sort((a,b)=> (b.checkout||0) - (a.checkout||0));
  return out;
}

function updateHomeReceiptsIndicator(){
  try{
    const btn = document.getElementById("homeReceiptsTop");
    const dot = document.getElementById("homeReceiptsDot");
    if (!btn) return;

    const isHome = (state.page === "home");
    const isOp = !!(state.session && isOperatoreSession(state.session));
    const has = (state.pendingReceiptsCount || 0) > 0;

    // visibile SOLO in HOME e solo admin
    btn.hidden = (!isHome) || isOp;

    btn.classList.toggle("is-alert", has);
    if (dot) dot.hidden = !has;

    // accessibilità
    if (has){
      btn.setAttribute("aria-label", `Ricevute mancanti (${state.pendingReceiptsCount})`);
    }else{
      btn.setAttribute("aria-label", "Ricevute");
    }
  }catch(_){}
}

function openReceiptDueModal(){
  const modal = document.getElementById("receiptDueModal");
  const list = document.getElementById("receiptDueList");
  const title = document.getElementById("receiptDueTitle");
  if (!modal || !list) return;

  const items = Array.isArray(state.pendingReceipts) ? state.pendingReceipts : [];
  const n = items.length;

  if (title){
    title.textContent = n ? `Ricevute mancanti (${n})` : "Ricevute mancanti";
  }

  list.innerHTML = "";
  if (!n){
    const empty = document.createElement("div");
    empty.className = "muted";
    empty.style.padding = "8px 2px";
    empty.textContent = "Nessuna ricevuta da emettere.";
    list.appendChild(empty);
  } else {
    for (const it of items){
      const row = document.createElement("button");
      row.type = "button";
      row.className = "receipt-row";
      const miss = (it.missing || []).join(", ");
      row.innerHTML = `<div>
          <div class="rr-name">${escapeHtml(it.name)}</div>
          <div class="rr-meta">${escapeHtml(miss)}</div>
        </div>
        <div class="rr-meta">apri</div>`;
      bindFastTap(row, () => {
        try{ closeReceiptDueModal(); }catch(_){}
        try{
          if (it.guest){
            enterGuestViewMode(it.guest);
            showPage("ospite");
            return;
          }
          // fallback: cerca tra ospiti già in memoria
          const id = String(it.id || "").trim();
          const g = (Array.isArray(state.pendingReceiptsGuests) ? state.pendingReceiptsGuests : [])
            .find(x => guestIdOf(x) === id);
          if (g){
            enterGuestViewMode(g);
            showPage("ospite");
          }
        }catch(e){
          console.error(e);
        }
      });
      list.appendChild(row);
    }
  }

  modal.hidden = false;
  modal.setAttribute("aria-hidden","false");
  updateHomeReceiptsIndicator();
}

function closeReceiptDueModal(){
  const modal = document.getElementById("receiptDueModal");
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute("aria-hidden","true");
}

async function checkReceiptsOnStartup(){
  try{
    if (!(state.session && state.session.user_id)) { state.pendingReceipts = []; state.pendingReceiptsCount = 0; updateHomeReceiptsIndicator(); return; }
    if (state.session && isOperatoreSession(state.session)) { state.pendingReceipts = []; state.pendingReceiptsCount = 0; updateHomeReceiptsIndicator(); return; }

    // dati ospiti (no popup, no loader)
    let rows = [];
    try{
      const data = await cachedGet("ospiti", {}, { showLoader:false, ttlMs: 0, force:true });
      rows = Array.isArray(data) ? data : [];
    }catch(_){ rows = []; }

    state.pendingReceiptsGuests = rows;
    state.pendingReceipts = computePendingReceipts(rows);
    state.pendingReceiptsCount = state.pendingReceipts.length;

    updateHomeReceiptsIndicator();
  }catch(e){
    console.error(e);
    try{ updateHomeReceiptsIndicator(); }catch(_){}
  }
}

function computeAziendaBilancio(){
  const guests = Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []);
  const spese = Array.isArray(state.spese) ? state.spese : [];

  // Incassi con/senza ricevuta: acconto + saldo
  let incassiCon = 0;
  let incassiSenza = 0;

  for (const g of guests){
    const dep = _num(g?.acconto_importo ?? g?.accontoImporto ?? 0);
    const saldo = _num(g?.saldo_pagato ?? g?.saldoPagato ?? g?.saldo ?? 0);

    if (dep > 0){
      if (_isRicevutaFlag(g, "acconto")) incassiCon += dep;
      else incassiSenza += dep;
    }
    if (saldo > 0){
      if (_isRicevutaFlag(g, "saldo")) incassiCon += saldo;
      else incassiSenza += saldo;
    }
  }

  // Fatturato netto IVA (assumiamo IVA 10%)
  const fatturatoNettoIva = incassiCon / 1.10;

  // Spese operative nette IVA: escludi contanti e tassa soggiorno; togli IVA
  let speseNetteIva = 0;

  for (const s of spese){
    const lordo = _num(s?.importoLordo ?? s?.lordo ?? 0);
    if (!isFinite(lordo) || lordo <= 0) continue;

    const cat = (s?.categoria ?? s?.cat ?? "").toString().trim().toLowerCase();
    if (cat.includes("contant")) continue;
    if (cat.includes("tassa") && cat.includes("sogg")) continue;

    let rate = 0;
    if (cat.includes("iva")){
      if (cat.includes("22")) rate = 22;
      else if (cat.includes("10")) rate = 10;
      else if (cat.includes("4")) rate = 4;
    } else {
      const n = parseFloat(String(s?.aliquotaIva ?? s?.aliquota_iva ?? s?.aliquota ?? "").replace(",", "."));
      if (!isNaN(n)) {
        if (n >= 21.5) rate = 22;
        else if (n >= 9.5 && n < 11.5) rate = 10;
        else if (n >= 3.5 && n < 5.5) rate = 4;
      }
    }

    const imponibile = rate > 0 ? (lordo / (1 + rate/100)) : lordo;
    if (isFinite(imponibile)) speseNetteIva += imponibile;
  }

  const compenso = _lsGetNum(LS_ADMIN_COMPENSO, 25000);
  const tfm = _lsGetNum(LS_ADMIN_TFM, 15000);
  const irapPct = _lsGetNum(LS_IRAP_PCT, 3.9);

  const inps = compenso * 0.16;

  const ebt = fatturatoNettoIva - speseNetteIva - compenso - inps - tfm;
  const ires = Math.max(0, ebt * 0.24);
  const irap = Math.max(0, ebt * (irapPct/100));
  const utileNetto = ebt - ires - irap;

  return {
    incassiConRicevuta: incassiCon,
    incassiSenzaRicevuta: incassiSenza,
    fatturatoNettoIva,
    speseOperativeNetteIva: speseNetteIva,
    compensoAmministratore: compenso,
    tfmAccantonamento: tfm,
    inpsQuotaAzienda: inps,
    ebt,
    ires,
    irapPct,
    irap,
    utileNetto,
  };
}

function _euroSigned(n){
  const v = Number(n || 0);
  if (!isFinite(v) || v === 0) return euro(0);
  if (v < 0) return "− " + euro(Math.abs(v));
  return "+ " + euro(v);
}


function renderStatCancellazioni(){
  const root = document.getElementById("page-statcancellazioni");
  if (!root) return;

  const delRows = Array.isArray(state.deletedGuests) ? state.deletedGuests : [];
  const cancRows = delRows.filter(r => String(r.delete_reason || "").toLowerCase() === "cancellazione" || String(r.delete_reason || "").trim() === "");
  const cancN = cancRows.length;

  const activeRows = Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []);
  const activeN = activeRows.length;

  const total = activeN + cancN;
  const pctCanc = total > 0 ? (cancN / total * 100) : 0;

  const elTot = document.getElementById("cancTot");
  const elCanc = document.getElementById("cancN");
  const elPct = document.getElementById("cancPct");
  if (elTot) elTot.textContent = String(total);
  if (elCanc) elCanc.textContent = String(cancN);
  if (elPct) elPct.textContent = pctCanc.toFixed(1) + "%";

  const slices = [
    { label: "Attive", value: activeN, color: "#2b7cb4" },
    { label: "Cancellate", value: cancN, color: "#ff3b30" },
  ];

  drawPie("statCancCanvas", slices, { centerTitle: "Totale", centerFormatter: (n)=> String(Math.round(Number(n||0))) });

  const leg = document.getElementById("statCancLegend");
  if (leg){
    const t = slices.reduce((a,x)=>a+Math.max(0,Number(x.value||0)),0);
    leg.innerHTML = "";
    slices.forEach((sl)=>{
      const v = Math.max(0, Number(sl.value || 0));
      const pct = t > 0 ? (v/t*100) : 0;
      const row = document.createElement("div");
      row.className = "legrow";
      row.innerHTML = `
        <div class="legleft">
          <div class="dot" style="background:${sl.color}"></div>
          <div class="legname">${escapeHtml(sl.label)}</div>
        </div>
        <div class="legright">${pct.toFixed(1)}% · ${String(Math.round(v))}</div>
      `;
      leg.appendChild(row);
    });
  }
  try{
    const wrapChart = root.querySelector('.statgen-line-chart-wrap');
    if (wrapChart){
      wrapChart.dataset.statLineChartKey = 'statcancellazioni';
      __applyStatGenRegChartWrapVisual__(wrapChart);
      __bindStatSharedLineChartLongPress__(wrapChart);
      __bindStatChartWrapReset__('statcancellazioni');
      wrapChart.title = 'Pressione lunga per cambiare design grafico';
    }
  }catch(_){ }
  try{ drawStatCancellazioniPercentLineChart('statCancellazioniLineChart'); }catch(_){ }
  try{ __scheduleStatLandscapeGraphOnlyRefresh__(); }catch(_){ }

  try{
    document.querySelectorAll('#page-statcancellazioni .kpi-card').forEach((card, index) => {
      const keys = ['percentuale','totale','cancellate'];
      const fallbacks = ['#ff3b30','#2b7cb4','#c9772b'];
      const cardKey = keys[index] || `kpi-${index+1}`;
      const fallback = fallbacks[index] || '#2b7cb4';
      card.dataset.statCardKey = cardKey;
      __applyStatCardTextColor__(card, 'statcancellazioni', cardKey, fallback);
      __bindStatCardColorLongPress__(card, 'statcancellazioni', cardKey, fallback);
      __bindStatChartCardToggle__(card, 'statcancellazioni', cardKey);
      __setStatCardSelectionState__(card, 'statcancellazioni', cardKey);
    });
  }catch(_){ }
}


function renderStatAzienda(){
  const rowsWrap = document.getElementById("aziendaRows");
  if (!rowsWrap) return;

  const s = computeAziendaBilancio();
  state.statAzienda = s;

  const totalTasse = (
    Number(s.ires || 0) +
    Number(s.irap || 0) +
    Number(s.inpsQuotaAzienda || 0)
  );

  const rows = [
    { k: "FATTURATO NETTO IVA", v: +s.fatturatoNettoIva, cls: "c-blue" },
    { k: "Spese Operative nette IVA", v: -s.speseOperativeNetteIva, cls: "c-bordeaux" },
    { k: "Compenso Amministratore (Lordo)", v: -s.compensoAmministratore },
    { k: "INPS (Quota Azienda 2/3)", v: -s.inpsQuotaAzienda },
    { k: "Accantonamento TFM", v: -s.tfmAccantonamento },
    { k: "UTILE ANTE TASSE (EBT)", v: +s.ebt },
    { k: "IRES (24%)", v: -s.ires },
    { k: `IRAP (${Number(s.irapPct || 0).toFixed(1)}%)`, v: -s.irap },
    { k: "TOTALE TASSE", v: -totalTasse, cls: "c-red" },
    { k: "UTILE NETTO SRL", v: +s.utileNetto, cls: "c-green" },
  ];

  rowsWrap.innerHTML = rows.map(r => {
    const extra = r.cls ? (" " + r.cls) : "";
    return `<div class="fin-row${extra}">
      <div class="fin-label">${escapeHtml(r.k)}</div>
      <div class="fin-val">${_euroSigned(r.v)}</div>
    </div>`;
  }).join("");
  try{
    const card = document.querySelector('#page-statazienda .fin-card');
    if (card){
      __applyStatCardTextColor__(card, 'statazienda', 'bilancio', '#2b7cb4');
      __bindStatCardColorLongPress__(card, 'statazienda', 'bilancio', '#2b7cb4');
    }
  }catch(_){ }
}



function openAdminInputsModal(){
  if (getStatFiscalMode() === "forfettario"){
    try{ toast("Compenso e TFM non usati in modalità forfettario"); }catch(_){ }
    return;
  }
  const m = document.getElementById("adminInputsModal");
  if (!m) return;
  try{ renderStatAmministratore(); }catch(_){}
  try{
    const c = document.getElementById("adminCompensoInput");
    const t = document.getElementById("adminTfmInput");
    if (c) c.value = String(_lsGetNum(LS_ADMIN_COMPENSO, 25000)).replace(".", ",");
    if (t) t.value = String(_lsGetNum(LS_ADMIN_TFM, 15000)).replace(".", ",");
  }catch(_){ }
  m.hidden = false;
  m.setAttribute("aria-hidden","false");
  setTimeout(()=>{ try{ const c = document.getElementById("adminCompensoInput"); if (c) c.focus(); }catch(_){ } }, 50);
}
function closeAdminInputsModal(){
  const m = document.getElementById("adminInputsModal");
  if (!m) return;
  m.hidden = true;
  m.setAttribute("aria-hidden","true");
}

function _buildLegend_(el, slices){
  if (!el) return;
  const total = slices.reduce((a,x)=>a+Math.max(0,Number(x.value||0)),0);
  el.innerHTML = "";
  slices.forEach((sl)=>{
    const v = Math.max(0, Number(sl.value || 0));
    const pct = total > 0 ? (v/total*100) : 0;
    const row = document.createElement("div");
    row.className = "legrow";
    row.innerHTML = `
      <div class="legleft">
        <div class="dot" style="background:${sl.color}"></div>
        <div class="legname">${escapeHtml(sl.label)}</div>
      </div>
      <div class="legval">${_euroNoSign(v)}</div>
      <div class="legpct">${pct.toFixed(1)}%</div>
    `;
    el.appendChild(row);
  });
}

async function openAdminChartModal(){
  const m = document.getElementById("adminChartModal");
  if (!m) return;

  try{ await ensurePeriodData({ showLoader:true, force:false }); }catch(_){}

  // Usa i dati di bilancio azienda (IRES / IRAP / INPS quota azienda / totale tasse)
  let s = null;
  try{ s = computeAziendaBilancio(); }catch(_){ s = null; }
  if (!s) s = { ires:0, irap:0, inpsQuotaAzienda:0 };

  const slices = [
    { label: "IRES", value: Math.max(0, Number(s.ires||0)), color: "#2b7cb4" },
    { label: "IRAP", value: Math.max(0, Number(s.irap||0)), color: "#c9772b" },
    { label: "INPS (Quota Azienda)", value: Math.max(0, Number(s.inpsQuotaAzienda||0)), color: "#7a3d9b" },
  ];

  drawPie("adminPieCanvas", slices);
  try{ _buildLegend_(document.getElementById("adminPieLegend"), slices); }catch(_){}

  m.hidden = false;
  m.setAttribute("aria-hidden","false");
}

function closeAdminChartModal(){
  const m = document.getElementById("adminChartModal");
  if (!m) return;
  m.hidden = true;
  m.setAttribute("aria-hidden","true");
}


function __statAmmRates__(){
  const irpefPct = Math.max(0, _lsGetNum("dDAE_IRPEF_PCT", 15)) / 100;
  const inpsPct = Math.max(0, _lsGetNum("dDAE_INPS_PCT", 26.23)) / 100;
  return { irpefPct, inpsPct };
}

function getStatForfettarioStartup5(){
  try{
    const raw = String(localStorage.getItem(LS_STAT_FORFETTARIO_STARTUP_5) || "").trim().toLowerCase();
    return raw === "1" || raw === "true" || raw === "yes" || raw === "on";
  }catch(_){ }
  return false;
}

function setStatForfettarioStartup5(enabled){
  try{ localStorage.setItem(LS_STAT_FORFETTARIO_STARTUP_5, enabled ? "1" : "0"); }catch(_){ }
  updateStatFiscalModeUI();
  try{ if (state.page === "statamministratore") renderStatAmministratore(); }catch(_){ }
}

function __statAmmForfettarioConfig__(){
  const startup5 = getStatForfettarioStartup5();
  return {
    redditivitaPct: 0.40,
    impostaPct: startup5 ? 0.05 : 0.15,
    startup5,
    inpsMinimale: 18555,
    inpsFisso: 4460.64,
    inpsEccedenzaPct: 0.24
  };
}

function __statAmmForfettarioTaxesForRevenue__(fatturatoRicevute){
  const cfg = __statAmmForfettarioConfig__();
  const ricavi = Math.max(0, Number(fatturatoRicevute || 0) || 0);
  const imponibileForfettario = Math.round((ricavi * cfg.redditivitaPct) * 100) / 100;
  const irpef = Math.round((imponibileForfettario * cfg.impostaPct) * 100) / 100;
  const eccedenzaInps = Math.max(0, ricavi - cfg.inpsMinimale);
  const inps = ricavi > 0 ? Math.round((cfg.inpsFisso + (eccedenzaInps * cfg.inpsEccedenzaPct)) * 100) / 100 : 0;
  return { imponibileForfettario, irpef, inps, eccedenzaInps, impostaPct: cfg.impostaPct, startup5: cfg.startup5 };
}

function __statAmmEstimatedBookingRevenue__(guest){
  const pren = __statGuestMoney__(guest?.importo_prenotazione ?? guest?.importo_prenota ?? guest?.importoPrenotazione ?? guest?.importoPrenota ?? guest?.totale_prenotazione ?? guest?.totalePrenotazione ?? guest?.totale ?? 0);
  if (pren > 0) return pren;
  const dep = __statGuestMoney__(guest?.acconto_importo ?? guest?.accontoImporto ?? 0);
  const saldo = __statGuestMoney__(guest?.saldo_pagato ?? guest?.saldoPagato ?? guest?.saldo ?? 0);
  return Math.max(0, dep + saldo);
}

function computeStatAmministratoreFromGuests(sourceRows){
  const fiscalMode = getStatFiscalMode();
  const guests = Array.isArray(sourceRows) ? sourceRows : (Array.isArray(state.statsGuests) ? state.statsGuests : (Array.isArray(state.guests) ? state.guests : []));
  const { irpefPct, inpsPct } = __statAmmRates__();
  const ricevute = __statGuestDualMonthlySeriesForRows__(guests, (guest) => {
    const stimato = __statAmmEstimatedBookingRevenue__(guest);
    const hasReceipt = _guestHasFiscalReceipt(guest);
    return {
      primary: hasReceipt ? stimato : 0,
      secondary: hasReceipt ? 0 : stimato
    };
  });
  const fatturatoMensile = Array.isArray(ricevute.primary) ? ricevute.primary : new Array(12).fill(0);
  const fatturatoSenzaMensile = Array.isArray(ricevute.secondary) ? ricevute.secondary : new Array(12).fill(0);
  const fatturatoRicevute = Math.round(fatturatoMensile.reduce((a,b)=>a+(Number(b||0)||0),0) * 100) / 100;
  const fatturatoSenzaRicevuta = Math.round(fatturatoSenzaMensile.reduce((a,b)=>a+(Number(b||0)||0),0) * 100) / 100;
  const ricaviStimatiTotali = Math.round((fatturatoRicevute + fatturatoSenzaRicevuta) * 100) / 100;
  let imponibileForfettario = 0;
  let irpef = 0;
  let inps = 0;
  let ivaDaVersare = 0;
  let impostaForfettariaPct = 0;
  let startup5 = false;
  if (fiscalMode === 'forfettario'){
    const taxes = __statAmmForfettarioTaxesForRevenue__(ricaviStimatiTotali);
    imponibileForfettario = taxes.imponibileForfettario;
    irpef = taxes.irpef;
    inps = taxes.inps;
    impostaForfettariaPct = Number(taxes.impostaPct || 0) || 0;
    startup5 = !!taxes.startup5;
  } else {
    irpef = Math.round(ricaviStimatiTotali * irpefPct * 100) / 100;
    inps = Math.round(ricaviStimatiTotali * inpsPct * 100) / 100;
    try{ ivaDaVersare = Number(computeStatGen()?.ivaDaVersare || 0) || 0; }catch(_){ ivaDaVersare = 0; }
  }
  const totaleTasse = Math.round((irpef + inps + Math.max(0, Number(ivaDaVersare || 0))) * 100) / 100;
  const utileSpendibileNettoAnno = Math.round((ricaviStimatiTotali - totaleTasse) * 100) / 100;
  const nettoMensile = Math.round((utileSpendibileNettoAnno / 12) * 100) / 100;
  return { fiscalMode, ricaviStimatiTotali, fatturatoRicevute, fatturatoSenzaRicevuta, imponibileForfettario, irpef, inps, ivaDaVersare, totaleTasse, utileSpendibileNettoAnno, nettoMensile, fatturatoMensile, fatturatoSenzaMensile, irpefPct, inpsPct, impostaForfettariaPct, startup5 };
}

function __statAmministratoreSeriesListFromGuests__(sourceRows, pageKey){
  const safePage = pageKey || 'statamministratore';
  const data = computeStatAmministratoreFromGuests(sourceRows);
  const fattCum = __statMonthlyCumulative__(data.fatturatoMensile || new Array(12).fill(0));
  const senzaCum = __statMonthlyCumulative__(data.fatturatoSenzaMensile || new Array(12).fill(0));
  const ricaviCum = fattCum.map((v, idx)=> Math.round(((Number(v||0)||0) + (Number(senzaCum[idx]||0)||0)) * 100) / 100);
  let irpefCum = new Array(12).fill(0);
  let inpsCum = new Array(12).fill(0);
  if (data.fiscalMode === 'forfettario'){
    irpefCum = ricaviCum.map((v)=> __statAmmForfettarioTaxesForRevenue__(Number(v||0)).irpef);
    inpsCum = ricaviCum.map((v)=> __statAmmForfettarioTaxesForRevenue__(Number(v||0)).inps);
  } else {
    irpefCum = ricaviCum.map((v)=> Math.round((Number(v||0) * data.irpefPct) * 100) / 100);
    inpsCum = ricaviCum.map((v)=> Math.round((Number(v||0) * data.inpsPct) * 100) / 100);
  }
  const ivaTotal = Number(data.ivaDaVersare || 0) || 0;
  const revenueTotal = Math.max(1, Number(data.ricaviStimatiTotali || 0) || 0);
  const ivaCum = ricaviCum.map((v)=> Math.round(((Number(v||0) / revenueTotal) * ivaTotal) * 100) / 100);
  const totaleCum = irpefCum.map((v, idx)=> Math.round(((Number(v||0)||0) + (Number(inpsCum[idx]||0)||0) + (Number(ivaCum[idx]||0)||0)) * 100) / 100);
  const utileAnnoCum = ricaviCum.map((v, idx)=> Math.round(((Number(v||0)||0) - (Number(totaleCum[idx]||0)||0)) * 100) / 100);
  const nettoMensileCum = utileAnnoCum.map((v)=> Math.round(((Number(v||0)||0) / 12) * 100) / 100);
  const defs = [
    { key:'fatturato-ricevute', label:'Fatturato ricevute', values:fattCum, fallback:'#2b7cb4' },
    { key:'fatturato-senza-ricevuta', label:'Fatturato senza ricevuta', values:senzaCum, fallback:'#9aa3af' },
    { key:'irpef', label:'IRPEF', values:irpefCum, fallback:'#c9772b' },
    { key:'inps', label:'INPS', values:inpsCum, fallback:'#7a3d9b' },
    { key:'iva-da-versare', label:'IVA da versare', values:ivaCum, fallback:'#d39b3b' },
    { key:'totale-tasse', label:'Totale tasse', values:totaleCum, fallback:'#e05a4f' },
    { key:'utile-netto-anno', label:'Utile netto anno', values:utileAnnoCum, fallback:'#22c55e' },
    { key:'netto-mensile', label:'Netto Mensile', values:nettoMensileCum, fallback:'#2aa36b' }
  ];
  return defs.map((item)=>({ ...item, color: __statChartLineColorFromRenderedCard__(safePage, item.key, item.fallback) }));
}

function drawStatAmministratoreLineChart(canvasId){
  const selected = __getStatChartFilter__('statamministratore');
  let seriesList = __statAmministratoreSeriesListFromGuests__(null, 'statamministratore').filter((item)=> __statChartSeriesIsVisible__('statamministratore', item.key));
  if (!seriesList.length){
    try{ __clearStatChartFilter__('statamministratore'); }catch(_){ }
    seriesList = __statAmministratoreSeriesListFromGuests__(null, 'statamministratore');
  }
  if (__ensureStatGenCompareEnabled__()){
    try{
      const compareYear = __ensureStatGenCompareYear__();
      const hasValidSnapshot = String(state.statGenCompareSnapshotYear || '') === String(compareYear) && state.statGenCompareSnapshot;
      if (!hasValidSnapshot && !state.statGenCompareLoading){ try{ __loadStatGenCompareGuests__({ force:true }); }catch(_){ } }
      const compareRows = hasValidSnapshot ? (state.statGenCompareSnapshot?.guests || []) : (Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests : []);
      const compareList = __statAmministratoreSeriesListFromGuests__(compareRows, 'statamministratore');
      const compareMap = {};
      compareList.forEach((item)=>{ compareMap[String(item.key||'')] = item; });
      const currentByKey = {};
      seriesList.forEach((item)=>{ currentByKey[String(item.key||'')] = item; });
      const compareKeys = selected ? [selected] : seriesList.map((item)=>String(item.key||'')).filter(Boolean);
      compareKeys.forEach((key)=>{
        const cmp = compareMap[key];
        if (!cmp || !Array.isArray(cmp.values) || !cmp.values.some((v)=>Math.abs(Number(v||0)||0)>0.0001)) return;
        const base = currentByKey[key] || cmp;
        seriesList.push({ key:key+'-compare-year', label:String(base.label||'Confronto')+' '+String(compareYear), values:cmp.values, color:base.color||'#9aa3af', dash:[6,4], pointFill:'#ffffff', lineWidth:1.8, radius:2.5, pointLineWidth:1.4 });
      });
    }catch(_){ }
  }
  __drawSharedMonthlyLineChart__(canvasId, (seriesList[0] && seriesList[0].values) ? seriesList[0].values : new Array(12).fill(0), {
    mode:'currency', seriesList,
    bubbleFormatter:(value)=>__statLineChartCompactEuro__(value),
    yTickFormatter:(value)=>__statLineChartCompactEuro__(value),
    pointAriaLabel:'Serie amministratore'
  });
}

function bindStatAmministratoreControls(){
  updateStatFiscalModeUI();
  __updateStatGenCompareYearButtonUI__();
  try{
    const btn = document.getElementById('statAmmFiscalModeBtn');
    if (btn && !btn.__boundFiscalMode){
      btn.__boundFiscalMode = true;
      let timer=null, fired=false;
      const clear=()=>{ if(timer){ clearTimeout(timer); timer=null; } };
      const block=(e)=>{ try{e&&e.preventDefault&&e.preventDefault();}catch(_){} try{e&&e.stopPropagation&&e.stopPropagation();}catch(_){} return false; };
      const start=(e)=>{ try{ if(e&&e.type==='pointerdown'&&e.pointerType==='mouse'&&e.button!==0) return; }catch(_){} fired=false; clear(); timer=setTimeout(()=>{ fired=true; __openStatFiscalModeButtonColorPicker__(); },500); };
      const stop=(e)=>{ clear(); if(fired){ block(e); setTimeout(()=>{fired=false;},0); } };
      btn.addEventListener('click',(e)=>{ if(fired) return block(e); setStatFiscalMode(getStatFiscalMode()==='societa'?'forfettario':'societa'); });
      ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt,start,{passive:true}); }catch(_){} });
      ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt,stop,{passive:false}); }catch(_){} });
      try{ btn.addEventListener('contextmenu',(e)=>block(e),true); }catch(_){}
    }
  }catch(_){ }
  try{
    const btn = document.getElementById('statAmmStartupRateBtn');
    if (btn && !btn.__boundStartupRate){
      btn.__boundStartupRate = true;
      let timer=null, fired=false;
      const clear=()=>{ if(timer){ clearTimeout(timer); timer=null; } };
      const block=(e)=>{ try{e&&e.preventDefault&&e.preventDefault();}catch(_){} try{e&&e.stopPropagation&&e.stopPropagation();}catch(_){} return false; };
      const start=(e)=>{ try{ if(e&&e.type==='pointerdown'&&e.pointerType==='mouse'&&e.button!==0) return; }catch(_){} fired=false; clear(); timer=setTimeout(()=>{ fired=true; __openStatFiscalModeButtonColorPicker__(); },500); };
      const stop=(e)=>{ clear(); if(fired){ block(e); setTimeout(()=>{fired=false;},0); } };
      btn.addEventListener('click',(e)=>{ if(fired) return block(e); setStatForfettarioStartup5(!getStatForfettarioStartup5()); });
      ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt,start,{passive:true}); }catch(_){} });
      ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt,stop,{passive:false}); }catch(_){} });
      try{ btn.addEventListener('contextmenu',(e)=>block(e),true); }catch(_){}
    }
  }catch(_){ }
  try{
    const btn = document.getElementById('statAmmCompareToggleBtn');
    if (btn && !btn.__boundAdvanced){
      btn.__boundAdvanced = true;
      let timer=null, fired=false;
      const clear=()=>{ if(timer){ clearTimeout(timer); timer=null; } };
      const block=(e)=>{ try{e&&e.preventDefault&&e.preventDefault();}catch(_){} try{e&&e.stopPropagation&&e.stopPropagation();}catch(_){} return false; };
      const start=(e)=>{ try{ if(e&&e.type==='pointerdown'&&e.pointerType==='mouse'&&e.button!==0) return; }catch(_){} fired=false; clear(); timer=setTimeout(()=>{ fired=true; try{btn.classList.add('is-pressing');}catch(_){} __openStatGenCompareYearButtonColorPicker__(); },500); };
      const stop=(e)=>{ clear(); if(fired){ block(e); setTimeout(()=>{fired=false; try{btn.classList.remove('is-pressing');}catch(_){}},0); return; } try{btn.classList.remove('is-pressing');}catch(_){} };
      btn.addEventListener('click',(e)=>{ if(fired) return block(e); __toggleStatGenCompareEnabled__(); });
      ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt,start,{passive:true}); }catch(_){} });
      ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt,stop,{passive:false}); }catch(_){} });
      try{ btn.addEventListener('contextmenu',(e)=>block(e),true); }catch(_){}
    }
    const yearBtn = document.getElementById('statAmmCompareYearBtn');
    if (yearBtn && !yearBtn.__boundOpen){
      yearBtn.__boundOpen = true;
      const open=(e)=>{ try{e&&e.preventDefault&&e.preventDefault();}catch(_){} try{e&&e.stopPropagation&&e.stopPropagation();}catch(_){} __openStatGenCompareYearPicker__(); };
      yearBtn.addEventListener('click', open);
      yearBtn.addEventListener('keydown',(e)=>{ if(e && (e.key==='Enter'||e.key===' ')) open(e); });
    }
  }catch(_){ }
}

function renderStatAmministratore(){
  bindStatAmministratoreControls();
  updateStatFiscalModeUI();
  __updateStatGenCompareYearButtonUI__();
  const s = computeStatAmministratoreFromGuests();
  state.statAmministratore = s;
  const set = (id, v) => { const el = document.getElementById(id); if (el) el.textContent = euro(Number(v || 0)); };
  set('saFatturato', s.fatturatoRicevute);
  set('saFatturatoSenza', s.fatturatoSenzaRicevuta);
  set('saIrpef', s.irpef);
  set('saInps', s.inps);
  set('saIva', s.ivaDaVersare);
  set('saTotaleTasse', s.totaleTasse);
  set('saUtileNettoAnno', s.utileSpendibileNettoAnno);
  set('saNettoMensile', s.nettoMensile);
  try{
    const ivaRow = document.getElementById('saIvaRow');
    if (ivaRow) ivaRow.hidden = (s.fiscalMode !== 'societa');
    const irpefName = document.getElementById('saIrpefName');
    if (irpefName) irpefName.textContent = s.fiscalMode === 'forfettario' ? `Imposta ${s.startup5 ? '5%' : '15%'}` : 'IRPEF';
  }catch(_){ }
  try{
    const rows = Array.from(document.querySelectorAll('#page-statamministratore .stat-row'));
    rows.forEach((row, idx)=>{
      const cardKey = String(row.dataset.statCardKey || `statamministratore-${idx+1}`);
      row.dataset.statCardKey = cardKey;
      const fallback = getComputedStyle(row).getPropertyValue('--mcol') || getComputedStyle(row).getPropertyValue('--statbg') || '#2B7CB4';
      __applyStatCardTextColor__(row, 'statamministratore', cardKey, fallback);
      __bindStatCardColorLongPress__(row, 'statamministratore', cardKey, fallback);
      __bindStatChartCardToggle__(row, 'statamministratore', cardKey);
      __setStatCardSelectionState__(row, 'statamministratore', cardKey);
      row.title = 'Pressione lunga per cambiare colori card';
    });
  }catch(_){ }
  try{ __applyStatisticsCardTheme__(); }catch(_){ }
  try{ __headerActionApplyAll__(); }catch(_){ }
  try{ __applyStatFiscalModeButtonVisual__(); }catch(_){ }
  try{ __applyStatGenCompareYearButtonVisual__(); }catch(_){ }
  try{
    const wrap = document.querySelector('#page-statamministratore .statgen-line-chart-wrap');
    if (wrap){
      wrap.dataset.statLineChartKey = 'statamministratore';
      __applyStatGenRegChartWrapVisual__(wrap);
      __bindStatSharedLineChartLongPress__(wrap);
      __bindStatChartWrapReset__('statamministratore');
      wrap.title = 'Pressione lunga per cambiare design grafico';
    }
  }catch(_){ }
  try{ drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
  try{ __scheduleStatsLineChartRedraw__('statamministratore'); }catch(_){ }
  try{ __scheduleStatLandscapeGraphOnlyRefresh__(); }catch(_){ }
  try{ __loadStatGenCompareGuests__({ force:false }); }catch(_){ }
}

function saveStatAmministratore(){
  if (getStatFiscalMode() === "forfettario"){
    closeAdminInputsModal();
    try{ toast("Compenso e TFM esclusi dal forfettario"); }catch(_){ }
    try{ renderStatAmministratore(); }catch(_){ }
    return;
  }
  const cIn = document.getElementById("adminCompensoInput");
  const tIn = document.getElementById("adminTfmInput");
  if (!cIn || !tIn) return;

  const compenso = _num(cIn.value);
  const tfm = _num(tIn.value);

  _lsSetNum(LS_ADMIN_COMPENSO, compenso);
  _lsSetNum(LS_ADMIN_TFM, tfm);

  toast("Salvato");
  try{ if (state.page === "statazienda") renderStatAzienda(); }catch(_){ }
  try{ if (state.page === "statamministratore") renderStatAmministratore(); }catch(_){ }
}

/* IRAP modal */
function openIrapModal(){
  const m = document.getElementById("irapModal");
  const input = document.getElementById("irapPctInput");
  if (!m || !input) return;
  const v = _lsGetNum(LS_IRAP_PCT, 3.9);
  input.value = String(v).replace(".", ",");
  m.hidden = false;
  m.setAttribute("aria-hidden","false");
  setTimeout(()=>{ try{ input.focus(); input.select(); }catch(_){ } }, 0);
}
function closeIrapModal(){
  const m = document.getElementById("irapModal");
  if (!m) return;
  m.hidden = true;
  m.setAttribute("aria-hidden","true");
}
function saveIrapModal(){
  const input = document.getElementById("irapPctInput");
  if (!input) return;
  const v = _num(input.value);
  _lsSetNum(LS_IRAP_PCT, v);
  closeIrapModal();
  toast("IRAP salvata");
  try{ if (state.page === "statazienda") renderStatAzienda(); }catch(_){ }
  try{ if (state.page === "statamministratore") renderStatAmministratore(); }catch(_){ }
}

function computeStatSpese(){
  const items = __getStatsSpese();
  const acc = { CONTANTI:0, TASSA_SOGGIORNO:0, IVA_22:0, IVA_10:0, IVA_4:0 };

  const money = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === "number") return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    if (s.includes(",") && s.includes(".")) s = s.replace(/\./g, "").replace(",", ".");
    else if (s.includes(",")) s = s.replace(",", ".");
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };

  for (const s of items){
    const lordo = money(s?.importoLordo ?? s?.lordo ?? 0);
    if (!isFinite(lordo) || lordo === 0) continue;

    const catRaw = (s?.categoria ?? s?.cat ?? "").toString().trim().toLowerCase();

    if (catRaw.includes("contant")) { acc.CONTANTI += lordo; continue; }
    if (catRaw.includes("tassa") && catRaw.includes("sogg")) { acc.TASSA_SOGGIORNO += lordo; continue; }

    if (catRaw.includes("iva")){
      if (catRaw.includes("22")) { acc.IVA_22 += lordo; continue; }
      if (catRaw.includes("10")) { acc.IVA_10 += lordo; continue; }
      if (catRaw.includes("4")) { acc.IVA_4 += lordo; continue; }
    }

    // fallback su aliquota numerica
    const n = parseFloat(String(s?.aliquotaIva ?? s?.aliquota_iva ?? "").replace(",","."));
    if (!isNaN(n)){
      if (n >= 21.5) acc.IVA_22 += lordo;
      else if (n >= 9.5 && n < 11.5) acc.IVA_10 += lordo;
      else if (n >= 3.5 && n < 5.5) acc.IVA_4 += lordo;
      else if (__isFuoriBudgetSpesa__(s)) acc.CONTANTI += lordo;
    } else if (__isFuoriBudgetSpesa__(s)) {
      acc.CONTANTI += lordo;
    }
  }

  return {
    totale: (acc.CONTANTI + acc.TASSA_SOGGIORNO + acc.IVA_22 + acc.IVA_10 + acc.IVA_4),
    contanti: acc.CONTANTI,
    tassaSoggiorno: acc.TASSA_SOGGIORNO,
    iva22: acc.IVA_22,
    iva10: acc.IVA_10,
    iva4: acc.IVA_4,
  };
}// ===== Report locale (per-account): calcolato dalla lista spese corrente =====
function buildReportFromSpese(items){
  const list = (Array.isArray(items) ? items : []).filter((row)=> !__isFuoriBudgetSpesa__(row));
  const money = (v) => {
    if (v === null || v === undefined) return 0;
    if (typeof v === "number") return isFinite(v) ? v : 0;
    let s = String(v).trim();
    if (!s) return 0;
    if (s.includes(",") && s.includes(".")) s = s.replace(/\./g, "").replace(",", ".");
    else if (s.includes(",")) s = s.replace(",", ".");
    const n = Number(s);
    return isFinite(n) ? n : 0;
  };

  const normCat = (row) => {
    const catRaw = (row?.categoria ?? row?.cat ?? "").toString().trim().toLowerCase();
    const aliqRaw = (row?.aliquotaIva ?? row?.aliquota_iva ?? "").toString().trim();

    if (catRaw.includes("contant")) return "CONTANTI";
    if (catRaw.includes("tassa") && catRaw.includes("sogg")) return "TASSA_SOGGIORNO";

    if (catRaw.includes("iva")){
      if (catRaw.includes("22")) return "IVA_22";
      if (catRaw.includes("10")) return "IVA_10";
      if (catRaw.includes("4")) return "IVA_4";
    }

    const n = parseFloat(aliqRaw.replace(",", "."));
    if (!isNaN(n)){
      if (n >= 21.5) return "IVA_22";
      if (n >= 9.5 && n < 11.5) return "IVA_10";
      if (n >= 3.5 && n < 5.5) return "IVA_4";
    }
    return null;
  };

  const totals = { importoLordo:0, imponibile:0, iva:0, ivaDetraibile:0 };
  const byCategoria = {
    CONTANTI:{ importoLordo:0, imponibile:0, iva:0, ivaDetraibile:0 },
    TASSA_SOGGIORNO:{ importoLordo:0, imponibile:0, iva:0, ivaDetraibile:0 },
    IVA_22:{ importoLordo:0, imponibile:0, iva:0, ivaDetraibile:0 },
    IVA_10:{ importoLordo:0, imponibile:0, iva:0, ivaDetraibile:0 },
    IVA_4:{ importoLordo:0, imponibile:0, iva:0, ivaDetraibile:0 },
  };

  for (const row of list){
    const lordo = money(row?.importoLordo ?? row?.lordo ?? row?.importo ?? 0);
    let imponibile = money(row?.imponibile ?? 0);
    let iva = money(row?.iva ?? 0);
    let ivaDet = money(row?.ivaDetraibile ?? row?.iva_detraibile ?? 0);

    // Se mancano imponibile/iva ma ho aliquota e lordo, ricava
    if ((imponibile === 0 && iva === 0) && lordo > 0){
      const aliqRaw = (row?.aliquotaIva ?? row?.aliquota_iva ?? "").toString().trim();
      const rate = parseFloat(aliqRaw.replace(",", "."));
      if (!isNaN(rate) && rate > 0){
        const imp = lordo / (1 + rate/100);
        const iv = lordo - imp;
        if (isFinite(imp)) imponibile = imp;
        if (isFinite(iv)) iva = iv;
      }
    }

    // Se non c'e' ivaDetraibile, usa iva come fallback (mantiene KPI coerenti)
    if (!ivaDet && iva) ivaDet = iva;

    totals.importoLordo += lordo;
    totals.imponibile += imponibile;
    totals.iva += iva;
    totals.ivaDetraibile += ivaDet;

    const k = normCat(row);
    if (k && byCategoria[k]){
      byCategoria[k].importoLordo += lordo;
      byCategoria[k].imponibile += imponibile;
      byCategoria[k].iva += iva;
      byCategoria[k].ivaDetraibile += ivaDet;
    }
  }

  return { totals, byCategoria, source: "local_spese" };
}


function __speseGraphSlicesBase__(){
  return [
    { key:"CONTANTI", label: categoriaLabel("CONTANTI"), value:Number((state && state.statSpese && state.statSpese.contanti) || 0), color:(COLORS.CONTANTI || "#2b7cb4") },
    { key:"TASSA_SOGGIORNO", label: categoriaLabel("TASSA_SOGGIORNO"), value:Number((state && state.statSpese && state.statSpese.tassaSoggiorno) || 0), color:(COLORS.TASSA_SOGGIORNO || "#d8bd97") },
    { key:"IVA_22", label: categoriaLabel("IVA_22"), value:Number((state && state.statSpese && state.statSpese.iva22) || 0), color:(COLORS.IVA_22 || "#c9772b") },
    { key:"IVA_10", label: categoriaLabel("IVA_10"), value:Number((state && state.statSpese && state.statSpese.iva10) || 0), color:(COLORS.IVA_10 || "#7ac0db") },
    { key:"IVA_4", label: categoriaLabel("IVA_4"), value:Number((state && state.statSpese && state.statSpese.iva4) || 0), color:(COLORS.IVA_4 || "#1f2937") },
  ];
}

function __speseGraphSlicesCustom__(){
  return __applyGraphCustomColors__('spese', __speseGraphSlicesBase__());
}

function __syncStatSpeseCardColors__(){
  const rows = {
    CONTANTI: document.getElementById('ssRowContanti'),
    TASSA_SOGGIORNO: document.getElementById('ssRowTassa'),
    IVA_22: document.getElementById('ssRowIva22'),
    IVA_10: document.getElementById('ssRowIva10'),
    IVA_4: document.getElementById('ssRowIva4')
  };
  const custom = __speseGraphSlicesCustom__();
  custom.forEach((slice)=>{
    const row = rows[String(slice && slice.key || '')];
    if (!row) return;
    const hex = __graphColorValueToHex__(slice && slice.color, '#2B7CB4');
    row.style.setProperty('--statbg', hex);
    try{
      const parsed = parseInt(hex.slice(1), 16);
      const r = (parsed >> 16) & 255, g = (parsed >> 8) & 255, b = parsed & 255;
      const luminance = (0.2126*r + 0.7152*g + 0.0722*b) / 255;
      row.style.setProperty('--statfg', luminance > 0.72 ? 'rgba(15,23,42,0.92)' : '#ffffff');
    }catch(_){
      row.style.setProperty('--statfg', '#ffffff');
    }
  });
}

function __getSpeseGraphSliceColor__(key, fallback){
  try{
    const slices = __speseGraphSlicesCustom__();
    const found = (Array.isArray(slices) ? slices : []).find((slice)=>String(slice && slice.key || '') === String(key || ''));
    if (found && found.color) return __graphColorValueToHex__(found.color, fallback || '#2B7CB4');
  }catch(_){ }
  return __graphColorValueToHex__(fallback || '#2B7CB4', '#2B7CB4');
}

function __applySpesaCardColor__(el, row){
  if (!el) return;
  try{
    const key = __spesaVisualKeyForRow__(row);
    if (!key) return;
    const fallbackSpec = __spesaVisualFallbackSpecForRow__(row);
    const visual = __spesaCardVisualForKey__(key, fallbackSpec);
    const bgHex = __operatoreColorHex__(visual.bg || __closestGraphColorSpec__(fallbackSpec || '#2B7CB4'));
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || __closestGraphColorSpec__(fallbackSpec || '#2B7CB4'));
    const fgHex = visual.fg ? __operatoreColorHex__(visual.fg) : '';
    const parsed = parseInt(String(bgHex || '#2B7CB4').slice(1), 16);
    const r = (parsed >> 16) & 255;
    const g = (parsed >> 8) & 255;
    const b = parsed & 255;
    const borderParsed = parseInt(String(borderHex || bgHex || '#2B7CB4').slice(1), 16);
    const br = (borderParsed >> 16) & 255;
    const bgv = (borderParsed >> 8) & 255;
    const bb = borderParsed & 255;
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.22);
    const darkOpacity = Math.max(0.06, Math.min(0.18, opacity));
    const borderOpacity = Math.max(0.12, Math.min(0.50, opacity + 0.10));
    const darkBorderOpacity = Math.max(0.16, Math.min(0.36, darkOpacity + 0.10));
    el.style.setProperty('--spesa-accent-rgb', `${r}, ${g}, ${b}`);
    el.style.setProperty('--spesa-accent', bgHex);
    el.style.setProperty('--spesa-border-rgb', `${br}, ${bgv}, ${bb}`);
    el.style.setProperty('--spesa-card-opacity', String(opacity));
    el.style.setProperty('--spesa-card-opacity-dark', String(darkOpacity));
    el.style.setProperty('--spesa-card-border-opacity', String(borderOpacity));
    el.style.setProperty('--spesa-card-border-opacity-dark', String(darkBorderOpacity));
    const dark = !!(__isDarkModeEnabled__ && __isDarkModeEnabled__());
    const resolvedBorderOpacity = dark ? darkBorderOpacity : borderOpacity;
    el.style.setProperty('border-style', 'solid');
    el.style.setProperty('border-width', '1px');
    el.style.setProperty('border-color', `rgba(${br}, ${bgv}, ${bb}, ${resolvedBorderOpacity})`);
    if (fgHex){
      el.style.setProperty('--spesa-text-color', fgHex);
      el.style.setProperty('--spesa-text-color-dark', fgHex);
    }else{
      el.style.removeProperty('--spesa-text-color');
      el.style.removeProperty('--spesa-text-color-dark');
    }
  }catch(_){ }
}

function renderStatSpese(){
  try{ __syncSpeseBudgetToggles__(); }catch(_){ }
  try{ const title = document.querySelector('#page-statspese .stats-title'); if (title) title.textContent = __speseBudgetModeIsFuori__() ? 'Spese fuori budget' : 'Spese generali'; }catch(_){ }
  const s = computeStatSpese();
  state.statSpese = s;

  const set = (id, v) => {
    const el = document.getElementById(id);
    if (el) el.textContent = euro(Number(v || 0));
  };

  set("ssTotaleSpese", s.totale);

  set("ssContanti", s.contanti);
  set("ssTassa", s.tassaSoggiorno);
  set("ssIva22", s.iva22);
  set("ssIva10", s.iva10);
  set("ssIva4", s.iva4);
  __syncStatSpeseCardColors__();
  try{
    const rows = Array.from(document.querySelectorAll('#page-statspese .stat-row'));
    const keys = ['totale-spese','contanti','tassa-soggiorno','iva-22','iva-10','iva-4'];
    rows.forEach((row, idx)=>{
      const explicitKey = String(keys[idx] || `statspese-${idx+1}`);
      const cardKey = explicitKey;
      row.dataset.statCardKey = cardKey;
      const fallback = getComputedStyle(row).getPropertyValue('--mcol') || getComputedStyle(row).getPropertyValue('--statbg') || '#2B7CB4';
      __applyStatCardTextColor__(row, 'statspese', cardKey, fallback);
      __bindStatCardColorLongPress__(row, 'statspese', cardKey, fallback);
      __bindStatChartCardToggle__(row, 'statspese', cardKey);
      __setStatCardSelectionState__(row, 'statspese', cardKey);
      row.title = 'Pressione lunga per cambiare colori card';
    });
  }catch(_){ }

  // Dettaglio elenco spese (visibile in Statistiche → Spese)
  const list = document.getElementById("statSpeseList");
  try{ __applyStatisticsCardTheme__(); }catch(_){ }
  try{
    const wrapChart = document.querySelector('#page-statspese .statgen-line-chart-wrap');
    if (wrapChart){
      __applyStatGenRegChartWrapVisual__(wrapChart);
      __bindStatSharedLineChartLongPress__(wrapChart);
      __bindStatChartWrapReset__('statspese');
      wrapChart.title = 'Pressione lunga per cambiare design grafico';
    }
  }catch(_){ }
  try{ drawStatSpesePercentLineChart('statSpeseLineChart'); }catch(_){ }
  try{ __scheduleStatsLineChartRedraw__('statspese'); }catch(_){ }
  try{ __scheduleStatLandscapeGraphOnlyRefresh__(); }catch(_){ }

  if (list){
    list.innerHTML = "";
    let items = Array.isArray(__getStatsSpese()) ? [...__getStatsSpese()] : [];

    const toTime = (v) => {
      if (!v) return null;
      const s = String(v);
      const iso = s.slice(0, 10);
      if (/^\d{4}-\d{2}-\d{2}$/.test(iso)){
        const t = Date.parse(iso + "T00:00:00Z");
        return isNaN(t) ? null : t;
      }
      const t = Date.parse(s);
      return isNaN(t) ? null : t;
    };

    items.sort((a,b) => {
      const da = toTime(a.dataSpesa || a.data || a.data_spesa);
      const db = toTime(b.dataSpesa || b.data || b.data_spesa);
      if (da == null && db == null) return 0;
      if (da == null) return 1;
      if (db == null) return -1;
      return db - da;
    });

    if (!items.length){
      list.innerHTML = `<div style="font-size:13px; opacity:.75; padding:8px 2px;">Nessuna spesa nel periodo.</div>`;
    } else {
      items.forEach((sp) => {
        const el = document.createElement("div");
        el.className = "item spesa-bg";
        try{ el.__spesaRow = sp; }catch(_){ }
        const cls = spesaCategoryClass(sp);
        if (cls) el.classList.add(cls);

        const importo = Number(sp.importoLordo || 0);
        const data = formatShortDateIT(sp.dataSpesa || sp.data || sp.data_spesa || "");
        const motivoTxt = (sp.motivazione || sp.motivo || "").toString();
        const motivo = escapeHtml(motivoTxt);
        const categoria = escapeHtml(__spesaCategoriaDisplay__(sp));

        el.innerHTML = `
          <div class="item-top" style="align-items:center;">
            <div class="spesa-line" title="${motivo}">
              <span class="spesa-imp">${euro(importo)}</span>
              <span class="spesa-sep">·</span>
              <span class="spesa-date">${data}</span>
              <span class="spesa-sep">·</span>
              <span class="spesa-motivo">${motivo}</span>${categoria ? `<span class="spesa-sep">·</span><span class="spesa-cat">${categoria}</span>` : ''}
            </div>
          </div>
        `;
        list.appendChild(el);
      });
    }
  }
}

function openStatSpesePieModal(){
  try{
    if (!state.statSpese) state.statSpese = computeStatSpese();
  }catch(_){ state.statSpese = state.statSpese || null; }

  const m = document.getElementById("statSpesePieModal");
  if (!m) return;
  m.hidden = false;
  m.setAttribute("aria-hidden", "false");

  const s = state.statSpese || computeStatSpese();
  state.statSpese = s;
  const slices = __speseGraphSlicesCustom__().map((slice)=>({
    ...slice,
    value: ({ CONTANTI:s.contanti, TASSA_SOGGIORNO:s.tassaSoggiorno, IVA_22:s.iva22, IVA_10:s.iva10, IVA_4:s.iva4 }[slice.key] ?? slice.value ?? 0)
  }));

  drawPie("statSpesePieCanvas", slices);

  const leg = document.getElementById("statSpesePieLegend");
  if (leg){
    const total = slices.reduce((a,x)=>a+Math.max(0,Number(x.value||0)),0);
    leg.innerHTML = "";
    slices.forEach((sl)=>{
      const v = Math.max(0, Number(sl.value || 0));
      const pct = total > 0 ? (v/total*100) : 0;
      const row = document.createElement("div");
      row.className = "legrow";
      row.innerHTML = `
        <div class="legleft">
          <div class="dot" style="background:${sl.color}"></div>
          <div class="legname">${escapeHtml(sl.label)}</div>
        </div>
        <div class="legright">${pct.toFixed(1)}% · ${euro(v)}</div>
      `;
      leg.appendChild(row);
    });
  }
}

function closeStatSpesePieModal(){
  const m = document.getElementById("statSpesePieModal");
  if (!m) return;
  m.hidden = true;
  m.setAttribute("aria-hidden", "true");
}

/* Wire buttons */


function bindPeriodAuto(fromSel, toSel){
  const fromEl = document.querySelector(fromSel);
  const toEl = document.querySelector(toSel);
  if (!fromEl || !toEl) return;

  let timer = null;

  const schedule = () => {
    if (periodSyncLock > 0) return; // update programmatici: ignora
    if (timer) clearTimeout(timer);
    timer = setTimeout(async () => {
      if (periodSyncLock > 0) return;
      const from = fromEl.value;
      const to = toEl.value;

      if (!from || !to) return;
      if (from > to) {
        toast("Periodo non valido");
        return;
      }

      setPresetValue("custom");
      setPeriod(from, to);

      try { await onPeriodChanged({ showLoader:false }); } catch (e) { toast(e.message); }
    }, 220);
  };

  fromEl.addEventListener("change", schedule);
  toEl.addEventListener("change", schedule);
}

function bindPeriodAutoGuests(fromSel, toSel){
  const fromEl = document.querySelector(fromSel);
  const toEl = document.querySelector(toSel);
  if (!fromEl || !toEl) return;

  let timer = null;

  const schedule = () => {
    if (periodSyncLock > 0) return;
    if (timer) clearTimeout(timer);
    timer = setTimeout(async () => {
      if (periodSyncLock > 0) return;
      const from = fromEl.value;
      const to = toEl.value;
      if (!from || !to) return;

      // valida
      if (from > to){
        toast("Periodo non valido");
        return;
      }

      setPresetValue("custom");
      setPeriod(from, to);

      try { await loadOspiti({ from, to }); } catch (e) { toast(e.message); }
    }, 220);
  };

  fromEl.addEventListener("change", schedule);
  toEl.addEventListener("change", schedule);
}






function updateGuestRemaining(){
  const out = document.getElementById("guestRemaining");
  if (!out) return;

  const totalEl = document.getElementById("guestTotal");
  const servicesEl = document.getElementById("guestServices");
  const discountEl = document.getElementById("guestDiscount");
  const depEl = document.getElementById("guestDeposit");
  const saldoEl = document.getElementById("guestSaldo");

  const totalStr = (totalEl?.value ?? "");
  const servicesStr = (servicesEl?.value ?? "");
  const discountStr = (discountEl?.value ?? "");
  const depStr = (depEl?.value ?? "");
  const saldoStr = (saldoEl?.value ?? "");

  const anyFilled = [totalStr, servicesStr, discountStr, depStr, saldoStr].some(s => String(s).trim().length > 0);
  if (!anyFilled) {
    out.value = "";
    try { refreshFloatingLabels(); } catch (_) {}
    return;
  }

  const total = parseFloat(totalStr || "0") || 0;
  const services = parseFloat(servicesStr || "0") || 0;
  const discount = parseFloat(discountStr || "0") || 0;
  const deposit = parseFloat(depStr || "0") || 0;
  const saldo = parseFloat(saldoStr || "0") || 0;
  const remaining = (total + services) - discount - deposit - saldo;

  out.value = (isFinite(remaining) ? remaining.toFixed(2) : "");
  try { refreshFloatingLabels(); } catch (_) {}
}

function recalcGuestCommission(){
  const totalEl = document.getElementById("guestTotal");
  const pctEl = document.getElementById("guestChannelCommission");
  const outEl = document.getElementById("guestBooking");
  if (!outEl) return;
  const totalRaw = String(totalEl?.value ?? "").trim();
  const pctRaw = String(pctEl?.value ?? "").trim();
  if (!totalRaw && !pctRaw){
    outEl.value = "";
    try { refreshFloatingLabels(); } catch (_) {}
    return;
  }
  const total = parseFloat(totalRaw || "0") || 0;
  const pct = parseFloat(pctRaw || "0") || 0;
  const commission = Math.round((total * pct) * 100) / 10000;
  outEl.value = (isFinite(commission) ? commission.toFixed(2) : "");
  try { refreshFloatingLabels(); } catch (_) {}
}

function updateGuestPriceVisibility(){
  try{
    const mode = String(state.guestMode || '').toLowerCase();
    const isCreate = (mode === 'create');
    const isCreateFromGroup = !!state.guestCreateFromGroup;

    // dDAE_2.827 — nella sola scheda di creazione nuovo ospite
    // non devono comparire sezioni servizi/pagamenti extra.
    const createOnlyRows = ['servicesRow','depositRow','saldoRow','discountRow','remainingRow','servicesListWrap'];
    createOnlyRows.forEach((id) => {
      const el = document.getElementById(id);
      if (!el) return;
      el.hidden = !!isCreate;
      el.classList.toggle('ddae-create-hidden', !!isCreate);
      if (isCreate){
        el.setAttribute('aria-hidden', 'true');
      } else {
        el.removeAttribute('aria-hidden');
      }
    });

    // In create da gruppo esistente restano nascosti anche totale/commissione
    // come da comportamento precedente, senza impattare creazione normale.
    const hideGroupPrice = (isCreate && isCreateFromGroup);
    ['guestTotal','guestBooking'].forEach((id) => {
      const el = document.getElementById(id);
      if (!el) return;
      const field = el.closest('.field');
      if (field) field.hidden = hideGroupPrice;
    });

    ['depositType','saldoType','regTags'].forEach((id) => {
      const el = document.getElementById(id);
      if (!el) return;
      const row = el.closest('.field.two-col.payment-row');
      if (row) row.hidden = !!isCreate;
    });
  }catch(_){ }
}


function enterGuestCreateMode(){
  setGuestFormViewOnly(false);

  state.guestViewItem = null;

  // Multi prenotazioni: quando si crea da un gruppo esistente, possiamo nascondere i prezzi
  state.guestCreateFromGroup = false;

  // Multi prenotazioni: reset contesto
  state.guestGroupBookings = null;
  state.guestGroupActiveId = null;
  state.guestGroupKey = null;
  try{ clearGuestMulti(); }catch(_){ }

  state.guestMode = "create";
  try{ updateGuestFormModeClass(); }catch(_){ }
  try{ syncGuestCreatePrimaryStack(); }catch(_){ }
  state.guestEditId = null;
  state.guestEditCreatedAt = null;
  state.guestEditCardKey = null;
  state.guestCreateFromGroupCardKey = null;

  try{ ensureRoomsPickerButtons(); }catch(_){ }
  const title = document.getElementById("ospiteFormTitle");
  if (title) title.textContent = "Nuovo ospite";
  try{ __syncGuestNationalityPlacement__(); }catch(_){}
  const btn = document.getElementById("createGuestCard");
  if (btn) btn.textContent = "Crea ospite";
  try{ const row = document.getElementById('guestCreateChoiceRow'); if (row) row.hidden = false; if (btn) btn.hidden = true; }catch(_){ }
  // dDAE: in modal 'Nuovo ospite' la tassa di soggiorno non deve comparire
  try{
    const taxPill = document.getElementById('guestTaxTotal');
    const taxVal = document.getElementById('guestTaxTotalVal');
    if (taxVal) taxVal.textContent = '€0,00';
    if (taxPill) taxPill.hidden = true;
  }catch(_){ }


  // reset fields
  const fields = ["guestName","guestPhone","guestNationality","guestEmail","guestBookingNumber","guestMen","guestWomen","guestAdults","guestKidsU10","guestCheckOut","guestTotal","guestChannel","guestChannelCommission","guestBooking","guestServices","guestDiscount","guestDeposit","guestSaldo","guestRemaining","guestNotes"];
  fields.forEach(id => { const el = document.getElementById(id); if (el) el.value = ""; });
  try{ __applyGuestNameUppercase__(document.getElementById("guestName")); }catch(_){ }
  // reset servizi state
  try{ state.guestServicesItems = []; state.guestServicesComputedTotal = 0; state.guestServicesManualOverride = false; state.guestServicesLoadedFor = null; }catch(_){ }
  try { updateGuestRemaining(); } catch (_) {}

  const ci = document.getElementById("guestCheckIn");
  if (ci) ci.value = todayISO();
  try{ const co = document.getElementById("guestCheckOut"); if (co) co.value = ci ? (ci.value || todayISO()) : todayISO(); }catch(_){ }
  try{ __updateGuestDateRangeTrigger__(); }catch(_){ }
  try{ populateGuestChannelOptions(); }catch(_){ }
  try{ applySelectedChannelToGuestForm(""); }catch(_){ }
  try{ setGuestNationality("", { manual:false }); }catch(_){ }
  try{ state.guestGender = ""; __syncGuestGenderTabs__(); }catch(_){ }

  setMarriage(false);
  setGroup(false);
  setColC(false);
  state.guestRooms = state.guestRooms || new Set();
  state.guestRooms.clear();
  state.lettiPerStanza = {};
  state.bedsDirty = false;
  state.stanzeSnapshotOriginal = "";

  // Pagamenti (pillole): default contanti + ricevuta OFF
  state.guestDepositType = "";
  state.guestSaldoType = "";
  state.guestDepositReceipt = false;
  state.guestSaldoReceipt = false;

  setPayType("depositType", state.guestDepositType);
  setPayType("saldoType", state.guestSaldoType);
  setPayReceipt("depositType", state.guestDepositReceipt);
  setPayReceipt("saldoType", state.guestSaldoReceipt);


  // Registrazioni (PS/ISTAT): default OFF
  state.guestPSRegistered = false;
  state.guestISTATRegistered = false;
  state.guestCheckInDone = false;
  setRegFlags("regTags", state.guestPSRegistered, state.guestISTATRegistered);
  try{ __syncGuestCheckInButton__(); }catch(_){ }
  // refresh rooms UI if present
  try {
    document.querySelectorAll("#roomsPicker .room-dot").forEach(btn => {
      btn.classList.remove("selected");
      btn.setAttribute("aria-pressed", "false");
    });
  } catch (_) {}
  try { updateOspiteHdActions(); } catch (_) {}


  try { updateGuestPriceVisibility(); } catch (_) {}
  try{ syncGuestNotesUI(null, { open:false }); }catch(_){}

  // (Create mode) nulla da fare sulle stanze: la disponibilita' si aggiorna quando l'utente inserisce le date.
}

function enterGuestEditMode(ospite){
  try{ ensureRoomsPickerButtons(); }catch(_){ }
  setGuestFormViewOnly(false);

  state.guestCreateFromGroup = false;

  state.guestViewItem = null;

  // Snapshot del record originale: serve per tornare alla scheda in sola lettura
  // dalla modalità "modifica" senza dipendere dai campi eventualmente alterati.
  try{ state.guestEditSourceItem = ospite ? JSON.parse(JSON.stringify(ospite)) : null; }catch(_){ state.guestEditSourceItem = ospite || null; }

  // ✅ FIX dDAE: evita "leak" delle stanze tra prenotazioni multiple (multi booking).
  // Quando si passa da un gruppo all'altro, se il nuovo record non ha 'stanze' valorizzate,
  // lo stato precedente poteva rimanere e finire dentro il nuovo salvataggio.
  try {
    state.guestRooms = state.guestRooms || new Set();
    state.guestRooms.clear();
    state.lettiPerStanza = {};
    state.bedsDirty = false;
    state.stanzeSnapshotOriginal = "";
    document.querySelectorAll("#roomsPicker .room-dot").forEach(btn => {
      if (btn.id === "roomMarriage" || btn.id === "roomGroup") return;
      btn.classList.remove("selected");
      btn.setAttribute("aria-pressed", "false");
    });
  } catch (_) {}

  state.guestMode = "edit";
  try{ updateGuestFormModeClass(); }catch(_){ }
  try{ syncGuestCreatePrimaryStack(); }catch(_){ }
  state.guestEditId = ospite?.id ?? null;
  state.guestEditCardKey = __guestCardKeyOf__(ospite);
  

  // Servizi: prepara cache locale per apertura istantanea (layout invariato)
  try{
    state.guestServicesManualOverride = false;
    const ospiteId = guestIdOf(ospite) || (ospite?.id ?? null);
    const key = ospiteId ? String(ospiteId) : "";
    const cache = (key && state.guestServicesCacheById) ? state.guestServicesCacheById[key] : null;

    state.guestServicesLoadingFor = null;

    if (cache && Array.isArray(cache.items)){
      state.guestServicesItems = cache.items.slice();
      state.guestServicesComputedTotal = isFinite(cache.total) ? cache.total : serviziComputeTotal(state.guestServicesItems);
      state.guestServicesLoadedFor = ospiteId;
      state.guestServicesLoadedAt = cache.loadedAt || 0;
    } else {
      state.guestServicesLoadedFor = null;
      state.guestServicesLoadedAt = 0;
      state.guestServicesItems = [];
      state.guestServicesComputedTotal = 0;
    }
  }catch(_){ }
  // Precarica servizi in background (UI apre subito)
  try{ setTimeout(() => { try{ loadServiziForOspite(ospite); }catch(_){} }, 0); }catch(_){}


state.guestEditCreatedAt = (ospite?.created_at ?? ospite?.createdAt ?? null);

  const title = document.getElementById("ospiteFormTitle");
  if (title) title.textContent = "Modifica ospite";
  try{ __syncGuestNationalityPlacement__(); }catch(_){}
  const btn = document.getElementById("createGuestCard");
  if (btn) btn.textContent = "Salva modifiche";
  try{ const row = document.getElementById('guestCreateChoiceRow'); if (row) row.hidden = true; if (btn) btn.hidden = false; }catch(_){ }

  document.getElementById("guestName").value = ospite.nome || ospite.name || "";
  try{ __applyGuestNameUppercase__(document.getElementById("guestName")); }catch(_){ }
  document.getElementById("guestPhone").value = ospite.telefono ?? ospite.tel ?? ospite.phone ?? "";
  try{ setGuestNationality(__readGuestNationalityFromRecord__(ospite).code || "", { manual:true }); }catch(_){}
  document.getElementById("guestEmail").value = ospite.email ?? ospite.mail ?? "";
  try{ document.getElementById("guestMen").value = __readGuestPeopleCount__(ospite, 'men'); }catch(_){ }
  try{ document.getElementById("guestWomen").value = __readGuestPeopleCount__(ospite, 'women'); }catch(_){ }
  try{ state.guestGender = __readGuestGender__(ospite); __syncGuestGenderTabs__(); }catch(_){ }
  document.getElementById("guestBookingNumber").value = ospite.numero_prenotazione ?? ospite.numeroPrenotazione ?? ospite.prenotazione_numero ?? ospite.prenotazioneNumero ?? ospite.booking_number ?? ospite.bookingNumber ?? ospite.reservation_number ?? ospite.reservationNumber ?? ospite.id_prenotazione ?? ospite.idPrenotazione ?? ospite.id_booking ?? ospite.idBooking ?? "";
  document.getElementById("guestAdults").value = ospite.adulti ?? ospite.adults ?? 0;
  document.getElementById("guestKidsU10").value = ospite.bambini_u10 ?? ospite.kidsU10 ?? 0;
  document.getElementById("guestCheckIn").value = formatISODateLocal(ospite.check_in || ospite.checkIn || "") || "";
  document.getElementById("guestCheckOut").value = formatISODateLocal(ospite.check_out || ospite.checkOut || "") || "";
  try{ __updateGuestDateRangeTrigger__(); }catch(_){ }
  document.getElementById("guestTotal").value = ospite.importo_prenotazione ?? ospite.total ?? 0;
  try{ populateGuestChannelOptions(ospite.channel_id ?? ospite.channelId ?? ""); }catch(_){ }
  const __guestChannelCommission = (ospite.channel_commissione ?? ospite.channelCommissione ?? ospite.channel_commission_pct ?? ospite.channelCommissionPct ?? 0);
  document.getElementById("guestChannelCommission").value = (__guestChannelCommission === null || __guestChannelCommission === undefined || __guestChannelCommission === "") ? "" : __guestChannelCommission;
  try{ document.getElementById("guestChannel").value = String(ospite.channel_id ?? ospite.channelId ?? ""); }catch(_){ }
  try{ applySelectedChannelToGuestForm(ospite.channel_id ?? ospite.channelId ?? "", { preserveManual:true }); }catch(_){ }
  document.getElementById("guestBooking").value = ospite.importo_booking ?? ospite.booking ?? 0;
  document.getElementById("guestServices").value = ospite.servizi_totale ?? ospite.serviziTotal ?? ospite.importo_servizi ?? 0;
  document.getElementById("guestDiscount").value = ospite.sconto ?? ospite.discount ?? 0;
  document.getElementById("guestDeposit").value = ospite.acconto_importo ?? ospite.deposit ?? 0;
  document.getElementById("guestSaldo").value = ospite.saldo_pagato ?? ospite.saldoPagato ?? ospite.saldo ?? 0;
  try{ syncGuestNotesUI(ospite, { open:false }); }catch(_){}

  // matrimonio / G
  state.guestMarriage = !!(ospite.matrimonio);
  setMarriage(state.guestMarriage);
  state.guestGroup = !!(truthy(ospite.g ?? ospite.flag_g ?? ospite.gruppo_g ?? ospite.group ?? ospite.G ?? ospite.g_flag));
  setGroup(state.guestGroup);
  state.guestColC = !!(truthy(ospite.col_c ?? ospite.colC ?? ospite.c ?? ospite.C ?? ospite.flag_c ?? ospite.flagC ?? ospite.colc ?? ospite.c_flag));
  setColC(state.guestColC);
refreshFloatingLabels();
  try { updateGuestRemaining(); } catch (_) {}

  // Servizi: carica sempre (anche se l'ospite non ha servizi_totale valorizzato)
  try{ loadServiziForOspite(ospite); }catch(_){ }


  // deposit type (se disponibile). Retroattivo: se importo Acconto/Saldo è zero, nessun led deve restare acceso.
  const __depAmountNow = parseFloat(document.getElementById("guestDeposit")?.value || "0") || 0;
  const __saldoAmountNow = parseFloat(document.getElementById("guestSaldo")?.value || "0") || 0;
  const dtRaw = (ospite.acconto_tipo ?? ospite.accontoTipo ?? ospite.depositType ?? ospite.deposit_type ?? "");
  const dt = (__depAmountNow > 0) ? dtRaw : "";
  state.guestDepositType = dt;
  setPayType("depositType", dt);

  const stRaw = (ospite.saldo_tipo ?? ospite.saldoTipo ?? ospite.balanceType ?? ospite.balance_type ?? "");
  const st = (__saldoAmountNow > 0) ? stRaw : "";
  state.guestSaldoType = st;
  setPayType("saldoType", st);

  // ricevuta fiscale (toggle indipendente). Retroattivo: spenta se il relativo importo è zero.
  const depRec = (__depAmountNow > 0) && truthy(ospite.acconto_ricevuta ?? ospite.accontoRicevuta ?? ospite.ricevuta_acconto ?? ospite.ricevutaAcconto ?? ospite.acconto_ricevutain);
  const saldoRec = (__saldoAmountNow > 0) && truthy(ospite.saldo_ricevuta ?? ospite.saldoRicevuta ?? ospite.ricevuta_saldo ?? ospite.ricevutaSaldo ?? ospite.saldo_ricevutain);
  state.guestDepositReceipt = depRec;
  state.guestSaldoReceipt = saldoRec;
  setPayReceipt("depositType", depRec);
  setPayReceipt("saldoType", saldoRec);
  try{ __syncPaymentLedsWithAmounts__(); }catch(_){ }



  // registrazioni PS/ISTAT
  const psReg = truthy(ospite.ps_registrato ?? ospite.psRegistrato);
  const istatReg = truthy(ospite.istat_registrato ?? ospite.istatRegistrato);
  state.guestPSRegistered = psReg;
  state.guestISTATRegistered = istatReg;
  state.guestCheckInDone = __guestCheckInDone__(ospite);
  setRegFlags("regTags", psReg, istatReg);
  try{ __syncGuestCheckInButton__(); }catch(_){ }
  // stanze: in lettura possono arrivare in vari formati (legacy, JSON, date-convertite da Sheets)
  try {
    const roomsArr = _parseRoomsArr(ospite?.stanze);
    if (roomsArr.length){
      state.guestRooms = new Set(roomsArr);
      document.querySelectorAll("#roomsPicker .room-dot").forEach(btn => {
        const n = parseInt(btn.getAttribute("data-room"), 10);
        const on = state.guestRooms.has(n);
        btn.classList.toggle("selected", on);
        btn.setAttribute("aria-pressed", on ? "true" : "false");
      });
    }
  } catch (_) {}

  // --- FIX A+B (dDAE): preserva la configurazione letti esistente e non riscrivere "stanze" se non è cambiata ---
  try {
    state.bedsDirty = false;

    // Ricostruisci lettiPerStanza dai dati già salvati sul foglio "stanze" (state.stanzeByKey)
    const gid = String(guestIdOf(ospite) || ospite?.id || "").trim();
    const next = {};
    const roomsNow = Array.from(state.guestRooms || []).map(n=>parseInt(n,10)).filter(n=>isFinite(n));
    for (const rn of roomsNow){
      const key = `${gid}:${String(rn)}`;
      const d = (state.stanzeByKey && state.stanzeByKey[key]) ? state.stanzeByKey[key] : {};
      next[String(rn)] = {
        matrimoniale: !!(d.letto_m),
        singoli: parseInt(d.letto_s || 0, 10) || 0,
        culla: !!(d.culla),
        note: ""
      };
    }
    state.lettiPerStanza = next;

    // Snapshot originale per evitare riscritture inutili su salvataggio
    state.stanzeSnapshotOriginal = JSON.stringify(buildArrayFromState());
  } catch (_) {}

  try { updateOspiteHdActions(); } catch (_) {}

  // ✅ FIX dDAE: entrando in modifica con date gia' valorizzate, ricalcola subito disponibilita' stanze.
  // In iOS/Safari PWA gli handler input/change dei campi date possono non partire finche' l'utente non li tocca.
  // refreshRoomsAvailability/renderRooms sono definiti in setupOspite: li esponiamo su window e li richiamiamo qui.
  try {
    state._roomsAvailKey = "";
    const run = () => {
      try { window.__ddae_renderRooms && window.__ddae_renderRooms(); } catch (_) {}
      try { window.__ddae_refreshRoomsAvailability && window.__ddae_refreshRoomsAvailability(); } catch (_) {}
    };
    setTimeout(run, 50);
    setTimeout(run, 180);
  } catch (_) {}

  // Multi prenotazioni: in modifica mostra sempre il riquadro (anche se singolo) + tasto +
  try{
    // Se non abbiamo contesto gruppo, ricostruiscilo dal dataset corrente
    if (!Array.isArray(state.guestGroupBookings) || !state.guestGroupBookings.length){
      const items = Array.isArray(state.ospiti) && state.ospiti.length ? state.ospiti : (Array.isArray(state.guests) ? state.guests : []);
      const groups = groupGuestsByName(items || []);
      const cardKey = String(__guestCardKeyOf__(ospite)).trim();
      const g = cardKey ? groups.find(x => String(x.key) === cardKey) : null;
      if (g && Array.isArray(g.bookings) && g.bookings.length){
        state.guestGroupBookings = g.bookings;
        state.guestGroupKey = g.key;
      } else {
        state.guestGroupBookings = [ospite];
        state.guestGroupKey = cardKey || null;
      }
    } else if (!state.guestGroupKey){
      const cardKey = String(__guestCardKeyOf__(ospite)).trim();
      state.guestGroupKey = cardKey || state.guestGroupKey || null;
    }

    state.guestGroupActiveId = guestIdOf(ospite);
    renderGuestMulti({ mode: "edit" });
  }catch(_){ }


  try { updateGuestPriceVisibility(); } catch (_) {}
}

function _guestIdOf(item){
  return String(item?.id || item?.ID || item?.ospite_id || item?.ospiteId || item?.guest_id || item?.guestId || "").trim();
}

function getConfiguredRoomsCount(fallback = 6){
  try{
    const n = parseInt(String(state?.settings?.byKey?.numero_stanze?.value ?? state?.settings?.byKey?.numero_stanze?.Value ?? state?.settings?.byKey?.numero_stanze?.val ?? fallback), 10);
    if (Number.isFinite(n) && n >= 0) return n;
  }catch(_){ }
  return Math.max(0, parseInt(fallback, 10) || 6);
}

function updateSettingsRoomsButtonLabel(){
  try{
    const el = document.getElementById("settingsRoomsBtn");
    if (!el) return;
    const label = el.querySelector('.settings-btn-label');
    const n = getConfiguredRoomsCount(6);
    if (label) label.textContent = `Design`;
    el.setAttribute('aria-label', `Design. Numero attuale: ${n}`);
    el.title = `Design. Numero attuale: ${n}`;
  }catch(_){ }
}

const __ROOMS_UI_DEFAULT_ROOM_COLORS__ = ["blue-4","mint-3","beige-3","beige-4","orange-4","orange-5","red-4","pink-4","indigo-3","indigo-4","blue-3","mint-4"];

function __roomsUiColorPair__(input, fallbackBg){
  return __tagColorPairFromValue__(input, fallbackBg || 'blue-4');
}

function __roomsUiVisualNormalize__(input, fallbackBg){
  const pair = __roomsUiColorPair__(input, fallbackBg || 'blue-4');
  return {
    bg: pair.bg || __normalizeOperatoreColor__(fallbackBg || 'blue-4'),
    border: pair.border || pair.bg || __normalizeOperatoreColor__(fallbackBg || 'blue-4'),
    fg: pair.fg || '',
    opacity: __designBgOpacityNormalize__(input?.opacity ?? pair?.opacity ?? 0.80)
  };
}

function __buildDefaultRoomsUiConfig__(){
  const rooms = {};
  for (let i = 1; i <= 12; i++) rooms[String(i)] = __roomsUiColorPair__(__ROOMS_UI_DEFAULT_ROOM_COLORS__[i - 1] || 'blue-4', 'blue-4');
  return {
    nights: __roomsUiColorPair__('indigo-4', 'indigo-4'),
    options: { m: __roomsUiColorPair__('blue-4', 'blue-4'), g: __roomsUiColorPair__('yellow-4', 'yellow-4'), c: __roomsUiColorPair__('indigo-6', 'indigo-6') },
    beds: { matrimoniale: __roomsUiColorPair__('red-4', 'red-4'), singolo: __roomsUiColorPair__('blue-4', 'blue-4'), culla: __roomsUiColorPair__('yellow-4', 'yellow-4') },
    rooms,
  };
}

function __sanitizeRoomsUiConfig__(input){
  const base = __buildDefaultRoomsUiConfig__();
  const src = (input && typeof input === 'object') ? input : {};
  const out = {
    nights: __roomsUiColorPair__(src?.nights, base.nights.bg),
    options: {
      m: __roomsUiColorPair__(src?.options?.m || src?.mgc?.m, base.options.m.bg),
      g: __roomsUiColorPair__(src?.options?.g || src?.mgc?.g, base.options.g.bg),
      c: __roomsUiColorPair__(src?.options?.c || src?.mgc?.c, base.options.c.bg),
    },
    beds: {
      matrimoniale: __roomsUiColorPair__(src?.beds?.matrimoniale || src?.beds?.m, base.beds.matrimoniale.bg),
      singolo: __roomsUiColorPair__(src?.beds?.singolo || src?.beds?.s, base.beds.singolo.bg),
      culla: __roomsUiColorPair__(src?.beds?.culla || src?.beds?.c, base.beds.culla.bg),
    },
    rooms: {},
  };
  for (let i = 1; i <= 12; i++) out.rooms[String(i)] = __roomsUiColorPair__(src?.rooms?.[String(i)] || src?.rooms?.[i], base.rooms[String(i)].bg);
  return out;
}

function getRoomsUiConfig(){
  try{
    const raw = getSettingText('stanze_ui', '');
    if (!String(raw || '').trim()) return __sanitizeRoomsUiConfig__(__BACKUP_GRAPHIC_DEFAULTS_ROOMS_UI__ || null);
    return __sanitizeRoomsUiConfig__(JSON.parse(String(raw || '{}')));
  }catch(_){
    return __sanitizeRoomsUiConfig__(null);
  }
}

function __roomsUiTextColor__(spec, fallback, preferWhite = false){
  const fg = __normalizeOptionalOperatoreColor__(spec?.fg || spec?.textColor || fallback || '');
  if (fg) return __operatoreColorHex__(fg);
  if (preferWhite) return '#ffffff';
  try{
    const parsed = __parseOperatoreColorSpec__((spec && typeof spec === 'object' ? spec.bg : spec) || 'blue-4');
    if ((['yellow','beige','acid','mint','sky','gray','violet'].includes(parsed.base) && parsed.shade <= 4) || (parsed.base === 'orange' && parsed.shade <= 2)) return '#0b1f3a';
  }catch(_){ }
  return '#ffffff';
}

function __roomsUiButtonStyle__(spec, preferWhite = true){
  const pair = __roomsUiVisualNormalize__(spec, 'blue-4');
  const main = __operatoreColorHex__(pair.bg || 'blue-4');
  const borderHex = __operatoreColorHex__(pair.border || pair.bg || 'blue-4');
  const opacity = __designBgOpacityNormalize__(pair.opacity ?? 0.80);
  return `background:${hexToRgba(main, opacity)};background-color:${hexToRgba(main, opacity)};border-color:${hexToRgba(borderHex, opacity)};color:${__roomsUiTextColor__(pair, '', preferWhite)};-webkit-text-fill-color:${__roomsUiTextColor__(pair, '', preferWhite)};`;
}

function __roomsUiBadgeStyle__(spec){
  const pair = __roomsUiVisualNormalize__(spec, 'blue-4');
  const main = __operatoreColorHex__(pair.bg || 'blue-4');
  const borderHex = __operatoreColorHex__(pair.border || pair.bg || 'blue-4');
  const opacity = __designBgOpacityNormalize__(pair.opacity ?? 0.92);
  return `background:${hexToRgba(main, opacity)};background-color:${hexToRgba(main, opacity)};border-color:${hexToRgba(borderHex, opacity)};color:${__roomsUiTextColor__(pair)};-webkit-text-fill-color:${__roomsUiTextColor__(pair)};`;
}

function __applyRoomsUiConfig__(){
  try{
    const root = document.documentElement;
    if (!root) return;
    const cfg = getRoomsUiConfig();
    const setVar = (prefix, spec) => {
      const pair = __roomsUiVisualNormalize__(spec, 'blue-4');
      const main = __operatoreColorHex__(pair.bg || 'blue-4');
      const borderHex = __operatoreColorHex__(pair.border || pair.bg || 'blue-4');
      const opacity = __designBgOpacityNormalize__(pair.opacity ?? 0.95);
      root.style.setProperty(`${prefix}-bg`, hexToRgba(main, opacity));
      root.style.setProperty(`${prefix}-border`, hexToRgba(borderHex, opacity));
      root.style.setProperty(`${prefix}-fg`, __roomsUiTextColor__(pair));
    };
    setVar('--ddae-room-nights', cfg.nights);
    setVar('--ddae-room-m', cfg.options.m);
    setVar('--ddae-room-g', cfg.options.g);
    setVar('--ddae-room-c', cfg.options.c);
    setVar('--ddae-bed-m', cfg.beds.matrimoniale);
    setVar('--ddae-bed-s', cfg.beds.singolo);
    setVar('--ddae-bed-c', cfg.beds.culla);
    for (let i = 1; i <= 12; i++) {
      const pair = __roomsUiVisualNormalize__(cfg.rooms?.[String(i)] || 'blue-4', 'blue-4');
      const main = __operatoreColorHex__(pair.bg || 'blue-4');
      const borderHex = __operatoreColorHex__(pair.border || pair.bg || 'blue-4');
      const opacity = __designBgOpacityNormalize__(pair.opacity ?? 0.55);
      root.style.setProperty(`--room${i}`, hexToRgba(main, opacity));
      root.style.setProperty(`--room${i}-solid`, hexToRgba(main, opacity));
      root.style.setProperty(`--room${i}-border`, hexToRgba(borderHex, opacity));
      root.style.setProperty(`--room${i}-fg`, __roomsUiTextColor__(pair));
    }
  }catch(_){ }
}

async function saveRoomsUiConfigToSettings(config, { showToast = false } = {}){

  const clean = __sanitizeRoomsUiConfig__(config);
  const raw = JSON.stringify(clean);
  await api('impostazioni', { method:'POST', body:{ stanze_ui: raw }, showLoader:true });
  try{
    state.settings = state.settings || {};
    state.settings.byKey = state.settings.byKey || {};
    state.settings.byKey.stanze_ui = { key:'stanze_ui', value:raw, val:raw, Value:raw };
  }catch(_){ }
  await ensureSettingsLoaded({ force:true, showLoader:false });
  try{ __applyRoomsUiConfig__(); }catch(_){ }
  try{ renderRoomSettingsPage(); }catch(_){ }
  try{ if (state?.page === 'ospiti') loadOspiti({ ...(state.period || {}), force:true }).catch(()=>{}); }catch(_){ }
  try{ if (state?.page === 'ospite' && state?.guestMode === 'view' && state?.guestViewItem) renderRoomsReadOnly(state.guestViewItem); }catch(_){ }
  if (showToast) try{ toast('Colori stanze aggiornati'); }catch(_){ }
  return clean;
}

function __openRoomSettingsColorPicker__(target){
  const key = String(target || '').trim().toLowerCase();
  const cfg = getRoomsUiConfig();
  let current = cfg.nights;
  if (/^room:\d+$/.test(key)){
    const n = key.split(':')[1];
    current = cfg.rooms?.[String(n)] || current;
  } else if (key === 'nights'){
    current = cfg.nights;
  } else if (key === 'option:m'){
    current = cfg.options?.m || current;
  } else if (key === 'option:g'){
    current = cfg.options?.g || current;
  } else if (key === 'option:c'){
    current = cfg.options?.c || current;
  } else if (key === 'bed:matrimoniale'){
    current = cfg.beds?.matrimoniale || current;
  } else if (key === 'bed:singolo'){
    current = cfg.beds?.singolo || current;
  } else if (key === 'bed:culla'){
    current = cfg.beds?.culla || current;
  }
  __tagColorPopupOpen__('roomsettings', current, async(payload) => {
    try{
      const pair = __roomsUiVisualNormalize__(current, 'blue-4');
      const mode = String(payload?.mode || 'bg').trim().toLowerCase();
      const spec = __parseOperatoreColorSpec__(payload?.spec || pair.bg || 'blue-4').spec;
      if (mode === 'bg') pair.bg = spec;
      else if (mode === 'border') pair.border = spec;
      else if (mode === 'opacity') pair.opacity = __designBgOpacityNormalize__(payload?.opacity ?? pair.opacity ?? 0.80);
      else pair.fg = spec;
      const nextVisual = { bg: pair.bg, border: pair.border || pair.bg, fg: pair.fg || '', opacity: __designBgOpacityNormalize__(pair.opacity ?? 0.80) };
      const next = getRoomsUiConfig();
      if (/^room:\d+$/.test(key)){
        const n = key.split(':')[1];
        next.rooms[String(n)] = nextVisual;
      } else if (key === 'nights'){
        next.nights = nextVisual;
      } else if (key === 'option:m'){
        next.options.m = nextVisual;
      } else if (key === 'option:g'){
        next.options.g = nextVisual;
      } else if (key === 'option:c'){
        next.options.c = nextVisual;
      } else if (key === 'bed:matrimoniale'){
        next.beds.matrimoniale = nextVisual;
      } else if (key === 'bed:singolo'){
        next.beds.singolo = nextVisual;
      } else if (key === 'bed:culla'){
        next.beds.culla = nextVisual;
      }
      await saveRoomsUiConfigToSettings(next, { showToast:true });
    }catch(e){ try{ toast(e?.message || 'Errore colori stanze'); }catch(_){ } }
  }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:(current?.opacity ?? __designBgOpacityRead__()), defaultMode:'bg', fallbackBg:(current?.bg || current || 'blue-4'), applyCategory:{ message:(/^room:\d+$/.test(key) ? 'Applicare le modifiche a tutte le stanze?' : (key.startsWith('option:') ? 'Applicare le modifiche a tutte le opzioni?' : (key.startsWith('bed:') ? 'Applicare le modifiche a tutti i letti?' : 'Applicare le modifiche a tutta la categoria di questo tasto?'))), apply: async(payload, changed) => { await __applyRoomSettingsChangesToCategory__(key, payload, changed); } } });
}

const __ROOM_SETTINGS_THEME_SLOTS_STORAGE_KEY__ = 'dDAE_roomsettings_theme_slots_v1';
const __ROOM_SETTINGS_THEME_BUTTON_VISUAL_STORAGE_KEY__ = 'dDAE_roomsettings_theme_button_visual_v1';
const __ROOM_SETTINGS_THEME_ACTIVE_SLOT_STORAGE_KEY__ = 'dDAE_roomsettings_theme_active_slot_v1';
const __ROOM_SETTINGS_TEXT_BUTTON_VISUAL_STORAGE_KEY__ = 'dDAE_roomsettings_text_button_visual_v1';
const __ROOM_SETTINGS_TEXT_BUTTON_KEYS__ = ['bold','1','2','3'];

function __roomSettingsThemeButtonVisualMapRead__(){
  try{
    const raw = localStorage.getItem(__ROOM_SETTINGS_THEME_BUTTON_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return (parsed && typeof parsed === 'object') ? parsed : {};
  }catch(_){ return {}; }
}

function __roomSettingsThemeButtonVisualMapWrite__(map){
  try{ localStorage.setItem(__ROOM_SETTINGS_THEME_BUTTON_VISUAL_STORAGE_KEY__, JSON.stringify((map && typeof map === 'object') ? map : {})); }catch(_){ }
}

function __roomSettingsThemeButtonStateLabel__(stateKey){
  return __translateExactText__(String(stateKey || '').trim().toLowerCase() === 'active' ? 'Attivo' : 'Disattivo');
}

function __roomSettingsThemeButtonLocalizedLabel__(slotOrEl){
  try{
    const slot = String((slotOrEl && slotOrEl.getAttribute && slotOrEl.getAttribute('data-theme-slot')) || slotOrEl || '').trim();
    if (slot) return slot;
    return __translateExactText__('Tema') || 'Tema';
  }catch(_){
    return __translateExactText__('Tema') || 'Tema';
  }
}

function __roomSettingsThemeButtonEditStatePrompt__(label){
  const safeLabel = String(label || '').trim() || (__translateExactText__('Tema') || 'Tema');
  switch (__getAppLanguage__()){
    case 'en': return `Which state do you want to edit for theme ${safeLabel}?`;
    case 'fr': return `Quel état voulez-vous modifier pour le thème ${safeLabel} ?`;
    case 'de': return `Welchen Status möchten Sie für Thema ${safeLabel} bearbeiten?`;
    case 'es': return `¿Qué estado quieres modificar para el tema ${safeLabel}?`;
    default: return `Quale stato vuoi modificare per il tema ${safeLabel}?`;
  }
}

function __roomSettingsThemeButtonStateUpdatedMessage__(label, stateKey){
  const safeLabel = String(label || '').trim() || (__translateExactText__('Tema') || 'Tema');
  const stateLabel = __roomSettingsThemeButtonStateLabel__(stateKey);
  switch (__getAppLanguage__()){
    case 'en': return `Theme button ${safeLabel}: ${String(stateLabel || '').toLowerCase()} state updated`;
    case 'fr': return `Bouton thème ${safeLabel} : état ${String(stateLabel || '').toLowerCase()} mis à jour`;
    case 'de': return `Thema-Taste ${safeLabel}: Status ${String(stateLabel || '').toLowerCase()} aktualisiert`;
    case 'es': return `Botón tema ${safeLabel}: estado ${String(stateLabel || '').toLowerCase()} actualizado`;
    default: return `Tasto tema ${safeLabel}: stato ${String(stateLabel || '').toLowerCase()} aggiornato`;
  }
}

function __roomSettingsThemeButtonCategoryPrompt__(){
  switch (__getAppLanguage__()){
    case 'en': return 'Apply the changes to all theme buttons?';
    case 'fr': return 'Appliquer les modifications à tous les boutons thème ?';
    case 'de': return 'Änderungen auf alle Themen-Tasten anwenden?';
    case 'es': return '¿Aplicar los cambios a todos los botones tema?';
    default: return 'Applicare le modifiche a tutti i tasti Tema?';
  }
}

function __roomSettingsThemeButtonTargetSlots__(){
  return ['1','2','3','4'];
}

function __roomSettingsThemeActiveSlotRead__(){
  try{
    const raw = String(localStorage.getItem(__ROOM_SETTINGS_THEME_ACTIVE_SLOT_STORAGE_KEY__) || '').trim();
    const num = parseInt(raw, 10);
    if (!Number.isFinite(num) || num < 1 || num > 4) return '';
    return String(num);
  }catch(_){ return ''; }
}

function __roomSettingsThemeActiveSlotWrite__(slot){
  try{
    const num = parseInt(slot, 10);
    if (!Number.isFinite(num) || num < 1 || num > 4){
      localStorage.removeItem(__ROOM_SETTINGS_THEME_ACTIVE_SLOT_STORAGE_KEY__);
      return;
    }
    localStorage.setItem(__ROOM_SETTINGS_THEME_ACTIVE_SLOT_STORAGE_KEY__, String(num));
  }catch(_){ }
}

function __roomSettingsThemeButtonDefaultVisual__(slot, stateKey, fallbackVisual){
  const safeSlot = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 1)));
  const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
  const fallback = __launcherVisualNormalize__(fallbackVisual || {}, 'blue-4');
  if (mode === 'active'){
    return __launcherVisualNormalize__({
      bg: fallback.bg || 'blue-4',
      border: fallback.border || fallback.bg || 'blue-4',
      fg: fallback.fg || '',
      opacity: __designBgOpacityNormalize__(fallback.opacity ?? 0.75)
    }, fallback.bg || 'blue-4');
  }
  return __launcherVisualNormalize__({ bg:'gray-2', border:'gray-3', fg:'blue-6', opacity:0.75 }, 'gray-2');
}

function __roomSettingsThemeButtonVisualGet__(slot, stateKey, fallbackVisual){
  try{
    const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
    const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    const map = __roomSettingsThemeButtonVisualMapRead__();
    const entry = (map[key] && typeof map[key] === 'object') ? map[key] : {};
    const fallback = __roomSettingsThemeButtonDefaultVisual__(key, mode, fallbackVisual);
    let raw = null;
    if ((entry.active && typeof entry.active === 'object') || (entry.distractive && typeof entry.distractive === 'object')) raw = entry[mode];
    else if (mode === 'active') raw = entry;
    const clean = __launcherVisualNormalize__(raw || fallback, fallback.bg || 'blue-4');
    return {
      bg: clean.bg || fallback.bg || 'blue-4',
      border: clean.border || clean.bg || fallback.border || fallback.bg || 'blue-4',
      fg: clean.fg || fallback.fg || '',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.75),
      bold: false
    };
  }catch(_){ return __launcherVisualNormalize__(fallbackVisual || {}, 'blue-4'); }
}

function __roomSettingsThemeButtonVisualSet__(slot, visual, stateKey){
  try{
    const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
    const mode = String(stateKey || 'active').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    const map = __roomSettingsThemeButtonVisualMapRead__();
    const entry = (map[key] && typeof map[key] === 'object') ? map[key] : {};
    const fallback = __roomSettingsThemeButtonDefaultVisual__(key, mode, visual || {});
    const clean = __launcherVisualNormalize__(visual || {}, fallback.bg || 'blue-4');
    entry[mode] = {
      bg: clean.bg || fallback.bg || 'blue-4',
      border: clean.border || clean.bg || fallback.border || fallback.bg || 'blue-4',
      fg: clean.fg || fallback.fg || '',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.75),
      bold: false
    };
    map[key] = entry;
    __roomSettingsThemeButtonVisualMapWrite__(map);
  }catch(_){ }
}

function __roomSettingsThemeButtonCurrentState__(el){
  try{ return el && el.classList && el.classList.contains('is-active') ? 'active' : 'distractive'; }catch(_){ return 'distractive'; }
}

function __roomSettingsThemeButtonVisualApply__(el, slot, fallbackVisual, forcedState){
  try{
    if (!el) return;
    const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
    const mode = String(forcedState || __roomSettingsThemeButtonCurrentState__(el) || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    const fallback = __roomSettingsThemeButtonDefaultVisual__(key, mode, fallbackVisual);
    const stored = __roomSettingsThemeButtonVisualGet__(key, mode, fallbackVisual || fallback);
    const visual = {
      bg: stored.bg || fallback.bg || 'blue-4',
      border: stored.border || stored.bg || fallback.border || fallback.bg || 'blue-4',
      fg: stored.fg || fallback.fg || '',
      opacity: __designBgOpacityNormalize__(stored.opacity ?? fallback.opacity ?? 0.75),
      bold: false
    };
    const bgHex = __operatoreColorHex__(visual.bg || 'blue-4');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    const fgHex = visual.fg ? __operatoreColorHex__(visual.fg) : '#000000';
    el.setAttribute('style', `background:${hexToRgba(bgHex, visual.opacity)} !important;background-color:${hexToRgba(bgHex, visual.opacity)} !important;border-color:${hexToRgba(borderHex, 1)} !important;color:${fgHex} !important;-webkit-text-fill-color:${fgHex} !important;`);
    __applyVisualTextWeight__(el, visual);
    el.classList.toggle('is-active', mode === 'active');
    el.setAttribute('aria-pressed', mode === 'active' ? 'true' : 'false');
    el.classList.remove('room-settings-square-btn-placeholder');
  }catch(_){ }
}

async function __applyRoomSettingsThemeButtonChangesToCategory__(stateKey, payload, changed){
  try{
    const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    __roomSettingsThemeButtonTargetSlots__().forEach((slot) => {
      const saved = __roomSettingsThemeSlotGet__(slot);
      const visualFallback = saved?.launcherGridTheme || { bg:'blue-4', border:'blue-4', fg:'', opacity:0.75 };
      const current = __roomSettingsThemeButtonVisualGet__(slot, mode, visualFallback);
      const fallback = __roomSettingsThemeButtonDefaultVisual__(slot, mode, visualFallback).bg || 'blue-4';
      const next = __applyDesignPayloadToVisual__(current, payload, changed, fallback);
      __roomSettingsThemeButtonVisualSet__(slot, next, mode);
    });
    try{ renderRoomSettingsPage(); }catch(_){ }
  }catch(_){ }
}

function __roomSettingsTextButtonStateLabel__(stateKey){
  return __translateExactText__(String(stateKey || '').trim().toLowerCase() === 'active' ? 'Attivo' : 'Disattivo');
}

function __roomSettingsTextButtonLocalizedLabel__(btnOrKey){
  try{
    const safeKey = String((btnOrKey && btnOrKey.id) || btnOrKey || '').trim();
    if (safeKey === 'bold') return 'G';
    if (safeKey === '1' || safeKey === '2' || safeKey === '3') return safeKey;
    const btn = (btnOrKey && btnOrKey.nodeType === 1) ? btnOrKey : document.getElementById(safeKey === 'bold' ? 'roomSettingsTextBoldBtn' : `roomSettingsTextSizeBtn${safeKey}`);
    const raw = String(btn?.textContent || btn?.getAttribute?.('aria-label') || '').replace(/\s+/g, ' ').trim();
    return raw || (__translateExactText__('Tasto') || 'Tasto');
  }catch(_){
    return __translateExactText__('Tasto') || 'Tasto';
  }
}

function __roomSettingsTextButtonEditStatePrompt__(label){
  const safeLabel = String(label || '').trim() || (__translateExactText__('Tasto') || 'Tasto');
  switch (__getAppLanguage__()){
    case 'en': return `Which state do you want to edit for ${safeLabel}?`;
    case 'fr': return `Quel état voulez-vous modifier pour ${safeLabel} ?`;
    case 'de': return `Welchen Status möchten Sie für ${safeLabel} bearbeiten?`;
    case 'es': return `¿Qué estado quieres modificar para ${safeLabel}?`;
    default: return `Quale stato vuoi modificare per ${safeLabel}?`;
  }
}

function __roomSettingsTextButtonStateUpdatedMessage__(label, stateKey){
  const safeLabel = String(label || '').trim() || (__translateExactText__('Tasto') || 'Tasto');
  const stateLabel = __roomSettingsTextButtonStateLabel__(stateKey);
  switch (__getAppLanguage__()){
    case 'en': return `Text button ${safeLabel}: ${String(stateLabel || '').toLowerCase()} state updated`;
    case 'fr': return `Bouton texte ${safeLabel} : état ${String(stateLabel || '').toLowerCase()} mis à jour`;
    case 'de': return `Texttaste ${safeLabel}: Status ${String(stateLabel || '').toLowerCase()} aktualisiert`;
    case 'es': return `Botón texto ${safeLabel}: estado ${String(stateLabel || '').toLowerCase()} actualizado`;
    default: return `Tasto testo ${safeLabel}: stato ${String(stateLabel || '').toLowerCase()} aggiornato`;
  }
}

function __roomSettingsTextButtonCategoryPrompt__(){
  switch (__getAppLanguage__()){
    case 'en': return 'Apply the changes to all text buttons?';
    case 'fr': return 'Appliquer les modifications à tous les boutons texte ?';
    case 'de': return 'Änderungen auf alle Texttasten anwenden?';
    case 'es': return '¿Aplicar los cambios a todos los botones de texto?';
    default: return 'Applicare le modifiche a tutti i tasti Testi?';
  }
}

function __roomSettingsTextButtonTargetKeys__(){
  return __ROOM_SETTINGS_TEXT_BUTTON_KEYS__.filter((key) => String(key || '').trim() !== 'bold');
}

function __roomSettingsTextButtonDefaultVisual__(key, stateKey){
  const safeKey = String(key || '').trim();
  const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
  if (mode === 'active'){
    return __launcherVisualNormalize__(safeKey === 'bold'
      ? { bg:'green-4', border:'green-4', fg:'white', opacity:0.75 }
      : { bg:'blue-4', border:'blue-4', fg:'white', opacity:0.75 }, safeKey === 'bold' ? 'green-4' : 'blue-4');
  }
  return __launcherVisualNormalize__({ bg:'gray-2', border:'gray-3', fg:'blue-6', opacity:0.75 }, 'gray-2');
}

function __roomSettingsTextButtonVisualMapRead__(){
  try{
    const raw = localStorage.getItem(__ROOM_SETTINGS_TEXT_BUTTON_VISUAL_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return (parsed && typeof parsed === 'object') ? parsed : {};
  }catch(_){ return {}; }
}

function __roomSettingsTextButtonVisualMapWrite__(map){
  try{ localStorage.setItem(__ROOM_SETTINGS_TEXT_BUTTON_VISUAL_STORAGE_KEY__, JSON.stringify((map && typeof map === 'object') ? map : {})); }catch(_){ }
}

function __roomSettingsTextButtonVisualForState__(key, stateKey){
  try{
    const safeKey = String(key || '').trim();
    const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    const fallback = __roomSettingsTextButtonDefaultVisual__(safeKey, mode);
    const map = __roomSettingsTextButtonVisualMapRead__();
    const entry = (map[safeKey] && typeof map[safeKey] === 'object') ? map[safeKey] : {};
    let raw = null;
    if ((entry.active && typeof entry.active === 'object') || (entry.distractive && typeof entry.distractive === 'object')) raw = entry[mode];
    else if (mode === 'active') raw = entry;
    const clean = __launcherVisualNormalize__(raw || fallback, fallback.bg || 'blue-4');
    return {
      bg: clean.bg || fallback.bg || 'blue-4',
      border: clean.border || clean.bg || fallback.border || fallback.bg || 'blue-4',
      fg: clean.fg || fallback.fg || '',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.75),
      bold: false
    };
  }catch(_){ return __launcherVisualNormalize__({}, 'blue-4'); }
}

function __roomSettingsTextButtonVisualSet__(key, stateKey, visual){
  try{
    const safeKey = String(key || '').trim();
    if (!safeKey) return;
    const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    const map = __roomSettingsTextButtonVisualMapRead__();
    const entry = (map[safeKey] && typeof map[safeKey] === 'object') ? map[safeKey] : {};
    const fallback = __roomSettingsTextButtonDefaultVisual__(safeKey, mode);
    const clean = __launcherVisualNormalize__(visual || {}, fallback.bg || 'blue-4');
    entry[mode] = {
      bg: clean.bg || fallback.bg || 'blue-4',
      border: clean.border || clean.bg || fallback.border || fallback.bg || 'blue-4',
      fg: clean.fg || fallback.fg || '',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? fallback.opacity ?? 0.75),
      bold: false
    };
    map[safeKey] = entry;
    __roomSettingsTextButtonVisualMapWrite__(map);
  }catch(_){ }
}

function __roomSettingsTextButtonCurrentState__(el){
  try{ return el && el.classList && el.classList.contains('is-active') ? 'active' : 'distractive'; }catch(_){ return 'distractive'; }
}

function __roomSettingsTextButtonVisualApply__(el, key, fallbackVisual, forcedState){
  try{
    if (!el) return;
    const safeKey = String(key || '').trim();
    const stateKey = String(forcedState || __roomSettingsTextButtonCurrentState__(el) || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    const stateFallback = __roomSettingsTextButtonDefaultVisual__(safeKey, stateKey);
    const fallback = __launcherVisualNormalize__(fallbackVisual || stateFallback, stateFallback.bg || 'blue-4');
    const stored = __roomSettingsTextButtonVisualForState__(safeKey, stateKey);
    const visual = {
      bg: stored.bg || fallback.bg || stateFallback.bg || 'blue-4',
      border: stored.border || stored.bg || fallback.border || fallback.bg || stateFallback.border || stateFallback.bg || 'blue-4',
      fg: stored.fg || fallback.fg || stateFallback.fg || '',
      opacity: __designBgOpacityNormalize__(stored.opacity ?? fallback.opacity ?? stateFallback.opacity ?? 0.75),
      bold: false
    };
    el.setAttribute('style', `${__tagColorInlineStyle__(visual.bg || 'blue-4', visual.fg || '', { opacity:visual.opacity, borderOpacity:1, preferWhiteText:false })}border-color:${hexToRgba(__operatoreColorHex__(visual.border || visual.bg || 'blue-4'), 1)} !important;`);
    __applyVisualTextWeight__(el, visual);
    el.classList.remove('room-settings-square-btn-placeholder');
  }catch(_){ }
}

async function __applyRoomSettingsTextButtonChangesToCategory__(stateKey, payload, changed){
  try{
    const mode = String(stateKey || 'distractive').trim().toLowerCase() === 'active' ? 'active' : 'distractive';
    __roomSettingsTextButtonTargetKeys__().forEach((safeKey) => {
      const current = __roomSettingsTextButtonVisualForState__(safeKey, mode);
      const fallback = __roomSettingsTextButtonDefaultVisual__(safeKey, mode).bg || 'blue-4';
      const next = __applyDesignPayloadToVisual__(current, payload, changed, fallback);
      __roomSettingsTextButtonVisualSet__(safeKey, mode, next);
    });
    try{ renderRoomSettingsTextControls(); }catch(_){ }
    try{ renderRoomSettingsPage(); }catch(_){ }
  }catch(_){ }
}

async function __openRoomSettingsTextButtonColorPicker__(key){
  try{
    const safeKey = String(key || '').trim();
    if (!safeKey) return;
    const el = document.getElementById(safeKey === 'bold' ? 'roomSettingsTextBoldBtn' : `roomSettingsTextSizeBtn${safeKey}`);
    if (!el) return;
    const label = __roomSettingsTextButtonLocalizedLabel__(safeKey);
    const choice = await __confirmTwoActions__(
      __roomSettingsTextButtonEditStatePrompt__(label),
      __roomSettingsTextButtonStateLabel__('active'),
      __roomSettingsTextButtonStateLabel__('distractive')
    );
    if (choice !== 'yes' && choice !== 'no') return;
    const stateKey = choice === 'yes' ? 'active' : 'distractive';
    const initial = __roomSettingsTextButtonVisualForState__(safeKey, stateKey);
    const fallback = __roomSettingsTextButtonDefaultVisual__(safeKey, stateKey);
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = {
        bg: colors.bg || initial.bg || fallback.bg || 'blue-4',
        border: colors.border || initial.border || colors.bg || initial.bg || fallback.border || fallback.bg || 'blue-4',
        fg: colors.fg || initial.fg || fallback.fg || '',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? initial.opacity ?? fallback.opacity ?? 0.75),
        bold: false
      };
      __roomSettingsTextButtonVisualSet__(safeKey, stateKey, next);
      try{ renderRoomSettingsTextControls(); }catch(_){ }
      try{ renderRoomSettingsPage(); }catch(_){ }
    };
    const revertVisual = () => {
      __roomSettingsTextButtonVisualSet__(safeKey, stateKey, initial);
      try{ renderRoomSettingsTextControls(); }catch(_){ }
      try{ renderRoomSettingsPage(); }catch(_){ }
    };
    __tagColorPopupOpen__('room-text-button', initial, (payload) => {
      try{
        applyVisual(payload);
        try{ toast(__roomSettingsTextButtonStateUpdatedMessage__(label, stateKey)); }catch(_){ }
      }catch(_){ }
    }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:true,
      supportsOpacity:true,
      opacity:(initial.opacity ?? 0.75),
      defaultMode:'bg',
      fallbackBg:(initial.bg || fallback.bg || 'blue-4'),
      onPreview:applyVisual,
      onRevert:revertVisual,
      applyCategory:{
        message: __roomSettingsTextButtonCategoryPrompt__(),
        apply: async(payload, changed) => { await __applyRoomSettingsTextButtonChangesToCategory__(stateKey, payload, changed); }
      }
    });
  }catch(_){ }
}

async function __openRoomSettingsThemeButtonColorPicker__(slot){
  try{
    const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
    const el = document.getElementById(`roomSettingsThemeBtn${key}`);
    if (!el) return;
    const label = __roomSettingsThemeButtonLocalizedLabel__(key);
    const choice = await __confirmTwoActions__(
      __roomSettingsThemeButtonEditStatePrompt__(label),
      __roomSettingsThemeButtonStateLabel__('active'),
      __roomSettingsThemeButtonStateLabel__('distractive')
    );
    if (choice !== 'yes' && choice !== 'no') return;
    const stateKey = choice === 'yes' ? 'active' : 'distractive';
    const saved = __roomSettingsThemeSlotGet__(key);
    const visualFallback = saved?.launcherGridTheme || { bg:'blue-4', border:'blue-4', fg:'', opacity:0.75 };
    const initial = __roomSettingsThemeButtonVisualGet__(key, stateKey, visualFallback);
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = {
        bg: colors.bg || initial.bg || visualFallback.bg || 'blue-4',
        border: colors.border || initial.border || colors.bg || initial.bg || visualFallback.border || visualFallback.bg || 'blue-4',
        fg: colors.fg || initial.fg || visualFallback.fg || '',
        opacity: __designBgOpacityNormalize__(payload?.opacity ?? initial.opacity ?? visualFallback.opacity ?? 0.75),
        bold: false
      };
      __roomSettingsThemeButtonVisualSet__(key, next, stateKey);
      try{ renderRoomSettingsPage(); }catch(_){ }
    };
    const revertVisual = () => {
      __roomSettingsThemeButtonVisualSet__(key, initial, stateKey);
      try{ renderRoomSettingsPage(); }catch(_){ }
    };
    __tagColorPopupOpen__('room-theme-button', initial, (payload) => {
      try{
        applyVisual(payload);
        try{ toast(__roomSettingsThemeButtonStateUpdatedMessage__(label, stateKey)); }catch(_){ }
      }catch(_){ }
    }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:true,
      supportsOpacity:true,
      opacity:initial.opacity ?? 0.75,
      defaultMode:'bg',
      fallbackBg:(initial.bg || visualFallback.bg || 'blue-4'),
      onPreview:applyVisual,
      onRevert:revertVisual,
      applyCategory:{
        message: __roomSettingsThemeButtonCategoryPrompt__(),
        apply: async(payload, changed) => { await __applyRoomSettingsThemeButtonChangesToCategory__(stateKey, payload, changed); }
      }
    });
  }catch(_){ }
}

function __roomSettingsThemeSlotsRead__(){
  try{
    const raw = localStorage.getItem(__ROOM_SETTINGS_THEME_SLOTS_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    return (parsed && typeof parsed === 'object') ? parsed : {};
  }catch(_){ return {}; }
}

function __roomSettingsThemeSlotsWrite__(slots){
  try{ localStorage.setItem(__ROOM_SETTINGS_THEME_SLOTS_STORAGE_KEY__, JSON.stringify((slots && typeof slots === 'object') ? slots : {})); }catch(_){ }
}

function __roomSettingsThemeStoragePrefixes__(){
  return [
    'ddae_graph_colors_',
    'dDAE_statcard_colors_',
    'dDAE_stat_card_text_',
    'ddae_stat_card_text_',
    'dDAE_statistics_card_theme_',
    'dDAE_stat_shared_line_chart_local_v1_'
  ];
}

function __roomSettingsThemeAdditionalStorageKeys__(){
  return [
    __LAUNCHER_ICON_COLOR_STORAGE_KEY__,
    __LAUNCHER_GRID_THEME_STORAGE_KEY__,
    __STATISTICS_CARD_THEME_STORAGE_KEY__,
    __DESIGN_BG_OPACITY_STORAGE_KEY__,
    __HEADER_ACTION_THEME_STORAGE_KEY__,
    __HEADER_ACTION_COLOR_STORAGE_KEY__,
    __PILL_THEME_STORAGE_KEY__,
    __PILL_COLOR_STORAGE_KEY__,
    __GUEST_FILTER_BUTTON_VISUAL_STORAGE_KEY__,
    __ROOM_SETTINGS_TEXT_BUTTON_VISUAL_STORAGE_KEY__,
    LS_APP_TEXT_UI,
    __SPESA_CARD_OPACITY_STORAGE_KEY__,
    __SPESA_CARD_VISUAL_STORAGE_KEY__,
    __TAX_QUARTER_VISUAL_STORAGE_KEY__,
    __TAX_PAGE_CARD_VISUAL_STORAGE_KEY__,
    __SINGLE_ACTION_BUTTON_VISUAL_STORAGE_KEY__,
    __GUEST_LIST_CARD_VISUAL_STORAGE_KEY__,
    __ROOM_SETTINGS_THEME_BUTTON_VISUAL_STORAGE_KEY__,
    __ROOM_SETTINGS_CARD_THEME_STORAGE_KEY__,
    __ROOM_SETTINGS_SHELL_THEME_STORAGE_KEY__,
    __STAT_SHARED_LINE_CHART_VISUAL_STORAGE_KEY__,
    __STATGEN_REG_CHART_VISUAL_STORAGE_KEY__,
    __STATGEN_COMPARE_YEAR_KEY__,
    __STATGEN_COMPARE_ENABLED_KEY__,
    __STATGEN_COMPARE_BTN_VISUAL_KEY__,
    LS_STAT_FISCAL_MODE,
    __STAT_FISCAL_BTN_VISUAL_KEY__,
    __statSharedLineChartLocalStorageKey__('statgen'),
    __statSharedLineChartLocalStorageKey__('statspese'),
    __statSharedLineChartLocalStorageKey__('statmensili'),
    __statSharedLineChartLocalStorageKey__('statcancellazioni')
  ].filter(Boolean);
}

function __roomSettingsThemeStatsStorageCollect__(){
  const out = {};
  try{
    const prefixes = __roomSettingsThemeStoragePrefixes__();
    const directKeys = new Set(__roomSettingsThemeAdditionalStorageKeys__());
    for (let i = 0; i < localStorage.length; i++){
      const key = String(localStorage.key(i) || '');
      if (!key) continue;
      if (!directKeys.has(key) && !prefixes.some((prefix) => key.startsWith(prefix))) continue;
      try{ out[key] = String(localStorage.getItem(key) ?? ''); }catch(_){ }
    }
  }catch(_){ }
  return out;
}

function __roomSettingsThemeStatsStorageNormalize__(payload){
  const src = (payload && typeof payload === 'object') ? payload : {};
  const out = {};
  try{
    const directKeys = new Set(__roomSettingsThemeAdditionalStorageKeys__());
    Object.keys(src).forEach((key) => {
      const safeKey = String(key || '');
      if (!safeKey) return;
      if (!directKeys.has(safeKey) && !__roomSettingsThemeStoragePrefixes__().some((prefix) => safeKey.startsWith(prefix))) return;
      out[safeKey] = String(src[key] ?? '');
    });
  }catch(_){ }
  return out;
}

function __roomSettingsThemeStatsStorageApply__(payload){
  try{
    const prefixes = __roomSettingsThemeStoragePrefixes__();
    const directKeys = new Set(__roomSettingsThemeAdditionalStorageKeys__());
    const next = __roomSettingsThemeStatsStorageNormalize__(payload);
    const toDelete = [];
    for (let i = 0; i < localStorage.length; i++){
      const key = String(localStorage.key(i) || '');
      if (!key) continue;
      if (directKeys.has(key) || prefixes.some((prefix) => key.startsWith(prefix))) toDelete.push(key);
    }
    toDelete.forEach((key) => {
      if (!(key in next)){
        try{ localStorage.removeItem(key); }catch(_){ }
      }
    });
    Object.keys(next).forEach((key) => {
      try{ localStorage.setItem(key, String(next[key] ?? '')); }catch(_){ }
    });
  }catch(_){ }
}


function __roomSettingsShellThemeRead__(){
  try{
    const raw = localStorage.getItem(__ROOM_SETTINGS_SHELL_THEME_STORAGE_KEY__);
    if (!raw) return { bg:'', border:'', opacity:0.92 };
    const parsed = JSON.parse(raw);
    return __launcherVisualNormalize__(parsed, 'blue-4');
  }catch(_){ return { bg:'', border:'', opacity:0.92 }; }
}

function __roomSettingsShellThemeWrite__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    localStorage.setItem(__ROOM_SETTINGS_SHELL_THEME_STORAGE_KEY__, JSON.stringify({
      fg: clean.fg || '',
      bg: clean.bg || '',
      border: clean.border || '',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.92)
    }));
  }catch(_){ }
}

function __roomSettingsShellThemeVisual__(){
  const visual = __roomSettingsShellThemeRead__();
  return {
    fg: visual.fg || '',
    bg: visual.bg || 'blue-4',
    border: visual.border || visual.bg || 'blue-4',
    opacity: __designBgOpacityNormalize__(visual.opacity ?? 0.92)
  };
}

function __applyRoomSettingsShellTheme__(){
  try{
    const shell = document.querySelector('#page-roomsettings > .card');
    if (!shell) return;
    const visual = __roomSettingsShellThemeVisual__();
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.92);
    const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    const bgHex = __operatoreColorHex__(visual.bg || (isDark ? '#0f172a' : 'blue-4'));
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || (isDark ? '#94a3b8' : 'blue-4'));
    const bg = isDark ? hexToRgba(bgHex, Math.max(0.22, opacity)) : hexToRgba(bgHex, opacity);
    const border = isDark ? hexToRgba(borderHex, Math.max(0.34, opacity)) : hexToRgba(borderHex, 1);
    try{ shell.style.setProperty('background', bg, 'important'); }catch(_){ }
    try{ shell.style.setProperty('background-color', bg, 'important'); }catch(_){ }
    try{ shell.style.setProperty('border-color', border, 'important'); }catch(_){ }
    try{ shell.style.setProperty('box-shadow', 'none', 'important'); }catch(_){ }
    try{ shell.style.setProperty('backdrop-filter', 'none', 'important'); }catch(_){ }
  }catch(_){ }
}

function __openRoomSettingsShellThemePicker__(){
  try{
    const current = __roomSettingsShellThemeVisual__();
    __tagColorPopupOpen__('room-settings-shell-theme', current, (payload) => {
      try{
        const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
        const nextVisual = {
          bg: colors.bg || current.bg || 'blue-4',
          border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
          fg: '',
          opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.92)
        };
        __roomSettingsShellThemeWrite__(nextVisual);
        __applyRoomSettingsShellTheme__();
        try{ renderRoomSettingsPage(); }catch(_){ }
        try{ toast('Design container aggiornato'); }catch(_){ }
      }catch(_){ }
    }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:false,
      supportsOpacity:true,
      opacity:current.opacity ?? 0.92,
      defaultMode:'bg',
      fallbackBg:(current.bg || 'blue-4')
    });
  }catch(_){ }
}

function __bindRoomSettingsShellThemeLongPress__(){
  try{
    const el = document.querySelector('#page-roomsettings > .card');
    if (!el || el.dataset.roomSettingsShellThemeBound === '1') return;
    el.dataset.roomSettingsShellThemeBound = '1';
    let timer = null;
    let fired = false;
    const clear = ()=>{ try{ if (timer) clearTimeout(timer); }catch(_){ } timer = null; };
    const block = (e)=>{ try{ e?.preventDefault?.(); }catch(_){ } try{ e?.stopPropagation?.(); }catch(_){ } };
    const shouldIgnore = (target) => {
      try{
        if (!target || !target.closest) return false;
        return !!target.closest('.room-settings-panel, .room-settings-section-card, .room-settings-color-dot, .room-settings-square-btn, .room-settings-count-btn');
      }catch(_){ return false; }
    };
    const start = (e)=>{
      try{ if (e && e.type === 'mousedown' && (e.button || 0) !== 0) return; }catch(_){ }
      if (shouldIgnore(e?.target)) return;
      fired = false;
      clear();
      timer = setTimeout(() => {
        fired = true;
        try{ __sfxGlass(); }catch(_){ }
        __openRoomSettingsShellThemePicker__();
      }, 500);
    };
    const stop = (e)=>{
      clear();
      if (fired){
        block(e);
        setTimeout(()=>{ fired = false; },0);
      }
    };
    ['touchstart','mousedown'].forEach((evt)=>{ try{ el.addEventListener(evt, start, { passive:false, capture:true }); }catch(_){ } });
    ['touchend','touchcancel','mouseup','mouseleave'].forEach((evt)=>{ try{ el.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ } });
    try{ el.addEventListener('click', (e)=>{ if (!fired) return; block(e); }, true); }catch(_){ }
    try{ el.addEventListener('contextmenu', (e)=>{ if (shouldIgnore(e?.target)) return; block(e); }, true); }catch(_){ }
  }catch(_){ }
}


function __roomSettingsCardThemeRead__(){
  try{
    const raw = localStorage.getItem(__ROOM_SETTINGS_CARD_THEME_STORAGE_KEY__);
    if (!raw) return { bg:'', border:'', opacity:0.92 };
    const parsed = JSON.parse(raw);
    return __launcherVisualNormalize__(parsed, 'blue-4');
  }catch(_){ return { bg:'', border:'', opacity:0.92 }; }
}

function __roomSettingsCardThemeWrite__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    localStorage.setItem(__ROOM_SETTINGS_CARD_THEME_STORAGE_KEY__, JSON.stringify({
      fg: clean.fg || '',
      bg: clean.bg || '',
      border: clean.border || '',
      opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.92)
    }));
  }catch(_){ }
}

function __roomSettingsCardThemeVisual__(){
  const visual = __roomSettingsCardThemeRead__();
  return {
    fg: visual.fg || '',
    bg: visual.bg || 'blue-4',
    border: visual.border || visual.bg || 'blue-4',
    opacity: __designBgOpacityNormalize__(visual.opacity ?? 0.92)
  };
}

function __applyRoomSettingsCardTheme__(){
  try{
    const visual = __roomSettingsCardThemeVisual__();
    const opacity = __designBgOpacityNormalize__(visual.opacity ?? 0.92);
    const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
    const bgHex = __operatoreColorHex__(visual.bg || (isDark ? '#0f172a' : 'blue-4'));
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || (isDark ? '#94a3b8' : 'blue-4'));
    const bg = isDark ? hexToRgba(bgHex, Math.max(0.22, opacity)) : hexToRgba(bgHex, opacity);
    const border = isDark ? hexToRgba(borderHex, Math.max(0.34, opacity)) : hexToRgba(borderHex, 1);
    document.querySelectorAll('#page-roomsettings .room-settings-panel').forEach((el) => {
      try{ el.style.setProperty('background', bg, 'important'); }catch(_){ }
      try{ el.style.setProperty('background-color', bg, 'important'); }catch(_){ }
      try{ el.style.setProperty('border-color', border, 'important'); }catch(_){ }
      try{ el.style.setProperty('box-shadow', 'none', 'important'); }catch(_){ }
      try{ el.style.setProperty('backdrop-filter', 'none', 'important'); }catch(_){ }
    });
    document.querySelectorAll('#page-roomsettings .room-settings-section-card').forEach((el) => {
      try{ el.style.removeProperty('background'); }catch(_){ }
      try{ el.style.removeProperty('background-color'); }catch(_){ }
      try{ el.style.removeProperty('border-color'); }catch(_){ }
      try{ el.style.removeProperty('box-shadow'); }catch(_){ }
      try{ el.style.removeProperty('backdrop-filter'); }catch(_){ }
    });
  }catch(_){ }
}

function __openRoomSettingsCardThemePicker__(){
  try{
    const current = __roomSettingsCardThemeVisual__();
    __tagColorPopupOpen__('room-settings-card-theme', current, (payload) => {
      try{
        const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
        const nextVisual = {
          bg: colors.bg || current.bg || 'blue-4',
          border: colors.border || current.border || colors.bg || current.bg || 'blue-4',
          fg: '',
          opacity: __designBgOpacityNormalize__(payload?.opacity ?? current.opacity ?? 0.92)
        };
        __roomSettingsCardThemeWrite__(nextVisual);
        __applyRoomSettingsCardTheme__();
        try{ renderRoomSettingsPage(); }catch(_){ }
        try{ toast('Design schede aggiornato'); }catch(_){ }
      }catch(_){ }
    }, {
      supportsBg:true,
      supportsBorder:true,
      supportsFg:false,
      supportsOpacity:true,
      opacity:current.opacity ?? 0.92,
      defaultMode:'bg',
      fallbackBg:(current.bg || 'blue-4')
    });
  }catch(_){ }
}

function __bindRoomSettingsCardThemeLongPress__(){
  try{
    document.querySelectorAll('#page-roomsettings .room-settings-panel').forEach((el) => {
      try{
        if (!el || el.dataset.roomSettingsCardThemeBound === '1') return;
        el.dataset.roomSettingsCardThemeBound = '1';
        let timer = null;
        let fired = false;
        const clear = ()=>{ try{ if (timer) clearTimeout(timer); }catch(_){ } timer = null; };
        const block = (e)=>{ try{ e?.preventDefault?.(); }catch(_){ } try{ e?.stopPropagation?.(); }catch(_){ } };
        const shouldIgnore = (target) => {
          try{
            if (!target || !target.closest) return false;
            return !!target.closest('.room-settings-color-dot, .room-settings-square-btn, .room-settings-count-btn');
          }catch(_){ return false; }
        };
        const start = (e)=>{
          try{ if (e && e.type === 'mousedown' && (e.button || 0) !== 0) return; }catch(_){ }
          if (shouldIgnore(e?.target)) return;
          fired = false;
          clear();
          timer = setTimeout(() => {
            fired = true;
            try{ __sfxGlass(); }catch(_){ }
            __openRoomSettingsCardThemePicker__();
          }, 500);
        };
        const stop = (e)=>{
          clear();
          if (fired){
            block(e);
            setTimeout(()=>{ fired = false; },0);
          }
        };
        ['touchstart','mousedown'].forEach((evt)=>{ try{ el.addEventListener(evt, start, { passive:false, capture:true }); }catch(_){ } });
        ['touchend','touchcancel','mouseup','mouseleave'].forEach((evt)=>{ try{ el.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ } });
        try{ el.addEventListener('click', (e)=>{ if (!fired) return; block(e); }, true); }catch(_){ }
        try{ el.addEventListener('contextmenu', (e)=>{ if (shouldIgnore(e?.target)) return; block(e); }, true); }catch(_){ }
      }catch(_){ }
    });
  }catch(_){ }
}

function __refreshRoomSettingsThemeStatsUi__(){
  try{ renderStatGen(); }catch(_){ }
  try{ renderStatSpese(); }catch(_){ }
  try{ renderStatMensili(); }catch(_){ }
  try{ renderStatCancellazioni(); }catch(_){ }
  try{ __applyStatSharedLineChartWrapVisualToAll__(); }catch(_){ }
  try{ __refreshStatSharedLineCharts__(); }catch(_){ }
  try{ __refreshStatGraphPreviews__(); }catch(_){ }
  try{ if (state.page === 'statistiche') loadStatistichePage({ force:true }); }catch(_){ }
  try{ if (state.page === 'spese' && state.speseView === 'list') renderSpese(); }catch(_){ }
  try{ if (state.page === 'tassa' && typeof initTassaPage === 'function') initTassaPage(); }catch(_){ }
  try{ document.querySelectorAll('.btn.tax-quarter').forEach((node)=>{ try{ __applyTaxQuarterVisual__(node); }catch(_){ } }); }catch(_){ }
  try{ __applyTaxPageCardAll__(); }catch(_){ }
  try{ __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
  try{ __applyGuestFilterButtonVisuals__(); }catch(_){ }
  try{ __applyGuestListCardAll__(); }catch(_){ }
  try{ __applyRoomSettingsShellTheme__(); }catch(_){ }
  try{ __applyRoomSettingsShellTheme__(); }catch(_){ }
  try{ __applyRoomSettingsCardTheme__(); }catch(_){ }
}

function __themeTransferSingleActionIds__(){
  return ['themeTransferImport','themeTransferExport','themeTransferCancel'];
}

function __themeTransferSingleActionVisualsMerge__(incoming){
  try{
    const base = (__loadSingleActionButtonVisualMap__ && typeof __loadSingleActionButtonVisualMap__ === 'function')
      ? (__loadSingleActionButtonVisualMap__() || {})
      : {};
    const next = (incoming && typeof incoming === 'object') ? { ...incoming } : {};
    __themeTransferSingleActionIds__().forEach((id) => {
      try{
        if (!(id in next) && base && typeof base === 'object' && (id in base)) next[id] = base[id];
      }catch(_){ }
    });
    return next;
  }catch(_){ return (incoming && typeof incoming === 'object') ? incoming : {}; }
}

function __roomSettingsThemePayloadBuild__(){
  return {
    roomsUi: __sanitizeRoomsUiConfig__(getRoomsUiConfig()),
    launcherGridTheme: __launcherGridThemeRead__(),
    launcherIconColors: __launcherIconColorMapRead__(),
    statisticsCardTheme: __statisticsCardThemeRead__(),
    designBgOpacity: __designBgOpacityRead__(),
    headerActionTheme: __headerActionThemeRead__(),
    headerActionColors: __headerActionColorMapRead__(),
    pillTheme: __pillThemeRead__(),
    pillColors: __pillColorMapRead__(),
    singleActionButtonVisuals: __themeTransferSingleActionVisualsMerge__(__loadSingleActionButtonVisualMap__()),
    themeTransferButtonVisual: __roomSettingsThemeTransferButtonVisualRead__(),
    statsThemeStorage: __roomSettingsThemeStatsStorageCollect__(),
    roomThemeButtonVisuals: __roomSettingsThemeButtonVisualMapRead__(),
    textButtonVisuals: __roomSettingsTextButtonVisualMapRead__(),
    dateRangeCalendarTheme: __dateRangeCalendarThemeRead__()
  };
}

function __roomSettingsThemePayloadNormalize__(payload){
  const src = (payload && typeof payload === 'object') ? payload : {};
  const launcherIconColors = (src.launcherIconColors && typeof src.launcherIconColors === 'object') ? src.launcherIconColors : {};
  const designBgOpacity = __designBgOpacityNormalize__(src.designBgOpacity != null ? src.designBgOpacity : __designBgOpacityRead__());
  const statsThemeStorage = __roomSettingsThemeStatsStorageNormalize__(src.statsThemeStorage || src.statisticheThemeStorage || src.statsStorage || src.statistiche || {});
  try{
    if (!( __LAUNCHER_ICON_COLOR_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__LAUNCHER_ICON_COLOR_STORAGE_KEY__] = JSON.stringify(launcherIconColors || {});
  }catch(_){ }
  try{
    if (!( __LAUNCHER_GRID_THEME_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__LAUNCHER_GRID_THEME_STORAGE_KEY__] = JSON.stringify(src.launcherGridTheme || src.launcherTheme || {});
  }catch(_){ }
  try{
    if (!( __STATISTICS_CARD_THEME_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__STATISTICS_CARD_THEME_STORAGE_KEY__] = JSON.stringify(src.statisticsCardTheme || src.statCardsTheme || src.statsCardTheme || src.launcherGridTheme || src.launcherTheme || {});
  }catch(_){ }
  try{
    if (!( __DESIGN_BG_OPACITY_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__DESIGN_BG_OPACITY_STORAGE_KEY__] = String(designBgOpacity);
  }catch(_){ }
  try{
    if (!( __HEADER_ACTION_THEME_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__HEADER_ACTION_THEME_STORAGE_KEY__] = JSON.stringify(src.headerActionTheme || {});
  }catch(_){ }
  try{
    if (!( __HEADER_ACTION_COLOR_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__HEADER_ACTION_COLOR_STORAGE_KEY__] = JSON.stringify((src.headerActionColors && typeof src.headerActionColors === 'object') ? src.headerActionColors : {});
  }catch(_){ }
  try{
    if (!( __PILL_THEME_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__PILL_THEME_STORAGE_KEY__] = JSON.stringify(src.pillTheme || {});
  }catch(_){ }
  try{
    if (!( __PILL_COLOR_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__PILL_COLOR_STORAGE_KEY__] = JSON.stringify((src.pillColors && typeof src.pillColors === 'object') ? src.pillColors : {});
  }catch(_){ }
  try{
    if (!( __SINGLE_ACTION_BUTTON_VISUAL_STORAGE_KEY__ in statsThemeStorage)) statsThemeStorage[__SINGLE_ACTION_BUTTON_VISUAL_STORAGE_KEY__] = JSON.stringify((src.singleActionButtonVisuals && typeof src.singleActionButtonVisuals === 'object') ? src.singleActionButtonVisuals : {});
  }catch(_){ }
  return {
    roomsUi: __sanitizeRoomsUiConfig__(src.roomsUi || src.stanzeUi || src.rooms || null),
    launcherGridTheme: __launcherVisualNormalize__(src.launcherGridTheme || src.launcherTheme || {}, 'blue-4'),
    launcherIconColors,
    statisticsCardTheme: __launcherVisualNormalize__(src.statisticsCardTheme || src.statCardsTheme || src.statsCardTheme || src.launcherGridTheme || src.launcherTheme || {}, 'blue-4'),
    designBgOpacity,
    headerActionTheme: __launcherVisualNormalize__(src.headerActionTheme || {}, 'blue-4'),
    headerActionColors: (src.headerActionColors && typeof src.headerActionColors === 'object') ? src.headerActionColors : {},
    pillTheme: __launcherVisualNormalize__(src.pillTheme || {}, 'blue-4'),
    pillColors: (src.pillColors && typeof src.pillColors === 'object') ? src.pillColors : {},
    singleActionButtonVisuals: __themeTransferSingleActionVisualsMerge__((src.singleActionButtonVisuals && typeof src.singleActionButtonVisuals === 'object') ? src.singleActionButtonVisuals : {}),
    themeTransferButtonVisual: __launcherVisualNormalize__(src.themeTransferButtonVisual || src.roomSettingsThemeTransferButtonVisual || {}, 'blue-4'),
    statsThemeStorage,
    roomThemeButtonVisuals: (src.roomThemeButtonVisuals && typeof src.roomThemeButtonVisuals === 'object') ? src.roomThemeButtonVisuals : {},
    textButtonVisuals: (src.textButtonVisuals && typeof src.textButtonVisuals === 'object') ? src.textButtonVisuals : {},
    dateRangeCalendarTheme: __dateRangeCalendarThemeNormalize__(src.dateRangeCalendarTheme || src.calendarRangeTheme || src.dateRangeTheme || {})
  };
}

function __roomSettingsThemeSlotGet__(slot){
  const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
  const slots = __roomSettingsThemeSlotsRead__();
  const current = slots[key];
  return current ? __roomSettingsThemePayloadNormalize__(current) : null;
}

async function __roomSettingsThemeSlotSave__(slot){
  const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
  const slots = __roomSettingsThemeSlotsRead__();
  slots[key] = __roomSettingsThemePayloadBuild__();
  __roomSettingsThemeSlotsWrite__(slots);
  try{ renderRoomSettingsPage(); }catch(_){ }
  try{ toast(`Tema ${key} salvato`); }catch(_){ }
}

async function __roomSettingsThemeSlotApply__(slot){
  const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
  const payload = __roomSettingsThemeSlotGet__(key);
  if (!payload){
    try{ toast(`Tema ${key} non salvato`); }catch(_){ }
    return;
  }
  await saveRoomsUiConfigToSettings(payload.roomsUi, { showToast:false });
  const statsThemeStorage = (payload && payload.statsThemeStorage && typeof payload.statsThemeStorage === 'object') ? payload.statsThemeStorage : {};
  const hasLauncherSnapshot = !!(statsThemeStorage && (__LAUNCHER_ICON_COLOR_STORAGE_KEY__ in statsThemeStorage || __LAUNCHER_GRID_THEME_STORAGE_KEY__ in statsThemeStorage));
  const hasStatisticsSnapshot = !!(statsThemeStorage && (__STATISTICS_CARD_THEME_STORAGE_KEY__ in statsThemeStorage || __SPESA_CARD_VISUAL_STORAGE_KEY__ in statsThemeStorage || __SPESA_CARD_OPACITY_STORAGE_KEY__ in statsThemeStorage || __TAX_QUARTER_VISUAL_STORAGE_KEY__ in statsThemeStorage || Object.keys(statsThemeStorage).some((k) => String(k || '').startsWith('dDAE_statcard_colors_') || String(k || '').startsWith('dDAE_stat_card_text_') || String(k || '').startsWith('ddae_stat_card_text_') || String(k || '').startsWith('ddae_graph_colors_'))));
  const hasHeaderSnapshot = !!(statsThemeStorage && (__HEADER_ACTION_THEME_STORAGE_KEY__ in statsThemeStorage || __HEADER_ACTION_COLOR_STORAGE_KEY__ in statsThemeStorage));
  try{
    __launcherGridThemeWrite__(payload.launcherGridTheme);
    __launcherIconColorMapWrite__(payload.launcherIconColors || {});
    __designBgOpacityWrite__(payload.designBgOpacity != null ? payload.designBgOpacity : __designBgOpacityRead__());
    if (!hasLauncherSnapshot) __launcherGridThemeOverwriteTargets__(payload.launcherGridTheme);
    if (!hasStatisticsSnapshot) __statisticsCardThemeWrite__(payload.statisticsCardTheme || payload.launcherGridTheme);
    else {
      const statsVisual = __launcherVisualNormalize__(payload.statisticsCardTheme || payload.launcherGridTheme || {}, 'blue-4');
      localStorage.setItem(__STATISTICS_CARD_THEME_STORAGE_KEY__, JSON.stringify({ fg: statsVisual.fg || 'white', bg: statsVisual.bg || 'blue-4', border: statsVisual.border || statsVisual.bg || 'blue-4', opacity: __designBgOpacityNormalize__(statsVisual.opacity ?? designBgOpacity ?? 0.80) }));
    }
    __headerActionThemeWrite__(payload.headerActionTheme || { fg:'blue-4', bg:'white', border:'white' });
    __headerActionColorMapWrite__(payload.headerActionColors || {});
    __pillThemeWrite__(payload.pillTheme || { fg:'white', bg:'blue-4', border:'blue-4' });
    __pillColorMapWrite__(payload.pillColors || {});
    __saveSingleActionButtonVisualMap__(__themeTransferSingleActionVisualsMerge__((payload.singleActionButtonVisuals && typeof payload.singleActionButtonVisuals === 'object') ? payload.singleActionButtonVisuals : {}));
    __roomSettingsThemeTransferButtonVisualWrite__(payload.themeTransferButtonVisual || __roomSettingsThemeTransferButtonVisualRead__());
    __roomSettingsThemeButtonVisualMapWrite__((payload.roomThemeButtonVisuals && typeof payload.roomThemeButtonVisuals === 'object') ? payload.roomThemeButtonVisuals : {});
    __roomSettingsTextButtonVisualMapWrite__((payload.textButtonVisuals && typeof payload.textButtonVisuals === 'object') ? payload.textButtonVisuals : {});
    __dateRangeCalendarThemeWrite__(payload.dateRangeCalendarTheme || __dateRangeCalendarThemeDefault__());
  }catch(_){ }
  try{ __roomSettingsThemeStatsStorageApply__(statsThemeStorage); }catch(_){ }
  try{ applyAppTextUi(document.body); }catch(_){ }
  try{
    if (!hasHeaderSnapshot) __headerActionThemeOverwriteTargets__(payload.headerActionTheme || { fg:'blue-4', bg:'white', border:'white' });
  }catch(_){ }
  try{ __launcherIconApplyAll__(); }catch(_){ }
  try{ __headerActionApplyAll__(); }catch(_){ }
  try{ __pillApplyAll__(); }catch(_){ }
  try{ __dateRangeCalendarApplyTheme__(); }catch(_){ }
  try{ __refreshRoomSettingsThemeStatsUi__(); }catch(_){ }
  try{ __roomSettingsThemeActiveSlotWrite__(key); }catch(_){ }
  try{ renderRoomSettingsPage(); }catch(_){ }
  try{ toast(`Tema ${key} richiamato`); }catch(_){ }
}

async function __roomSettingsThemeSlotAskSave__(slot){
  const key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
  const choice = await __confirmTwoActions__(`Salvare il tema ${key}?`, 'Sì', 'No');
  if (choice !== 'yes') return;
  return __roomSettingsThemeSlotSave__(key);
}


const __THEME_EXPORT_KIND__ = 'dDAE_theme_export';

const __ROOM_SETTINGS_THEME_TRANSFER_BTN_STORAGE_KEY__ = 'dDAE_room_settings_theme_transfer_btn_v1';

function __roomSettingsThemeTransferButtonVisualRead__(){
  try{
    const raw = localStorage.getItem(__ROOM_SETTINGS_THEME_TRANSFER_BTN_STORAGE_KEY__);
    const parsed = raw ? JSON.parse(raw) : {};
    const clean = __launcherVisualNormalize__(parsed || {}, 'blue-4');
    return { bg: clean.bg || 'white', border: clean.border || clean.bg || 'blue-4', fg: clean.fg || 'blue-4', opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.75), bold: !!clean.bold };
  }catch(_){ return { bg:'white', border:'blue-4', fg:'blue-4', opacity:0.75, bold:false }; }
}

function __roomSettingsThemeTransferButtonVisualWrite__(visual){
  try{
    const clean = __launcherVisualNormalize__(visual || {}, 'blue-4');
    localStorage.setItem(__ROOM_SETTINGS_THEME_TRANSFER_BTN_STORAGE_KEY__, JSON.stringify({ bg: clean.bg || 'white', border: clean.border || clean.bg || 'blue-4', fg: clean.fg || 'blue-4', opacity: __designBgOpacityNormalize__(clean.opacity ?? 0.75), bold: !!clean.bold }));
  }catch(_){ }
}

function __roomSettingsThemeTransferButtonVisualApply__(el){
  try{
    if (!el) return;
    const visual = __roomSettingsThemeTransferButtonVisualRead__();
    const bgHex = __operatoreColorHex__(visual.bg || 'white');
    const borderHex = __operatoreColorHex__(visual.border || visual.bg || 'blue-4');
    const fgHex = __tagColorTextHex__(visual.bg || bgHex, visual.fg || 'blue-4', false) || __operatoreColorHex__(visual.fg || 'blue-4');
    el.setAttribute('style', `background:${hexToRgba(bgHex, visual.opacity)} !important;background-color:${hexToRgba(bgHex, visual.opacity)} !important;border-color:${hexToRgba(borderHex, 1)} !important;color:${fgHex} !important;-webkit-text-fill-color:${fgHex} !important;`);
    try{ el.querySelectorAll('.room-settings-square-preview, .room-settings-square-preview *, svg, .ui-ico').forEach((node)=>{ node.style.setProperty('color', fgHex, 'important'); node.style.setProperty('stroke', fgHex, 'important'); }); }catch(_){ }
    try{ el.querySelectorAll('path, circle, rect, line, polyline, polygon, ellipse').forEach((node)=>{ node.style.setProperty('stroke', fgHex, 'important'); if (String(node.getAttribute('fill') || '').toLowerCase() !== 'none') node.style.setProperty('fill', 'none', 'important'); }); }catch(_){ }
    __applyVisualTextWeight__(el, visual);
  }catch(_){ }
}

async function __openRoomSettingsThemeTransferButtonColorPicker__(){
  try{
    const btn = document.getElementById('roomSettingsThemeTransferBtn');
    if (!btn) return;
    const initial = __roomSettingsThemeTransferButtonVisualRead__();
    const applyVisual = (payload) => {
      const colors = (payload && payload.colors && typeof payload.colors === 'object') ? payload.colors : {};
      const next = { bg: colors.bg || initial.bg || 'white', border: colors.border || initial.border || colors.bg || initial.bg || 'blue-4', fg: colors.fg || initial.fg || 'blue-4', opacity: __designBgOpacityNormalize__(payload?.opacity ?? initial.opacity ?? 0.75), bold: false };
      __roomSettingsThemeTransferButtonVisualWrite__(next);
      try{ renderRoomSettingsPage(); }catch(_){ }
    };
    const revertVisual = () => { __roomSettingsThemeTransferButtonVisualWrite__(initial); try{ renderRoomSettingsPage(); }catch(_){ } };
    __tagColorPopupOpen__('room-theme-transfer-button', initial, (payload) => { applyVisual(payload); }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:initial.opacity ?? 0.75, defaultMode:'bg', fallbackBg:(initial.bg || 'white'), onPreview:applyVisual, onRevert:revertVisual });
  }catch(_){ }
}

function __themeTransferFileName__(){
  try{
    const dtObj = new Date();
    const dd = String(dtObj.getDate()).padStart(2, '0');
    const mm = String(dtObj.getMonth() + 1).padStart(2, '0');
    const yyyy = String(dtObj.getFullYear());
    return `${dd}-${mm}-${yyyy}_Tema_${BUILD_VERSION}.json`;
  }catch(_){ return `Tema_${BUILD_VERSION}.json`; }
}

function __closeThemeTransferModal__(){
  try{ __modalHideById__('themeTransferModal'); }catch(_){ }
}

function __openThemeTransferModal__(){
  try{ __modalShowById__('themeTransferModal'); }catch(_){ }
}

async function __exportThemeFile__(){
  try{
    const payload = {
      kind: __THEME_EXPORT_KIND__,
      schemaVersion: 1,
      exportedAt: __nowIso__(),
      build: BUILD_VERSION,
      theme: __roomSettingsThemePayloadBuild__(),
      themeSlots: __collectBackupThemeSlots__()
    };
    const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = __safeFileName__(__themeTransferFileName__());
    document.body.appendChild(a);
    try{ a.click(); }catch(_){ }
    setTimeout(()=>{
      try{ document.body.removeChild(a); }catch(_){ }
      try{ URL.revokeObjectURL(url); }catch(_){ }
    }, 700);
    try{ toast('Tema esportato', 'green'); }catch(_){ }
  }catch(_){
    try{ toast('Errore export tema', 'orange'); }catch(__){ }
  }
}

async function __importThemeFile__(){
  try{
    const file = await __pickDbImportFile__();
    if (!file) return;
    const text = await file.text();
    let data = null;
    try{ data = JSON.parse(text); }catch(_){ data = null; }
    if (!data || typeof data !== 'object'){
      try{ toast('File tema non valido', 'orange'); }catch(_){ }
      return;
    }
    const directTheme = (data.theme && typeof data.theme === 'object') ? data.theme : null;
    const fallbackTheme = (data.roomsUi || data.launcherGridTheme || data.headerActionTheme || data.pillTheme || data.singleActionButtonVisuals) ? data : null;
    const themePayload = directTheme || fallbackTheme;
    if (!themePayload || typeof themePayload !== 'object'){
      try{ toast('Tema non compatibile', 'orange'); }catch(_){ }
      return;
    }
    const normalized = __roomSettingsThemePayloadNormalize__(themePayload);
    await saveRoomsUiConfigToSettings(normalized.roomsUi, { showToast:false });
    try{ __launcherGridThemeWrite__(normalized.launcherGridTheme); }catch(_){ }
    try{ __launcherIconColorMapWrite__(normalized.launcherIconColors || {}); }catch(_){ }
    try{ __statisticsCardThemeWrite__(normalized.statisticsCardTheme || normalized.launcherGridTheme); }catch(_){ }
    try{ __designBgOpacityWrite__(normalized.designBgOpacity != null ? normalized.designBgOpacity : __designBgOpacityRead__()); }catch(_){ }
    try{ __headerActionThemeWrite__(normalized.headerActionTheme || { fg:'blue-4', bg:'white', border:'white' }); }catch(_){ }
    try{ __headerActionColorMapWrite__(normalized.headerActionColors || {}); }catch(_){ }
    try{ __pillThemeWrite__(normalized.pillTheme || { fg:'white', bg:'blue-4', border:'blue-4' }); }catch(_){ }
    try{ __pillColorMapWrite__(normalized.pillColors || {}); }catch(_){ }
    try{ __saveSingleActionButtonVisualMap__(__themeTransferSingleActionVisualsMerge__((normalized.singleActionButtonVisuals && typeof normalized.singleActionButtonVisuals === 'object') ? normalized.singleActionButtonVisuals : {})); }catch(_){ }
    try{ __roomSettingsThemeTransferButtonVisualWrite__(normalized.themeTransferButtonVisual || __roomSettingsThemeTransferButtonVisualRead__()); }catch(_){ }
    try{ __roomSettingsThemeButtonVisualMapWrite__((normalized.roomThemeButtonVisuals && typeof normalized.roomThemeButtonVisuals === 'object') ? normalized.roomThemeButtonVisuals : {}); }catch(_){ }
    try{ __roomSettingsTextButtonVisualMapWrite__((normalized.textButtonVisuals && typeof normalized.textButtonVisuals === 'object') ? normalized.textButtonVisuals : {}); }catch(_){ }
    try{ __dateRangeCalendarThemeWrite__(normalized.dateRangeCalendarTheme || __dateRangeCalendarThemeDefault__()); }catch(_){ }
    try{ __roomSettingsThemeStatsStorageApply__(normalized.statsThemeStorage || {}); }catch(_){ }
    try{ __restoreBackupThemeSlots__(data.themeSlots || data.slots || (data.meta && data.meta.themeSlots) || null); }catch(_){ }
    try{ applyAppTextUi(document.body); }catch(_){ }
    try{ __launcherIconApplyAll__(); }catch(_){ }
    try{ __headerActionApplyAll__(); }catch(_){ }
    try{ __pillApplyAll__(); }catch(_){ }
    try{ __dateRangeCalendarApplyTheme__(); }catch(_){ }
    try{ __refreshRoomSettingsThemeStatsUi__(); }catch(_){ }
    try{ renderRoomSettingsPage(); }catch(_){ }
    try{ __closeThemeTransferModal__(); }catch(_){ }
    try{ toast('Tema importato', 'green'); }catch(_){ }
  }catch(_){
    try{ toast('Errore import tema', 'orange'); }catch(__){ }
  }
}

function __setupThemeTransferModal__(){
  try{
    [['themeTransferCancel', __closeThemeTransferModal__], ['themeTransferExport', __exportThemeFile__], ['themeTransferImport', __importThemeFile__]].forEach(([id, fn])=>{
      const el = document.getElementById(id);
      if (el && !el.__boundThemeTransferAction){ el.__boundThemeTransferAction = true; bindFastTap(el, fn); }
    });
    const modal = document.getElementById('themeTransferModal');
    if (modal && !modal.__boundThemeTransferModal){
      modal.__boundThemeTransferModal = true;
      modal.addEventListener('click', (e)=>{ if (e.target === modal) __closeThemeTransferModal__(); });
    }
  }catch(_){ }
}

function renderRoomSettingsPage(){
  try{
    const page = document.getElementById('page-roomsettings');
    if (page){
      const pageTitle = page.querySelector('.page-title');
      if (pageTitle) pageTitle.textContent = __designTranslate__('Design', { en:'Design', fr:'Design', de:'Design', es:'Diseño' });
      const sectionTitles = page.querySelectorAll('.room-settings-section-title');
      if (sectionTitles[0]) sectionTitles[0].textContent = __designTranslate__('Tema', { en:'Theme', fr:'Thème', de:'Thema', es:'Tema' });
      if (sectionTitles[1]) sectionTitles[1].textContent = __designTranslate__('Testi', { en:'Text', fr:'Texte', de:'Text', es:'Texto' });
      if (sectionTitles[2]) sectionTitles[2].textContent = __designTranslate__('Tag colore stanze', { en:'Room color tags', fr:'Tags couleur des chambres', de:'Zimmer-Farbmarken', es:'Etiquetas de color de habitaciones' });
      if (sectionTitles[3]) sectionTitles[3].textContent = __designTranslate__('Tag colore opzioni', { en:'Option color tags', fr:'Tags couleur des options', de:'Options-Farbmarken', es:'Etiquetas de color de opciones' });
      if (sectionTitles[4]) sectionTitles[4].textContent = __designTranslate__('Tag colore letti', { en:'Bed color tags', fr:'Tags couleur des lits', de:'Bett-Farbmarken', es:'Etiquetas de color de camas' });
      const title = page.querySelector('.room-settings-title');
      if (title) title.textContent = __designTranslate__('Numero stanze', { en:'Room count', fr:'Nombre de chambres', de:'Zimmeranzahl', es:'Número de habitaciones' });
      const textSubtitle = document.getElementById('roomSettingsTextSubtitle');
      if (textSubtitle) textSubtitle.textContent = __designTranslate__('1 2 3 = dimensione', { en:'1 2 3 = size', fr:'1 2 3 = taille', de:'1 2 3 = Größe', es:'1 2 3 = tamaño' });
    }
    const cfg = getRoomsUiConfig();
    const count = getConfiguredRoomsCount(6);
    renderRoomSettingsTextControls();
    const countBtn = document.getElementById('roomSettingsCountBtn');
    if (countBtn){
      countBtn.textContent = String(count);
      countBtn.setAttribute('aria-label', `Numero stanze: ${count}`);
      countBtn.title = `Numero stanze: ${count}`;
    }
    const applyBtn = (id, spec, label) => {
      const el = document.getElementById(id);
      if (!el) return;
      el.textContent = label;
      el.setAttribute('style', __roomsUiButtonStyle__(spec, true));
    };
    applyBtn('roomSettingsNightsBtn', cfg.nights, 'N');
    applyBtn('roomSettingsMBtn', cfg.options?.m, 'M');
    applyBtn('roomSettingsGBtn', cfg.options?.g, 'G');
    applyBtn('roomSettingsCBtn', cfg.options?.c, 'C');
    applyBtn('roomSettingsBedMBtn', cfg.beds?.matrimoniale, 'M');
    applyBtn('roomSettingsBedSBtn', cfg.beds?.singolo, 'S');
    applyBtn('roomSettingsBedCBtn', cfg.beds?.culla, 'C');
    const activeThemeSlot = __roomSettingsThemeActiveSlotRead__();
    [1,2,3,4].forEach((slot) => {
      const el = document.getElementById(`roomSettingsThemeBtn${slot}`);
      if (!el) return;
      const saved = __roomSettingsThemeSlotGet__(slot);
      const isActive = String(activeThemeSlot || '') === String(slot);
      el.textContent = String(slot);
      el.classList.toggle('is-active', isActive);
      const visualFallback = saved?.launcherGridTheme || { bg:'blue-4', border:'blue-4', fg:'', opacity:0.75 };
      __roomSettingsThemeButtonVisualApply__(el, slot, visualFallback, isActive ? 'active' : 'distractive');
      if (saved){
        const savedLabel = __designTranslate__(`Tema ${slot} salvato. Tap per richiamare, doppio tap per salvare, pressione lunga per modificare il tasto`, { en:`Theme ${slot} saved. Tap to recall, double tap to save, long press to edit button`, fr:`Thème ${slot} enregistré. Touchez pour rappeler, double tap pour enregistrer, appui long pour modifier le bouton`, de:`Thema ${slot} gespeichert. Tippen zum Laden, doppelt tippen zum Speichern, lange drücken zum Bearbeiten`, es:`Tema ${slot} guardado. Toca para aplicar, doble toque para guardar, pulsación larga para editar el botón` });
        el.setAttribute('aria-label', savedLabel);
        el.title = savedLabel;
      }else{
        const emptyLabel = __designTranslate__(`Tema ${slot} vuoto. Tap per richiamare, doppio tap per salvare, pressione lunga per modificare il tasto`, { en:`Theme ${slot} empty. Tap to recall, double tap to save, long press to edit button`, fr:`Thème ${slot} vide. Touchez pour rappeler, double tap pour enregistrer, appui long pour modifier le bouton`, de:`Thema ${slot} leer. Tippen zum Laden, doppelt tippen zum Speichern, lange drücken zum Bearbeiten`, es:`Tema ${slot} vacío. Toca para aplicar, doble toque para guardar, pulsación larga para editar el botón` });
        el.setAttribute('aria-label', emptyLabel);
        el.title = emptyLabel;
      }
    });
    const launcherThemeBtn = document.getElementById('roomSettingsLauncherThemeBtn');
    if (launcherThemeBtn){
      launcherThemeBtn.innerHTML = '<span aria-hidden="true" class="room-settings-square-preview room-settings-square-preview-grid"></span><span class="room-settings-square-caption">App</span>'; 
      launcherThemeBtn.setAttribute('style', __launcherGridThemeButtonStyle__());
      launcherThemeBtn.classList.remove('room-settings-square-btn-placeholder');
      launcherThemeBtn.setAttribute('aria-label', __designTranslate__('Design icone e tasti griglie', { en:'Grid icon and button design', fr:'Design des icônes et boutons de grille', de:'Design der Raster-Icons und -Tasten', es:'Diseño de iconos y botones de cuadrícula' }));
      launcherThemeBtn.title = __designTranslate__('Design icone e tasti griglie', { en:'Grid icon and button design', fr:'Design des icônes et boutons de grille', de:'Design der Raster-Icons und -Tasten', es:'Diseño de iconos y botones de cuadrícula' });
    }
    const statisticsThemeBtn = document.getElementById('roomSettingsLauncherExtraBtn1');
    if (statisticsThemeBtn){
      statisticsThemeBtn.innerHTML = '<span aria-hidden="true" class="room-settings-square-preview room-settings-square-preview-cards"></span><span class="room-settings-square-caption">Card</span>'; 
      statisticsThemeBtn.setAttribute('style', __statisticsCardThemeButtonStyle__());
      statisticsThemeBtn.classList.remove('room-settings-square-btn-placeholder');
      statisticsThemeBtn.setAttribute('aria-label', __designTranslate__('Design card statistiche', { en:'Statistics card design', fr:'Design des cartes statistiques', de:'Design der Statistik-Karten', es:'Diseño de tarjetas estadísticas' }));
      statisticsThemeBtn.title = __designTranslate__('Design card statistiche', { en:'Statistics card design', fr:'Design des cartes statistiques', de:'Design der Statistik-Karten', es:'Diseño de tarjetas estadísticas' });
    }
    const opacityBtn = document.getElementById('roomSettingsLauncherExtraBtn2');
    if (opacityBtn){
      opacityBtn.innerHTML = '<span aria-hidden="true" class="room-settings-square-preview room-settings-square-preview-topbar"><span></span></span><span class="room-settings-square-caption">Barra</span>'; 
      opacityBtn.setAttribute('style', __headerActionThemeButtonStyle__());
      opacityBtn.classList.remove('room-settings-square-btn-placeholder');
      opacityBtn.setAttribute('aria-label', __designTranslate__('Design tasti top bar e titoli pagina', { en:'Top bar and page title button design', fr:'Design des boutons de la barre supérieure et des titres de page', de:'Design der Topbar- und Seitentitel-Schaltflächen', es:'Diseño de botones de la barra superior y títulos de página' }));
      opacityBtn.title = __designTranslate__('Design tasti top bar e titoli pagina', { en:'Top bar and page title button design', fr:'Design des boutons de la barre supérieure et des titres de page', de:'Design der Topbar- und Seitentitel-Schaltflächen', es:'Diseño de botones de la barra superior y títulos de página' });
    }
    const pillBtn = document.getElementById('roomSettingsLauncherExtraBtn3');
    if (pillBtn){
      pillBtn.innerHTML = '<span aria-hidden="true" class="room-settings-square-preview room-settings-square-preview-pill"></span><span class="room-settings-square-caption">Pill</span>'; 
      pillBtn.setAttribute('style', __pillThemeButtonStyle__());
      pillBtn.classList.remove('room-settings-square-btn-placeholder');
      pillBtn.setAttribute('aria-label', __designTranslate__('Design pulsanti pill', { en:'Pill button design', fr:'Design des boutons pill', de:'Pill-Button-Design', es:'Diseño de botones pill' }));
      pillBtn.title = __designTranslate__('Design pulsanti pill', { en:'Pill button design', fr:'Design des boutons pill', de:'Pill-Button-Design', es:'Diseño de botones pill' });
    }
    const themeTransferBtn = document.getElementById('roomSettingsThemeTransferBtn');
    if (themeTransferBtn){
      themeTransferBtn.innerHTML = '<span aria-hidden="true" class="room-settings-square-preview room-settings-square-preview-upload"><svg class="ui-ico" viewBox="0 0 24 24"><path d="M12 19V7"></path><path d="m7 12 5-5 5 5"></path><path d="M5 21h14"></path></svg></span><span class="room-settings-square-caption"></span>';
      __roomSettingsThemeTransferButtonVisualApply__(themeTransferBtn);
      themeTransferBtn.classList.remove('room-settings-square-btn-placeholder');
      themeTransferBtn.setAttribute('aria-label', __designTranslate__('Importa o esporta tema', { en:'Import or export theme', fr:'Importer ou exporter le thème', de:'Thema importieren oder exportieren', es:'Importar o exportar tema' }));
      themeTransferBtn.title = __designTranslate__('Importa o esporta tema', { en:'Import or export theme', fr:'Importer ou exporter le thème', de:'Thema importieren oder exportieren', es:'Importar o exportar tema' });
    }
    const dots = document.getElementById('roomSettingsDots');
    if (dots){
      const parts = [];
      for (let i = 1; i <= count; i++) parts.push(`<button aria-label="Colore stanza ${i}" class="room-settings-color-dot" data-room-color="${i}" style="${__roomsUiButtonStyle__(cfg.rooms?.[String(i)] || 'blue-4', true)}" type="button">${i}</button>`);
      dots.innerHTML = parts.join('');
      dots.style.setProperty('--room-settings-cols', String(Math.min(6, Math.max(1, count || 1))));
    }
  }catch(_){ }
  try{ __applyRoomSettingsCardTheme__(); }catch(_){ }
}

function setupRoomSettingsPage(){
  bindRoomSettingsTextControls();
  try{ __setupThemeTransferModal__(); }catch(_){ }
  const countBtn = document.getElementById('roomSettingsCountBtn');
  if (countBtn && !countBtn.__boundRoomsConfigTap){
    countBtn.__boundRoomsConfigTap = true;
    let holdTimer = null;
    let holdTriggered = false;
    let touchAt = 0;
    const clearHold = () => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      holdTimer = null;
      holdTriggered = false;
    };
    const saveValue = async (next) => {
      try{
        const current = getConfiguredRoomsCount(6);
        if (Number(next) === Number(current)) { renderRoomSettingsPage(); return; }
        await saveRoomsCountSetting(next);
        renderRoomSettingsPage();
      }catch(e){ try{ toast(e?.message || 'Errore numero stanze'); }catch(_){ } }
    };
    const doTap = async () => {
      try{ __sfxTap(); }catch(_){ }
      const current = getConfiguredRoomsCount(6);
      const next = (current >= 12) ? 1 : (current + 1);
      await saveValue(next);
    };
    const doLong = async () => {
      try{ __sfxGlass(); }catch(_){ }
      holdTriggered = true;
      await saveValue(0);
    };
    countBtn.addEventListener('touchstart', (e) => {
      touchAt = Date.now();
      clearHold();
      holdTimer = setTimeout(() => { doLong(); }, 500);
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, { passive:false, capture:true });
    countBtn.addEventListener('touchend', async (e) => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      if (!holdTriggered) await doTap();
      clearHold();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, { passive:false, capture:true });
    countBtn.addEventListener('touchcancel', (e) => {
      clearHold();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, { passive:false, capture:true });
    countBtn.addEventListener('click', async (e) => {
      if (Date.now() - touchAt < 450) { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } return; }
      await doTap();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, true);
  }
  [1,2,3,4].forEach((slot) => {
    const el = document.getElementById(`roomSettingsThemeBtn${slot}`);
    if (!el || el.__boundRoomThemeSlot) return;
    el.__boundRoomThemeSlot = true;
    let holdTimer = null;
    let holdTriggered = false;
    let touchAt = 0;
    let lastTapAt = 0;
    let singleTapTimer = null;
    const clearHold = () => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      holdTimer = null;
    };
    const clearSingleTap = () => {
      try{ if (singleTapTimer) clearTimeout(singleTapTimer); }catch(_){ }
      singleTapTimer = null;
    };
    const doTap = async () => {
      try{ __sfxTap(); }catch(_){ }
      await __roomSettingsThemeSlotApply__(slot);
    };
    const doDouble = async () => {
      try{ __sfxGlass(); }catch(_){ }
      await __roomSettingsThemeSlotSave__(slot);
    };
    const doLong = async () => {
      try{ __sfxGlass(); }catch(_){ }
      holdTriggered = true;
      clearSingleTap();
      await __openRoomSettingsThemeButtonColorPicker__(slot);
    };
    const scheduleTap = () => {
      const now = Date.now();
      if (now - lastTapAt <= 320){
        lastTapAt = 0;
        clearSingleTap();
        doDouble();
        return;
      }
      lastTapAt = now;
      clearSingleTap();
      singleTapTimer = setTimeout(() => {
        lastTapAt = 0;
        doTap();
      }, 280);
    };
    el.addEventListener('touchstart', (e) => {
      touchAt = Date.now();
      holdTriggered = false;
      clearHold();
      holdTimer = setTimeout(() => { doLong(); }, 500);
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, { passive:false, capture:true });
    el.addEventListener('touchend', (e) => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      if (!holdTriggered) scheduleTap();
      clearHold();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
      setTimeout(()=>{ holdTriggered = false; },0);
    }, { passive:false, capture:true });
    el.addEventListener('touchcancel', (e) => {
      clearHold();
      holdTriggered = false;
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, { passive:false, capture:true });
    el.addEventListener('click', (e) => {
      if (Date.now() - touchAt < 450) { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } return; }
      if (holdTriggered){ try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } holdTriggered = false; return; }
      scheduleTap();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, true);
    el.addEventListener('dblclick', (e) => {
      clearSingleTap();
      lastTapAt = 0;
      doDouble();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    }, true);
    el.addEventListener('contextmenu', (e) => { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } });
    el.addEventListener('mousedown', (e) => {
      if ((e.button || 0) !== 0) return;
      holdTriggered = false;
      clearHold();
      holdTimer = setTimeout(() => { doLong(); }, 500);
    });
    ['mouseup','mouseleave'].forEach((evt) => el.addEventListener(evt, () => {
      try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ }
      clearHold();
    }));
  });
  const launcherThemeBtn = document.getElementById('roomSettingsLauncherThemeBtn');
  if (launcherThemeBtn && !launcherThemeBtn.__boundLauncherThemeBtn){
    launcherThemeBtn.__boundLauncherThemeBtn = true;
    bindFastTap(launcherThemeBtn, () => { __openLauncherGridThemePicker__(); });
  }
  const statisticsThemeBtn = document.getElementById('roomSettingsLauncherExtraBtn1');
  if (statisticsThemeBtn && !statisticsThemeBtn.__boundStatisticsCardThemeBtn){
    statisticsThemeBtn.__boundStatisticsCardThemeBtn = true;
    bindFastTap(statisticsThemeBtn, () => { __openStatisticsCardThemePicker__(); });
  }
  const opacityBtn = document.getElementById('roomSettingsLauncherExtraBtn2');
  if (opacityBtn && !opacityBtn.__boundDesignOpacityBtn){
    opacityBtn.__boundDesignOpacityBtn = true;
    bindFastTap(opacityBtn, () => { __openHeaderActionThemePicker__(); });
  }
  const pillBtn = document.getElementById('roomSettingsLauncherExtraBtn3');
  if (pillBtn && !pillBtn.__boundPillThemeBtn){
    pillBtn.__boundPillThemeBtn = true;
    bindFastTap(pillBtn, () => { __openPillThemePicker__(); });
  }
  const themeTransferBtn = document.getElementById('roomSettingsThemeTransferBtn');
  if (themeTransferBtn && !themeTransferBtn.__boundThemeTransferBtn){
    themeTransferBtn.__boundThemeTransferBtn = true;
    let holdTimer = null;
    let holdTriggered = false;
    let touchAt = 0;
    const clearHold = () => { try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ } holdTimer = null; };
    const doTap = () => { try{ __sfxTap(); }catch(_){ } __openThemeTransferModal__(); };
    const doLong = async() => { try{ __sfxGlass(); }catch(_){ } holdTriggered = true; await __openRoomSettingsThemeTransferButtonColorPicker__(); };
    themeTransferBtn.addEventListener('touchstart', (e) => { touchAt = Date.now(); holdTriggered = false; clearHold(); holdTimer = setTimeout(() => { doLong(); }, 500); try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } }, { passive:false, capture:true });
    themeTransferBtn.addEventListener('touchend', (e) => { try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ } if (!holdTriggered) doTap(); clearHold(); try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } setTimeout(()=>{ holdTriggered = false; },0); }, { passive:false, capture:true });
    themeTransferBtn.addEventListener('touchcancel', (e) => { clearHold(); holdTriggered = false; try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } }, { passive:false, capture:true });
    themeTransferBtn.addEventListener('click', (e) => { if (Date.now() - touchAt < 450) { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } return; } if (holdTriggered){ try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } holdTriggered = false; return; } doTap(); try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } }, true);
    themeTransferBtn.addEventListener('contextmenu', (e) => { try{ e.preventDefault(); e.stopPropagation(); }catch(_){ } });
    themeTransferBtn.addEventListener('mousedown', (e) => { if ((e.button || 0) !== 0) return; holdTriggered = false; clearHold(); holdTimer = setTimeout(() => { doLong(); }, 500); });
    ['mouseup','mouseleave'].forEach((evt) => themeTransferBtn.addEventListener(evt, () => { try{ if (holdTimer) clearTimeout(holdTimer); }catch(_){ } clearHold(); }));
  }
  const dotsWrap = document.getElementById('roomSettingsDots');
  if (dotsWrap && !dotsWrap.__boundRoomColorTap){
    dotsWrap.__boundRoomColorTap = true;
    dotsWrap.addEventListener('click', (ev) => {
      const btn = ev.target && ev.target.closest ? ev.target.closest('button[data-room-color]') : null;
      if (!btn) return;
      const room = String(btn.getAttribute('data-room-color') || '').trim();
      if (!room) return;
      __openRoomSettingsColorPicker__(`room:${room}`);
    });
  }
  [['roomSettingsNightsBtn','nights'],['roomSettingsMBtn','option:m'],['roomSettingsGBtn','option:g'],['roomSettingsCBtn','option:c'],['roomSettingsBedMBtn','bed:matrimoniale'],['roomSettingsBedSBtn','bed:singolo'],['roomSettingsBedCBtn','bed:culla']].forEach(([id, target]) => {
    const el = document.getElementById(id);
    if (!el || el.__boundRoomSettingsColorTap) return;
    el.__boundRoomSettingsColorTap = true;
    bindFastTap(el, () => { __openRoomSettingsColorPicker__(target); });
  });
  try{ __bindRoomSettingsShellThemeLongPress__(); }catch(_){ }
  try{ __bindRoomSettingsCardThemeLongPress__(); }catch(_){ }
  try{ __applyRoomSettingsShellTheme__(); }catch(_){ }
  try{ __applyRoomSettingsCardTheme__(); }catch(_){ }
}

function ensureRoomsPickerButtons(){
  try{
    const picker = document.getElementById('roomsPicker');
    if (!picker) return;
    const count = getConfiguredRoomsCount(6);
    const selected = (state && state.guestRooms instanceof Set) ? new Set(Array.from(state.guestRooms)) : new Set();
    const parts = [];
    for (let i = 1; i <= count; i++) {
      const on = selected.has(i);
      parts.push(`<button aria-pressed="${on ? "true" : "false"}" class="room-dot${on ? " selected" : ""}" data-room="${i}" type="button">${i}</button>`);
    }
    picker.innerHTML = parts.join('');
  }catch(_){ }
}

async function saveRoomsCountSetting(nextCount){
  const n = Math.max(0, Math.min(12, parseInt(nextCount, 10) || 0));
  if (!Number.isFinite(n) || n < 0) throw new Error('Numero stanze non valido');
  await api('impostazioni', { method: 'POST', body: { numero_stanze: n }, showLoader: true });
  try{
    state.settings = state.settings || {};
    state.settings.byKey = state.settings.byKey || {};
    state.settings.byKey.numero_stanze = { key:'numero_stanze', value:n, val:n, Value:n };
  }catch(_){ }
  await ensureSettingsLoaded({ force:true, showLoader:false });
  try{
    if (state && state.guestRooms instanceof Set){
      state.guestRooms = new Set(Array.from(state.guestRooms).filter(v => Number(v) >= 1 && Number(v) <= n).sort((a,b)=>a-b));
    }
  }catch(_){ }
  try{ ensureRoomsPickerButtons(); }catch(_){ }
  try{ updateSettingsRoomsButtonLabel(); }catch(_){ }
  try{ window.__ddae_renderRooms?.(); }catch(_){ }
  try{ window.__ddae_refreshRoomsAvailability?.(); }catch(_){ }
  try{ window.__ddae_refreshPulizieGrid?.({ forceReload:true }); }catch(_){ }
  try{ if (state && state.page === 'calendario') renderCalendario?.(); }catch(_){ }
  try{ if (state && state.page === 'ospite'){
    const current = state.guestMode === 'view' ? state.guestViewItem : state.guestEditSourceItem;
    if (current) renderRoomsReadOnly?.(current);
  } }catch(_){ }
  try{ renderRoomSettingsPage(); }catch(_){ }
  toast('Numero stanze aggiornato');
}

function normalizeRoomsList(arr, fallback = 6){
  const maxRooms = getConfiguredRoomsCount(fallback);
  return Array.from(new Set((Array.isArray(arr) ? arr : [])
    .map(n => parseInt(n, 10))
    .filter(n => Number.isFinite(n) && n >= 1 && n <= maxRooms)))
    .sort((a,b) => a - b);
}

function _parseRoomsArr(stanzeField){
  // Restituisce sempre un array unico/sortato di stanze [1..N configurato]
  const norm = (arr) => normalizeRoomsList(arr, 6);

  const fromDateParts = (d, m, y) => {
    const dd = parseInt(d, 10);
    const mm = parseInt(m, 10);
    let yy = parseInt(y, 10);
    if (!isFinite(dd) || !isFinite(mm) || !isFinite(yy)) return null;
    if (yy >= 100) yy = yy % 100;
    if (dd >= 1 && dd <= 6 && mm >= 1 && mm <= 6 && yy >= 1 && yy <= 6) return [dd, mm, yy];
    return null;
  };

  try {
    if (Array.isArray(stanzeField)) return norm(stanzeField);
    if (stanzeField == null) return [];

    // Date object (da Sheets)
    if (stanzeField instanceof Date){
      const parts = fromDateParts(stanzeField.getDate(), stanzeField.getMonth() + 1, stanzeField.getFullYear());
      return parts ? norm(parts) : [];
    }

    let s = String(stanzeField).trim();
    if (!s) return [];

    // JSON array: [2,3,4]
    if (s[0] === "["){
      try {
        const arr = JSON.parse(s);
        if (Array.isArray(arr)) return norm(arr);
      } catch (_) {}
    }

    // JSON object (tolleranza): {stanze:[...]}
    if (s[0] === "{"){
      try {
        const obj = JSON.parse(s);
        const maybe = (obj && (obj.stanze ?? obj.rooms ?? obj.stanza ?? obj.room)) ?? null;
        if (Array.isArray(maybe)) return norm(maybe);
        if (typeof maybe === "string") s = String(maybe).trim();
      } catch (_) {}
    }

    // Sheets conversione data con virgole: "2,3,2004"
    let m = s.match(/^\s*(\d{1,2})\s*,\s*(\d{1,2})\s*,\s*(\d{2,4})\s*$/);
    if (m){
      const parts = fromDateParts(m[1], m[2], m[3]);
      if (parts) return norm(parts);
    }

    // Data con slash: "02/03/2004" oppure "2-3-04"
    m = s.match(/^\s*(\d{1,2})\s*[\/\-.]\s*(\d{1,2})\s*[\/\-.]\s*(\d{2,4})\s*$/);
    if (m){
      const parts = fromDateParts(m[1], m[2], m[3]);
      if (parts) return norm(parts);
    }

    // ISO date: "2004-03-02" o "2004-03-02T..."
    m = s.match(/^\s*(\d{4})-(\d{2})-(\d{2})(?:[T\s].*)?$/);
    if (m){
      const parts = fromDateParts(m[3], m[2], m[1]);
      if (parts) return norm(parts);
    }

    // Lista stanze: supporta "2,3,4", "2|3|4", "10 11" e JSON/stringhe simili
    const tokens = s.split(/[|,;\s]+/).map(t => t.trim()).filter(Boolean);
    const nums = tokens.map(t => parseInt(t, 10)).filter(n => Number.isFinite(n));
    return norm(nums);
  } catch (_) {
    return [];
  }
}




function __guestRangeWindowForCalendarAvailability__(ci, co){
  try{
    const from = addDaysISO(String(ci || '').slice(0,10), -3);
    const to = addDaysISO(String(co || '').slice(0,10), 3);
    if (!from || !to) return null;
    return { from, to, key:`A:${from}|${to}` };
  }catch(_){ return null; }
}

function __guestCalendarRowsCoverRange__(ci, co){
  try{
    const cal = state && state.calendar ? state.calendar : null;
    if (!cal || !cal.ready || !Array.isArray(cal.guests)) return false;
    const wf = String(cal.winFrom || '').slice(0,10);
    const wt = String(cal.winTo || '').slice(0,10);
    const a = String(ci || '').slice(0,10);
    const b = String(co || '').slice(0,10);
    return !!(wf && wt && a && b && wf <= a && wt >= b);
  }catch(_){ return false; }
}

async function __loadCalendarGuestsForAvailability__(range, { force = true } = {}){
  const ci = String(range?.ci || '').slice(0,10);
  const co = String(range?.co || '').slice(0,10);
  if (!ci || !co || co <= ci) return [];
  if (!state.calendar) state.calendar = { anchor:new Date(ci + 'T00:00:00'), ready:false, guests:[], rangeKey:'' };

  if (!force && __guestCalendarRowsCoverRange__(ci, co)){
    return Array.isArray(state.calendar.guests) ? state.calendar.guests : [];
  }

  const win = __guestRangeWindowForCalendarAvailability__(ci, co);
  if (!win) return [];

  const data = await cachedGet('ospiti', { from: win.from, to: win.to }, { showLoader:false, ttlMs:0, swrMs:0, force:true });
  const rows = __guestFilterPreventiviRows__(Array.isArray(data) ? data : [], false);

  try{
    state.calendar.guests = rows;
    state.calendar.ready = true;
    state.calendar.rangeKey = win.key;
    state.calendar.winFrom = win.from;
    state.calendar.winTo = win.to;
    state.calendar.fetchedAt = Date.now();
    state.calendar.availabilityRangeKey = `${ci}|${co}`;
  }catch(_){ }

  return rows;
}

function __calendarOccupiedRoomsForRange__(rows, range, editId = ''){
  const occ = new Set();
  try{
    const ci = String(range?.ci || '').slice(0,10);
    const co = String(range?.co || '').slice(0,10);
    const skipId = String(editId || '').trim();
    if (!ci || !co || co <= ci) return occ;

    for (const g of (Array.isArray(rows) ? rows : [])){
      const gid = guestIdOf(g);
      if (skipId && gid && gid === skipId) continue;

      const gi = formatISODateLocal(g?.check_in ?? g?.checkIn ?? g?.checkin ?? g?.arrivo ?? g?.arrival ?? '');
      const go = formatISODateLocal(g?.check_out ?? g?.checkOut ?? g?.checkout ?? g?.partenza ?? g?.departure ?? '');
      if (!gi || !go) continue;

      if (!(gi < co && go > ci)) continue;

      const roomsArr = _parseRoomsArr(g?.stanze ?? g?.rooms ?? g?.stanza ?? g?.room ?? '');
      roomsArr.forEach((r) => occ.add(r));
    }
  }catch(_){ }
  return occ;
}

function buildRoomsStackHTML(guestId, roomsArr){
  if (!roomsArr || !roomsArr.length) return `<span class="room-dot-badge is-empty" aria-label="Nessuna stanza">—</span>`;
  return `<div class="rooms-stack" aria-label=" e letti">` + normalizeRoomsList(roomsArr, 6).map((n) => {
    const key = `${guestId}:${n}`;
    const info = (state.stanzeByKey && state.stanzeByKey[key]) ? state.stanzeByKey[key] : { letto_m: 0, letto_s: 0, culla: 0 };
    const lettoM = Number(info.letto_m || 0) || 0;
    const lettoS = Number(info.letto_s || 0) || 0;
    const culla  = Number(info.culla  || 0) || 0;

    let dots = "";
    if (lettoM > 0) dots += `<span class="bed-dot bed-dot-m" aria-label="Letto matrimoniale"></span>`;
    for (let i = 0; i < lettoS; i++) dots += `<span class="bed-dot bed-dot-s" aria-label="Letto singolo"></span>`;
    if (culla > 0) dots += `<span class="bed-dot bed-dot-c" aria-label="Culla"></span>`;

    return `<div class="room-row">
      <span class="room-dot-badge room-${n}" style="${__roomsUiBadgeStyle__(getRoomsUiConfig().rooms?.[String(n)] || 'blue-4')}">${n}</span>
      <div class="bed-dots" aria-label="Letti">${dots || `<span class="bed-dot bed-dot-empty" aria-label="Nessun letto"></span>`}</div>
    </div>`;
  }).join("") + `</div>`;
}


function __guestGroupInfoRowElements__(){
  try{
    const rows = [];
    const add = (el) => { if (el) rows.push(el); };
    const total = document.getElementById('guestTotal');
    // dDAE_2.837 — il blocco Importo prenotazione + Note deve essere trattato
    // come una sola riga del pacchetto gruppo stanze: nascosto finché non si seleziona il gruppo.
    add(total ? (total.closest('.total-row') || total.closest('.field')) : null);
    [
      'mgcRow',
      'guestChannelRow',
      'servicesRow',
      'depositRow',
      'saldoRow',
      'discountRow',
      'remainingRow',
      'servicesListWrap'
    ].forEach((id) => add(document.getElementById(id)));
    add(document.getElementById('guestNotesWrap'));
    return Array.from(new Set(rows.filter(Boolean)));
  }catch(_){ return []; }
}

function __setGuestGroupInfoVisible__(visible){
  try{
    const on = !!visible;
    const card = document.querySelector('#page-ospite .guest-form-card');
    if (card) card.classList.toggle('guest-group-info-open', on);
    __guestGroupInfoRowElements__().forEach((el) => {
      try{
        el.hidden = !on;
        // dDAE_2.837: alcuni layout storici usano display!important su righe economiche;
        // applica anche lo style inline per garantire che Importo commissione/Channel
        // restino davvero nascosti finché non si apre un gruppo stanze.
        if (!on) el.style.setProperty('display','none','important');
        else el.style.removeProperty('display');
      }catch(_){ }
    });
    if (!on){
      try{ const notes = document.getElementById('guestNotesWrap'); if (notes){ notes.hidden = true; notes.style.setProperty('display','none','important'); } }catch(_){ }
      try{ const services = document.getElementById('servicesListWrap'); if (services){ services.hidden = true; services.style.setProperty('display','none','important'); } }catch(_){ }
      try{ const notesBtn = document.getElementById('guestNotesToggle'); if (notesBtn) notesBtn.setAttribute('aria-expanded','false'); }catch(_){ }
      try{ const servicesBtn = document.getElementById('servicesPillView'); if (servicesBtn) servicesBtn.setAttribute('aria-expanded','false'); }catch(_){ }
    } else {
      try{ const notes = document.getElementById('guestNotesWrap'); if (notes){ notes.hidden = true; notes.style.removeProperty('display'); } }catch(_){ }
      try{ const services = document.getElementById('servicesListWrap'); if (services){ services.hidden = true; services.style.removeProperty('display'); } }catch(_){ }
    }
    try{ __syncGuestCheckInButton__(); }catch(_){ }
  }catch(_){ }
}

function __guestBookingById__(bookingId){
  try{
    const id = String(bookingId || '').trim();
    if (!id) return null;
    const list = Array.isArray(state.guestGroupBookings) ? state.guestGroupBookings : [];
    const found = list.find(x => String(guestIdOf(x) || x?.id || '').trim() === id);
    if (found) return found;
    const view = state.guestViewItem || null;
    if (view && String(guestIdOf(view) || view?.id || '').trim() === id) return view;
  }catch(_){ }
  return null;
}

function __guestActiveBookingForAction__(){
  try{
    const id = String(state.guestGroupActiveId || '').trim();
    if (!id) return null;
    return __guestBookingById__(id);
  }catch(_){ return null; }
}

function __populateGuestGroupInfoFromBooking__(ospite){
  try{
    if (!ospite) return;
    try{ document.getElementById('guestTotal').value = ospite.importo_prenotazione ?? ospite.total ?? 0; }catch(_){ }
    try{ populateGuestChannelOptions(ospite.channel_id ?? ospite.channelId ?? ''); }catch(_){ }
    try{
      const pct = (ospite.channel_commissione ?? ospite.channelCommissione ?? ospite.channel_commission_pct ?? ospite.channelCommissionPct ?? 0);
      document.getElementById('guestChannelCommission').value = (pct === null || pct === undefined || pct === '') ? '' : pct;
      document.getElementById('guestChannel').value = String(ospite.channel_id ?? ospite.channelId ?? '');
      applySelectedChannelToGuestForm(ospite.channel_id ?? ospite.channelId ?? '', { preserveManual:true });
    }catch(_){ }
    try{ document.getElementById('guestBooking').value = ospite.importo_booking ?? ospite.booking ?? 0; }catch(_){ }
    try{ document.getElementById('guestServices').value = ospite.servizi_totale ?? ospite.serviziTotal ?? ospite.importo_servizi ?? 0; }catch(_){ }
    try{ document.getElementById('guestDiscount').value = ospite.sconto ?? ospite.discount ?? 0; }catch(_){ }
    try{ document.getElementById('guestDeposit').value = ospite.acconto_importo ?? ospite.deposit ?? 0; }catch(_){ }
    try{ document.getElementById('guestSaldo').value = ospite.saldo_pagato ?? ospite.saldoPagato ?? ospite.saldo ?? 0; }catch(_){ }
    try{ syncGuestNotesUI(ospite, { open:false }); }catch(_){ }

    const depAmount = parseFloat(document.getElementById('guestDeposit')?.value || '0') || 0;
    const saldoAmount = parseFloat(document.getElementById('guestSaldo')?.value || '0') || 0;
    const depType = (depAmount > 0) ? (ospite.acconto_tipo ?? ospite.accontoTipo ?? ospite.depositType ?? ospite.deposit_type ?? '') : '';
    const saldoType = (saldoAmount > 0) ? (ospite.saldo_tipo ?? ospite.saldoTipo ?? ospite.balanceType ?? ospite.balance_type ?? '') : '';
    state.guestDepositType = depType;
    state.guestSaldoType = saldoType;
    try{ setPayType('depositType', depType); }catch(_){ }
    try{ setPayType('saldoType', saldoType); }catch(_){ }
    state.guestDepositReceipt = !!(depAmount > 0 && truthy(ospite.acconto_ricevuta ?? ospite.accontoRicevuta ?? ospite.ricevuta_acconto ?? ospite.ricevutaAcconto ?? ospite.acconto_ricevutain));
    state.guestSaldoReceipt = !!(saldoAmount > 0 && truthy(ospite.saldo_ricevuta ?? ospite.saldoRicevuta ?? ospite.ricevuta_saldo ?? ospite.ricevutaSaldo ?? ospite.saldo_ricevutain));
    try{ setPayReceipt('depositType', state.guestDepositReceipt); }catch(_){ }
    try{ setPayReceipt('saldoType', state.guestSaldoReceipt); }catch(_){ }

    state.guestPSRegistered = truthy(ospite.ps_registrato ?? ospite.psRegistrato);
    state.guestISTATRegistered = truthy(ospite.istat_registrato ?? ospite.istatRegistrato);
    try{ setRegFlags('regTags', state.guestPSRegistered, state.guestISTATRegistered); }catch(_){ }
    state.guestCheckInDone = __guestCheckInDone__(ospite);
    try{ updateGuestRemaining(); }catch(_){ }
    try{ updateGuestNotesIndicator(); }catch(_){ }
    try{ refreshFloatingLabels(); }catch(_){ }
    try{ updateGuestTaxTotalPill(ospite); }catch(_){ try{ updateGuestTaxTotalPill(); }catch(__){ } }
    try{ loadServiziForOspite(ospite); }catch(_){ }
    try{ __syncGuestCheckInButton__(); }catch(_){ }
  }catch(_){ }
}

function __toggleGuestGroupInfoForBooking__(bookingId){
  try{
    if (String(state.guestMode || '') !== 'view') return;
    const id = String(bookingId || '').trim();
    if (!id) return;
    const already = String(state.guestGroupActiveId || '').trim() === id;
    const card = document.querySelector('#page-ospite .guest-form-card');
    const isOpen = !!(card && card.classList.contains('guest-group-info-open'));
    if (already && isOpen){
      state.guestGroupActiveId = null;
      state.guestCheckInDone = false;
      __setGuestGroupInfoVisible__(false);
    } else {
      const item = __guestBookingById__(id);
      if (!item) return;
      state.guestGroupActiveId = id;
      __populateGuestGroupInfoFromBooking__(item);
      __setGuestGroupInfoVisible__(true);
    }
    try{ renderRoomsReadOnly(state.guestViewItem || __guestBookingById__(state.guestViewPrimaryId) || null); }catch(_){ }
    try{ renderGuestMulti({ mode:'view' }); }catch(_){ }
    try{ updateOspiteHdActions(); }catch(_){ }
    try{ __syncGuestCheckInButton__(); }catch(_){ }
  }catch(_){ }
}

function renderRoomsReadOnly(ospite){
  const ro = document.getElementById("roomsReadOnly");
  if (!ro) return;

  const guestId = _guestIdOf(ospite);
  const activeId = String(state.guestGroupActiveId || '').trim();
  const primaryActive = !!(guestId && activeId && String(guestId) === activeId);
  let roomsArr = _parseRoomsArr(ospite?.stanze);

  // fallback: se per qualche motivo non arriva 'stanze' dal backend, usa lo stato locale
  if (!roomsArr.length && state.guestRooms && state.guestRooms.size){
    roomsArr = Array.from(state.guestRooms)
      .map(n => parseInt(n,10))
      .filter(n => Number.isFinite(n) && n>=1 && n<=getConfiguredRoomsCount(6))
      .sort((a,b)=>a-b);
  }

  let stackHTML = buildRoomsStackHTML(guestId, roomsArr);

  const nights = calcStayNights(ospite);

  // Tassa di soggiorno: mostra il totale accanto al pallino della prima stanza
  try{
    if (nights != null){
      const tt = calcTouristTax(ospite, nights);
    }
  }catch(_){ }

  // Range date sempre visibile (pill bianca)
  const rangeCompact = formatRangeCompactIT(ospite?.check_in ?? ospite?.checkIn ?? "", ospite?.check_out ?? ospite?.checkOut ?? "");
  const datesHTML = `<div class="guest-booking-dates-pill">${rangeCompact || "—"}</div>`;

  // Matrimonio + pallino notti (azzurro) a destra della pill data
  const marriageOn = !!(ospite?.matrimonio);
  const gOn = truthy(ospite?.g ?? ospite?.flag_g ?? ospite?.gruppo_g ?? ospite?.group ?? ospite?.g_flag);
  const cOn = truthy(ospite?.col_c ?? ospite?.colC ?? ospite?.c ?? ospite?.C ?? ospite?.flag_c ?? ospite?.flagC ?? ospite?.colc ?? ospite?.c_flag);
  const marriageHTML = marriageOn ? `<span class="marriage-dot" aria-label="${escapeHtml(__translateExactText__('Matrimonio') || 'Matrimonio')}">M</span>` : ``;
  const gHTML = gOn ? `<span class="g-dot" aria-label="G">G</span>` : ``;
  const cHTML = cOn ? `<span class="c-dot" aria-label="C">C</span>` : ``;
  const nightsHTML = buildNightsDotHTML(nights);

  const topRightHTML = (marriageHTML || gHTML || cHTML || nightsHTML) ? `<div class="guest-booking-right">${marriageHTML}${gHTML}${cHTML}${nightsHTML}</div>` : ``;

  // Coerenza UI: usa lo stesso riquadro smussato delle prenotazioni multiple
  ro.innerHTML = `
    <div class="guest-booking-block guest-booking-block--primary ${primaryActive ? "is-active" : ""}" data-booking-id="${guestId || ''}" role="button" tabindex="0" aria-expanded="${primaryActive ? 'true' : 'false'}">
      <div class="guest-booking-top">
        <div class="guest-booking-top-row">
          ${datesHTML}
          ${topRightHTML}
        </div>
      </div>
      <div class="guest-booking-rooms guest-booking-ro">
        <div class="rooms-readonly-wrap">${stackHTML}</div>
      </div>
    </div>
  `;

  try{ updateGuestTaxTotalPill(); }catch(_){ }
}


// ===== dDAE_1.020 — Multi prenotazioni per stesso nome =====
function __guestBookingNumberLabel__(guest){
  try{
    const candidates = [
      guest?.numero_prenotazione, guest?.numeroPrenotazione, guest?.prenotazione_numero, guest?.prenotazioneNumero,
      guest?.booking_number, guest?.bookingNumber, guest?.reservation_number, guest?.reservationNumber,
      guest?.booking_code, guest?.bookingCode, guest?.reservation_code, guest?.reservationCode,
      guest?.id_prenotazione, guest?.idPrenotazione, guest?.id_booking, guest?.idBooking
    ];
    for (const raw of candidates){
      const clean = String(raw == null ? '' : raw).trim();
      if (clean) return clean;
    }
  }catch(_){ }
  return '';
}

function normalizeGuestNameKey(name){
  try{ return collapseSpaces(String(name || "").trim()).toLowerCase(); }catch(_){ return String(name||"").trim().toLowerCase(); }
}

function buildGuestBookingBlockHTML(ospite, { mode="view", showSelect=false, activeId="" } = {}){
  const gid = guestIdOf(ospite);
  const roomsArr = _parseRoomsArr(ospite?.stanze);
  let roomsHTML = buildRoomsStackHTML(gid, roomsArr);

  const rangeCompact = formatRangeCompactIT(ospite?.check_in ?? ospite?.checkIn ?? "", ospite?.check_out ?? ospite?.checkOut ?? "") || "—";

  const modeL = String(mode || "view").toLowerCase();
  const nights = calcStayNights(ospite);

  // In sola lettura: inserisci tassa accanto alla prima stanza
  try{
    if (modeL === "view" && nights != null){
      const tt = calcTouristTax(ospite, nights);
    }
  }catch(_){ }

  const nightsHTML = (modeL === "view") ? buildNightsDotHTML(nights) : ``;

  const marriageOn = !!(ospite?.matrimonio);

  const isActive = (activeId && gid && String(activeId) === String(gid));
  const actionsHTML = (showSelect && gid)
    ? `<div class="guest-booking-actions" aria-label="Azioni prenotazione">
        <button class="icon-round-btn is-edit" type="button" data-guest-select="${gid}" aria-label="Modifica prenotazione" ${isActive ? "disabled" : ""}>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M12 20h9" />
            <path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z" />
          </svg>
        </button>
        <button class="icon-round-btn is-del" type="button" data-guest-del-booking="${gid}" aria-label="Elimina prenotazione">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M3 6h18" />
            <path d="M8 6V4h8v2" />
            <path d="M19 6l-1 14H6L5 6" />
            <path d="M10 11v6" />
            <path d="M14 11v6" />
          </svg>
        </button>
      </div>`
    : ``;

  // Top right: in edit mostra azioni; in view mostra matrimonio + notti
  const gOn = truthy(ospite?.g ?? ospite?.flag_g ?? ospite?.gruppo_g ?? ospite?.group ?? ospite?.g_flag);
  const cOn = truthy(ospite?.col_c ?? ospite?.colC ?? ospite?.c ?? ospite?.C ?? ospite?.flag_c ?? ospite?.flagC ?? ospite?.colc ?? ospite?.c_flag);
  const marriageHTML = (modeL === "view" && marriageOn) ? `<span class="marriage-dot" aria-label="${escapeHtml(__translateExactText__('Matrimonio') || 'Matrimonio')}">M</span>` : ``;
  const gHTML = (modeL === "view" && gOn) ? `<span class="g-dot" aria-label="G">G</span>` : ``;
  const cHTML = (modeL === "view" && cOn) ? `<span class="c-dot" aria-label="C">C</span>` : ``;
  const viewRight = (marriageHTML || gHTML || cHTML || nightsHTML) ? `<div class="guest-booking-right">${marriageHTML}${gHTML}${cHTML}${nightsHTML}</div>` : ``;
const topRightHTML = actionsHTML || viewRight;

  return `<div class="guest-booking-block ${isActive ? "is-active" : ""}" data-booking-id="${gid}" role="button" tabindex="0" aria-expanded="${isActive ? 'true' : 'false'}">
    <div class="guest-booking-top">
      <div class="guest-booking-top-row">
        <div class="guest-booking-dates-pill">${rangeCompact}</div>
        ${topRightHTML}
      </div>
    </div>
    <div class="guest-booking-rooms">${roomsHTML}</div>
  </div>`;
}


function clearGuestMulti(){
  const el = document.getElementById("guestMulti");
  if (!el) return;
  el.hidden = true;
  el.innerHTML = "";
}

function __guestMoneyValue__(value){
  try{
    if (value === null || value === undefined) return 0;
    if (typeof value === 'number') return Number.isFinite(value) ? value : 0;
    const raw = String(value).trim();
    if (!raw) return 0;
    const clean = raw
      .replace(/€/g, '')
      .replace(/\s+/g, '')
      .replace(/[^0-9,.-]/g, '')
      .replace(/\.(?=\d{3}(\D|$))/g, '')
      .replace(',', '.');
    const n = parseFloat(clean);
    return Number.isFinite(n) ? n : 0;
  }catch(_){ return 0; }
}

function __guestBookingAmountOfBooking__(guest){
  try{
    const candidates = [
      guest?.importo_prenotazione, guest?.importo_prenota, guest?.importoPrenotazione, guest?.importoPrenota,
      guest?.booking_amount, guest?.bookingAmount, guest?.reservation_amount, guest?.reservationAmount,
      guest?.prezzo_prenotazione, guest?.prezzoPrenotazione, guest?.totale_prenotazione, guest?.totalePrenotazione,
      guest?.total, guest?.totale, guest?.amount, guest?.importo, guest?.prezzo
    ];
    for (const v of candidates){
      const n = __guestMoneyValue__(v);
      if (n) return n;
      if (String(v ?? '').trim() === '0') return 0;
    }
  }catch(_){ }
  return 0;
}

function __guestCommissionAmountOfBooking__(guest){
  try{
    const direct = guest?.importo_booking ?? guest?.booking ?? guest?.commissione_importo ?? guest?.commissioneImporto ?? guest?.channel_commission_amount ?? guest?.channelCommissionAmount;
    const directText = String(direct ?? '').trim();
    const n = __guestMoneyValue__(direct);
    if (directText !== '' && Number.isFinite(n)) return n;
    const total = __guestBookingAmountOfBooking__(guest);
    const pct = __guestMoneyValue__(guest?.channel_commissione ?? guest?.channelCommissione ?? guest?.channel_commission_pct ?? guest?.channelCommissionPct ?? 0);
    const calc = (total * pct) / 100;
    return Number.isFinite(calc) ? Math.round(calc * 100) / 100 : 0;
  }catch(_){ return 0; }
}

function __guestTaxAmountOfBooking__(guest){
  try{
    const direct = guest?.tassa_soggiorno ?? guest?.tassaSoggiorno ?? guest?.tourist_tax ?? guest?.touristTax ?? guest?.city_tax ?? guest?.cityTax;
    const directText = String(direct ?? '').trim();
    const dn = __guestMoneyValue__(direct);
    if (directText !== '' && Number.isFinite(dn) && dn > 0) return dn;
    const nights = calcStayNights(guest);
    if (nights == null) return 0;
    const tt = calcTouristTax(guest, nights);
    return __guestMoneyValue__(tt?.total ?? tt?.amount ?? tt?.value ?? tt ?? 0);
  }catch(_){ return 0; }
}

function __guestGroupBookingTotal__(list){
  try{
    return (Array.isArray(list) ? list : []).reduce((sum, item) => sum + __guestBookingAmountOfBooking__(item), 0);
  }catch(_){ return 0; }
}

function __guestGroupCommissionTotal__(list){
  try{
    return (Array.isArray(list) ? list : []).reduce((sum, item) => sum + __guestCommissionAmountOfBooking__(item), 0);
  }catch(_){ return 0; }
}

function __guestGroupTaxTotal__(list){
  try{
    return (Array.isArray(list) ? list : []).reduce((sum, item) => sum + __guestTaxAmountOfBooking__(item), 0);
  }catch(_){ return 0; }
}

function __guestGroupCommissionTotalHTML__(list){
  const bookingTotal = __guestGroupBookingTotal__(list);
  return `<div class="guest-commission-total-tab guest-summary-total-tab" role="status" aria-label="Somma importo prenotazioni">
    <span class="guest-commission-total-label">Somma importo prenotazioni</span>
    <span class="guest-commission-total-value">${euro(bookingTotal)}</span>
  </div>`;
}

async function __guestAddBookingFromCurrentEdit__(){
  try{
    const nameNow = (document.getElementById("guestName")?.value || "").trim();
    const phoneNow = (document.getElementById("guestPhone")?.value || "").trim();
    const nationalityNow = String(document.getElementById("guestNationality")?.value || "").trim();
    const emailNow = (document.getElementById("guestEmail")?.value || "").trim();
    const menNow = parseInt(document.getElementById("guestMen")?.value || "0", 10) || 0;
    const womenNow = parseInt(document.getElementById("guestWomen")?.value || "0", 10) || 0;
    const adultsNow = parseInt(document.getElementById("guestAdults")?.value || "0", 10) || 0;
    const kidsNow = parseInt(document.getElementById("guestKidsU10")?.value || "0", 10) || 0;
    const marriageNow = !!(state.guestMarriage);
    const groupNow = !!(state.guestGroup);
    const depTypeNow = state.guestDepositType || "";
    const saldoTypeNow = state.guestSaldoType || "";
    const psNow = !!state.guestPSRegistered;
    const istNow = !!state.guestISTATRegistered;
    const baseCardKey = String(state.guestEditCardKey || state.guestGroupKey || __guestCardKeyOf__(state.guestEditSourceItem || { id: state.guestEditId, nome: nameNow })).trim();
    enterGuestCreateMode();
    state.guestCreateFromGroup = true;
    state.guestCreateFromGroupCardKey = baseCardKey || null;
    try { updateGuestPriceVisibility(); } catch (_) {}
    try {
      document.getElementById("guestName").value = nameNow;
      try{ __applyGuestNameUppercase__(document.getElementById("guestName")); }catch(_){}
      document.getElementById("guestPhone").value = phoneNow;
      setGuestNationality(nationalityNow, { manual:true });
      document.getElementById("guestEmail").value = emailNow;
      document.getElementById("guestMen").value = menNow;
      document.getElementById("guestWomen").value = womenNow;
      document.getElementById("guestAdults").value = adultsNow;
      document.getElementById("guestKidsU10").value = kidsNow;
      setMarriage(marriageNow);
      setGroup(groupNow);
      state.guestDepositType = depTypeNow;
      setPayType("depositType", depTypeNow);
      state.guestSaldoType = saldoTypeNow;
      setPayType("saldoType", saldoTypeNow);
      state.guestPSRegistered = psNow;
      state.guestISTATRegistered = istNow;
      setRegFlags("regTags", psNow, istNow);
      refreshFloatingLabels();
    } catch (_) {}
    showPage("ospite");
  }catch(err){ try{ toast(err?.message || 'Errore nuova prenotazione'); }catch(_){} }
}

function renderGuestMulti({ mode="view" } = {}){
  const el = document.getElementById("guestMulti");
  if (!el) return;

  let list = Array.isArray(state.guestGroupBookings) ? state.guestGroupBookings : [];
  const activeId = String(state.guestGroupActiveId || state.guestEditId || "").trim();

  // dDAE_2.837: in modifica il tasto + deve essere sempre disponibile,
  // anche quando il contesto gruppo non è ancora stato ricostruito.
  if ((!list || !list.length) && mode === "edit"){
    const fallback = state.guestEditSourceItem || state.guestViewItem || null;
    list = fallback ? [fallback] : [];
  }

  if ((!list || !list.length) && mode !== "edit"){
    clearGuestMulti();
    return;
  }

  if (mode === "edit"){
    const showSelect = true;
    const title = `<div class="guest-multi-title">Prenotazioni</div>`;
    const blocks = list.map(g => buildGuestBookingBlockHTML(g, { mode, showSelect, activeId })).join("");
    const totalTab = __guestGroupCommissionTotalHTML__(list);
    el.innerHTML = `${title}${blocks}${totalTab}`;
    el.hidden = false;
    return;
  }

  const primaryId = String(state.guestViewPrimaryId || guestIdOf(state.guestViewItem || {}) || '').trim();
  const shown = (list || []).filter(x => String(guestIdOf(x) || x?.id || '').trim() !== primaryId);
  const blocks = shown.map(g => buildGuestBookingBlockHTML(g, { mode, showSelect:false, activeId })).join("");
  const totalTab = __guestGroupCommissionTotalHTML__(list);

  // In sola lettura, se c'è una sola prenotazione mostra comunque il totale aggregato
  // sotto il gruppo stanze principale; con più prenotazioni lo mostra sotto tutti i gruppi.
  el.innerHTML = `${blocks}${totalTab}`;
  el.hidden = false;
}

function updateOspiteHdActions(){

  const hdActions = document.getElementById("ospiteHdActions");
  if (!hdActions) return;

  // Mostra il contenitore (poi nascondiamo i singoli pallini senza azione)
  hdActions.hidden = false;

  const btnCheckIn = hdActions.querySelector("[data-guest-checkin]");
  const btnCal  = hdActions.querySelector("[data-guest-cal]");
  const btnReport = hdActions.querySelector("[data-guest-report]");
  const btnBack = hdActions.querySelector("[data-guest-back]");
  const btnEdit = hdActions.querySelector("[data-guest-edit]");
  const btnDel  = hdActions.querySelector("[data-guest-del]");

  const mode = state.guestMode; // "create" | "edit" | "view"

  // Check-in: solo in sola lettura
  if (btnCheckIn) btnCheckIn.hidden = (mode !== "view");
  try{ __syncGuestCheckInButton__(); }catch(_){ }

  // Indaco: aggiunge un nuovo gruppo stanze anche in sola lettura; non apre più il calendario.
  if (btnCal){
    const canAddGroup = (mode === "edit" || mode === "view");
    btnCal.hidden = !canAddGroup;
    btnCal.disabled = false;
    btnCal.setAttribute('aria-label', 'Aggiungi prenotazione');
    btnCal.setAttribute('title', 'Aggiungi prenotazione');
    const sr = btnCal.querySelector('.sr-only');
    if (sr) sr.textContent = 'Aggiungi prenotazione';
    const svg = btnCal.querySelector('svg');
    if (svg) svg.innerHTML = '<path d="M12 5v14"></path><path d="M5 12h14"></path>';
  }
  if (btnReport) btnReport.hidden = (mode !== "view");

  // Verde: sempre presente (torna alla lista ospiti)
  if (btnBack) btnBack.hidden = false;

  // Giallo: solo in sola lettura (azione: passa a modifica del gruppo stanze selezionato)
  if (btnEdit){
    btnEdit.hidden = (mode !== "view");
    const hasActiveGroup = !!String(state.guestGroupActiveId || '').trim();
    btnEdit.disabled = (mode === "view" && !hasActiveGroup);
    btnEdit.classList.toggle('is-disabled', mode === "view" && !hasActiveGroup);
    btnEdit.setAttribute('aria-disabled', (mode === "view" && !hasActiveGroup) ? 'true' : 'false');
    btnEdit.setAttribute('title', (mode === "view" && !hasActiveGroup) ? 'Seleziona prima un gruppo stanze' : 'Modifica gruppo stanze');
  }

  // Rosso: solo in sola lettura (azione: elimina ospite)
  if (btnDel) btnDel.hidden = (mode !== "view");
}


function updateGuestFormModeClass(){
  try{
    const card = document.querySelector("#page-ospite .guest-form-card");
    if (!card) return;
    const mode = String(state.guestMode || "").toLowerCase();
    const isView = (mode === "view");
    card.classList.toggle("is-view", isView);
    card.classList.toggle("is-create", !isView && mode === "create");
    card.classList.toggle("is-edit", !isView && mode === "edit");
    try{ __syncGuestNationalityPlacement__(); }catch(_){}
  }catch(_){}
}

function __syncGuestNationalityPlacement__(){
  try{
    const btn = document.getElementById("guestNationalityBtn");
    const titleRow = document.querySelector("#page-ospite .guest-title-row");
    const title = document.getElementById("ospiteFormTitle");
    const nameField = document.querySelector("#page-ospite .guest-name-field");
    const hidden = document.getElementById("guestNationality");
    if (!btn || !titleRow || !nameField) return;
    const mode = String(state.guestMode || "create").toLowerCase();
    const isCreate = (mode === "create" || !mode);
    if (isCreate){
      btn.classList.add("guest-nationality-title-btn");
      btn.classList.remove("guest-nationality-name-btn");
      if (btn.parentNode !== titleRow){
        titleRow.insertBefore(btn, title || titleRow.firstChild);
      } else if (title && btn.nextSibling !== title){
        titleRow.insertBefore(btn, title);
      }
    } else {
      btn.classList.remove("guest-nationality-title-btn");
      btn.classList.add("guest-nationality-name-btn");
      if (hidden && hidden.parentNode === nameField){
        nameField.insertBefore(btn, hidden);
      } else if (btn.parentNode !== nameField){
        nameField.appendChild(btn);
      }
    }
    try{ updateGuestNationalityButton(); }catch(_){}
  }catch(_){}
}

function syncGuestCreatePrimaryStack(){
  try{
    const formStack = document.querySelector("#page-ospite .guest-form-card .bd.form-stack");
    const mgcRow = document.getElementById("mgcRow");
    if (!formStack || !mgcRow) return;

    let stack = document.getElementById("guestCreatePrimaryStack");
    if (!stack){
      stack = document.createElement("div");
      stack.id = "guestCreatePrimaryStack";
      stack.hidden = true;
      formStack.insertBefore(stack, mgcRow.nextSibling);
    }

    const card = document.querySelector("#page-ospite .guest-form-card");
    const isCreate = !!card && card.classList.contains("is-create");

    const defs = [
      { el: document.getElementById("guestNotesToggle")?.closest(".subfield") || document.getElementById("guestNotesToggle")?.parentElement, anchorId: "guestNotesToggleAnchor" },
      { el: document.getElementById("guestChannel")?.closest(".subfield"), anchorId: "guestChannelCreateAnchor" },
      { el: document.getElementById("guestTotal")?.closest(".subfield"), anchorId: "guestTotalCreateAnchor" }
    ];

    defs.forEach(def => {
      if (!def.el || !def.el.parentNode) return;
      let anchor = document.getElementById(def.anchorId);
      if (!anchor){
        anchor = document.createElement("span");
        anchor.id = def.anchorId;
        anchor.hidden = true;
        def.el.parentNode.insertBefore(anchor, def.el);
      }
    });

    const bookingField = document.getElementById("guestBooking")?.closest('.subfield') || document.getElementById("guestBooking")?.closest('.field');
    if (bookingField) bookingField.hidden = !!isCreate;

    if (isCreate){
      stack.hidden = false;
      const orderedEls = defs.map((def) => def.el).filter(Boolean);
      orderedEls.forEach((el) => {
        try{ stack.appendChild(el); }catch(_){ }
      });
      try{
        Array.from(stack.children).forEach((child) => {
          if (!orderedEls.includes(child)) child.remove();
        });
      }catch(_){ }
      try{
        const nameField = document.getElementById("guestName")?.closest('.field.float, .field');
        const baseControl = document.getElementById("guestName");
        const baseLabel = nameField?.querySelector('label[for="guestName"]') || baseControl?.nextElementSibling;
        const controlStyle = getComputedStyle(baseControl || nameField);
        const labelStyle = getComputedStyle(baseLabel || nameField || baseControl);
        const targetHeight = Math.round(parseFloat(controlStyle.height) || baseControl?.getBoundingClientRect?.().height || nameField?.getBoundingClientRect?.().height || 56);
        const targetRadius = Math.round(parseFloat(controlStyle.borderRadius) || parseFloat(getComputedStyle(nameField || baseControl).borderRadius) || 24) || 24;
        const targetPadTop = Math.round(parseFloat(controlStyle.paddingTop) || 26);
        const targetPadRight = Math.round(parseFloat(controlStyle.paddingRight) || 14);
        const targetPadBottom = Math.round(parseFloat(controlStyle.paddingBottom) || 12);
        const targetPadLeft = Math.round(parseFloat(controlStyle.paddingLeft) || 14);
        const targetLabelTop = Math.round(parseFloat(labelStyle.top) || 18);
        const targetLabelLeft = Math.round(parseFloat(labelStyle.left) || 14);
        const targetFontSize = parseFloat(controlStyle.fontSize) || 16;
        const targetLabelFontSize = parseFloat(labelStyle.fontSize) || 13;
        const targetLabelWeight = parseFloat(labelStyle.fontWeight) || 500;
        stack.style.setProperty('--guest-create-control-height', `${targetHeight}px`);
        stack.style.setProperty('--guest-create-control-radius', `${targetRadius}px`);
        stack.style.setProperty('--guest-create-control-padding-top', `${targetPadTop}px`);
        stack.style.setProperty('--guest-create-control-padding-right', `${targetPadRight}px`);
        stack.style.setProperty('--guest-create-control-padding-bottom', `${targetPadBottom}px`);
        stack.style.setProperty('--guest-create-control-padding-left', `${targetPadLeft}px`);
        stack.style.setProperty('--guest-create-label-top', `${targetLabelTop}px`);
        stack.style.setProperty('--guest-create-label-left', `${targetLabelLeft}px`);
        stack.style.setProperty('--guest-create-control-font-size', `${targetFontSize}px`);
        stack.style.setProperty('--guest-create-label-font-size', `${targetLabelFontSize}px`);
        stack.style.setProperty('--guest-create-label-font-weight', `${targetLabelWeight}`);
      }catch(_){ }
    } else {
      stack.hidden = true;
      defs.forEach(def => {
        const anchor = document.getElementById(def.anchorId);
        if (def.el && anchor && anchor.parentNode && def.el.parentNode !== anchor.parentNode){
          anchor.parentNode.insertBefore(def.el, anchor.nextSibling);
        }
      });
      try{
        [
          '--guest-create-control-height',
          '--guest-create-control-radius',
          '--guest-create-control-padding-top',
          '--guest-create-control-padding-right',
          '--guest-create-control-padding-bottom',
          '--guest-create-control-padding-left',
          '--guest-create-label-top',
          '--guest-create-label-left',
          '--guest-create-control-font-size',
          '--guest-create-label-font-size',
          '--guest-create-label-font-weight'
        ].forEach(key => stack.style.removeProperty(key));
      }catch(_){ }
    }
  }catch(_){ }
}


function __placeServicesPillForView(isView){
  try{
    const pill = document.getElementById("servicesPillView");
    if (!pill) return;

    // Anchor: ricorda la posizione originale (layout modifica invariato)
    let anchor = document.getElementById("servicesPillViewAnchor");
    if (!anchor){
      anchor = document.createElement("span");
      anchor.id = "servicesPillViewAnchor";
      anchor.hidden = true;
      try{
        if (pill.parentNode) pill.parentNode.insertBefore(anchor, pill);
      }catch(_){}
    }

    const servicesRow = document.getElementById("servicesRow");
    const targetWrap = servicesRow ? servicesRow.querySelector(".paywrap") : null;

    if (isView){
      if (targetWrap && pill.parentNode !== targetWrap){
        // accanto a "Importo servizi"
        try{ targetWrap.insertBefore(pill, targetWrap.firstChild); }catch(_){}
      }
      try{ pill.hidden = false; }catch(_){}
    } else {
      // ripristina accanto a "Importo booking"
      if (anchor && anchor.parentNode){
        try{ anchor.parentNode.insertBefore(pill, anchor.nextSibling); }catch(_){}
      }
    }
  }catch(_){}
}


const __GUEST_NATIONALITY_OPTIONS__ = [
  { code:'', flag:'🏳️', name:'Non selezionata' },
  { code:'IT', flag:'🇮🇹', name:'Italia' },
  { code:'FR', flag:'🇫🇷', name:'Francia' },
  { code:'DE', flag:'🇩🇪', name:'Germania' },
  { code:'ES', flag:'🇪🇸', name:'Spagna' },
  { code:'GB', flag:'🇬🇧', name:'Regno Unito' },
  { code:'IE', flag:'🇮🇪', name:'Irlanda' },
  { code:'PT', flag:'🇵🇹', name:'Portogallo' },
  { code:'NL', flag:'🇳🇱', name:'Paesi Bassi' },
  { code:'BE', flag:'🇧🇪', name:'Belgio' },
  { code:'LU', flag:'🇱🇺', name:'Lussemburgo' },
  { code:'CH', flag:'🇨🇭', name:'Svizzera' },
  { code:'AT', flag:'🇦🇹', name:'Austria' },
  { code:'DK', flag:'🇩🇰', name:'Danimarca' },
  { code:'SE', flag:'🇸🇪', name:'Svezia' },
  { code:'NO', flag:'🇳🇴', name:'Norvegia' },
  { code:'FI', flag:'🇫🇮', name:'Finlandia' },
  { code:'IS', flag:'🇮🇸', name:'Islanda' },
  { code:'PL', flag:'🇵🇱', name:'Polonia' },
  { code:'CZ', flag:'🇨🇿', name:'Repubblica Ceca' },
  { code:'SK', flag:'🇸🇰', name:'Slovacchia' },
  { code:'HU', flag:'🇭🇺', name:'Ungheria' },
  { code:'RO', flag:'🇷🇴', name:'Romania' },
  { code:'BG', flag:'🇧🇬', name:'Bulgaria' },
  { code:'HR', flag:'🇭🇷', name:'Croazia' },
  { code:'SI', flag:'🇸🇮', name:'Slovenia' },
  { code:'GR', flag:'🇬🇷', name:'Grecia' },
  { code:'CY', flag:'🇨🇾', name:'Cipro' },
  { code:'MT', flag:'🇲🇹', name:'Malta' },
  { code:'EE', flag:'🇪🇪', name:'Estonia' },
  { code:'LV', flag:'🇱🇻', name:'Lettonia' },
  { code:'LT', flag:'🇱🇹', name:'Lituania' },
  { code:'AL', flag:'🇦🇱', name:'Albania' },
  { code:'RS', flag:'🇷🇸', name:'Serbia' },
  { code:'ME', flag:'🇲🇪', name:'Montenegro' },
  { code:'BA', flag:'🇧🇦', name:'Bosnia ed Erzegovina' },
  { code:'MK', flag:'🇲🇰', name:'Macedonia del Nord' },
  { code:'XK', flag:'🇽🇰', name:'Kosovo' },
  { code:'UA', flag:'🇺🇦', name:'Ucraina' },
  { code:'MD', flag:'🇲🇩', name:'Moldavia' },
  { code:'US', flag:'🇺🇸', name:'Stati Uniti' },
  { code:'CA', flag:'🇨🇦', name:'Canada' },
  { code:'MX', flag:'🇲🇽', name:'Messico' },
  { code:'CR', flag:'🇨🇷', name:'Costa Rica' },
  { code:'BR', flag:'🇧🇷', name:'Brasile' },
  { code:'AR', flag:'🇦🇷', name:'Argentina' },
  { code:'AU', flag:'🇦🇺', name:'Australia' },
  { code:'NZ', flag:'🇳🇿', name:'Nuova Zelanda' },
  { code:'JP', flag:'🇯🇵', name:'Giappone' },
  { code:'CN', flag:'🇨🇳', name:'Cina' },
  { code:'IN', flag:'🇮🇳', name:'India' },
  { code:'KR', flag:'🇰🇷', name:'Corea del Sud' },
  { code:'AE', flag:'🇦🇪', name:'Emirati Arabi Uniti' },
  { code:'SA', flag:'🇸🇦', name:'Arabia Saudita' },
  { code:'IL', flag:'🇮🇱', name:'Israele' },
  { code:'TR', flag:'🇹🇷', name:'Turchia' },
  { code:'EG', flag:'🇪🇬', name:'Egitto' },
  { code:'MA', flag:'🇲🇦', name:'Marocco' },
  { code:'TN', flag:'🇹🇳', name:'Tunisia' },
  { code:'DZ', flag:'🇩🇿', name:'Algeria' },
  { code:'ZA', flag:'🇿🇦', name:'Sudafrica' }
];
function __normalizeGuestNationalityCode__(code){
  return String(code || '').trim().toUpperCase();
}
function __getGuestNationalityOption__(code){
  const target = __normalizeGuestNationalityCode__(code);
  return __GUEST_NATIONALITY_OPTIONS__.find((item) => String(item.code || '').toUpperCase() === target) || __GUEST_NATIONALITY_OPTIONS__[0];
}
function __readGuestNationalityFromRecord__(ospite){
  const code = __normalizeGuestNationalityCode__(ospite?.nazionalita_code ?? ospite?.nazionalitaCode ?? ospite?.country_code ?? ospite?.countryCode ?? ospite?.nazionalita ?? ospite?.nazione ?? ospite?.country ?? ospite?.paese ?? '');
  if (code) return __getGuestNationalityOption__(code);
  const name = String(ospite?.nazionalita_nome ?? ospite?.nazionalitaNome ?? ospite?.country_name ?? ospite?.countryName ?? '').trim().toLowerCase();
  if (name){
    const found = __GUEST_NATIONALITY_OPTIONS__.find((item) => String(item.name || '').trim().toLowerCase() === name);
    if (found) return found;
  }
  return __GUEST_NATIONALITY_OPTIONS__[0];
}
function updateGuestNationalityButton(){
  try{
    const hidden = document.getElementById('guestNationality');
    const flag = document.getElementById('guestNationalityFlag');
    const btn = document.getElementById('guestNationalityBtn');
    if (!hidden || !flag || !btn) return;
    const option = __getGuestNationalityOption__(hidden.value || '');
    hidden.value = option.code || '';
    flag.textContent = option.flag || '🏳️';
    btn.dataset.code = option.code || '';
    btn.dataset.name = option.name || '';
    btn.setAttribute('title', option.name || 'Nazionalità');
    btn.setAttribute('aria-label', option.name ? ('Nazionalità: ' + option.name) : 'Seleziona nazionalità');
  }catch(_){ }
}
function setGuestNationality(code, { manual = true } = {}){
  try{
    const hidden = document.getElementById('guestNationality');
    if (!hidden) return;
    const option = __getGuestNationalityOption__(code);
    hidden.value = option.code || '';
    hidden.dataset.manual = manual ? '1' : '0';
    updateGuestNationalityButton();
  }catch(_){ }
}
function closeGuestNationalityModal(){
  try{
    const modal = document.getElementById('guestNationalityModal');
    if (!modal) return;
    modal.hidden = true;
    modal.setAttribute('aria-hidden', 'true');
  }catch(_){ }
}
function renderGuestNationalityModal(){
  try{
    const list = document.getElementById('guestNationalityList');
    if (!list) return;
    const current = __normalizeGuestNationalityCode__(document.getElementById('guestNationality')?.value || '');
    const options = [...__GUEST_NATIONALITY_OPTIONS__].sort((a, b) => {
      const nameA = String(a?.name || '').trim();
      const nameB = String(b?.name || '').trim();
      return nameA.localeCompare(nameB, 'it', { sensitivity:'base' });
    });
    list.innerHTML = options.map((item) => {
      const selected = String(item.code || '').toUpperCase() === current;
      return `<button type="button" class="guest-nationality-option${selected ? ' is-selected' : ''}" data-code="${escapeHtml(item.code || '')}" aria-selected="${selected ? 'true' : 'false'}"><span aria-hidden="true" class="guest-nationality-option-flag">${escapeHtml(item.flag || '🏳️')}</span><span class="guest-nationality-option-name">${escapeHtml(item.name || '')}</span></button>`;
    }).join('');
  }catch(_){ }
}
function __guestNationalityInfoText__(){
  try{
    const option = __getGuestNationalityOption__(document.getElementById('guestNationality')?.value || '');
    return String(option?.name || 'Nazionalità non selezionata').trim() || 'Nazionalità non selezionata';
  }catch(_){
    return 'Nazionalità non selezionata';
  }
}
function __showGuestNationalityInfoPopup__(){
  try{
    const text = __guestNationalityInfoText__();
    toast(text, 'blue');
  }catch(_){ }
}
function openGuestNationalityModal(){
  try{
    const isView = !!(state && state.page === 'ospite' && state.guestMode === 'view');
    if (isView){
      __showGuestNationalityInfoPopup__();
      return;
    }
    const modal = document.getElementById('guestNationalityModal');
    if (!modal) return;
    renderGuestNationalityModal();
    modal.hidden = false;
    modal.setAttribute('aria-hidden', 'false');
  }catch(_){ }
}
function __bindGuestNationalityUI__(){
  try{
    const btn = document.getElementById('guestNationalityBtn');
    if (btn && !btn.__boundGuestNationality){
      btn.__boundGuestNationality = true;
      bindFastTap(btn, () => { openGuestNationalityModal(); });
    }
    const modal = document.getElementById('guestNationalityModal');
    if (modal && !modal.__boundGuestNationality){
      modal.__boundGuestNationality = true;
      modal.addEventListener('click', (e) => {
        const pick = e.target && e.target.closest ? e.target.closest('.guest-nationality-option') : null;
        if (pick){
          const code = pick.getAttribute('data-code') || '';
          setGuestNationality(code, { manual:true });
          closeGuestNationalityModal();
          return;
        }
        if (e.target === modal) closeGuestNationalityModal();
      });
    }
    const closeBtn = document.getElementById('guestNationalityModalClose');
    if (closeBtn && !closeBtn.__boundGuestNationality){
      closeBtn.__boundGuestNationality = true;
      bindFastTap(closeBtn, () => { closeGuestNationalityModal(); });
    }
    try{ __bindGuestPhoneNationalityAuto__(); }catch(_){ }
  }catch(_){ }
}

function normalizeWhatsAppPhone(raw){
  let original = String(raw || '').trim();
  if (!original) return '';
  let s = original.replace(/[^\d+]/g, '');
  if (!s) return '';

  // I numeri WhatsApp vanno passati in formato internazionale senza "+".
  // Se il numero contiene già un prefisso internazionale (+48, 0048, ecc.)
  // non deve mai essere anteposto il prefisso italiano 39.
  if (s.startsWith('00')) s = '+' + s.slice(2);
  if (s.startsWith('+')){
    s = s.slice(1).replace(/\D/g, '');
    return s;
  }

  s = s.replace(/\D/g, '');
  if (!s) return '';

  const uniquePrefixes = Array.from(new Set([
    '972','971','966','995','994','90','81','82','86','91','212','213','216','20','27','1','7',
    '30','31','32','33','34','39','40','41','43','44','45','46','47','48','49','351','352',
    '353','354','358','36','380','381','385','386','389','420','421','355','356','52','54',
    '55','61','64','506'
  ])).sort(function(a,b){ return b.length - a.length; });

  // Numeri italiani locali: 0..., 3... o lunghezze nazionali senza prefisso.
  if (s.startsWith('0')) return '39' + s.slice(1);
  if (s.startsWith('39')) return s;

  // Se il numero senza + inizia con un prefisso internazionale noto diverso da 39,
  // lo considero già internazionale. Esempio: 48601611796 => Polonia, non Italia.
  for (const prefix of uniquePrefixes){
    if (prefix && prefix !== '39' && s.startsWith(prefix) && s.length > prefix.length + 5){
      return s;
    }
  }

  return '39' + s;
}

const __GUEST_NATIONALITY_PHONE_PREFIXES__ = [
  ['972','IL'], ['971','AE'], ['966','SA'], ['972','IL'], ['972','IL'], ['995','GE'], ['994','AZ'],
  ['90','TR'], ['81','JP'], ['82','KR'], ['86','CN'], ['91','IN'], ['212','MA'], ['213','DZ'], ['216','TN'], ['20','EG'], ['27','ZA'],
  ['1','US'], ['7','RU'], ['30','GR'], ['31','NL'], ['32','BE'], ['33','FR'], ['34','ES'], ['39','IT'], ['40','RO'], ['41','CH'], ['43','AT'], ['44','GB'], ['45','DK'], ['46','SE'], ['47','NO'], ['48','PL'], ['49','DE'],
  ['351','PT'], ['352','LU'], ['353','IE'], ['354','IS'], ['358','FI'], ['36','HU'], ['380','UA'], ['381','RS'], ['385','HR'], ['386','SI'], ['389','MK'], ['420','CZ'], ['421','SK'], ['355','AL'], ['356','MT'],
  ['52','MX'], ['54','AR'], ['55','BR'], ['61','AU'], ['64','NZ'], ['506','CR'], ['972','IL']
];
function __detectGuestNationalityFromPhone__(raw){
  try{
    let s = String(raw || '').trim();
    if (!s) return '';
    s = s.replace(/[^\d+]/g, '');
    if (!s) return '';
    if (s.startsWith('00')) s = '+' + s.slice(2);
    if (s.startsWith('+')) s = s.slice(1);
    else if (s.startsWith('0')) s = '39' + s.slice(1);
    s = s.replace(/\D/g, '');
    if (!s) return '';
    const hit = __GUEST_NATIONALITY_PHONE_PREFIXES__.slice().sort((a,b)=>b[0].length-a[0].length).find(([prefix]) => s.startsWith(prefix));
    return hit ? String(hit[1] || '').trim().toUpperCase() : '';
  }catch(_){ return ''; }
}
function __syncGuestNationalityFromPhone__(force=false){
  try{
    const phoneEl = document.getElementById('guestPhone');
    const hidden = document.getElementById('guestNationality');
    if (!phoneEl || !hidden) return;
    const manual = String(hidden.dataset.manual || '0') === '1';
    // dDAE_2.774: una nazionalità scelta manualmente deve restare modificabile e non
    // deve essere sovrascritta dall'automatismo del prefisso telefono, nemmeno sugli
    // eventi input/change/blur che richiamano questa funzione con force=true.
    if (manual) return;
    const code = __detectGuestNationalityFromPhone__(phoneEl.value || '');
    if (code){
      setGuestNationality(code, { manual:false });
      hidden.dataset.phoneAutoLast = String(code || '');
      return;
    }
    if (force){
      setGuestNationality('', { manual:false });
      hidden.dataset.phoneAutoLast = '';
    }
  }catch(_){ }
}
function __bindGuestPhoneNationalityAuto__(){
  try{
    const phoneEl = document.getElementById('guestPhone');
    if (!phoneEl || phoneEl.dataset.nationalityAutoBound === '1') return;
    phoneEl.dataset.nationalityAutoBound = '1';
    const handler = () => { try{ __syncGuestNationalityFromPhone__(true); }catch(_){ } };
    phoneEl.addEventListener('input', handler);
    phoneEl.addEventListener('change', handler);
    phoneEl.addEventListener('blur', handler);
  }catch(_){ }
}

function openGuestPhoneWhatsApp(){
  try{
    const phoneEl = document.getElementById('guestPhone');
    const isView = !!(state && state.page === 'ospite' && state.guestMode === 'view');
    if (!phoneEl || !isView) return;
    const raw = String(phoneEl.value || '').trim();
    const normalized = normalizeWhatsAppPhone(raw);
    if (!normalized) return;
    const url = 'https://wa.me/' + encodeURIComponent(normalized);
    try{ window.location.href = url; }catch(_){ window.open(url, '_blank', 'noopener'); }
  }catch(_){ }
}

function syncGuestPhoneWhatsAppLink(isView){
  try{
    const phoneEl = document.getElementById('guestPhone');
    if (!phoneEl) return;
    if (!phoneEl.dataset.waBound){
      phoneEl.addEventListener('click', function(){
        try{ openGuestPhoneWhatsApp(); }catch(_){ }
      });
      phoneEl.addEventListener('keydown', function(ev){
        const key = ev && (ev.key || ev.code || '');
        if (key === 'Enter' || key === ' ' || key === 'Spacebar'){
          try{ ev.preventDefault(); }catch(_){ }
          try{ openGuestPhoneWhatsApp(); }catch(_){ }
        }
      });
      phoneEl.dataset.waBound = '1';
    }
    const normalized = normalizeWhatsAppPhone(phoneEl.value || '');
    const enabled = !!isView && !!normalized;
    phoneEl.classList.toggle('whatsapp-link', enabled);
    try{ phoneEl.setAttribute('aria-label', enabled ? 'Apri chat WhatsApp' : 'Telefono'); }catch(_){ }
    try{ phoneEl.setAttribute('title', enabled ? 'Apri chat WhatsApp' : ''); }catch(_){ }
    try{ phoneEl.readOnly = !!isView; }catch(_){ }
    try{ phoneEl.tabIndex = enabled ? 0 : -1; }catch(_){ }
  }catch(_){ }
}


function __readGuestGender__(ospite){
  try{
    const raw = String(
      ospite?.sesso ?? ospite?.gender ?? ospite?.genere ?? ospite?.sex ?? ""
    ).trim().toLowerCase();
    if (!raw) return "";
    if (raw === "m" || raw === "male" || raw === "maschio" || raw === "uomo") return "uomo";
    if (raw === "f" || raw === "female" || raw === "femmina" || raw === "donna") return "donna";
    return "";
  }catch(_){ return ""; }
}

function __readGuestPeopleCount__(ospite, kind){
  try{
    const isWomen = String(kind || '').toLowerCase() === 'women';
    const raw = isWomen
      ? (ospite?.donne ?? ospite?.donne_count ?? ospite?.donneCount ?? ospite?.women ?? ospite?.women_count ?? ospite?.womenCount ?? ospite?.femmine ?? ospite?.females ?? ospite?.female_count ?? ospite?.femaleCount)
      : (ospite?.uomini ?? ospite?.uomini_count ?? ospite?.uominiCount ?? ospite?.men ?? ospite?.men_count ?? ospite?.menCount ?? ospite?.maschi ?? ospite?.males ?? ospite?.male_count ?? ospite?.maleCount);
    const n = parseInt(String(raw ?? '').replace(',', '.'), 10);
    return Number.isFinite(n) && n > 0 ? n : 0;
  }catch(_){ return 0; }
}

function __syncGuestSexCountInputsReadonly__(isView){
  try{
    const row = document.getElementById('guestSexCountRow');
    if (row) row.hidden = !isView;
    ['guestMen','guestWomen'].forEach((id)=>{
      const el = document.getElementById(id);
      if (!el) return;
      el.readOnly = true;
      try{ el.setAttribute('readonly','readonly'); }catch(_){ }
      try{ el.setAttribute('inputmode','none'); }catch(_){ }
      try{ el.classList.add('tap-counter'); }catch(_){ }
      try{ el.tabIndex = isView ? 0 : -1; }catch(_){ }
    });
    if (isView) __setupGuestSexCountTapCounters__();
  }catch(_){ }
}

function __guestSexCountFields__(kind, value){
  const isWomen = String(kind || '').toLowerCase() === 'women';
  const n = Math.max(0, parseInt(String(value || '0'), 10) || 0);
  return isWomen ? {
    donne:n, donne_count:n, donneCount:n, femmine:n, women:n, women_count:n, womenCount:n
  } : {
    uomini:n, uomini_count:n, uominiCount:n, maschi:n, men:n, men_count:n, menCount:n
  };
}

async function __saveGuestSexCountFromView__(kind, value){
  try{
    const base = state.guestViewItem || state.guestEditSourceItem || null;
    const id = guestIdOf(base) || base?.id || state.guestEditId || '';
    if (!id) return;
    const body = Object.assign({ id:String(id) }, __guestSexCountFields__(kind, value));
    await api('ospiti', { method:'PUT', body, showLoader:false });
    try{
      const patch = __guestSexCountFields__(kind, value);
      if (state.guestViewItem) Object.assign(state.guestViewItem, patch);
      if (state.guestEditSourceItem) Object.assign(state.guestEditSourceItem, patch);
      const gid = String(id || '').trim();
      [state.ospiti, state.guests, state.bookings, state.guestList].forEach((list)=>{
        if (!Array.isArray(list)) return;
        list.forEach((row)=>{ try{ if (String(guestIdOf(row) || row?.id || '').trim() === gid) Object.assign(row, patch); }catch(_){ } });
      });
    }catch(_){ }
    try{ invalidateApiCache('ospiti|'); }catch(_){ }
  }catch(e){ try{ toast(e?.message || 'Errore salvataggio conteggio'); }catch(_){ } }
}

function __setupGuestSexCountTapCounters__(){
  try{
    const specs = [
      { id:'guestMen', kind:'men' },
      { id:'guestWomen', kind:'women' }
    ];
    specs.forEach(({id, kind})=>{
      const el = document.getElementById(id);
      if (!el || el.__sexTapCounterBound) return;
      el.__sexTapCounterBound = true;
      try{ el.setAttribute('readonly','readonly'); }catch(_){ }
      try{ el.setAttribute('inputmode','none'); }catch(_){ }
      try{ el.classList.add('tap-counter'); }catch(_){ }
      let timer = null;
      let longFired = false;
      let saveTimer = null;
      const isActiveView = ()=> !!(state && state.page === 'ospite' && state.guestMode === 'view');
      const clearTimer = ()=>{ if (timer){ clearTimeout(timer); timer = null; } };
      const setVal = (v, immediate=false)=>{
        const n = Math.max(0, parseInt(String(v || '0'), 10) || 0);
        el.value = String(n);
        try{ el.dispatchEvent(new Event('input', { bubbles:true })); }catch(_){ }
        try{ el.dispatchEvent(new Event('change', { bubbles:true })); }catch(_){ }
        if (saveTimer) clearTimeout(saveTimer);
        const runSave = ()=>{ try{ __saveGuestSexCountFromView__(kind, n); }catch(_){ } };
        if (immediate) runSave(); else saveTimer = setTimeout(runSave, 180);
      };
      const onDown = (e)=>{
        if (!isActiveView()) return;
        longFired = false;
        clearTimer();
        try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
        timer = setTimeout(()=>{
          longFired = true;
          try{ __sfxGlass(); }catch(_){ }
          setVal(0, true);
        }, 500);
      };
      const onUp = (e)=>{
        if (!isActiveView()) return;
        clearTimer();
        try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
        if (longFired){ longFired = false; return; }
        setVal((parseInt(el.value || '0', 10) || 0) + 1, false);
      };
      const onCancel = ()=>{ clearTimer(); };
      if ((typeof window !== 'undefined') && ('PointerEvent' in window)){
        try{ el.addEventListener('pointerdown', onDown, { passive:false }); }catch(_){ el.addEventListener('pointerdown', onDown); }
        try{ el.addEventListener('pointerup', onUp, { passive:false }); }catch(_){ el.addEventListener('pointerup', onUp); }
        try{ el.addEventListener('pointercancel', onCancel, { passive:true }); }catch(_){ el.addEventListener('pointercancel', onCancel); }
      }else{
        try{ el.addEventListener('touchstart', onDown, { passive:false }); }catch(_){ el.addEventListener('touchstart', onDown); }
        try{ el.addEventListener('touchend', onUp, { passive:false }); }catch(_){ el.addEventListener('touchend', onUp); }
        try{ el.addEventListener('touchcancel', onCancel, { passive:true }); }catch(_){ el.addEventListener('touchcancel', onCancel); }
        try{ el.addEventListener('click', (e)=>{ if (isActiveView()){ e.preventDefault(); e.stopPropagation(); onUp(e); } }, { passive:false }); }catch(_){ }
      }
      try{ el.addEventListener('click', (e)=>{ if (isActiveView()){ e.preventDefault(); e.stopPropagation(); } }, { passive:false }); }catch(_){ }
      try{ el.addEventListener('contextmenu', (e)=>{ if (isActiveView()) e.preventDefault(); }); }catch(_){ }
      try{ el.addEventListener('focus', ()=>{ if (isActiveView()) try{ el.blur(); }catch(_){ } }); }catch(_){ }
    });
  }catch(_){ }
}

function __syncGuestGenderTabs__(){
  try{
    const row = document.getElementById('guestGenderTabsRow');
    // dDAE_2.776: i tab Uomo/Donna sono stati eliminati dalla scheda;
    // resta solo il conteggio Uomini/Donne editabile con multitap.
    if (row) row.hidden = true;
    const current = String(state?.guestGender || '').trim().toLowerCase();
    ['guestGenderMale','guestGenderFemale'].forEach((id) => {
      const btn = document.getElementById(id);
      if (!btn) return;
      const val = String(btn.dataset.genderValue || '').trim().toLowerCase();
      const on = !!(current && val === current);
      btn.classList.toggle('is-selected', on);
      btn.setAttribute('aria-pressed', on ? 'true' : 'false');
      try{ __applySingleActionButtonVisual__(btn, on ? 'on' : 'off'); }catch(_){ }
    });
  }catch(_){ }
}

async function __setGuestGenderFromView__(value){
  try{
    const safe = String(value || '').trim().toLowerCase() === 'donna' ? 'donna' : 'uomo';
    state.guestGender = safe;
    __syncGuestGenderTabs__();
    const base = state.guestViewItem || state.guestEditSourceItem || null;
    const id = guestIdOf(base) || base?.id || state.guestEditId || '';
    if (!id) return;
    const body = {
      id:String(id),
      sesso:safe,
      gender:safe,
      genere:safe
    };
    await api('ospiti', { method:'PUT', body, showLoader:false });
    try{
      if (state.guestViewItem){ state.guestViewItem.sesso = safe; state.guestViewItem.gender = safe; state.guestViewItem.genere = safe; }
      if (state.guestEditSourceItem){ state.guestEditSourceItem.sesso = safe; state.guestEditSourceItem.gender = safe; state.guestEditSourceItem.genere = safe; }
    }catch(_){ }
    try{ invalidateApiCache('ospiti|'); }catch(_){ }
    try{ toast(safe === 'donna' ? 'Donna salvato' : 'Uomo salvato'); }catch(_){ }
  }catch(e){ try{ toast(e?.message || 'Errore salvataggio genere'); }catch(_){ } }
}

function __setupGuestGenderTabs__(){
  try{
    ['guestGenderMale','guestGenderFemale'].forEach((id) => {
      const btn = document.getElementById(id);
      if (!btn || btn.dataset.genderBound === '1') return;
      btn.dataset.genderBound = '1';
      try{ __bindSingleActionButtonColorHold__(btn); }catch(_){ }
      try{ __applySingleActionButtonVisual__(btn); }catch(_){ }
      btn.addEventListener('click', (e) => {
        try{
          if ((btn.__singleActionButtonSuppressTapUntil || 0) > Date.now()) return;
          e.preventDefault();
          e.stopPropagation();
        }catch(_){ }
        try{ __setGuestGenderFromView__(btn.dataset.genderValue || ''); }catch(_){ }
      });
    });
  }catch(_){ }
}

function setGuestFormViewOnly(isView, ospite){
  try{ updateGuestFormModeClass(); }catch(_){ }
  try{ syncGuestCreatePrimaryStack(); }catch(_){ }
  try{ __bindGuestNameCopyInView__(); }catch(_){ }
  const card = document.querySelector("#page-ospite .guest-form-card");
  if (card) card.classList.toggle("is-view", !!isView);
  try{ const nameEl = document.getElementById('guestName'); if (nameEl) nameEl.readOnly = !!isView; }catch(_){ }
  try{ __syncGuestSexCountInputsReadonly__(!!isView); }catch(_){ }

  const btn = document.getElementById("createGuestCard");
  const createChoice = document.getElementById("guestCreateChoiceRow");
  const isCreateMode = String(state.guestMode || "").toLowerCase() === "create";
  if (btn) btn.hidden = !!isView || isCreateMode;
  if (createChoice) createChoice.hidden = !!isView || !isCreateMode;

  const picker = document.getElementById("roomsPicker");
  if (picker) { try{ ensureRoomsPickerButtons(); }catch(_){ } picker.hidden = !!isView; }

  const ro = document.getElementById("roomsReadOnly");
  if (ro) {
    ro.hidden = !isView;
    if (isView) renderRoomsReadOnly(ospite);
    else ro.innerHTML = "";
  }

  
  // In sola lettura: nascondi i campi di immissione non necessari (ospiti + date)
  try{
    const hideRowByInputId = (inputId, hide) => {
      const el = document.getElementById(inputId);
      if (!el) return;
      const row = el.closest('.field');
      if (row) row.hidden = !!hide;
    };
    // Numero ospiti (adulti/bambini)
    hideRowByInputId("guestAdults", !!isView);
    // Date check-in / check-out
    hideRowByInputId("guestCheckIn", !!isView);
    // In sola lettura mantieni visibile importo commissione e channel; percentuale channel nascosta
    try{
      const commWrap = document.getElementById("guestChannelCommissionWrap");
      if (commWrap) commWrap.hidden = true;
    }catch(_){ }
  }catch(_){ }

  try{ const notesEl = document.getElementById("guestNotes"); if (notesEl) notesEl.readOnly = !!isView; }catch(_){}
  try{ syncGuestPhoneWhatsAppLink(!!isView); }catch(_){}
  try{ __syncGuestGenderTabs__(); }catch(_){}
  try{ __setupGuestGenderTabs__(); }catch(_){}
  try{ updateGuestNationalityButton(); }catch(_){}
  // Servizi: in sola lettura mostra il tasto accanto a "Importo servizi" (layout modifica invariato)
  try{ __placeServicesPillForView(!!isView); }catch(_){ }

  // Servizi: chiudi lista quando non in view
  try{
    const wrap = document.getElementById("servicesListWrap");
    if (wrap && !isView) wrap.hidden = true;
  }catch(_){ }
// Aggiorna i pallini in testata in base alla modalità corrente
  try { updateOspiteHdActions(); } catch (_) {}
  try{ if (isView && !String(state.guestGroupActiveId || '').trim()) __setGuestGroupInfoVisible__(false); }catch(_){ }
}

function enterGuestViewMode(ospite){
  // Riempiamo la maschera usando la stessa logica dell'edit, poi blocchiamo tutto in sola lettura
  enterGuestEditMode(ospite);
  state.guestMode = "view";
  try{ updateGuestFormModeClass(); }catch(_){ }
  try{ syncGuestCreatePrimaryStack(); }catch(_){ }
  state.guestViewItem = ospite || null;
  state.guestViewPrimaryId = guestIdOf(ospite) || ospite?.id || null;
  state.guestGroupActiveId = null;

  
  // Servizi: carica sempre elenco e totale (anche se non ci sono multi-prenotazioni)
  try{
    state.guestServicesManualOverride = false;
    const ospiteId = guestIdOf(ospite) || (ospite?.id ?? null);
    const key = ospiteId ? String(ospiteId) : "";
    const cache = (key && state.guestServicesCacheById) ? state.guestServicesCacheById[key] : null;

    state.guestServicesLoadingFor = null;

    if (cache && Array.isArray(cache.items)){
      state.guestServicesItems = cache.items.slice();
      state.guestServicesComputedTotal = isFinite(cache.total) ? cache.total : serviziComputeTotal(state.guestServicesItems);
      state.guestServicesLoadedFor = ospiteId;
      state.guestServicesLoadedAt = cache.loadedAt || 0;
    } else {
      state.guestServicesLoadedFor = null;
      state.guestServicesLoadedAt = 0;
      state.guestServicesItems = [];
      state.guestServicesComputedTotal = 0;
    }
    loadServiziForOspite(ospite);
  }catch(_){ }

const title = document.getElementById("ospiteFormTitle");
  if (title) title.textContent = "";
  try{ __syncGuestNationalityPlacement__(); }catch(_){}

  setGuestFormViewOnly(true, ospite);
  // Multi prenotazioni: mostra prenotazioni aggiuntive sotto la prima
  try{
    if (Array.isArray(state.guestGroupBookings) && state.guestGroupBookings.length > 1){
      renderGuestMulti({ mode: "view" });
    } else {
      clearGuestMulti();
    }
    __setGuestGroupInfoVisible__(false);
  }catch(_){ }
  try { updateOspiteHdActions(); } catch (_) {}
}

/* ──────────────────────────────────────────────────────────────
   SERVIZI (ospite): lista + totale + popup aggiunta
   ────────────────────────────────────────────────────────────── */

function normalizeServiziResponse(res){
  if (!res) return [];
  if (Array.isArray(res)) return res;
  if (Array.isArray(res.items)) return res.items;
  if (Array.isArray(res.data)) return res.data;
  if (Array.isArray(res.rows)) return res.rows;
  return [];
}

function serviziComputeTotal(items){
  let sum = 0;
  (items || []).forEach(s => {
    if (!s) return;
    const del = s.isDeleted ?? s.is_deleted ?? s.deleted;
    if (String(del) === "1" || del === true) return;
    const qty = parseFloat(s.qty ?? 1) || 1;
    const amt = parseFloat(s.importo ?? s.amount ?? 0) || 0;
    sum += (qty * amt);
  });
  return Math.round(sum * 100) / 100;
}

function serviziPreviewText(items){
  const n = (items || []).filter(s => {
    const del = s?.isDeleted ?? s?.is_deleted ?? s?.deleted;
    return !(String(del) === "1" || del === true);
  }).length;
  return n > 0 ? (n + " servizi") : "";
}


function guestNotesValue(item){
  return String(item?.note ?? item?.notes ?? item?.nota ?? "").trim();
}

function guestHasNotes(item){
  return guestNotesValue(item).length > 0;
}

function setGuestNotesExpanded(expanded){
  try{
    const wrap = document.getElementById("guestNotesWrap");
    const btn = document.getElementById("guestNotesToggle");
    if (wrap) wrap.hidden = !expanded;
    if (btn){
      btn.setAttribute("aria-expanded", expanded ? "true" : "false");
      btn.classList.toggle("is-open", !!expanded);
    }
  }catch(_){ }
  try{ state.guestNotesExpanded = !!expanded; }catch(_){ }
}

function updateGuestNotesIndicator(){
  try{
    const dot = document.getElementById("guestNotesToggleDot");
    if (!dot) return;
    const notesVal = String(document.getElementById("guestNotes")?.value || "").trim();
    dot.hidden = !(notesVal.length > 0);
  }catch(_){ }
}

function syncGuestNotesUI(item, opts = {}){
  const notesEl = document.getElementById("guestNotes");
  if (!notesEl) return;
  const notes = (item === null || item === undefined) ? "" : guestNotesValue(item);
  notesEl.value = notes;
  const placeholder = (String(state.guestMode || "") === "view") ? "Nessuna nota" : "Inserisci eventuali note";
  notesEl.placeholder = placeholder;
  updateGuestNotesIndicator();
  const wantsOpen = Object.prototype.hasOwnProperty.call(opts, "open") ? !!opts.open : (!!notes && !!state.guestNotesExpanded);
  setGuestNotesExpanded(wantsOpen);
}

function renderServiziList(){
  const wrap = document.getElementById("servicesListWrap");
  const body = document.getElementById("servicesListBody");
  const totalEl = document.getElementById("servicesListTotal");
  const addBtn = document.getElementById("servicesPillEdit");
  if (!wrap || !body || !totalEl) return;

  const mode = String(state.guestMode || "").toLowerCase();
  const isEdit = (mode === "edit");
  try{ wrap.dataset.mode = isEdit ? "edit" : "view"; }catch(_){}
  try{ if (addBtn) addBtn.hidden = !isEdit; }catch(_){}

  totalEl.textContent = formatEUR(state.guestServicesComputedTotal || 0);

  const items = (state.guestServicesItems || []).filter(s => {
    const del = s?.isDeleted ?? s?.is_deleted ?? s?.deleted;
    return !(String(del) === "1" || del === true);
  });

  body.innerHTML = "";

  const currentId = (mode === "view") ? guestIdOf(state.guestViewItem) : state.guestEditId;
  const isLoading = !!currentId && String(state.guestServicesLoadingFor || "") === String(currentId);

  if (!items.length){
    const empty = document.createElement("div");
    empty.className = "service-item";
    const msg = isLoading ? "Caricamento..." : "Nessun servizio";
    const amt = isLoading ? "…" : "€0,00";
    empty.innerHTML = `<div class="meta"><div class="name">${msg}</div><div class="desc">—</div></div><div class="amt">${amt}</div>`;
    body.appendChild(empty);
    return;
  }

  items.forEach((s) => {
    const name = String(s.servizio ?? s.name ?? "").trim() || "Servizio";
    const desc = String(s.descrizione ?? s.desc ?? "").trim() || "";
    const qty = parseFloat(s.qty ?? 1) || 1;
    const amt = parseFloat(s.importo ?? s.amount ?? 0) || 0;
    const total = qty * amt;

    const row = document.createElement("div");
    row.className = "service-item";

    const meta = document.createElement("div");
    meta.className = "meta";
    meta.innerHTML = `
      <div class="name">${escapeHtml(name)}${(qty && qty !== 1) ? ` <span style="opacity:.8;font-weight:800">×${qty}</span>` : ""}</div>
      ${desc ? `<div class="desc">${escapeHtml(desc)}</div>` : `<div class="desc">—</div>`}
    `;
    row.appendChild(meta);

    if (isEdit){
      const right = document.createElement("div");
      right.className = "svc-right";

      const delBtn = document.createElement("button");
      delBtn.type = "button";
      delBtn.className = "svc-del";
      delBtn.setAttribute("aria-label", "Elimina servizio");
      delBtn.title = "Elimina";

      bindFastTap(delBtn, async () => {
        try{
          const all = state.guestServicesItems || [];
          let idx = all.indexOf(s);
          if (idx < 0){
            // fallback: match by content
            idx = all.findIndex(x =>
              String(x?.servizio ?? x?.name ?? "") === String(s?.servizio ?? s?.name ?? "") &&
              String(x?.descrizione ?? x?.desc ?? "") === String(s?.descrizione ?? s?.desc ?? "") &&
              (parseFloat(x?.importo ?? x?.amount ?? 0) || 0) === (parseFloat(s?.importo ?? s?.amount ?? 0) || 0) &&
              (parseFloat(x?.qty ?? 1) || 1) === (parseFloat(s?.qty ?? 1) || 1)
            );
          }
          if (idx >= 0) all.splice(idx, 1);

          state.guestServicesItems = all;
          state.guestServicesComputedTotal = serviziComputeTotal(all);

          // se non c'è override manuale, aggiorna input con la somma
          if (!state.guestServicesManualOverride){
            const sIn = document.getElementById("guestServices");
            if (sIn) sIn.value = String(state.guestServicesComputedTotal || 0);
          }

          try { updateGuestRemaining(); } catch (_) {}
          try { renderServiziList(); } catch (_) {}

          // persisti subito (questa operazione, lato backend, riscrive l'elenco senza il servizio)
          await persistServiziForCurrentGuest();

          try{ __syncServiziToCurrentGuest(state.guestEditId); }catch(_){}
          try{ toast("Servizio eliminato"); }catch(_){}
        }catch(_){}
      });

      const amtEl = document.createElement("div");
      amtEl.className = "amt";
      amtEl.textContent = formatEUR(total);

      right.appendChild(amtEl);
      right.appendChild(delBtn);
      row.appendChild(right);
    } else {
      const amtEl = document.createElement("div");
      amtEl.className = "amt";
      amtEl.textContent = formatEUR(total);
      row.appendChild(amtEl);
    }

    body.appendChild(row);
  });
}



async function deleteServizioFromCurrentGuest(serviceIdx){
  try{
    if (String(state.guestMode || "").toLowerCase() !== "edit") return;
    const ospiteId = state.guestEditId;
    if (!ospiteId) return;

    const arr = Array.isArray(state.guestServicesItems) ? state.guestServicesItems : [];
    if (!arr.length) return;
    if (serviceIdx < 0 || serviceIdx >= arr.length) return;

    // Rimuovi subito in UI
    arr.splice(serviceIdx, 1);
    state.guestServicesItems = arr;
    state.guestServicesComputedTotal = serviziComputeTotal(arr);
    try{ state.guestServicesManualOverride = false; }catch(_){ }
    try{ applyServiziTotalsToUI({ servizi_totale: state.guestServicesComputedTotal }); }catch(_){ }
    try{ renderServiziList(); }catch(_){ }

    // Persisti (POST: backend cancella le righe del foglio per ospite e reinserisce le restanti)
    try{
      await persistServiziForCurrentGuest();
      try{ toast("Servizio eliminato"); }catch(_){ }
    }catch(err){
      // In caso di errore, ricarica dal backend per riallineare
      try{ state.guestServicesLoadedFor = null; state.guestServicesLoadedAt = 0; }catch(_){ }
      try{ await loadServiziForOspite({ id: ospiteId }); }catch(_){ }
      try{ renderServiziList(); }catch(_){ }
      try{ toast("Errore eliminazione servizio"); }catch(_){ }
    }
  }catch(_){}
}

async function loadServiziForOspite(ospite){
  const ospiteId = guestIdOf(ospite) || state.guestEditId;
  if (!ospiteId) return;

  // Cache immediata: l'elenco può aprirsi istantaneamente anche prima della risposta API
  try{
    const key = String(ospiteId);
    const cache = state.guestServicesCacheById ? state.guestServicesCacheById[key] : null;
    if (cache && Array.isArray(cache.items)){
      const needApply = (state.guestServicesLoadedFor !== ospiteId);
      if (needApply){
        state.guestServicesItems = cache.items.slice();
        state.guestServicesComputedTotal = isFinite(cache.total) ? cache.total : serviziComputeTotal(state.guestServicesItems);
        state.guestServicesLoadedFor = ospiteId;
        state.guestServicesLoadedAt = cache.loadedAt || 0;
        try { applyServiziTotalsToUI(ospite); } catch (_) {}
        try { __syncServiziToCurrentGuest(ospiteId); } catch (_) {}
      }
    }
  }catch(_){}


  // evita richieste duplicate contemporanee ma NON bloccare i retry
  if (state.guestServicesLoadingFor === ospiteId) return;

  // se abbiamo già caricato di recente, riusa (ma in view aprendo la lista vogliamo dati freschi)
  const loadedSame = (state.guestServicesLoadedFor === ospiteId);
  const loadedAt = state.guestServicesLoadedAt || 0;
  const ageMs = Date.now() - loadedAt;

  const mode = String(state.guestMode || "").toLowerCase();
  const allowReuse = loadedSame && ageMs < 4000; // riuso ultra-breve per evitare doppie chiamate sul cambio modalità

  if (allowReuse){
    try { applyServiziTotalsToUI(ospite); } catch (_) {}
    try { __syncServiziToCurrentGuest(ospiteId); } catch (_) {}
    return;
  }

  state.guestServicesLoadingFor = ospiteId;

  let items = null;
  try{
    const res = await api("servizi", { method: "GET", params: { ospite_id: ospiteId }, showLoader: false });
    items = normalizeServiziResponse(res);
  }catch(_){
    items = null; // forza retry al prossimo tentativo
  }

  // Solo se abbiamo ottenuto una risposta valida aggiorniamo 'loadedFor'
  if (items !== null){
    state.guestServicesItems = Array.isArray(items) ? items : [];
    state.guestServicesComputedTotal = serviziComputeTotal(state.guestServicesItems);
    state.guestServicesLoadedFor = ospiteId;
    state.guestServicesLoadedAt = Date.now();
    try{
      const key = String(ospiteId);
      if (!state.guestServicesCacheById) state.guestServicesCacheById = {};
      state.guestServicesCacheById[key] = {
        items: (state.guestServicesItems || []).slice(),
        total: (state.guestServicesComputedTotal || 0),
        loadedAt: (state.guestServicesLoadedAt || Date.now())
      };
    }catch(_){}
    try { applyServiziTotalsToUI(ospite); } catch (_) {}
  } else {
    // non segnare come caricato → al prossimo tap riprova
    try { state.guestServicesLoadedFor = null; } catch (_) {}
  }

  state.guestServicesLoadingFor = null;
}

function applyServiziTotalsToUI(ospite){
  const input = document.getElementById("guestServices");
  if (!input) return;

  // default: se esiste override in ospite.servizi_totale, usalo; altrimenti somma
  const sheetOverride = parseFloat(ospite?.servizi_totale ?? ospite?.serviziTotal ?? ospite?.importo_servizi ?? "") ;
  const hasOverride = isFinite(sheetOverride);
  const base = hasOverride ? sheetOverride : (state.guestServicesComputedTotal || 0);

  // in edit: se l'utente ha già modificato a mano, non sovrascrivere
  const isEdit = String(state.guestMode || "").toLowerCase() === "edit";
  if (isEdit && state.guestServicesManualOverride) {
    // non toccare il valore digitato dall'utente
  } else {
    input.value = (isFinite(base) ? String(base) : "0");
  }

  try { updateGuestRemaining(); } catch (_) {}

  // aggiorna dropdown se visibile
  try { renderServiziList(); } catch (_) {}
}

function __syncServiziToCurrentGuest(ospiteId){
  const id = String(ospiteId||"").trim();
  if (!id) return;
  const computed = (state.guestServicesComputedTotal || 0);
  const preview = serviziPreviewText(state.guestServicesItems || []);
  // aggiorna oggetto ospite in view
  try{
    if (state.guestViewItem && String(state.guestViewItem.id||"") === id){
      state.guestViewItem.servizi_totale = computed;
      state.guestViewItem.servizi_preview = preview;
    }
  }catch(_){}
  // aggiorna lista ospiti in memoria (se presente)
  try{
    if (Array.isArray(state.guests)){
      const g = state.guests.find(x => String(x?.id||"") === id);
      if (g){
        g.servizi_totale = computed;
        g.servizi_preview = preview;
      }
    }
  }catch(_){}
}

async function persistServiziForCurrentGuest(){
  const ospiteId = state.guestEditId;
  if (!ospiteId) return;

  const items = (state.guestServicesItems || []).filter(s => {
    const del = s?.isDeleted ?? s?.is_deleted ?? s?.deleted;
    return !(String(del) === "1" || del === true);
  }).map(s => ({
    servizio: String(s.servizio ?? s.name ?? "").trim(),
    descrizione: String(s.descrizione ?? s.desc ?? "").trim(),
    importo: parseFloat(s.importo ?? s.amount ?? 0) || 0,
    qty: parseFloat(s.qty ?? 1) || 1
  }));

  try{
    await api("servizi", { method: "POST", body: { ospite_id: ospiteId, servizi: items } });

    // aggiorna cache locale immediatamente per evitare flicker (comparsa/scomparsa/ricomparsa)
    try{
      const key = String(ospiteId);
      if (!state.guestServicesCacheById) state.guestServicesCacheById = {};
      state.guestServicesCacheById[key] = {
        items: (state.guestServicesItems || []).slice(),
        total: (state.guestServicesComputedTotal || serviziComputeTotal(state.guestServicesItems || [])),
        loadedAt: Date.now()
      };
    }catch(_){}

  

    // dopo persistenza: forza reload dal backend e sincronizza il totale in UI (anche in sola lettura)
    try{ state.guestServicesLoadedFor = null; state.guestServicesLoadedAt = 0; }catch(_){ }
    try{ await loadServiziForOspite({ id: ospiteId }); }catch(_){ }
    try{ __syncServiziToCurrentGuest(ospiteId); }catch(_){ }
}catch(_){}
}

function serviziAddLocal({ servizio, descrizione, importo }){
  const row = {
    id: genId("sv"),
    ospite_id: state.guestEditId,
    servizio: servizio,
    descrizione: descrizione,
    importo: importo,
    qty: 1,
    isDeleted: ""
  };
  state.guestServicesItems = state.guestServicesItems || [];
  state.guestServicesItems.push(row);
  state.guestServicesComputedTotal = serviziComputeTotal(state.guestServicesItems);
}

/* bind UI */
function initServiziUI(){
  const pillView = document.getElementById("servicesPillView");
  const pillEdit = document.getElementById("servicesPillEdit");
  const wrap = document.getElementById("servicesListWrap");

  const modal = document.getElementById("servicesModal");
  const closeBtn = document.getElementById("servicesModalClose");
  const btnCancel = document.getElementById("svcCancel");
  const btnSave = document.getElementById("svcSave");
  const inName = document.getElementById("svcName");
  const inAmt = document.getElementById("svcAmount");
  const servicesInput = document.getElementById("guestServices");

  const openModal = () => {
    if (!modal) return;
    // solo in edit
    if (String(state.guestMode || "").toLowerCase() !== "edit") return;
    modal.hidden = false;
    try{ modal.setAttribute("aria-hidden", "false"); }catch(_){}
    try{ inName && inName.focus(); }catch(_){}
  };
  const closeModal = () => {
    if (!modal) return;
    modal.hidden = true;
    try{ modal.setAttribute("aria-hidden", "true"); }catch(_){}
  };
  const clearModal = () => {
    if (inName) inName.value = "";
    if (inAmt) inAmt.value = "";
    try { refreshFloatingLabels(); } catch (_) {}
  };

  const canToggle = () => {
    const m = String(state.guestMode || "").toLowerCase();
    return (m === "view" || m === "edit");
  };

  const toggleList = () => {
    if (!canToggle()) return;
    if (!wrap) return;

    const willShow = !!wrap.hidden;
    wrap.hidden = !willShow;

    if (!willShow) return;

    // Apertura istantanea: primo paint subito, poi caricamento in background
    const raf = (cb) => {
      try{
        const w = (typeof window !== "undefined") ? window : null;
        if (w && typeof w.requestAnimationFrame === "function") return w.requestAnimationFrame(cb);
      }catch(_){}
      return setTimeout(cb, 0);
    };

    raf(() => { try { renderServiziList(); } catch (_) {} });

    // fetch in background dopo il primo paint
    setTimeout(() => {
      try{
        const m = String(state.guestMode || "").toLowerCase();
        const target = (m === "view") ? state.guestViewItem : ({ id: state.guestEditId });
        Promise.resolve(loadServiziForOspite(target)).then(() => {
          raf(() => { try { renderServiziList(); } catch (_) {} });
        }).catch(() => {});
      }catch(_){}
    }, 0);
  };


  if (servicesInput){
    servicesInput.addEventListener("input", () => {
      if (String(state.guestMode || "").toLowerCase() === "edit") {
        state.guestServicesManualOverride = true;
      }
      try { updateGuestRemaining(); } catch (_) {}
    });
  }

  // + (aggiungi servizio): sempre in edit
  if (pillEdit) bindFastTap(pillEdit, openModal);
  // fallback open (iOS): assicura apertura anche se fastTap non intercetta
  if (pillEdit){
    try{
      pillEdit.addEventListener("click", (e)=>{ try{ e.preventDefault(); e.stopPropagation(); }catch(_){} openModal(); }, { passive:false });
      pillEdit.addEventListener("touchend", (e)=>{ try{ e.preventDefault(); e.stopPropagation(); }catch(_){} openModal(); }, { passive:false });
    }catch(_){}
  }

  if (closeBtn) bindFastTap(closeBtn, closeModal);
  if (btnCancel) bindFastTap(btnCancel, () => { clearModal(); });
  if (btnSave) bindFastTap(btnSave, async () => {
    if (!inName || !inAmt) return;
    const servizio = String(inName.value || "").trim();
    const importo = parseFloat(String(inAmt.value || "0").replace(",", ".")) || 0;
    if (!servizio) return toast("Inserisci il servizio");
    if (!isFinite(importo)) return toast("Importo non valido");

    serviziAddLocal({ servizio, descrizione: "", importo });
    // se non c'è override manuale, aggiorna input con la somma
    if (!state.guestServicesManualOverride){
      const sIn = document.getElementById("guestServices");
      if (sIn) sIn.value = String(state.guestServicesComputedTotal || 0);
    }
    try { updateGuestRemaining(); } catch (_) {}
    try { renderServiziList(); } catch (_) {}

    // persisti subito su sheet "servizi"
    await persistServiziForCurrentGuest();

    clearModal();
    closeModal();
  });

  if (modal) modal.addEventListener("click", (e)=>{ if (e.target === modal) closeModal(); });

  // "Servizi" (tasto): su iOS deve aprire con tap semplice, senza richiedere pressioni lunghe.
  // Evitiamo listener sovrapposti/anticipati che possono rendere il tap inaffidabile su Safari.
  if (pillView && pillView.dataset.servicesTapBound !== "1"){
    pillView.dataset.servicesTapBound = "1";
    let last = 0;
    const toggle = (e)=>{
      const now = Date.now();
      if (now - last < 300) return;
      last = now;
      try{ e && e.preventDefault && e.preventDefault(); }catch(_){}
      try{ e && e.stopPropagation && e.stopPropagation(); }catch(_){}
      toggleList();
    };
    try{ pillView.addEventListener("pointerup", toggle, { passive:false }); }catch(_){ }
    try{ pillView.addEventListener("touchend", toggle, { passive:false }); }catch(_){ }
    try{ pillView.addEventListener("click", toggle, { passive:false }); }catch(_){ }
  }

}


async function saveGuest(opts = {}){
  try{ __syncGuestNationalityFromPhone__(true); }catch(_){ }
  const name = (document.getElementById("guestName")?.value || "").trim();
  const telefono = (document.getElementById("guestPhone")?.value || "").trim();
  const nationalityOption = __getGuestNationalityOption__(document.getElementById("guestNationality")?.value || "");
  const email = (document.getElementById("guestEmail")?.value || "").trim();
  const menCount = parseInt(document.getElementById("guestMen")?.value || "0", 10) || 0;
  const womenCount = parseInt(document.getElementById("guestWomen")?.value || "0", 10) || 0;
  const bookingNumber = (document.getElementById("guestBookingNumber")?.value || "").trim();
  const adults = parseInt(document.getElementById("guestAdults")?.value || "0", 10) || 0;
  const kidsU10 = parseInt(document.getElementById("guestKidsU10")?.value || "0", 10) || 0;
  const checkIn = document.getElementById("guestCheckIn")?.value || "";
  const checkOut = document.getElementById("guestCheckOut")?.value || "";
  const total = parseFloat(document.getElementById("guestTotal")?.value || "0") || 0;
  const channelId = String(document.getElementById("guestChannel")?.value || "").trim();
  const channelItem = getChannelCatalogItemById(channelId);
  const channelCommissionPct = parseFloat(document.getElementById("guestChannelCommission")?.value || "0") || 0;
  const booking = parseFloat(document.getElementById("guestBooking")?.value || "0") || 0;
  const serviziTotale = parseFloat(document.getElementById("guestServices")?.value || "0") || 0;
  const discount = parseFloat(document.getElementById("guestDiscount")?.value || "0") || 0;
  const deposit = parseFloat(document.getElementById("guestDeposit")?.value || "0") || 0;
  const saldoPagato = parseFloat(document.getElementById("guestSaldo")?.value || "0") || 0;
  const notes = String(document.getElementById("guestNotes")?.value || "").trim();
  const guestTypeFromOpts = String(opts?.tipoScheda || opts?.guestType || '').trim().toLowerCase();
  const isPreventivoPayload = guestTypeFromOpts === 'preventivo' ? true : (guestTypeFromOpts === 'prenotazione' ? false : (state.guestMode === 'edit' ? __guestIsPreventivo__(state.guestEditSourceItem || {}) : false));
  const saldoTipo = (saldoPagato > 0) ? (state.guestSaldoType || "") : "";
  const rooms = Array.from(state.guestRooms || [])
    .map(n => parseInt(n,10))
    .filter(n => Number.isFinite(n) && n>=1 && n<=getConfiguredRoomsCount(6))
    .sort((a,b)=>a-b);
  const depositType = (deposit > 0) ? (state.guestDepositType || "") : "";
  const matrimonio = !!(state.guestMarriage);
  const g = !!(state.guestGroup);
if (!name) return toast("Inserisci il nome");
  if (!channelItem) return toast("Seleziona il channel");
  const payload = {
    nome: name,
    telefono: telefono,
    nazionalita: nationalityOption.code || "",
    nazione: nationalityOption.code || "",
    nazionalita_code: nationalityOption.code || "",
    nazionalita_nome: nationalityOption.name || "",
    country_code: nationalityOption.code || "",
    country_name: nationalityOption.name || "",
    country_flag: nationalityOption.flag || "",
    email: email,
    sesso: String(state.guestGender || "").trim(),
    gender: String(state.guestGender || "").trim(),
    genere: String(state.guestGender || "").trim(),
    uomini: menCount,
    uomini_count: menCount,
    uominiCount: menCount,
    maschi: menCount,
    men: menCount,
    men_count: menCount,
    menCount: menCount,
    donne: womenCount,
    donne_count: womenCount,
    donneCount: womenCount,
    femmine: womenCount,
    women: womenCount,
    women_count: womenCount,
    womenCount: womenCount,
    numero_prenotazione: bookingNumber,
    numeroPrenotazione: bookingNumber,
    prenotazione_numero: bookingNumber,
    booking_number: bookingNumber,
    reservation_number: bookingNumber,
    id_prenotazione: bookingNumber,
    adulti: adults,
    bambini_u10: kidsU10,
    check_in: checkIn,
    check_out: checkOut,
    importo_prenotazione: total,
    channel_id: channelItem ? String(channelItem.id) : "",
    channel_nome: channelItem ? String(channelItem.nome) : "",
    channel_colore: channelItem ? String(channelItem.colore) : "",
    channel_colore_testo: channelItem ? String(channelItem.coloreTesto || "") : "",
    channel_iniziale: channelItem ? String(channelItem.iniziale || __channelInitialFromName__(channelItem.nome)) : "",
    channel_commissione: Math.round(channelCommissionPct * 100) / 100,
    channel_commission_pct: Math.round(channelCommissionPct * 100) / 100,
    importo_booking: booking,
    servizi_totale: serviziTotale,
    servizi_preview: serviziPreviewText(state.guestServicesItems || []),
    sconto: discount,
    discount: discount,
    acconto_importo: deposit,
    acconto_tipo: depositType,
    saldo_pagato: saldoPagato,
    saldo_tipo: saldoTipo,
    acconto_ricevuta: !!(deposit > 0 && state.guestDepositReceipt),
    acconto_ricevutain: !!(deposit > 0 && state.guestDepositReceipt),
    saldo_ricevuta: !!(saldoPagato > 0 && state.guestSaldoReceipt),
    saldo_ricevutain: !!(saldoPagato > 0 && state.guestSaldoReceipt),
    note: notes,
    notes: notes,
    nota: notes,
    matrimonio,
    g: g ? "1" : "",
    col_c: (state.guestColC ? "1" : ""),
    c: (state.guestColC ? "1" : ""),
    ps_registrato: state.guestPSRegistered ? "1" : "",
    istat_registrato: state.guestISTATRegistered ? "1" : "",
    checkin_effettuato: state.guestCheckInDone ? "1" : "",
    check_in_effettuato: state.guestCheckInDone ? "1" : "",
    checkInEffettuato: state.guestCheckInDone ? "1" : "",
    checked_in: state.guestCheckInDone ? "1" : "",
    stanze: JSON.stringify(rooms)
  };
  __guestSetPreventivoFields__(payload, isPreventivoPayload);



  const isEdit = state.guestMode === "edit";
  if (isEdit){
    if (!state.guestEditId) return toast("ID ospite mancante");
    payload.id = state.guestEditId;
    const existingCardKey = String(state.guestEditCardKey || __guestCardKeyOf__(state.guestEditSourceItem || { id: state.guestEditId })).trim();
    if (existingCardKey) Object.assign(payload, __guestCardPayloadFields__(existingCardKey));
    // preserva la data di inserimento (non deve cambiare con le modifiche)
    const ca = state.guestEditCreatedAt;
    if (ca){
      payload.createdAt = ca;
      payload.created_at = ca;
    }
    try{ __applyGuestRegistrationExtensionOnPayload__(payload, state.guestEditSourceItem || null); }catch(_){ }
  }

  
  else {
    // CREATE: genera subito un ID stabile, così possiamo salvare le stanze al primo tentativo
    payload.id = payload.id || genId("o");
    const cardKey = String(state.guestCreateFromGroupCardKey || `card:${payload.id}`).trim();
    Object.assign(payload, __guestCardPayloadFields__(cardKey));
  }
// CREATE vs UPDATE (backend GAS: POST=create, PUT=update)
  const method = isEdit ? "PUT" : "POST";

  // Snapshot PRIMA di qualsiasi navigazione (showPage("ospiti") fa enterGuestCreateMode e può pulire lo stato)
  const ospiteId = payload.id;
  const stanzeSnap = buildArrayFromState();
  const snapOrig = isEdit ? (state.stanzeSnapshotOriginal || "") : "";

  let shouldSave = true;
  if (isEdit){
    try {
      const snapNow = JSON.stringify(stanzeSnap);
      shouldSave = (snapNow !== snapOrig);
    } catch (_) {
      shouldSave = true;
    }
  }

  const instantGoList = !!(opts && opts.instantGoList);
  if (instantGoList){
    // Naviga SUBITO alla guest list
    try { showPage("ospiti"); } catch (_) {}
  }

  const res = await api("ospiti", { method, body: payload });

  // stanze: backend gestisce POST e sovrascrive (deleteWhere + append)
  if (shouldSave){
    try { await api("stanze", { method:"POST", body: { ospite_id: ospiteId, stanze: stanzeSnap } }); } catch (_) {}
  }

  // servizi: salva elenco (se presente)
  try{
    if (Array.isArray(state.guestServicesItems) && state.guestServicesItems.length){
      await api("servizi", { method:"POST", body: { ospite_id: ospiteId, servizi: (state.guestServicesItems||[]).filter(s=>!(String(s?.isDeleted||s?.deleted||"" )==="1"||s?.isDeleted===true||s?.deleted===true)).map(s=>({ servizio:String(s.servizio??s.name??"").trim(), descrizione:String(s.descrizione??s.desc??"").trim(), importo: parseFloat(s.importo??s.amount??0)||0, qty: parseFloat(s.qty??1)||1 })) } });
    }
  }catch(_){ }

  // Invalida cache in-memory/locali (ospiti/stanze/statistiche) e forza refresh Calendario.
  // Le statistiche devono rispecchiare subito la scheda ospite appena salvata,
  // soprattutto quando una spunta ricevuta viene tolta.
  try{ invalidateApiCache("ospiti|"); }catch(_){ }
  try{ invalidateApiCache("stanze|"); }catch(_){ }
  try{ state._statsDataKey = ""; state.statGen = null; state.statMensili = null; state.statPrenotazioni = null; }catch(_){ }
  try{ if (Array.isArray(state.statsGuests)){
    const ix = state.statsGuests.findIndex(x => String(guestIdOf(x) || x?.id || "") === String(ospiteId || ""));
    if (ix >= 0) state.statsGuests[ix] = Object.assign({}, state.statsGuests[ix] || {}, payload);
  } }catch(_){ }
  try{ if (state.calendar){ state.calendar.ready = false; state.calendar.rangeKey = ""; state.calendar.winFrom = ""; state.calendar.winTo = ""; } state._roomsAvailKey = ""; }catch(_){ }

  if (instantGoList){
    // Sei già in lista: aggiorna subito lista e LED top bar senza bloccare la UI.
    try{
      loadOspiti({ ...(state.period || {}), force:true })
        .then(()=>{ try{ refreshTopGuestAlerts({ force:true, keepModal:true }); }catch(_){ } })
        .catch(e => toast(e.message));
    }catch(_){ }
    try{ setTimeout(()=>{ try{ refreshTopGuestAlerts({ force:true, keepModal:true }); }catch(_){ } }, 120); }catch(_){ }
    try{ __sfxSave(); }catch(_){ }
  toast(isEdit ? "Modifiche salvate" : "Ospite creato");
    return;
  }

  await loadOspiti({ ...(state.period || {}), force:true });
  try{ refreshTopGuestAlerts({ force:true, keepModal:true }); }catch(_){ }
  try{ __sfxSave(); }catch(_){ }
  toast(isEdit ? "Modifiche salvate" : "Ospite creato");

  // Dopo salvataggio: torna sempre alla lista ospiti
  try { enterGuestCreateMode(); } catch (_) {}
  showPage("ospiti");
}


function __normalizeGuestNameUppercaseValue__(value){
  try{
    return String(value ?? '').toLocaleUpperCase('it-IT');
  }catch(_){
    try{ return String(value ?? '').toUpperCase(); }catch(__){ return ''; }
  }
}

function __applyGuestNameUppercase__(el, opts = {}){
  try{
    if (!el) return '';
    const upper = __normalizeGuestNameUppercaseValue__(el.value);
    if (el.value !== upper){
      const start = typeof el.selectionStart === 'number' ? el.selectionStart : null;
      const end = typeof el.selectionEnd === 'number' ? el.selectionEnd : null;
      el.value = upper;
      if (opts && opts.preserveCaret && start !== null && end !== null){
        try{ el.setSelectionRange(start, end); }catch(_){ }
      }
    }
    return upper;
  }catch(_){ return ''; }
}

function __bindGuestNameUppercaseField__(){
  const el = document.getElementById('guestName');
  if (!el) return;
  try{ __applyGuestNameUppercase__(el); }catch(_){ }
  if (el.__guestUppercaseBound) return;
  el.__guestUppercaseBound = true;
  el.setAttribute('autocapitalize', 'characters');
  try{ el.style.textTransform = 'uppercase'; }catch(_){ }
  el.addEventListener('input', ()=>{ try{ __applyGuestNameUppercase__(el, { preserveCaret:true }); }catch(_){ } });
  el.addEventListener('change', ()=>{ try{ __applyGuestNameUppercase__(el); }catch(_){ } });
  el.addEventListener('blur', ()=>{ try{ __applyGuestNameUppercase__(el); }catch(_){ } });
  el.addEventListener('paste', ()=>{
    setTimeout(()=>{ try{ __applyGuestNameUppercase__(el, { preserveCaret:true }); }catch(_){ } }, 0);
  });
}


async function __copyGuestNameFromView__(){
  try{
    const card = document.querySelector('#page-ospite .guest-form-card');
    if (!card || !card.classList.contains('is-view')) return false;
    const el = document.getElementById('guestName');
    const item = state?.guestViewItem || {};
    const name = String(el?.value || item?.nome || item?.name || '').trim();
    if (!name) return false;
    const ciIso = formatISODateLocal(item?.check_in ?? item?.checkIn ?? item?.arrivo ?? item?.dataArrivo ?? item?.arrival ?? item?.guestCheckIn ?? '');
    const ciDate = ciIso ? new Date(ciIso + 'T00:00:00') : null;
    const checkInDay = (ciDate && !Number.isNaN(ciDate.getTime())) ? String(ciDate.getDate()).padStart(2, '0') : String(new Date().getDate()).padStart(2, '0');
    const copyText = `${checkInDay} - ${name}`;

    const cleanupSelection = () => {
      try{ if (el) el.blur(); }catch(_){ }
      try{
        const sel = window.getSelection && window.getSelection();
        if (sel && sel.removeAllRanges) sel.removeAllRanges();
      }catch(_){ }
    };

    const fallbackCopy = () => {
      let ta = null;
      try{
        ta = document.createElement('textarea');
        ta.value = copyText;
        ta.setAttribute('readonly','readonly');
        ta.setAttribute('aria-hidden','true');
        ta.style.position = 'fixed';
        ta.style.left = '0';
        ta.style.top = '0';
        ta.style.width = '1px';
        ta.style.height = '1px';
        ta.style.opacity = '0';
        ta.style.pointerEvents = 'none';
        ta.style.zIndex = '-1';
        document.body.appendChild(ta);
        ta.focus({ preventScroll:true });
        ta.select();
        try{ ta.setSelectionRange(0, ta.value.length); }catch(_){ }
        const ok = document.execCommand && document.execCommand('copy');
        return !!ok;
      }catch(_){ return false; }
      finally{ try{ if (ta && ta.parentNode) ta.parentNode.removeChild(ta); }catch(__){ } }
    };

    let copied = false;
    // Su iOS/PWA navigator.clipboard può esistere ma fallire: il fallback va sempre tentato.
    try{ copied = fallbackCopy(); }catch(_){ copied = false; }
    if (!copied){
      try{
        if (navigator.clipboard && navigator.clipboard.writeText){
          await navigator.clipboard.writeText(copyText);
          copied = true;
        }
      }catch(_){ copied = false; }
    }

    cleanupSelection();
    try{ toast(copied ? 'Nome copiato' : 'Copia non riuscita', copied ? 'green' : 'orange'); }catch(_){ }
    return !!copied;
  }catch(_){ try{ toast('Copia non riuscita', 'orange'); }catch(__){ } return false; }
}
function __bindGuestNameCopyInView__(){
  const el = document.getElementById('guestName');
  if (!el || el.__guestNameCopyBound) return;
  el.__guestNameCopyBound = true;
  const field = el.closest('.guest-name-field') || el.parentElement;
  const label = field ? field.querySelector('label[for="guestName"]') : null;

  const isView = () => {
    try{
      const card = document.querySelector('#page-ospite .guest-form-card');
      return !!(card && card.classList.contains('is-view'));
    }catch(_){ return false; }
  };
  const shouldHandle = (target) => {
    try{
      if (!isView()) return false;
      if (!target) return false;
      if (target === el || target === label) return true;
      return !!(field && field.contains(target) && !target.closest('button'));
    }catch(_){ return false; }
  };
  const blockSelection = () => {
    try{ el.blur(); }catch(_){ }
    try{
      const sel = window.getSelection && window.getSelection();
      if (sel && sel.removeAllRanges) sel.removeAllRanges();
    }catch(_){ }
  };
  const run = (ev) => {
    try{
      if (!shouldHandle(ev && ev.target)) return;
      ev.preventDefault();
      ev.stopPropagation();
      if (ev.stopImmediatePropagation) ev.stopImmediatePropagation();
      blockSelection();
      if (el.__guestNameCopyBusy && Date.now() - el.__guestNameCopyBusy < 350) return;
      el.__guestNameCopyBusy = Date.now();
      __copyGuestNameFromView__();
    }catch(_){ }
  };
  const guardOnly = (ev) => {
    try{
      if (!shouldHandle(ev && ev.target)) return;
      blockSelection();
    }catch(_){ }
  };

  try{ el.setAttribute('role','button'); }catch(_){ }
  try{ el.setAttribute('aria-label','Copia nome ospite'); }catch(_){ }

  ['pointerdown','touchstart','mousedown'].forEach((evt) => {
    try{ el.addEventListener(evt, guardOnly, { passive:true, capture:true }); }catch(_){ }
    try{ if (label) label.addEventListener(evt, guardOnly, { passive:true, capture:true }); }catch(_){ }
    try{ if (field) field.addEventListener(evt, guardOnly, { passive:true, capture:true }); }catch(_){ }
  });
  ['click','touchend'].forEach((evt) => {
    try{ el.addEventListener(evt, run, { passive:false, capture:true }); }catch(_){ }
    try{ if (label) label.addEventListener(evt, run, { passive:false, capture:true }); }catch(_){ }
    try{ if (field) field.addEventListener(evt, run, { passive:false, capture:true }); }catch(_){ }
  });
  ['selectstart','dragstart','contextmenu'].forEach((evt) => {
    try{ el.addEventListener(evt, (ev)=>{ if (isView()){ ev.preventDefault(); blockSelection(); } }, { passive:false, capture:true }); }catch(_){ }
    try{ if (label) label.addEventListener(evt, (ev)=>{ if (isView()){ ev.preventDefault(); blockSelection(); } }, { passive:false, capture:true }); }catch(_){ }
  });
  el.addEventListener('keydown', (ev) => {
    const key = ev && (ev.key || ev.code || '');
    if (key === 'Enter' || key === ' ' || key === 'Spacebar') run(ev);
  });
}

function setupOspite(){
  try{ bindGuestTapCounters(); }catch(_){ }
  try{ __bindGuestNameUppercaseField__(); }catch(_){ }
  try{ __bindGuestNameCopyInView__(); }catch(_){ }

  const hb = document.getElementById("hamburgerBtnOspite");
  if (hb) hb.addEventListener("click", () => { hideLauncher(); showPage("home"); });

  // HOME: ricevute mancanti (solo in HOME)
  const btnRec = document.getElementById("homeReceiptsTop");
  if (btnRec) bindFastTap(btnRec, () => { openReceiptDueModal(); });

  const recClose = document.getElementById("receiptDueClose");
  if (recClose) bindFastTap(recClose, () => closeReceiptDueModal());
  const recModal = document.getElementById("receiptDueModal");
  if (recModal) recModal.addEventListener("click", (e)=>{ if (e.target === recModal) closeReceiptDueModal(); });

  const guestLedLeft = document.getElementById("dbLedRead");
  if (guestLedLeft) bindFastTap(guestLedLeft, () => openGuestAlertModal('ps'));
  const guestLedIstat = document.getElementById("dbLedIstat");
  if (guestLedIstat) bindFastTap(guestLedIstat, () => openGuestAlertModal('istat'));
  const guestLedRight = document.getElementById("dbLedWrite");
  if (guestLedRight) bindFastTap(guestLedRight, () => openGuestAlertModal('payment'));
  const guestLedReceipt = document.getElementById("dbLedReceipt");
  if (guestLedReceipt) bindFastTap(guestLedReceipt, () => openGuestAlertModal('receipt'));
  const guestAlertClose = document.getElementById("guestAlertClose");
  if (guestAlertClose) bindFastTap(guestAlertClose, () => closeGuestAlertModal());
  const guestAlertModal = document.getElementById("guestAlertModal");
  if (guestAlertModal) guestAlertModal.addEventListener("click", (e)=>{ if (e.target === guestAlertModal) closeGuestAlertModal(); });

  // AMMINISTRATORE: popup dati + grafico
  const btnAdminInputsTop = document.getElementById("btnAdminInputsTop");
  if (btnAdminInputsTop) bindFastTap(btnAdminInputsTop, () => { openAdminInputsModal(); });
  const btnAdminChartTop = document.getElementById("btnAdminChartTop");
  if (btnAdminChartTop) bindFastTap(btnAdminChartTop, () => { openAdminChartModal(); });
  const adminInputsClose = document.getElementById("adminInputsClose");
  if (adminInputsClose) bindFastTap(adminInputsClose, () => closeAdminInputsModal());
  const adminChartClose = document.getElementById("adminChartClose");
  if (adminChartClose) bindFastTap(adminChartClose, () => closeAdminChartModal());
  const adminInputsModal = document.getElementById("adminInputsModal");
  if (adminInputsModal) adminInputsModal.addEventListener("click", (e)=>{ if (e.target === adminInputsModal) closeAdminInputsModal(); });
  const adminChartModal = document.getElementById("adminChartModal");
  if (adminChartModal) adminChartModal.addEventListener("click", (e)=>{ if (e.target === adminChartModal) closeAdminChartModal(); });

  // Azioni Scheda ospite (solo lettura): verde=indietro, giallo=modifica, rosso=elimina
  const hdActions = document.getElementById("ospiteHdActions");
  if (hdActions && !hdActions.__bound){
    hdActions.__bound = true;
    hdActions.addEventListener("click", async (e) => {
      const btn = e.target.closest("button");
      if (!btn || !hdActions.contains(btn) || btn.hidden) return;

      if (btn.hasAttribute("data-guest-checkin")){
        const item = __guestActiveBookingForAction__();
        const id = guestIdOf(item) || item?.id || '';
        if (!item || !id){
          try{ toast('Seleziona prima un gruppo stanze'); }catch(_){ }
          try{ __syncGuestCheckInButton__(); }catch(_){ }
          return;
        }
        if (__guestIsPreventivo__(item)){
          try{
            const payload = Object.assign({}, item, { id });
            __guestSetPreventivoFields__(payload, false);
            await api("ospiti", { method:"PUT", body: payload });
            try{
              [state.ospiti, state.guests, state.bookings, state.guestList, state.statsGuests].forEach((list)=>{
                if (!Array.isArray(list)) return;
                list.forEach((row)=>{ try{ if (String(guestIdOf(row) || row?.id || '').trim() === String(id)) __guestSetPreventivoFields__(row, false); }catch(_){ } });
              });
            }catch(_){ }
            __guestRefreshAccountingAfterPreventivoChange__();
            try{ await loadOspiti({ ...(state.period || {}), force:true }); }catch(_){ }
            try{ renderRoomsReadOnly(payload); renderGuestMulti({ mode:'view' }); }catch(_){ }
            try{ refreshTopGuestAlerts({ force:true, keepModal:true }); }catch(_){ }
            try{ renderGuestCards(); }catch(_){ }
            toast('Preventivo concretizzato');
          }catch(err){
            toast(err?.message || 'Errore concretizzazione preventivo');
          }
          return;
        }
        const next = !__guestCheckInDone__(item);
        state.guestCheckInDone = next;
        item.checkin_effettuato = next ? "1" : "";
        item.check_in_effettuato = next ? "1" : "";
        item.checkInEffettuato = next ? "1" : "";
        item.checked_in = next ? "1" : "";
        try{ __syncGuestCheckInButton__(); }catch(_){ }
        try{
          const patchCheckInState = (row) => {
            if (!row) return;
            row.checkin_effettuato = next ? "1" : "";
            row.check_in_effettuato = next ? "1" : "";
            row.checkInEffettuato = next ? "1" : "";
            row.checked_in = next ? "1" : "";
          };
          const gid = String(id || '').trim();
          try{
            const lists = [state.ospiti, state.guests, state.bookings, state.guestList];
            lists.forEach((list) => {
              if (!Array.isArray(list)) return;
              list.forEach((row) => {
                try{ if (String(guestIdOf(row) || row?.id || '').trim() === gid) patchCheckInState(row); }catch(_){ }
              });
            });
          }catch(_){ }
          try{
            if (Array.isArray(state.guestGroupBookings)){
              state.guestGroupBookings.forEach((row) => {
                try{ if (String(guestIdOf(row) || row?.id || '').trim() === gid) patchCheckInState(row); }catch(_){ }
              });
            }
          }catch(_){ }
          try{ refreshTopGuestAlerts({ force:true, keepModal:true }); }catch(_){ }
          try{ renderOspiti && renderOspiti(); }catch(_){ }
        }catch(_){ }
        try{
          const payload = Object.assign({}, item, {
            id,
            checkin_effettuato: next ? "1" : "",
            check_in_effettuato: next ? "1" : "",
            checkInEffettuato: next ? "1" : "",
            checked_in: next ? "1" : ""
          });
          await api("ospiti", { method:"PUT", body: payload });
          try{ invalidateApiCache("ospiti|"); }catch(_){ }
          try{ await loadOspiti({ ...(state.period || {}), force:true }); }catch(_){ }
          try{ renderRoomsReadOnly(state.guestViewItem || __guestBookingById__(state.guestViewPrimaryId) || null); renderGuestMulti({ mode:'view' }); }catch(_){ }
          try{ refreshTopGuestAlerts({ force:true, keepModal:true }); }catch(_){ }
          try{ renderOspiti && renderOspiti(); }catch(_){ }
          toast(next ? "Check-in effettuato" : "Check-in non effettuato");
        }catch(err){
          state.guestCheckInDone = !next;
          item.checkin_effettuato = !next ? "1" : "";
          item.check_in_effettuato = !next ? "1" : "";
          item.checkInEffettuato = !next ? "1" : "";
          item.checked_in = !next ? "1" : "";
          try{ __syncGuestCheckInButton__(); }catch(_){ }
          toast(err?.message || "Errore salvataggio check-in");
        }
        return;
      }

      // Indaco: aggiunge sempre un nuovo gruppo stanze dalla scheda ospite.
      if (btn.hasAttribute("data-guest-cal")){
        if (["edit", "view"].includes(String(state.guestMode || '').toLowerCase())) {
          await __guestAddBookingFromCurrentEdit__();
          return;
        }
        return;
      }

      if (btn.hasAttribute("data-guest-report")){
        __openGuestReportModal__(state.guestViewItem || null);
        return;
      }

      // Verde: torna sempre alla lista ospiti (anche in Nuovo/Modifica)
      if (btn.hasAttribute("data-guest-back")){
        // pulisci contesto multi
        try{ state.guestGroupBookings = null; state.guestGroupActiveId = null; state.guestGroupKey = null; clearGuestMulti(); }catch(_){ }
        showPage("ospiti");
        return;
      }

      const mode = state.guestMode;
      const item = state.guestViewItem;

      // Giallo: dalla sola lettura passa a modifica solo del gruppo stanze selezionato
      if (btn.hasAttribute("data-guest-edit")){
        const selected = __guestActiveBookingForAction__();
        if (!selected){
          try{ toast('Seleziona prima un gruppo stanze'); }catch(_){ }
          return;
        }
        enterGuestEditMode(selected);
        try { updateOspiteHdActions(); } catch (_) {}
        return;
      }

      // Rosso: elimina (solo in sola lettura o modifica)
      if (btn.hasAttribute("data-guest-del")){
        let gid = null;

        if (mode === "view"){
          if (!item) return;
          gid = String(state.guestGroupActiveId || '').trim() || guestIdOf(item) || item.id;
        } else if (mode === "edit"){
          gid = state.guestEditId || null;
        }

        if (!gid) return;

        // In sola lettura: se esistono più prenotazioni per lo stesso ospite, elimina TUTTI i gruppi contemporaneamente
        let idsToDelete = [String(gid)];
        try{
          if (mode === "view" && !String(state.guestGroupActiveId || '').trim()){
            // Nessun gruppo selezionato: elimina tutti i gruppi della scheda.
            if (Array.isArray(state.guestGroupBookings) && state.guestGroupBookings.length){
              const all = state.guestGroupBookings
                .map(b => String(guestIdOf(b) || b?.id || "").trim())
                .filter(Boolean);
              if (all.length && all.includes(String(gid))) idsToDelete = Array.from(new Set(all));
            }

            // 2) fallback: ricostruisci gruppo dal dataset corrente (utile se aperto dal calendario)
            if (!idsToDelete || idsToDelete.length <= 1){
              const itemsNow = Array.isArray(state.ospiti) && state.ospiti.length ? state.ospiti : (Array.isArray(state.guests) ? state.guests : []);
              const key = String(state.guestGroupKey || __guestCardKeyOf__(item)).trim();
              if (key){
                const groups = groupGuestsByName(itemsNow || []);
                const g = groups.find(x => String(x.key) === String(key));
                if (g && Array.isArray(g.bookings) && g.bookings.length){
                  const all2 = g.bookings.map(b => String(guestIdOf(b) || b?.id || "").trim()).filter(Boolean);
                  if (all2.length && all2.includes(String(gid))) idsToDelete = Array.from(new Set(all2));
                }
              }
            }
          }
        }catch(_){ idsToDelete = [String(gid)]; }

        const msg = (idsToDelete.length > 1)
          ? "Eliminare definitivamente questa prenotazione (tutti i gruppi)?"
          : "Eliminare definitivamente questo ospite?";
        if (!confirm(msg)) return;

        // ✅ dDAE_1.020: dopo cancellazione, vai SUBITO alla guest list (UX immediata su iOS)
        // 1) Navigazione istantanea + rimozione ottimistica dalla lista
        try{
          const idsSet = new Set((idsToDelete || []).map(x => String(x)));
          const keep = (o) => {
            const oid = String(guestIdOf(o) || o?.id || "").trim();
            return oid && !idsSet.has(oid);
          };
          if (Array.isArray(state.guests)) state.guests = state.guests.filter(keep);
          if (Array.isArray(state.ospiti)) state.ospiti = state.ospiti.filter(keep);
          try{
            const from = state?.period?.from || "";
            const to   = state?.period?.to   || "";
            if (from && to && Array.isArray(state.guests)){
              __lsSet(`ospiti|${from}|${to}`, state.guests);
            }
          }catch(_){ }
          try{ requestAnimationFrame(renderGuestCards); }catch(_){ try{ renderGuestCards(); }catch(_2){} }
        }catch(_){ }

        // pulisci contesto multi e reset form
        try{ state.guestGroupBookings = null; state.guestGroupActiveId = null; state.guestGroupKey = null; clearGuestMulti(); }catch(_){ }
        try{ enterGuestCreateMode(); }catch(_){ }

        // vai subito alla lista ospiti
        try{ showPage("ospiti"); }catch(_){ }

        // 2) Operazione reale in background (senza bloccare la UI)
        (async () => {
          try{
            for (const id of idsToDelete){
              await api("ospiti", { method:"DELETE", params:{ id }});
            }
            toast("Ospite eliminato");
            invalidateApiCache("ospiti|");
    try{ refreshTopGuestAlerts({ force:true }); }catch(_){ }
            invalidateApiCache("stanze|");
            try{ if (state.calendar){ state.calendar.ready = false; state.calendar.rangeKey = ""; state.calendar.winFrom = ""; state.calendar.winTo = ""; } state._roomsAvailKey = ""; }catch(_){ }

            // refresh lista (SWR) — non blocca la navigazione
            try{ loadOspiti({ ...(state.period || {}), force:true }).catch(()=>{}); }catch(_){ }
          }catch(err){
            toast(err?.message || "Errore");
            try{ loadOspiti({ ...(state.period || {}), force:true }).catch(()=>{}); }catch(_){ }
          }
        })();

        return;
      }
    });
}

  // Selezione/Eliminazione prenotazione (multi) in modifica
  const multi = document.getElementById("guestMulti");
  if (multi && !multi.__bound){
    multi.__bound = true;
    multi.addEventListener("click", async (e) => {
      if (String(state.guestMode || '') === 'view'){
        const block = e.target && e.target.closest ? e.target.closest('.guest-booking-block[data-booking-id]') : null;
        if (block && multi.contains(block)){
          e.preventDefault();
          e.stopPropagation();
          __toggleGuestGroupInfoForBooking__(block.getAttribute('data-booking-id') || '');
          return;
        }
      }
      // Elimina singola prenotazione (solo il gruppo selezionato)
      const delBtn = e.target.closest("button[data-guest-del-booking]");
      if (delBtn && multi.contains(delBtn)){
        e.preventDefault();
        e.stopPropagation();
        const delId = String(delBtn.getAttribute("data-guest-del-booking") || "").trim();
        if (!delId) return;
        if (!confirm("Eliminare questa prenotazione?")) return;

        try{
          await api("ospiti", { method:"DELETE", params:{ id: delId }});
          toast("Prenotazione eliminata");
          invalidateApiCache("ospiti|");
    try{ refreshTopGuestAlerts({ force:true }); }catch(_){ }
          invalidateApiCache("stanze|");
          try{ if (state.calendar){ state.calendar.ready = false; state.calendar.rangeKey = ""; state.calendar.winFrom = ""; state.calendar.winTo = ""; } state._roomsAvailKey = ""; }catch(_){ }

          // ricarica dati e ripristina contesto multi
          await loadOspiti({ ...(state.period || {}), force:true });

          const items = Array.isArray(state.ospiti) && state.ospiti.length ? state.ospiti : (Array.isArray(state.guests) ? state.guests : []);
          const groups = groupGuestsByName(items || []);

          const keyWanted = String(state.guestGroupKey || "").trim();
          let group = keyWanted ? groups.find(g => String(g.key) === keyWanted) : null;
          if (!group){
            // fallback: prova con la card corrente oppure, se manca, col record in modifica
            const currentCardKey = String(state.guestEditCardKey || "").trim();
            if (currentCardKey) group = groups.find(g => String(g.key) === currentCardKey);
            if (!group && state.guestEditId) group = groups.find(g => (g.bookings || []).some(b => String(guestIdOf(b) || b?.id || "").trim() === String(state.guestEditId)));
          }

          if (group && Array.isArray(group.bookings) && group.bookings.length){
            state.guestGroupBookings = group.bookings;
            state.guestGroupKey = group.key;

            // Se abbiamo eliminato quella in modifica, passa alla prima disponibile
            const next = group.bookings.find(b => String(guestIdOf(b)) !== delId) || group.bookings[0];
            state.guestGroupActiveId = guestIdOf(next);
            enterGuestEditMode(next);
            showPage("ospite");
          } else {
            // non esiste piu' alcuna prenotazione per quel nome
            state.guestGroupBookings = null;
            state.guestGroupKey = null;
            state.guestGroupActiveId = null;
            try{ clearGuestMulti(); }catch(_){ }
            showPage("ospiti");
          }
        }catch(err){
          toast(err?.message || "Errore");
        }
        return;
      }

      // Aggiungi nuova prenotazione allo stesso ospite (tasto +)
      const addBtn = e.target.closest("button[data-guest-add-booking]");
      if (addBtn && multi.contains(addBtn)) {
        e.preventDefault();
        e.stopPropagation();
        await __guestAddBookingFromCurrentEdit__();
        return;
      }

      // Seleziona prenotazione da modificare
      const btn = e.target.closest("button[data-guest-select]");
      if (!btn || !multi.contains(btn)) return;
      const id = String(btn.getAttribute("data-guest-select") || "").trim();
      if (!id) return;

      const list = Array.isArray(state.guestGroupBookings) ? state.guestGroupBookings : [];
      const target = list.find(x => String(guestIdOf(x)) === id);
      if (!target) return;

      state.guestGroupActiveId = id;
      enterGuestEditMode(target);
      // enterGuestEditMode renderizza gia' la lista in modalita' edit
    });
  }

  const notesToggle = document.getElementById("guestNotesToggle");
  if (notesToggle && !notesToggle.__bound){
    notesToggle.__bound = true;
    notesToggle.addEventListener("click", (e) => {
      try{ e.preventDefault(); }catch(_){ }
      const next = !document.getElementById("guestNotesWrap")?.hidden;
      setGuestNotesExpanded(!next);
    });
  }
  const notesField = document.getElementById("guestNotes");
  if (notesField && !notesField.__bound){
    notesField.__bound = true;
    notesField.addEventListener("input", () => { updateGuestNotesIndicator(); });
  }

  try{ ensureRoomsPickerButtons(); }catch(_){ }
  const roomsWrap = document.getElementById("roomsPicker");

  // Toggle M/G (ora accanto a Importo prenotazione)
  try{
    const mBtn = document.getElementById("roomMarriage");
    if (mBtn) mBtn.addEventListener("click", (ev)=>{ try{ ev.preventDefault(); }catch(_){} try{ ev.stopPropagation(); }catch(_){} setMarriage(!state.guestMarriage); });
    const gBtn = document.getElementById("roomGroup");
    if (gBtn) gBtn.addEventListener("click", (ev)=>{ try{ ev.preventDefault(); }catch(_){} try{ ev.stopPropagation(); }catch(_){} setGroup(!state.guestGroup); });
    const cBtn = document.getElementById("roomColC");
    if (cBtn) cBtn.addEventListener("click", (ev)=>{ try{ ev.preventDefault(); }catch(_){} try{ ev.stopPropagation(); }catch(_){} setColC(!state.guestColC); });
  }catch(_){}

  const roomsOut = null; // removed UI string output

  function _getGuestDateRange(){
    try{
      const ci = (document.getElementById("guestCheckIn")?.value || "").trim();
      const co = (document.getElementById("guestCheckOut")?.value || "").trim();
      if (!ci || !co) return null;
      // Date ISO YYYY-MM-DD: confronto lessicografico ok
      if (co <= ci) return null;
      return { ci, co };
    }catch(_){ return null; }
  }

  async function refreshRoomsAvailability(){
    // Regola: nessuna stanza selezionabile senza intervallo date valido
    const range = _getGuestDateRange();

    const editId = String(state.guestEditId || "").trim();

    // reset/lock
    if (!range){
      state.occupiedRooms = new Set();
      state._roomsAvailKey = "";
      // se l'utente non ha ancora inserito date, non deve poter selezionare stanze
      if (state.guestRooms && state.guestRooms.size){
        state.guestRooms.clear();
        if (state.lettiPerStanza) state.lettiPerStanza = {};
      }
      renderRooms();
      return;
    }

    const key = `${range.ci}|${range.co}|${editId}|guest`;
    if (state._roomsAvailKey === key && state.occupiedRooms instanceof Set) {
      renderRooms();
      return;
    }
    state._roomsAvailKey = key;

    let rows = [];
    try{
      // Disponibilita' stanze: usa sempre la stessa finestra e lo stesso dataset del Calendario.
      // Evita la lettura generica di "ospiti" per non divergere dal calendario visibile.
      rows = await __loadCalendarGuestsForAvailability__(range, { force:true });
    }catch(_){
      rows = (state?.calendar && Array.isArray(state.calendar.guests)) ? state.calendar.guests : [];
    }

    let occ = __calendarOccupiedRoomsForRange__(rows, range, editId);
    try{
      state.occupiedRooms = new Set([...(occ || new Set())]);
    }catch(_){ }

    // Se l'utente aveva selezionato stanze che ora risultano occupate, le togliamo
    let removed = false;
    try{
      for (const r of Array.from(state.guestRooms || [])){
        if (occ.has(r)){
          state.guestRooms.delete(r);
          if (state.lettiPerStanza) delete state.lettiPerStanza[String(r)];
          removed = true;
        }
      }
    }catch(_){}

    if (removed){
      try{ toast("Alcune stanze non sono disponibili"); }catch(_){}
    }

    renderRooms();
  }

  function renderRooms(){
    const range = _getGuestDateRange();
    const locked = !range;
    const occSet = (state.occupiedRooms instanceof Set) ? state.occupiedRooms : new Set();
    const occRealSet = (state.occupiedRoomsReal instanceof Set) ? state.occupiedRoomsReal : new Set();

    roomsWrap?.querySelectorAll(".room-dot").forEach(btn => {
      // Il pallino "M" non è una stanza numerata
      if (btn.id === "roomMarriage" || btn.id === "roomGroup") return;

      const n = parseInt(btn.getAttribute("data-room"), 10);
      const on = state.guestRooms.has(n);
      const occ = !locked && occSet.has(n);
      const occReal = !locked && occRealSet.has(n);

      btn.classList.toggle("selected", on);
      btn.classList.toggle("occupied", occ);
      btn.classList.toggle("occupied-real", occReal);

      const dis = locked || occReal;
      btn.disabled = !!dis;
      btn.setAttribute("aria-disabled", dis ? "true" : "false");
      btn.setAttribute("aria-pressed", on ? "true" : "false");
    });

    // matrimonio dot (rimane gestibile come flag)
    setMarriage(state.guestMarriage);
    setGroup(state.guestGroup);
  }

  // Espone le funzioni (scope setupOspite) per poterle richiamare da enterGuestEditMode
  // senza dipendere dall'evento input/change dei campi date (iOS/Safari PWA).
  try {
    window.__ddae_refreshRoomsAvailability = refreshRoomsAvailability;
    window.__ddae_renderRooms = renderRooms;
  } catch (_) {}

  // iOS/PWA: a volte, nei contenitori orizzontali, il target del tap puo' "slittare" sul pallino precedente.
  // Per evitare che una selezione multipla aggiunga la stanza sbagliata, scegliamo SEMPRE il pallino piu' vicino
  // alle coordinate reali del tap.
  function __pickRoomDotFromEvent(e){
    try{
      if (!roomsWrap) return null;
      // 1) coordinate (touch/pointer/mouse)
      let x = null, y = null;
      const te = e;
      if (te && te.changedTouches && te.changedTouches[0]){ x = te.changedTouches[0].clientX; y = te.changedTouches[0].clientY; }
      else if (te && te.touches && te.touches[0]){ x = te.touches[0].clientX; y = te.touches[0].clientY; }
      else if (typeof te.clientX === 'number'){ x = te.clientX; y = te.clientY; }

      // 2) fallback per tastiera: usa il closest normale
      if (x == null || y == null){
        const b0 = te?.target?.closest ? te.target.closest('.room-dot') : null;
        return b0 && roomsWrap.contains(b0) ? b0 : null;
      }

      // 3) trova il bottone piu' vicino al punto del tap
      const dots = Array.from(roomsWrap.querySelectorAll('.room-dot'));
      let best = null;
      let bestD = Infinity;
      for (const d of dots){
        if (!d) continue;
        const r = d.getBoundingClientRect();
        const cx = r.left + (r.width/2);
        const cy = r.top + (r.height/2);
        const dx = cx - x;
        const dy = cy - y;
        const dist = (dx*dx) + (dy*dy);
        if (dist < bestD){ bestD = dist; best = d; }
      }

      // se il tap e' molto lontano dai pallini, ignora
      if (!best) return null;
      return best;
    }catch(_){ return null; }
  }

  // Stanze:
  // - tap breve su stanza spenta => seleziona + apre popup letti
  // - tap breve su stanza accesa => apre popup letti (cambio tipologia)
  // - pressione lunga (>=0.5s) su stanza accesa => deseleziona (SENZA popup)
  let __roomPressTimer = null;
  let __roomPressBtn = null;
  let __roomLongFired = false;
  let __roomSuppressClickUntil = 0;

  function __room_now(){
    try{ return (typeof performance !== 'undefined' && performance.now) ? performance.now() : Date.now(); }
    catch(_){ return Date.now(); }
  }
  function __room_markSuppress(ms){
    try{ __roomSuppressClickUntil = __room_now() + (ms || 650); }catch(_){ }
  }
  function __room_isSuppressed(){
    try{ return __room_now() < __roomSuppressClickUntil; }catch(_){ return false; }
  }
  function __room_clearPress(){
    try{ if (__roomPressTimer){ clearTimeout(__roomPressTimer); } }catch(_){ }
    __roomPressTimer = null;
    __roomPressBtn = null;
    __roomLongFired = false;
  }

  function __room_getDotFromEvent(e){
    try{
      const b = __pickRoomDotFromEvent(e);
      if (!b) return null;
      if (!roomsWrap || !roomsWrap.contains(b)) return null;
      if (!b.classList || !b.classList.contains('room-dot')) return null;
      return b;
    }catch(_){ return null; }
  }

  function __room_canInteract(b){
    const range = _getGuestDateRange();
    if (!range){
      try{ toast('Seleziona prima check-in e check-out'); }catch(_){ }
      return false;
    }
    if (b.classList.contains('occupied-real') || b.disabled){
      try{ toast('Stanza occupata'); }catch(_){ }
      return false;
    }
    return true;
  }

  function __room_handleShortTap(b){
    // Matrimonio: flag separato
    if (b.id === 'roomMarriage') { setMarriage(!state.guestMarriage); return; }
    if (!__room_canInteract(b)) return;

    const n = parseInt(b.getAttribute('data-room'), 10);
    if (!isFinite(n)) return;

    // Se era spenta, accendi
    if (!state.guestRooms.has(n)) {
      state.guestRooms.add(n);
      renderRooms();
    }

    // Tap breve su stanza accesa/spenta => apre popup configurazione letti
    try{ openRoomConfig(n); }catch(_){ }
  }

  function __room_handleLongPress(b){
    // Matrimonio: nessun long-press
    if (b.id === 'roomMarriage' || b.id === 'roomGroup') return;
    if (!__room_canInteract(b)) return;

    const n = parseInt(b.getAttribute('data-room'), 10);
    if (!isFinite(n)) return;

    // Deselezione SOLO con pressione lunga e SOLO se era accesa
    if (state.guestRooms.has(n)) {
      state.guestRooms.delete(n);
      if (state.lettiPerStanza) delete state.lettiPerStanza[String(n)];
      renderRooms();

      // Se il popup era aperto su questa stanza, chiudilo
      try{
        if (typeof __rc_room !== 'undefined' && String(__rc_room) === String(n)){
          const m = document.getElementById('roomConfigModal');
          if (m) m.hidden = true;
          try{ __rc_room = null; }catch(_){ }
        }
      }catch(_){ }
    }
  }

  function __room_onPressStart(e){
    const b = __room_getDotFromEvent(e);
    if (!b) return;

    // Evita click "fantasma" dopo touch/pointer
    __room_markSuppress(900);

    // Evita callout/scroll strani su iOS durante pressione lunga
    try{ e.preventDefault(); }catch(_){ }

    __room_clearPress();
    __roomPressBtn = b;

    __roomPressTimer = setTimeout(() => {
      __roomLongFired = true;
      __room_handleLongPress(b);
    }, 500);
  }

  function __room_onPressEnd(e){
    if (!__roomPressBtn) return;
    const b = __roomPressBtn;
    const wasLong = __roomLongFired;

    __room_clearPress();

    // Sopprimi click generato da touchend
    __room_markSuppress(900);
    try{ e.preventDefault(); }catch(_){ }

    if (wasLong) return;
    __room_handleShortTap(b);
  }

  function __room_onPressCancel(_e){
    __room_clearPress();
  }

  // Pointer events (preferiti) + fallback touch/mouse
  try{
    if (window.PointerEvent) {
      roomsWrap?.addEventListener('pointerdown', __room_onPressStart, { passive:false });
      roomsWrap?.addEventListener('pointerup', __room_onPressEnd, { passive:false });
      roomsWrap?.addEventListener('pointercancel', __room_onPressCancel, { passive:true });
    } else {
      roomsWrap?.addEventListener('touchstart', __room_onPressStart, { passive:false });
      roomsWrap?.addEventListener('touchend', __room_onPressEnd, { passive:false });
      roomsWrap?.addEventListener('touchcancel', __room_onPressCancel, { passive:true });
      roomsWrap?.addEventListener('mousedown', __room_onPressStart, { passive:false });
      roomsWrap?.addEventListener('mouseup', __room_onPressEnd, { passive:false });
    }
  }catch(_){ }

  // Click (tastiera/desktop). Ignorato se appena gestito da touch/pointer.
  roomsWrap?.addEventListener('click', (e) => {
    if (__room_isSuppressed()) return;
    const b = __room_getDotFromEvent(e);
    if (!b) return;
    __room_handleShortTap(b);
  });

  function bindPayPill(containerId, kind){
    const wrap = document.getElementById(containerId);
    if (!wrap) return;
    wrap.addEventListener("click", (e) => {
      const btn = e.target.closest(".pay-dot");
      if (!btn || !wrap.contains(btn)) return;

      const t = btn.getAttribute("data-type");
      if (t) {
        if (__guestPaymentAmountForKind__(kind) <= 0){
          if (kind === "deposit") state.guestDepositType = "";
          if (kind === "saldo") state.guestSaldoType = "";
          setPayType(containerId, "");
          return;
        }
        if (kind === "deposit") state.guestDepositType = t;
        if (kind === "saldo") state.guestSaldoType = t;
        setPayType(containerId, t);
        return;
      }

      if (btn.hasAttribute("data-receipt")) {
        if (__guestPaymentAmountForKind__(kind) <= 0){
          if (kind === "deposit") state.guestDepositReceipt = false;
          if (kind === "saldo") state.guestSaldoReceipt = false;
          setPayReceipt(containerId, false);
          return;
        }
        if (kind === "deposit") state.guestDepositReceipt = !state.guestDepositReceipt;
        if (kind === "saldo") state.guestSaldoReceipt = !state.guestSaldoReceipt;
        setPayReceipt(containerId, kind === "deposit" ? state.guestDepositReceipt : state.guestSaldoReceipt);
        return;
      }
    });
  }

  bindPayPill("depositType", "deposit");
  bindPayPill("saldoType", "saldo");
  try{
    document.getElementById("guestDeposit")?.addEventListener("input", __syncPaymentLedsWithAmounts__);
    document.getElementById("guestSaldo")?.addEventListener("input", __syncPaymentLedsWithAmounts__);
  }catch(_){ }



  function bindRegPill(containerId){
    const wrap = document.getElementById(containerId);
    if (!wrap) return;
    wrap.addEventListener("click", (e) => {
      const btn = e.target.closest('.pay-dot[data-flag]');
      if (!btn || !wrap.contains(btn)) return;

      const flag = (btn.getAttribute("data-flag") || "").toLowerCase();
      if (flag === "ps") state.guestPSRegistered = !state.guestPSRegistered;
      if (flag === "istat") state.guestISTATRegistered = !state.guestISTATRegistered;

      setRegFlags(containerId, state.guestPSRegistered, state.guestISTATRegistered);
    });
  }

  bindRegPill("regTags");

  const guestChannelSel = document.getElementById("guestChannel");
  if (guestChannelSel && !guestChannelSel.__boundChannel){
    guestChannelSel.__boundChannel = true;
    guestChannelSel.addEventListener("change", () => { try{ applySelectedChannelToGuestForm(guestChannelSel.value); }catch(_){ } });
  }
  try{ populateGuestChannelOptions(); }catch(_){ }

  // Rimanenza da pagare (Importo prenotazione + Servizi - Sconto - Acconto - Saldo)
  ["guestTotal","guestServices","guestDiscount","guestDeposit","guestSaldo"].forEach((id) => {
    const el = document.getElementById(id);
    if (!el) return;
    el.addEventListener("input", () => {
      try { if (id === "guestTotal") recalcGuestCommission(); } catch (_) {}
      try { updateGuestRemaining(); } catch (_) {}
    });
    el.addEventListener("change", () => {
      try { if (id === "guestTotal") recalcGuestCommission(); } catch (_) {}
      try { updateGuestRemaining(); } catch (_) {}
    });
  });
  try { recalcGuestCommission(); } catch (_) {}
  try { updateGuestRemaining(); } catch (_) {}


  function syncGuestCheckoutPickerMonth(opts = {}){
    try{
      const ci = document.getElementById("guestCheckIn");
      const co = document.getElementById("guestCheckOut");
      if (!ci || !co) return;
      const checkInVal = String(ci.value || "").trim();
      const checkOutVal = String(co.value || "").trim();
      if (checkInVal){
        co.min = checkInVal;
      }else{
        co.removeAttribute("min");
      }
      if (!checkInVal) return;
      const forceMonth = !!(opts && opts.forceMonth);
      if (!checkOutVal || checkOutVal < checkInVal || forceMonth){
        co.value = checkInVal;
      }
    }catch(_){ }
  }

  // ✅ Stanze: blocca selezione finché non c'è un intervallo date valido + segna stanze occupate (rosso)
  ["guestCheckIn","guestCheckOut"].forEach((id) => {
    const el = document.getElementById(id);
    if (!el) return;
    el.addEventListener("input", () => {
      try{ if (id === "guestCheckIn") syncGuestCheckoutPickerMonth(); }catch(_){ }
      try { refreshRoomsAvailability(); } catch (_) {}
      try { __updateGuestDateRangeTrigger__(); } catch (_) {}
      try { __updateGuestDateRangeTrigger__(); } catch (_) {}
    });
    el.addEventListener("change", () => {
      try{ if (id === "guestCheckIn") syncGuestCheckoutPickerMonth(); }catch(_){ }
      try { refreshRoomsAvailability(); } catch (_) {}
      try { __updateGuestDateRangeTrigger__(); } catch (_) {}
    });
  });
  const guestCheckOutEl = document.getElementById("guestCheckOut");
  if (guestCheckOutEl && !guestCheckOutEl.__boundSyncMonth){
    guestCheckOutEl.__boundSyncMonth = true;
    const syncMonthBeforeOpen = () => { try{ syncGuestCheckoutPickerMonth({ forceMonth:true }); }catch(_){ } };
    guestCheckOutEl.addEventListener("focus", syncMonthBeforeOpen);
    guestCheckOutEl.addEventListener("pointerdown", syncMonthBeforeOpen);
    guestCheckOutEl.addEventListener("touchstart", syncMonthBeforeOpen, { passive:true });
    guestCheckOutEl.addEventListener("click", syncMonthBeforeOpen);
  }
  try { syncGuestCheckoutPickerMonth(); } catch (_) {}
  try { refreshRoomsAvailability(); } catch (_) {}
  try { __updateGuestDateRangeTrigger__(); } catch (_) {}
  try{ __dateRangeCalendarApplyTheme__(); }catch(_){ }

  const guestDateRangeTrigger = document.getElementById("guestDateRangeTrigger");
  if (guestDateRangeTrigger && !guestDateRangeTrigger.__boundRangeModal){
    guestDateRangeTrigger.__boundRangeModal = true;
    bindFastTap(guestDateRangeTrigger, () => { try{ __openGuestDateRangeModal__(); }catch(_){ } });
  }
  const guestDateRangeModal = document.getElementById("guestDateRangeModal");
  if (guestDateRangeModal && !guestDateRangeModal.__boundRangeModal){
    guestDateRangeModal.__boundRangeModal = true;
    const closeRangeModal = () => { try{ __closeGuestDateRangeModal__(true); }catch(_){ } };
    ["guestDateRangeModalClose","guestDateRangeCancel"].forEach((id) => { const el = document.getElementById(id); if (el) bindFastTap(el, closeRangeModal); });
    const applyBtn = document.getElementById("guestDateRangeApply");
    if (applyBtn) bindFastTap(applyBtn, () => { try{ __applyGuestDateRangeDraft__(); }catch(_){ } });
    const prevBtn = document.getElementById("guestDateRangePrev");
    if (prevBtn) bindFastTap(prevBtn, () => { try{ const m = __guestDateRangeState__.month || __guestDateRangeMonthStart__(); __guestDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()-1, 1); __renderGuestDateRangeCalendar__(); }catch(_){ } });
    const nextBtn = document.getElementById("guestDateRangeNext");
    if (nextBtn) bindFastTap(nextBtn, () => { try{ const m = __guestDateRangeState__.month || __guestDateRangeMonthStart__(); __guestDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()+1, 1); __renderGuestDateRangeCalendar__(); }catch(_){ } });
    guestDateRangeModal.addEventListener("click", (ev) => { try{ if (ev.target === guestDateRangeModal) closeRangeModal(); }catch(_){ } });
    const grid = document.getElementById("guestDateRangeGrid");
    try{ __bindDateRangeCalendarHold__(grid); }catch(_){ }
    grid?.addEventListener("click", (ev) => {
      const btn = ev.target.closest?.('.guest-date-range-day[data-date]');
      if (!btn) return;
      try{ __handleGuestDateRangePick__(btn.getAttribute('data-date') || ''); }catch(_){ }
    });
  }

  try{ __translateTree__(document.getElementById("guestCreateChoiceRow")); }catch(_){}

  const btnBookingCreate = document.getElementById("createGuestBookingBtn");
  btnBookingCreate?.addEventListener("click", () => {
    try{
      Promise.resolve(saveGuest({ instantGoList: true, tipoScheda: "prenotazione" }))
        .catch((e) => { try { toast(e?.message || "Errore"); } catch (_) {} });
    }catch(e){ try { toast(e?.message || "Errore"); } catch (_) {} }
  });
  const btnEstimateCreate = document.getElementById("createGuestEstimateBtn");
  btnEstimateCreate?.addEventListener("click", () => {
    try{
      Promise.resolve(saveGuest({ instantGoList: true, tipoScheda: "preventivo" }))
        .catch((e) => { try { toast(e?.message || "Errore"); } catch (_) {} });
    }catch(e){ try { toast(e?.message || "Errore"); } catch (_) {} }
  });

  const btnCreate = document.getElementById("createGuestCard");
  btnCreate?.addEventListener("click", () => {
    try {
      // Richiesta: al tap su Salva/Crea, vai SUBITO alla guest list.
      // saveGuest gestisce la navigazione immediata e prosegue il salvataggio in background.
      Promise.resolve(saveGuest({ instantGoList: true }))
        .catch((e) => { try { toast(e?.message || "Errore"); } catch (_) {} });
    } catch (e) {
      try { toast(e?.message || "Errore"); } catch (_) {}
    }
  });

  // Default: check-in oggi (solo UI)
  const today = new Date();
  const iso = today.toISOString().slice(0,10);
  const ci = document.getElementById("guestCheckIn");
  if (ci && !ci.value) ci.value = iso;

  renderRooms();
  renderGuestCards();
}


function euro(n){
  try { return (Number(n)||0).toLocaleString(__getCurrentLocale__(), { style:"currency", currency:"EUR" }); }
  catch { return (Number(n)||0).toFixed(2) + " €"; }
}

function __guestCardKeyOf__(item, opts = {}){
  const preferOwnId = !!(opts && opts.preferOwnId === true);
  const stored = !preferOwnId
    ? String(
        item?.guest_card_id ??
        item?.guestCardId ??
        item?.card_uid ??
        item?.cardUid ??
        item?.card_id ??
        item?.cardId ??
        item?.scheda_ospite_id ??
        item?.schedaOspiteId ??
        ""
      ).trim()
    : "";
  if (stored) return stored;
  const rawName = collapseSpaces(String(item?.nome ?? item?.name ?? "").trim()) || "Ospite";
  const normalizedName = normalizeGuestNameKey(rawName);
  if (normalizedName) return `card:name:${normalizedName}`;
  const ownId = String(guestIdOf(item) || item?.id || item?.ID || "").trim();
  if (ownId) return `card:${ownId}`;
  return `card:fallback:${Math.random().toString(16).slice(2)}`;
}

function __guestCardPayloadFields__(cardKey){
  const value = String(cardKey || "").trim();
  if (!value) return {};
  return {
    guest_card_id: value,
    guestCardId: value,
    card_uid: value,
    cardUid: value,
    card_id: value,
    cardId: value,
    scheda_ospite_id: value,
    schedaOspiteId: value
  };
}


function __guestIsPreventivo__(item){
  try{
    const values = [
      item?.tipo_scheda, item?.tipoScheda, item?.scheda_tipo, item?.schedaTipo,
      item?.stato_scheda, item?.statoScheda, item?.booking_status, item?.bookingStatus,
      item?.preventivo, item?.is_preventivo, item?.isPreventivo, item?.quote, item?.is_quote
    ];
    return values.some((v) => {
      const raw = String(v ?? '').trim().toLowerCase();
      return raw === 'preventivo' || raw === 'quote' || raw === 'stima' || raw === '1' || raw === 'true' || raw === 'yes';
    });
  }catch(_){ return false; }
}

function __guestSetPreventivoFields__(payload, isPreventivo){
  try{
    const on = !!isPreventivo;
    payload.tipo_scheda = on ? 'preventivo' : 'prenotazione';
    payload.tipoScheda = payload.tipo_scheda;
    payload.scheda_tipo = payload.tipo_scheda;
    payload.schedaTipo = payload.tipo_scheda;
    payload.preventivo = on ? '1' : '';
    payload.is_preventivo = on ? '1' : '';
    payload.isPreventivo = on ? '1' : '';
    payload.quote = on ? '1' : '';
    payload.is_quote = on ? '1' : '';
    payload.escludi_statistiche = on ? '1' : '';
    payload.escludiStatistiche = on ? '1' : '';
    payload.exclude_stats = on ? '1' : '';
    payload.exclude_calendar = on ? '1' : '';
  }catch(_){ }
  return payload;
}

function __guestFilterPreventiviRows__(rows, includePreventivi){
  try{
    const src = Array.isArray(rows) ? rows : [];
    return src.filter((row)=> includePreventivi ? __guestIsPreventivo__(row) : !__guestIsPreventivo__(row));
  }catch(_){ return Array.isArray(rows) ? rows : []; }
}

function __guestRefreshAccountingAfterPreventivoChange__(){
  try{ invalidateApiCache('ospiti|'); }catch(_){ }
  try{ invalidateApiCache('stanze|'); }catch(_){ }
  try{ state._statsDataKey = ''; state.statGen = null; state.statMensili = null; state.statPrenotazioni = null; }catch(_){ }
  try{ if (state.calendar){ state.calendar.ready = false; state.calendar.rangeKey = ''; state.calendar.winFrom = ''; state.calendar.winTo = ''; } state._roomsAvailKey = ''; }catch(_){ }
}

function groupGuestsByName(items){
  const map = new Map();
  for (const it of (items || [])){
    const rawName = it?.nome ?? it?.name ?? "";
    const display = collapseSpaces(String(rawName || "").trim()) || "Ospite";
    const key = __guestCardKeyOf__(it);
    let g = map.get(key);
    if (!g){
      g = { key, nome: display, bookings: [] };
      map.set(key, g);
    }
    g.bookings.push(it);
  }

  const groups = Array.from(map.values());
  for (const g of groups){
    // Ordina le prenotazioni nello stesso gruppo: per arrivo, poi per inserimento
    g.bookings = g.bookings.slice().sort((a,b) => {
      const ta = parseDateTs(a?.check_in ?? a?.checkIn);
      const tb = parseDateTs(b?.check_in ?? b?.checkIn);
      if (ta == null && tb == null){
        return (Number(a?._insNo) || 1e18) - (Number(b?._insNo) || 1e18);
      }
      if (ta == null) return 1;
      if (tb == null) return -1;
      if (ta !== tb) return ta - tb;
      return (Number(a?._insNo) || 1e18) - (Number(b?._insNo) || 1e18);
    });

    // Chiavi di ordinamento a livello gruppo
    const ins = g.bookings.map(x => Number(x?._insNo) || 1e18);
    g._insNo = Math.min.apply(null, ins.length ? ins : [1e18]);
    const arrTs = g.bookings.map(x => parseDateTs(x?.check_in ?? x?.checkIn)).filter(t => t != null);
    g._arrivoTs = arrTs.length ? Math.min.apply(null, arrTs) : null;
    const outTs = g.bookings.map(x => parseDateTs(x?.check_out ?? x?.checkOut ?? x?.checkout ?? x?.data_check_out ?? x?.partenza ?? x?.departure)).filter(t => t != null);
    g._checkoutTs = outTs.length ? Math.min.apply(null, outTs) : null;
  }
  return groups;
}

function sortGuestGroups(groups){
  const by = state.guestSortBy || "arrivo";
  const forceChronologicalAsc = (by === "arrivo" || by === "checkout");
  const dir = forceChronologicalAsc ? 1 : ((state.guestSortDir === "desc") ? -1 : 1);
  const nameKey = (s) => normalizeGuestNameKey(s);

  const out = (groups || []).slice();
  out.sort((a,b) => {
    if (by === "nome"){
      return nameKey(a?.nome).localeCompare(nameKey(b?.nome), "it") * dir;
    }
    if (by === "inserimento"){
      const aa = Number(a?._insNo) || 1e18;
      const bb = Number(b?._insNo) || 1e18;
      return (aa - bb) * dir;
    }
    if (by === "checkout"){
      const ta = (a?._checkoutTs == null) ? null : Number(a._checkoutTs);
      const tb = (b?._checkoutTs == null) ? null : Number(b._checkoutTs);
      if (ta == null && tb == null) return 0;
      if (ta == null) return 1;
      if (tb == null) return -1;
      return (ta - tb) * dir;
    }
    const ta = (a?._arrivoTs == null) ? null : Number(a._arrivoTs);
    const tb = (b?._arrivoTs == null) ? null : Number(b._arrivoTs);
    if (ta == null && tb == null) return 0;
    if (ta == null) return 1;
    if (tb == null) return -1;
    return (ta - tb) * dir;
  });
  return out;
}




function __guestBookingNumberPresent__(guest){ return !!String(__guestBookingNumberLabel__(guest) || '').trim(); }

function __guestBookingStatusForGroup__(guest){ const list = Array.isArray(guest?._groupBookings) && guest._groupBookings.length ? guest._groupBookings : [guest]; return list.some((item) => __guestBookingNumberPresent__(item)); }
function __guestReportResolveGuest__(){ return state.guestViewItem || null; }
function __guestReportResolveBookings__(guest){
  const list = Array.isArray(guest?._groupBookings) && guest._groupBookings.length ? guest._groupBookings.slice() : [guest];
  return list.filter(Boolean).sort((a,b)=>{
    const aa = parseDateTs(a?.check_in ?? a?.checkIn) ?? Number.MAX_SAFE_INTEGER;
    const bb = parseDateTs(b?.check_in ?? b?.checkIn) ?? Number.MAX_SAFE_INTEGER;
    if (aa !== bb) return aa - bb;
    return String(_guestIdOf(a) || '').localeCompare(String(_guestIdOf(b) || ''));
  });
}
function __guestReportResolveLanguage__(guest){
  const bookings = __guestReportResolveBookings__(guest);
  const candidates = [];
  const pushCandidates = (item) => {
    if (!item) return;
    candidates.push(
      item?.lingua_ospite, item?.linguaOspite, item?.guest_language, item?.guestLanguage,
      item?.language, item?.lang, item?.lingua, item?.locale, item?.preferred_language,
      item?.preferredLanguage, item?.lingua_cliente, item?.linguaCliente,
      item?.guest_lang, item?.guestLang, item?.report_language, item?.reportLanguage,
      item?.report_lang, item?.reportLang, item?.lingua_report, item?.linguaReport,
      item?.selected_language, item?.selectedLanguage, item?.list_language, item?.listLanguage,
      item?.guest_list_language, item?.guestListLanguage, item?.guest_language_code, item?.guestLanguageCode
    );
  };
  bookings.forEach(pushCandidates);
  pushCandidates(guest);
  try{
    const domCandidates = [
      document.getElementById('guestLanguage')?.value,
      document.getElementById('guestLingua')?.value,
      document.getElementById('guestPreferredLanguage')?.value,
      document.getElementById('guestLanguageSelect')?.value,
      document.getElementById('guestReportLanguage')?.value
    ];
    candidates.push(...domCandidates);
  }catch(_){ }
  for (const value of candidates){
    const raw = String(value || '').trim().toLowerCase();
    if (!raw) continue;
    if (raw.startsWith('it') || raw === 'italiano') return 'it';
    if (raw.startsWith('en') || raw.includes('ingles')) return 'en';
    if (raw.startsWith('fr') || raw.includes('fran')) return 'fr';
    if (raw.startsWith('de') || raw.includes('tede') || raw.includes('deut')) return 'de';
    if (raw.startsWith('es') || raw.includes('spagn') || raw.includes('espa')) return 'es';
  }
  try{ return __getAppLanguage__(); }catch(_){ return 'it'; }
}
function __guestReportTextMap__(){ return {
  title:{ it:'REPORT OSPITE', en:'GUEST REPORT', fr:'RAPPORT CLIENT', de:'GASTBERICHT', es:'REPORTE HUÉSPED' },
  guestFallback:{ it:'Ospite', en:'Guest', fr:'Client', de:'Gast', es:'Huésped' },
  booking:{ it:'Prenotazione', en:'Booking', fr:'Réservation', de:'Buchung', es:'Reserva' },
  stay:{ it:'Soggiorno', en:'Stay', fr:'Séjour', de:'Aufenthalt', es:'Estancia' },
  rooms:{ it:'Stanze', en:'Rooms', fr:'Chambres', de:'Zimmer', es:'Habitaciones' },
  guests:{ it:'Ospiti', en:'Guests', fr:'Clients', de:'Gäste', es:'Huéspedes' },
  extraServices:{ it:'Servizi', en:'Services', fr:'Services', de:'Services', es:'Servicios' },
  bookingAmount:{ it:'Importo prenotazione', en:'Booking amount', fr:'Montant réservation', de:'Buchungsbetrag', es:'Importe reserva' },
  services:{ it:'Servizi', en:'Services', fr:'Services', de:'Services', es:'Servicios' },
  discount:{ it:'Sconto', en:'Discount', fr:'Remise', de:'Rabatt', es:'Descuento' },
  deposit:{ it:'Acconto', en:'Deposit', fr:'Acompte', de:'Anzahlung', es:'Depósito' },
  balancePaid:{ it:'Saldo', en:'Paid balance', fr:'Solde payé', de:'Bezahlter Saldo', es:'Saldo pagado' },
  touristTax:{ it:'Tassa soggiorno', en:'Tourist tax', fr:'Taxe de séjour', de:'Kurtaxe', es:'Tasa turística' },
  remaining:{ it:'Rimanenza da pagare', en:'Remaining to pay', fr:'Reste à payer', de:'Restbetrag zu zahlen', es:'Pendiente de pago' },
  noExtraServices:{ it:'Nessun servizio', en:'No services', fr:'Aucun service', de:'Keine Services', es:'Sin servicios' },
  adults_one:{ it:'adulto', en:'adult', fr:'adulte', de:'Erwachsener', es:'adulto' },
  adults_other:{ it:'adulti', en:'adults', fr:'adultes', de:'Erwachsene', es:'adultos' },
  children_one:{ it:'bambino', en:'child', fr:'enfant', de:'Kind', es:'niño' },
  children_other:{ it:'bambini', en:'children', fr:'enfants', de:'Kinder', es:'niños' },
  doubleBed_one:{ it:'letto matrimoniale', en:'double bed', fr:'lit double', de:'Doppelbett', es:'cama doble' },
  doubleBed_other:{ it:'letti matrimoniali', en:'double beds', fr:'lits doubles', de:'Doppelbetten', es:'camas dobles' },
  singleBed_one:{ it:'letto singolo', en:'single bed', fr:'lit simple', de:'Einzelbett', es:'cama individual' },
  singleBed_other:{ it:'letti singoli', en:'single beds', fr:'lits simples', de:'Einzelbetten', es:'camas individuales' },
  crib_one:{ it:'culla', en:'crib', fr:'lit bébé', de:'Kinderbett', es:'cuna' },
  crib_other:{ it:'culle', en:'cribs', fr:'lits bébé', de:'Kinderbetten', es:'cunas' },
  none:{ it:'—', en:'—', fr:'—', de:'—', es:'—' },
  reportReady:{ it:'Report ospite pronto', en:'Guest report ready', fr:'Rapport client prêt', de:'Gastbericht bereit', es:'Reporte huésped listo' },
  reportTitle:{ it:'Report ospite', en:'Guest report', fr:'Rapport client', de:'Gastbericht', es:'Reporte huésped' },
  whatsappHint:{ it:'Report ospite', en:'Guest report', fr:'Rapport client', de:'Gastbericht', es:'Reporte huésped' },
  whatsappMissingPhone:{ it:'Numero ospite assente', en:'Guest phone number missing', fr:'Numéro du client absent', de:'Telefonnummer des Gastes fehlt', es:'Falta el número del huésped' },
  room:{ it:'Stanza', en:'Room', fr:'Chambre', de:'Zimmer', es:'Habitación' },
  date:{ it:'Data', en:'Date', fr:'Date', de:'Datum', es:'Fecha' }
}; }
function __guestReportT__(lang, key){ const map=__guestReportTextMap__(); const row=map[key] || {}; return String(row[lang] || row.it || key || ''); }
function __guestReportPlural__(lang, count, oneKey, otherKey){
  if (lang === 'it'){
    if (oneKey === 'singleBed_one' && otherKey === 'singleBed_other' && Number(count) === 2) return 'due ' + __guestReportT__(lang, otherKey);
    if (oneKey === 'doubleBed_one' && otherKey === 'doubleBed_other' && Number(count) === 2) return 'due ' + __guestReportT__(lang, otherKey);
  }
  return `${count} ${__guestReportT__(lang, count===1 ? oneKey : otherKey)}`;
}
function __guestReportFormatRange__(lang, checkInValue, checkOutValue){
  const ciIso = formatISODateLocal(checkInValue); const coIso = formatISODateLocal(checkOutValue);
  if (!ciIso || !coIso) return '';
  if (!/^\d{4}-\d{2}-\d{2}$/.test(ciIso) || !/^\d{4}-\d{2}-\d{2}$/.test(coIso)) return '';
  const ci = new Date(ciIso + 'T00:00:00'); const co = new Date(coIso + 'T00:00:00');
  if (isNaN(ci) || isNaN(co)) return '';
  let end = new Date(co); if (end < ci) end = new Date(ci);
  const locale = __I18N_LOCALES__[lang] || 'it-IT';
  const monthCap = (dt) => { const s = dt.toLocaleDateString(locale, { month:'long' }); return __capitalizeLocale__(s); };
  const d1 = ci.getDate(), d2 = end.getDate(), m1 = ci.getMonth(), m2 = end.getMonth(), y1 = ci.getFullYear(), y2 = end.getFullYear();
  if (y1 === y2 && m1 === m2) return `${d1}-${d2} ${monthCap(ci)}`;
  if (y1 === y2) return `${d1} ${monthCap(ci)}-${d2} ${monthCap(end)}`;
  return `${d1} ${monthCap(ci)}-${d2} ${monthCap(end)}`;
}
function __guestReportGuestsLabel__(lang, guest){
  const bookings = __guestReportResolveBookings__(guest);
  // dDAE_2.785 — Report: non sommare gli occupanti per ogni stanza/prenotazione duplicata.
  // Il valore visualizzato deve rappresentare il dato della scheda ospite una sola volta.
  const pickCount = (keys) => {
    const candidates = [];
    const pushFrom = (item) => {
      if (!item) return;
      keys.forEach((k) => {
        const n = parseInt(item?.[k] ?? '', 10);
        if (Number.isFinite(n) && n >= 0) candidates.push(n);
      });
    };
    pushFrom(guest);
    bookings.forEach(pushFrom);
    try{
      keys.forEach((k) => {
        const el = document.querySelector(`[name="${k}"], #${k}`);
        const n = parseInt(el?.value ?? '', 10);
        if (Number.isFinite(n) && n >= 0) candidates.push(n);
      });
    }catch(_){ }
    const positive = candidates.filter((n) => n > 0);
    if (positive.length) return Math.max(...positive);
    return candidates.length ? candidates[0] : 0;
  };
  const totals = {
    adults: pickCount(['adulti','adults']),
    children: pickCount(['bambini_u10','kids_u10','children_u10'])
  };
  return `${__guestReportPlural__(lang, totals.adults, 'adults_one', 'adults_other')} · ${__guestReportPlural__(lang, totals.children, 'children_one', 'children_other')}`;
}
function __guestReportGuestPhone__(guest){ return String(guest?.telefono ?? guest?.tel ?? guest?.phone ?? document.getElementById('guestPhone')?.value ?? '').trim(); }
function __guestReportResolveRoomsArr__(guest){
  const direct = _parseRoomsArr(guest?.stanze ?? guest?.rooms ?? guest?.stanza ?? guest?.room ?? '');
  if (direct.length) return direct;
  if (state.guestRooms && state.guestRooms.size){
    return Array.from(state.guestRooms).map((n)=>parseInt(n,10)).filter((n)=>Number.isFinite(n)).sort((a,b)=>a-b);
  }
  return [];
}
function __guestReportRoomBedsValue__(lang, counts){
  const parts = [];
  const m = Number(counts?.lettoM || counts?.letto_m || 0) || 0;
  const s = Number(counts?.lettoS || counts?.letto_s || 0) || 0;
  const c = Number(counts?.culla || 0) || 0;
  if (m > 0) parts.push(__guestReportPlural__(lang, m, 'doubleBed_one', 'doubleBed_other'));
  if (s > 0) parts.push(__guestReportPlural__(lang, s, 'singleBed_one', 'singleBed_other'));
  if (c > 0) parts.push(__guestReportPlural__(lang, c, 'crib_one', 'crib_other'));
  return '(' + (parts.length ? parts.join(' + ') : __guestReportT__(lang, 'none')) + ')';
}
function __guestReportRoomVisual__(roomNumber){
  try{
    const cfg = getRoomsUiConfig();
    const pair = __roomsUiVisualNormalize__(cfg.rooms?.[String(roomNumber)] || 'blue-4', 'blue-4');
    const bg = __operatoreColorHex__(pair.bg || 'blue-4');
    const border = __operatoreColorHex__(pair.border || pair.bg || 'blue-4');
    return { bg:hexToRgba(bg, 0.30), border:hexToRgba(border, 0.30), fg:'#000000' };
  }catch(_){
    return { bg:'rgba(77,156,197,0.30)', border:'rgba(77,156,197,0.30)', fg:'#000000' };
  }
}
function __guestReportResolveStayRanges__(lang, guest){
  const list = __guestReportResolveBookings__(guest)
    .map((item)=>__guestReportFormatRange__(lang, item?.check_in ?? item?.checkIn ?? '', item?.check_out ?? item?.checkOut ?? ''))
    .filter(Boolean);
  return Array.from(new Set(list));
}
function __guestReportResolveRoomCards__(lang, guest){
  const bookings = __guestReportResolveBookings__(guest);
  const cards = [];
  bookings.forEach((booking) => {
    const roomsArr = __guestReportResolveRoomsArr__(booking);
    const gid = _guestIdOf(booking);
    roomsArr.forEach((n) => {
      const key = `${gid}:${n}`;
      const info = (state.stanzeByKey && state.stanzeByKey[key]) ? state.stanzeByKey[key] : { letto_m:0, letto_s:0, culla:0 };
      const range = __guestReportFormatRange__(lang, booking?.check_in ?? booking?.checkIn ?? '', booking?.check_out ?? booking?.checkOut ?? '');
      const beds = __guestReportRoomBedsValue__(lang, info);
      cards.push({
        kind:'roomCard',
        label:`${__guestReportT__(lang, 'room')} ${n}`,
        value:(range ? `${range} ${beds}` : beds),
        dateRange:range,
        bedsValue:beds,
        roomNumber:n,
        visual:__guestReportRoomVisual__(n)
      });
    });
  });
  return cards;
}
function __guestReportResolveServicesItems__(guest){
  const bookings = __guestReportResolveBookings__(guest);
  const out = [];
  const seen = new Set();
  const pushItems = (items) => {
    (Array.isArray(items) ? items : []).forEach((s, idx) => {
      const del = s?.isDeleted ?? s?.is_deleted ?? s?.deleted;
      if (String(del) === '1' || del === true) return;
      const name = String(s?.servizio ?? s?.nome ?? s?.name ?? s?.titolo ?? s?.label ?? '').trim();
      if (!name) return;
      const key = [name, s?.descrizione ?? s?.description ?? '', s?.importo ?? s?.amount ?? '', s?.qty ?? s?.quantita ?? '', idx].join('|');
      if (seen.has(key)) return;
      seen.add(key);
      out.push(s);
    });
  };
  try{
    bookings.forEach((booking) => {
      const gid = String(guestIdOf(booking) || _guestIdOf(booking) || booking?.id || '').trim();
      const cache = gid && state.guestServicesCacheById ? state.guestServicesCacheById[gid] : null;
      if (cache && Array.isArray(cache.items)) pushItems(cache.items);
      pushItems(booking?.servizi_items || booking?.serviziItems || booking?.services_items || booking?.servicesItems);
    });
  }catch(_){}
  if (!out.length) pushItems(state.guestServicesItems);
  return out;
}
async function __guestReportEnsureServicesLoaded__(guest){
  const bookings = __guestReportResolveBookings__(guest);
  for (const booking of bookings){
    try{
      const gid = String(guestIdOf(booking) || _guestIdOf(booking) || booking?.id || '').trim();
      const cache = gid && state.guestServicesCacheById ? state.guestServicesCacheById[gid] : null;
      if (gid && !(cache && Array.isArray(cache.items))){
        await loadServiziForOspite(booking);
      }
    }catch(_){}
  }
}
function __guestReportWhatsappText__(guest){
  const safeGuest = guest || __guestReportResolveGuest__();
  if (!safeGuest) return '';
  const payload = __guestReportResolveRows__(safeGuest);
  const lang = payload.lang || __guestReportResolveLanguage__(safeGuest);
  const locale = __I18N_LOCALES__[lang] || 'it-IT';
  const moneyFmt=(v)=>{ try{ return (Number(v)||0).toLocaleString(locale, { style:'currency', currency:'EUR' }); }catch(_){ return euro(v||0); } };
  const lines = [
    __guestReportT__(lang, 'reportTitle'),
    String(safeGuest?.nome || safeGuest?.name || __guestReportT__(lang, 'guestFallback')).trim() || __guestReportT__(lang, 'guestFallback')
  ];
  (payload.rows || []).forEach((row) => {
    if (!row) return;
    if (row.kind === 'hero' || row.kind === 'roomCard') return;
    if (row.kind === 'servicesList' && Array.isArray(row.serviceItems) && row.serviceItems.length > 1){
      lines.push(`${row.label}:`);
      row.serviceItems.forEach((name)=>{ lines.push(`• ${name}`); });
      return;
    }
    const value = row.kind && row.kind.indexOf('money') === 0 ? moneyFmt(row.value) : String(row.value || __guestReportT__(lang, 'none'));
    lines.push(`${row.label}: ${value}`);
  });
  (payload.roomCards || []).forEach((row) => {
    lines.push(`${row.label} ${row.value}`);
  });
  return lines.filter(Boolean).join('\n');
}
function __guestReportResolveRows__(guest){
  const lang = __guestReportResolveLanguage__(guest);
  const bookings = __guestReportResolveBookings__(guest);
  const stayRanges = __guestReportResolveStayRanges__(lang, guest);
  const roomCards = __guestReportResolveRoomCards__(lang, guest);
  const total = bookings.reduce((sum, item)=> sum + __guestBookingAmountOfBooking__(item), 0);
  const services = bookings.reduce((sum, item)=> sum + money(item?.servizi_totale ?? item?.serviziTotal ?? item?.importo_servizi ?? 0), 0);
  const discount = bookings.reduce((sum, item)=> sum + money(item?.sconto ?? item?.discount ?? 0), 0);
  const deposit = bookings.reduce((sum, item)=> sum + money(item?.acconto_importo ?? item?.accontoImporto ?? item?.deposit ?? 0), 0);
  const saldo = bookings.reduce((sum, item)=> sum + money(item?.saldo_pagato ?? item?.saldoPagato ?? item?.saldo ?? 0), 0);
  const remaining = Math.round((((total + services) - discount) - deposit - saldo) * 100) / 100;
  const serviceNames = (() => {
    const items = __guestReportResolveServicesItems__(guest);
    return items
      .map((s)=>String(s?.servizio ?? s?.nome ?? s?.name ?? s?.titolo ?? s?.label ?? __guestReportT__(lang, 'services')).trim())
      .filter(Boolean);
  })();
  const servicesLabel = serviceNames.length ? serviceNames.join(', ') : __guestReportT__(lang, 'noExtraServices');
  const taxVal = bookings.reduce((sum, item) => {
    try{
      return sum + __guestTaxAmountOfBooking__(item);
    }catch(_){ return sum; }
  }, 0);
  const __guestReportShouldRenderPositiveMoneyRow__ = (row) => {
    if (!row) return false;
    const kind = String(row.kind || '');
    if (kind === 'moneyStrong') return true;
    if (kind !== 'money') return true;
    return Number(row.value || 0) > 0;
  };
  const rows = [
    { kind:'hero', label:__guestReportT__(lang, 'booking'), value: bookings.map((item)=>__guestBookingNumberLabel__(item)).filter(Boolean).join(' · ') || __guestReportT__(lang, 'none') },
    ...roomCards,
    { label:__guestReportT__(lang, 'guests'), value:__guestReportGuestsLabel__(lang, guest) },
    { kind:'servicesList', label:__guestReportT__(lang, 'extraServices'), value: servicesLabel, serviceItems: serviceNames },
    { kind:'money', label:__guestReportT__(lang, 'bookingAmount'), value: total },
    { kind:'money', label:__guestReportT__(lang, 'services'), value: services },
    { kind:'money', label:__guestReportT__(lang, 'discount'), value: discount },
    { kind:'money', label:__guestReportT__(lang, 'deposit'), value: deposit },
    { kind:'money', label:__guestReportT__(lang, 'balancePaid'), value: saldo },
    { kind:'money', label:__guestReportT__(lang, 'touristTax'), value: taxVal },
    { kind:'moneyStrong', label:__guestReportT__(lang, 'remaining'), value: remaining }
  ].filter(__guestReportShouldRenderPositiveMoneyRow__);
  return {
    lang,
    stayLabel: stayRanges.length ? stayRanges.join(' - ') : __guestReportT__(lang, 'none'),
    roomCards,
    rows
  };
}
function __guestReportCanvas__(guest){
  const safeGuest=guest || __guestReportResolveGuest__(); if(!safeGuest) return null;
  const payload=__guestReportResolveRows__(safeGuest); const lang=payload.lang || __guestReportResolveLanguage__(safeGuest); const rows=payload.rows || [];
  const moneyFmt=(v)=>{ try{ return (Number(v)||0).toLocaleString(__I18N_LOCALES__[lang] || 'it-IT', { style:'currency', currency:'EUR' }); }catch(_){ return euro(v||0); } };
  const width=1320, cardH=212, gap=26, topPad=66, footerH=112;
  const serviceRowHeight=(row)=>{
    const count = row && row.kind === 'servicesList' && Array.isArray(row.serviceItems) ? row.serviceItems.length : 0;
    return count > 1 ? Math.max(cardH, 120 + Math.min(count, 8) * 48 + (count > 8 ? 26 : 0)) : cardH;
  };
  const height=topPad+190+rows.reduce((sum,row)=>sum+serviceRowHeight(row)+gap,0)+footerH;
  const canvas=document.createElement('canvas'); canvas.width=width; canvas.height=height; const ctx=canvas.getContext('2d'); if(!ctx) return null;
  const radiusRect=(x,y,w,h,r)=>{ const rr=Math.min(r,w/2,h/2); ctx.beginPath(); ctx.moveTo(x+rr,y); ctx.arcTo(x+w,y,x+w,y+h,rr); ctx.arcTo(x+w,y+h,x,y+h,rr); ctx.arcTo(x,y+h,x,y,rr); ctx.arcTo(x,y,x+w,y,rr); ctx.closePath(); };
  const wrapText=(value,maxChars,maxLines=2)=>{ const txt=String(value || '').trim(); if(!txt) return [__guestReportT__(lang, 'none')]; if(txt.length<=maxChars) return [txt]; const parts=[]; let rest=txt; while(rest.length && parts.length<maxLines){ if(rest.length<=maxChars){ parts.push(rest); break; } let chunk=rest.slice(0,maxChars+1); let cut=Math.max(chunk.lastIndexOf(' '), chunk.lastIndexOf('+'), chunk.lastIndexOf(',')); if(cut<Math.floor(maxChars*0.5)) cut=maxChars; parts.push(rest.slice(0,cut).trim()); rest=rest.slice(cut).trim(); } if(rest.length && parts.length){ const last=parts[parts.length-1] || ''; parts[parts.length-1]=(last.slice(0,Math.max(0,maxChars-1)) + '…').trim(); } return parts.filter(Boolean); };
  ctx.fillStyle='#edf3f8'; ctx.fillRect(0,0,width,height); radiusRect(28,28,width-56,height-56,48); ctx.fillStyle='#ffffff'; ctx.fill(); ctx.lineWidth=2; ctx.strokeStyle='rgba(15,23,42,0.08)'; ctx.stroke();
  const guestName=String(safeGuest?.nome || safeGuest?.name || __guestReportT__(lang, 'guestFallback')).trim() || __guestReportT__(lang, 'guestFallback');
  const titleLines=wrapText(guestName, 26, 2);
  ctx.textAlign='left'; ctx.fillStyle='#2d9cdb'; ctx.font='900 82px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText(titleLines[0],92,162); if(titleLines[1]){ ctx.font='900 68px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText(titleLines[1],92,238); }
  let y=titleLines[1] ? 286 : 228;
  rows.forEach((row,idx)=>{
    const rowH = serviceRowHeight(row);
    radiusRect(92,y,width-184,rowH,34);
    const isRoom = row.kind==='roomCard';
    const isHero = row.kind==='hero';
    const isDateRange = row.kind==='dateRange';
    const isStrong = row.kind==='moneyStrong';
    const bgc = isHero ? '#f1f6fa' : (isStrong ? '#f1f6fa' : (isRoom ? '#eef5fb' : (isDateRange ? '#f1f6fa' : (idx%2===0?'#f9fbfd':'#f1f6fa'))));
    const stroke = isHero ? '#d7e5f1' : (isStrong ? '#f2a23a' : (isRoom ? '#d7e5f1' : 'rgba(15,23,42,0.08)'));
    ctx.fillStyle=bgc; ctx.fill(); ctx.strokeStyle=stroke; ctx.stroke();
    ctx.fillStyle=isHero?'#60738a':(isStrong?'#60738a':(isRoom?'#43617c':'#526173'));
    if(!isDateRange){ ctx.font='800 38px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText(String(row.label || ''),134,y+(isRoom?64:64)); }
    ctx.fillStyle=isHero?'#f2a23a':(isStrong?'#f2a23a':'#10243e');
    const val=row.kind&&row.kind.indexOf('money')===0?moneyFmt(row.value):String(row.value || __guestReportT__(lang, 'none'));
    if(isDateRange){
      ctx.textAlign='left';
      ctx.font='800 38px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif';
      ctx.fillStyle='#526173';
      ctx.fillText(String(row.label || ''),134,y+64);
      ctx.textAlign='right';
      ctx.font='900 52px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif';
      ctx.fillStyle='#10243e';
      const lines=wrapText(val, 28, 2);
      ctx.fillText(lines[0] || __guestReportT__(lang, 'none'),width-134,y+96);
      if(lines[1]){ ctx.font='900 38px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText(lines[1],width-134,y+150); }
    }
    else if(row.kind === 'servicesList' && Array.isArray(row.serviceItems) && row.serviceItems.length > 1){
      ctx.textAlign='left';
      ctx.font='900 38px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif';
      const bulletX = 690;
      let by = y + 64;
      const visibleItems = row.serviceItems.slice(0, 8);
      visibleItems.forEach((name) => {
        const lines = wrapText('• ' + String(name || '').trim(), 28, 1);
        ctx.fillText(lines[0] || '•', bulletX, by);
        by += 48;
      });
      if(row.serviceItems.length > visibleItems.length){
        ctx.font='900 32px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif';
        ctx.fillText('• …', bulletX, by);
      }
    }
    else if(isRoom){
      // dDAE_2.785 — Nel tab stanza il periodo resta sopra, la tipologia letti sotto.
      ctx.textAlign='right';
      const dateTxt = String(row.dateRange || '').trim();
      const bedsTxt = String(row.bedsValue || '').trim();
      const dateLines = wrapText(dateTxt || __guestReportT__(lang, 'none'), 30, 1);
      const bedLines = wrapText(bedsTxt || __guestReportT__(lang, 'none'), 38, 2);
      ctx.font='900 44px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif';
      ctx.fillStyle='#10243e';
      ctx.fillText(dateLines[0] || __guestReportT__(lang, 'none'), width-134, y+92);
      ctx.font='900 34px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif';
      ctx.fillStyle='#526173';
      ctx.fillText(bedLines[0] || __guestReportT__(lang, 'none'), width-134, y+144);
      if(bedLines[1]){ ctx.font='900 30px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText(bedLines[1], width-134, y+180); }
    }
    else { const lines=wrapText(val, 28, 2); ctx.textAlign='right'; ctx.font='900 52px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText(lines[0] || __guestReportT__(lang, 'none'),width-134,y+96); if(lines[1]){ ctx.font='900 38px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText(lines[1],width-134,y+150); } }
    ctx.textAlign='left'; y += rowH + gap;
  });
  ctx.fillStyle='#60738a'; ctx.font='700 28px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif'; ctx.fillText('Daedalium',92,height-90); return canvas;
}
function __guestReportBase64ToBytes__(base64){ const bin=atob(base64); const bytes=new Uint8Array(bin.length); for(let i=0;i<bin.length;i+=1) bytes[i]=bin.charCodeAt(i); return bytes; }
function __guestReportPdfFromJpegDataUrl__(jpegDataUrl, imgWidth, imgHeight){
  const base64=String(jpegDataUrl || '').split(',')[1] || '';
  const imgBytes=__guestReportBase64ToBytes__(base64);
  const scale=595.28 / Math.max(1, Number(imgWidth) || 1);
  const pageW=Number(imgWidth) * scale;
  const pageH=Number(imgHeight) * scale;
  const enc=new TextEncoder();
  const chunks=[]; let offset=0; const offsets=[0];
  const pushText=(txt)=>{ const bytes=enc.encode(txt); chunks.push(bytes); offset += bytes.length; };
  const pushBytes=(bytes)=>{ chunks.push(bytes); offset += bytes.length; };
  pushText(`%PDF-1.4
%ÿÿÿÿ
`);
  const addObj=(id, bodyParts)=>{ offsets[id]=offset; pushText(`${id} 0 obj
`); bodyParts.forEach((part)=>{ if(typeof part==='string') pushText(part); else pushBytes(part); }); pushText(`
endobj
`); };
  addObj(1, [`<< /Type /Catalog /Pages 2 0 R >>
`]);
  addObj(2, [`<< /Type /Pages /Count 1 /Kids [3 0 R] >>
`]);
  addObj(3, [`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageW.toFixed(2)} ${pageH.toFixed(2)}] /Resources << /XObject << /Im0 4 0 R >> /ProcSet [/PDF /ImageC] >> /Contents 5 0 R >>
`]);
  addObj(4, [`<< /Type /XObject /Subtype /Image /Width ${imgWidth} /Height ${imgHeight} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${imgBytes.length} >>
stream
`, imgBytes, `
endstream
`]);
  const content=`q
${pageW.toFixed(2)} 0 0 ${pageH.toFixed(2)} 0 0 cm
/Im0 Do
Q
`;
  addObj(5, [`<< /Length ${content.length} >>
stream
${content}endstream
`]);
  const xrefOffset=offset;
  pushText(`xref
0 6
0000000000 65535 f 
`);
  for(let i=1;i<=5;i+=1) pushText(`${String(offsets[i]).padStart(10,'0')} 00000 n 
`);
  pushText(`trailer
<< /Size 6 /Root 1 0 R >>
startxref
${xrefOffset}
%%EOF`);
  return new Blob(chunks,{type:'application/pdf'});
}
async function __guestReportPdfBlob__(guest){
  const canvas=__guestReportCanvas__(guest); if(!canvas) return null;
  const jpegDataUrl=canvas.toDataURL('image/jpeg', 0.96);
  return __guestReportPdfFromJpegDataUrl__(jpegDataUrl, canvas.width, canvas.height);
}
function __guestReportFileName__(guest){ const safeGuest=guest || __guestReportResolveGuest__() || {}; const name=String(safeGuest?.nome || 'ospite').trim().replace(/[^a-z0-9]+/gi,'_').replace(/^_+|_+$/g,'') || 'ospite'; const ci=String((safeGuest?.check_in ?? safeGuest?.checkIn ?? '') || '').slice(0,10).replace(/-/g,''); const co=String((safeGuest?.check_out ?? safeGuest?.checkOut ?? '') || '').slice(0,10).replace(/-/g,''); return `dDAE_Ospite_${name}_${ci || '00000000'}_${co || '00000000'}.pdf`; }
async function __openGuestReportModal__(guest){ const safeGuest=guest || __guestReportResolveGuest__(); const modal=document.getElementById('guestReportModal'); const preview=document.getElementById('guestReportPreview'); if(!safeGuest || !modal || !preview) return; try{ await __guestReportEnsureServicesLoaded__(safeGuest); }catch(_){} const canvas=__guestReportCanvas__(safeGuest); if(!canvas) return; const closeA=document.getElementById('guestReportClose'); const shareA=document.getElementById('guestReportShare'); const waA=document.getElementById('guestReportWhatsApp'); if(closeA && !closeA.__boundGuestReport){ closeA.__boundGuestReport=true; bindFastTap(closeA, ()=>{ __closeGuestReportModal__(); }); } if(shareA && !shareA.__boundGuestReport){ shareA.__boundGuestReport=true; bindFastTap(shareA, async ()=>{ await __shareGuestReport__(state.guestReportCurrent || null); }); } if(waA && !waA.__boundGuestReport){ waA.__boundGuestReport=true; bindFastTap(waA, async ()=>{ await __shareGuestReportToWhatsApp__(state.guestReportCurrent || null); }); } if(!modal.__boundGuestReportModal){ modal.__boundGuestReportModal=true; modal.addEventListener('click',(e)=>{ if(e.target===modal) __closeGuestReportModal__(); }); } preview.innerHTML=`<img src="${canvas.toDataURL('image/png')}" alt="${escapeHtml(__guestReportT__(__guestReportResolveLanguage__(safeGuest), 'reportTitle'))}"/>`; modal.hidden=false; modal.setAttribute('aria-hidden','false'); document.body.classList.add('modal-open'); state.guestReportCurrent=safeGuest; }
function __closeGuestReportModal__(){ const modal=document.getElementById('guestReportModal'); const preview=document.getElementById('guestReportPreview'); if(preview) preview.innerHTML=''; if(!modal) return; modal.hidden=true; modal.setAttribute('aria-hidden','true'); document.body.classList.remove('modal-open'); }
async function __shareGuestReport__(guest){ const safeGuest=guest || state.guestReportCurrent || __guestReportResolveGuest__(); if(!safeGuest) return false; const lang=__guestReportResolveLanguage__(safeGuest); const blob=await __guestReportPdfBlob__(safeGuest); if(!blob) return false; const filename=__guestReportFileName__(safeGuest); const file=new File([blob],filename,{type:'application/pdf'}); try{ if(navigator.canShare && navigator.canShare({ files:[file] })){ await navigator.share({ title:__guestReportT__(lang, 'reportTitle'), files:[file] }); return true; } }catch(err){ if(err && err.name==='AbortError') return false; }
  const url=URL.createObjectURL(blob); try{ const a=document.createElement('a'); a.href=url; a.download=filename; document.body.appendChild(a); try{ a.click(); }catch(_){} try{ document.body.removeChild(a); }catch(_){} try{ toast(__guestReportT__(lang, 'reportReady'),'blue'); }catch(_){} return true; } finally { setTimeout(()=>{ try{ URL.revokeObjectURL(url); }catch(_){} },1200); }
}

function __guestReportResolveWhatsAppPhone__(guest){
  try{
    const candidates = [];
    const push = (v) => {
      const raw = String(v ?? '').trim();
      if (raw) candidates.push(raw);
    };
    const addFrom = (item) => {
      if (!item || typeof item !== 'object') return;
      [
        item.whatsapp, item.whatsApp, item.WhatsApp, item.numero_whatsapp, item.numeroWhatsapp,
        item.telefono_whatsapp, item.telefonoWhatsApp, item.phone_whatsapp, item.phoneWhatsApp,
        item.cellulare, item.mobile, item.telefono, item.tel, item.phone, item.numeroTelefono, item.numero_telefono
      ].forEach(push);
    };
    addFrom(guest);
    try{ (__guestReportResolveBookings__(guest) || []).forEach(addFrom); }catch(_){ }
    try{
      const phoneEl = document.getElementById('guestPhone');
      if (phoneEl) push(phoneEl.value);
    }catch(_){ }
    for (const raw of candidates){
      const normalized = normalizeWhatsAppPhone(raw);
      if (normalized) return normalized;
    }
  }catch(_){ }
  return '';
}
function __openGuestReportWhatsAppChat__(guest){
  try{
    const safeGuest = guest || state.guestReportCurrent || __guestReportResolveGuest__();
    const normalized = __guestReportResolveWhatsAppPhone__(safeGuest);
    if (!normalized){
      try{ toast('Numero WhatsApp ospite mancante', 'orange'); }catch(_){ }
      return false;
    }
    const url = 'https://wa.me/' + encodeURIComponent(normalized);
    try{ window.location.href = url; }catch(_){ window.open(url, '_blank', 'noopener'); }
    return true;
  }catch(_){ return false; }
}
async function __shareGuestReportToWhatsApp__(guest){
  const safeGuest = guest || state.guestReportCurrent || __guestReportResolveGuest__();
  if (!safeGuest) return false;
  const normalized = __guestReportResolveWhatsAppPhone__(safeGuest);
  if (!normalized){
    try{ toast('Numero WhatsApp ospite mancante', 'orange'); }catch(_){ }
    return false;
  }
  const lang = __guestReportResolveLanguage__(safeGuest);
  const blob = await __guestReportPdfBlob__(safeGuest);
  if (!blob) return false;
  const filename = __guestReportFileName__(safeGuest);
  const guestName = String(safeGuest?.nome || safeGuest?.name || __guestReportT__(lang, 'guestFallback')).trim() || __guestReportT__(lang, 'guestFallback');

  // Il tasto WhatsApp deve essere distinto dal tasto Condividi:
  // non apre il pannello generico di condivisione, ma prepara il PDF e apre
  // direttamente la chat WhatsApp dell'ospite. Le PWA/iOS non permettono
  // l'allegato automatico di un PDF dentro una chat WhatsApp via URL.
  let downloaded = false;
  const url = URL.createObjectURL(blob);
  try{
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    a.target = '_blank';
    a.rel = 'noopener';
    document.body.appendChild(a);
    try{ a.click(); downloaded = true; }catch(_){}
    try{ document.body.removeChild(a); }catch(_){}
  } finally {
    setTimeout(()=>{ try{ URL.revokeObjectURL(url); }catch(_){} }, 3500);
  }

  const msg = `${__guestReportT__(lang, 'reportTitle')} - ${guestName}`;
  const waUrl = 'https://wa.me/' + encodeURIComponent(normalized) + '?text=' + encodeURIComponent(msg);
  try{
    if (downloaded) toast('PDF report pronto: allegalo nella chat WhatsApp aperta', 'blue');
  }catch(_){ }
  setTimeout(()=>{
    try{ window.location.href = waUrl; }
    catch(_){ try{ window.open(waUrl, '_blank', 'noopener'); }catch(__){} }
  }, downloaded ? 250 : 0);
  return true;
}

function __guestCardRoomWord__(count){
  try{
    const lang = (typeof __getAppLanguage__ === 'function') ? __getAppLanguage__() : 'it';
    const singular = { it:'stanza', en:'room', fr:'chambre', de:'Zimmer', es:'habitación' };
    const plural = { it:'stanze', en:'rooms', fr:'chambres', de:'Zimmer', es:'habitaciones' };
    return String((Number(count) === 1 ? singular : plural)[lang] || (Number(count) === 1 ? singular.it : plural.it));
  }catch(_){ return Number(count) === 1 ? 'stanza' : 'stanze'; }
}

function __guestCardRoomsOccupiedAria__(){
  try{
    return __designTranslate__('Stanze occupate', { en:'Occupied rooms', fr:'Chambres occupées', de:'Belegte Zimmer', es:'Habitaciones ocupadas' });
  }catch(_){ return 'Stanze occupate'; }
}

function __guestCardRoomsLabel__(guest){
  try{
    const rows = (guest && Array.isArray(guest._groupBookings) && guest._groupBookings.length) ? guest._groupBookings : [guest];
    const set = new Set();
    rows.forEach((row) => {
      const rooms = (typeof _parseRoomsArr === 'function')
        ? _parseRoomsArr(row?.stanze ?? row?.rooms ?? row?.stanza ?? row?.room ?? '')
        : [];
      rooms.forEach((n) => {
        const v = parseInt(n, 10);
        if (Number.isFinite(v) && v > 0) set.add(v);
      });
    });
    const arr = Array.from(set).sort((a,b)=>a-b);
    if (!arr.length) return '';
    const word = __guestCardRoomWord__(arr.length);
    if (arr.length === 1) return `${word} ${arr[0]}`;
    return `${word} ${arr.join(' - ')}`;
  }catch(_){ return ''; }
}

function renderGuestCards(){
  const wrap = document.getElementById("guestCards");
  if (!wrap) return;
  wrap.hidden = false;
  wrap.replaceChildren();

  const frag = document.createDocumentFragment();

  let items = Array.isArray(state.ospiti) && state.ospiti.length
    ? state.ospiti
    : (Array.isArray(state.guests) ? state.guests : []);

  // Filtro per anno di esercizio (obbligatorio, anche per la guest list)
  try{
    const y = state.exerciseYear || loadExerciseYear();
    items = __filterByExerciseYear__(items, y, [
      "check_in","checkIn","arrivo","dataArrivo","check_out","checkOut","partenza","dataPartenza",
      "createdAt","created_at","updatedAt","updated_at"
    ]);
  }catch(_){ }

  // Preventivi: di default nascosti dalla guest list; il tasto dedicato mostra solo preventivi.
  try{
    const onlyPreventivi = !!state.guestShowPreventiviOnly;
    items = __guestFilterPreventiviRows__(items, onlyPreventivi);
  }catch(_){ }

  // Base completa della guest list dopo il filtro anno: serve anche per mantenere
  // stabile il numero del pallino prenotazione quando si passa da Tutti a Oggi.
  const allItemsForGuestListNumbering = (items || []).slice();

  // Filtro data guest list: usa check-in oppure check-out in base al tab attivo.
  const arrivalFilter = __ddae2862NormalizeFilterMode__(state.guestArrivalFilter || 'today', 'guest');
  if (arrivalFilter !== 'all' && !state.guestShowPreventiviOnly){
    const today = todayISO();
    const soon3Limit = addDaysISO(today, 3);
    const soon7Limit = addDaysISO(today, 7);
    const prev3Start = addDaysISO(today, -3);
    const prev7Start = addDaysISO(today, -7);
    const useCheckoutDate = String(state.guestSortBy || '').trim() === 'checkout';
    items = (items || []).filter(g => {
      const inRaw = (g?.check_in ?? g?.checkIn ?? g?.arrivo ?? g?.dataArrivo ?? g?.arrival ?? g?.guestCheckIn ?? "");
      const outRaw = (g?.check_out ?? g?.checkOut ?? g?.checkout ?? g?.data_check_out ?? g?.dataPartenza ?? g?.partenza ?? g?.departure ?? g?.guestCheckOut ?? "");
      const v = useCheckoutDate ? outRaw : inRaw;
      const iso = __parseDateFlexibleToISO(v);
      const d = iso ? iso.slice(0,10) : String(v || '').trim().slice(0,10);
      if (!d) return false;
      if (arrivalFilter === '3days') return d >= today && d <= soon3Limit;
      if (arrivalFilter === '7days') return d >= today && d <= soon7Limit;
      if (arrivalFilter === '-3days') return d >= prev3Start && d <= today;
      if (arrivalFilter === '-7days') return d >= prev7Start && d <= today;
      if (arrivalFilter === 'today' && !useCheckoutDate){
        const inIso = __parseDateFlexibleToISO(inRaw);
        const outIso = __parseDateFlexibleToISO(outRaw);
        const checkIn = inIso ? inIso.slice(0,10) : String(inRaw || '').trim().slice(0,10);
        const checkOut = outIso ? outIso.slice(0,10) : String(outRaw || '').trim().slice(0,10);
        return !!(checkIn && checkIn <= today && (!checkOut || checkOut > today));
      }
      return d === today;
    });
  }


  if (!items.length){
    wrap.replaceChildren();
    const empty = document.createElement("div");
    empty.style.opacity = ".7";
    empty.style.fontSize = "14px";
    empty.style.padding = "8px";
    const arrivalFilter = __ddae2862NormalizeFilterMode__(state.guestArrivalFilter || 'today', 'guest');
    if (state.guestShowPreventiviOnly){
      empty.textContent = 'Nessun preventivo nel periodo.';
      frag.appendChild(empty);
      wrap.appendChild(frag);
      return;
    }
    empty.textContent = arrivalFilter === '3days'
      ? (__translateExactText__('Nessun ospite nei prossimi 3 giorni.') || 'Nessun ospite nei prossimi 3 giorni.')
      : (arrivalFilter === '7days'
          ? (__translateExactText__('Nessun ospite nei prossimi 7 giorni.') || 'Nessun ospite nei prossimi 7 giorni.')
          : (arrivalFilter === '-3days'
              ? (__translateExactText__('Nessun ospite negli ultimi 3 giorni.') || 'Nessun ospite negli ultimi 3 giorni.')
              : (arrivalFilter === '-7days'
                  ? (__translateExactText__('Nessun ospite negli ultimi 7 giorni.') || 'Nessun ospite negli ultimi 7 giorni.')
                  : (arrivalFilter === 'all'
                      ? (__translateExactText__('Nessun ospite nel periodo.') || 'Nessun ospite nel periodo.')
                      : (__translateExactText__('Nessun ospite per oggi.') || 'Nessun ospite per oggi.')))));
    frag.appendChild(empty);
    wrap.appendChild(frag);
    return;
  }

  // Numero progressivo di inserimento (stabile).
  // Il pallino della prenotazione deve usare la numerazione della vista Tutti:
  // filtrando Oggi cambia la lista visibile, non il numero assegnato alla card.
  const allInsMap = computeInsertionMap(allItemsForGuestListNumbering);
  allItemsForGuestListNumbering.forEach((it) => {
    const id = guestIdOf(it);
    it._insNo = id ? allInsMap[id] : null;
  });
  const __guestAllDisplayNoByKey = new Map();
  try{
    const allGroupsForNumbering = groupGuestsByName(allItemsForGuestListNumbering || []).map((group) => {
      const bookings = Array.isArray(group?.bookings) ? group.bookings.slice() : [];
      const first = bookings[0] || null;
      if (!first) return null;
      const sourceInsNo = bookings
        .map(x => Number(x?._insNo) || null)
        .filter(x => x != null && x > 0)
        .sort((a,b)=>a-b)[0] || null;
      const arrTs = bookings
        .map(x => parseDateTs(x?.check_in ?? x?.checkIn ?? x?.arrivo ?? x?.dataArrivo ?? x?.arrival ?? x?.guestCheckIn))
        .filter(t => t != null)
        .sort((a,b)=>a-b)[0] ?? null;
      const checkoutTs = bookings
        .map(x => parseDateTs(x?.check_out ?? x?.checkOut ?? x?.checkout ?? x?.data_check_out ?? x?.dataPartenza ?? x?.partenza ?? x?.departure ?? x?.guestCheckOut))
        .filter(t => t != null)
        .sort((a,b)=>a-b)[0] ?? null;
      return {
        key: group?.key || __guestCardKeyOf__(first),
        nome: group?.nome || (String(first?.nome ?? first?.name ?? first?.guest ?? '').trim() || 'Ospite'),
        _arrivoTs: arrTs,
        _checkoutTs: checkoutTs,
        _insNo: sourceInsNo,
        _sourceInsNo: sourceInsNo
      };
    }).filter(Boolean).sort((a,b) => {
      const aa = Number(a?._sourceInsNo) || 1e18;
      const bb = Number(b?._sourceInsNo) || 1e18;
      if (aa !== bb) return aa - bb;
      const ta = (a?._arrivoTs == null) ? 1e18 : Number(a._arrivoTs);
      const tb = (b?._arrivoTs == null) ? 1e18 : Number(b._arrivoTs);
      if (ta !== tb) return ta - tb;
      return normalizeGuestNameKey(a?.nome).localeCompare(normalizeGuestNameKey(b?.nome), 'it');
    });
    allGroupsForNumbering.forEach((group, idx) => {
      const key = String(group?.key || '').trim();
      if (key) __guestAllDisplayNoByKey.set(key, idx + 1);
    });
  }catch(_){ }

  items.forEach((it) => {
    const id = guestIdOf(it);
    it._insNo = id ? allInsMap[id] : null;
  });

  // Guest list: una card unica per ospite/nome, anche se esistono più prenotazioni
  // o più gruppi di stanze. I dettagli della scheda mostrano poi i blocchi separati.
  let cards = groupGuestsByName(items || []).map((group) => {
    const bookings = Array.isArray(group?.bookings) ? group.bookings.slice() : [];
    const first = bookings[0] || null;
    if (!first) return null;
    const sourceInsNo = bookings
      .map(x => Number(x?._insNo) || null)
      .filter(x => x != null && x > 0)
      .sort((a,b)=>a-b)[0] || null;
    const arrTs = bookings
      .map(x => parseDateTs(x?.check_in ?? x?.checkIn ?? x?.arrivo ?? x?.dataArrivo ?? x?.arrival ?? x?.guestCheckIn))
      .filter(t => t != null)
      .sort((a,b)=>a-b)[0] ?? null;
    const checkoutTs = bookings
      .map(x => parseDateTs(x?.check_out ?? x?.checkOut ?? x?.checkout ?? x?.data_check_out ?? x?.dataPartenza ?? x?.partenza ?? x?.departure ?? x?.guestCheckOut))
      .filter(t => t != null)
      .sort((a,b)=>a-b)[0] ?? null;
    const hasNotes = bookings.some(x => guestHasNotes(x));
    return {
      ...first,
      nome: group?.nome || (String(first?.nome ?? first?.name ?? first?.guest ?? "").trim() || "Ospite"),
      _arrivoTs: arrTs,
      _checkoutTs: checkoutTs,
      _insNo: sourceInsNo,
      _sourceInsNo: sourceInsNo,
      _groupBookings: bookings,
      _hasNotesAny: hasNotes
    };
  }).filter(Boolean);

  const cardsByInsertion = cards.slice().sort((a,b) => {
    const aa = Number(a?._sourceInsNo) || 1e18;
    const bb = Number(b?._sourceInsNo) || 1e18;
    if (aa !== bb) return aa - bb;
    const ta = (a?._arrivoTs == null) ? 1e18 : Number(a._arrivoTs);
    const tb = (b?._arrivoTs == null) ? 1e18 : Number(b._arrivoTs);
    if (ta !== tb) return ta - tb;
    return normalizeGuestNameKey(a?.nome).localeCompare(normalizeGuestNameKey(b?.nome), "it");
  });
  cardsByInsertion.forEach((card, idx) => {
    const key = String(__guestCardKeyOf__(card)).trim();
    const allNo = key ? Number(__guestAllDisplayNoByKey.get(key)) : null;
    const displayNo = (Number.isFinite(allNo) && allNo > 0) ? allNo : (idx + 1);
    card._groupInsNo = displayNo;
    card._insNo = displayNo;
    card._displayInsNo = displayNo;
  });

  cards = sortGuestGroups(cards);

  cards.forEach(first => {
    if (!first) return;

    // Evidenzia checkout oggi con pagamento in sospeso (rimanenza > 0)
    const __today = todayISO();
    const outISO = __parseDateFlexibleToISO(first?.check_out || first?.checkOut);
    const total = money(first?.importo_prenotazione ?? first?.importo_prenota ?? first?.total ?? 0);
    const services = money(first?.servizi_totale ?? first?.serviziTotal ?? first?.importo_servizi ?? 0);
    const dep = money(first?.acconto_importo ?? first?.accontoImporto ?? first?.deposit ?? 0);
    const saldo = money(first?.saldo_pagato ?? first?.saldoPagato ?? first?.saldo ?? 0);
    const discount = money(first?.sconto ?? first?.discount ?? first?.sconto_importo ?? first?.scontoImporto ?? 0);
    const remainingRaw = (total + services) - discount - dep - saldo;
    const remaining = Math.max(0, Math.round((isFinite(remainingRaw) ? remainingRaw : 0) * 100) / 100);
    const __hasCheckoutPending = !!(outISO && outISO === __today && isFinite(remaining) && remaining > 0.0001);

    const card = document.createElement("div");
    card.className = "guest-card";
    if (__guestIsPreventivo__(first)) card.classList.add("is-preventivo");
    if (__hasCheckoutPending && !__guestIsPreventivo__(first)) card.classList.add("checkout-pending");

    const nome = escapeHtml(first.nome || String(first?.name ?? first?.guest ?? "").trim() || "Ospite");

    const insNo = (Number(first._displayInsNo) && Number(first._displayInsNo) > 0 && Number(first._displayInsNo) < 1e18) ? Number(first._displayInsNo) : null;

    const nationalityOption = __readGuestNationalityFromRecord__(first);
    const nationalityFlag = escapeHtml(String(first?.country_flag ?? nationalityOption?.flag ?? '🏳️').trim() || '🏳️');
    const nationalityName = escapeHtml(String(nationalityOption?.name || 'Nazionalità non selezionata').trim() || 'Nazionalità non selezionata');

    const led = guestLedStatus(first);
    // dDAE_2.748 — Il LED stato della guest list deve lampeggiare per gli arrivi di oggi
    // quando il check-in non è ancora stato confermato. Include anche eventuali classi
    // stato composte/legacy, evitando solo stati grigi/rossi.
    const checkInDueBlink = __guestGroupCheckInExpectedToday__(first) && !String(led.cls || '').includes('led-gray') && !String(led.cls || '').includes('led-red');

    const marriageOn = !!(first?.matrimonio);
    const hasNotes = !!(first?._hasNotesAny) || guestHasNotes(first);
    const stayNights = calcStayNights(first);

    const arrivoText = __guestCardStayRangeLabel__(first) || formatArrivalDayIT(first.check_in || first.checkIn || "") || "—";

    const roomsLabel = escapeHtml(__guestCardRoomsLabel__(first));
    const channelBadge = getGuestChannelBadgeData(first);

    card.tabIndex = 0;
    card.setAttribute("role", "button");
    card.setAttribute("aria-label", `${__translateExactText__("Apri scheda ospite") || "Apri scheda ospite"}: ${nome}`);
    try{ card.dataset.arrivalIso = __guestCardArrivalISO__(first) || ""; }catch(_){ }
    try{ card.dataset.checkoutIso = __guestCardCheckoutISO__(first) || ""; }catch(_){ }
    try{
      const visualKey = __guestListCardVisualKeyFor__(guestIdOf(first) || first?.id || first?.guest_id || first?.booking_id || normalizeGuestNameKey(first?.nome || String(first?.name ?? first?.guest ?? "").trim() || ""));
      if (visualKey) card.dataset.guestVisualKey = visualKey;
    }catch(_){ }

    card.innerHTML = `
      <div class="guest-row guest-row-compact">
        <div class="guest-main">
          ${insNo ? `<span class="guest-insno ${__guestBookingStatusForGroup__(first) ? 'is-ready' : 'is-missing'}${hasNotes ? ` has-notes` : ``}" aria-label="${__guestBookingStatusForGroup__(first) ? 'Numero prenotazione inserito' : 'Numero prenotazione mancante'}" title="${__guestBookingStatusForGroup__(first) ? 'Numero prenotazione inserito' : 'Numero prenotazione mancante'}"${hasNotes ? ` data-has-notes="1"` : ``}>${insNo}</span>` : ``}
          <span class="guest-nationality-dot" aria-label="Nazionalità: ${nationalityName}" title="${nationalityName}"><span class="guest-nationality-flag" aria-hidden="true">${nationalityFlag}</span></span>
          <div class="guest-nameblock">
            <span class="guest-name-tab guest-name-text">${nome}</span>
            <span class="guest-arrivo guest-arrivo-under" aria-label="${escapeHtml(__translateExactText__('Arrivo') || 'Arrivo')}">${arrivoText}</span>
            ${roomsLabel ? `<span class="guest-contact guest-room-label" aria-label="${escapeHtml(__guestCardRoomsOccupiedAria__())}">${roomsLabel}</span>` : ``}
          </div>
        </div>
        <div class="guest-meta-right" aria-label="${escapeHtml(__translateExactText__('Stato') || 'Stato')}">
          <div class="guest-meta-dots">
            ${buildNightsDotHTML(stayNights)}
            ${(channelBadge && channelBadge.name) ? `<span class="guest-channel-inline"><span class="guest-channel-dot color-${channelBadge.color}" style="${escapeHtml(channelBadge.style || __tagColorInlineStyle__(channelBadge.color || 'orange', channelBadge.textColor || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }))}" aria-label="${escapeHtml(channelBadge.name)}" title="${escapeHtml(channelBadge.name)}"><span>${escapeHtml(channelBadge.initial)}</span></span></span>` : ``}
            ${marriageOn ? `<span class="marriage-dot" aria-label="${escapeHtml(__translateExactText__('Matrimonio') || 'Matrimonio')}">M</span>` : ``}
            ${(truthy(first?.g ?? first?.flag_g ?? first?.gruppo_g ?? first?.group ?? first?.g_flag) ? `<span class="g-dot" aria-label="G">G</span>` : ``)}
            ${(truthy(first?.col_c ?? first?.colC ?? first?.c ?? first?.C ?? first?.flag_c ?? first?.flagC ?? first?.colc ?? first?.c_flag) ? `<span class="c-dot" aria-label="C">C</span>` : ``)}
            <span class="guest-led ${led.cls}${checkInDueBlink ? ' is-checkin-due-blink' : ''}" aria-label="${led.label}${checkInDueBlink ? ' — ' + (__translateExactText__('check-in non effettuato') || 'check-in non effettuato') : ''}" title="${led.label}${checkInDueBlink ? ' — ' + (__translateExactText__('check-in non effettuato') || 'check-in non effettuato') : ''}"></span>
          </div>
          ${buildGuestCardAlertLedsHTML(first)}
        </div>
      </div>
    `;

    try{ __applyGuestListCardVisual__(card); }catch(_){ }
    try{ __bindGuestListCardColorHold__(card); }catch(_){ }

    const open = () => {
      try{ if ((card.__guestListCardSuppressTapUntil || 0) > Date.now()) return; }catch(_){ }

      const sameNameItems = Array.isArray(first?._groupBookings) && first._groupBookings.length
        ? {
            key: String(__guestCardKeyOf__(first)).trim(),
            bookings: first._groupBookings
          }
        : groupGuestsByName(items || []).find(g => String(g.key) === String(__guestCardKeyOf__(first)).trim());
      if (sameNameItems && sameNameItems.bookings && sameNameItems.bookings.length > 1){
        state.guestGroupBookings = sameNameItems.bookings;
        state.guestGroupKey = sameNameItems.key;
        state.guestGroupActiveId = guestIdOf(first);
      } else {
        state.guestGroupBookings = null;
        state.guestGroupKey = null;
        state.guestGroupActiveId = null;
        try{ clearGuestMulti(); }catch(_){ }
      }
      enterGuestViewMode(first);
      showPage("ospite");
    };
    card.addEventListener("click", open);
    card.addEventListener("keydown", (e) => {
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        open();
      }
    });

    frag.appendChild(card);
  });
  wrap.appendChild(frag);
  try{ __updateGuestArrivoTodayButtonVisibility__(); }catch(_){ }
  try{ if (state.page === 'ospiti' && state.guestListScrollRestorePending) __scheduleGuestListScrollRestore__(); }catch(_){ }
}








// =========================
// COLAZIONE (lista spesa)
// =========================

function __normBool01(v){
  const s = String(v ?? "").trim().toLowerCase();
  if (s === "1" || s === "true" || s === "yes" || s === "y") return 1;
  return 0;
}

function __colazioneAny(){
  try{ return Array.isArray(state.colazione.items) && state.colazione.items.filter(i=>!__normBool01(i.isDeleted)).length > 0; }catch(_){ return false; }
}

function updateProdottiHomeBlink(){
  const btn = document.getElementById("goProdotti");
  const any = (
    Array.isArray(state.colazione?.items) && state.colazione.items.some(i=>!__normBool01(i.isDeleted))
  ) || (
    Array.isArray(state.prodotti_pulizia?.items) && state.prodotti_pulizia.items.some(i=>!__normBool01(i.isDeleted))
  );
  if (btn) btn.classList.toggle("colazione-attn", !!any);

  // Topbar LED: acceso quando esiste almeno un prodotto "salvato" (pallino rosso)
  // nella relativa lista.
  try{
    const ledC = document.getElementById("prodLedColazione");
    const ledP = document.getElementById("prodLedPulizia");
    if (ledC && ledP){
      const anySavedIn = (arr) => {
        try{
          if (!Array.isArray(arr)) return false;
          return arr.some((i)=>{
            if (__normBool01(i?.isDeleted)) return false;
            const q = parseInt(String(i?.qty ?? 0), 10);
            const qty = isNaN(q) ? 0 : Math.max(0, q);
            return qty > 0;
          });
        }catch(_){ return false; }
      };
      const hasC = anySavedIn(state.colazione?.items);
      const hasP = anySavedIn(state.prodotti_pulizia?.items);
      ledC.classList.toggle("is-on", !!hasC);
      ledP.classList.toggle("is-on", !!hasP);
      try{ ledC.setAttribute("aria-label", hasC ? "Lista spesa attiva" : "Lista spesa vuota"); }catch(_){ }
      try{ ledC.setAttribute("title", hasC ? "Lista spesa attiva" : "Lista spesa vuota"); }catch(_){ }
      try{ ledP.setAttribute("aria-label", hasP ? "Prodotti attivi" : "Prodotti vuoti"); }catch(_){ }
      try{ ledP.setAttribute("title", hasP ? "Prodotti attivi" : "Prodotti vuoti"); }catch(_){ }
    }
  }catch(_){ }
}

function __freqKey_(action, id){
  const uid = String(state?.session?.user_id || "").trim();
  const yr = String(state?.exerciseYear || "").trim();
  return `freq:${uid}:${yr}:${String(action||"")}:${String(id||"")}`;
}

function getFreq_(action, id){
  try{
    const k = __freqKey_(action, id);
    state._freqCache = state._freqCache || {};
    if (k in state._freqCache) return state._freqCache[k];
    const v = parseInt(String(localStorage.getItem(k) || "0"), 10);
    const n = isNaN(v) ? 0 : Math.max(0, v);
    state._freqCache[k] = n;
    return n;
  }catch(_){ return 0; }
}

function incFreq_(action, id){
  try{
    const k = __freqKey_(action, id);
    const n = getFreq_(action, id) + 1;
    localStorage.setItem(k, String(n));
    state._freqCache = state._freqCache || {};
    state._freqCache[k] = n;
  }catch(_){ }
}

async function loadColazione({ force=false, showLoader=true } = {}){
  const s = state.colazione;
  const now = Date.now();
  const ttl = 20000;
  if (!force && s.loaded && (now - (s.loadedAt||0) < ttl)) {
    updateProdottiHomeBlink();
    return s.items || [];
  }
  const res = await api("colazione", { method:"GET", params:{}, showLoader });
  const rows = Array.isArray(res) ? res : (res && Array.isArray(res.rows) ? res.rows : (res && Array.isArray(res.data) ? res.data : []));
  s.items = (rows || []).filter(r => !__normBool01(r.isDeleted));
  // Normalizza qty/saved per coerenza multi-dispositivo (LED + pallini rossi)
  try{
    (s.items || []).forEach((it)=>{
      if (__normBool01(it?.isDeleted)) return;
      const n = parseInt(String(it?.qty ?? 0), 10);
      const q = isNaN(n) ? 0 : Math.max(0, n);
      it.qty = q;
      it.saved = q > 0 ? 1 : 0;
    });
  }catch(_){ }
  s.loaded = true;
  s.loadedAt = now;
  updateProdottiHomeBlink();
  return s.items;
}

function renderColazione(){
  const wrap = document.getElementById("colazioneList");
  if (!wrap) return;
  const items = (state.colazione.items || []).filter(r => !__normBool01(r.isDeleted));

  wrap.innerHTML = "";
  const frag = document.createDocumentFragment();

  items.forEach((it) => {
    const row = document.createElement("div");
    row.className = "colazione-item";
    row.dataset.id = String(it.id || "");

    const qtyBtn = document.createElement("button");
    qtyBtn.type = "button";
    qtyBtn.className = "colazione-dot colazione-qtydot";
    const draftQty = __prodDraftGetQty_(it.id);
    const hasDraft = (draftQty !== null && draftQty !== undefined);
    const qty = hasDraft ? parseInt(String(draftQty ?? 0), 10) : parseInt(String(it.qty ?? 0), 10);
    const q = (isNaN(qty) ? 0 : Math.max(0, qty));
    qtyBtn.textContent = q > 0 ? String(q) : "";

    const saved = hasDraft ? 0 : (q > 0 ? 1 : 0);
    qtyBtn.classList.toggle("is-saved", saved && q > 0);

    const text = document.createElement("div");
    text.className = "colazione-text";
    text.textContent = String(it.prodotto || "").trim();

    const checkBtn = document.createElement("button");
    checkBtn.type = "button";
    checkBtn.className = "colazione-dot colazione-checkdot";
    checkBtn.innerHTML = '<svg aria-hidden="true" viewBox="0 0 24 24"><path d="M20 6 9 17l-5-5"></path></svg>';
    checkBtn.classList.toggle("is-checked", __normBool01(it.checked) === 1);

    row.appendChild(qtyBtn);
    row.appendChild(text);
    row.appendChild(checkBtn);

    frag.appendChild(row);
  });

  wrap.appendChild(frag);
}


// =========================
// SPESA (Colazione + Prodotti)
// Modulo riscritto da zero per stabilità multi-dispositivo:
// - stato LED Home calcolato SEMPRE su liste caricate (loadSpesaAll)
// - tab Colazione/Prodotti indipendenti (draft separati)
// - Salva: UI immediata + sync backend in background + reload forzato
// =========================

// ---- UI key helpers ----
function __spesaEnsureUI_(){
  state.prodottiUI = state.prodottiUI || { list:"colazione" };
  const raw = String(state.prodottiUI.list || "colazione").toLowerCase();
  state.prodottiUI.list = (raw === "pulizia" || raw === "prodotti") ? "pulizia" : "colazione";
  return state.prodottiUI.list;
}

function __prodListKey_(){
  return __spesaEnsureUI_();
}

function __spesaActionByKey_(key){
  return (key === "pulizia") ? "prodotti_pulizia" : "colazione";
}

function __prodAction_(){
  return __spesaActionByKey_(__prodListKey_());
}

function __spesaBucketByKey_(key){
  return (key === "pulizia") ? state.prodotti_pulizia : state.colazione;
}

function __prodStateBucket_(){
  return __spesaBucketByKey_(__prodListKey_());
}

// ---- Draft (non persistito finché non premi SALVA) ----
function __spesaDraftEnsure_(){
  state._spesaDraft = state._spesaDraft || { colazione:{}, pulizia:{} };
  state._spesaDirty = state._spesaDirty || { colazione:{}, pulizia:{} };
  const key = __prodListKey_();
  state._spesaDraft[key] = state._spesaDraft[key] || {};
  state._spesaDirty[key] = state._spesaDirty[key] || {};
  return key;
}

function __prodDraftBucket_(){
  const key = __spesaDraftEnsure_();
  return state._spesaDraft[key];
}

function __prodDraftDirtyBucket_(){
  const key = __spesaDraftEnsure_();
  return state._spesaDirty[key];
}

function __prodDraftGetQty_(id){
  try{
    const sid = String(id||"");
    const b = __prodDraftBucket_();
    if (!b || !Object.prototype.hasOwnProperty.call(b, sid)) return null;
    return b[sid];
  }catch(_){ return null; }
}

function __prodDraftSetQty_(id, qty){
  try{
    const sid = String(id||"");
    const b = __prodDraftBucket_();
    const d = __prodDraftDirtyBucket_();
    if (!b || !d) return;
    const n = parseInt(String(qty ?? 0), 10);
    const q = isNaN(n) ? 0 : Math.max(0, n);
    b[sid] = q;
    d[sid] = 1;
  }catch(_){}
}

function __prodDraftClear_(){
  try{
    const key = __spesaDraftEnsure_();
    state._spesaDraft[key] = {};
    state._spesaDirty[key] = {};
  }catch(_){}
}

// ---- Data helpers ----
function __prodNameKey_(it){
  return String(it?.prodotto || it?.nome || "").trim().toLowerCase();
}

function __spesaNormalizeItem_(it){
  try{
    if (!it) return it;
    if (__normBool01(it.isDeleted)) return it;
    const n = parseInt(String(it.qty ?? 0), 10);
    const q = isNaN(n) ? 0 : Math.max(0, n);
    it.qty = q;
    it.saved = (q > 0) ? 1 : 0;
    it.checked = __normBool01(it.checked) ? 1 : 0;
  }catch(_){}
  return it;
}

// ---- Loaders ----
async function loadProdottiList_(action, bucket, { force=false, showLoader=true } = {}){
  const s = bucket;
  const now = Date.now();
  const ttl = 20000;
  if (!force && s.loaded && (now - (s.loadedAt||0) < ttl)) {
    updateProdottiHomeBlink();
    return s.items || [];
  }

  const res = await api(action, { method:"GET", params:{}, showLoader });
  const rows = Array.isArray(res) ? res : (res && Array.isArray(res.rows) ? res.rows : (res && Array.isArray(res.data) ? res.data : []));
  const items = (rows || []).filter(r => !__normBool01(r.isDeleted));
  items.forEach(__spesaNormalizeItem_);

  // Deduplica per nome prodotto (evita card duplicate) + cleanup backend (best-effort)
  try{
    const map = new Map();
    const dupIds = [];
    (items || []).forEach((it)=>{
      const k = __prodNameKey_(it);
      if (!k) return;
      if (!map.has(k)) { map.set(k, it); return; }
      // duplicato: tieni il primo, marca gli altri come deleted
      try{ if (it && it.id != null) dupIds.push(String(it.id)); }catch(_){}
    });
    const uniq = Array.from(map.values());
    // sostituisci in-place
    items.length = 0;
    uniq.forEach(x=>items.push(x));
    if (dupIds.length){
      // pulizia backend in background, senza bloccare UI
      Promise.all(dupIds.map((id)=>api(action, { method:"PUT", body:{ id:String(id), isDeleted:1, qty:0, saved:0, checked:0 }, showLoader:false })))
        .then(()=>{})
        .catch(()=>{});
    }
  }catch(_){}

  s.items = items;
  s.loaded = true;
  s.loadedAt = now;

  updateProdottiHomeBlink();
  return s.items;
}

async function loadProdotti({ force=false, showLoader=true } = {}){
  const key = __prodListKey_();
  const action = __spesaActionByKey_(key);
  const bucket = __spesaBucketByKey_(key);
  return loadProdottiList_(action, bucket, { force, showLoader });
}

// Carica SEMPRE entrambe le liste (serve per LED Home multi-dispositivo)
async function loadSpesaAll({ force=false, showLoader=true } = {}){
  await Promise.all([
    loadProdottiList_("colazione", state.colazione, { force, showLoader }),
    loadProdottiList_("prodotti_pulizia", state.prodotti_pulizia, { force, showLoader }),
  ]);
  updateProdottiHomeBlink();
  return true;
}

// ---- UI controls ----
function updateProdottiControls_(){
  try{
    const tabC = document.getElementById("prodTabColazione");
    const tabP = document.getElementById("prodTabPulizia");
    const key = __prodListKey_();
    if (tabC) tabC.classList.toggle("is-active", key === "colazione");
    if (tabP) tabP.classList.toggle("is-active", key === "pulizia");
  }catch(_){}
}


function __prodottiScrollToFirst_(){
  try{
    const list = document.getElementById("prodottiList");
    if (!list) return;
    const reset = () => {
      try{
        list.scrollTop = 0;
        if (typeof list.scrollTo === "function") list.scrollTo({ top: 0, left: 0, behavior: "auto" });
      }catch(_){ try{ list.scrollTop = 0; }catch(__){} }
    };
    reset();
    try{ requestAnimationFrame(reset); }catch(_){ }
    try{ setTimeout(reset, 0); }catch(_){ }
    try{ setTimeout(reset, 80); }catch(_){ }
  }catch(_){}
}


function __prodCurrentDomOrderMap_(){
  const map = new Map();
  try{
    const wrap = document.getElementById("prodottiList");
    if (!wrap) return map;
    Array.from(wrap.querySelectorAll(".prod-item-block[data-id]")).forEach((el, idx) => {
      const id = String(el?.dataset?.id || "");
      if (id && !map.has(id)) map.set(id, idx);
    });
  }catch(_){}
  return map;
}

function __prodHoldSortForMultiTap_(delayMs = 2000){
  try{
    const now = Date.now();
    state._prodSpesaSortHold = state._prodSpesaSortHold || {};
    state._prodSpesaSortHold.until = now + Math.max(0, parseInt(String(delayMs || 0), 10) || 0);
    state._prodSpesaSortHold.order = Array.from(__prodCurrentDomOrderMap_().entries());
    if (state._prodSpesaSortHold.timer){
      try{ clearTimeout(state._prodSpesaSortHold.timer); }catch(_){}
      state._prodSpesaSortHold.timer = null;
    }
    state._prodSpesaSortHold.timer = setTimeout(() => {
      try{
        if (state._prodSpesaSortHold){
          state._prodSpesaSortHold.until = 0;
          state._prodSpesaSortHold.order = [];
          state._prodSpesaSortHold.timer = null;
        }
        if (state && state.page === "prodotti") renderProdotti();
      }catch(_){}
    }, Math.max(0, parseInt(String(delayMs || 0), 10) || 0));
  }catch(_){}
}

function __prodHeldOrderMap_(){
  try{
    const h = state && state._prodSpesaSortHold;
    if (!h || !(Number(h.until || 0) > Date.now())) return null;
    const entries = Array.isArray(h.order) ? h.order : [];
    if (!entries.length) return null;
    return new Map(entries.map(([id, idx]) => [String(id), Number(idx)]));
  }catch(_){ return null; }
}

function renderProdotti(){
  const wrap = document.getElementById("prodottiList");
  if (!wrap) return;

  const bucket = __prodStateBucket_();
  const items = (bucket.items || []).filter(r => !__normBool01(r.isDeleted));

  let arr = items.slice();
  const __prodEffectiveQtyForSort__ = (it) => {
    try{
      const draftQty = __prodDraftGetQty_(it && it.id);
      const src = (draftQty !== null && draftQty !== undefined) ? draftQty : (it?.qty ?? 0);
      const n = parseInt(String(src ?? 0), 10);
      return isNaN(n) ? 0 : Math.max(0, n);
    }catch(_){ return 0; }
  };
  const __heldOrderMap__ = __prodHeldOrderMap_();
  arr.sort((a,b)=> {
    const ca = __normBool01(a?.checked) ? 0 : 1;
    const cb = __normBool01(b?.checked) ? 0 : 1;
    if (ca !== cb) return ca - cb;
    if (ca === 0 && cb === 0){
      return __prodNameKey_(a).localeCompare(__prodNameKey_(b), "it", { sensitivity:"base" });
    }

    if (__heldOrderMap__){
      const ia = __heldOrderMap__.has(String(a?.id || "")) ? __heldOrderMap__.get(String(a?.id || "")) : 999999;
      const ib = __heldOrderMap__.has(String(b?.id || "")) ? __heldOrderMap__.get(String(b?.id || "")) : 999999;
      if (ia !== ib) return ia - ib;
    }

    const sa = __prodEffectiveQtyForSort__(a) > 0 ? 0 : 1;
    const sb = __prodEffectiveQtyForSort__(b) > 0 ? 0 : 1;
    if (sa !== sb) return sa - sb;

    return __prodNameKey_(a).localeCompare(__prodNameKey_(b), "it", { sensitivity:"base" });
  });

  wrap.innerHTML = "";
  const frag = document.createDocumentFragment();

  arr.forEach((it) => {
    const block = document.createElement("div");
    block.className = "prod-item-block";
    block.dataset.id = String(it.id || "");

    const row = document.createElement("div");
    row.className = "colazione-item";
    row.dataset.id = String(it.id || "");

    const qtyBtn = document.createElement("button");
    qtyBtn.type = "button";
    qtyBtn.className = "colazione-dot colazione-qtydot";

    const draftQty = __prodDraftGetQty_(it.id);
    const hasDraft = (draftQty !== null);
    const qtySrc = hasDraft ? draftQty : (it.qty ?? 0);
    const n = parseInt(String(qtySrc ?? 0), 10);
    const q = isNaN(n) ? 0 : Math.max(0, n);

    qtyBtn.textContent = q > 0 ? String(q) : "";
    // se NON c'è draft, mostra saved in base alla qty persistita
    const saved = hasDraft ? 0 : (q > 0 ? 1 : 0);
    qtyBtn.classList.toggle("is-saved", !!saved && q > 0);

    const text = document.createElement("div");
    text.className = "colazione-text";
    text.textContent = String(it.prodotto || it.nome || "").trim();

    const checkBtn = document.createElement("button");
    checkBtn.type = "button";
    checkBtn.className = "colazione-dot colazione-checkdot";
    checkBtn.innerHTML = '<svg aria-hidden="true" viewBox="0 0 24 24"><path d="M20 6 9 17l-5-5"></path></svg>';
    checkBtn.classList.toggle("is-checked", __normBool01(it.checked) === 1);

    row.appendChild(qtyBtn);
    row.appendChild(text);
    row.appendChild(checkBtn);

    block.appendChild(row);
    frag.appendChild(block);
  });

  wrap.appendChild(frag);
  updateProdottiControls_();
}

// ---- Setup & handlers ----
function setupProdotti(){
  const btnAdd = document.getElementById("prodAddBtn");
  const btnReset = document.getElementById("prodResetBtn");
  const list = document.getElementById("prodottiList");

  const tabC = document.getElementById("prodTabColazione");
  const tabP = document.getElementById("prodTabPulizia");

  const modal = document.getElementById("prodAddModal");
  const close = document.getElementById("prodAddClose");
  const input = document.getElementById("prodAddInput");
  const toC = document.getElementById("prodAddToColazione");
  const toP = document.getElementById("prodAddToPulizia");

  const openModal = () => {
    if (!modal) return;
    modal.hidden = false;
    try{ modal.setAttribute("aria-hidden", "false"); }catch(_){}
    try{ input && input.focus(); }catch(_){}
  };
  const closeModal = () => {
    if (!modal) return;
    modal.hidden = true;
    try{ modal.setAttribute("aria-hidden", "true"); }catch(_){}
  };

  try{ __prodDraftClear_(); }catch(_){ }

  if (btnAdd) bindFastTap(btnAdd, openModal);
  if (close) bindFastTap(close, closeModal);
  if (modal) modal.addEventListener("click", (e)=>{ if (e.target === modal) closeModal(); });

  const createItem = async (action) => {
    const raw = String(input?.value || "");
    const v = raw.trim();
    if (!v) return;
    const prodotto = v.toUpperCase();
    // Non permettere duplicati (un record unico: update/delete su id)
    try{
      const targetKey = (action === "prodotti_pulizia") ? "pulizia" : "colazione";
      const bucket = __spesaBucketByKey_(targetKey);
      if (!bucket.loaded) { try{ await loadProdottiList_(action, bucket, { force:false, showLoader:false }); }catch(_){ } }
      const key = String(prodotto).trim().toLowerCase();
      const exists = (bucket.items || []).some((it)=>{
        if (__normBool01(it?.isDeleted)) return false;
        return __prodNameKey_(it) === key;
      });
      if (exists){
        try{ toast("Prodotto già presente"); }catch(_){}
        if (input) input.value = "";
        closeModal();
        return;
      }
    }catch(_){}

    if (input) input.value = "";
    closeModal();
    try{
      await api(action, { method:"POST", body:{ op:"create", prodotto }, showLoader:true });
      // reload lista target
      if (action === "colazione") await loadProdottiList_("colazione", state.colazione, { force:true, showLoader:false });
      if (action === "prodotti_pulizia") await loadProdottiList_("prodotti_pulizia", state.prodotti_pulizia, { force:true, showLoader:false });
      renderProdotti();
      updateProdottiHomeBlink();
    }catch(e){ toast(e.message); }
  };

  if (toC) bindFastTap(toC, () => createItem("colazione"));
  if (toP) bindFastTap(toP, () => createItem("prodotti_pulizia"));
  if (input) input.addEventListener("keydown", (e)=>{ if (e.key === "Enter") { e.preventDefault(); createItem(__prodAction_()); } });

  if (tabC) bindFastTap(tabC, async () => {
    state.prodottiUI = state.prodottiUI || { list:"colazione" };
    state.prodottiUI.list = "colazione";
    __prodottiScrollToFirst_();
    await loadProdotti({ force:false, showLoader:true });
    renderProdotti();
    __prodottiScrollToFirst_();
  });

  if (tabP) bindFastTap(tabP, async () => {
    state.prodottiUI = state.prodottiUI || { list:"colazione" };
    state.prodottiUI.list = "pulizia";
    __prodottiScrollToFirst_();
    await loadProdotti({ force:false, showLoader:true });
    renderProdotti();
    __prodottiScrollToFirst_();
  });

  if (btnReset) bindFastTap(btnReset, async () => {
    const action = __prodAction_();
    const bucket = __prodStateBucket_();
    try{
      // UI immediata: azzera qty + checked e pulisci draft
      __prodDraftClear_();
      (bucket.items || []).forEach((it)=>{
        if (__normBool01(it?.isDeleted)) return;
        it.qty = 0;
        it.saved = 0;
        it.checked = 0;
        it.updatedAt = new Date().toISOString();
      });
      renderProdotti();
      updateProdottiHomeBlink();

      // backend (non bloccare)
      api(action, { method:"POST", body:{ op:"resetQty" }, showLoader:false })
        .then(() => loadProdotti({ force:true, showLoader:false }).then(()=>{ renderProdotti(); updateProdottiHomeBlink(); }).catch(()=>{}))
        .catch((e)=>{ try{ toast(e.message || "Errore"); }catch(_){ } });
    }catch(e){ toast(e.message); }
  });

  

  if (!list) return;

  // dDAE_2.736 — iOS PWA: fix interferenza scope funzioni popup colore launcher.
  try{
    if (!list.dataset.ddaeTouchScrollBound){
      list.dataset.ddaeTouchScrollBound = "1";
      let __spesaTouchY = 0;
      let __spesaMoved = false;
      list.addEventListener("touchstart", (e) => {
        try{
          if (!e.touches || !e.touches.length) return;
          __spesaTouchY = e.touches[0].clientY;
          __spesaMoved = false;
        }catch(_){}
      }, { passive:true, capture:true });
      list.addEventListener("touchmove", (e) => {
        try{
          if (!e.touches || !e.touches.length) return;
          if (list.scrollHeight <= list.clientHeight + 2) return;
          const y = e.touches[0].clientY;
          const dy = __spesaTouchY - y;
          if (Math.abs(dy) < 3) return;
          __spesaMoved = true;
          __spesaTouchY = y;
          clearProdQtyPress && clearProdQtyPress();
          clearProdCardPress && clearProdCardPress();
          list.scrollTop = Math.max(0, Math.min(list.scrollHeight - list.clientHeight, list.scrollTop + dy));
          e.preventDefault();
          e.stopPropagation();
        }catch(_){}
      }, { passive:false, capture:true });
      list.addEventListener("touchend", () => { try{ __spesaTouchY = 0; __spesaMoved = false; }catch(_){} }, { passive:true, capture:true });
      list.addEventListener("touchcancel", () => { try{ __spesaTouchY = 0; __spesaMoved = false; }catch(_){} }, { passive:true, capture:true });
    }
  }catch(_){}

  const findItem = (id) => {
    const sid = String(id || "");
    return (__prodStateBucket_().items || []).find(x => String(x.id||"") === sid);
  };

  // Auto-save (0.5s) — nessun salvataggio manuale
  const __prodAuto__ = { timer:null, pending:{} };

  const __prodApplyLocal__ = (id, patch) => {
    const it = findItem(id);
    if (!it) return null;
    try{
      Object.assign(it, patch || {});
      __spesaNormalizeItem_(it);
      it.updatedAt = new Date().toISOString();
    }catch(_){}
    return it;
  };

  const __prodAutoCommit__ = async () => {
    try{
      if (__prodAuto__.timer){ clearTimeout(__prodAuto__.timer); __prodAuto__.timer = null; }
      const entries = Object.entries(__prodAuto__.pending || {});
      if (!entries.length) return;
      __prodAuto__.pending = {};

      const byAction = {};
      entries.forEach(([id, obj]) => {
        const act = String(obj?.action || __prodAction_() || "");
        if (!act) return;
        byAction[act] = byAction[act] || [];
        byAction[act].push({ id, patch: (obj && obj.patch) ? obj.patch : {} });
      });

      // Scritture seriali: su Apps Script/iOS più PUT paralleli sulla lista spesa
      // possono arrivare fuori ordine e causare la perdita del primo check rapido.
      for (const act of Object.keys(byAction)){
        const rows = byAction[act] || [];
        for (const {id, patch} of rows){
          await api(act, { method:"PUT", body: Object.assign({ id:String(id) }, patch || {}), showLoader:false });
        }
      }

      // Mantieni la UI ottimistica già aggiornata; aggiorna solo LED/stato senza
      // ricaricare subito dati remoti che potrebbero non essere ancora propagati.
      try{ updateProdottiHomeBlink(); }catch(_){}
    }catch(e){
      try{ toast(e.message || "Errore"); }catch(_){}
    }
  };

  const __prodAutoSchedule__ = (id, patch, { immediate=false } = {}) => {
    const sid = String(id || "");
    if (!sid) return;
    const action = __prodAction_();
    __prodAuto__.pending[sid] = __prodAuto__.pending[sid] || { action, patch:{} };
    __prodAuto__.pending[sid].action = action;
    __prodAuto__.pending[sid].patch = Object.assign(__prodAuto__.pending[sid].patch || {}, patch || {});

    if (__prodAuto__.timer){ clearTimeout(__prodAuto__.timer); __prodAuto__.timer = null; }
    if (immediate){
      __prodAutoCommit__();
    }else{
      __prodAuto__.timer = setTimeout(__prodAutoCommit__, 500);
    }
  };

  // Event delegation: qty / check

  // Qty dot: tap cycle, long-press (0.5s) azzera quantità
  let prodQtyTimer = null;
  let prodQtyTargetId = null;
  let prodQtyLongFired = false;
  let prodLastQtyTouch = 0;
  // Card: double-tap = elimina, long-press (0.5s) = azzera quantità
  let prodCardTimer = null;
  let prodCardTargetId = null;
  let prodCardLongFired = false;
  let prodCardLongFiredAt = 0;
  let prodCardLastTapAt = 0;
  let prodCardLastTapId = "";

  const clearProdCardPress = () => {
    if (prodCardTimer){ clearTimeout(prodCardTimer); prodCardTimer = null; }
    prodCardTargetId = null;
    prodCardLongFired = false;
  };

  const startProdCardPress = (id) => {
    clearProdCardPress();
    prodCardTargetId = id;
    prodCardTimer = setTimeout(() => {
      prodCardLongFired = true;
      prodCardLongFiredAt = Date.now();
      // Long press (0.5s) sulla card: svuota quantità (pallino)
      const base = getProdBaseQty(id);
      if (base > 0){
        __prodApplyLocal__(id, { qty: 0, saved: 0 });
        renderProdotti();
        updateProdottiHomeBlink();
        // il gesto dura già 0,5s → salva subito
        __prodAutoSchedule__(id, { qty: 0, saved: 0 }, { immediate:true });
      }
    }, 500);
  };



  const clearProdQtyPress = () => {
    if (prodQtyTimer){ clearTimeout(prodQtyTimer); prodQtyTimer = null; }
    prodQtyTargetId = null;
    prodQtyLongFired = false;
  };

  const getProdBaseQty = (id) => {
    const it = findItem(id);
    const n = parseInt(String(it?.qty ?? 0), 10);
    return isNaN(n) ? 0 : Math.max(0, n);
  };

  const cycleProdQty = (id) => {
    const it = findItem(id);
    if (!it) return;
    __prodHoldSortForMultiTap_(2000);
    const base = getProdBaseQty(id);
    const next = (base >= 9) ? 0 : (base + 1);
    __prodApplyLocal__(id, { qty: next, saved: (next > 0 ? 1 : 0) });
    renderProdotti();
    updateProdottiHomeBlink();
    // memorizza automaticamente dopo 0,5s
    __prodAutoSchedule__(id, { qty: next, saved: (next > 0 ? 1 : 0) }, { immediate:false });
  };

  const startProdQtyPress = (id) => {
    clearProdQtyPress();
    prodQtyTargetId = id;
    prodQtyTimer = setTimeout(() => {
      prodQtyLongFired = true;
      // Long press (0.5s): svuota quantità (pallino)
      const base = getProdBaseQty(id);
      if (base > 0){
        try{ __sfxGlass(); }catch(_){ }
        __prodApplyLocal__(id, { qty: 0, saved: 0 });
        renderProdotti();
        updateProdottiHomeBlink();
        // il gesto dura già 0,5s → salva subito
        __prodAutoSchedule__(id, { qty: 0, saved: 0 }, { immediate:true });
      }
    }, 500);
  };

  // Delegation (touch): long-press su qtydot
  list.addEventListener("touchstart", (e) => {
    const row = e.target.closest && e.target.closest(".colazione-item");
    if (!row) return;
    const id = String(row.dataset.id || "");
    if (!id) return;

    if (e.target.closest && e.target.closest(".colazione-qtydot")) {
      prodLastQtyTouch = Date.now();
      startProdQtyPress(id);
      e.preventDefault();
      e.stopPropagation();
      return;
    }

    // Long-press sulla card (0,5s) → azzera quantità
    if (e.target.closest && (e.target.closest(".colazione-checkdot") || e.target.closest(".colazione-qtydot"))) return;
    startProdCardPress(id);
  }, { passive:false, capture:true });

  list.addEventListener("touchend", (e) => {
    const row = e.target.closest && e.target.closest(".colazione-item");
    const id = row ? String(row.dataset.id || "") : "";
    if (!id){
      clearProdQtyPress();
      clearProdCardPress();
      return;
    }

    if (e.target.closest && e.target.closest(".colazione-qtydot")) {
      if (prodQtyTimer){ clearTimeout(prodQtyTimer); prodQtyTimer = null; }
      if (!prodQtyLongFired) cycleProdQty(id);
      clearProdQtyPress();
      e.preventDefault();
      e.stopPropagation();
      return;
    }

    const wasLong = !!prodCardLongFired;
    clearProdCardPress();
    if (wasLong){
      e.preventDefault();
      e.stopPropagation();
      return;
    }
  }, { passive:false, capture:true });

  list.addEventListener("touchcancel", (e) => {
    clearProdQtyPress();
    clearProdCardPress();
    try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
  }, { passive:false, capture:true });


  list.addEventListener("click", (e) => {
    const t = e.target;
    const qtyBtn = t && t.closest ? t.closest(".colazione-qtydot") : null;
    const chkBtn = t && t.closest ? t.closest(".colazione-checkdot") : null;
    const row = t && t.closest ? t.closest(".colazione-item") : null;
    const id = row ? String(row.dataset.id || "") : "";
    if (!id) return;

    // ignore click subito dopo long-press
    if (Date.now() - prodCardLongFiredAt < 700){
      e.preventDefault();
      e.stopPropagation();
      return;
    }

    // qty: ciclo 0->1->2..->9->0 (auto-save 0,5s)
    if (qtyBtn){
      if (Date.now() - prodLastQtyTouch < 450) { e.preventDefault(); e.stopPropagation(); return; }
      e.preventDefault();
      cycleProdQty(id);
      return;
    }

    // check: toggle (auto-save 0,5s)
    if (chkBtn){
      e.preventDefault();
      const it = findItem(id);
      const cur = __normBool01(it?.checked) ? 1 : 0;
      const next = cur ? 0 : 1;
      __prodApplyLocal__(id, { checked: next });
      renderProdotti();
      updateProdottiHomeBlink();
      __prodAutoSchedule__(id, { checked: next }, { immediate:false });
      return;
    }

    // double click/tap sulla card: elimina la card
    const now = Date.now();
    if (prodCardLastTapId === id && (now - prodCardLastTapAt) < 350){
      e.preventDefault();
      prodCardLastTapId = "";
      prodCardLastTapAt = 0;

      __prodApplyLocal__(id, { isDeleted: 1, qty: 0, saved: 0, checked: 0 });
      renderProdotti();
      updateProdottiHomeBlink();
      __prodAutoSchedule__(id, { isDeleted: 1, qty: 0, saved: 0, checked: 0 }, { immediate:true });
      return;
    }
    prodCardLastTapId = id;
    prodCardLastTapAt = now;
  });

  // Prima render (se già caricato)
  updateProdottiControls_();
}





function setupColazione(){
  const input = document.getElementById("colazioneInput");
  const btnAdd = document.getElementById("colazioneAddBtn");
  const btnReset = document.getElementById("colazioneResetBtn");
  const btnSave = document.getElementById("colazioneSaveBtn");
  const list = document.getElementById("colazioneList");

  if (!input || !btnAdd || !btnReset || !btnSave || !list) return;

  const findItem = (id) => {
    const sid = String(id || "");
    return (state.colazione.items || []).find(x => String(x.id||"") === sid);
  };

  const patchItem = async (id, patch, {showLoader=false} = {}) => {
    const it = findItem(id);
    if (it) Object.assign(it, patch, { updatedAt: new Date().toISOString() });
    renderColazione();
    updateProdottiHomeBlink();
    await api("colazione", { method:"PUT", body: Object.assign({ id: String(id) }, patch), showLoader });
    try{ await loadColazione({ force:true, showLoader:false }); }catch(_){ }
    renderColazione();
    updateProdottiHomeBlink();
  };

  const addItem = async () => {
    const raw = String(input.value || "");
    const v = raw.trim();
    if (!v) return;
    const prodotto = v.toUpperCase();
    input.value = "";
    try{
      await api("colazione", { method:"POST", body:{ op:"create", prodotto }, showLoader:true });
      await loadColazione({ force:true, showLoader:false });
      renderColazione();
      updateProdottiHomeBlink();
      input.focus();
    }catch(e){
      toast(e.message);
    }
  };

  bindFastTap(btnAdd, addItem);
  input.addEventListener("keydown", (e) => {
    if (e.key === "Enter") {
      e.preventDefault();
      addItem();
    }
  });

  bindFastTap(btnReset, async () => {
    try{
      await api("colazione", { method:"POST", body:{ op:"resetQty" }, showLoader:true });
      await loadColazione({ force:true, showLoader:false });
      // reset locale qty/saved
      (state.colazione.items || []).forEach(it => { it.qty = 0; it.saved = 0; });
      renderColazione();
      updateProdottiHomeBlink();
    }catch(e){ toast(e.message); }
  });

  bindFastTap(btnSave, async () => {
    try{
      await api("colazione", { method:"POST", body:{ op:"save" }, showLoader:true });
      await loadColazione({ force:true, showLoader:false });
      renderColazione();
      updateProdottiHomeBlink();
    }catch(e){ toast(e.message); }
  });

  // Qty dot: tap increment, long-press (0.5s) reset qty only
  let qtyTimer = null;
  let qtyTargetId = null;
  let qtyLongFired = false;
  let lastQtyTouch = 0;

  const clearQtyPress = () => {
    if (qtyTimer){ clearTimeout(qtyTimer); qtyTimer = null; }
    qtyTargetId = null;
    qtyLongFired = false;
  };

  const startQtyPress = (id) => {
    clearQtyPress();
    qtyTargetId = id;
    qtyTimer = setTimeout(() => {
      qtyLongFired = true;
      try{ __sfxGlass(); }catch(_){ }
      patchItem(id, { qty: 0, saved: 0 }, { showLoader:false }).catch(()=>{});
    }, 500);
  };

  const tapQty = (id) => {
    try{ __sfxTap(); }catch(_){ }
    const it = findItem(id);
    const cur = parseInt(String(it?.qty ?? 0), 10);
    const next = (isNaN(cur) ? 0 : cur) + 1;
    patchItem(id, { qty: next, saved: 0 }, { showLoader:false }).catch(()=>{});
  };

  // Delete: long press 2s on text
  let delTimer = null;
  let delTargetId = null;
  let delFired = false;
  let lastDelTouch = 0;

  const clearDelPress = () => {
    if (delTimer){ clearTimeout(delTimer); delTimer = null; }
    delTargetId = null;
    delFired = false;
  };

  const startDelPress = (id) => {
    clearDelPress();
    delTargetId = id;
    delTimer = setTimeout(() => {
      delFired = true;
      try{ __sfxGlass(); }catch(_){ }
      patchItem(id, { isDeleted: 1, qty: 0, checked: 0, saved: 0 }, { showLoader:true }).catch(()=>{});
    }, 2000);
  };

  const toggleCheck = (id) => {
    try{ __sfxTap(); }catch(_){ }
    const it = findItem(id);
    const cur = __normBool01(it?.checked);
    patchItem(id, { checked: cur ? 0 : 1 }, { showLoader:false }).catch(()=>{});
  };

  // Delegation (touch)
  list.addEventListener("touchstart", (e) => {
    const row = e.target.closest && e.target.closest(".colazione-item");
    if (!row) return;
    const id = row.dataset.id;

    if (e.target.closest && e.target.closest(".colazione-qtydot")) {
      lastQtyTouch = Date.now();
      startQtyPress(id);
      e.preventDefault();
      e.stopPropagation();
      return;
    }

    if (e.target.closest && e.target.closest(".colazione-text")) {
      lastDelTouch = Date.now();
      startDelPress(id);
      e.preventDefault();
      e.stopPropagation();
      return;
    }
  }, { passive:false, capture:true });

  list.addEventListener("touchend", (e) => {
    const row = e.target.closest && e.target.closest(".colazione-item");
    if (!row) return;
    const id = row.dataset.id;

    if (e.target.closest && e.target.closest(".colazione-qtydot")) {
      if (qtyTimer){ clearTimeout(qtyTimer); qtyTimer = null; }
      if (!qtyLongFired) tapQty(id);
      clearQtyPress();
      e.preventDefault();
      e.stopPropagation();
      return;
    }

    if (e.target.closest && e.target.closest(".colazione-text")) {
      if (delTimer){ clearTimeout(delTimer); delTimer = null; }
      if (!delFired) {
        // niente tap sul testo
      }
      clearDelPress();
      e.preventDefault();
      e.stopPropagation();
      return;
    }

    if (e.target.closest && e.target.closest(".colazione-checkdot")) {
      toggleCheck(id);
      e.preventDefault();
      e.stopPropagation();
      return;
    }
  }, { passive:false, capture:true });

  list.addEventListener("touchcancel", (e) => {
    clearQtyPress();
    clearDelPress();
    try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
  }, { passive:false, capture:true });

  // Click (desktop) + anti ghost-click after touch
  list.addEventListener("click", (e) => {
    const row = e.target.closest && e.target.closest(".colazione-item");
    if (!row) return;
    const id = row.dataset.id;

    if (e.target.closest && e.target.closest(".colazione-qtydot")) {
      if (Date.now() - lastQtyTouch < 450) { e.preventDefault(); e.stopPropagation(); return; }
      tapQty(id);
      e.preventDefault();
      e.stopPropagation();
      return;
    }

    if (e.target.closest && e.target.closest(".colazione-checkdot")) {
      if (Date.now() - lastDelTouch < 450) { e.preventDefault(); e.stopPropagation(); return; }
      toggleCheck(id);
      e.preventDefault();
      e.stopPropagation();
      return;
    }
  }, true);
}

function initFloatingLabels(){
  const fields = document.querySelectorAll(".field.float");
  fields.forEach((f) => {
    const control = f.querySelector("input, select, textarea");
    if (!control) return;
    const update = () => {
      const has = !!(control.value && String(control.value).trim().length);
      f.classList.toggle("has-value", has);
    };
    control.addEventListener("input", update);
    control.addEventListener("change", update);
    update();
  });
}


function refreshFloatingLabels(){
  try{
    document.querySelectorAll(".field.float").forEach(f => {
      const c = f.querySelector("input, select, textarea");
      const v = c ? String(c.value ?? "").trim() : "";
      f.classList.toggle("has-value", v.length > 0);
    });
  }catch(_){}
}



/* =========================
   Piscina (dDAE_1.020)
========================= */
const PISCINA_ACTION = "piscina";

function __pad2(n){ return String(n).padStart(2,"0"); }
function __isoDayLocal(d){
  try{
    const x = new Date(d);
    const y = x.getFullYear();
    const m = __pad2(x.getMonth()+1);
    const dd = __pad2(x.getDate());
    return `${y}-${m}-${dd}`;
  }catch(_){ return ""; }
}

function __capFirst(s){
  s = String(s||"");
  if (!s) return s;
  return s.charAt(0).toUpperCase() + s.slice(1);
}

function __fmtItDateLong(d){
  try{
    const fmt = new Intl.DateTimeFormat((typeof __getCurrentLocale__ === 'function' ? __getCurrentLocale__() : "it-IT"), { weekday:"long", day:"2-digit", month:"long", year:"numeric" });
    return __capFirst(fmt.format(d));
  }catch(_){
    try{ return new Date(d).toLocaleDateString((typeof __getCurrentLocale__ === 'function' ? __getCurrentLocale__() : "it-IT")); }catch(__){ return ""; }
  }
}

function __fmtMonthYear(d){
  try{
    const fmt = new Intl.DateTimeFormat((typeof __getCurrentLocale__ === 'function' ? __getCurrentLocale__() : "it-IT"), { month:"long", year:"numeric" });
    return __capFirst(fmt.format(d));
  }catch(_){
    const x = new Date(d);
    return `${x.getMonth()+1}/${x.getFullYear()}`;
  }
}

function __hash01(str){
  try{
    let h = 2166136261;
    const s = String(str||"");
    for (let i=0;i<s.length;i++){
      h ^= s.charCodeAt(i);
      h = Math.imul(h, 16777619);
    }
    // to [0,1)
    return (h >>> 0) / 4294967296;
  }catch(_){ return Math.random(); }
}

function __dayOfYear(d){
  const x = new Date(d);
  const start = new Date(x.getFullYear(), 0, 0);
  const diff = x - start;
  return Math.floor(diff / 86400000);
}

function piscinaSimulateForDay(day){
  const uid = String(state?.session?.user_id || "u");
  const yr = String(state?.exerciseYear || new Date().getFullYear());
  const key = `${uid}|${yr}`;
  const h = __hash01(key);
  const phaseA = h * Math.PI * 2;
  const phaseB = __hash01(key+"b") * Math.PI * 2;

  const doy = __dayOfYear(day);
  const t = doy / 365;

  // Temperatura stagionale (Italia): inverno più bassa, estate più alta
  const tempBase = 20 + 6 * Math.sin(2*Math.PI*(t - 0.22)); // picco ~ estate
  const tempFine = 0.6 * Math.sin(2*Math.PI*(doy/14) + phaseA) + 0.4 * Math.sin(2*Math.PI*(doy/31) + phaseB);
  let temp = tempBase + tempFine;
  temp = Math.max(12, Math.min(30, temp));

  // Cloro libero (ottimale ~ 1.2–1.8)
  const clA = 1.5 + 0.18 * Math.sin(2*Math.PI*(doy/9) + phaseA) + 0.10 * Math.sin(2*Math.PI*(doy/21) + phaseB);
  let clLib = Math.max(1.20, Math.min(1.80, clA));

  // Cloro combinato (ottimale basso 0.0–0.3)
  const ccA = 0.12 + 0.05 * Math.sin(2*Math.PI*(doy/11) + phaseB) + 0.03 * Math.sin(2*Math.PI*(doy/27) + phaseA);
  let clComb = Math.max(0.00, Math.min(0.30, ccA));

  // pH (ottimale ~ 7.2–7.6)
  const phA = 7.4 + 0.10 * Math.sin(2*Math.PI*(doy/13) + phaseA) + 0.06 * Math.sin(2*Math.PI*(doy/33) + phaseB);
  let ph = Math.max(7.20, Math.min(7.60, phA));

  // arrotondamenti “da scheda”
  const round2 = (x)=>Math.round(x*100)/100;
  const round1 = (x)=>Math.round(x*10)/10;

  return {
    cloro_attivo_libero: round2(clLib),
    cloro_attivo_combinato: round2(clComb),
    ph: round2(ph),
    temp_acqua: round1(temp),
  };
}

function piscinaEnsureState(){
  if (!state._piscina) {
    state._piscina = {
      rows: [],
      fetchedAt: 0,
      viewMonth: null,
      byDay: {},
      monthKey: "",
    };
  }
  return state._piscina;
}

function piscinaIndexRows(){
  const s = piscinaEnsureState();
  const map = {};
  (s.rows||[]).forEach(r=>{
    const ts = r.timestamp_report || r.timestamp || r.ts || r.createdAt || r.created_at || "";
    const d = ts ? new Date(ts) : null;
    const dayKey = d && !isNaN(d) ? __isoDayLocal(d) : (r.data || r.date || "");
    if (!dayKey) return;
    // uno per giorno (se duplicati, prende il più recente)
    if (!map[dayKey]) { map[dayKey] = r; return; }
    try{
      const a = new Date(map[dayKey].timestamp_report || map[dayKey].createdAt || 0).getTime() || 0;
      const b = new Date(ts).getTime() || 0;
      if (b >= a) map[dayKey] = r;
    }catch(_){ map[dayKey] = r; }
  });
  s.byDay = map;
}

async function loadPiscinaAll({ force=false, showLoader=false } = {}){
  const s = piscinaEnsureState();
  if (!state?.session?.user_id) return [];
  const age = Date.now() - (s.fetchedAt || 0);
  if (!force && s.fetchedAt && age < 60*1000 && Array.isArray(s.rows)) return s.rows;

  const res = await api(PISCINA_ACTION, { method:"GET", showLoader: !!showLoader, params:{} });
  const rows = Array.isArray(res) ? res
    : (res && Array.isArray(res.data) ? res.data
    : (res && res.data && Array.isArray(res.data.data) ? res.data.data
    : (res && Array.isArray(res.rows) ? res.rows
    : [])));

  s.rows = rows || [];
  s.fetchedAt = Date.now();
  piscinaIndexRows();
  return s.rows;
}

function piscinaGetRowByDayKey(dayKey){
  const s = piscinaEnsureState();
  return s.byDay ? s.byDay[dayKey] : null;
}

function piscinaSetViewMonth(d){
  const s = piscinaEnsureState();
  const x = new Date(d.getFullYear(), d.getMonth(), 1);
  s.viewMonth = x;
  s.monthKey = `${x.getFullYear()}-${__pad2(x.getMonth()+1)}`;
}

function piscinaGetViewMonth(){
  const s = piscinaEnsureState();
  if (!s.viewMonth) piscinaSetViewMonth(new Date());
  return s.viewMonth;
}

function renderPiscinaCalendar(){
  const s = piscinaEnsureState();
  const grid = document.getElementById("piscinaGrid");
  const label = document.getElementById("piscinaNavLabel");
  const todayLbl = document.getElementById("piscinaTodayLabel");
  if (!grid || !label || !todayLbl) return;

  const viewMonth = piscinaGetViewMonth();
  label.textContent = __fmtMonthYear(viewMonth);

  const now = new Date();
  todayLbl.textContent = "";

  const y = viewMonth.getFullYear();
  const m = viewMonth.getMonth();
  const first = new Date(y, m, 1);
  const last = new Date(y, m+1, 0);
  const daysInMonth = last.getDate();

  // offset: lun=0 ... dom=6
  let dow = first.getDay(); // dom=0
  dow = (dow + 6) % 7;

  const frag = document.createDocumentFragment();

  // blank cells
  for (let i=0;i<dow;i++){
    const blank = document.createElement("div");
    blank.className = "piscina-cell";
    blank.setAttribute("disabled","disabled");
    blank.style.visibility = "hidden";
    frag.appendChild(blank);
  }

  for (let day=1; day<=daysInMonth; day++){
    const d = new Date(y, m, day);
    const dayKey = __isoDayLocal(d);
    const btn = document.createElement("button");
    btn.type = "button";
    btn.className = "piscina-cell";
    btn.setAttribute("role","gridcell");
    btn.dataset.date = dayKey;

    const span = document.createElement("div");
    span.className = "piscina-day";
    span.textContent = String(day);
    btn.appendChild(span);

    const row = piscinaGetRowByDayKey(dayKey);
    if (row){
      const dot = document.createElement("div");
      dot.className = "piscina-dot";
      btn.appendChild(dot);
    }

    frag.appendChild(btn);
  }

  grid.innerHTML = "";
  grid.appendChild(frag);
}

function piscinaOpenModal(dayKey){
  const modal = document.getElementById("piscinaModal");
  if (!modal) return;
  const title = document.getElementById("piscinaModalTitle");
  const r = piscinaGetRowByDayKey(dayKey);
  if (!r) { toast("Nessun report per questo giorno"); return; }

  try{
    if (title) title.textContent = `Report piscina — ${__fmtItDateLong(new Date(dayKey+"T00:00:00"))}`;
  }catch(_){}

  const set = (id, v)=>{ const el = document.getElementById(id); if (el) el.textContent = v; };

  set("pmCloroLibero", (r.cloro_attivo_libero ?? "—") + " ppm");
  set("pmCloroComb", (r.cloro_attivo_combinato ?? "—") + " ppm");
  set("pmPh", (r.ph ?? "—"));
  set("pmTemp", (r.temp_acqua ?? "—") + " °C");
  try{ modal.hidden = false; }catch(_){ modal.style.display = "block"; }
}

function piscinaTodayDayKey(){
  const d = new Date();
  return isoDate(new Date(d.getFullYear(), d.getMonth(), d.getDate()));
}

function piscinaOpenEditTodayModal(){
  const modal = document.getElementById("piscinaEditModal");
  if (!modal) return;
  const title = document.getElementById("piscinaEditTitle");
  const dayKey = piscinaTodayDayKey();
  const row = piscinaGetRowByDayKey(dayKey);
  const base = row || piscinaSimulateForDay(new Date(dayKey + "T00:00:00"));
  try{ if (title) title.textContent = `Report piscina — ${__fmtItDateLong(new Date(dayKey+"T00:00:00"))}`; }catch(_){ }
  const setVal = (id, v)=>{ const el = document.getElementById(id); if (el) el.value = (v ?? '') === '' ? '' : String(v); };
  setVal("peCloroLibero", base?.cloro_attivo_libero ?? '');
  setVal("peCloroComb", base?.cloro_attivo_combinato ?? '');
  setVal("pePh", base?.ph ?? '');
  setVal("peTemp", base?.temp_acqua ?? '');
  try{ modal.hidden = false; }catch(_){ modal.style.display = "block"; }
}

function piscinaCloseEditModal(){
  const modal = document.getElementById("piscinaEditModal");
  if (!modal) return;
  try{ modal.hidden = true; }catch(_){ modal.style.display = "none"; }
}

async function piscinaSaveTodayManual(){
  const dayKey = piscinaTodayDayKey();
  const d = new Date(dayKey + "T00:00:00");
  if (isNaN(d)) throw new Error("Data non valida");
  const parseField = (id, label)=>{
    const el = document.getElementById(id);
    const raw = String(el?.value ?? '').trim().replace(',', '.');
    const val = Number(raw);
    if (!raw || !isFinite(val)) throw new Error(`${label} non valido`);
    return val;
  };
  const cloroLibero = parseField("peCloroLibero", "Cloro attivo libero");
  const cloroComb = parseField("peCloroComb", "Cloro attivo combinato");
  const ph = parseField("pePh", "pH");
  const temp = parseField("peTemp", "Temperatura acqua");
  const ts = new Date(d.getFullYear(), d.getMonth(), d.getDate(), 19, 0, 0, 0).toISOString();
  const nowIso = new Date().toISOString();
  const existing = piscinaGetRowByDayKey(dayKey);
  const payload = {
    timestamp_report: ts,
    origine: "manual",
    cloro_attivo_libero: cloroLibero,
    cloro_attivo_combinato: cloroComb,
    ph,
    temp_acqua: temp,
    updatedAt: nowIso,
  };
  const s = piscinaEnsureState();
  if (existing && existing.id) {
    await api(PISCINA_ACTION, { method:"PUT", body: Object.assign({ id:String(existing.id) }, payload), showLoader:false });
    Object.assign(existing, payload);
  } else {
    const created = Object.assign({
      id: genId("piscina"),
      createdAt: nowIso,
    }, payload);
    await api(PISCINA_ACTION, { method:"POST", body: created, showLoader:false });
    s.rows = Array.isArray(s.rows) ? s.rows : [];
    s.rows.push(Object.assign({}, created));
  }
  s.fetchedAt = Date.now();
  piscinaIndexRows();
  renderPiscinaCalendar();
  piscinaCloseEditModal();
  toast("Report piscina salvato", "blue");
  return true;
}

function piscinaCloseModal(){
  const modal = document.getElementById("piscinaModal");
  if (!modal) return;
  try{ modal.hidden = true; }catch(_){ modal.style.display = "none"; }
}

function __piscinaValuesForMonth(viewMonth){
  const y = viewMonth.getFullYear();
  const m = viewMonth.getMonth();
  const last = new Date(y, m+1, 0);
  const days = last.getDate();
  const out = [];
  for (let d=1; d<=days; d++){
    const dayKey = __isoDayLocal(new Date(y,m,d));
    const row = piscinaGetRowByDayKey(dayKey);
    out.push({ day: d, dayKey, row });
  }
  return out;
}

function __piscinaMonthStats__(monthItems){
  const nums = (field) => monthItems.map(x => x.row ? Number(x.row[field]) : null).filter(v => v !== null && !isNaN(v));
  const avg = (arr) => arr.length ? (arr.reduce((a,b)=>a+b,0) / arr.length) : null;
  return {
    totalDays: monthItems.length,
    filledDays: monthItems.filter(x => !!x.row).length,
    cloroLiberoAvg: avg(nums("cloro_attivo_libero")),
    cloroCombAvg: avg(nums("cloro_attivo_combinato")),
    phAvg: avg(nums("ph")),
    tempAvg: avg(nums("temp_acqua")),
  };
}

function __piscinaFmtVal__(x, digits=2, unit=""){
  if (x === null || x === undefined || Number.isNaN(Number(x))) return "—";
  return `${Number(x).toFixed(digits)}${unit}`;
}

function __piscinaPdfFileName__(viewMonth){
  const y = viewMonth.getFullYear();
  const m = String(viewMonth.getMonth() + 1).padStart(2, "0");
  return `report-piscina-${y}-${m}.pdf`;
}

function __piscinaLoadImage__(src){
  return new Promise((resolve, reject) => {
    const img = new Image();
    img.onload = () => resolve(img);
    img.onerror = reject;
    img.src = src;
  });
}

function __piscinaBase64ToBytes__(base64){
  const bin = atob(base64);
  const bytes = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; i += 1) bytes[i] = bin.charCodeAt(i);
  return bytes;
}

function __piscinaPdfFromJpegDataUrl__(jpegDataUrl, imgWidth, imgHeight){
  const base64 = String(jpegDataUrl || '').split(',')[1] || '';
  const imgBytes = __piscinaBase64ToBytes__(base64);
  const pageW = 595.28;
  const pageH = 841.89;
  const enc = new TextEncoder();
  const chunks = [];
  let offset = 0;
  const offsets = [0];

  const pushText = (txt) => {
    const bytes = enc.encode(txt);
    chunks.push(bytes);
    offset += bytes.length;
  };
  const pushBytes = (bytes) => {
    chunks.push(bytes);
    offset += bytes.length;
  };

  pushText(`%PDF-1.4
%ÿÿÿÿ
`);

  const addObj = (id, bodyParts) => {
    offsets[id] = offset;
    pushText(`${id} 0 obj
`);
    bodyParts.forEach((part) => {
      if (typeof part === 'string') pushText(part);
      else pushBytes(part);
    });
    pushText(`
endobj
`);
  };

  addObj(1, [`<< /Type /Catalog /Pages 2 0 R >>
`]);
  addObj(2, [`<< /Type /Pages /Count 1 /Kids [3 0 R] >>
`]);
  addObj(3, [`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${pageW.toFixed(2)} ${pageH.toFixed(2)}] /Resources << /XObject << /Im0 4 0 R >> /ProcSet [/PDF /ImageC] >> /Contents 5 0 R >>
`]);
  addObj(4, [
    `<< /Type /XObject /Subtype /Image /Width ${imgWidth} /Height ${imgHeight} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${imgBytes.length} >>
stream
`,
    imgBytes,
    `
endstream
`
  ]);
  const content = `q
${pageW.toFixed(2)} 0 0 ${pageH.toFixed(2)} 0 0 cm
/Im0 Do
Q
`;
  addObj(5, [`<< /Length ${content.length} >>
stream
${content}endstream
`]);

  const xrefOffset = offset;
  pushText(`xref
0 6
0000000000 65535 f 
`);
  for (let i = 1; i <= 5; i += 1) pushText(`${String(offsets[i]).padStart(10, '0')} 00000 n 
`);
  pushText(`trailer
<< /Size 6 /Root 1 0 R >>
startxref
${xrefOffset}
%%EOF`);

  return new Blob(chunks, { type: 'application/pdf' });
}

async function __piscinaReportCanvas__(viewMonth){
  const monthItems = __piscinaValuesForMonth(viewMonth);
  const rows = monthItems.filter(x => !!x.row);
  if (!rows.length) throw new Error('Nessun report nel mese selezionato');

  const stats = __piscinaMonthStats__(monthItems);
  const canvas = document.createElement('canvas');
  canvas.width = 1240;
  canvas.height = 1754;
  const ctx = canvas.getContext('2d', { alpha:false });
  if (!ctx) throw new Error('Canvas non disponibile');

  const W = canvas.width;
  const H = canvas.height;
  const pad = 44;
  const contentW = W - pad * 2;
  const rowCount = monthItems.length;
  const footerY = H - 28;
  const tableBottomMax = H - 92;
  const tableTop = 688;
  const headerH = 32;
  const rowH = Math.max(22, Math.min(30, Math.floor((tableBottomMax - (tableTop + headerH)) / Math.max(1, rowCount))));
  const tableH = headerH + (rowH * rowCount);
  const chartAreaY = 330;
  const chartAreaH = 288;
  const monthTitle = __fmtMonthYear(viewMonth);
  const logoSrc = `./assets/logo.jpg?v=${(window.APP_VERSION || '2.674')}`;
  const tableFont = rowH <= 23 ? 12 : rowH <= 25 ? 13 : 14;
  const tableHeaderFont = rowH <= 23 ? 13 : 14;
  const colDay = 76;
  const remainingW = contentW - colDay;
  const colRatios = [1.15, 1.15, 0.75, 0.95];
  const ratioSum = colRatios.reduce((a, b) => a + b, 0);
  const colWidths = colRatios.map(v => remainingW * (v / ratioSum));
  const headerXs = [pad, pad + colDay];
  for (let i = 1; i < colWidths.length; i += 1) headerXs.push(headerXs[1] + colWidths.slice(0, i).reduce((a, b) => a + b, 0));

  const drawTextFit = (text, x, y, maxWidth, opts={}) => {
    const { font='600 16px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif', color='#0f172a', align='left' } = opts;
    ctx.save();
    ctx.font = font;
    ctx.fillStyle = color;
    ctx.textAlign = align;
    let out = String(text == null ? '' : text);
    while (out.length > 1 && ctx.measureText(out).width > maxWidth) out = out.slice(0, -1);
    if (out !== text && out.length > 1) out = out.slice(0, -1) + '…';
    ctx.fillText(out, x, y, maxWidth);
    ctx.restore();
  };

  const drawSeriesCard = (x, y, w, h, title, values, opts={}) => {
    const color = opts.color || '#2B7CB4';
    const digits = opts.digits ?? 2;
    const unit = opts.unit || '';
    const nums = values.map(v => Number(v)).filter(v => Number.isFinite(v));
    ctx.fillStyle = '#ffffff';
    ctx.strokeStyle = 'rgba(15,23,42,0.10)';
    ctx.lineWidth = 2;
    roundRect(ctx, x, y, w, h, 20);
    ctx.fill();
    ctx.stroke();

    ctx.fillStyle = color;
    ctx.font = '800 18px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText(title, x + 16, y + 26);

    const info = nums.length
      ? `${Math.min(...nums).toFixed(digits)} / ${Math.max(...nums).toFixed(digits)}${unit}`
      : 'n.d.';
    ctx.fillStyle = 'rgba(15,23,42,0.64)';
    ctx.font = '600 12px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    drawTextFit(info, x + w - 16, y + 26, 120, { font:'600 12px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif', color:'rgba(15,23,42,0.64)', align:'right' });

    const chartX = x + 14;
    const chartY = y + 42;
    const chartW = w - 28;
    const chartH = h - 64;

    ctx.strokeStyle = 'rgba(15,23,42,0.08)';
    ctx.lineWidth = 1;
    for (let i = 0; i <= 2; i += 1) {
      const gy = chartY + (chartH * i / 2);
      ctx.beginPath();
      ctx.moveTo(chartX, gy);
      ctx.lineTo(chartX + chartW, gy);
      ctx.stroke();
    }

    if (!nums.length) {
      ctx.fillStyle = 'rgba(15,23,42,0.42)';
      ctx.font = '600 13px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
      ctx.fillText('Nessun dato', x + 16, y + h - 16);
      return;
    }

    let min = Math.min(...nums);
    let max = Math.max(...nums);
    if (min === max) { min -= 1; max += 1; }
    const points = [];
    monthItems.forEach((item, idx) => {
      const raw = item.row ? Number(item.row[opts.key]) : NaN;
      if (!Number.isFinite(raw)) return;
      const px = chartX + (idx / Math.max(1, rowCount - 1)) * chartW;
      const py = chartY + chartH - ((raw - min) / (max - min)) * chartH;
      points.push([px, py, raw]);
    });

    if (points.length === 1) {
      ctx.fillStyle = color;
      ctx.beginPath();
      ctx.arc(points[0][0], points[0][1], 4, 0, Math.PI * 2);
      ctx.fill();
    } else if (points.length > 1) {
      ctx.strokeStyle = color;
      ctx.lineWidth = 2.5;
      ctx.beginPath();
      points.forEach((p, i) => {
        if (!i) ctx.moveTo(p[0], p[1]);
        else ctx.lineTo(p[0], p[1]);
      });
      ctx.stroke();
      ctx.fillStyle = color;
      points.forEach((p) => {
        ctx.beginPath();
        ctx.arc(p[0], p[1], 2.7, 0, Math.PI * 2);
        ctx.fill();
      });
    }

    const lastVal = nums[nums.length - 1];
    ctx.fillStyle = '#0f172a';
    ctx.font = '800 15px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    drawTextFit(`Ultimo: ${lastVal.toFixed(digits)}${unit}`, x + 16, y + h - 14, w - 32, { font:'800 15px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif', color:'#0f172a' });
  };

  ctx.fillStyle = '#ffffff';
  ctx.fillRect(0, 0, W, H);

  try{
    const logo = await __piscinaLoadImage__(logoSrc);
    ctx.save();
    ctx.beginPath();
    const ls = 76;
    const lx = pad;
    const ly = pad;
    ctx.moveTo(lx + 18, ly);
    ctx.arcTo(lx + ls, ly, lx + ls, ly + ls, 18);
    ctx.arcTo(lx + ls, ly + ls, lx, ly + ls, 18);
    ctx.arcTo(lx, ly + ls, lx, ly, 18);
    ctx.arcTo(lx, ly, lx + ls, ly, 18);
    ctx.closePath();
    ctx.clip();
    ctx.drawImage(logo, lx, ly, ls, ls);
    ctx.restore();
  }catch(_){ }

  const colors = ['#2B7CB4','#4D9CC5','#6FB7D6','#96BFC7','#BFBEA9','#D6B286','#CF9458','#C9772B'];
  const barX = pad + 96;
  const barY = pad + 18;
  const barW = contentW - 96;
  const segW = barW / colors.length;
  colors.forEach((c, i) => {
    ctx.fillStyle = c;
    ctx.fillRect(barX + segW * i, barY, segW + 1, 10);
  });

  ctx.fillStyle = '#2B7CB4';
  ctx.font = '900 36px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText('Report Piscina', pad + 96, pad + 64);
  ctx.fillStyle = '#0f172a';
  ctx.font = '700 22px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText(monthTitle, pad + 96, pad + 95);
  ctx.fillStyle = 'rgba(15,23,42,0.62)';
  ctx.font = '500 17px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';

  const cardsY = 180;
  const cardGap = 16;
  const cardW = (contentW - cardGap * 3) / 4;
  const cardH = 108;
  const summaryCards = [
    { title:'Mese', value:`${stats.filledDays}/${stats.totalDays}`, sub:'report presenti', fill:'#f7fbfe' },
    { title:'Cloro libero', value:__piscinaFmtVal__(stats.cloroLiberoAvg, 2), sub:'media ppm', fill:'#f2f8fc' },
    { title:'Cloro comb.', value:__piscinaFmtVal__(stats.cloroCombAvg, 2), sub:'media ppm', fill:'#fefaf5' },
    { title:'pH · Temp', value:`${__piscinaFmtVal__(stats.phAvg, 2)} · ${__piscinaFmtVal__(stats.tempAvg, 1)}`, sub:'media mese', fill:'#f8f7fd' }
  ];
  summaryCards.forEach((card, idx) => {
    const x = pad + idx * (cardW + cardGap);
    ctx.fillStyle = card.fill;
    ctx.strokeStyle = 'rgba(15,23,42,0.09)';
    ctx.lineWidth = 2;
    roundRect(ctx, x, cardsY, cardW, cardH, 20);
    ctx.fill();
    ctx.stroke();
    ctx.fillStyle = '#2B7CB4';
    ctx.font = '800 18px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    drawTextFit(card.title, x + 16, cardsY + 26, cardW - 32, { font:'800 18px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif', color:'#2B7CB4' });
    ctx.fillStyle = '#0f172a';
    ctx.font = '900 26px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    drawTextFit(card.value, x + 16, cardsY + 60, cardW - 32, { font:'900 26px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif', color:'#0f172a' });
    ctx.fillStyle = 'rgba(15,23,42,0.62)';
    ctx.font = '700 14px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    drawTextFit(card.sub, x + 16, cardsY + 84, cardW - 32, { font:'700 14px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif', color:'rgba(15,23,42,0.62)' });
  });

  ctx.fillStyle = '#2B7CB4';
  ctx.font = '900 22px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText('Andamento compatto', pad, chartAreaY - 12);

  const chartGap = 16;
  const chartW = (contentW - chartGap) / 2;
  const chartH = 132;
  drawSeriesCard(pad, chartAreaY, chartW, chartH, 'Cloro libero', rows.map(x => x.row?.cloro_attivo_libero), { color:'#2B7CB4', digits:2, unit:' ppm', key:'cloro_attivo_libero' });
  drawSeriesCard(pad + chartW + chartGap, chartAreaY, chartW, chartH, 'Cloro combinato', rows.map(x => x.row?.cloro_attivo_combinato), { color:'#C9772B', digits:2, unit:' ppm', key:'cloro_attivo_combinato' });
  drawSeriesCard(pad, chartAreaY + chartH + chartGap, chartW, chartH, 'pH', rows.map(x => x.row?.ph), { color:'#6B5CE7', digits:2, unit:'', key:'ph' });
  drawSeriesCard(pad + chartW + chartGap, chartAreaY + chartH + chartGap, chartW, chartH, 'Temperatura', rows.map(x => x.row?.temp_acqua), { color:'#17A673', digits:1, unit:' °C', key:'temp_acqua' });

  ctx.fillStyle = '#2B7CB4';
  ctx.font = '900 22px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText('Dettaglio giornaliero', pad, tableTop - 14);

  ctx.fillStyle = 'rgba(77,156,197,0.18)';
  roundRect(ctx, pad, tableTop, contentW, headerH, 14);
  ctx.fill();

  const headers = ['G', 'Cloro lib.', 'Cloro comb.', 'pH', 'Temp'];
  const colBoxes = [colDay, ...colWidths];
  headers.forEach((h, idx) => {
    const x = headerXs[idx] + 10;
    const maxWidth = colBoxes[idx] - 20;
    drawTextFit(h, x, tableTop + 21, maxWidth, { font:`800 ${tableHeaderFont}px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif`, color:'#0f172a' });
  });

  monthItems.forEach((item, idx) => {
    const y = tableTop + headerH + (idx * rowH);
    ctx.fillStyle = idx % 2 === 0 ? '#ffffff' : 'rgba(77,156,197,0.05)';
    ctx.fillRect(pad, y, contentW, rowH);
    ctx.strokeStyle = 'rgba(15,23,42,0.10)';
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(pad, y + rowH);
    ctx.lineTo(pad + contentW, y + rowH);
    ctx.stroke();

    const row = item.row || {};
    const vals = [
      String(item.day).padStart(2, '0'),
      item.row ? __piscinaFmtVal__(row.cloro_attivo_libero, 2, '') : '—',
      item.row ? __piscinaFmtVal__(row.cloro_attivo_combinato, 2, '') : '—',
      item.row ? __piscinaFmtVal__(row.ph, 2, '') : '—',
      item.row ? __piscinaFmtVal__(row.temp_acqua, 1, '°') : '—',
    ];
    vals.forEach((v, i) => {
      const x = headerXs[i] + 10;
      const maxWidth = colBoxes[i] - 20;
      drawTextFit(v, x, y + Math.max(15, rowH - 7), maxWidth, { font:`700 ${tableFont}px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif`, color:'#0f172a' });
    });
  });

  ctx.fillStyle = 'rgba(15,23,42,0.55)';
  ctx.font = '500 15px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  const generated = `${(typeof __designTranslate__ === 'function' ? __designTranslate__('Generato il', {en:'Generated on', fr:'Généré le', de:'Erstellt am', es:'Generado el'}) : 'Generato il')} ${new Date().toLocaleString((typeof __getCurrentLocale__ === 'function' ? __getCurrentLocale__() : 'it-IT'))}`;
  const note = rows.length === rowCount ? '· tutti i giorni valorizzati' : `· giorni senza dato: ${rowCount - rows.length}`;
  drawTextFit(`${generated} ${note}`, pad, footerY, contentW, { font:'500 15px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif', color:'rgba(15,23,42,0.55)' });
  return canvas;
}
async function piscinaShareCurrentMonthPdf(){
  const viewMonth = piscinaGetViewMonth();
  const filename = __piscinaPdfFileName__(viewMonth);
  const canvas = await __piscinaReportCanvas__(viewMonth);
  const jpegDataUrl = canvas.toDataURL('image/jpeg', 0.92);
  const pdfBlob = __piscinaPdfFromJpegDataUrl__(jpegDataUrl, canvas.width, canvas.height);
  const file = new File([pdfBlob], filename, { type: 'application/pdf' });

  try{
    if (navigator.canShare && navigator.canShare({ files:[file] })) {
      await navigator.share({ title: `Report piscina ${__fmtMonthYear(viewMonth)}`, files:[file] });
      return true;
    }
  }catch(err){
    if (err && err.name === 'AbortError') return false;
  }

  const url = URL.createObjectURL(pdfBlob);
  try{
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    document.body.appendChild(a);
    try{ a.click(); }catch(_){ }
    try{ document.body.removeChild(a); }catch(_){ }
    toast('PDF report pronto', 'blue');
    return true;
  } finally {
    setTimeout(() => { try{ URL.revokeObjectURL(url); }catch(_){ } }, 2000);
  }
}

async function piscinaCreateReportForDay(dayKey, { origine="auto19" } = {}){
  const s = piscinaEnsureState();
  if (piscinaGetRowByDayKey(dayKey)) return false;

  const d = new Date(dayKey + "T00:00:00");
  if (isNaN(d)) return false;

  const ts = new Date(d.getFullYear(), d.getMonth(), d.getDate(), 19, 0, 0, 0).toISOString();

  const sim = piscinaSimulateForDay(d);
  const payload = {
    id: genId("piscina"),
    timestamp_report: ts,
    origine,
    cloro_attivo_libero: sim.cloro_attivo_libero,
    cloro_attivo_combinato: sim.cloro_attivo_combinato,
    ph: sim.ph,
    temp_acqua: sim.temp_acqua,
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  };

  await api(PISCINA_ACTION, { method:"POST", body: payload, showLoader:false });

  // aggiorna cache locale
  s.rows = Array.isArray(s.rows) ? s.rows : [];
  s.rows.push(Object.assign({}, payload));
  s.fetchedAt = Date.now();
  piscinaIndexRows();
  return true;
}

async function piscinaEnsureDailyReports(){
  if (!state?.session?.user_id) return;
  await loadPiscinaAll({ force:false, showLoader:false });

  const now = new Date();
  const nowDayKey = __isoDayLocal(now);
  const canIncludeToday = (now.getHours() > 19) || (now.getHours() === 19 && now.getMinutes() >= 0);

  const viewMonth = new Date(now.getFullYear(), now.getMonth(), 1);
  const y = viewMonth.getFullYear();
  const m = viewMonth.getMonth();
  const last = new Date(y, m+1, 0);
  const days = last.getDate();

  for (let d=1; d<=days; d++){
    const dayKey = __isoDayLocal(new Date(y,m,d));
    // solo giorni passati + oggi dopo le 19
    if (dayKey > nowDayKey) continue;
    if (dayKey === nowDayKey && !canIncludeToday) continue;
    if (!piscinaGetRowByDayKey(dayKey)){
      try{ await piscinaCreateReportForDay(dayKey, { origine:"auto19" }); }catch(_){}
    }
  }

  if (state.page === "statpiscina") {
    try{ renderPiscinaCalendar(); }catch(_){}
  }
}

async function piscinaBackfillCurrentMonth(){
  if (!state?.session?.user_id) return;
  await loadPiscinaAll({ force:false, showLoader:true });

  const viewMonth = piscinaGetViewMonth();
  const y = viewMonth.getFullYear();
  const m = viewMonth.getMonth();
  const last = new Date(y, m+1, 0);
  const days = last.getDate();

  const now = new Date();
  const nowDayKey = __isoDayLocal(now);
  const isCurrentMonth = (y === now.getFullYear() && m === now.getMonth());

  for (let d=1; d<=days; d++){
    const dayKey = __isoDayLocal(new Date(y,m,d));
    if (isCurrentMonth && dayKey > nowDayKey) break;
    if (!piscinaGetRowByDayKey(dayKey)){
      await piscinaCreateReportForDay(dayKey, { origine:"backfill" });
    }
  }

  if (state.page === "statpiscina") {
    renderPiscinaCalendar();
  }
  toast("Report mese aggiornati");
}

async function piscinaResetCurrentMonthReports(){
  if (!state?.session?.user_id) return;
  const viewMonth = piscinaGetViewMonth();
  const y = viewMonth.getFullYear();
  const m = viewMonth.getMonth();
  const monthLabel = __fmtMonthYear(new Date(y, m, 1));
  const choice = await __confirmTwoActions__(
    `Azzerare tutti i report piscina del mese visualizzato (${monthLabel})?`,
    "Azzera",
    "Annulla"
  );
  if (choice !== "yes") return;

  await loadPiscinaAll({ force:false, showLoader:true });

  const startKey = __isoDayLocal(new Date(y, m, 1));
  const endKey = __isoDayLocal(new Date(y, m + 1, 0));

  const s = piscinaEnsureState();
  const rows = Array.isArray(s.rows) ? s.rows.slice() : [];
  const toDelete = rows.filter((row) => {
    const raw = String(
      row?.timestamp_report ||
      row?.data ||
      row?.date ||
      row?.giorno ||
      ""
    ).trim();
    if (!raw) return false;
    const dayKey = __isoDayLocal(new Date(raw));
    if (!dayKey) return false;
    return dayKey >= startKey && dayKey <= endKey;
  });

  if (!toDelete.length){
    try{ toast(`Nessun report piscina nel mese visualizzato (${monthLabel})`); }catch(_){ }
    return;
  }

  for (const row of toDelete){
    const id = String(row?.id || "").trim();
    if (!id) continue;
    await api(PISCINA_ACTION, { method:"DELETE", params:{ id }, showLoader:false });
  }

  s.rows = rows.filter((row) => {
    const id = String(row?.id || "").trim();
    return !toDelete.some((it) => String(it?.id || "").trim() === id);
  });
  s.fetchedAt = Date.now();
  piscinaIndexRows();

  try{
    if (state.page === "statpiscina") renderPiscinaCalendar();
  }catch(_){}

  try{ toast(`Report mese visualizzato azzerati (${monthLabel})`); }catch(_){ }
}


function __bindPiscinaResetMonthColorHold__(btn){
  try{
    if (!btn || btn.dataset.piscinaResetColorHoldBound === '1') return;
    btn.dataset.piscinaResetColorHoldBound = '1';
    let timer = null;
    let fired = false;
    const clear = ()=>{ try{ if (timer) clearTimeout(timer); }catch(_){ } timer = null; };
    const block = (e)=>{ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } };
    const openPicker = ()=>{
      fired = true;
      __tagColorPopupOpen__('header-action-button', __headerActionVisualFor__(btn.id), (payload) => {
        const current = __headerActionVisualFor__(btn.id);
        const nextVisual = __designVisualFromPayload__(current, payload, current.bg || 'white');
        __writeHeaderActionVisual__(btn.id, nextVisual);
        if (payload && payload.opacity != null) __designBgOpacityWrite__(payload.opacity);
        __headerActionApplyAll__();
        try{ renderRoomSettingsPage(); }catch(_){ }
      }, { supportsBg:true, supportsBorder:true, supportsFg:true, supportsOpacity:true, opacity:__designBgOpacityRead__(), defaultMode:'bg', fallbackBg:(__headerActionVisualFor__(btn.id).bg || 'white'), applyCategory:{ message:'Applicare le modifiche a tutti i tasti della top bar?', confirmYesLabel:'Sì', confirmNoLabel:'No', apply: async(payload, changed) => { await __applyHeaderActionChangesToCategory__(payload, changed); } } });
    };
    const start = (e)=>{
      try{ if (e && e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
      fired = false;
      clear();
      timer = setTimeout(()=>{ openPicker(); }, 500);
    };
    const stop = (e)=>{
      clear();
      if (fired){
        block(e);
        setTimeout(()=>{ fired = false; }, 0);
      }
    };
    ['pointerdown','touchstart','mousedown'].forEach((evt)=>{ try{ btn.addEventListener(evt, start, { passive:true, capture:true }); }catch(_){ } });
    ['pointerup','pointerleave','pointercancel','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach((evt)=>{ try{ btn.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ } });
    try{ btn.addEventListener('click', (e)=>{ if (fired){ block(e); fired = false; } }, true); }catch(_){ }
    try{ btn.addEventListener('contextmenu', (e)=>{ block(e); }, true); }catch(_){ }
  }catch(_){ }
}

function setupPiscina(){
  // default: mese corrente
  try{ piscinaSetViewMonth(new Date()); }catch(_){}

  const prev = document.getElementById("piscinaPrevMonth");
  const next = document.getElementById("piscinaNextMonth");
  const grid = document.getElementById("piscinaGrid");
  const close = document.getElementById("piscinaModalClose");
  const modal = document.getElementById("piscinaModal");
  const shareBtn = document.getElementById("piscinaShareBtn");
  const resetMonthBtn = document.getElementById("piscinaResetMonthBtn");
  try{ __bindPiscinaResetMonthColorHold__(resetMonthBtn); }catch(_){ }
  const editModal = document.getElementById("piscinaEditModal");
  const editCancelBtn = document.getElementById("piscinaEditCancel");
  const editSaveBtn = document.getElementById("piscinaEditSave");

  if (prev) bindFastTap(prev, ()=>{ const vm = piscinaGetViewMonth(); piscinaSetViewMonth(new Date(vm.getFullYear(), vm.getMonth()-1, 1)); renderPiscinaCalendar(); });
  if (next) bindFastTap(next, ()=>{ const vm = piscinaGetViewMonth(); piscinaSetViewMonth(new Date(vm.getFullYear(), vm.getMonth()+1, 1)); renderPiscinaCalendar(); });

  if (grid){
    grid.addEventListener("click", (e)=>{
      const cell = e.target && e.target.closest ? e.target.closest(".piscina-cell") : null;
      if (!cell || cell.hasAttribute("disabled")) return;
      const dayKey = String(cell.dataset.date||"").trim();
      if (!dayKey) return;
      piscinaOpenModal(dayKey);
    });
  }

  if (close) bindFastTap(close, ()=>piscinaCloseModal());
  if (modal){
    modal.addEventListener("click", (e)=>{ if (e.target === modal) piscinaCloseModal(); });
  }
  if (editModal){
    editModal.addEventListener("click", (e)=>{ if (e.target === editModal) piscinaCloseEditModal(); });
  }
  if (shareBtn) bindFastTap(shareBtn, async ()=>{ try{ await piscinaShareCurrentMonthPdf(); }catch(e){ toast(e?.message || "Errore PDF"); } });
  if (resetMonthBtn) bindFastTap(resetMonthBtn, async ()=>{ try{ await piscinaResetCurrentMonthReports(); }catch(e){ toast(e?.message || "Errore azzeramento"); } });
  if (editCancelBtn) bindFastTap(editCancelBtn, ()=>piscinaCloseEditModal());
  if (editSaveBtn) bindFastTap(editSaveBtn, async ()=>{ try{ await piscinaSaveTodayManual(); }catch(e){ toast(e?.message || "Errore salvataggio"); } });

  // scheduler robusto: quando l'app è aperta o torna attiva
  try{
    const tick = ()=>{ try{ piscinaEnsureDailyReports(); }catch(_){ } };
    setTimeout(tick, 350);
    window.addEventListener("focus", tick);
    document.addEventListener("visibilitychange", ()=>{ if (!document.hidden) tick(); });
    // ogni 5 minuti (leggero)
    setInterval(tick, 5*60*1000);
  }catch(_){}
}

async function init(){
  try{ __seedBackupGraphicDefaults__(); }catch(_){}
  // Perf mode: deve girare DOPO che body esiste e DOPO init delle costanti
  applyPerfMode();
  try{ __applyDarkMode__(__isDarkModeEnabled__()); }catch(_){ }
  try{ setupAudioUI(); }catch(_){ }
  try{ applyAppTextUi(document.body); }catch(_){ }
  try{ initAppTextUiObserver(); }catch(_){ }
  const __restore = __readRestoreState();
  state.session = loadSession();
  try{
    const forcedBootSession = await __completeForcedAdminLoginAfterAuthBackupImport__();
    if (forcedBootSession && forcedBootSession.user_id) state.session = forcedBootSession;
  }catch(_){ }
  if (state.session && state.session.user_id){
    try{ const bootCheck = await __ensureSessionLicenseOnBoot__(); if (bootCheck && bootCheck.valid) state.session = bootCheck.session || state.session; }catch(_){ }
  }
  state.exerciseYear = loadExerciseYear();
  updateYearPill();

  document.body.dataset.page = (state.session && state.session.user_id) ? "home" : "auth";
  setupHeader();
  setupAuth();
  setupHome();
  // Check ricevute mancanti (solo a riavvio)
  try{ setTimeout(()=>{ try{ checkReceiptsOnStartup(); }catch(_){ } }, 120); }catch(_){ }
  try{ scheduleTopGuestAlertsRefresh(); }catch(_){ }
  try{ setTimeout(()=>{ try{ refreshTopGuestAlerts({ force:true }); }catch(_){ } }, 180); }catch(_){ }

  try{ applyRoleMode(); }catch(_){ }
  setupCalendario();
  setupImpostazioni();
  try{ __applyRoomsUiConfig__(); }catch(_){ }
  setupOperatoriPage();
  setupChannelPage();
  setupTagColorPopup();
  setupRoomSettingsPage();
  setupLaundryCatalogPage();
setupPiscina();
setupProdotti();
// Avvio: prima cosa dopo il bootstrap UI (utente autenticato) è controllare entrambe le liste spesa
try{
  if (state.session && state.session.user_id){
    setTimeout(() => { try{ loadSpesaAll({ force:true, showLoader:false }); }catch(_){ } }, 0);
  }
}catch(_){ }

  // setupColazione legacy (non più usata)
  try{ setupColazione(); }catch(_){}

    setupOspite();
    try{ initServiziUI(); }catch(_){}
  initFloatingLabels();

  // Non chiamare showPage qui: evitiamo doppie navigazioni/render all'avvio
// periodo iniziale
  if (__restore && __restore.preset) state.periodPreset = __restore.preset;
  if (__restore && __restore.period && __restore.period.from && __restore.period.to) {
    setPeriod(__restore.period.from, __restore.period.to);
  } else {
    // LOCAL mode: default periodo = anno esercizio (evita liste vuote e totali a zero su iOS)
    if (typeof __LOCAL_MODE__ !== "undefined" && __LOCAL_MODE__) {
      const y = (state && state.exerciseYear) ? String(state.exerciseYear) : String(new Date().getFullYear());
      const from = `${y}-01-01`;
      const to   = `${y}-12-31`;
      setPeriod(from,to);
    } else {
      const [from,to] = monthRangeISO(new Date());
      setPeriod(from,to);
    }
  }

  // Preset periodo (scroll iOS)
  bindPresetSelect("#periodPreset1");
  bindPresetSelect("#periodPreset2");
  bindPresetSelect("#periodPreset3");
  bindPresetSelect("#periodPreset4");
  setPresetValue(state.periodPreset || "this_month");

  // Ordinamento Spese (lista)
  if (!state.speseSort) state.speseSort = "date";
  const spSort = document.getElementById("speseSort");
  if (spSort){
    spSort.value = state.speseSort;
    spSort.addEventListener("change", () => {
      state.speseSort = spSort.value || "date";
      try { if (state.page === "spese" && state.speseView === "list") renderSpese(); } catch(_){}
    });
  }


  // Periodo automatico (niente tasto Applica)
  bindPeriodAuto("#fromDate", "#toDate");
  bindPeriodAuto("#fromDate2", "#toDate2");
  bindPeriodAuto("#fromDate3", "#toDate3");
  setupGuestListControls();

  $("#spesaData").value = todayISO();
  try{ __setupSpesaCategoryButtons__(); }catch(_){ }
  try{ __setupSpeseCategoryFilterButtons__(); }catch(_){ }

  // Motivazione: se l'utente scrive una variante già esistente, usa la versione canonica
  const mot = $("#spesaMotivazione");
  if (mot) {
    mot.addEventListener("blur", () => {
      const v = collapseSpaces((mot.value || "").trim());
      if (!v) return;
      const canonical = findCanonicalMotivazione(v);
      if (canonical) mot.value = canonical;
      else mot.value = v; // pulizia spazi multipli
    });
  }

  $("#btnSaveSpesa").addEventListener("click", async () => {
    try { await saveSpesa(); } catch(e){ toast(e.message); }
  });


  // prefetch leggero (no await): evita blocchi e “clessidra” ripetute all'avvio
  // Colazione: prefetch per segnale in HOME
  if (state.session && state.session.user_id){
    try { loadColazione({ force:false, showLoader:false }).catch(() => {}); } catch(_){ }
  }

  if (state.session && state.session.user_id){
    try { loadMotivazioni().catch(() => {}); } catch(_){ }
    try { ensureSettingsLoaded({ force:false, showLoader:false }).catch(() => {}); } catch(_){ }
  }

  // avvio: se c'è uno stato di restore (es. dopo sync/import o reload SW),
  // riapri la pagina salvata; altrimenti mantieni HOME come default per login normale.
  const restoredPage = (__restore && __sanitizePage(__restore.page)) ? __sanitizePage(__restore.page) : null;
  const targetPage = (state.session && state.session.user_id)
    ? (restoredPage || "home")
    : "auth";
  showPage(targetPage);
  if (!state.session && __authPendingUser__ && __authUiApi && typeof __authUiApi__.showActivationForUser === 'function'){
    try{ /* activation flow disabled: license managed from settings */ }catch(_){ }
  }
  if (__restore) setTimeout(() => { try { __applyUiState(__restore); } catch(_) {} }, 0);


  // --- Pulizie (solo grafica) ---
  const cleanPrev = document.getElementById("cleanPrev");
  const cleanNext = document.getElementById("cleanNext");
  const cleanToday = document.getElementById("cleanToday");

  const cleanGrid = document.getElementById("cleanGrid");
  const cleanSaveLaundry = document.getElementById("cleanSaveLaundry");
  const cleanSaveHours = document.getElementById("cleanSaveHours");
  const btnLaundryFromPulizie = document.getElementById("topLaundryBtn");
  const btnOrePuliziaFromPulizie = document.getElementById("topWorkBtn");

  const cleanResetLaundry = document.getElementById("cleanResetLaundry");
  const cleanResetHours = document.getElementById("cleanResetHours");
  const cleanResetAll = document.getElementById("cleanResetAll");

  const __CLEAN_COLS__ = () => getLaundryComponentCodes();

  const doResetAllPulizie = async () => {
    if (!ensureCanEditPulizieDay()) return;
    const ok = await confirmYesNo("Resettare tutto?");
    if (!ok) return;

    try{
      try{ __dirtyLaundryRooms = new Set(); __dirtyLaundryCells = new Set(); }catch(_){ }

      const __allSlots = Array.from(document.querySelectorAll(".clean-grid .cell.slot"));
      __allSlots.forEach(el => {
        try{ el.classList.remove("is-saved"); }catch(_){ }
        try{ writeCell(el, 0); }catch(_){ }
        try{ __markLaundryDirty(el); }catch(_){ }
      });

      try{
        (opEls||[]).forEach(r => {
          try{ r.hours.classList.remove("is-saved"); }catch(_){ }
          try{ writeHourDot(r.hours, 0); }catch(_){ }
        });
      }catch(_){ }

      await saveLaundryNow();
      await saveHoursNow();
    }catch(err){
      try{ toast(String(err && err.message || "Errore reset pulizie"), "orange"); }catch(_){ }
    }
  };

  const __bindResetAllCorner = (el) => {
    try{
      if (!el || el.__boundResetAllCorner) return;
      el.__boundResetAllCorner = true;
      bindFastTap(el, doResetAllPulizie);
      el.addEventListener("keydown", (e) => {
        const k = (e && e.key) ? e.key : "";
        if (k === "Enter" || k === " "){
          try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
          try{ doResetAllPulizie(); }catch(_){ }
        }
      }, true);
    }catch(_){ }
  };

  const rebuildPulizieGrid = ({ preserveValues = true } = {}) => {
    try{
      if (!cleanGrid) return;
      const cols = __CLEAN_COLS__();
      const prev = new Map();
      if (preserveValues){
        try{
          cleanGrid.querySelectorAll('.cell.slot').forEach((cell) => {
            const room = String(cell?.dataset?.room || '').trim();
            const col = String(cell?.dataset?.col || '').trim().toUpperCase();
            if (!room || !col) return;
            prev.set(`${room}|${col}`, {
              value: readCell(cell),
              saved: !!cell.classList.contains('is-saved')
            });
          });
        }catch(_){ }
      }
      const count = Math.max(0, Math.min(12, getConfiguredRoomsCount(6)));
      try{ cleanGrid.style.setProperty('--cg-cols', String(Math.max(1, cols.length))); }catch(_){ }
      try{ cleanGrid.style.setProperty('--cg-rows', String(Math.max(1, count + 1))); }catch(_){ }
      try{ cleanGrid.style.gridTemplateRows = `var(--cg-head-h, 50px) repeat(${Math.max(1, count + 1)}, var(--cg-row-h, 50px))`; }catch(_){ }
      try{ cleanGrid.style.gridTemplateColumns = `var(--cg-corner, 40px) repeat(${Math.max(1, cols.length)}, minmax(0, 1fr))`; }catch(_){ }
      const parts = [];
      const laundryCatalogMap = __laundryCatalogMapByCode__(getLaundryCatalogFromSettings());
      parts.push('<div aria-label="Reset pulizie" class="c cell head corner clean-reset-corner" id="cleanResetAll" role="button" tabindex="0"><svg aria-hidden="true" class="cr-icon" viewBox="0 0 24 24"><path d="M6 6l12 12"></path><path d="M18 6L6 18"></path></svg></div>');
      cols.forEach((col) => {
        const item = laundryCatalogMap.get(__normalizeLaundryCode__(col));
        const color = __normalizeLaundryColor__(item?.colore || 'blue');
        const title = String(item?.titolo || col).trim() || col;
        parts.push(`<div class="c cell head laundry-head color-${color}" data-col="${col}" title="${__escapeHtmlBasic__(title)}">${col}</div>`);
      });
      for (let r = 1; r <= count; r++) {
        parts.push(`<div class="c cell room r${r} room-${r}">${r}</div>`);
        cols.forEach((col) => {
          const item = laundryCatalogMap.get(__normalizeLaundryCode__(col));
          const color = __normalizeLaundryColor__(item?.colore || 'blue');
          parts.push(`<div class="c cell slot room-${r} color-${color}" data-col="${col}" data-room="${r}"></div>`);
        });
      }
      parts.push('<div class="c cell room rres room-res">RES</div>');
      cols.forEach((col) => {
        const item = laundryCatalogMap.get(__normalizeLaundryCode__(col));
        const color = __normalizeLaundryColor__(item?.colore || 'blue');
        parts.push(`<div class="c cell slot room-res color-${color}" data-col="${col}" data-room="RES"></div>`);
      });
      cleanGrid.innerHTML = parts.join('');
      try{ __bindResetAllCorner(document.getElementById("cleanResetAll")); }catch(_){ }
      try{ if (typeof cleanGridHandlersBound !== 'undefined') cleanGridHandlersBound = false; }catch(_){ }
      try{ __dirtyLaundryRooms = new Set(); __dirtyLaundryCells = new Set(); }catch(_){ }
      try{
        cleanGrid.querySelectorAll('.cell.slot').forEach((cell) => {
          const room = String(cell?.dataset?.room || '').trim();
          const col = String(cell?.dataset?.col || '').trim().toUpperCase();
          const hit = prev.get(`${room}|${col}`);
          if (!hit) return;
          writeCell(cell, hit.value || 0);
          cell.classList.toggle('is-saved', !!hit.saved && Number(hit.value || 0) > 0);
        });
      }catch(_){ }
    }catch(_){ }
  };

  // --- Autosave (debounce 1s): Pulizie (biancheria) + Ore operatori ---
  let __laundrySaveT = null;
  let __hoursSaveT = null;
  let __savingLaundry = false;
  let __pendingLaundry = false;
  let __laundryRefreshT = null;
  let __savingHours = false;
  let __pendingHours = false;
  // dDAE_1.020: salvataggio PULIZIE per-stanza (evita generazione righe/report inutili)
  // Mantiene UI fluida: nessun "blink" dei numeri durante autosave / refresh.
  let __dirtyLaundryRooms = new Set();   // stanze modificate (solo queste vengono salvate)
  let __dirtyLaundryCells = new Set();   // celle modificate (solo queste ricevono bordo rosso post-save)

  const __recalcDirtyLaundryRoomsFromCells = () => {
    try{
      const next = new Set();
      __dirtyLaundryCells.forEach(el => { try{ if (el && el.dataset && el.dataset.room) next.add(String(el.dataset.room)); }catch(_){ } });
      __dirtyLaundryRooms = next;
    }catch(_){}
  };

  const __markLaundryDirty = (cellEl) => {
    try{
      if (!cellEl || !cellEl.dataset) return;
      const room = String(cellEl.dataset.room || "").trim();
      if (!room) return;
      __dirtyLaundryRooms.add(room);
      __dirtyLaundryCells.add(cellEl);
    }catch(_){}
  };

  function scheduleLaundrySave(){
    try{
      if (__laundrySaveT) clearTimeout(__laundrySaveT);
      __laundrySaveT = setTimeout(() => { try{ saveLaundryNow(); }catch(_){ } }, 1000);
    }catch(_){}
  }

  function schedulePulizieRefresh(){
    try{
      if (__laundryRefreshT) clearTimeout(__laundryRefreshT);
      __laundryRefreshT = setTimeout(() => { try{ loadPulizieForDay({ clearFirst:false }); }catch(_){ } }, 800);
    }catch(_){}
  }

  function scheduleHoursSave(){
    try{
      if (__hoursSaveT) clearTimeout(__hoursSaveT);
      __hoursSaveT = setTimeout(() => { try{ saveHoursNow(); }catch(_){ } }, 1000);
    }catch(_){}
  }

  async function saveLaundryNow(){
    try{
      if (!ensureCanEditPulizieDay()) return;
      if (__savingLaundry){ __pendingLaundry = true; return; }

      // Salva SOLO le stanze effettivamente toccate (evita righe/report inutili)
      try{ __recalcDirtyLaundryRoomsFromCells(); }catch(_){}

      const rooms = Array.from(__dirtyLaundryRooms || []);
      if (!rooms.length) return;

      const touchedCells = Array.from(__dirtyLaundryCells || []);

      __savingLaundry = true;

      const payload = buildPuliziePayload(rooms);
      await api("pulizie", { method:"POST", body: payload, showLoader:false });

      // Post-save UI: nessun refresh/clear → niente blink.
      // Applica bordo rosso (is-saved) solo sulle celle toccate in base al valore corrente.
      touchedCells.forEach(el => {
        try{
          if (!el || !el.classList) return;
          const v = readCell(el);
          el.classList.toggle("is-saved", (typeof v === "number" ? v : parseInt(String(v||0),10)) > 0);
        }catch(_){}
      });

      // Rimuove dal "dirty" le celle salvate in questo batch (le nuove modifiche restano)
      touchedCells.forEach(el => { try{ __dirtyLaundryCells.delete(el); }catch(_){ } });
      try{ __recalcDirtyLaundryRoomsFromCells(); }catch(_){}

    }catch(err){
      try{ toast(String(err && err.message || "Errore salvataggio biancheria")); }catch(_){}
    }finally{
      __savingLaundry = false;
      if (__pendingLaundry){
        __pendingLaundry = false;
        try{ saveLaundryNow(); }catch(_){}
      }
    }
  }


  async function saveHoursNow(){
    try{
      if (!ensureCanEditPulizieDay()) return;
      if (__savingHours){ __pendingHours = true; return; }
      __savingHours = true;

      const isOpSession = !!(state && state.session && isOperatoreSession(state.session));

      const parseRows = (res) => {
        const rows = Array.isArray(res) ? res
          : (res && Array.isArray(res.rows) ? res.rows
          : (res && Array.isArray(res.data) ? res.data
          : (res && res.data && Array.isArray(res.data.data) ? res.data.data
          : [])));
        return Array.isArray(rows) ? rows : [];
      };

      const buildMergedForOperatore = async () => {
        const date = getCleanDate();
        const names = getOperatorNamesFromSettings();
        const hasAnyName = names.some(n => String(n || '').trim());
        if (!hasAnyName) throw new Error("Imposta i nomi operatori in Impostazioni");

        const rawU = String(state.session._op_local || state.session.username || state.session.user || state.session.nome || state.session.name || state.session.email || '').trim();
        if (!rawU) throw new Error('Operatore non valido');
        const normU = rawU.toLowerCase();
        const activeName = (names||[]).find(n => String(n||'').trim().toLowerCase() === normU) || rawU;

        // carica ore esistenti del giorno (per preservare gli altri)
        let existing = [];
        try{
          const res = await api('operatori', { method:'GET', params:{ data: date }, showLoader:false });
          existing = parseRows(res);
        }catch(_){ existing = []; }

        const mapExact = new Map();
        const mapKey = new Map();
        const _max = (a,b)=> (a>b?a:b);
        const _n = (s)=>String(s||"").trim().toLowerCase();
        const _k = (s)=>_n(s).replace(/[^a-z0-9]+/g,"");
        existing.forEach(r=>{
          const raw = (r?.operatore || r?.nome || '');
          const op = _n(raw);
          const key = _k(raw);
          const ore = Number(String(r?.ore ?? 0).replace(',', '.'));
          const v = (!isFinite(ore)) ? 0 : __normalizeHourDotValue__(_max(0, ore));
          if (op) mapExact.set(op, v);
          if (key) mapKey.set(key, v);
        });

        const idxActive = (names||[]).findIndex(n => String(n||'').trim().toLowerCase() === String(activeName||'').trim().toLowerCase());

        const rows = [];
        (names||[]).forEach((nm, idx)=>{
          const name = String(nm||'').trim();
          if (!name) return;

          let hours = 0;
          if (idx === idxActive && idxActive >= 0){
            const el = opEls[idxActive];
            hours = el ? readHourDot(el.hours) : 0;
          } else {
            hours = (mapExact.get(_n(name)) ?? mapKey.get(_k(name)) ?? 0);
          }

          const isActive = (idx === idxActive && idxActive >= 0);
          if (hours > 0 || isActive){
            const benzinaOperatore = getOperatoreBenzinaByName(name, 0);
            const tariffaOperatore = getOperatoreTariffaByName(name, 0);
            rows.push({
              data: date,
              operatore: name,
              ore: hours,
              benzina_euro: (hours > 0 ? benzinaOperatore : 0),
              benzina_unit_euro: benzinaOperatore,
              tariffa_euro: tariffaOperatore,
              colore: (getOperatoreCatalogItemByName(name)?.colore || 'blue')
            });
          }
        });

        return { touched: true, payload: { data: date, operatori: rows, replaceDay: true } };
      };

      const out = isOpSession ? await buildMergedForOperatore() : buildOperatoriPayload();
      const { touched, payload: opPayload } = out || {};
      if (!touched) return;

      await api("operatori", { method:"POST", body: opPayload });
      try{
        // UI: evidenzia subito i pallini ore salvati (cerchio rosso)
        opEls.forEach((r, idx) => {
          try{
            const rowEl = (r.hours && r.hours.closest) ? r.hours.closest('.clean-op-row') : null;
            if (rowEl && rowEl.style && rowEl.style.display === 'none') return;
            const v = readHourDot(r.hours);
            r.hours.classList.toggle("is-saved", v > 0);
          }catch(_){ }
        });
      }catch(_){ }

      try{ await loadOperatoriForDay({ clearFirst:false }); }catch(_){ }
    }catch(err){
      try{ toast(String(err && err.message || "Errore salvataggio ore lavoro")); }catch(_){}
    }finally{
      __savingHours = false;
      if (__pendingHours){
        __pendingHours = false;
        try{ saveHoursNow(); }catch(_){}
      }
    }
  }


  // --- Pulizie: popup descrizioni intestazioni (MAT/SIN/...) ---
  const cleanHeaderModal = document.getElementById("cleanHeaderModal");
  const cleanHeaderText = document.getElementById("cleanHeaderText");
  const cleanHeaderClose = document.getElementById("cleanHeaderClose");

  const getCleanHeaderDescMap = () => {
    const map = {};
    try{
      getLaundryCatalogFromSettings().forEach((item) => {
        const code = __normalizeLaundryCode__(item?.abbreviazione);
        const title = __laundryDisplayTitle__(item, String(item?.titolo || '').trim());
        if (code && title) map[code] = title;
      });
    }catch(_){ }
    return map;
  };

  const openCleanHeaderModal = (code) => {
    if (!cleanHeaderModal || !cleanHeaderText) return;
    const c = String(code || "").trim().toUpperCase();
    const text = getCleanHeaderDescMap()[c] || "";
    if (!text) return;
    cleanHeaderText.textContent = text;
    cleanHeaderModal.hidden = false;
  };

  const closeCleanHeaderModal = () => {
    if (!cleanHeaderModal) return;
    cleanHeaderModal.hidden = true;
  };

  if (cleanHeaderClose){
    cleanHeaderClose.addEventListener("click", (e) => {
      e.preventDefault();
      e.stopPropagation();
      closeCleanHeaderModal();
    }, true);
  }

  if (cleanHeaderModal){
    cleanHeaderModal.addEventListener("click", (e) => {
      // click fuori dalla card chiude
      if (e.target === cleanHeaderModal) closeCleanHeaderModal();
    }, true);
  }



  const readCell = (el) => {
    const v = String(el.textContent || "").trim();
    const n = parseInt(v, 10);
    return isNaN(n) ? 0 : n;
  };
  const writeCell = (el, n) => {
    const val = Math.max(0, parseInt(n || 0, 10) || 0);
    el.textContent = val ? String(val) : "";
  };

  const getCleanDate = () => {
    try{
      const __isOp = !!(state && state.session && isOperatoreSession(state.session));
      if (__isOp) return toISODateLocal(new Date());
    }catch(_){ }
    const d = state.cleanDay ? new Date(state.cleanDay) : new Date();
    return toISODateLocal(d);
  };


  // --- Guard: OPERATORE può modificare SOLO il giorno corrente ---
  const ONLY_TODAY_MSG = "è possibile memorizzare solo i dati del giorno corrente";
  let __onlyTodayToastAt = 0;

  const isCleanDayToday = () => {
    try { return getCleanDate() === toISODateLocal(new Date()); }
    catch (_) { return true; }
  };

  const canEditPulizieDay = () => {
    try {
      const isOp = !!(state && state.session && isOperatoreSession(state.session));
      if (!isOp) return true;
      return isCleanDayToday();
    } catch (_) { return true; }
  };

  const ensureCanEditPulizieDay = () => {
    if (canEditPulizieDay()) return true;
    const now = Date.now();
    if (now - __onlyTodayToastAt > 800) {
      __onlyTodayToastAt = now;
      try { toast(ONLY_TODAY_MSG, "orange"); } catch (_) { try { toast(ONLY_TODAY_MSG); } catch (__){ } }
    }
    return false;
  };


  // --- Ore operatori (foglio "operatori") ---

  // dDAE_1.025 — Operatore: in Pulizie il nome è lo username loggato (non dipende da Impostazioni)
  const __getLoggedOperatorName = () => {
    try{
      if (!(state && state.session)) return "";
      if (!isOperatoreSession(state.session)) return "";
      return String(
        state.session._op_local ||
        state.session.username ||
        state.session.user ||
        state.session.nome ||
        state.session.name ||
        state.session.email ||
        ""
      ).trim();
    }catch(_){ return ""; }
  };

  const __getPulizieOperatorNames = () => {
    const ops = (getActiveOperatorNames ? getActiveOperatorNames() : []).map(x=>String(x||"").trim()).filter(Boolean);
    return Array.from(new Set(ops.filter(Boolean)));
  };

  const __escapeHtmlBasic__ = (value) => String(value || '').replace(/[&<>"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]));
  let opEls = [];

  const __ensurePulizieOperatorRows__ = (names = []) => {
    const host = document.getElementById('cleanOps');
    if (!host) return [];
    const safeNames = Array.from(new Set((Array.isArray(names) ? names : []).map(n => String(n || '').trim()).filter(Boolean)));
    host.innerHTML = safeNames.map((name, idx) => `
<div class="clean-op-row" data-op="${idx+1}" data-name="${__escapeHtmlBasic__(name)}">
<div aria-label="${__escapeHtmlBasic__(name)}" class="clean-op-name"></div>
<button aria-label="Ore ${__escapeHtmlBasic__(name)}" class="clean-hour-dot is-zero" data-value="0" type="button"></button>
</div>`).join('');
    opEls = Array.from(host.querySelectorAll('.clean-op-row')).map((row) => ({
      row,
      name: row.querySelector('.clean-op-name'),
      hours: row.querySelector('.clean-hour-dot')
    })).filter(x => x.row && x.name && x.hours);
    opEls.forEach(r => {
      if (r.hours && !r.hours.dataset.boundHourDot){
        r.hours.dataset.boundHourDot = '1';
        try{ bindHourDot(r.hours); }catch(_){ }
      }
    });
    return opEls;
  };


  const __applyOperatorePulizieVisuals__ = (rowEl, nameEl, hourEl, operatorName, fallbackColor = 'blue') => {
    const hex = getOperatoreColorHexByName(operatorName, fallbackColor || 'blue');
    const soft = (typeof hexToRgba === 'function') ? hexToRgba(hex, 0.22) : 'rgba(111,183,214,0.22)';
    const softStrong = (typeof hexToRgba === 'function') ? hexToRgba(hex, 0.72) : 'rgba(111,183,214,0.72)';
    try{ if (rowEl) rowEl.dataset.color = __normalizeOperatoreColor__(getOperatoreCatalogItemByName(operatorName)?.colore || fallbackColor || 'blue'); }catch(_){ }
    try{ if (nameEl) nameEl.style.color = hex; }catch(_){ }
    try{
      if (hourEl){
        hourEl.style.borderColor = softStrong;
        hourEl.style.color = hex;
        hourEl.style.background = 'rgba(255,255,255,0.80)';
        hourEl.style.boxShadow = `0 10px 22px rgba(15,23,42,0.10), inset 0 0 0 1px rgba(255,255,255,0.35), 0 0 0 4px ${soft}`;
      }
    }catch(_){ }
  };

  const __HOUR_DOT_STEP__ = 0.5;
  const __normalizeHourDotValue__ = (value) => {
    const raw = Number(String(value ?? 0).replace(',', '.'));
    if (!isFinite(raw) || raw <= 0) return 0;
    const stepped = Math.round(raw / __HOUR_DOT_STEP__) * __HOUR_DOT_STEP__;
    return Math.max(0, Math.round(stepped * 100) / 100);
  };
  const readHourDot = (el) => __normalizeHourDotValue__(el?.dataset?.value || 0);
  const writeHourDot = (el, n) => {
    const val = __normalizeHourDotValue__(n);
    el.dataset.value = String(val);
    el.textContent = val ? (__fmtHours_(val) || String(val).replace('.', ',')) : "";
    el.classList.toggle("is-zero", !val);
  };

  const bindHourDot = (el) => {
    // Tap incrementa, long press (0.5s) azzera — come la biancheria
    let pressTimer = null;
    let longFired = false;
    let lastTouchAt = 0;

    const clear = () => {
      if (pressTimer){ clearTimeout(pressTimer); pressTimer = null; }
      longFired = false;
    };

    const onLong = () => { if (!ensureCanEditPulizieDay()) return;  try{ __sfxGlass(); }catch(_){ } el.classList.remove("is-saved"); writeHourDot(el, 0); scheduleHoursSave(); };
    const onTap = () => { if (!ensureCanEditPulizieDay()) return;  try{ __sfxTap(); }catch(_){ } el.classList.remove("is-saved"); writeHourDot(el, readHourDot(el) + __HOUR_DOT_STEP__); scheduleHoursSave(); };

    el.addEventListener("touchstart", (e) => {
      lastTouchAt = Date.now();
      clear();
      pressTimer = setTimeout(() => {
        longFired = true;
        onLong();
      }, 500);
      e.preventDefault();
      e.stopPropagation();
    }, { passive: false, capture: true });

    el.addEventListener("touchend", (e) => {
      if (pressTimer){ clearTimeout(pressTimer); pressTimer = null; }
      if (!longFired) onTap();
      clear();
      e.preventDefault();
      e.stopPropagation();
    }, { passive: false, capture: true });

    el.addEventListener("touchcancel", (e) => {
      clear();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){}
    }, { passive: false, capture: true });

    // Click (desktop) + anti ghost-click dopo touch
    el.addEventListener("click", (e) => {
      if (Date.now() - lastTouchAt < 450) { e.preventDefault(); e.stopPropagation(); return; }
      onTap();
      e.preventDefault();
      e.stopPropagation();
    }, true);
  };

  const syncCleanOperators = () => {
    const names = __getPulizieOperatorNames();
    __ensurePulizieOperatorRows__(names);

    const loggedName = (() => { try{ return __getLoggedOperatorName(); }catch(_){ return ''; } })();
    const loggedNorm = String(loggedName || '').trim().toLowerCase();
    const isOpSession = !!(state && state.session && isOperatoreSession(state.session));

    opEls.forEach((r, idx) => {
      const rowEl = r.row || (r.hours && r.hours.closest ? r.hours.closest('.clean-op-row') : null);
      const n = String(names[idx] || rowEl?.dataset?.name || '').trim();
      if (!n || !rowEl) return;

      rowEl.dataset.name = n;
      rowEl.style.display = '';
      r.name.textContent = n;
      r.name.classList.remove('is-placeholder');
      try{ __applyOperatorePulizieVisuals__(rowEl, r.name, r.hours, n, __OPERATORI_COLOR_KEYS__[idx % __OPERATORI_COLOR_KEYS__.length] || 'blue'); }catch(_){ }
      if (!r.hours.dataset.value) writeHourDot(r.hours, 0);
      try {
        r.name.setAttribute('aria-label', n);
        r.hours.setAttribute('aria-label', 'Ore ' + n);
      } catch (_) {}

      if (isOpSession){
        const show = !!loggedNorm && (n.toLowerCase() === loggedNorm || n.toLowerCase().includes(loggedNorm) || loggedNorm.includes(n.toLowerCase()));
        rowEl.style.display = show ? '' : 'none';
        if (!show){
          try{ writeHourDot(r.hours, 0); }catch(_){ }
          try{ r.hours.classList.remove('is-saved'); }catch(_){ }
        }
      }
    });

    if (isOpSession && loggedName){
      const visible = opEls.some(r => (r.row || (r.hours && r.hours.closest ? r.hours.closest('.clean-op-row') : null))?.style.display !== 'none');
      if (!visible){
        __ensurePulizieOperatorRows__([loggedName]);
        opEls.forEach(r => {
          const rowEl = r.row || (r.hours && r.hours.closest ? r.hours.closest('.clean-op-row') : null);
          if (!rowEl) return;
          rowEl.style.display = '';
          rowEl.dataset.name = loggedName;
          r.name.textContent = loggedName;
          try{ __applyOperatorePulizieVisuals__(rowEl, r.name, r.hours, loggedName, 'blue'); }catch(_){ }
          if (!r.hours.dataset.value) writeHourDot(r.hours, 0);
        });
      }
    }
  };


  try{ window.__syncCleanOperators__ = syncCleanOperators; }catch(_){ }

  try{ syncCleanOperators(); }catch(_){}
  try{ rebuildPulizieGrid({ preserveValues:false }); }catch(_){ }

  const buildOperatoriPayload = () => {
    const date = getCleanDate();
    const rows = [];
    const names = __getPulizieOperatorNames(); // [op1, op2, op3] oppure [username,"",""]

    const hasAnyName = names.some(n => String(n || "").trim());
    if (!hasAnyName){
      return { touched: false, payload: { data: date, operatori: [], replaceDay: true } };
    }

    // IMPORTANTE: inviamo ANCHE le ore a 0.
    // Il backend farà "replace" del giorno: cancella i record esistenti per quella data
    // e reinserisce solo quelli con ore > 0. Così un secondo salvataggio SOVRASCRIVE.
    opEls.forEach((r, idx) => {
      const name = String(names[idx] || "").trim();
      if (!name) return; // operatore non configurato

      const hours = readHourDot(r.hours); // può essere 0
      const benzinaOperatore = getOperatoreBenzinaByName(name, 0);
      const tariffaOperatore = getOperatoreTariffaByName(name, 0);
      rows.push({
        data: date,
        operatore: name,
        ore: hours,
        benzina_euro: (hours > 0 ? benzinaOperatore : 0),
        benzina_unit_euro: benzinaOperatore,
        tariffa_euro: tariffaOperatore,
        colore: (getOperatoreCatalogItemByName(name)?.colore || 'blue')
      });
    });

    return { touched: true, payload: { data: date, operatori: rows, replaceDay: true } };
  };



  
  const clearAllSlots = () => {
    document.querySelectorAll(".clean-grid .cell.slot").forEach(el => { el.textContent = ""; el.classList.remove("is-saved"); });
  };

  const applyPulizieRows = (rows, { preserveDirty = false } = {}) => {
    if (!Array.isArray(rows)) rows = [];
    const map = new Map();
    rows.forEach(r => {
      const room = String(r?.stanza || r?.room || r?.Stanza || r?.ROOM || "").trim();
      if (room) map.set(room, r);
    });

    const cols = getLaundryComponentCodes();

    // Snapshot update: aggiorna tutte le celle in un colpo solo (niente clear→repaint→blink)
    document.querySelectorAll(".clean-grid .cell.slot").forEach(cell => {
      try{
        if (!cell || !cell.dataset) return;
        if (preserveDirty && __dirtyLaundryCells && __dirtyLaundryCells.has(cell)) return;

        const room = String(cell.dataset.room || "").trim();
        const col  = String(cell.dataset.col  || "").trim().toUpperCase();
        if (!room || !col) return;

        const r = map.get(room);
        const n = parseInt((r && (r[col] ?? r[col.toLowerCase()])) ?? 0, 10);
        const isPos = (!isNaN(n) && n > 0);

        cell.textContent = isPos ? String(n) : "";
        cell.classList.toggle("is-saved", isPos);
      }catch(_){}
    });
  };

  // --- Ore operatori: carica dal DB per il giorno selezionato (così un nuovo salvataggio SOVRASCRIVE davvero) ---
  const _normOpName = (s) => String(s || "").trim().toLowerCase();
  // chiave "robusta" per tollerare differenze di spazi/punteggiatura post-sync
  const _normOpKey  = (s) => _normOpName(s).replace(/[^a-z0-9]+/g, "");

  const applyOperatoriRows = (rows) => {
    if (!Array.isArray(rows)) rows = [];
    const mapExact = new Map();
    const mapKey = new Map();
    rows.forEach(r => {
      const raw = (r?.operatore || r?.nome || "");
      const op = _normOpName(raw);
      const key = _normOpKey(raw);
      const ore = Number(String(r?.ore ?? 0).replace(',', '.'));
      const v = (!isFinite(ore)) ? 0 : __normalizeHourDotValue__(Math.max(0, ore));
      if (op) mapExact.set(op, v);
      if (key) mapKey.set(key, v);
    });

    const names = __getPulizieOperatorNames(); // [op1, op2, op3] oppure [username,"",""]
    opEls.forEach((r, idx) => {
      const name = String(names[idx] || "").trim();
      if (!name) return; // non configurato (riga nascosta)
      const v = (mapExact.get(_normOpName(name)) ?? mapKey.get(_normOpKey(name)) ?? 0);
      writeHourDot(r.hours, v);
      r.hours.classList.toggle("is-saved", v > 0);
    });
  };

  const loadOperatoriForDay = async ({ clearFirst = true } = {}) => {
    if (clearFirst){
      // azzera dots visivamente (se poi arrivano dati li ripopola)
      const names = __getPulizieOperatorNames();
      opEls.forEach((r, idx) => {
        const name = String(names[idx] || "").trim();
        if (!name) return;
        writeHourDot(r.hours, 0);
        r.hours.classList.remove("is-saved");
      });
    }
    try{
      const data = getCleanDate();
      const res = await api("operatori", { method:"GET", params:{ data }, showLoader:false });

      const rows = Array.isArray(res) ? res
        : (res && Array.isArray(res.rows) ? res.rows
        : (res && Array.isArray(res.data) ? res.data
        : []));
      const rowsY = __filterByExerciseYear__(rows, state.exerciseYear || loadExerciseYear(), ["data","date","createdAt","created_at","updatedAt","updated_at"]);
      applyOperatoriRows(rowsY);
    }catch(_){
      // offline/errore: se clearFirst era true, restano a 0
    }
  };


  const loadPulizieForDay = async ({ clearFirst = true } = {}) => {
    // Quando cambi giorno: griglia subito vuota, poi (se ci sono) applica dati salvati.
    if (clearFirst) clearAllSlots();
    try{
      const day = (state && state.session && isOperatoreSession(state.session)) ? new Date() : (state.cleanDay ? new Date(state.cleanDay) : new Date());
      const data = toISODateLocal(day);
      const res = await api("pulizie", { method:"GET", params:{ data }, showLoader:false });

      // Supporta risposte: array diretto, oppure {data:[...]} / {rows:[...]} / nesting
      const rows = Array.isArray(res) ? res
        : (res && Array.isArray(res.data) ? res.data
        : (res && res.data && Array.isArray(res.data.data) ? res.data.data
        : (res && Array.isArray(res.rows) ? res.rows
        : [])));

      const preserveDirty = (!clearFirst) && (__savingLaundry || (__dirtyLaundryCells && __dirtyLaundryCells.size));
      const rowsY = __filterByExerciseYear__(rows, state.exerciseYear || loadExerciseYear(), ["data","date","createdAt","created_at","updatedAt","updated_at"]);
      applyPulizieRows(rowsY, { preserveDirty });

    }catch(_){
      // offline/errore: se stiamo cambiando giorno, resta vuota; se è un refresh "soft", non tocchiamo
      if (clearFirst) clearAllSlots();
    }
  };

const buildPuliziePayload = (roomsList = null) => {
    const data = getCleanDate();
    const cols = getLaundryComponentCodes();

    let rooms = null;
    if (Array.isArray(roomsList) && roomsList.length){
      rooms = roomsList.map(r => String(r)).filter(Boolean);
    } else {
      // fallback: tutte le stanze presenti nella griglia (1..6, ecc.)
      try{
        rooms = Array.from(new Set(Array.from(document.querySelectorAll('.clean-grid .cell.slot'))
          .map(el => String(el && el.dataset ? el.dataset.room : '').trim())
          .filter(Boolean)));
      }catch(_){ rooms = []; }
    }

    const rows = (rooms||[]).map(stanza => {
      const row = { data, stanza };
      cols.forEach(c => {
        const cell = document.querySelector(`.clean-grid .cell.slot[data-room="${stanza}"][data-col="${c}"]`);
        row[c] = cell ? readCell(cell) : 0;
      });
      return row;
    });

    return { data, rows };
  };

  // Tap incrementa, long press (0.5s) azzera
  let pressTimer = null;
  let pressTarget = null;
  let longFired = false;
  let lastTouchAt = 0;

  const clearPress = () => {
    if (pressTimer){ clearTimeout(pressTimer); pressTimer = null; }
    pressTarget = null;
    longFired = false;
  };

  const startPress = (slot) => {
    if (!ensureCanEditPulizieDay()) return;
    clearPress();
    pressTarget = slot;
    pressTimer = setTimeout(() => {
      longFired = true;
      try{ __sfxGlass(); }catch(_){ }
      slot.classList.remove("is-saved");
      writeCell(slot, 0);
      try{ __markLaundryDirty(slot); }catch(_){ }
      scheduleLaundrySave();
    }, 500);
  };

  const tapSlot = (slot) => {
    if (!ensureCanEditPulizieDay()) return;
    try{ __sfxTap(); }catch(_){ }
    slot.classList.remove("is-saved");
    writeCell(slot, readCell(slot) + 1);
    try{ __markLaundryDirty(slot); }catch(_){ }
    scheduleLaundrySave();
  };

  if (cleanGrid){
    // Header click (MAT/SIN/FED...): mostra descrizione in popup
    let __lastHeadTouchAt = 0;
    const __pickHeadCode = (ev) => {
      const head = ev.target && ev.target.closest ? ev.target.closest(".cell.head") : null;
      if (!head || head.classList.contains("corner")) return null;
      const code = String(head.textContent || "").trim().toUpperCase();
      return getCleanHeaderDescMap()[code] ? code : null;
    };

    cleanGrid.addEventListener("touchend", (e) => {
      const code = __pickHeadCode(e);
      if (!code) return;
      __lastHeadTouchAt = Date.now();
      try{ __sfxTap(); }catch(_){ }
      openCleanHeaderModal(code);
      e.preventDefault();
      e.stopPropagation();
    }, { passive: false, capture: true });

    cleanGrid.addEventListener("click", (e) => {
      const code = __pickHeadCode(e);
      if (!code) return;
      if (Date.now() - __lastHeadTouchAt < 450) { e.preventDefault(); e.stopPropagation(); return; }
      try{ __sfxTap(); }catch(_){ }
      openCleanHeaderModal(code);
      e.preventDefault();
      e.stopPropagation();
    }, true);


    // Touch (iPhone)
    cleanGrid.addEventListener("touchstart", (e) => {
      const slot = e.target.closest && e.target.closest(".cell.slot");
      if (!slot) return;
      lastTouchAt = Date.now();
      startPress(slot);
      // blocca altri handler globali
      e.preventDefault();
      e.stopPropagation();
    }, { passive: false, capture: true });

    cleanGrid.addEventListener("touchend", (e) => {
      const slot = e.target.closest && e.target.closest(".cell.slot");
      if (!slot) return;
      if (pressTimer){ clearTimeout(pressTimer); pressTimer = null; }
      if (!longFired) tapSlot(slot);
      clearPress();
      e.preventDefault();
      e.stopPropagation();
    }, { passive: false, capture: true });

    cleanGrid.addEventListener("touchcancel", (e) => {
      clearPress();
      try{ e.preventDefault(); e.stopPropagation(); }catch(_){}
    }, { passive: false, capture: true });

    // Click (desktop) + anti ghost-click dopo touch
    cleanGrid.addEventListener("click", (e) => {
      const slot = e.target.closest && e.target.closest(".cell.slot");
      if (!slot) return;
      if (Date.now() - lastTouchAt < 450) { e.preventDefault(); e.stopPropagation(); return; }
      tapSlot(slot);
      e.preventDefault();
      e.stopPropagation();
    }, true);
  }

  // Salva biancheria (foglio "pulizie")
if (cleanSaveLaundry){
  cleanSaveLaundry.addEventListener("click", (e) => {
    try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    try{ saveLaundryNow(); }catch(_){ }
  }, true);
}

// Salva ore lavoro (foglio "operatori")
if (cleanSaveHours){
  cleanSaveHours.addEventListener("click", (e) => {
    try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
    try{ saveHoursNow(); }catch(_){ }
  }, true);
}

// Reset SOLO biancheria
if (cleanResetLaundry){
  bindFastTap(cleanResetLaundry, async () => {
    try{
      // azzera tutte le celle biancheria (griglia pulizie)
      // IMPORTANTE: marca tutte le stanze/celle come "dirty" così il salvataggio parte davvero
      // e lo script può cancellare i record quando tutto è a zero.
      try{ __dirtyLaundryRooms = new Set(); __dirtyLaundryCells = new Set(); }catch(_){}

      const __allSlots = Array.from(document.querySelectorAll(".clean-grid .cell.slot"));
      __allSlots.forEach(el => {
        try{ el.classList.remove("is-saved"); }catch(_){ }
        try{ writeCell(el, 0); }catch(_){ }
        try{ __markLaundryDirty(el); }catch(_){ }
      });
      // salva immediatamente
      await saveLaundryNow();
    }catch(err){
      try{ toast(String(err && err.message || "Errore reset biancheria")); }catch(_){ }
    }
  });
}

// Reset SOLO ore (operatori)
if (cleanResetHours){
  bindFastTap(cleanResetHours, async () => {
    try{
      // azzera tutti i pallini ore (solo operatori visibili)
      try{
        (opEls||[]).forEach(r => {
          try{ r.hours.classList.remove("is-saved"); }catch(_){ }
          try{ writeHourDot(r.hours, 0); }catch(_){ }
        });
      }catch(_){ }
      // salva immediatamente
      await saveHoursNow();
    }catch(err){
      try{ toast(String(err && err.message || "Errore reset ore")); }catch(_){ }
    }
  });
}


// Reset TUTTO (Pulizie + Ore) dalla cella corner (con conferma Sì/No)
if (cleanResetAll){
  try{ __bindResetAllCorner(document.getElementById('cleanResetAll')); }catch(_){ }
}

  try{
    window.__ddae_refreshPulizieGrid = async ({ forceReload = false, includeOperatori = true } = {}) => {
      try{ syncCleanOperators(); }catch(_){ }
      try{ rebuildPulizieGrid({ preserveValues: !forceReload }); }catch(_){ }
      try{
        if (state && state.page === "pulizie") {
          await loadPulizieForDay({ clearFirst: forceReload });
          if (includeOperatori) await loadOperatoriForDay({ clearFirst: forceReload });
        }
      }catch(_){ }
    };
    window.__ddae_refreshPulizieOperatori = async ({ clearFirst = false } = {}) => {
      try{ syncCleanOperators(); }catch(_){ }
      try{ await loadOperatoriForDay({ clearFirst }); }catch(_){ }
    };
  }catch(_){ }

  const updateCleanLabel = () => {
    const lab = document.getElementById("cleanDateLabel");
    const base = (state && state.session && isOperatoreSession(state.session)) ? new Date() : (state.cleanDay ? new Date(state.cleanDay) : new Date());
    const day = startOfLocalDay(base);
    if (lab) lab.textContent = formatFullDateIT(day);
    try{ if (state && state.page === "pulizie") __setTopbarCenterLabel__(); }catch(_){ }
  };

  const shiftClean = (deltaDays) => {
    try{
      const __isOp = !!(state && state.session && isOperatoreSession(state.session));
      if (__isOp){
        state.cleanDay = startOfLocalDay(new Date()).toISOString();
        updateCleanLabel();
        try{ loadPulizieForDay(); }catch(_){ }
        try{ loadOperatoriForDay(); }catch(_){ }
        return;
      }
    }catch(_){ }
    const base = state.cleanDay ? new Date(state.cleanDay) : new Date();
    const d = startOfLocalDay(base);
    d.setDate(d.getDate() + deltaDays);
    state.cleanDay = d.toISOString();
    updateCleanLabel();
    try{ loadPulizieForDay(); }catch(_){ }
    try{ loadOperatoriForDay(); }catch(_){ }
  };

  if (cleanPrev) cleanPrev.addEventListener("click", () => shiftClean(-1));
  if (cleanNext) cleanNext.addEventListener("click", () => shiftClean(1));
  if (cleanToday) cleanToday.addEventListener("click", () => {
    state.cleanDay = startOfLocalDay(new Date()).toISOString();
    updateCleanLabel();
    try{ loadPulizieForDay(); }catch(_){ }
    try{ loadOperatoriForDay(); }catch(_){ }
  });

  // inizializza label se apri direttamente la pagina
  try{
    const __isOp = !!(state && state.session && isOperatoreSession(state.session));
    const nav = document.querySelector("#page-pulizie .clean-nav");
    if (nav) nav.style.display = __isOp ? "none" : "";
    if (__isOp){
      state.cleanDay = startOfLocalDay(new Date()).toISOString();
    } else {
      if (!state.cleanDay) state.cleanDay = startOfLocalDay(new Date()).toISOString();
    }
  }catch(_){ if (!state.cleanDay) state.cleanDay = startOfLocalDay(new Date()).toISOString(); }
  updateCleanLabel();
  try{ loadPulizieForDay(); }catch(_){ }
  try{ loadOperatoriForDay(); }catch(_){ }



// --- Lavanderia ---
  const btnLaundryGenerate = document.getElementById("btnLaundryGenerate");
  const btnLaundryGenerateTop = document.getElementById("btnLaundryGenerateTop");
  const btnLaundryPricesTop = document.getElementById("btnLaundryPricesTop");
  const laundryPricesModal = document.getElementById("laundryPricesModal");
  const laundryPricesClose = document.getElementById("laundryPricesClose");
  const laundryPricesCancel = document.getElementById("laundryPricesCancel");
  const laundryPricesSave = document.getElementById("laundryPricesSave");
  try{
    const fromEl = document.getElementById("laundryFrom");
    const toEl   = document.getElementById("laundryTo");
    const trigger = document.getElementById("laundryDateRangeTrigger");
    const modal = document.getElementById("laundryDateRangeModal");
    if (fromEl){ fromEl.addEventListener("change", syncLaundryDateText_); fromEl.addEventListener("input", syncLaundryDateText_); }
    if (toEl){ toEl.addEventListener("change", syncLaundryDateText_); toEl.addEventListener("input", syncLaundryDateText_); }
    if (trigger && !trigger.__boundLaundryRangeModal){
      trigger.__boundLaundryRangeModal = true;
      bindFastTap(trigger, () => { try{ __openLaundryDateRangeModal__(); }catch(_){ } });
    }
    if (modal && !modal.__boundLaundryRangeModal){
      modal.__boundLaundryRangeModal = true;
      const closeModal = () => { try{ __closeLaundryDateRangeModal__(true); }catch(_){ } };
      ["laundryDateRangeModalClose","laundryDateRangeCancel"].forEach((id) => { const el = document.getElementById(id); if (el) bindFastTap(el, closeModal); });
      const applyBtn = document.getElementById("laundryDateRangeApply");
      if (applyBtn) bindFastTap(applyBtn, () => { try{ __applyLaundryDateRangeModal__(); }catch(_){ } });
      const prevBtn = document.getElementById("laundryDateRangePrev");
      if (prevBtn) bindFastTap(prevBtn, () => { try{ const m = __laundryDateRangeState__.month || __guestDateRangeMonthStart__(); __laundryDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()-1, 1); __renderLaundryDateRangeCalendar__(); }catch(_){ } });
      const nextBtn = document.getElementById("laundryDateRangeNext");
      if (nextBtn) bindFastTap(nextBtn, () => { try{ const m = __laundryDateRangeState__.month || __guestDateRangeMonthStart__(); __laundryDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()+1, 1); __renderLaundryDateRangeCalendar__(); }catch(_){ } });
      modal.addEventListener("click", (ev) => { try{ if (ev.target === modal) closeModal(); }catch(_){ } });
      const grid = document.getElementById("laundryDateRangeGrid");
      try{ __bindDateRangeCalendarHold__(grid); }catch(_){ }
      if (grid) grid.addEventListener("click", (ev) => {
        const btn = ev.target.closest?.('.guest-date-range-day[data-date]');
        if (!btn) return;
        const iso = String(btn.dataset.date || '').trim();
        if (!iso) return;
        __selectLaundryDateRangeDay__(iso);
      });
    }
    syncLaundryDateText_();
  }catch(_){ }

if (btnLaundryGenerate){
    bindFastTap(btnLaundryGenerate, async () => {
      try{
        showPage("lavanderia");
        await createLavanderiaReport_();
      }catch(e){
        console.error(e);
        try{ toast(e.message || "Errore"); }catch(_){}
      }
    });
  }

  // Topbar (Lavanderia): Genera report accanto al tasto Home
  if (btnLaundryGenerateTop){
    bindFastTap(btnLaundryGenerateTop, async () => {
      try{
        showPage("lavanderia");
        await createLavanderiaReport_();
      }catch(e){
        console.error(e);
        try{ toast(e.message || "Errore"); }catch(_){ }
      }
    });
  }
  if (btnLaundryPricesTop){
    bindFastTap(btnLaundryPricesTop, async () => {
      try{
        showPage("lavanderia");
        await __openLaundryPricesModal__();
      }catch(e){
        console.error(e);
        try{ toast(e.message || "Errore"); }catch(_){ }
      }
    });
  }
  if (laundryPricesClose && !laundryPricesClose.__boundLaundryPricesClose){
    laundryPricesClose.__boundLaundryPricesClose = true;
    bindFastTap(laundryPricesClose, () => { __closeLaundryPricesModal__(); });
  }
  if (laundryPricesCancel && !laundryPricesCancel.__boundLaundryPricesCancel){
    laundryPricesCancel.__boundLaundryPricesCancel = true;
    bindFastTap(laundryPricesCancel, () => { __closeLaundryPricesModal__(); });
  }
  if (laundryPricesSave && !laundryPricesSave.__boundLaundryPricesSave){
    laundryPricesSave.__boundLaundryPricesSave = true;
    bindFastTap(laundryPricesSave, async () => {
      try{ await __saveLaundryPricesModal__(); }catch(e){ try{ toast(e?.message || "Errore"); }catch(_){ } }
    });
  }
  if (laundryPricesModal && !laundryPricesModal.__boundLaundryPricesBackdrop){
    laundryPricesModal.__boundLaundryPricesBackdrop = true;
    laundryPricesModal.addEventListener('click', (ev) => { if (ev.target === laundryPricesModal) __closeLaundryPricesModal__(); });
  }
if (typeof btnOrePuliziaFromPulizie !== "undefined" && btnOrePuliziaFromPulizie){
    bindFastTap(btnOrePuliziaFromPulizie, () => {
      try{ showPage("orepulizia"); }catch(_){}
    });
  }

  if (typeof btnLaundryFromPulizie !== "undefined" && btnLaundryFromPulizie){
    bindFastTap(btnLaundryFromPulizie, async () => {
      try{
        showPage("lavanderia");
        }catch(e){
        console.error(e);
        try{ toast(e.message || "Errore"); }catch(_){}
      }
    });
  }

}


// ===== CALENDARIO (dDAE_2.856) — cursore giorno + badge stanze occupate =====
function __calendarNormalizeIso__(value){
  try{
    if (value instanceof Date) return isoDate(value);
    const raw = String(value || '').trim();
    if (/^\d{4}-\d{2}-\d{2}$/.test(raw)) return raw;
    const formatted = formatISODateLocal(raw);
    if (formatted) return String(formatted).slice(0,10);
  }catch(_){ }
  return todayISO();
}

function __calendarDateFromIso__(iso){
  try{
    const safe = __calendarNormalizeIso__(iso);
    const d = new Date(safe + 'T00:00:00');
    d.setHours(0,0,0,0);
    return d;
  }catch(_){ const d = new Date(); d.setHours(0,0,0,0); return d; }
}

function __calendarSelectedIso__(){
  try{
    if (!state.calendar) state.calendar = { anchor:new Date(), ready:false, guests:[] };
    if (!state.calendar.selectedDateISO) state.calendar.selectedDateISO = todayISO();
    return __calendarNormalizeIso__(state.calendar.selectedDateISO);
  }catch(_){ return todayISO(); }
}

function __calendarSelectedColumnIndex(anchor){
  try{
    const selected = __calendarDateFromIso__(__calendarSelectedIso__());
    const a = (anchor instanceof Date) ? anchor : new Date(anchor || Date.now());
    if (selected.getFullYear() !== a.getFullYear() || selected.getMonth() !== a.getMonth()) return -1;
    return selected.getDate();
  }catch(_){ return -1; }
}

function __calendarOccupiedRoomsCountForDate__(dateIso){
  try{
    const safeIso = __calendarNormalizeIso__(dateIso);
    const guests = (state && state.calendar && Array.isArray(state.calendar.guests)) ? state.calendar.guests : [];
    const rooms = new Set();
    for (const g of guests){
      try{
        const ciStr = formatISODateLocal(g.check_in || g.checkIn || "");
        const coStr = formatISODateLocal(g.check_out || g.checkOut || "");
        if (!ciStr || !coStr) continue;
        if (!(ciStr <= safeIso && safeIso < coStr)) continue;
        let roomsArr = [];
        const st = g.stanze;
        if (Array.isArray(st)) roomsArr = st;
        else if (st != null && String(st).trim().length) roomsArr = _parseRoomsArr(st);
        roomsArr = normalizeRoomsList(roomsArr, getConfiguredRoomsCount(6));
        roomsArr.forEach((r) => { const n = Number(r || 0) || 0; if (n > 0) rooms.add(String(n)); });
      }catch(_){ }
    }
    return rooms.size;
  }catch(_){ return 0; }
}

function __calendarCheckoutRoomsCountForDate__(dateIso){
  try{
    const safeIso = __calendarNormalizeIso__(dateIso);
    const guests = (state && state.calendar && Array.isArray(state.calendar.guests)) ? state.calendar.guests : [];
    const rooms = new Set();
    for (const g of guests){
      try{
        const coStr = formatISODateLocal(g.check_out || g.checkOut || "");
        if (!coStr || coStr !== safeIso) continue;
        let roomsArr = [];
        const st = g.stanze;
        if (Array.isArray(st)) roomsArr = st;
        else if (st != null && String(st).trim().length) roomsArr = _parseRoomsArr(st);
        roomsArr = normalizeRoomsList(roomsArr, getConfiguredRoomsCount(6));
        roomsArr.forEach((r) => { const n = Number(r || 0) || 0; if (n > 0) rooms.add(String(n)); });
      }catch(_){ }
    }
    return rooms.size;
  }catch(_){ return 0; }
}

function __updateCalendarTomorrowCheckoutBadge__({ show=true } = {}){
  try{
    const btn = document.getElementById('calTomorrowCheckoutBadge');
    if (!btn) return;
    const selected = __calendarDateFromIso__(__calendarSelectedIso__());
    const tomorrowIso = isoDate(addDays(selected, 1));
    const count = __calendarCheckoutRoomsCountForDate__(tomorrowIso);
    btn.textContent = String(count);
    const label = `${__translateText__('Stanze da pulire')} ${tomorrowIso}: ${count}`;
    btn.setAttribute('aria-label', label);
    btn.title = label;
    btn.hidden = !show;
    try{ __applySingleActionButtonVisual__(btn); }catch(_){ }
    try{ __bindSingleActionButtonColorHold__(btn); }catch(_){ }
  }catch(_){ }
}

function __updateCalendarSelectedDayBadges__({ show=true } = {}){
  try{ __updateCalendarTodayOccupancyBadge__({ show }); }catch(_){ }
  try{ __updateCalendarTomorrowCheckoutBadge__({ show }); }catch(_){ }
}

function __updateCalendarTodayOccupancyBadge__({ show=true } = {}){
  try{
    const btn = document.getElementById('calTodayOccupancyBadge');
    if (!btn) return;
    const iso = __calendarSelectedIso__();
    const count = __calendarOccupiedRoomsCountForDate__(iso);
    btn.textContent = String(count);
    const label = `${__translateText__('Stanze occupate')} ${iso}: ${count}`;
    btn.setAttribute('aria-label', label);
    btn.title = label;
    btn.hidden = !show;
    try{ __applySingleActionButtonVisual__(btn); }catch(_){ }
    try{ __bindSingleActionButtonColorHold__(btn); }catch(_){ }
  }catch(_){ }
}

function __hideCalendarTodayOccupancyBadge__(){
  try{ __updateCalendarSelectedDayBadges__({ show:true }); }catch(_){ }
}

// ===== CALENDARIO (dDAE_1.020) =====
function setupCalendario(){
  const pickBtn = document.getElementById("calPickBtn");
  const todayBtn = document.getElementById("calTodayBtn");
  const prevMonthBtn = document.getElementById("calPrevMonthBtn");
  const prevDayBtn = document.getElementById("calPrevDayBtn");
  const nextDayBtn = document.getElementById("calNextDayBtn");
  const prevBtn = document.getElementById("calPrevBtn");
  const nextBtn = document.getElementById("calNextBtn");
  const nextMonthBtn = document.getElementById("calNextMonthBtn");
  const syncBtn = document.getElementById("calSyncBtn");
  const input = document.getElementById("calDateInput");
  const toggleMonthBtn = document.getElementById("calToggleMonthBtn");
  try{ __updateCalendarSelectedDayBadges__({ show:false }); }catch(_){ }

  if (!state.calendar) {
    state.calendar = { anchor: new Date(), ready: false, guests: [] };
  }
  try{ if (!state.calendar.selectedDateISO) state.calendar.selectedDateISO = todayISO(); }catch(_){}
// View mode: "week" (default) / "month"
  state.calendar.viewMode = "month";

  const applyCalendarViewUI = () => {
    const sec = document.getElementById("page-calendario");
    const isMonth = (state.calendar && state.calendar.viewMode === "month");
    try{ if (sec) sec.classList.toggle("is-month-view", !!isMonth); }catch(_){

    try{ if (!isMonth) document.body.classList.remove("cal-month-landscape"); }catch(_){}
}

    if (toggleMonthBtn){
      try{
        toggleMonthBtn.setAttribute("aria-label", __translateText__(isMonth ? "Calendario settimanale" : "Calendario mensile"));
        toggleMonthBtn.classList.toggle("is-active", !!isMonth);
      }catch(_){}
    }
  };


  const __scheduleCalendarFetch = (() => {
    let t = null;
    return ({ force=false, showLoader=false } = {}) => {
      if (!state.calendar) return;
      if (t) { try{ clearTimeout(t); }catch(_){} }
      const req = (state.calendar._reqId = (state.calendar._reqId || 0) + 1);
      state.calendar.loading = true;
      t = setTimeout(async () => {
        const my = req;
        try{
          await ensureCalendarData({ force, showLoader });
        }catch(e){
          console.error(e);
        }finally{
          try{ if (state.calendar && state.calendar._reqId === my) state.calendar.loading = false; }catch(_){}
        }
        try{
          if (state.page === "calendario" && state.calendar && state.calendar._reqId === my){
            renderCalendario();
            try{ __updateCalendarSelectedDayBadges__({ show:true }); }catch(_){ }
          }
        }catch(_){ }
      }, 120);
    };
  })();

  // Sync: forza lettura database (tap-safe iOS PWA)
  if (syncBtn){
    syncBtn.setAttribute("aria-label", __translateText__("Forza lettura database"));
    bindFastTap(syncBtn, async () => {
      try{
        syncBtn.disabled = true;
        syncBtn.classList.add("is-loading");
        if (state.calendar) state.calendar.ready = false;
        await ensureCalendarData({ force:true, showLoader:false });
        renderCalendario();
        try{ toast("Aggiornato"); }catch(_){ }
      }catch(e){
        console.error(e);
        try{ toast(e.message || "Errore"); }catch(_){ }
      }finally{
        syncBtn.classList.remove("is-loading");
        syncBtn.disabled = false;
      }
    });
  }

  const openPicker = () => {
    if (!input) return;
    try { input.value = formatISODateLocal(state.calendar.anchor) || todayISO(); } catch(_) {}
    input.click();
  };

  if (pickBtn) pickBtn.addEventListener("click", openPicker);
  if (input) input.addEventListener("change", () => {
    if (!input.value) return;
    const d = new Date(input.value + "T00:00:00");
    d.setHours(0,0,0,0);
    state.calendar.anchor = d;
    state.calendar.selectedDateISO = isoDate(d);
    renderCalendario();
    __updateCalendarSelectedDayBadges__({ show:true });
    requestAnimationFrame(() => { try{ scrollCalendarMonthToDayLeft(d.getDate()); }catch(_){ } });
    __scheduleCalendarFetch({ force:false, showLoader:false });
  });
  if (todayBtn) todayBtn.addEventListener("click", () => {
    const d = new Date();
    d.setHours(0,0,0,0);
    state.calendar.anchor = d;
    state.calendar.selectedDateISO = isoDate(d);
    renderCalendario();
    __updateCalendarSelectedDayBadges__({ show:true });
    requestAnimationFrame(() => { try{ scrollCalendarMonthToDayLeft(d.getDate()); }catch(_){ } });
    __scheduleCalendarFetch({ force:false, showLoader:false });
  });

  const addMonthsClamped = (dt, delta) => {
    const d = new Date(dt);
    const day = d.getDate();
    // vai al primo del mese per evitare overflow (es. 31 -> mese con 30)
    d.setHours(0,0,0,0);
    d.setDate(1);
    d.setMonth(d.getMonth() + delta);
    const last = new Date(d.getFullYear(), d.getMonth() + 1, 0).getDate();
    d.setDate(Math.min(day, last));
    d.setHours(0,0,0,0);
    return d;
  };

  const shiftAnchorAndRender = (newAnchor, { force=false, scrollDayLeft=null, selectedDate=null } = {}) => {
    newAnchor.setHours(0,0,0,0);
    state.calendar.anchor = newAnchor;
    if (selectedDate){
      state.calendar.selectedDateISO = __calendarNormalizeIso__(selectedDate);
    } else {
      state.calendar.selectedDateISO = isoDate(newAnchor);
    }
    // Render immediato: prima cambia pagina, poi aggiorna i dati
    renderCalendario();
    __updateCalendarSelectedDayBadges__({ show:true });
    if (scrollDayLeft != null){
      requestAnimationFrame(() => {
        try{ scrollCalendarMonthToDayLeft(scrollDayLeft); }catch(_){ }
      });
    }
    // Refresh in background (no loader)
    __scheduleCalendarFetch({ force, showLoader:false });
  };

  const firstOfShiftedMonth = (dt, delta) => {
    const base = (dt instanceof Date) ? dt : new Date(dt || Date.now());
    const d = new Date(base.getFullYear(), base.getMonth() + delta, 1);
    d.setHours(0,0,0,0);
    return d;
  };
  const shiftSelectedDay = (delta) => {
    const current = __calendarDateFromIso__(__calendarSelectedIso__());
    const next = addDays(current, delta);
    next.setHours(0,0,0,0);
    shiftAnchorAndRender(next, { scrollDayLeft: next.getDate(), selectedDate: isoDate(next) });
  };
  if (prevDayBtn) prevDayBtn.addEventListener("click", () => { shiftSelectedDay(-1); });
  if (nextDayBtn) nextDayBtn.addEventListener("click", () => { shiftSelectedDay(1); });
  if (prevMonthBtn) prevMonthBtn.addEventListener("click", () => {
    const d = firstOfShiftedMonth(state.calendar.anchor, -1);
    shiftAnchorAndRender(d, { scrollDayLeft:1, selectedDate: isoDate(d) });
  });
  if (nextMonthBtn) nextMonthBtn.addEventListener("click", () => {
    const d = firstOfShiftedMonth(state.calendar.anchor, 1);
    shiftAnchorAndRender(d, { scrollDayLeft:1, selectedDate: isoDate(d) });
  });
  // Applica stato UI all'avvio
  applyCalendarViewUI();

  // Landscape-fit: ridimensiona subito anche su iOS al cambio orientamento / viewport
  try{
    if (!window.__ddaeCalMonthFitBound){
      window.__ddaeCalMonthFitBound = true;
      const __refreshCalendarLayout = () => {
        try{ __scheduleCalendarioLayoutRefresh(); }catch(_){ }
      };
      window.addEventListener("resize", __refreshCalendarLayout, { passive:true });
      window.addEventListener("orientationchange", __refreshCalendarLayout, { passive:true });
      window.addEventListener("pageshow", __refreshCalendarLayout, { passive:true });
      try{
        if (window.visualViewport){
          window.visualViewport.addEventListener("resize", __refreshCalendarLayout, { passive:true });
        }
      }catch(_){ }
      try{
        const mq = window.matchMedia ? window.matchMedia("(orientation: landscape)") : null;
        if (mq){
          if (typeof mq.addEventListener === 'function') mq.addEventListener('change', __refreshCalendarLayout);
          else if (typeof mq.addListener === 'function') mq.addListener(__refreshCalendarLayout);
        }
      }catch(_){ }
    }
  }catch(_){ }
}



async function ensureCalendarData({ force = false, showLoader = false } = {}) {
  if (!state.calendar) state.calendar = { anchor: new Date(), ready: false, guests: [], rangeKey: "" };

  const anchor = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();

  const mode = "month";
  let winFrom, winTo, rangeKey;

  if (mode === "month"){
    const monthStart = new Date(anchor.getFullYear(), anchor.getMonth(), 1);
    const monthEndEx = new Date(anchor.getFullYear(), anchor.getMonth() + 1, 1);

    // Finestra dati mese: piccolo buffer ai bordi (copre check-in/out adiacenti)
    winFrom = toISO(addDays(monthStart, -3));
    winTo   = toISO(addDays(monthEndEx, 3));
    rangeKey = `M:${winFrom}|${winTo}`;
  } else {
    const start = startOfWeekMonday(anchor);

    // Finestra dati: 2 settimane prima + 2 settimane dopo (evita payload enormi)
    winFrom = toISO(addDays(start, -14));
    winTo   = toISO(addDays(start, 7 + 14));
    rangeKey = `W:${winFrom}|${winTo}`;
  }

  // Se ho già i dati per questa finestra, non ricarico
  if (!force && state.calendar.ready && state.calendar.rangeKey === rangeKey) return;

  // Carica configurazione letti ("stanze") solo se serve (evita loader ad ogni navigazione)
  if (!state.stanzeRows || !state.stanzeRows.length){
    try{ await load({ showLoader }); }catch(_){ }
  }

  const data = await cachedGet("ospiti", { from: winFrom, to: winTo }, { showLoader, ttlMs: 60*1000, force });
  state.calendar.guests = __guestFilterPreventiviRows__(Array.isArray(data) ? data : [], false);
  state.calendar.ready = true;
  state.calendar.rangeKey = rangeKey;
  state.calendar.winFrom = winFrom;
  state.calendar.winTo = winTo;
  state.calendar.fetchedAt = Date.now();
}


function renderCalendario(){
  if (!state.calendar) state.calendar = { anchor: new Date(), ready: false, guests: [] };
  const mode = "month";
  try{ ensureCalendarCellModalBound(); }catch(_){ }

  try{ __setTopbarCenterLabel__(); }catch(_){}

  try{
    const sec = document.getElementById("page-calendario");
    if (sec) sec.classList.toggle("is-month-view", mode === "month");
  }catch(_){}

  try{ applyCalRoomFreeze(mode); }catch(_){ }

  const gWeek = document.getElementById("calGrid");
  const gMonth = document.getElementById("calGridMonth");
  try{
    if (gWeek && mode !== "month") gWeek.hidden = false;
    if (gMonth && mode !== "month") gMonth.hidden = true;
  }catch(_){}

  return renderCalendarioMonth();
}


/* dDAE_2.290 — Calendario: focus giorno corrente corretto + colonna stanze contrasto */
function ensureCalendarFixedRailStructure(){
  const page = document.getElementById("page-calendario");
  if (!page) return {};
  const wrap = page.querySelector(".cal-grid-wrap");
  if (!wrap) return {};

  let rail = document.getElementById("calRoomRail");
  if (!rail){
    rail = document.createElement("div");
    rail.id = "calRoomRail";
    rail.className = "cal-room-rail";
  }

  let daysWrap = document.getElementById("calDaysWrap");
  if (!daysWrap){
    daysWrap = document.createElement("div");
    daysWrap.id = "calDaysWrap";
    daysWrap.className = "cal-days-wrap";
  }

  const gridWeek = document.getElementById("calGrid");
  const gridMonth = document.getElementById("calGridMonth");

  if (!wrap.contains(rail)) wrap.insertBefore(rail, wrap.firstChild || null);
  if (!wrap.contains(daysWrap)) wrap.appendChild(daysWrap);
  if (gridWeek && gridWeek.parentElement !== daysWrap) daysWrap.appendChild(gridWeek);
  if (gridMonth && gridMonth.parentElement !== daysWrap) daysWrap.appendChild(gridMonth);

  try{ wrap.classList.add("has-fixed-room-rail"); }catch(_){ }
  return { wrap, rail, daysWrap, gridWeek, gridMonth };
}

function renderCalendarRoomRail(roomsCount){
  const parts = ensureCalendarFixedRailStructure();
  const rail = parts.rail;
  if (!rail) return;
  rail.replaceChildren();

  const corner = document.createElement("div");
  corner.className = "cal-room-rail-head";
  corner.innerHTML = `<div class="cal-corner-text">ST</div>`;
  rail.appendChild(corner);

  for (let r = 1; r <= roomsCount; r++){
    const pill = document.createElement("div");
    pill.className = `cal-room-rail-pill room-${r}`;
    pill.setAttribute("aria-label", `Stanza ${r}`);
    const rn = document.createElement("span");
    rn.className = "cal-room-num";
    rn.textContent = String(r);
    pill.appendChild(rn);
    rail.appendChild(pill);
  }
}

function applyCalRoomFreeze(mode){
  const parts = ensureCalendarFixedRailStructure();
  if (!parts.wrap) return;
  try{ parts.wrap.dataset.calMode = String(mode || "week"); }catch(_){ }
}

function renderCalendarioWeek(){
  const parts = ensureCalendarFixedRailStructure();
  const grid = parts.gridWeek || document.getElementById("calGrid");
  try{ if (grid) grid.classList.toggle("is-loading", !!(state.calendar && state.calendar.loading)); }catch(_){ }
  const title = document.getElementById("calWeekTitle");
  const input = document.getElementById("calDateInput");
  if (!grid) return;

  grid.replaceChildren();
  const frag = document.createDocumentFragment();

  const anchor = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();
  const start = startOfWeekMonday(anchor);
  const days = Array.from({ length: 7 }, (_, i) => addDays(start, i));

  try{ if (input) input.value = formatISODateLocal(anchor) || todayISO(); }catch(_){ }
  if (title) {
    title.textContent = "";
    title.hidden = true;
  }
  const occ = buildWeekOccupancy(start);
  const roomsCount = getConfiguredRoomsCount(6);
  renderCalendarRoomRail(roomsCount);

  grid.innerHTML = "";

  for (let i = 0; i < 7; i++) {
    const d = days[i];
    const dayPill = document.createElement("div");
    dayPill.className = "cal-cell cal-head";
    dayPill.dataset.dayIndex = String(i + 1);
    if (getCalendarTodayColumnIndexForWeek(anchor) === (i + 1)) dayPill.classList.add('is-today-col');

    const ab = document.createElement("div");
    ab.className = "cal-day-abbrev";
    ab.textContent = ((window.matchMedia && window.matchMedia("(orientation: landscape)").matches) ? weekdayShortIT(d).toUpperCase().slice(0,1) : weekdayShortIT(d).toUpperCase());

    const num = document.createElement("div");
    num.className = "cal-day-num";
    num.textContent = String(d.getDate());

    dayPill.appendChild(ab);
    dayPill.appendChild(num);
    frag.appendChild(dayPill);
  }

  for (let r = 1; r <= roomsCount; r++) {
    for (let i = 0; i < 7; i++) {
      const d = days[i];
      const dIso = isoDate(d);

      const cell = document.createElement("button");
      cell.type = "button";
      cell.className = `cal-cell room-${r}`;
      cell.setAttribute("aria-label", `Stanza ${r}, ${weekdayShortIT(d)} ${d.getDate()}`);
      cell.dataset.date = dIso;
      cell.dataset.room = String(r);
      if (getCalendarTodayColumnIndexForWeek(anchor) === (i + 1)) cell.classList.add('is-today-col');
      const info = occ.get(`${dIso}:${r}`);
      bindCalendarCellActions(cell, { room:r, dateIso:dIso, info: info || null });
      if (info) {
        cell.classList.add("has-booking");
        try{
          const chrome = document.createElement("div");
          chrome.className = "cal-corner-chrome";

          if (info.channelInitial){
            const ch = document.createElement("span");
            ch.className = `cal-channel-tag operatori-tag color-${info.channelColor || "orange"}`;
            ch.setAttribute('style', info.channelStyle || __tagColorInlineStyle__(info.channelColor || 'orange', info.channelTextColor || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }));
            ch.textContent = String(info.channelInitial).slice(0,1).toUpperCase();
            chrome.appendChild(ch);
          }

          const flags = document.createElement("div");
          flags.className = "cal-flags";
          if (info.mOn){ const f = document.createElement("span"); f.className = "cal-flag cal-flag-m"; f.textContent = "M"; flags.appendChild(f); }
          if (info.gOn){ const f = document.createElement("span"); f.className = "cal-flag cal-flag-g"; f.textContent = "G"; flags.appendChild(f); }
          if (info.cOn){ const f = document.createElement("span"); f.className = "cal-flag cal-flag-c"; f.textContent = "C"; flags.appendChild(f); }
          if (flags.childNodes.length) chrome.appendChild(flags);

          if (chrome.childNodes.length) cell.appendChild(chrome);
        }catch(_){ }

        const inner = document.createElement("div");
        inner.className = "cal-cell-inner";

        const full = document.createElement("div");
        full.className = "cal-fullname is-single-cell";
        full.textContent = __calendarGuestDisplayName__(info, 1);
        inner.appendChild(full);

        const dots = document.createElement("div");
        dots.className = "cal-dots";
        const arr = info.dots.slice(0, 4);
        for (const t of arr) {
          const s = document.createElement("span");
          s.className = `bed-dot ${t === "m" ? "bed-dot-m" : t === "s" ? "bed-dot-s" : "bed-dot-c"}`;
          dots.appendChild(s);
        }
        inner.appendChild(dots);
        cell.appendChild(inner);

      }

      frag.appendChild(cell);
    }
  }
  grid.appendChild(frag);
  try{ addCalendarTodayColumnOutline(grid, getCalendarTodayColumnIndexForWeek(anchor), roomsCount + 1); }catch(_){ }
}





function syncCalendarRoomRailHeights(){
  try{
    const rail = document.getElementById("calRoomRail");
    const grid = document.getElementById("calGridMonth");
    const page = document.getElementById("page-calendario");
    const wrap = document.getElementById("calDaysWrap") || document.querySelector("#page-calendario .cal-grid-wrap");
    if (!rail || !grid || !page || !wrap || grid.hidden) return;

    const head = grid.querySelector('.cal-cell.cal-head');
    if (!head) return;

    const rows = [];
    const headH = Math.round(head.getBoundingClientRect().height || head.offsetHeight || 0);
    if (headH <= 0) return;
    rows.push(`${headH}px`);

    const roomsCount = getConfiguredRoomsCount(6);
    for (let r = 1; r <= roomsCount; r++){
      const cell = grid.querySelector(`.cal-cell[data-room="${r}"]`);
      const h = Math.round(cell?.getBoundingClientRect().height || cell?.offsetHeight || headH || 0);
      rows.push(`${Math.max(1, h)}px`);
    }

    rail.style.gridTemplateRows = rows.join(' ');
    rail.style.setProperty('--cal-cell-h', rows[1] || `${headH}px`);
    wrap.style.setProperty('--cal-cell-h', rows[1] || `${headH}px`);

    const headEl = rail.querySelector('.cal-room-rail-head');
    if (headEl) headEl.style.height = rows[0];
    for (let r = 1; r <= roomsCount; r++){
      const pill = rail.querySelector(`.cal-room-rail-pill.room-${r}`);
      if (pill) pill.style.height = rows[r] || '';
    }
  }catch(_){ }
}

function __runCalendarioLayoutRefreshPass__(){
  try{
    if (!state || state.page !== 'calendario') return;
    if (!state.calendar) return;
    if (state.calendar.viewMode === 'month') {
      __fitCalendarioMonthLandscape();
      syncCalendarRoomRailHeights();
    }
    refreshCalendarTodayColumnOutline();
  }catch(_){ }
}

const __scheduleCalendarioLayoutRefresh = (() => {
  let t1 = null;
  let t2 = null;
  let t3 = null;
  return function(){
    try{ if (t1) clearTimeout(t1); }catch(_){ }
    try{ if (t2) clearTimeout(t2); }catch(_){ }
    try{ if (t3) clearTimeout(t3); }catch(_){ }
    const fire = () => {
      try{ requestAnimationFrame(() => { try{ __runCalendarioLayoutRefreshPass__(); }catch(_){ } }); }catch(_){ }
    };
    t1 = setTimeout(fire, 0);
    t2 = setTimeout(fire, 120);
    t3 = setTimeout(fire, 280);
  };
})();

function __fitCalendarioMonthLandscape(){
  try{
    if (!state || state.page !== "calendario") return;
    if (!state.calendar || state.calendar.viewMode !== "month") return;

    const isLandscape = (window.matchMedia && window.matchMedia("(orientation: landscape)").matches);

    // dDAE_1.020: in vista mese su iPad landscape usa tutta la larghezza disponibile (margine 10px L/R)
    try{ document.body.classList.toggle("cal-month-landscape", !!isLandscape); }catch(_){}

    const grid = document.getElementById("calGridMonth");
    const wrap = document.getElementById("calDaysWrap") || document.querySelector("#page-calendario .cal-grid-wrap");
    if (!grid || !wrap) return;

    // Aggiorna abbreviazioni giorni (solo iniziale in landscape)
    try{
      const a = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();
      const ms = new Date(a.getFullYear(), a.getMonth(), 1);
      const abEls = grid.querySelectorAll('.cal-cell.cal-head:not(.cal-corner) .cal-day-abbrev');
      for (let i = 0; i < abEls.length; i++){
        const d = new Date(ms.getFullYear(), ms.getMonth(), i+1);
        let t = weekdayShortIT(d).toUpperCase();
        if (isLandscape) t = t.slice(0,1);
        abEls[i].textContent = t;
      }
    }catch(_){}

    // Pulisci override se non landscape
    if (!isLandscape){
      try{ grid.style.removeProperty("--cal-cell-h"); }catch(_){}
      try{ grid.style.removeProperty("--cal-pill-h"); }catch(_){}
      try{ grid.style.removeProperty("grid-template-columns"); }catch(_){}
      try{ wrap.style.removeProperty("--cal-cell-h"); }catch(_){}
      try{ wrap.style.removeProperty("--cal-pill-h"); }catch(_){}
      // Ripristina template dinamico standard (var day width) se possibile
      try{
        const anchor = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();
        const daysCount = new Date(anchor.getFullYear(), anchor.getMonth() + 1, 0).getDate();
        grid.style.gridTemplateColumns = `repeat(${daysCount}, var(--cal-day-w))`;
      }catch(_){}
      return;
    }

    const anchor = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();
    const daysCount = new Date(anchor.getFullYear(), anchor.getMonth() + 1, 0).getDate();

    // Misure reali (px)
    let wrapW = wrap.clientWidth || 0;
    // Rispetta padding/margini interni (10px L/R) se presenti
    try{
      const stWrap = getComputedStyle(wrap);
      const pl = parseFloat(stWrap.paddingLeft || "0") || 0;
      const pr = parseFloat(stWrap.paddingRight || "0") || 0;
      wrapW = Math.max(0, wrapW - pl - pr);
    }catch(_){}
    if (wrapW <= 0) return;

    // La colonna stanze è fuori dalla griglia scrollabile

    // Gap colonna
    let gap = 0;
    try{
      const st = getComputedStyle(grid);
      gap = parseFloat(st.columnGap || st.gap || "0") || 0;
    }catch(_){}
    // gap totali tra (1+daysCount) colonne = daysCount
    const totalGap = gap * daysCount;

    const availDaysW = wrapW - totalGap;
    if (availDaysW <= 0) return;

    let dayW = Math.floor(availDaysW / daysCount);
    // Limiti di sicurezza: evita colonne troppo piccole o negative
    dayW = Math.max(10, Math.min(dayW, 64));

    // Altezza: 1 header + 6 righe stanze = 7 righe
    const rows = 7;
    const rowGaps = gap * (rows - 1);
    let availH = 0;
    try{
      const rect = wrap.getBoundingClientRect();
      availH = Math.floor(window.innerHeight - rect.top - 10);
    }catch(_){}
    if (!availH || availH < 200) availH = 260;

    let cellH = Math.floor((availH - rowGaps) / rows);
    cellH = Math.max(34, Math.min(cellH, 120));

    
    // Riduci altezza celle del 10%
    cellH = Math.floor(cellH * 0.90);
let pillH = Math.floor(cellH * 0.38);
    pillH = Math.max(18, Math.min(pillH, 46));

    // Applica override inline (solo month+landscape)
    try{
      grid.style.gridTemplateColumns = `repeat(${daysCount}, minmax(${dayW}px, 1fr))`;
      grid.style.setProperty("--cal-cell-h", `${cellH}px`);
      grid.style.setProperty("--cal-pill-h", `${pillH}px`);
      wrap.style.setProperty("--cal-cell-h", `${cellH}px`);
      wrap.style.setProperty("--cal-pill-h", `${pillH}px`);
    }catch(_){}
  }catch(_){}
}

function getCalendarTodayColumnIndex(anchor){
  try{
    const now = new Date();
    now.setHours(0,0,0,0);
    const a = (anchor instanceof Date) ? anchor : new Date(anchor || Date.now());
    if (now.getFullYear() !== a.getFullYear() || now.getMonth() !== a.getMonth()) return -1;
    return now.getDate();
  }catch(_){ return -1; }
}

function addCalendarTodayColumnOutline(grid, todayCol, rowsCount){
  try{
    if (!grid) return;
    try{ grid.querySelectorAll(".cal-today-column-outline").forEach((el) => el.remove()); }catch(_){ }
    if (!todayCol || todayCol < 1) return;
    const head = grid.querySelector(`.cal-cell.cal-head[data-day-index="${todayCol}"]`);
    if (!head) return;

    const roomCells = Array.from(grid.querySelectorAll(".cal-cell[data-room]"));
    if (!roomCells.length) return;
    let topRoom = Number.POSITIVE_INFINITY;
    let bottomRoom = 0;
    roomCells.forEach((cell) => {
      try{
        const t = Number(cell.offsetTop || 0);
        const b = t + Number(cell.offsetHeight || 0);
        if (t < topRoom) topRoom = t;
        if (b > bottomRoom) bottomRoom = b;
      }catch(_){ }
    });
    if (!Number.isFinite(topRoom) || bottomRoom <= topRoom) return;

    const outline = document.createElement("div");
    outline.className = "cal-today-column-outline";
    outline.setAttribute("aria-hidden", "true");
    const offset = 2;
    const left = (head.offsetLeft || 0) - offset;
    const top = topRoom - offset;
    const width = (head.offsetWidth || 0) + (offset * 2);
    const height = (bottomRoom - topRoom) + (offset * 2);
    outline.style.left = `${left}px`;
    outline.style.top = `${top}px`;
    outline.style.width = `${width}px`;
    outline.style.height = `${height}px`;
    grid.appendChild(outline);
  }catch(_){ }
}

function refreshCalendarTodayColumnOutline(){
  try{
    const anchor = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();
    const mode = String(state?.calendar?.viewMode || '').toLowerCase();
    const grid = (mode === 'month') ? document.getElementById('calGridMonth') : document.getElementById('calGrid');
    if (!grid || grid.hidden) return;
    const todayCol = (mode === 'month') ? __calendarSelectedColumnIndex(anchor) : getCalendarTodayColumnIndexForWeek(anchor);
    addCalendarTodayColumnOutline(grid, todayCol, getConfiguredRoomsCount(6) + 1);
  }catch(_){ }
}

function getCalendarTodayColumnIndexForWeek(anchor){
  try{
    const start = startOfWeekMonday((anchor instanceof Date) ? anchor : new Date(anchor || Date.now()));
    const today = new Date();
    today.setHours(0,0,0,0);
    for (let i = 0; i < 7; i++){
      const d = addDays(start, i);
      d.setHours(0,0,0,0);
      if (d.getTime() === today.getTime()) return i + 1;
    }
  }catch(_){ }
  return -1;
}


function scrollCalendarMonthToDayLeft(dayIndex){
  try{
    const wrap = document.getElementById('calDaysWrap') || document.querySelector('#page-calendario .cal-grid-wrap');
    const grid = document.getElementById('calGridMonth');
    if (!wrap || !grid || !dayIndex || dayIndex < 1) return;
    const head = grid.querySelector(`.cal-cell.cal-head[data-day-index="${dayIndex}"]`);
    if (!head) return;
    const headLeft = head.offsetLeft || 0;
    const cellWidth = head.offsetWidth || 0;
    let gap = 0;
    try{
      const st = getComputedStyle(grid);
      gap = parseFloat(st.columnGap || st.gap || '0') || 0;
    }catch(_){ }
    const twoCellsBack = (cellWidth + gap) * 2;
    const target = Math.max(0, headLeft - twoCellsBack);
    try{ wrap.scrollTo({ left: target, behavior: 'auto' }); }catch(_){ wrap.scrollLeft = target; }
    try{ if (wrap.__roomFreezeUpdate) wrap.__roomFreezeUpdate(); }catch(_){ }
  }catch(_){ }
}

function __calendarGuestDisplayName__(info, span){
  try{
    const g = findCalendarGuestById(info && info.guestId);
    const raw = collapseSpaces(String((g?.nome || g?.name || g?.Nome || g?.NOME || g?.guestName || '')).trim());
    const fullName = raw || collapseSpaces(String(info?.fullName || '').trim());
    if (!fullName) return String((info && info.initials) || '').trim();
    return fullName;
  }catch(_){
    return String((info && info.initials) || '').trim();
  }
}

function getCalendarRoomBedSummary(guestId, room){
  const key = `${String(guestId || '').trim()}:${String(room || '').trim()}`;
  const info = (state.stanzeByKey && state.stanzeByKey[key]) ? state.stanzeByKey[key] : { letto_m:0, letto_s:0, culla:0 };
  const lettoM = Number(info.letto_m || 0) || 0;
  const lettoS = Number(info.letto_s || 0) || 0;
  const culla = Number(info.culla || 0) || 0;
  const items = [];
  if (lettoM > 0) items.push(`${lettoM} matrimoniale${lettoM > 1 ? 'i' : ''}`);
  if (lettoS > 0) items.push(`${lettoS} ${lettoS === 1 ? 'singolo' : 'singoli'}`);
  if (culla > 0) items.push(`${culla} culla${culla > 1 ? 'e' : ''}`);
  return { lettoM, lettoS, culla, items, text: items.join(', ') || 'Nessun letto configurato' };
}

function buildCalendarCellPayload(info, room, dateIso){
  const guest = info ? findCalendarGuestById(info.guestId) : null;
  const channelBadge = guest ? getGuestChannelBadgeData(guest) : { color:'orange', initial:'', name:'' };
  const beds = info ? getCalendarRoomBedSummary(info.guestId, room) : { lettoM:0, lettoS:0, culla:0, items:[], text:'Nessun letto configurato' };
  const dateLabel = formatFullDateIT(new Date(`${dateIso}T00:00:00`)) || String(dateIso || '');
  const rangeLabel = guest ? (formatRangeCompactIT(guest.check_in || guest.checkIn || '', guest.check_out || guest.checkOut || '') || '') : '';
  return {
    isEmpty: !info,
    room: String(room || ''),
    dateIso: String(dateIso || ''),
    dateLabel,
    guest,
    guestName: guest ? __calendarGuestDisplayName__({ guestId: guestIdOf(guest) }, 1) : 'Nessuna prenotazione',
    channelName: String(channelBadge?.name || guest?.channel_nome || guest?.channelName || '').trim(),
    channelInitial: String(channelBadge?.initial || info?.channelInitial || '').trim(),
    channelColor: String(channelBadge?.color || info?.channelColor || 'orange').trim(),
    channelTextColor: String(channelBadge?.textColor || info?.channelTextColor || '').trim(),
    channelStyle: String(channelBadge?.style || info?.channelStyle || __tagColorInlineStyle__(channelBadge?.color || info?.channelColor || 'orange', channelBadge?.textColor || info?.channelTextColor || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false })),
    beds,
    mgc: [
      { short:'M', label:'Matrimonio', on: !!(info && info.mOn) },
      { short:'G', label:'Glutine', on: !!(info && info.gOn) },
      { short:'C', label:'Colazione', on: !!(info && info.cOn) }
    ],
    rangeLabel
  };
}

function buildCalendarCellZoomMarkup(payload){
  const data = payload || {};
  if (!data || data.isEmpty){
    return `
      <div class="calendar-cell-zoom is-empty room-${escapeHtml(data?.room || '')}">
        <div class="calendar-cell-zoom-empty">Nessuna prenotazione</div>
      </div>`;
  }
  const dots = [];
  for (let i = 0; i < (data.beds?.lettoM || 0); i++) dots.push('<span class="bed-dot bed-dot-m"></span>');
  for (let i = 0; i < (data.beds?.lettoS || 0); i++) dots.push('<span class="bed-dot bed-dot-s"></span>');
  for (let i = 0; i < (data.beds?.culla || 0); i++) dots.push('<span class="bed-dot bed-dot-c"></span>');
  const flags = (Array.isArray(data.mgc) ? data.mgc : []).filter(x => x.on).map(x => `<span class="cal-flag cal-flag-${x.short.toLowerCase()}">${x.short}</span>`).join('');
  const channel = data.channelInitial ? `<span class="cal-channel-tag operatori-tag color-${escapeHtml(data.channelColor || 'orange')}" style="${escapeHtml(data.channelStyle || __tagColorInlineStyle__(data.channelColor || 'orange', data.channelTextColor || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }))}">${escapeHtml(String(data.channelInitial || '').slice(0,1).toUpperCase())}</span>` : '';
  const bedsItems = Array.isArray(data.beds?.items) ? data.beds.items.map(x => String(x || '').trim()).filter(Boolean) : [];
  const bedsText = String(data.beds?.text || '').trim();
  const bedsListItems = bedsItems.length ? bedsItems : (bedsText ? [bedsText] : []);
  const bedsLine = bedsListItems.length ? `<div class="cal-cell-beds-text"><span>Letti</span><ul class="cal-cell-beds-list">${bedsListItems.map(item => `<li>${escapeHtml(item)}</li>`).join('')}</ul></div>` : '';
  const saldoLine = (!data.isEmpty && __currentOperatorCanViewGuestBalance__())
    ? `<div class="cal-cell-balance"><span>Rimanenza da pagare</span><strong>${escapeHtml(__calendarGuestRemainingBalanceText__(data.guest))}</strong></div>`
    : '';
  return `
    <div class="calendar-cell-zoom room-${escapeHtml(data.room || '')} ${data.isEmpty ? 'is-empty' : 'has-booking'}">
      <div class="cal-corner-chrome">${channel}${flags ? `<div class="cal-flags">${flags}</div>` : ''}</div>
      <div class="cal-cell-inner">
        <div class="cal-fullname is-span-cell">${escapeHtml(data.guestName || '')}</div>
        ${bedsLine}
        ${saldoLine}
        <div class="cal-dots">${dots.join('')}</div>
      </div>
    </div>`;
}

function renderCalendarCellModalPreview(payload){
  const preview = document.getElementById('calendarCellModalPreview');
  if (!preview) return;
  preview.innerHTML = buildCalendarCellZoomMarkup(payload);
}

function ensureCalendarCellZoomLayer(){
  let layer = document.getElementById('calendarCellZoomLayer');
  if (layer) return layer;
  layer = document.createElement('div');
  layer.id = 'calendarCellZoomLayer';
  layer.className = 'calendar-cell-zoom-layer';
  layer.hidden = true;
  layer.setAttribute('aria-hidden', 'true');
  layer.innerHTML = '<div class="calendar-cell-zoom-backdrop"></div><div class="calendar-cell-zoom-stage" id="calendarCellZoomStage"></div>';
  document.body.appendChild(layer);
  layer.addEventListener('click', (ev) => {
    try{
      const stage = document.getElementById('calendarCellZoomStage');
      if (ev.target === layer || ev.target === layer.querySelector('.calendar-cell-zoom-backdrop') || (stage && !stage.contains(ev.target))) {
        closeCalendarCellZoom();
      }
    }catch(_){ }
  });
  return layer;
}

function openCalendarCellZoom(cell, payload){
  const layer = ensureCalendarCellZoomLayer();
  const stage = document.getElementById('calendarCellZoomStage');
  if (!layer || !stage) return;
  try{ closeCalendarCellModal(); }catch(_){ }
  try{
    const currentKey = layer.dataset.zoomKey || '';
    const nextKey = cell ? (cell.dataset.zoomKey || `${payload?.room || ''}|${payload?.dateIso || ''}|${payload?.guest ? guestIdOf(payload.guest) : ''}`) : '';
    if (nextKey && currentKey === nextKey && !layer.hidden && layer.classList.contains('is-open')){
      closeCalendarCellZoom();
      return;
    }
    layer.dataset.zoomKey = nextKey || '';
  }catch(_){ }
  try{
    document.querySelectorAll('.cal-cell.is-zoom-source').forEach((el) => el.classList.remove('is-zoom-source'));
    if (cell) cell.classList.add('is-zoom-source');
  }catch(_){ }
  stage.innerHTML = buildCalendarCellZoomMarkup(payload);
  try{
    const zoomCard = stage.querySelector('.calendar-cell-zoom');
    if (zoomCard){
      const room = String(payload?.room || '').trim();
      if (room) zoomCard.classList.add(`room-${room}`);
      // dDAE_2.755 — Secondo tap sulla cella zoomata = chiusura zoom.
      zoomCard.addEventListener('click', (ev) => {
        try{ ev.preventDefault(); ev.stopPropagation(); }catch(_){ }
        closeCalendarCellZoom();
      }, { once:true });
    }
  }catch(_){ }
  layer.hidden = false;
  layer.setAttribute('aria-hidden', 'false');
  try{ document.body.classList.add('calendar-cell-zoom-open'); }catch(_){ }
  requestAnimationFrame(() => {
    try{ layer.classList.add('is-open'); }catch(_){ }
  });
}

function closeCalendarCellZoom(){
  const layer = document.getElementById('calendarCellZoomLayer');
  if (!layer) return;
  try{ layer.classList.remove('is-open'); }catch(_){ }
  try{ layer.dataset.zoomKey = ''; }catch(_){ }
  layer.setAttribute('aria-hidden', 'true');
  try{ document.body.classList.remove('calendar-cell-zoom-open'); }catch(_){ }
  try{ document.querySelectorAll('.cal-cell.is-zoom-source').forEach((el) => el.classList.remove('is-zoom-source')); }catch(_){ }
  setTimeout(() => {
    try{
      const stage = document.getElementById('calendarCellZoomStage');
      if (stage) stage.innerHTML = '';
      layer.hidden = true;
    }catch(_){ }
  }, 180);
}


function __currentOperatorCanViewGuestBalance__(){
  try{
    if (!state || !state.session) return false;
    // dDAE_2.754 — Admin: la rimanenza da pagare nel calendario è sempre visibile
    // nella cella zoomata. Operatori: resta vincolata al permesso "Saldo calendario".
    if (!isOperatoreSession(state.session)) return true;
    const me = String(__operatorName__() || state?.session?.nome || state?.session?.name || '').trim().toLowerCase();
    if (!me) return false;
    const catalog = (typeof getOperatoriCatalogFromSettings === 'function') ? getOperatoriCatalogFromSettings() : [];
    return (Array.isArray(catalog) ? catalog : []).some((op) => {
      const name = String(op?.nome || op?.name || op?.username || '').trim().toLowerCase();
      return name === me && !!(op?.mostraSaldo || op?.showBalance || op?.saldoCalendario);
    });
  }catch(_){ return false; }
}
function __calendarGuestRemainingBalanceValue__(guest){
  const n = (x)=>{
    if (typeof x === 'number' && isFinite(x)) return x;
    const v = Number(String(x ?? '0').replace(',', '.'));
    return isFinite(v) ? v : 0;
  };
  const compute = (g)=>{
    if (!g || typeof g !== 'object') return 0;
    try{
      if (typeof _guestStayFinancials === 'function'){
        const fin = _guestStayFinancials(g);
        const val = Number(fin && fin.remaining);
        if (isFinite(val)) return Math.max(0, Math.round(val * 100) / 100);
      }
    }catch(_){ }
    const explicit = g?.rimanenza_da_pagare ?? g?.rimanenzaDaPagare ?? g?.remaining_to_pay ?? g?.remainingToPay ?? g?.guestRemaining ?? g?.remaining;
    if (explicit !== undefined && explicit !== null && String(explicit).trim() !== '') return Math.max(0, Math.round(n(explicit) * 100) / 100);
    const total = n(g?.importo_prenotazione ?? g?.importo_prenota ?? g?.importoPrenotazione ?? g?.importoPrenota ?? g?.total ?? g?.totale ?? g?.importo ?? g?.prezzo);
    const services = n(g?.servizi_totale ?? g?.serviziTotal ?? g?.importo_servizi ?? g?.servizi ?? g?.services);
    const discount = n(g?.sconto ?? g?.discount ?? g?.sconto_importo ?? g?.scontoImporto);
    const dep = n(g?.acconto_importo ?? g?.accontoImporto ?? g?.acconto_pagato ?? g?.accontoPagato ?? g?.deposit ?? g?.deposito ?? g?.acconto);
    const saldo = n(g?.saldo_pagato ?? g?.saldoPagato ?? g?.saldo);
    return Math.max(0, Math.round(((total + services - discount - dep - saldo) * 100)) / 100);
  };
  try{
    const id = (typeof guestIdOf === 'function') ? guestIdOf(guest) : String(guest?.id || guest?.ID || '').trim();
    if (id){
      const sources = [state?.calendar?.guests, state?.guestRows, state?.guests, state?.ospitiRows, state?.ospiti];
      let best = guest || null;
      let bestVal = compute(best);
      for (const src of sources){
        if (!Array.isArray(src)) continue;
        for (const row of src){
          try{
            if ((typeof guestIdOf === 'function' ? guestIdOf(row) : String(row?.id || row?.ID || '').trim()) !== id) continue;
            const val = compute(row);
            if (val > bestVal){ best = row; bestVal = val; }
          }catch(_){ }
        }
      }
      return bestVal;
    }
  }catch(_){ }
  return compute(guest || {});
}

function __calendarGuestRemainingBalanceText__(guest){
  try{ return euro(__calendarGuestRemainingBalanceValue__(guest)); }catch(_){ return euro(0); }
}

function openCalendarCellModal(payload, mode){
  try{ closeCalendarCellZoom(); }catch(_){ }
  const modal = document.getElementById('calendarCellModal');
  const title = document.getElementById('calendarCellModalTitle');
  const subtitle = document.getElementById('calendarCellModalSubtitle');
  const content = document.getElementById('calendarCellModalContent');
  const preview = document.getElementById('calendarCellModalPreview');
  if (!modal || !title || !subtitle || !content) return;

  const isOperatorMode = String(mode || '').toLowerCase() === 'operator-detail';
  title.textContent = payload && !payload.isEmpty ? payload.guestName : `Stanza ${payload?.room || ''}`;
  subtitle.textContent = `Stanza ${payload?.room || ''} • ${payload?.dateLabel || ''}`;
  if (preview) preview.hidden = !!isOperatorMode;
  if (!isOperatorMode) renderCalendarCellModalPreview(payload || {});
  else if (preview) preview.innerHTML = '';

  if (!payload || payload.isEmpty){
    content.innerHTML = `<div class="calendar-cell-modal-grid"><div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">Prenotazione</span><strong>Assente</strong></div></div>`;
  }else if (isOperatorMode){
    const activeMgc = Array.isArray(payload.mgc) ? payload.mgc.filter(item => !!item.on) : [];
    const mgcRows = activeMgc.map(item => `<div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">${item.short}</span><strong>${item.label}</strong></div>`).join('');
    const saldoRow = __currentOperatorCanViewGuestBalance__() ? `<div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">Rimanenza da pagare</span><strong>${escapeHtml(__calendarGuestRemainingBalanceText__(payload.guest))}</strong></div>` : '';
    content.innerHTML = `
      <div class="calendar-cell-modal-grid">
        ${mgcRows}
        <div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">Letti</span><strong>${escapeHtml(payload.beds.text || '—')}</strong></div>
        <div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">Channel</span><strong>${escapeHtml(payload.channelName || 'Non impostato')}</strong></div>
        ${saldoRow}
      </div>`;
  }else{
    const badges = payload.mgc.map(item => `<span class="calendar-cell-status ${item.on ? 'is-on' : ''}">${item.label}</span>`).join('');
    content.innerHTML = `
      <div class="calendar-cell-modal-grid">
        <div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">Periodo</span><strong>${escapeHtml(payload.rangeLabel || payload.dateLabel || '—')}</strong></div>
        <div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">Channel</span><strong>${escapeHtml(payload.channelName || 'Non impostato')}</strong></div>
        <div class="calendar-cell-detail-row"><span class="calendar-cell-detail-label">Letti</span><strong>${escapeHtml(payload.beds.text || '—')}</strong></div>
      </div>
      <div class="calendar-cell-status-list">${badges}</div>`;
  }

  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  try{ document.body.classList.add('modal-open'); }catch(_){ }
  requestAnimationFrame(() => {
    try{ modal.classList.add('is-open'); }catch(_){ }
  });
}

function closeCalendarCellModal(){
  const modal = document.getElementById('calendarCellModal');
  if (!modal) return;
  try{ modal.classList.remove('is-open'); }catch(_){ }
  modal.setAttribute('aria-hidden', 'true');
  try{ document.body.classList.remove('modal-open'); }catch(_){ }
  setTimeout(() => {
    try{ modal.hidden = true; }catch(_){ }
  }, 160);
}

function ensureCalendarCellModalBound(){
  const modal = document.getElementById('calendarCellModal');
  const closeBtn = document.getElementById('calendarCellModalClose');
  if (!modal || modal.dataset.bound === '1') return;
  modal.dataset.bound = '1';
  if (closeBtn) bindFastTap(closeBtn, closeCalendarCellModal);
  modal.addEventListener('click', (ev) => {
    try{ if (ev.target === modal) closeCalendarCellModal(); }catch(_){ }
  });
  try{
    document.addEventListener('keydown', (ev) => {
      if (String(ev.key || '') !== 'Escape') return;
      try{ closeCalendarCellModal(); }catch(_){ }
      try{ closeCalendarCellZoom(); }catch(_){ }
    });
  }catch(_){ }
}

function bindCalendarCellActions(cell, options){
  if (!cell || cell.dataset.calendarCellBound === '1') return;
  cell.dataset.calendarCellBound = '1';
  const room = options?.room;
  const dateIso = options?.dateIso;
  const info = options?.info || null;
  let holdTimer = null;
  let holdTriggered = false;
  const clearHold = () => {
    if (holdTimer) {
      clearTimeout(holdTimer);
      holdTimer = null;
    }
  };
  const startHold = () => {
    clearHold();
    holdTriggered = false;
    holdTimer = setTimeout(() => {
      holdTimer = null;
      holdTriggered = true;
      if (!info) return;
      const payload = buildCalendarCellPayload(info, room, dateIso);
      if (__isAdmin__()) {
        const ospite = payload.guest;
        if (!ospite) return;
        enterGuestViewMode(ospite);
        showPage('ospite');
        return;
      }
      openCalendarCellZoom(cell, payload);
    }, 500);
  };
  const endHold = () => {
    clearHold();
    setTimeout(() => { holdTriggered = false; }, 0);
  };
  const onClick = (ev) => {
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    if (holdTriggered) {
      holdTriggered = false;
      return;
    }
    const payload = buildCalendarCellPayload(info, room, dateIso);
    try{ cell.dataset.zoomKey = `${room || ''}|${dateIso || ''}|${payload?.guest ? guestIdOf(payload.guest) : ''}`; }catch(_){ }
    openCalendarCellZoom(cell, payload);
  };
  try{ cell.addEventListener('pointerdown', startHold, { passive:true }); }catch(_){ cell.addEventListener('pointerdown', startHold); }
  ['pointerup','pointerleave','pointercancel'].forEach((evt) => {
    try{ cell.addEventListener(evt, endHold, { passive:true }); }catch(_){ cell.addEventListener(evt, endHold); }
  });
  cell.addEventListener('contextmenu', (ev) => { try{ ev.preventDefault(); }catch(_){ } });
  cell.addEventListener('click', onClick);
}

function renderCalendarioMonth(){
  const parts = ensureCalendarFixedRailStructure();
  const grid = parts.gridMonth || document.getElementById("calGridMonth");
  const gridWeek = parts.gridWeek || document.getElementById("calGrid");
  try{ if (gridWeek) gridWeek.classList.remove("is-loading"); }catch(_){ }
  try{ if (grid) grid.classList.toggle("is-loading", !!(state.calendar && state.calendar.loading)); }catch(_){ }
  const title = document.getElementById("calWeekTitle");
  const input = document.getElementById("calDateInput");
  if (!grid) return;

  try{ if (gridWeek) gridWeek.hidden = true; }catch(_){ }
  try{ grid.hidden = false; }catch(_){ }

  grid.replaceChildren();
  const frag = document.createDocumentFragment();

  const anchor = (state.calendar && state.calendar.anchor) ? state.calendar.anchor : new Date();
  const monthStart = new Date(anchor.getFullYear(), anchor.getMonth(), 1);
  const daysCount = new Date(anchor.getFullYear(), anchor.getMonth() + 1, 0).getDate();
  const days = Array.from({ length: daysCount }, (_, i) => addDays(monthStart, i));

  try{ if (input) input.value = formatISODateLocal(anchor) || todayISO(); }catch(_){ }
  if (title) {
    title.textContent = "";
    title.hidden = true;
  }
  try{
    grid.style.gridTemplateColumns = `repeat(${daysCount}, var(--cal-day-w))`;
  }catch(_){ }

  const occ = buildMonthOccupancy(monthStart, daysCount);
  const roomsCount = getConfiguredRoomsCount(6);
  const todayCol = __calendarSelectedColumnIndex(anchor);
  renderCalendarRoomRail(roomsCount);

  for (let i = 0; i < daysCount; i++) {
    const d = days[i];
    const dayPill = document.createElement("div");
    dayPill.className = "cal-cell cal-head";
    dayPill.dataset.dayIndex = String(i + 1);
    if (todayCol === (i + 1)) dayPill.classList.add('is-today-col');

    const ab = document.createElement("div");
    ab.className = "cal-day-abbrev";
    ab.textContent = weekdayShortIT(d).toUpperCase();

    const num = document.createElement("div");
    num.className = "cal-day-num";
    num.textContent = String(d.getDate());

    dayPill.appendChild(ab);
    dayPill.appendChild(num);
    frag.appendChild(dayPill);
  }

  for (let r = 1; r <= roomsCount; r++) {
    for (let i = 0; i < daysCount; ) {
      const d = days[i];
      const dIso = isoDate(d);
      const info = occ.get(`${dIso}:${r}`);

      if (!info) {
        const cell = document.createElement("button");
        cell.type = "button";
        cell.className = `cal-cell room-${r}`;
        cell.setAttribute("aria-label", `Stanza ${r}, ${weekdayShortIT(d)} ${d.getDate()}`);
        cell.dataset.date = dIso;
        cell.dataset.room = String(r);
        if (todayCol === (i + 1)) cell.classList.add('is-today-col');
        bindCalendarCellActions(cell, { room:r, dateIso:dIso, info:null });
        frag.appendChild(cell);
        i += 1;
        continue;
      }

      let span = 1;
      while ((i + span) < daysCount) {
        const nextInfo = occ.get(`${isoDate(days[i + span])}:${r}`);
        if (!nextInfo) break;
        if (String(nextInfo.guestId || '') !== String(info.guestId || '')) break;
        span += 1;
      }

      const cell = document.createElement("button");
      cell.type = "button";
      cell.className = `cal-cell room-${r} has-booking`;
      cell.dataset.date = dIso;
      cell.dataset.room = String(r);
      cell.dataset.spanDays = String(span);
      cell.style.gridColumn = `${i + 1} / span ${span}`;
      cell.setAttribute("aria-label", span > 1
        ? `Stanza ${r}, dal ${days[i].getDate()} al ${days[i + span - 1].getDate()}`
        : `Stanza ${r}, ${weekdayShortIT(d)} ${d.getDate()}`);

      const coversToday = (todayCol >= (i + 1) && todayCol <= (i + span));
      if (coversToday) {
        cell.classList.add('is-today-col');
        try{
          cell.style.setProperty('--today-span', String(Math.max(1, span)));
          cell.style.setProperty('--today-offset', String(Math.max(0, todayCol - (i + 1))));
        }catch(_){ }
      }
      if (span === 1) cell.classList.add('booking-seg-single');
      else cell.classList.add('booking-span-merged');

      try{
        const chrome = document.createElement("div");
        chrome.className = "cal-corner-chrome";
        if (info.channelInitial){
          const ch = document.createElement("span");
          ch.className = `cal-channel-tag operatori-tag color-${info.channelColor || "orange"}`;
          ch.textContent = String(info.channelInitial).slice(0,1).toUpperCase();
          chrome.appendChild(ch);
        }
        const flags = document.createElement("div");
        flags.className = "cal-flags";
        if (info.mOn){ const f = document.createElement("span"); f.className = "cal-flag cal-flag-m"; f.textContent = "M"; flags.appendChild(f); }
        if (info.gOn){ const f = document.createElement("span"); f.className = "cal-flag cal-flag-g"; f.textContent = "G"; flags.appendChild(f); }
        if (info.cOn){ const f = document.createElement("span"); f.className = "cal-flag cal-flag-c"; f.textContent = "C"; flags.appendChild(f); }
        if (flags.childNodes.length) chrome.appendChild(flags);
        if (chrome.childNodes.length) cell.appendChild(chrome);
      }catch(_){ }

      const inner = document.createElement("div");
      inner.className = "cal-cell-inner";

      const full = document.createElement("div");
      full.className = `cal-fullname ${span <= 1 ? "is-single-cell" : "is-span-cell"}`;
      full.textContent = __calendarGuestDisplayName__(info, span);
      inner.appendChild(full);

      const dots = document.createElement("div");
      dots.className = "cal-dots";
      for (const t of (Array.isArray(info.dots) ? info.dots : []).slice(0, 4)) {
        const s = document.createElement("span");
        s.className = `bed-dot ${t === "m" ? "bed-dot-m" : t === "s" ? "bed-dot-s" : "bed-dot-c"}`;
        dots.appendChild(s);
      }
      if (dots.childNodes.length) inner.appendChild(dots);
      cell.appendChild(inner);

      bindCalendarCellActions(cell, { room:r, dateIso:dIso, info });

      frag.appendChild(cell);
      i += span;
    }
  }

  grid.appendChild(frag);
  try{ addCalendarTodayColumnOutline(grid, todayCol, roomsCount + 1); }catch(_){ }
  try{ __updateCalendarSelectedDayBadges__({ show:true }); }catch(_){ }
  try{ __scheduleCalendarioLayoutRefresh(); }catch(_){ }
}

function buildMonthOccupancy(monthStart, daysCount){
  const map = new Map();
  const guests = (state.calendar && Array.isArray(state.calendar.guests)) ? state.calendar.guests : [];
  const monthEndEx = addDays(monthStart, daysCount);

  for (const g of guests){
    const guestId = String(g.id ?? g.ID ?? g.ospite_id ?? g.ospiteId ?? g.guest_id ?? g.guestId ?? "").trim();
    if (!guestId) continue;

    const ciStr = formatISODateLocal(g.check_in || g.checkIn || "");
    const coStr = formatISODateLocal(g.check_out || g.checkOut || "");
    if (!ciStr || !coStr) continue;

    const ci = new Date(ciStr + "T00:00:00");
    const co = new Date(coStr + "T00:00:00");
    const last = addDays(co, -1);
    const lastIso = isoDate(last);

    let roomsArr = [];
    try {
      const st = g.stanze;
      if (Array.isArray(st)) roomsArr = st;
      else if (st != null && String(st).trim().length) {
        roomsArr = _parseRoomsArr(st);
      }
    } catch (_) {}
    roomsArr = normalizeRoomsList(roomsArr, 6);
    if (!roomsArr.length) continue;

    const initials = initialsFromName(g.nome || g.name || g.Nome || g.NOME || g.guestName || g.fullName || g.full_name || "");

    const mOn = !!(g.matrimonio);
    const gOn = truthy(g.g ?? g.flag_g ?? g.gruppo_g ?? g.group ?? g.g_flag);
    const cOn = truthy(g.col_c ?? g.colC ?? g.c ?? g.C ?? g.flag_c ?? g.flagC ?? g.colc ?? g.c_flag);

    for (let d = new Date(ci); d < co; d = addDays(d, 1)) {
      if (d < monthStart || d >= monthEndEx) continue;
      const dIso = isoDate(d);
      const isLast = dIso === lastIso;

      for (const r of roomsArr) {
        const dots = dotsForGuestRoom(guestId, r);
        const badge = getGuestChannelBadgeData(g);
        map.set(`${dIso}:${r}`, { guestId, initials, dots, lastDay: isLast, mOn, gOn, cOn, channelInitial: String(badge.initial || '').trim().slice(0,1).toUpperCase(), channelColor: badge.color, channelTextColor: badge.textColor || '', channelStyle: badge.style || __tagColorInlineStyle__(badge.color || 'orange', badge.textColor || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }) });
      }
    }
  }
  return map;
}

function buildWeekOccupancy(weekStart){
  const map = new Map();
  const guests = (state.calendar && Array.isArray(state.calendar.guests)) ? state.calendar.guests : [];
  const weekEnd = addDays(weekStart, 7);
  const todayIso = isoDate(new Date());


  for (const g of guests){
    const guestId = String(g.id ?? g.ID ?? g.ospite_id ?? g.ospiteId ?? g.guest_id ?? g.guestId ?? "").trim();
    if (!guestId) continue;

    const ciStr = formatISODateLocal(g.check_in || g.checkIn || "");
    const coStr = formatISODateLocal(g.check_out || g.checkOut || "");
    if (!ciStr || !coStr) continue;

    const ci = new Date(ciStr + "T00:00:00");
    const co = new Date(coStr + "T00:00:00");
    const last = addDays(co, -1);
    const lastIso = isoDate(last);
    const lastIsPresentOrFuture = (lastIso >= todayIso);

    let roomsArr = [];
    try {
      const st = g.stanze;
      if (Array.isArray(st)) roomsArr = st;
      else if (st != null && String(st).trim().length) {
        roomsArr = _parseRoomsArr(st);
      }
    } catch (_) {}
    roomsArr = normalizeRoomsList(roomsArr, 6);
    if (!roomsArr.length) continue;

    const initials = initialsFromName(g.nome || g.name || g.Nome || g.NOME || g.guestName || g.fullName || g.full_name || "");

    const mOn = !!(g.matrimonio);
    const gOn = truthy(g.g ?? g.flag_g ?? g.gruppo_g ?? g.group ?? g.g_flag);
    const cOn = truthy(g.col_c ?? g.colC ?? g.c ?? g.C ?? g.flag_c ?? g.flagC ?? g.colc ?? g.c_flag);

    for (let d = new Date(ci); d < co; d = addDays(d, 1)) {
      if (d < weekStart || d >= weekEnd) continue;
      const dIso = isoDate(d);
      const isLast = isoDate(d) === lastIso;

      for (const r of roomsArr) {
        const dots = dotsForGuestRoom(guestId, r);
        const badge = getGuestChannelBadgeData(g);
        map.set(`${dIso}:${r}`, { guestId, initials, dots, lastDay: isLast, mOn, gOn, cOn, channelInitial: String(badge.initial || '').trim().slice(0,1).toUpperCase(), channelColor: badge.color, channelTextColor: badge.textColor || '', channelStyle: badge.style || __tagColorInlineStyle__(badge.color || 'orange', badge.textColor || '', { opacity:0.80, borderOpacity:1, preferWhiteText:false }) });
      }
    }
  }
  return map;
}

function dotsForGuestRoom(guestId, room){
  const key = `${guestId}:${room}`;
  const info = (state.stanzeByKey && state.stanzeByKey[key]) ? state.stanzeByKey[key] : { letto_m:0, letto_s:0, culla:0 };
  const lettoM = Number(info.letto_m || 0) || 0;
  const lettoS = Number(info.letto_s || 0) || 0;
  const culla = Number(info.culla || 0) || 0;

  const arr = [];
  if (lettoM > 0) arr.push("m");
  for (let i=0;i<lettoS;i++) arr.push("s");
  if (culla > 0) arr.push("c");
  return arr;
}

function initialsFromName(name){
  const s = collapseSpaces(String(name||"").trim());
  if (!s) return "";
  const parts = s.split(" ").filter(Boolean);
  if (parts.length === 1) return parts[0].slice(0,2).toUpperCase();
  const a = parts[0].slice(0,1);
  const b = parts[parts.length-1].slice(0,1);
  return (a+b).toUpperCase();
}

function startOfWeekMonday(date){
  const d = new Date(date);
  d.setHours(0,0,0,0);
  const day = d.getDay(); // 0 Sun..6 Sat
  const diff = (day === 0 ? -6 : 1 - day);
  return addDays(d, diff);
}

function addDays(date, days){
  const d = new Date(date);
  d.setDate(d.getDate() + days);
  d.setHours(0,0,0,0);
  return d;
}

function isoDate(date){
  const d = new Date(date);
  d.setHours(0,0,0,0);
  const y = d.getFullYear();
  const m = String(d.getMonth()+1).padStart(2,"0");
  const da = String(d.getDate()).padStart(2,"0");
  return `${y}-${m}-${da}`;
}

function weekdayShortIT(date){
  try{
    const label = __getWeekdayShortForLocale__(date);
    return __capitalizeLocale__(label).replace('.', '');
  }catch(_){ return ""; }
}

function monthNameIT(date){
  try{
    return String(new Date(date).toLocaleDateString(__getCurrentLocale__(), { month:"long" }) || "");
  }catch(_){ return ""; }
}

function romanWeekOfMonth(weekStart){
  const d = new Date(weekStart);
  const y = d.getFullYear();
  const m = d.getMonth();
  const firstOfMonth = new Date(y, m, 1);
  const firstWeekStart = startOfWeekMonday(firstOfMonth);
  const diff = Math.floor((startOfWeekMonday(d) - firstWeekStart) / (7*24*60*60*1000));
  const n = Math.max(1, diff + 1);
  return toRoman(n);
}

function toRoman(n){
  const map = [[10,"X"],[9,"IX"],[8,"VIII"],[7,"VII"],[6,"VI"],[5,"V"],[4,"IV"],[3,"III"],[2,"II"],[1,"I"]];
  let out = "";
  let x = Math.max(1, Math.min(10, n));
  for (const [v,s] of map){
    while (x >= v){ out += s; x -= v; }
  }
  return out || "I";
}


/* =========================
   Lavanderia (dDAE_1.020)
========================= */
function getLaundryLabelByCode(code, source){
  const safe = __normalizeLaundryCode__(code);
  if (!safe) return '';
  const defs = source ? __getLaundryCatalogForRecord__(source) : getLaundryCatalogFromSettings();
  const hit = defs.find(item => String(item?.abbreviazione || '') === safe);
  return __laundryDisplayTitle__(hit || safe, safe) || safe;
}

function __laundryDefsFromSource__(source){
  return source ? __getLaundryCatalogForRecord__(source) : getLaundryCatalogFromSettings();
}

function __laundryDeletedKey__(){
  return "ddae_laundry_deleted_shared_board";
}
function __laundryDeletedIds__(){
  return new Set();
}
function __laundryMarkDeleted__(id){
  return;
}
function sanitizeLaundryItem_(it){
  it = it || {};
  const out = {};
  out.id = String(it.id || "").trim();
  out.startDate = String(it.startDate || it.start_date || it.from || "").trim();
  out.endDate = String(it.endDate || it.end_date || it.to || "").trim();
  out.createdAt = String(it.createdAt || it.created_at || "").trim();
  out.updatedAt = String(it.updatedAt || it.updated_at || it.updatedAt || "").trim();
  out.deletedAt = String(it.deletedAt || it.deleted_at || "").trim();
  out.isDeleted = __normBool01(it.isDeleted ?? it.is_deleted ?? it.deleted);
  out.is_deleted = out.isDeleted;
  out.laundryCatalog = __getLaundryCatalogForRecord__(it);
  const codes = out.laundryCatalog.map(item => String(item?.abbreviazione || '').trim()).filter(Boolean);
  codes.forEach((k) => {
    out[k] = 0;
    out[`${k}_resi`] = 0;
  });
  out.laundryPrices = {};
  out.laundryCatalog.forEach((item) => {
    const code = String(item?.abbreviazione || '').trim();
    if (!code) return;
    out.laundryPrices[code] = Math.round((Number(item?.prezzo || 0) || 0) * 100) / 100;
  });
  Object.keys(it).forEach(rawKey => {
    const key = String(rawKey || "").trim();
    const lowered = key.toLowerCase();
    let candidate = lowered;
    if (/_resi$/i.test(key)) candidate = lowered.slice(0, -5);
    else if (/_r$/i.test(key)) candidate = lowered.slice(0, -2);
    else {
      const maybe = lowered.slice(0, -1);
      if (codes.some(c => c.toLowerCase() === maybe)) candidate = maybe;
    }
    const baseKey = codes.find(c => c.toLowerCase() === candidate);
    if (!baseKey) return;
    let n = Number(it[rawKey]);
    if (!isFinite(n)) return;
    n = Math.floor(n);
    const isResKey = /_resi$/i.test(key) || /_r$/i.test(key) || (key.length === baseKey.length + 1 && key.toLowerCase().startsWith(baseKey.toLowerCase()) && key.toLowerCase().endsWith('r'));
    if (isResKey){
      out[`${baseKey}_resi`] += Math.max(0, Math.abs(n));
    } else if (n < 0){
      out[`${baseKey}_resi`] += Math.abs(n);
    } else {
      out[baseKey] += n;
    }
  });
  if (it && typeof it.laundryPrices === 'object' && it.laundryPrices){
    Object.keys(it.laundryPrices).forEach((rawKey) => {
      const code = __normalizeLaundryCode__(rawKey);
      if (!code) return;
      const n = Number(String(it.laundryPrices?.[rawKey] ?? '').replace(',', '.'));
      if (isFinite(n) && n >= 0) out.laundryPrices[code] = Math.round(n * 100) / 100;
    });
  }
  out.totalCost = (typeof it?.totalCost === 'number' && isFinite(it.totalCost)) ? Math.round(Number(it.totalCost) * 100) / 100 : __laundryComputeTotalCost__(out, out.laundryPrices);
  return out;
}

function setLaundryLabels_(){
  try{
    getLaundryCatalogFromSettings().forEach((item) => {
      const code = String(item?.abbreviazione || '').trim();
      const el = document.getElementById("laundryLbl" + code);
      if (el) el.textContent = __laundryDisplayTitle__(item, code) || code;
    });
  }catch(_){ }
  try{
    const cancelBtn = document.getElementById('laundryDateRangeCancel');
    const applyBtn = document.getElementById('laundryDateRangeApply');
    if (cancelBtn) cancelBtn.textContent = __designTranslate__('Annulla', { en:'Cancel', fr:'Annuler', de:'Abbrechen', es:'Cancelar' });
    if (applyBtn) applyBtn.textContent = __designTranslate__('Conferma', { en:'Confirm', fr:'Confirmer', de:'Bestätigen', es:'Confirmar' });
    __syncLaundryDateRangeUi__();
    if (!document.getElementById('laundryDateRangeModal')?.hidden) __renderLaundryDateRangeCalendar__();
  }catch(_){ }
}

function __laundryDisplayPricesForCurrentView__(){
  try{
    const current = state?.laundry?.current || null;
    if (current && current.laundryPrices && typeof current.laundryPrices === "object") return current.laundryPrices;
  }catch(_){ }
  return getLaundryPricesFromSettings();
}

function __laundryUpdatePriceHints__(){
  try{
    const prices = getLaundryPricesFromSettings();
    getLaundryCatalogFromSettings().forEach((item) => {
      const code = String(item?.abbreviazione || '').trim();
      const tile = document.querySelector(`#laundryGrid .laundry-tile[data-key="${code}"]`);
      if (!tile) return;
      const n = Number(prices?.[code] || 0) || 0;
      tile.setAttribute('title', `Costo ${__laundryDisplayTitle__(item, code) || code}: ${__laundryMoneyFmt__(n)}`);
      tile.dataset.unitPrice = String(n.toFixed(2));
    });
  }catch(_){ }
}

async function __openLaundryPricePrompt__(key){
  try{ await ensureSettingsLoaded({ force:false, showLoader:false }); }catch(_){ }
  const label = getLaundryLabelByCode(key) || key;
  const prices = getLaundryPricesFromSettings();
  const current = Number(prices?.[key] || 0) || 0;
  const raw = window.prompt(`Costo pulizia ${label} (${key})`, String(current).replace('.', ','));
  if (raw === null) return;
  const txt = String(raw || '').trim().replace(',', '.');
  if (!txt){
    prices[key] = 0;
  } else {
    const value = Number(txt);
    if (!isFinite(value) || value < 0){
      toast('Prezzo non valido', 'orange');
      return;
    }
    prices[key] = Math.round(value * 100) / 100;
  }
  await saveLaundryPricesToSettings(prices);
  __laundryUpdatePriceHints__();
  renderLaundry_(state?.laundry?.current || null);
  try{ toast('Costo salvato', 'blue'); }catch(_){ }
}

function __ensureLaundryPricesModalBuilt__(){
  const host = document.getElementById('laundryPricesList');
  if (!host || host.dataset.ready === '1') return;
  host.innerHTML = getLaundryCatalogFromSettings().map((item) => {
    const k = String(item?.abbreviazione || '').trim();
    const label = __laundryDisplayTitle__(item, k) || k;
    return `<label class="laundry-price-row"><div class="laundry-price-copy"><div class="laundry-price-title">${label}</div><div class="laundry-price-code">${k}</div></div><input class="laundry-price-input" data-key="${k}" inputmode="decimal" min="0" step="0.01" type="number" /></label>`;
  }).join('');
  host.dataset.ready = '1';
}

async function __openLaundryPricesModal__(){
  try{ await ensureSettingsLoaded({ force:false, showLoader:false }); }catch(_){ }
  __ensureLaundryPricesModalBuilt__();
  const modal = document.getElementById('laundryPricesModal');
  if (!modal) return;
  const prices = getLaundryPricesFromSettings();
  getLaundryCatalogFromSettings().forEach((item) => {
    const k = String(item?.abbreviazione || '').trim();
    const input = modal.querySelector(`.laundry-price-input[data-key="${k}"]`);
    if (!input) return;
    const n = Number(prices?.[k] || 0) || 0;
    input.value = n ? String(n.toFixed(2)) : '';
  });
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  document.body.classList.add('modal-open');
}

function __closeLaundryPricesModal__(){
  const modal = document.getElementById('laundryPricesModal');
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
  document.body.classList.remove('modal-open');
}

async function __saveLaundryPricesModal__(){
  const modal = document.getElementById('laundryPricesModal');
  if (!modal) return;
  const next = {};
  for (const item of getLaundryCatalogFromSettings()){
    const k = String(item?.abbreviazione || '').trim();
    const input = modal.querySelector(`.laundry-price-input[data-key="${k}"]`);
    const raw = String(input?.value ?? '').trim().replace(',', '.');
    if (!raw){
      next[k] = 0;
      continue;
    }
    const n = Number(raw);
    if (!isFinite(n) || n < 0){
      if (input && typeof input.focus === 'function') input.focus();
      throw new Error(`Prezzo non valido per ${__laundryDisplayTitle__(item, k) || k}`);
    }
    next[k] = Math.round(n * 100) / 100;
  }
  await saveLaundryPricesToSettings(next);
  __laundryUpdatePriceHints__();
  renderLaundry_(state?.laundry?.current || null);
  __closeLaundryPricesModal__();
  try{ toast('Costi salvati', 'blue'); }catch(_){ }
}

function __bindLaundryPriceLongPress__(){
  const host = document.getElementById('laundryGrid');
  if (!host || host.dataset.priceLongPressBound === '1') return;
  host.dataset.priceLongPressBound = '1';
  let timer = null;
  let suppressClick = false;
  let activeTile = null;
  const clearPress = () => {
    if (timer) { try{ clearTimeout(timer); }catch(_){ } }
    timer = null;
    activeTile = null;
  };
  const startPress = (ev) => {
    const tile = ev.target && ev.target.closest ? ev.target.closest('.laundry-tile[data-key]') : null;
    const key = tile ? String(tile.dataset.key || '').trim().toUpperCase() : '';
    if (!tile || !getLaundryComponentCodes().includes(key)) return;
    clearPress();
    activeTile = tile;
    suppressClick = false;
    timer = setTimeout(async () => {
      suppressClick = true;
      const useKey = String(activeTile?.dataset?.key || key || '').trim().toUpperCase();
      clearPress();
      try{ await __openLaundryPricePrompt__(useKey); }catch(e){ try{ toast(e?.message || 'Errore', 'orange'); }catch(_){ } }
      setTimeout(() => { suppressClick = false; }, 320);
    }, 500);
  };
  host.addEventListener('pointerdown', startPress, { passive:true });
  host.addEventListener('pointerup', clearPress, { passive:true });
  host.addEventListener('pointerleave', clearPress, { passive:true });
  host.addEventListener('pointercancel', clearPress, { passive:true });
  host.addEventListener('click', (ev) => {
    if (!suppressClick) return;
    const tile = ev.target && ev.target.closest ? ev.target.closest('.laundry-tile[data-key]') : null;
    const key = tile ? String(tile.dataset.key || '').trim().toUpperCase() : '';
    if (!getLaundryComponentCodes().includes(key)) return;
    ev.preventDefault();
    ev.stopPropagation();
  }, true);
}

function renderLaundry_(item){
  state.laundry.current = item;
  const period = document.getElementById("laundryPeriodLabel");
  const printRange = document.getElementById("laundryPrintRange");
  const tbody = document.getElementById("laundryPrintBody");
  const safeItem = item ? sanitizeLaundryItem_(item) : null;
  const defs = __laundryDefsFromSource__(safeItem || null);
  const pricesForView = safeItem?.laundryPrices && typeof safeItem?.laundryPrices === 'object' ? safeItem.laundryPrices : __laundryDisplayPricesForCurrentView__();

  __bindLaundryPriceLongPress__();
  __laundryUpdatePriceHints__();

  if (!safeItem){
    if (period) { period.hidden = true; period.textContent = "—"; }
    if (printRange) printRange.textContent = "—";
    defs.forEach((def) => {
      const code = String(def?.abbreviazione || '').trim();
      const v = document.getElementById("laundryVal" + code);
      if (v) v.textContent = "0";
    });
    const totalEl = document.getElementById('laundryValTOTAL');
    if (totalEl) totalEl.textContent = __laundryMoneyFmt__(0);
    if (tbody) tbody.innerHTML = "";
    return;
  }

  const startLbl = safeItem.startDate ? formatLongDateIT(safeItem.startDate) : String(safeItem.startDate || "");
  const endLbl = safeItem.endDate ? formatLongDateIT(safeItem.endDate) : String(safeItem.endDate || "");
  const rangeText = (startLbl && endLbl) ? `${startLbl} → ${endLbl}` : "—";

  if (period){
    period.hidden = false;
    period.innerHTML = `<b>${rangeText}</b>`;
  }
  if (printRange) printRange.textContent = rangeText;

  defs.forEach((def) => {
    const code = String(def?.abbreviazione || '').trim();
    const v = document.getElementById("laundryVal" + code);
    if (!v) return;
    const qty = Number(safeItem?.[code] || 0) || 0;
    const resi = Number(safeItem?.[`${code}_resi`] || 0) || 0;
    if (resi > 0) v.innerHTML = `${qty}<span class="laundry-resi"> (${resi})</span>`;
    else v.textContent = String(qty);
  });

  const computedTotal = (typeof safeItem?.totalCost === 'number' && isFinite(safeItem.totalCost))
    ? Math.round(Number(safeItem.totalCost) * 100) / 100
    : __laundryComputeTotalCost__(safeItem, pricesForView);
  const totalEl = document.getElementById('laundryValTOTAL');
  if (totalEl) totalEl.textContent = __laundryMoneyFmt__(computedTotal);

  if (tbody){
    tbody.innerHTML = defs.map((def) => {
      const code = String(def?.abbreviazione || '').trim();
      const label = __laundryDisplayTitle__(def, code) || code;
      const qty = Number(safeItem?.[code] || 0) || 0;
      const resi = Number(safeItem?.[`${code}_resi`] || 0) || 0;
      const unit = Number(pricesForView?.[code] ?? def?.prezzo ?? 0) || 0;
      const subtotal = Math.round((qty * unit) * 100) / 100;
      const qtyHtml = resi > 0 ? `${qty}<span class="laundry-resi"> (${resi})</span>` : `${qty}`;
      return `<tr><td><b>${label}</b> <span style="opacity:.7">(${code})</span></td><td style="text-align:right;font-weight:950">${qtyHtml} · ${__laundryMoneyFmt__(subtotal)}</td></tr>`;
    }).join('') + `<tr><td><b>Costo totale</b></td><td style="text-align:right;font-weight:950">${__laundryMoneyFmt__(computedTotal)}</td></tr>`;
  }
}

function __laundryReportRangeText__(item){
  const startLbl = item?.startDate ? formatLongDateIT(item.startDate) : String(item?.startDate || "");
  const endLbl = item?.endDate ? formatLongDateIT(item.endDate) : String(item?.endDate || "");
  return (startLbl && endLbl) ? `${startLbl} → ${endLbl}` : "—";
}

function __buildLaundryDetailShareText__(raw){
  const item = sanitizeLaundryItem_(raw || {});
  const prices = item?.laundryPrices && typeof item.laundryPrices === 'object' ? item.laundryPrices : __laundryDisplayPricesForCurrentView__();
  let imponibile = 0;
  const defs = __laundryDefsFromSource__(item);
  const rows = defs.map((def) => {
    const k = String(def?.abbreviazione || '').trim();
    const qty = Math.max(0, Number(item?.[k] || 0) || 0);
    const resi = Math.max(0, Number(item?.[`${k}_resi`] || 0) || 0);
    const unit = Math.max(0, Number(prices?.[k] ?? def?.prezzo ?? 0) || 0);
    const subtotal = Math.round(qty * unit * 100) / 100;
    imponibile += subtotal;
    const qtyText = resi > 0 ? `${qty} (${resi})` : `${qty}`;
    return `- ${__laundryDisplayTitle__(def, k) || k} (${k}): ${qtyText} x ${__laundryMoneyFmt__(unit)} = ${__laundryMoneyFmt__(subtotal)}`;
  });
  imponibile = Math.round(imponibile * 100) / 100;
  const ivato = Math.round(imponibile * 1.22 * 100) / 100;
  return [
    'Report lavanderia',
    __laundryReportRangeText__(item),
    '',
    ...rows,
    '',
    `Imponibile: ${__laundryMoneyFmt__(imponibile)}`,
    `Totale IVA 22%: ${__laundryMoneyFmt__(ivato)}`
  ].join('\n');
}

function __laundryRoundRect__(ctx, x, y, w, h, r){
  const rr = Math.min(r, w / 2, h / 2);
  ctx.beginPath();
  ctx.moveTo(x + rr, y);
  ctx.arcTo(x + w, y, x + w, y + h, rr);
  ctx.arcTo(x + w, y + h, x, y + h, rr);
  ctx.arcTo(x, y + h, x, y, rr);
  ctx.arcTo(x, y, x + w, y, rr);
  ctx.closePath();
}

function __laundryDetailFileName__(item){
  const from = String(item?.startDate || '').replace(/[^0-9]/g, '') || 'report';
  const to = String(item?.endDate || '').replace(/[^0-9]/g, '') || 'lavanderia';
  return __safeFileName__(`dDAE_Lavanderia_${from}_${to}.png`);
}

async function __laundryDetailImageBlob__(raw){
  const item = sanitizeLaundryItem_(raw || {});
  const prices = item?.laundryPrices && typeof item.laundryPrices === 'object' ? item.laundryPrices : __laundryDisplayPricesForCurrentView__();
  try{ if (document.fonts && document.fonts.ready) await document.fonts.ready; }catch(_){ }

  const defs = __laundryDefsFromSource__(item);
  const rows = defs.map((def) => {
    const k = String(def?.abbreviazione || '').trim();
    const qty = Math.max(0, Number(item?.[k] || 0) || 0);
    const resi = Math.max(0, Number(item?.[`${k}_resi`] || 0) || 0);
    const unit = Math.max(0, Number(prices?.[k] ?? def?.prezzo ?? 0) || 0);
    const subtotal = Math.round(qty * unit * 100) / 100;
    return { key:k, label: __laundryDisplayTitle__(def, k) || k, qty, resi, unit, subtotal, colors: __laundryColorTokens__(def?.colore || 'blue', def?.coloreTesto || '') };
  });
  const imponibile = Math.round(rows.reduce((acc, row) => acc + row.subtotal, 0) * 100) / 100;
  const ivato = Math.round(imponibile * 1.22 * 100) / 100;
  const width = 1200;
  const outerPad = 36;
  const cardPadX = 46;
  const cardPadTop = 42;
  const rowHeight = 120;
  const rowGap = 18;
  const totalsHeight = 92;
  const cardHeight = cardPadTop + 150 + rows.length * rowHeight + (rows.length - 1) * rowGap + 28 + totalsHeight + 42;
  const height = cardHeight + outerPad * 2;

  const canvas = document.createElement('canvas');
  canvas.width = width;
  canvas.height = height;
  const ctx = canvas.getContext('2d');
  if (!ctx) throw new Error('Canvas non disponibile');

  ctx.clearRect(0, 0, width, height);
  const cardX = outerPad;
  const cardY = outerPad;
  const cardW = width - outerPad * 2;
  const cardH = cardHeight;

  ctx.save();
  ctx.shadowColor = 'rgba(11,31,58,0.12)';
  ctx.shadowBlur = 26;
  ctx.shadowOffsetY = 10;
  __laundryRoundRect__(ctx, cardX, cardY, cardW, cardH, 42);
  ctx.fillStyle = '#f6fbff';
  ctx.fill();
  ctx.restore();

  ctx.fillStyle = '#4d9cc5';
  ctx.font = '900 24px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.letterSpacing = '0';
  ctx.fillText('REPORT LAVANDERIA', cardX + cardPadX, cardY + 54);

  ctx.fillStyle = '#0b1f3a';
  ctx.font = '900 52px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText('Dettaglio economico', cardX + cardPadX, cardY + 122);

  ctx.fillStyle = 'rgba(11,31,58,0.78)';
  ctx.font = '800 30px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText(__laundryReportRangeText__(item), cardX + cardPadX, cardY + 178);

  let y = cardY + 212;
  const rowX = cardX + 32;
  const rowW = cardW - 64;
  rows.forEach((row) => {
    const colors = row?.colors || __laundryColorTokens__('blue');
    __laundryRoundRect__(ctx, rowX, y, rowW, rowHeight, 34);
    ctx.fillStyle = colors.rowBg;
    ctx.fill();
    ctx.lineWidth = 2;
    ctx.strokeStyle = colors.rowBorder;
    ctx.stroke();

    const boxY = y + 14;
    __laundryRoundRect__(ctx, rowX + 18, boxY, 92, 92, 24);
    ctx.fillStyle = colors.badgePrimaryBg;
    ctx.fill();
    __laundryRoundRect__(ctx, rowX + 128, boxY, 92, 92, 24);
    ctx.fillStyle = colors.badgeSecondaryBg;
    ctx.fill();

    ctx.fillStyle = colors.badgePrimaryText;
    ctx.textAlign = 'center';
    ctx.font = '900 28px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText(String(row.qty), rowX + 64, y + 56);
    ctx.font = '900 14px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText('USATI', rowX + 64, y + 88);

    ctx.fillStyle = colors.badgeSecondaryText;
    ctx.font = '900 28px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText(String(row.resi), rowX + 174, y + 56);
    ctx.font = '900 14px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText('RESI', rowX + 174, y + 88);

    ctx.textAlign = 'left';
    ctx.fillStyle = colors.label;
    ctx.font = '900 28px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText(row.label, rowX + 250, y + 58);
    ctx.fillStyle = colors.meta;
    ctx.font = '700 22px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText(`${__laundryMoneyFmt__(row.unit)} / pezzo`, rowX + 250, y + 94);

    ctx.textAlign = 'right';
    ctx.fillStyle = colors.price;
    ctx.font = '900 30px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
    ctx.fillText(__laundryMoneyFmt__(row.subtotal), rowX + rowW - 32, y + 74);

    y += rowHeight + rowGap;
  });

  const totalsY = y + 10;
  const totalsGap = 20;
  const totalsW = (rowW - totalsGap) / 2;
  __laundryRoundRect__(ctx, rowX, totalsY, totalsW, totalsHeight, 28);
  ctx.fillStyle = '#0b1f3a';
  ctx.fill();
  __laundryRoundRect__(ctx, rowX + totalsW + totalsGap, totalsY, totalsW, totalsHeight, 28);
  ctx.fillStyle = '#4d9cc5';
  ctx.fill();

  ctx.textAlign = 'left';
  ctx.fillStyle = '#ffffff';
  ctx.font = '900 24px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText('Imponibile', rowX + 26, totalsY + 52);
  ctx.textAlign = 'right';
  ctx.font = '900 34px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText(__laundryMoneyFmt__(imponibile), rowX + totalsW - 26, totalsY + 58);

  ctx.textAlign = 'left';
  ctx.font = '900 22px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText('Totale IVA', rowX + totalsW + totalsGap + 26, totalsY + 42);
  ctx.fillText('22%', rowX + totalsW + totalsGap + 26, totalsY + 72);
  ctx.textAlign = 'right';
  ctx.font = '900 34px -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif';
  ctx.fillText(__laundryMoneyFmt__(ivato), rowX + rowW - 26, totalsY + 58);

  const blob = await new Promise((resolve) => canvas.toBlob(resolve, 'image/png', 1));
  if (blob) return blob;
  const dataUrl = canvas.toDataURL('image/png');
  const base64 = dataUrl.split(',')[1] || '';
  const bin = atob(base64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; i += 1) arr[i] = bin.charCodeAt(i);
  return new Blob([arr], { type: 'image/png' });
}

async function __shareLaundryDetail__(raw){
  const item = sanitizeLaundryItem_(raw || {});
  const blob = await __laundryDetailImageBlob__(item);
  const filename = __laundryDetailFileName__(item);
  const file = new File([blob], filename, { type: 'image/png' });
  try{
    if (navigator.canShare && navigator.canShare({ files:[file] })) {
      await navigator.share({ title: 'Report lavanderia', files:[file] });
      return true;
    }
  }catch(err){
    if (err && err.name === 'AbortError') return false;
  }
  const url = URL.createObjectURL(blob);
  try{
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    document.body.appendChild(a);
    try{ a.click(); }catch(_){ }
    try{ document.body.removeChild(a); }catch(_){ }
    try{ toast('Immagine report pronta', 'blue'); }catch(_){ }
    return true;
  }catch(_){
    return false;
  }finally{
    setTimeout(() => { try{ URL.revokeObjectURL(url); }catch(_){ } }, 1200);
  }
}

function __openLaundryDetailModal__(raw){
  const item = sanitizeLaundryItem_(raw || {});
  const modal = document.getElementById('laundryDetailModal');
  const title = document.getElementById('laundryDetailTitle');
  const range = document.getElementById('laundryDetailRange');
  const list = document.getElementById('laundryDetailList');
  const netEl = document.getElementById('laundryDetailNet');
  const vatEl = document.getElementById('laundryDetailVat');
  const shareBtn = document.getElementById('laundryDetailShare');
  if (!modal || !title || !range || !list || !netEl || !vatEl) return;
  const prices = item?.laundryPrices && typeof item.laundryPrices === 'object' ? item.laundryPrices : __laundryDisplayPricesForCurrentView__();
  const rangeText = __laundryReportRangeText__(item);
  title.textContent = 'Dettaglio economico';
  range.textContent = rangeText;
  let imponibile = 0;
  const defs = __laundryDefsFromSource__(item);
  list.innerHTML = defs.map((def) => {
    const k = String(def?.abbreviazione || '').trim();
    const qty = Math.max(0, Number(item?.[k] || 0) || 0);
    const resi = Math.max(0, Number(item?.[`${k}_resi`] || 0) || 0);
    const unit = Math.max(0, Number(prices?.[k] ?? def?.prezzo ?? 0) || 0);
    const subtotal = Math.round(qty * unit * 100) / 100;
    const colors = __laundryColorTokens__(def?.colore || 'blue', def?.coloreTesto || '');
    imponibile += subtotal;
    const style = [
      `--laundry-report-row-bg:${colors.rowBg}`,
      `--laundry-report-row-border:${colors.rowBorder}`,
      `--laundry-report-badge-primary-bg:${colors.badgePrimaryBg}`,
      `--laundry-report-badge-primary-text:${colors.badgePrimaryText}`,
      `--laundry-report-badge-secondary-bg:${colors.badgeSecondaryBg}`,
      `--laundry-report-badge-secondary-text:${colors.badgeSecondaryText}`,
      `--laundry-report-label:${colors.label}`,
      `--laundry-report-meta:${colors.meta}`,
      `--laundry-report-price:${colors.price}`
    ].join(';');
    return `<div class="laundry-detail-row" style="${__laundryEscapeAttr__(style)}"><div class="laundry-detail-rowLeft"><div class="laundry-detail-badges"><div class="laundry-detail-code"><span class="laundry-detail-codeValue">${qty}</span><span class="laundry-detail-codeLabel">usati</span></div><div class="laundry-detail-code laundry-detail-code--secondary"><span class="laundry-detail-codeValue">${resi}</span><span class="laundry-detail-codeLabel">resi</span></div></div><div class="laundry-detail-copy"><div class="laundry-detail-label">${escapeHtml(__laundryDisplayTitle__(def, k) || k)}</div><div class="laundry-detail-meta">${__laundryMoneyFmt__(unit)} / pezzo</div></div></div><div class="laundry-detail-price">${__laundryMoneyFmt__(subtotal)}</div></div>`;
  }).join('');
  imponibile = Math.round(imponibile * 100) / 100;
  const ivato = Math.round(imponibile * 1.22 * 100) / 100;
  netEl.textContent = __laundryMoneyFmt__(imponibile);
  vatEl.textContent = __laundryMoneyFmt__(ivato);
  modal.__currentLaundryItem = item;
  if (shareBtn && !shareBtn.__boundLaundryShare){
    shareBtn.__boundLaundryShare = true;
    bindFastTap(shareBtn, async () => {
      try{ await __shareLaundryDetail__(modal.__currentLaundryItem || item); }catch(_){ toast('Condivisione non disponibile'); }
    });
  }
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  document.body.classList.add('modal-open');
}

function __closeLaundryDetailModal__(){
  const modal = document.getElementById('laundryDetailModal');
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
  document.body.classList.remove('modal-open');
}

function renderLaundryHistory_(list){
  const host = document.getElementById("laundryHistory");
  if (!host) return;
  host.innerHTML = "";

  try{ list = (Array.isArray(list)?list:[]).filter(it => !(it && (it.isDeleted || it.is_deleted))); }catch(_){ }

  if (!list || !list.length){
    const empty = document.createElement("div");
    empty.className = "item";
    empty.style.opacity = "0.8";
    empty.textContent = "Nessun resoconto ancora.";
    host.appendChild(empty);
    return;
  }

  list.forEach((raw) => {
    const it = sanitizeLaundryItem_(raw);
    const btn = document.createElement("div");
    btn.setAttribute("role","button");
    btn.tabIndex = 0;
    btn.className = "item";
    btn.style.width = "100%";
    btn.style.textAlign = "left";
    btn.style.cursor = "pointer";
    btn.style.display = "flex";
    btn.style.justifyContent = "space-between";
    btn.style.alignItems = "center";
    btn.style.gap = "10px";

    const left = document.createElement("div");
    const startLbl = it.startDate ? formatShortDateIT(it.startDate) : "";
    const endLbl = it.endDate ? formatShortDateIT(it.endDate) : "";
    left.innerHTML = `<div style="font-weight:950">${startLbl} – ${endLbl}</div>`;


    const del = document.createElement("button");
    del.type = "button";
    del.className = "laundry-del";
    del.setAttribute("aria-label", "Elimina report");
    del.innerHTML = `<span class="x">✕</span>`;

    bindFastTap(del, async (ev) => {
      try {
        ev && ev.preventDefault && ev.preventDefault();
        ev && ev.stopPropagation && ev.stopPropagation();
        ev && ev.stopImmediatePropagation && ev.stopImmediatePropagation();
      } catch(_){}

      // Anti-doppio tap / tocchi multipli
      if (del.classList.contains("busy")) return;

      const startLblFull = it.startDate ? formatLongDateIT(it.startDate) : String(it.startDate || "");
      const endLblFull = it.endDate ? formatLongDateIT(it.endDate) : String(it.endDate || "");
      const msg = (startLblFull && endLblFull)
        ? `Eliminare il report lavanderia\n${startLblFull} → ${endLblFull}\n\n(Non tocca le Pulizie)`
        : "Eliminare questo report lavanderia?\n\n(Non tocca le Pulizie)";
      if (!confirm(msg)) return;

      const prevHTML = del.innerHTML;
      del.classList.add("busy");
      del.disabled = true;
      del.innerHTML = `<span class="spinner" aria-hidden="true"></span>`;

      // Nascondi subito in UI (e persisti localmente) per evitare che resti visibile
      try{ __laundryMarkDeleted__(it.id); }catch(_){ }
      try{
        if (state && state.laundry && Array.isArray(state.laundry.list)){
          state.laundry.list = state.laundry.list.filter(x => String(x && x.id) !== String(it.id));
        }
      }catch(_){ }
      try{ btn && btn.remove && btn.remove(); }catch(_){ }

      try{
        try{
          // Soft-delete (preferito): marca come eliminato
          await api("lavanderia", { method:"PUT", body:{ id: it.id, isDeleted:true, is_deleted:true }, showLoader:true });
        }catch(_e){
          // Fallback: DELETE
          await api("lavanderia", { method:"DELETE", body:{ id: it.id }, showLoader:true });
        }
        toast("Report eliminato");
        await loadLavanderia();
      }catch(e){
        console.error(e);
        toast(e && e.message ? e.message : "Errore eliminazione");
        // ripristina solo se l'elemento esiste ancora
        try{
          if (del && del.isConnected){
            del.innerHTML = prevHTML;
          }
        }catch(_){}
      }finally{
        try{
          if (del && del.isConnected){
            del.classList.remove("busy");
            del.disabled = false;
            // se non è stato ripristinato nel catch
            if (!del.querySelector(".x") && !del.querySelector(".spinner")) del.innerHTML = prevHTML;
          }
        }catch(_){}
      }
    }, true);;

    btn.appendChild(left);
    btn.appendChild(del);

    bindFastTap(btn, () => {
      renderLaundry_(it);
      try{ __openLaundryDetailModal__(it); }catch(_){ }
      try{ window.scrollTo({ top: 0, behavior: "smooth" }); }catch(_){
        window.scrollTo(0,0);
      }
    });

    host.appendChild(btn);
  });
}

const __laundryDateRangeState__ = { month:null, start:'', end:'', draftStart:'', draftEnd:'' };

function __laundryDateRangeFormatDisplay__(startDate, endDate){
  const a = String(startDate || '').trim();
  const b = String(endDate || '').trim();
  if (!a && !b) return __designTranslate__('Seleziona date', { en:'Select dates', fr:'Sélectionnez les dates', de:'Daten auswählen', es:'Selecciona fechas' });
  if (a && !b) return formatShortDateIT(a);
  if (a && b) return `${formatShortDateIT(a)} → ${formatShortDateIT(b)}`;
  return __designTranslate__('Seleziona date', { en:'Select dates', fr:'Sélectionnez les dates', de:'Daten auswählen', es:'Selecciona fechas' });
}

function __syncLaundryDateRangeUi__(){
  try{
    const fromEl = document.getElementById('laundryFrom');
    const toEl = document.getElementById('laundryTo');
    const value = document.getElementById('laundryDateRangeValue');
    const startDate = String(fromEl?.value || '').trim();
    const endDate = String(toEl?.value || '').trim();
    if (value) value.textContent = __laundryDateRangeFormatDisplay__(startDate, endDate);
  }catch(_){ }
}

function __renderLaundryDateRangeCalendar__(){
  try{
    const grid = document.getElementById('laundryDateRangeGrid');
    const monthTitle = document.getElementById('laundryDateRangeMonthTitle');
    const rangeTitle = document.getElementById('laundryDateRangeModalTitle');
    const weekdaysWrap = document.getElementById('laundryDateRangeWeekdays');
    if (!grid || !monthTitle || !weekdaysWrap) return;
    weekdaysWrap.innerHTML = __guestDateRangeWeekdayLabels__().map((d)=>`<span>${d}</span>`).join('');
    const monthDate = __laundryDateRangeState__.month || __guestDateRangeMonthStart__();
    __laundryDateRangeState__.month = __guestDateRangeMonthStart__(monthDate);
    const year = __laundryDateRangeState__.month.getFullYear();
    const month = __laundryDateRangeState__.month.getMonth();
    monthTitle.textContent = __laundryDateRangeState__.month.toLocaleDateString(__getCurrentLocale__(), { month:'long', year:'numeric' });
    const first = new Date(year, month, 1);
    const offset = (first.getDay() + 6) % 7;
    const start = new Date(year, month, 1 - offset);
    const todayIso = todayISO();
    const startIso = String(__laundryDateRangeState__.draftStart || '');
    const endIso = String(__laundryDateRangeState__.draftEnd || '');
    __dateRangeCalendarEnsureHeader__('laundryDateRangeModal', startIso, endIso, 'Da', 'A');
    __bindDateRangeCalendarModalTheme__('laundryDateRangeModal');
    const parts = [];
    for(let i=0;i<42;i++) {
      const d = new Date(start.getFullYear(), start.getMonth(), start.getDate()+i);
      const iso = `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
      const inMonth = d.getMonth() === month;
      const classes = ['guest-date-range-day'];
      if (!inMonth) classes.push('is-muted');
      if (__guestDateRangeSameDay__(iso, todayIso)) classes.push('is-today');
      if (__guestDateRangeSameDay__(iso, startIso) || __guestDateRangeSameDay__(iso, endIso)) classes.push('is-selected','is-edge');
      else if (__guestDateRangeInBetween__(iso, startIso, endIso)) classes.push('is-in-range');
      parts.push(`<button class="${classes.join(' ')}" data-date="${iso}" type="button">${d.getDate()}</button>`);
    }
    grid.innerHTML = parts.join('');
    if (rangeTitle) rangeTitle.textContent = __laundryDateRangeFormatDisplay__(startIso, endIso);
  }catch(_){ }
}

function __openLaundryDateRangeModal__(){
  try{
    const modal = document.getElementById('laundryDateRangeModal');
    const fromEl = document.getElementById('laundryFrom');
    const toEl = document.getElementById('laundryTo');
    if (!modal || !fromEl || !toEl) return;
    const startDateRaw = String(fromEl.value || '').trim();
    const startDate = startDateRaw || todayISO();
    const endRaw = String(toEl.value || '').trim();
    const endDate = (endRaw && endRaw >= startDate) ? endRaw : startDate;
    __laundryDateRangeState__.start = startDate;
    __laundryDateRangeState__.end = endDate;
    __laundryDateRangeState__.draftStart = startDate;
    __laundryDateRangeState__.draftEnd = endDate;
    __laundryDateRangeState__.month = __guestDateRangeMonthStart__(parseDateTs(startDate) != null ? new Date(parseDateTs(startDate)) : new Date());
    __renderLaundryDateRangeCalendar__();
    modal.hidden = false;
    modal.classList.add('is-open');
    modal.setAttribute('aria-hidden', 'false');
    document.body.classList.add('modal-open');
  }catch(_){ }
}

function __closeLaundryDateRangeModal__(restore = false){
  try{
    const modal = document.getElementById('laundryDateRangeModal');
    if (!modal) return;
    if (restore){
      __laundryDateRangeState__.draftStart = __laundryDateRangeState__.start || '';
      __laundryDateRangeState__.draftEnd = __laundryDateRangeState__.end || '';
    }
    modal.classList.remove('is-open');
    modal.hidden = true;
    modal.setAttribute('aria-hidden', 'true');
    document.body.classList.remove('modal-open');
  }catch(_){ }
}

const __speseDateRangeState__ = { month:null, start:'', end:'', draftStart:'', draftEnd:'' };
function __speseDateRangeFormatDisplay__(fromDate, toDate){
  const a = String(fromDate || '').trim();
  const b = String(toDate || '').trim();
  if (!a && !b) return __designTranslate__('Seleziona date', { en:'Select dates', fr:'Sélectionnez les dates', de:'Daten auswählen', es:'Selecciona fechas' });
  if (a && !b) return formatShortDateIT(a);
  if (a && b) return `${formatShortDateIT(a)} → ${formatShortDateIT(b)}`;
  return __designTranslate__('Seleziona date', { en:'Select dates', fr:'Sélectionnez les dates', de:'Daten auswählen', es:'Selecciona fechas' });
}
function __syncSpeseDateRangeUi__(){
  try{
    const value = document.getElementById('speseDateRangeValue');
    const fromDate = String(state?.period?.from || '').trim();
    const toDate = String(state?.period?.to || '').trim();
    if (value) value.textContent = __speseDateRangeFormatDisplay__(fromDate, toDate);
  }catch(_){ }
}
function __renderSpeseDateRangeCalendar__(){
  try{
    const grid = document.getElementById('speseDateRangeGrid');
    const monthTitle = document.getElementById('speseDateRangeMonthTitle');
    const rangeTitle = document.getElementById('speseDateRangeModalTitle');
    const weekdaysWrap = document.getElementById('speseDateRangeWeekdays');
    if (!grid || !monthTitle || !weekdaysWrap) return;
    weekdaysWrap.innerHTML = __guestDateRangeWeekdayLabels__().map((d)=>`<span>${d}</span>`).join('');
    const monthDate = __speseDateRangeState__.month || __guestDateRangeMonthStart__();
    __speseDateRangeState__.month = __guestDateRangeMonthStart__(monthDate);
    const year = __speseDateRangeState__.month.getFullYear();
    const month = __speseDateRangeState__.month.getMonth();
    monthTitle.textContent = __speseDateRangeState__.month.toLocaleDateString(__getCurrentLocale__(), { month:'long', year:'numeric' });
    const first = new Date(year, month, 1);
    const offset = (first.getDay() + 6) % 7;
    const start = new Date(year, month, 1 - offset);
    const todayIso = todayISO();
    const startIso = String(__speseDateRangeState__.draftStart || '');
    const endIso = String(__speseDateRangeState__.draftEnd || '');
    __dateRangeCalendarEnsureHeader__('speseDateRangeModal', startIso, endIso, 'Da', 'A');
    __bindDateRangeCalendarModalTheme__('speseDateRangeModal');
    const parts = [];
    for(let i=0;i<42;i++) {
      const d = new Date(start.getFullYear(), start.getMonth(), start.getDate()+i);
      const iso = `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
      const inMonth = d.getMonth() === month;
      const classes = ['guest-date-range-day'];
      if (!inMonth) classes.push('is-muted');
      if (__guestDateRangeSameDay__(iso, todayIso)) classes.push('is-today');
      if (__guestDateRangeSameDay__(iso, startIso) || __guestDateRangeSameDay__(iso, endIso)) classes.push('is-selected','is-edge');
      else if (__guestDateRangeInBetween__(iso, startIso, endIso)) classes.push('is-in-range');
      parts.push(`<button class="${classes.join(' ')}" data-date="${iso}" type="button">${d.getDate()}</button>`);
    }
    grid.innerHTML = parts.join('');
    if (rangeTitle) rangeTitle.textContent = __speseDateRangeFormatDisplay__(startIso, endIso);
  }catch(_){ }
}
function __selectSpeseDateRangeDay__(iso){
  const start = String(__speseDateRangeState__.draftStart || '');
  const end = String(__speseDateRangeState__.draftEnd || '');
  if (!start || (start && end)){
    __speseDateRangeState__.draftStart = iso;
    __speseDateRangeState__.draftEnd = '';
  } else if (iso < start){
    __speseDateRangeState__.draftStart = iso;
  } else {
    __speseDateRangeState__.draftEnd = iso;
  }
  __renderSpeseDateRangeCalendar__();
}
function __openSpeseDateRangeModal__(){
  try{
    const modal = document.getElementById('speseDateRangeModal');
    if (!modal) return;
    const fromDate = String(state?.period?.from || '').trim() || todayISO();
    const toDateRaw = String(state?.period?.to || '').trim();
    const toDate = (toDateRaw && toDateRaw >= fromDate) ? toDateRaw : fromDate;
    __speseDateRangeState__.start = fromDate;
    __speseDateRangeState__.end = toDate;
    __speseDateRangeState__.draftStart = fromDate;
    __speseDateRangeState__.draftEnd = toDate;
    __speseDateRangeState__.month = __guestDateRangeMonthStart__(parseDateTs(fromDate) != null ? new Date(parseDateTs(fromDate)) : new Date());
    __renderSpeseDateRangeCalendar__();
    modal.hidden = false;
    modal.classList.add('is-open');
    modal.setAttribute('aria-hidden', 'false');
    document.body.classList.add('modal-open');
  }catch(_){ }
}
function __closeSpeseDateRangeModal__(restore = false){
  try{
    const modal = document.getElementById('speseDateRangeModal');
    if (!modal) return;
    if (restore){
      __speseDateRangeState__.draftStart = __speseDateRangeState__.start || '';
      __speseDateRangeState__.draftEnd = __speseDateRangeState__.end || '';
    }
    modal.classList.remove('is-open');
    modal.hidden = true;
    modal.setAttribute('aria-hidden', 'true');
    document.body.classList.remove('modal-open');
  }catch(_){ }
}
async function __applySpeseDateRangeModal__(){
  try{
    let start = String(__speseDateRangeState__.draftStart || '').trim();
    let end = String(__speseDateRangeState__.draftEnd || '').trim();
    if (!start && !end) return __closeSpeseDateRangeModal__(true);
    if (start && !end) end = start;
    if (end && !start) start = end;
    if (end < start){ const swap = start; start = end; end = swap; }
    __speseDateRangeState__.start = start;
    __speseDateRangeState__.end = end;
    setPeriod(start, end);
    __closeSpeseDateRangeModal__(false);
    await onPeriodChanged({ showLoader:false });
  }catch(_){ }
}
function __bindSpeseDateRangeUi__(){
  try{
    const trigger = document.getElementById('speseDateRangeTrigger');
    const modal = document.getElementById('speseDateRangeModal');
    if (trigger && !trigger.__boundSpeseRangeModal){
      trigger.__boundSpeseRangeModal = true;
      bindFastTap(trigger, () => { try{ __openSpeseDateRangeModal__(); }catch(_){ } });
    }
    if (modal && !modal.__boundSpeseRangeModal){
      modal.__boundSpeseRangeModal = true;
      const closeModal = () => { try{ __closeSpeseDateRangeModal__(true); }catch(_){ } };
      ['speseDateRangeModalClose','speseDateRangeCancel'].forEach((id) => { const el = document.getElementById(id); if (el) bindFastTap(el, closeModal); });
      const applyBtn = document.getElementById('speseDateRangeApply');
      if (applyBtn) bindFastTap(applyBtn, () => { try{ __applySpeseDateRangeModal__(); }catch(_){ } });
      const prevBtn = document.getElementById('speseDateRangePrev');
      if (prevBtn) bindFastTap(prevBtn, () => { try{ const m = __speseDateRangeState__.month || __guestDateRangeMonthStart__(); __speseDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()-1, 1); __renderSpeseDateRangeCalendar__(); }catch(_){ } });
      const nextBtn = document.getElementById('speseDateRangeNext');
      if (nextBtn) bindFastTap(nextBtn, () => { try{ const m = __speseDateRangeState__.month || __guestDateRangeMonthStart__(); __speseDateRangeState__.month = new Date(m.getFullYear(), m.getMonth()+1, 1); __renderSpeseDateRangeCalendar__(); }catch(_){ } });
      modal.addEventListener('click', (ev) => { try{ if (ev.target === modal) closeModal(); }catch(_){ } });
      const grid = document.getElementById('speseDateRangeGrid');
      try{ __bindDateRangeCalendarHold__(grid); }catch(_){ }
      if (grid) grid.addEventListener('click', (ev) => {
        const btn = ev.target.closest?.('.guest-date-range-day[data-date]');
        if (!btn) return;
        const iso = String(btn.dataset.date || '').trim();
        if (!iso) return;
        __selectSpeseDateRangeDay__(iso);
      });
    }
    __syncSpeseDateRangeUi__();
  }catch(_){ }
}

function __applyLaundryDateRangeModal__(){
  try{
    const fromEl = document.getElementById('laundryFrom');
    const toEl = document.getElementById('laundryTo');
    if (!fromEl || !toEl) return;
    let startDate = String(__laundryDateRangeState__.draftStart || '').trim();
    let endDate = String(__laundryDateRangeState__.draftEnd || '').trim();
    if (!startDate) startDate = todayISO();
    if (!endDate || endDate < startDate) endDate = startDate;
    __laundryDateRangeState__.start = startDate;
    __laundryDateRangeState__.end = endDate;
    fromEl.value = startDate;
    toEl.value = endDate;
    syncLaundryDateText_();
    __closeLaundryDateRangeModal__(false);
  }catch(_){ }
}

function __selectLaundryDateRangeDay__(iso){
  try{
    const startDate = String(__laundryDateRangeState__.draftStart || '');
    const endDate = String(__laundryDateRangeState__.draftEnd || '');
    if (!startDate || (startDate && endDate)){
      __laundryDateRangeState__.draftStart = iso;
      __laundryDateRangeState__.draftEnd = '';
    } else if (iso < startDate){
      __laundryDateRangeState__.draftStart = iso;
    } else {
      __laundryDateRangeState__.draftEnd = iso;
    }
    __renderLaundryDateRangeCalendar__();
  }catch(_){ }
}

function syncLaundryDateText_(){
  try{ __syncLaundryDateRangeUi__(); }catch(_){ }
}

async function loadLavanderia() {
  setLaundryLabels_();
  try{ await ensureSettingsLoaded({ force:false, showLoader:false }); }catch(_){ }
  __bindLaundryPriceLongPress__();
  __laundryUpdatePriceHints__();
  const hint = document.getElementById("laundryHint");
  try {
    const res = await api("lavanderia", { method:"GET", showLoader:false });
    const rows = Array.isArray(res) ? res
      : (res && Array.isArray(res.data) ? res.data
      : (res && res.data && Array.isArray(res.data.data) ? res.data.data
      : (res && Array.isArray(res.rows) ? res.rows
      : [])));
    const rawList = (rows || []).map(sanitizeLaundryItem_);
    const list = rawList.filter(it => !(it && (it.isDeleted || it.is_deleted))).sort((a,b) => {
      const byEnd = String(b.endDate||"").localeCompare(String(a.endDate||""));
      if (byEnd) return byEnd;
      return String(b.updatedAt||b.createdAt||"").localeCompare(String(a.updatedAt||a.createdAt||""));
    });

    state.laundry.list = list;
    renderLaundryHistory_(list);
    renderLaundry_(list[0] || null);
    if (hint) hint.textContent = "";
  } catch (e) {
    if (hint) hint.textContent = "";
    throw e;
  }
}

async function createLavanderiaReport_() {
  const hint = document.getElementById("laundryHint");
  const fromEl = document.getElementById("laundryFrom");
  const toEl = document.getElementById("laundryTo");

  const startDate = (fromEl && fromEl.value) ? String(fromEl.value).trim() : "";
  const endDate = (toEl && toEl.value) ? String(toEl.value).trim() : "";
  try{ if (typeof __laundrySyncDateText === "function") __laundrySyncDateText(); }catch(_){ }

  if (!startDate || !endDate) {
    if (hint) hint.textContent = "";
    toast("Seleziona le date");
    return null;
  }
  if (startDate > endDate) {
    if (hint) hint.textContent = "";
    toast("Intervallo non valido");
    return null;
  }

  if (hint) hint.textContent = "";
  const res = await api("lavanderia", { method:"POST", body: { startDate, endDate }, showLoader:true });
  const item = sanitizeLaundryItem_(res && res.data ? res.data : res);

  await loadLavanderia();
  renderLaundry_(item);

  if (hint) hint.textContent = "";
  return item;
}


/* Service Worker: forza update su iOS (cache-bust via query) */
async function registerSW(){
  if (!("serviceWorker" in navigator)) return;
  try {
    // Query param = BUILD_VERSION -> forza fetch del file SW anche con cache aggressiva
    const reg = await navigator.serviceWorker.register(`./service-worker.js?v=${BUILD_VERSION}`, {
      updateViaCache: "none"
    });

    const checkUpdate = () => {
      try {
        const p = reg?.update?.();
        // Safari/iOS può rigettare la Promise con "Cannot update a null/nonexistent service worker registration"
        // se la registration è stata invalidata: evitiamo un unhandled rejection.
        if (p && typeof p.catch === "function") p.catch(() => {});
      } catch (_) {}
    };

    // check immediato + quando torna in primo piano
    checkUpdate();
    window.addEventListener("focus", checkUpdate);
    document.addEventListener("visibilitychange", () => {
      if (!document.hidden) checkUpdate();
    });
    // check periodico (non invasivo)
    setInterval(checkUpdate, 60 * 60 * 1000);

    // Se viene trovata una nuova versione, prova ad attivarla subito
    reg.addEventListener("updatefound", () => {
      const nw = reg.installing;
      if (!nw) return;
      nw.addEventListener("statechange", () => {
        if (nw.state === "installed" && navigator.serviceWorker.controller) {
          try { nw.postMessage({ type: "SKIP_WAITING" }); } catch (_) {}
        }
      });
    });

    // se cambia controller, ricarica una volta per prendere i file nuovi
    let reloaded = false;
    navigator.serviceWorker.addEventListener("controllerchange", () => {
      if (reloaded) return;
      reloaded = true;
      __requestSwReload();
    });
  } catch (_) {}
}
registerSW();





try{ hardUpdateCheck(); }catch(_){}

// iOS/PWA: quando l'app torna in foreground (senza un vero reload), alcune viste possono restare "stale".
// Forziamo un refresh mirato del Calendario se e' la pagina attiva.
async function __onAppResume(){
  // Se nel frattempo e' stata deployata una nuova build, hardUpdateCheck fara' reload.
  try{ await hardUpdateCheck(); }catch(_){ }

  try{
    if (state.page === "calendario") {
      if (state.calendar){ state.calendar.ready = false; }
      await ensureCalendarData({ force:true, showLoader:false });
      renderCalendario();
    }
  }catch(_){ }
}

try{
  window.addEventListener("focus", () => { __onAppResume(); });
  document.addEventListener("visibilitychange", () => {
    if (!document.hidden) __onAppResume();
  });
}catch(_){ }
// ---  helpers (sheet "stanze") ---
function buildArrayFromState(){
  const rooms = Array.from(state.guestRooms || []).map(n=>parseInt(n,10)).filter(n=>isFinite(n)).sort((a,b)=>a-b);
  const lp = state.lettiPerStanza || {};
  return rooms.map((n)=>{
    const d = lp[String(n)] || lp[n] || {};
    return {
      stanza_num: n,
      letto_m: !!d.matrimoniale,
      letto_s: parseInt(d.singoli || 0, 10) || 0,
      culla: !!d.culla,
      note: (d.note || "").toString()
    };
  });
}

function applyToState(rows){
  state.guestRooms = state.guestRooms || new Set();
  state.lettiPerStanza = {};
  state.bedsDirty = false;
  state.stanzeSnapshotOriginal = "";
  state.guestRooms.clear();
  (Array.isArray(rows) ? rows : []).forEach(r=>{
    const n = parseInt(r.stanza_num ?? r.stanzaNum ?? r.room ?? r.stanza, 10);
    if (!isFinite(n) || n<=0) return;
    state.guestRooms.add(n);
    state.lettiPerStanza[String(n)] = {
      matrimoniale: !!(r.letto_m ?? r.lettoM ?? r.matrimoniale),
      singoli: parseInt(r.letto_s ?? r.lettoS ?? r.singoli, 10) || 0,
      culla: !!(r.culla),
      note: (r.note || "").toString()
    };
  });
}

// --- Room beds config (non-invasive) ---
state.lettiPerStanza = state.lettiPerStanza || {};
let __rc_room = null;

function __rc_dotStyle__(spec){
  const pair = __roomsUiColorPair__(spec, 'blue-4');
  const main = __operatoreColorHex__(pair.bg || 'blue-4');
  return `--rc-dot-fill:${hexToRgba(main, 0.92)};--rc-dot-border:${hexToRgba(main, 0.95)};`;
}

function __rc_renderToggle(el, on){
  const key = String(el?.id || '').trim();
  const cfg = getRoomsUiConfig();
  const spec = key === 'rc_matrimoniale'
    ? (cfg.beds?.matrimoniale || 'red-4')
    : key === 'rc_culla'
      ? (cfg.beds?.culla || 'yellow-4')
      : (cfg.beds?.singolo || 'blue-4');
  el.innerHTML = `<span class="dot ${on?'on':''}" style="${__rc_dotStyle__(spec)}"></span>`;
  el.onclick = ()=> el.firstElementChild.classList.toggle('on');
}
function __rc_renderSingoli(el, n){
  el.innerHTML = '';
  const spec = getRoomsUiConfig().beds?.singolo || 'blue-4';
  const style = __rc_dotStyle__(spec);
  for(let i=1;i<=3;i++){
    const s=document.createElement('span');
    s.className='dot'+(i<=n?' on':'');
    s.setAttribute('style', style);
    s.onclick=()=>{
      [...el.children].forEach((c,ix)=>c.classList.toggle('on', ix < i));
    };
    el.appendChild(s);
  }
}

function openRoomConfig(room){
  __rc_room = String(room);
  const d = state.lettiPerStanza[__rc_room] || {matrimoniale:false,singoli:0,culla:false};
  document.getElementById('roomConfigTitle').textContent = 'Stanza '+room;
  __rc_renderToggle(document.getElementById('rc_matrimoniale'), d.matrimoniale);
  __rc_renderSingoli(document.getElementById('rc_singoli'), d.singoli);
  __rc_renderToggle(document.getElementById('rc_culla'), d.culla);
  document.getElementById('roomConfigModal').hidden = false;
}


document.getElementById('rc_save')?.addEventListener('click', ()=>{
  const matrimoniale = document.querySelector('#rc_matrimoniale .dot')?.classList.contains('on')||false;
  const culla = document.querySelector('#rc_culla .dot')?.classList.contains('on')||false;
  const singoli = document.querySelectorAll('#rc_singoli .dot.on').length;
  state.lettiPerStanza[__rc_room] = {matrimoniale, singoli, culla};
  state.bedsDirty = true;
  document.getElementById('roomConfigModal').hidden = true;
});

// Popup letti: Annulla (chiudi senza salvare)
document.getElementById('rc_cancel')?.addEventListener('click', ()=>{
  const m = document.getElementById('roomConfigModal');
  if (m) m.hidden = true;
});
// --- end room beds config ---


// --- FIX dDAE_1.020: renderSpese allineato al backend ---
// --- dDAE: Spese riga singola (senza IVA in visualizzazione) ---
function __speseFilterTitle__(key){
  const k = String(key || '').trim();
  const map = {
    CONTANTI: 'Contanti',
    TASSA_SOGGIORNO: 'Tassa',
    IVA_22: 'IVA 22%',
    IVA_10: 'IVA 10%',
    IVA_4: 'IVA 4%',
    FUORI_BUDGET: 'Extra'
  };
  return map[k] || (k ? categoriaLabel(k) : 'Spese');
}

function __updateSpeseTotalCard__(items, selectedKey){
  try{
    const label = document.getElementById('speseTotalLabel');
    const value = document.getElementById('speseTotalValue');
    if (!label || !value) return;
    const safeKey = String(selectedKey || '').trim();
    const rows = Array.isArray(items) ? items : [];
    const total = rows.reduce((sum, row) => sum + (Number(row?.importoLordo || row?.importo_lordo || row?.importo || 0) || 0), 0);
    label.textContent = (__speseBudgetModeIsFuori__() ? 'Totale Fuori Budget ' : 'Totale ') + __speseFilterTitle__(safeKey);
    value.textContent = euro(total);
  }catch(_){ }
}

function renderSpese(){
  try{ __syncSpeseBudgetToggles__(); }catch(_){ }
  const list = document.getElementById("speseList");
  if (!list) return;
  list.innerHTML = "";

  let items = __filterSpeseBudgetRows__(Array.isArray(state.spese) ? state.spese : [], __speseBudgetModeRead__());
  const categoryFilter = String(state.speseCategoryFilter || '').trim().toLowerCase();
  if (categoryFilter){
    items = items.filter((row) => String(row?.categoria || row?.cat || '').trim().toLowerCase() === categoryFilter);
  }
  try{ __updateSpeseTotalCard__(items, String(state.speseCategoryFilter || '').trim()); }catch(_){ }

  // Ordina: data / inserimento / motivazione
  const mode = String(state.speseSort || "date");
  const withIdx = items.map((s, idx) => ({ s, idx }));

  const toTime = (v) => {
    if (!v) return null;
    const s = String(v);
    const iso = s.slice(0, 10);
    if (/^\d{4}-\d{2}-\d{2}$/.test(iso)){
      const t = Date.parse(iso + "T00:00:00Z");
      return isNaN(t) ? null : t;
    }
    const t = Date.parse(s);
    return isNaN(t) ? null : t;
  };

  withIdx.sort((a, b) => {
    if (mode === "motivazione"){
      const am = (a.s.motivazione || a.s.motivo || "").toString().trim().toLowerCase();
      const bm = (b.s.motivazione || b.s.motivo || "").toString().trim().toLowerCase();
      const c = am.localeCompare(bm, "it", { sensitivity: "base" });
      return c !== 0 ? c : (a.idx - b.idx);
    }

    if (mode === "insert"){
      const ta = toTime(a.s.createdAt || a.s.created_at) ?? a.idx;
      const tb = toTime(b.s.createdAt || b.s.created_at) ?? b.idx;
      // Nuovi prima
      return (tb - ta);
    }

    // mode === "date" (default): più recenti prima
    const da = toTime(a.s.dataSpesa || a.s.data || a.s.data_spesa);
    const db = toTime(b.s.dataSpesa || b.s.data || b.s.data_spesa);
    if (da == null && db == null) return a.idx - b.idx;
    if (da == null) return 1;
    if (db == null) return -1;
    return (db - da);
  });

  items = withIdx.map(x => x.s);
  if (!items.length){
    list.innerHTML = `<div style="font-size:13px; opacity:.75; padding:8px 2px;">Nessuna spesa nel periodo.</div>`;
    return;
  }

  items.forEach(s => {
    const el = document.createElement("div");
    el.className = "item spesa-bg";
    try{ el.__spesaRow = s; }catch(_){ }
    const cls = spesaCategoryClass(s);
    if (cls) el.classList.add(cls);
    __applySpesaCardColor__(el, s);

    const importo = Number(s.importoLordo || 0);
    const data = formatShortDateIT(s.dataSpesa || s.data || s.data_spesa || "");
    const motivoTxt = (s.motivazione || s.motivo || "").toString();
    const motivo = escapeHtml(motivoTxt);
    const categoria = escapeHtml(__spesaCategoriaDisplay__(s));

    el.innerHTML = `
      <div class="item-top" style="align-items:center;">
        <div class="spesa-line" title="${motivo}">
          <span class="spesa-imp">${euro(importo)}</span>
          <span class="spesa-sep">·</span>
          <span class="spesa-date">${data}</span>
          <span class="spesa-sep">·</span>
          <span class="spesa-motivo">${motivo}</span>${categoria ? `<span class="spesa-sep">·</span><span class="spesa-cat">${categoria}</span>` : ''}
        </div>
        <button class="delbtn delbtn-x" type="button" aria-label="Elimina record" data-del="${s.id}">Elimina</button>
      </div>
    `;

    __bindSpesaCardEditLongPress__(el, s);
    __bindSpesaCardColorLongPress__(el, s);

    const btn = el.querySelector("[data-del]");
    if (btn) btn.addEventListener("click", async () => {
      if (!confirm("Eliminare definitivamente questa spesa?")) return;
      await api("spese", { method:"DELETE", params:{ id: s.id } });
      toast("Spesa eliminata");
      invalidateApiCache("spese|");
      invalidateApiCache("report|");
      try{
        if (Array.isArray(state.spese)) state.spese = state.spese.filter(row => String(row?.id || '') !== String(s.id || ''));
        if (Array.isArray(state.speseAll)) state.speseAll = state.speseAll.filter(row => String(row?.id || '') !== String(s.id || ''));
        state.report = buildReportFromSpese(Array.isArray(state.spese) ? state.spese : []);
        state.reportAll = buildReportFromSpese(Array.isArray(state.speseAll) ? state.speseAll : (Array.isArray(state.spese) ? state.spese : []));
      }catch(_){ }
      await ensurePeriodData({ showLoader:false, force:true });
      renderSpese();
    });

    list.appendChild(el);
  });
}



// --- FIX dDAE_1.020: delete reale ospiti ---
function attachDeleteOspite(card, ospite){
  const btn = document.createElement("button");
  btn.className = "delbtn";
  btn.textContent = "Elimina";
  btn.addEventListener("click", async () => {
    if (!confirm("Eliminare definitivamente questo ospite?")) return;
    try{ __sfxGlass(); }catch(_){ }
    await api("ospiti", { method:"DELETE", params:{ id: ospite.id } });
    toast("Ospite eliminato");
    invalidateApiCache("ospiti|");
    try{ refreshTopGuestAlerts({ force:true }); }catch(_){ }
    invalidateApiCache("stanze|");
    try{ if (state.calendar){ state.calendar.ready = false; state.calendar.rangeKey = ""; state.calendar.winFrom = ""; state.calendar.winTo = ""; } state._roomsAvailKey = ""; }catch(_){ }
    await loadOspiti({ ...(state.period || {}), force:true });
  });
  const actions = card.querySelector(".actions") || card;
  actions.appendChild(btn);
}


// Hook delete button into ospiti render
(function(){
  const orig = window.renderOspiti;
  if (!orig) return;
  window.renderOspiti = function(){
    orig();
    const cards = document.querySelectorAll(".guest-card");
    cards.forEach(card => {
      const id = card.getAttribute("data-id");
      const ospite = (state.ospiti||[]).find(o=>String(o.id)===String(id));
      if (ospite) attachDeleteOspite(card, ospite);
    });
  }
})();


// --- FIX dDAE_1.020: mostra nome ospite ---
(function(){
  const orig = window.renderOspiti;
  if (!orig) return;
  window.renderOspiti = function(){
    orig();
    document.querySelectorAll(".guest-card").forEach(card=>{
      const id = card.getAttribute("data-id");
      const ospite = (state.ospiti||[]).find(o=>String(o.id)===String(id));
      if(!ospite) return;
      if(card.querySelector(".guest-name")) return;
      const name = document.createElement("div");
      name.className = "guest-name";
      name.textContent = ospite.nome || ospite.name || "Ospite";
      name.style.fontWeight = "950";
      name.style.fontSize = "18px";
      name.style.marginBottom = "6px";
      card.prepend(name);
    });
  }
})();



// ===== Tassa di soggiorno =====
let __tassaBound = false;

function __parseDateFlexibleToISO(unknown){
  // Ritorna ISO YYYY-MM-DD oppure "" se non parsabile
  const s = String(unknown || "").trim();
  if (!s) return "";
  // ISO date (YYYY-MM-DD) or ISO datetime
  const mIso = s.match(/^(\d{4})-(\d{2})-(\d{2})/);
  if (mIso) return `${mIso[1]}-${mIso[2]}-${mIso[3]}`;
  // dd/mm/yyyy
  const mIt = s.match(/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/);
  if (mIt){
    const dd = String(mIt[1]).padStart(2,"0");
    const mm = String(mIt[2]).padStart(2,"0");
    const yy = mIt[3];
    return `${yy}-${mm}-${dd}`;
  }
  return "";
}

function __utcDay(y,m,d){ return Date.UTC(y, m-1, d); }

function __daysBetweenUTC(isoA, isoB){
  // isoA, isoB: YYYY-MM-DD ; ritorna giorni interi (B - A)
  const [ya,ma,da] = isoA.split("-").map(n=>parseInt(n,10));
  const [yb,mb,db] = isoB.split("-").map(n=>parseInt(n,10));
  const ta = __utcDay(ya,ma,da);
  const tb = __utcDay(yb,mb,db);
  return Math.round((tb - ta) / 86400000);
}

function __addDaysISO(iso, delta){
  const [y,m,d] = iso.split("-").map(n=>parseInt(n,10));
  const dt = new Date(Date.UTC(y, m-1, d));
  dt.setUTCDate(dt.getUTCDate() + delta);
  const yy = dt.getUTCFullYear();
  const mm = String(dt.getUTCMonth()+1).padStart(2,"0");
  const dd = String(dt.getUTCDate()).padStart(2,"0");
  return `${yy}-${mm}-${dd}`;
}

function __overlapNights(checkInISO, checkOutISO, fromISO, toISO_inclusive){
  // Intersezione tra [checkIn, checkOut) e [from, to+1)
  const toExcl = __addDaysISO(toISO_inclusive, 1);
  const start = (checkInISO > fromISO) ? checkInISO : fromISO;
  const end   = (checkOutISO < toExcl) ? checkOutISO : toExcl;
  const n = __daysBetweenUTC(start, end);
  return (isFinite(n) && n > 0) ? n : 0;
}

function resetTassaUI(){
  const res = $("#taxResults");
  if (res) res.hidden = true;
  const tt = $("#taxTotalRow");
  if (tt) tt.hidden = true;
  const rb = $("#taxReportBtn");
  if (rb) rb.disabled = true;

  const ids = ["taxPayingCount","taxPayingAmount","taxKidsCount","taxKidsAmount","taxReducedCount","taxReducedAmount"];
  ids.forEach(id => { const el = $("#"+id); if (el) el.textContent = "—"; });
  const ta = $("#taxTotalAmount");
  if (ta) ta.textContent = "—";
}

async function calcTassa(fromOverride, toOverride, opts){
  opts = opts || {};
  const includeUntagged = !!opts.includeUntagged;
  const fromEl = $("#taxFrom");
  const toEl = $("#taxTo");
  const from = (fromOverride || (fromEl ? fromEl.value : "")) || "";
  const to   = (toOverride   || (toEl ? toEl.value : ""))   || "";
  if (!from || !to){
    toast("Seleziona un periodo (Da/A)");
    resetTassaUI();
    return;
  }
  if (to < from){
    toast("Il periodo non è valido");
    resetTassaUI();
    return;
  }

  // Prende i dati SOLO dalle prenotazioni (foglio ospiti)
  const ospiti = await api("ospiti", { method:"GET" }) || [];

  let schede = 0;
  let adultsTot = 0;
  let kidsTot = 0;
  let taxableDaysTot = 0;
  let totalAmt = 0;

  for (const o of ospiti){
    const inISO  = __parseDateFlexibleToISO(o.check_in || o.checkIn);
    const outISO = __parseDateFlexibleToISO(o.check_out || o.checkOut);
    if (!inISO || !outISO) continue;
    // Tassa: per default considera SOLO prenotazioni con registrazioni PS o ISTAT
    if (!includeUntagged){
      const psReg = truthy(o.ps_registrato ?? o.psRegistrato);
      const istatReg = truthy(o.istat_registrato ?? o.istatRegistrato);
      if (!(psReg || istatReg)) continue;
    }
    const nights = __overlapNights(inISO, outISO, from, to);
    if (!nights) continue;

    // Somma SOLO il totale tassa della scheda (pill), con la stessa logica della pill
    const tt = calcTouristTax(o, nights);
    schede += 1;
    adultsTot += Number(tt?.adults || 0) || 0;

    const kidsRaw = (o.bambini_u10 ?? o.bambiniU10 ?? o.kids_u10 ?? o.kidsU10 ?? o.bambini ?? o.kids ?? 0);
    kidsTot += Math.max(0, parseInt(kidsRaw, 10) || 0);

    taxableDaysTot += Number(tt?.taxableDays || 0) || 0;
    totalAmt += Number(tt?.total || 0) || 0;
  }

  const rate = (state.settings && state.settings.loaded)
    ? (getSettingNumber("tassa_soggiorno", (typeof TOURIST_TAX_EUR_PPN !== "undefined" ? TOURIST_TAX_EUR_PPN : 0)) || 0)
    : (Number(typeof TOURIST_TAX_EUR_PPN !== "undefined" ? TOURIST_TAX_EUR_PPN : 0) || 0);

  // salva per report
  state._taxLast = { from, to, schede, adultsTot, kidsTot, taxableDaysTot, rate, totalAmt, mode: (includeUntagged ? "stima" : "taggate") };

  // UI: mostra solo dopo click Calcola
  const res = $("#taxResults");
  if (res) res.hidden = false;
  const ttRow = $("#taxTotalRow");
  if (ttRow) ttRow.hidden = false;
  const ttVal = $("#taxTotalAmount");
  if (ttVal) ttVal.textContent = formatEUR(totalAmt);
  const rb = $("#taxReportBtn");
  if (rb) rb.disabled = false;

  const pc = $("#taxPayingCount"); if (pc) pc.textContent = String(adultsTot);
  const pa = $("#taxPayingAmount"); if (pa) pa.textContent = formatEUR(totalAmt);

  const kc = $("#taxKidsCount"); if (kc) kc.textContent = String(kidsTot);
  const ka = $("#taxKidsAmount"); if (ka) ka.textContent = "—"; // non applicabile

  const rc = $("#taxReducedCount"); if (rc) rc.textContent = "0";
  const ra = $("#taxReducedAmount"); if (ra) ra.textContent = "—"; // non applicabile
}



function buildTaxReportText(){
  const t = state._taxLast;
  if (!t) return "Premi prima Calcola.";
  const lines = [];
  lines.push("Report tassa di soggiorno");
  lines.push(`Periodo: ${t.from} → ${t.to}`);
  lines.push("");
  lines.push(`Schede incluse: ${t.schede} (${t.mode === "stima" ? "tutte (anche non taggate)" : "solo taggate (PS o ISTAT)"})`);
  lines.push(`Adulti (somma): ${t.adultsTot}`);
  lines.push(`Bambini <10 (somma): ${t.kidsTot || 0}`);
  const maxTaxNights = Math.max(0, parseInt(getTouristTaxMaxNightsSetting(3), 10) || 0);
  lines.push(`Giorni tassabili (somma, max ${maxTaxNights}/scheda): ${t.taxableDaysTot}`);
  lines.push("");
  lines.push(`Tariffa: ${formatEUR(t.rate)} / persona / notte`);
  lines.push("");
  lines.push(`TOTALE: ${formatEUR(t.totalAmt)}`);
  return lines.join("\n");

}


function openTaxReportModal(text){
  const modal = document.getElementById("taxReportModal");
  const pre = document.getElementById("taxReportText");
  const closeBtn = document.getElementById("taxReportCloseBtn");
  const copyBtn = document.getElementById("taxReportCopyBtn");

  if (!modal || !pre) return;
  pre.textContent = text || "";

  modal.hidden = false;
  modal.setAttribute("aria-hidden","false");

  const close = () => {
    modal.hidden = true;
    modal.setAttribute("aria-hidden","true");
  };

  // click su overlay chiude
  const onOverlay = (e)=>{
    if (e.target === modal) close();
  };
  modal.addEventListener("click", onOverlay, { once:true });

  if (closeBtn){
    closeBtn.onclick = close;
  }

  if (copyBtn){
    copyBtn.onclick = async () => {
      try{
        const txt = pre.textContent || "";
        if (navigator.clipboard && navigator.clipboard.writeText){
          await navigator.clipboard.writeText(txt);
        } else {
          // fallback
          const ta = document.createElement("textarea");
          ta.value = txt;
          document.body.appendChild(ta);
          ta.select();
          document.execCommand("copy");
          ta.remove();
        }
        toast("Copiato");
      }catch(_){
        toast("Impossibile copiare");
      }
    };
  }
}



function initTassaPage(){
  // idempotent: può essere richiamata più volte (es. restore iOS/refresh)
  const bindOnce = (el, fn) => {
    if (!el) return false;
    try{ if (el.dataset && el.dataset.fastTapBound === "1") return true; }catch(_){ }
    try{ bindFastTap(el, fn); }catch(_){ try{ el.addEventListener("click", fn); }catch(__){} }
    try{ if (el.dataset) el.dataset.fastTapBound = "1"; }catch(_){ }
    return true;
  };

  const setYearLabel = () => {
    // Usa sempre l'anno di esercizio scelto in Impostazioni, non l'anno solare corrente.
    const raw = (state && state.exerciseYear) ? state.exerciseYear : (typeof loadExerciseYear === "function" ? loadExerciseYear() : "");
    const y = String(raw || new Date().getFullYear()).trim();
    const yl = $("#taxYearBtnLabel");
    if (yl) yl.textContent = String(y);
    return y;
  };

  const doCalcRange = async (fromISO, toISO) => {
    try { await calcTassa(fromISO, toISO); }
    catch (err) { toast(String(err && err.message || err || "Errore")); resetTassaUI(); }
  };

  const yearBtn = $("#taxYearBtn");
  if (yearBtn){
    bindOnce(yearBtn, async () => {
      const y = setYearLabel();
      await doCalcRange(`${y}-01-01`, `${y}-12-31`);
    });
  }

  const estBtn = $("#taxEstimateBtn");
  if (estBtn){
    bindOnce(estBtn, async () => {
      const y = setYearLabel();
      try { await calcTassa(`${y}-01-01`, `${y}-12-31`, { includeUntagged: true }); }
      catch (err) { toast(String(err && err.message || err || "Errore")); resetTassaUI(); }
    });
  }
  try{ __pillApplyAll__(); }catch(_){ }
  try{ __applyTaxPageCardAll__(); }catch(_){ }

  const q1 = $("#taxQ1Btn");
  const q2 = $("#taxQ2Btn");
  const q3 = $("#taxQ3Btn");
  const q4 = $("#taxQ4Btn");
  const quarterBind = (btn, fromMMDD, toMMDD) => {
    if (!btn) return;
    bindOnce(btn, async () => {
      const y = setYearLabel();
      await doCalcRange(`${y}-${fromMMDD}`, `${y}-${toMMDD}`);
    });
  };
  quarterBind(q1, "01-01", "03-31");
  quarterBind(q2, "04-01", "06-30");
  quarterBind(q3, "07-01", "09-30");
  quarterBind(q4, "10-01", "12-31");
  [q1,q2,q3,q4].forEach((btn)=>{ try{ __applyTaxQuarterVisual__(btn); __bindTaxQuarterColorHold__(btn); }catch(_){ } });

  // Mantieni listeners (inputs nascosti) per compatibilità
  const from = $("#taxFrom");
  const to = $("#taxTo");
  if (from) from.addEventListener("change", resetTassaUI);
  if (to) to.addEventListener("change", resetTassaUI);

  // Stato iniziale: risultati nascosti finché non premi un tasto
  setYearLabel();
  resetTassaUI();
}


/* =========================
   Ore pulizia (Calendario ore operatori)
   Build: dDAE_1.020
========================= */

state.orepulizia = state.orepulizia || {
  inited: false,
  monthKey: "",          // "YYYY-MM"
  operatore: "",         // nome operatore oppure "__ALL__"
  rows: [],              // righe da foglio operatori
  months: []             // [{key,label}]
};

function __capitalizeFirst_(s){
  s = String(s||"");
  return s ? (s[0].toUpperCase() + s.slice(1)) : "";
}

function formatMonthYearIT_(monthKey){
  // monthKey = "YYYY-MM"
  if (!monthKey || !/^\d{4}-\d{2}$/.test(monthKey)) return "";
  const parts = monthKey.split("-").map(n=>parseInt(n,10));
  const y = parts[0], m = parts[1];
  const dt = new Date(y, (m-1), 1);
  const s = dt.toLocaleDateString(__getCurrentLocale__(), { month:"long", year:"numeric" });
  return __capitalizeFirst_(s);
}

function __fmtHours_(h){
  const n = Number(h||0);
  if (!isFinite(n) || n <= 0) return "";
  const rounded = Math.round(n * 100) / 100;
  try{
    return new Intl.NumberFormat(__getCurrentLocale__(), { minimumFractionDigits: 0, maximumFractionDigits: 2 }).format(rounded);
  }catch(_){
    let s = rounded.toFixed(2);
    s = s.replace(/\.00$/, "").replace(/0$/, "");
    return s.replace(".", ",");
  }
}

function __fmtMoneyNoSpace_(amount){
  const n = Number(amount || 0);
  if (!isFinite(n)) return "—";
  try{
    const parts = new Intl.NumberFormat(__getCurrentLocale__(), { style:"currency", currency:"EUR", minimumFractionDigits: 2, maximumFractionDigits: 2 }).formatToParts(n);
    return parts.map((part) => part.type === "literal" ? "" : part.value).join("");
  }catch(_){
    const s = n.toLocaleString(__getCurrentLocale__(), { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    return s + "€";
  }
}


function __getUniqueMonthsFromRows_(rows){
  const set = new Set();
  (rows||[]).forEach(r=>{
    const iso = formatISODateLocal(r.data || r.date || r.Data || "");
    if (!iso) return;
    if (/^\d{4}-\d{2}-\d{2}$/.test(iso)) set.add(iso.slice(0,7));
  });
  return Array.from(set).sort();
}

async function __loadOperatoriRows_(){
  try{
    const res = await api("operatori", { method:"GET", showLoader:true });
    const rows = res && (res.rows || res.items) ? (res.rows || res.items) : [];
    return Array.isArray(rows) ? rows : [];
  }catch(e){
    console.warn("Operatori load failed", e);
    return [];
  }
}

function __fillSelect_(sel, items, value){
  if (!sel) return;
  sel.innerHTML = "";
  (items||[]).forEach(it=>{
    const opt = document.createElement("option");
    opt.value = it.value;
    opt.textContent = it.label;
    sel.appendChild(opt);
  });
  if (value) sel.value = value;
}

function __fmtHoursOrDash_(h){
  const s = __fmtHours_(h);
  return s ? s : "—";
}

function __opLabel_(op){
  const v = String(op||"").trim();
  if (!v || v === "__ALL__") return "Tutti";
  const low = v.toLowerCase();
  // Title-case semplice (spazi, trattini, apostrofi)
  return low.replace(/(^|[\s\-'])\S/g, (m) => m.toUpperCase());
}

function __orePuliziaReportPayload__(){
  const monthKey = String(state?.orepulizia?.monthKey || '').trim();
  if (!/^\d{4}-\d{2}$/.test(monthKey)) return null;
  const monthLabel = formatMonthYearIT_(monthKey) || monthKey;
  const operatorRaw = String(state?.orepulizia?.operatore || '').trim();
  const operatorLabel = __opLabel_(operatorRaw || '__ALL__');
  const parts = monthKey.split('-').map((n)=>parseInt(n, 10));
  const Y = parts[0];
  const M = parts[1];
  const first = new Date(Y, M - 1, 1);
  const daysInMonth = new Date(Y, M, 0).getDate();
  const jsDow = first.getDay();
  const dowMon0 = (jsDow + 6) % 7;
  const rows = Array.isArray(state?.orepulizia?.rows) ? state.orepulizia.rows : [];
  const hoursByDay = new Map();
  let totalHours = 0;
  let totalImporto = 0;
  let presenze = 0;
  let presenzeImporto = 0;
  const presenceKeys = new Set();
  rows.forEach((r) => {
    const iso = formatISODateLocal(r.data || r.date || r.Data || '');
    if (!iso || !iso.startsWith(monthKey + '-')) return;
    const oper = String(r.operatore || r.nome || '').trim();
    if (operatorRaw && operatorRaw !== '__ALL__' && oper.toLowerCase() !== operatorRaw.toLowerCase()) return;
    const oreRaw = (r.ore !== undefined && r.ore !== null) ? r.ore : (r.Ore !== undefined ? r.Ore : '');
    const ore = Number(String(oreRaw).trim().replace(',', '.'));
    if (!isFinite(ore) || ore <= 0) return;
    const d = parseInt(iso.slice(8,10), 10);
    if (!d) return;
    hoursByDay.set(d, (hoursByDay.get(d) || 0) + ore);
    totalHours += ore;
    const tariffaOperatore = getOperatoreTariffaByName(oper, Number(r?.tariffa_euro ?? 0));
    totalImporto += ore * tariffaOperatore;
    const presenceKey = `${iso}|${oper.toLowerCase()}`;
    if (!presenceKeys.has(presenceKey)){
      presenceKeys.add(presenceKey);
      presenze += 1;
      const benzinaOperatore = getOperatoreBenzinaByName(oper, Number(r?.benzina_unit_euro ?? r?.benzina_euro ?? 0));
      presenzeImporto += benzinaOperatore;
    }
  });
  const cells = [];
  for (let i = 0; i < 42; i += 1){
    const dayNum = i - dowMon0 + 1;
    if (dayNum < 1 || dayNum > daysInMonth) cells.push({ empty:true, day:null, hours:0, label:'' });
    else {
      const h = Number(hoursByDay.get(dayNum) || 0);
      cells.push({ empty:false, day:dayNum, hours:h, label:h > 0 ? __fmtHours_(h) : '' });
    }
  }
  return {
    monthKey,
    monthLabel,
    operatorRaw,
    operatorLabel,
    totalHours,
    totalHoursLabel: totalHours > 0 ? `${__fmtHours_(totalHours)} ore - ${__fmtMoneyNoSpace_(totalImporto)}` : '—',
    totalTrips: presenze,
    totalTripsLabel: presenze > 0 ? `${presenze} transfert - ${__fmtMoneyNoSpace_(presenzeImporto)}` : '—',
    daysInMonth,
    cells
  };
}

function __orePuliziaPdfFileName__(payload){
  const safe = String(payload?.operatorLabel || 'tutti').trim().toLowerCase().replace(/[^a-z0-9]+/gi, '-').replace(/^-+|-+$/g, '') || 'tutti';
  return `report-ore-pulizia-${String(payload?.monthKey || 'mese')}-${safe}.pdf`;
}

async function __orePuliziaReportCanvas__(){
  const payload = __orePuliziaReportPayload__();
  if (!payload) throw new Error('Mese non valido');
  const canvas = document.createElement('canvas');
  canvas.width = 1240;
  canvas.height = 1754;
  const ctx = canvas.getContext('2d', { alpha:false });
  if (!ctx) throw new Error('Canvas non disponibile');
  const W = canvas.width;
  const H = canvas.height;
  const padX = 62;
  const topY = 64;
  const contentW = W - padX * 2;
  const weekdayLabels = ['L','M','M','G','V','S','D'];
  const gridTop = 558;
  const cellGap = 14;
  const cellW = Math.floor((contentW - cellGap * 6) / 7);
  const cellH = 124;

  const round = (x, y, w, h, r) => { roundRect(ctx, x, y, w, h, r); };
  const fit = (text, x, y, maxWidth, opts={}) => {
    const font = opts.font || '700 22px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif';
    const color = opts.color || '#0f172a';
    const align = opts.align || 'left';
    let out = String(text == null ? '' : text);
    ctx.save();
    ctx.font = font;
    ctx.fillStyle = color;
    ctx.textAlign = align;
    while (out.length > 1 && ctx.measureText(out).width > maxWidth) out = out.slice(0, -1);
    if (out !== String(text == null ? '' : text) && out.length > 1) out = out.slice(0, -1) + '…';
    ctx.fillText(out, x, y, maxWidth);
    ctx.restore();
  };

  ctx.fillStyle = '#f7f8fa';
  ctx.fillRect(0, 0, W, H);

  ctx.fillStyle = '#ffffff';
  ctx.fillRect(padX - 8, topY - 18, contentW + 16, H - topY - 54);

  const topColors = ['#2B7CB4','#6FB7D6','#BFBEA9','#C9772B'];
  const segW = W / topColors.length;
  topColors.forEach((c, idx) => { ctx.fillStyle = c; ctx.fillRect(segW * idx, 0, segW + 1, 7); });

  fit('Ore Pulizia Mensili', padX, topY + 16, contentW - 100, { font:'800 32px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'#0f172a' });
  const btnSize = 48;
  const btnX = W - padX - btnSize;
  const btnY = topY - 4;
  ctx.fillStyle = 'rgba(255,255,255,0.82)';
  ctx.strokeStyle = 'rgba(148,163,184,0.36)';
  ctx.lineWidth = 2;
  round(btnX, btnY, btnSize, btnSize, 14);
  ctx.fill();
  ctx.stroke();
  ctx.strokeStyle = '#2B7CB4';
  ctx.lineWidth = 2.2;
  ctx.beginPath();
  ctx.moveTo(btnX + 17, btnY + 14); ctx.lineTo(btnX + 28, btnY + 14); ctx.lineTo(btnX + 34, btnY + 20);
  ctx.lineTo(btnX + 34, btnY + 34); ctx.lineTo(btnX + 17, btnY + 34); ctx.closePath(); ctx.stroke();
  ctx.beginPath(); ctx.moveTo(btnX + 28, btnY + 14); ctx.lineTo(btnX + 28, btnY + 21); ctx.lineTo(btnX + 34, btnY + 21); ctx.stroke();
  ctx.beginPath(); ctx.moveTo(btnX + 20, btnY + 24); ctx.lineTo(btnX + 31, btnY + 24); ctx.moveTo(btnX + 20, btnY + 29); ctx.lineTo(btnX + 31, btnY + 29); ctx.stroke();

  ctx.strokeStyle = '#000000';
  ctx.lineWidth = 2;
  ctx.beginPath();
  ctx.moveTo(padX, topY + 64);
  ctx.lineTo(W - padX, topY + 64);
  ctx.stroke();

  const fieldY = topY + 102;
  const fieldH = 96;
  const cardGap = 18;
  const cardW = Math.floor((contentW - cardGap) / 2);
  const fieldStyle = (x, y, w, h, fill) => {
    ctx.fillStyle = fill;
    ctx.strokeStyle = 'rgba(15,23,42,0.08)';
    ctx.lineWidth = 2;
    round(x, y, w, h, 24);
    ctx.fill();
    ctx.stroke();
  };
  fieldStyle(padX, fieldY, cardW, fieldH, '#ffffff');
  fieldStyle(padX + cardW + cardGap, fieldY, cardW, fieldH, '#ffffff');
  fit(String(payload.monthLabel || '').toUpperCase(), padX + 24, fieldY + 59, cardW - 78, { font:'800 26px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'#0f172a' });
  fit(String(payload.operatorLabel || '').toUpperCase(), padX + cardW + cardGap + 24, fieldY + 59, cardW - 78, { font:'800 26px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'#0f172a' });
  const drawChev = (x, y) => {
    ctx.strokeStyle = '#111111';
    ctx.lineWidth = 3;
    ctx.lineCap = 'round';
    ctx.lineJoin = 'round';
    ctx.beginPath(); ctx.moveTo(x - 8, y - 5); ctx.lineTo(x, y + 3); ctx.lineTo(x + 8, y - 5); ctx.stroke();
    ctx.beginPath(); ctx.moveTo(x - 8, y + 5); ctx.lineTo(x, y + 13); ctx.lineTo(x + 8, y + 5); ctx.stroke();
  };
  drawChev(padX + cardW - 42, fieldY + 42);
  drawChev(W - padX - 42, fieldY + 42);

  const statY = fieldY + fieldH + 22;
  const statH = 104;
  fieldStyle(padX, statY, cardW, statH, '#f3f8fb');
  fieldStyle(padX + cardW + cardGap, statY, cardW, statH, '#faf6f1');
  fit('Ore totali', padX + 22, statY + 31, cardW - 44, { font:'800 19px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(0,0,0,0.60)' });
  fit('Spese Benzina', padX + cardW + cardGap + 22, statY + 31, cardW - 44, { font:'800 19px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(0,0,0,0.60)' });
  fit(payload.totalHoursLabel, padX + 22, statY + 70, cardW - 44, { font:'800 24px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(43,124,180,0.95)' });
  fit(payload.totalTripsLabel, padX + cardW + cardGap + 22, statY + 70, cardW - 44, { font:'800 24px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(201,119,43,0.95)' });

  weekdayLabels.forEach((label, idx) => {
    const x = padX + idx * (cellW + cellGap) + cellW / 2;
    fit(label, x, gridTop - 24, cellW, { font:'800 20px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(43,124,180,0.95)', align:'center' });
  });

  payload.cells.forEach((cell, idx) => {
    const row = Math.floor(idx / 7);
    const col = idx % 7;
    const x = padX + col * (cellW + cellGap);
    const y = gridTop + row * (cellH + cellGap);
    if (cell.empty) return;
    ctx.fillStyle = '#f2f4f7';
    ctx.strokeStyle = 'rgba(15,23,42,0.10)';
    ctx.lineWidth = 2;
    round(x, y, cellW, cellH, 18);
    ctx.fill();
    ctx.stroke();
    fit(String(cell.day), x + 16, y + 28, cellW - 32, { font:'700 15px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(43,124,180,0.95)' });
    if (cell.label){
      fit(cell.label, x + cellW / 2, y + 78, cellW - 22, { font:'800 30px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(201,119,43,1)', align:'center' });
    }
  });

  const generated = `${(typeof __designTranslate__ === 'function' ? __designTranslate__('Generato il', {en:'Generated on', fr:'Généré le', de:'Erstellt am', es:'Generado el'}) : 'Generato il')} ${new Date().toLocaleString((typeof __getCurrentLocale__ === 'function' ? __getCurrentLocale__() : 'it-IT'))}`;
  fit(generated, W - padX, H - 34, contentW, { font:'500 14px -apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Arial,sans-serif', color:'rgba(15,23,42,0.55)', align:'right' });
  return { canvas, payload };
}

async function __shareOrePuliziaPdf__(){
  const out = await __orePuliziaReportCanvas__();
  const canvas = out?.canvas;
  const payload = out?.payload;
  if (!canvas || !payload) return false;
  const filename = __orePuliziaPdfFileName__(payload);
  const jpegDataUrl = canvas.toDataURL('image/jpeg', 0.94);
  const pdfBlob = __piscinaPdfFromJpegDataUrl__(jpegDataUrl, canvas.width, canvas.height);
  const file = new File([pdfBlob], filename, { type:'application/pdf' });
  try{
    if (navigator.canShare && navigator.canShare({ files:[file] })){
      await navigator.share({ title:`Ore pulizia ${payload.monthLabel}`, files:[file] });
      return true;
    }
  }catch(err){
    if (err && err.name === 'AbortError') return false;
  }
  const url = URL.createObjectURL(pdfBlob);
  try{
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    document.body.appendChild(a);
    try{ a.click(); }catch(_){ }
    try{ document.body.removeChild(a); }catch(_){ }
    try{ toast('PDF report pronto', 'blue'); }catch(_){ }
    return true;
  } finally {
    setTimeout(() => { try{ URL.revokeObjectURL(url); }catch(_){ } }, 2000);
  }
}

function __renderOrePuliziaCalendar_(){
  const grid = document.getElementById("opcalGrid");
  if (!grid) return;

  const monthKey = state.orepulizia.monthKey;
  if (!monthKey || !/^\d{4}-\d{2}$/.test(monthKey)) {
    grid.innerHTML = "";
    return;
  }

  const titleEl = document.getElementById("opcalTitleMain");
  const totalEl = document.getElementById("opcalTotalHours");
  const daysEl = document.getElementById("opcalDaysWithHours");
  const grandTotalEl = document.getElementById("opcalGrandTotal");

  const monthLabel = formatMonthYearIT_(monthKey) || monthKey;
  const op = String(state.orepulizia.operatore || "").trim();
  const opDisp = __opLabel_(op);

  if (titleEl) titleEl.textContent = `${opDisp} - ${monthLabel}`;

  const parts = monthKey.split("-").map(n=>parseInt(n,10));
  const Y = parts[0], M = parts[1]; // 1..12
  const first = new Date(Y, M-1, 1);
  const daysInMonth = new Date(Y, M, 0).getDate();

  // Lun=0..Dom=6
  const jsDow = first.getDay(); // Dom=0..Sab=6
  const dowMon0 = (jsDow + 6) % 7; // convert to Mon=0
  const totalCells = 42; // 6 settimane

  // ore per giorno + costi per operatore
  const rows = state.orepulizia.rows || [];
  const hoursByDay = new Map();
  let totalHours = 0;
  let totalImporto = 0;
  let presenze = 0;
  let presenzeImporto = 0;
  const presenceKeys = new Set();
  rows.forEach(r=>{
    const iso = formatISODateLocal(r.data || r.date || r.Data || '');
    if (!iso) return;
    if (!iso.startsWith(monthKey + '-')) return;

    const oper = String(r.operatore || r.nome || '').trim();
    if (op && op !== '__ALL__' && String(oper||'').trim().toLowerCase() !== String(op||'').trim().toLowerCase()) return;

    const oreRaw = (r.ore !== undefined && r.ore !== null) ? r.ore : (r.Ore !== undefined ? r.Ore : '');
    const ore = Number(String(oreRaw).trim().replace(',', '.'));
    if (!isFinite(ore) || ore <= 0) return;

    const d = parseInt(iso.slice(8,10), 10);
    if (!d) return;
    hoursByDay.set(d, (hoursByDay.get(d) || 0) + ore);
    totalHours += ore;

    const tariffaOperatore = getOperatoreTariffaByName(oper, Number(r?.tariffa_euro ?? 0));
    totalImporto += ore * tariffaOperatore;

    const presenceKey = `${iso}|${String(oper || '').trim().toLowerCase()}`;
    if (!presenceKeys.has(presenceKey)){
      presenceKeys.add(presenceKey);
      presenze += 1;
      const benzinaOperatore = getOperatoreBenzinaByName(oper, Number(r?.benzina_unit_euro ?? r?.benzina_euro ?? 0));
      presenzeImporto += benzinaOperatore;
    }
  });

  const hoursStr = __fmtHours_(totalHours);
  const totalImportoStr = totalImporto > 0 ? __fmtMoneyNoSpace_(totalImporto) : '—';
  const presenzeImportoStr = presenzeImporto > 0 ? __fmtMoneyNoSpace_(presenzeImporto) : '—';
  const grandTotal = Math.round((Number(totalImporto || 0) + Number(presenzeImporto || 0)) * 100) / 100;
  const grandTotalStr = grandTotal > 0 ? __fmtMoneyNoSpace_(grandTotal) : '—';

  if (grandTotalEl) {
    grandTotalEl.textContent = grandTotalStr;
  }
  if (totalEl) {
    totalEl.textContent = hoursStr ? `${hoursStr} ore - ${totalImportoStr}` : "—";
  }
  if (daysEl) {
    daysEl.textContent = presenze > 0 ? `${presenze} transfert - ${presenzeImportoStr}` : "—";
  }

  // build cells
  grid.innerHTML = "";
  for (let i=0; i<totalCells; i++) {
    const cell = document.createElement("div");
    cell.className = "opcal-cell";

    const dayNum = i - dowMon0 + 1;
    if (dayNum < 1 || dayNum > daysInMonth) {
      cell.classList.add("is-empty");
      grid.appendChild(cell);
      continue;
    }

    const dayEl = document.createElement("div");
    dayEl.className = "opcal-day";
    dayEl.textContent = String(dayNum);
    cell.appendChild(dayEl);

    const h = hoursByDay.get(dayNum) || 0;
    if (h > 0) {
      const hEl = document.createElement("div");
      hEl.className = "opcal-hours";
      hEl.textContent = __fmtHours_(h); // no zeri
      cell.appendChild(hEl);
    }

    grid.appendChild(cell);
  }
}

async function initOrePuliziaPage(){
  const s = state.orepulizia;
  const back = document.getElementById("opcalBack");
  const selMonth = document.getElementById("opcalMonthSelect");
  const selOp = document.getElementById("opcalOperatorSelect");
  const btnPdf = document.getElementById("btnOrePuliziaPdfTop");

  // Serve per mostrare importi in "Totali ore" e "Spese Benzina"
  try{ await ensureSettingsLoaded({ force:false, showLoader:false }); }catch(_){}

  if (!s.inited){
    s.inited = true;

    if (back) back.addEventListener("click", ()=>showPage("pulizie"));

    // Topbar: tasto arancione "torna a Pulizie"
    const topBack = document.getElementById("backBtnTop");
    if (topBack && !s._topBackBound){
      s._topBackBound = true;
      bindFastTap(topBack, () => { try{ showPage("pulizie"); }catch(_){ } });
    }

    if (selMonth) selMonth.addEventListener("change", ()=>{
      s.monthKey = selMonth.value;
      __renderOrePuliziaCalendar_();
    });

    if (selOp) selOp.addEventListener("change", ()=>{
      s.operatore = selOp.value;
      __renderOrePuliziaCalendar_();
    });

    if (btnPdf && !s._pdfBound){
      s._pdfBound = true;
      bindFastTap(btnPdf, async () => {
        try{ await __shareOrePuliziaPdf__(); }catch(err){ try{ toast(String(err && err.message || 'Errore report PDF'), 'orange'); }catch(_){ } }
      });
    }
  }

  // dati
  await ensureSettingsLoaded({ force:false, showLoader:false });
  s.rows = await __loadOperatoriRows_();

  // mesi
  const months = __getUniqueMonthsFromRows_(s.rows);
  const now = new Date();
  const nowKey = String(now.getFullYear()) + "-" + String(now.getMonth()+1).padStart(2,"0");
  if (!months.includes(nowKey)) months.push(nowKey);
  months.sort();

  s.months = months.map(k=>({ key:k, label: formatMonthYearIT_(k) }));
  const monthItems = s.months.map(m=>({ value:m.key, label:m.label }));

  // default month: ultimo (più recente)
  if (!s.monthKey) s.monthKey = monthItems.length ? monthItems[monthItems.length-1].value : nowKey;

  __fillSelect_(selMonth, monthItems, s.monthKey);

  // operatori list: solo catalogo attivo dell'account
  let fromSet = [];
  try{ fromSet = getActiveOperatorNames ? getActiveOperatorNames() : []; }catch(_){ fromSet = []; }
  const ops = Array.from(new Set((fromSet||[]).map(x=>String(x||"").trim()).filter(Boolean)))
    .sort((a,b)=>a.localeCompare(b, "it"));

  // opzioni: TUTTI + operatori
  const opItems = [{ value:"__ALL__", label:"TUTTI" }, ...ops.map(x=>({ value:x, label:x }))];

  // default operatore
  // In sessione OPERATORE: pre-seleziona sempre il proprio nome (se presente), così il report è leggibile subito.
  if (state && state.session && isOperatoreSession(state.session)){
    const raw = String(
      state.session._op_local ||
      state.session.username ||
      state.session.user ||
      state.session.nome ||
      state.session.name ||
      state.session.email ||
      ""
    ).trim();
    if (raw){
      const norm = raw.toLowerCase();
      const match = (ops||[]).find(x => String(x||"").trim().toLowerCase() === norm);
      if (match) s.operatore = match;
    }
  }
  if (!s.operatore) s.operatore = ops.length ? ops[0] : "__ALL__";
  if (!opItems.some(o=>o.value === s.operatore)) s.operatore = ops.length ? ops[0] : "__ALL__";

  __fillSelect_(selOp, opItems, s.operatore);

  __renderOrePuliziaCalendar_();
}

(async ()=>{ try{ await init(); } catch(e){ console.error(e); try{ toast(e.message||"Errore"); }catch(_){ } } })();

(function __setupLauncherIconPaletteBindings__(){
  const run = ()=>{
    try{ setupLauncherIconLongPressPalette(); }catch(_){ }
    try{ __launcherIconApplyAll__(); }catch(_){ }
  };
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', run, { once:true });
  else setTimeout(run, 0);
  try{ window.addEventListener('pageshow', run, { passive:true }); }catch(_){ }
  try{
    const mo = new MutationObserver(() => { run(); });
    mo.observe(document.documentElement || document.body, { childList:true, subtree:true });
    setTimeout(() => { try{ mo.disconnect(); }catch(_){ } }, 15000);
  }catch(_){ }
})();

/* dDAE_2.280 — Reset biancheria: icona X bianca in dark mode */
function __applyLaundryResetCloseIcon__(){
  try{
    const btn = document.getElementById("cleanResetLaundry");
    if (!btn) return false;
    btn.classList.add('clean-reset-btn--laundry');
    const svg = btn.querySelector('svg');
    if (svg){
      svg.setAttribute('viewBox','0 0 24 24');
      svg.innerHTML = '<path d="M6 6l12 12"></path><path d="M18 6L6 18"></path>';
      svg.classList.add('ui-ico');
      try{ svg.style.stroke = '#ffffff'; svg.style.color = '#ffffff'; svg.style.fill = 'none'; }catch(_){ }
    }
    return true;
  }catch(_){ return false; }
}

(function __bindSingleActionButtonPaletteWatcher__(){
  let rafId = 0;
  const schedule = ()=>{
    if (rafId) return;
    const runNow = ()=>{
      rafId = 0;
      try{ __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
    };
    try{ rafId = window.requestAnimationFrame(runNow); }catch(_){ rafId = setTimeout(runNow, 16); }
  };
  if (document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', schedule, { once:true });
  } else {
    setTimeout(schedule, 0);
  }
  try{ window.addEventListener('pageshow', schedule, { passive:true }); }catch(_){ }
  try{
    const mo = new MutationObserver((mutations) => {
      try{
        const shouldRun = Array.isArray(mutations) ? mutations.some((m) => m.type === 'childList' && ((m.addedNodes && m.addedNodes.length) || (m.removedNodes && m.removedNodes.length))) : true;
        if (shouldRun) schedule();
      }catch(_){ schedule(); }
    });
    mo.observe(document.documentElement || document.body, { childList:true, subtree:true });
    setTimeout(() => { try{ mo.disconnect(); }catch(_){ } }, 15000);
  }catch(_){ }
})();

(function __bindGuestNationalityWatcher__(){
  const run = ()=>{ try{ __bindGuestNationalityUI__(); }catch(_){ } try{ updateGuestNationalityButton(); }catch(_){ } };
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', run, { once:true });
  else setTimeout(run, 0);
  try{ window.addEventListener('pageshow', run, { passive:true }); }catch(_){ }
})();

(function __bindLaundryResetCloseIconWatcher__(){
  const run = ()=>{ try{ __applyLaundryResetCloseIcon__(); }catch(_){ } };
  if (document.readyState === 'loading'){
    document.addEventListener('DOMContentLoaded', run, { once:true });
  } else {
    setTimeout(run, 0);
  }
  try{
    const mo = new MutationObserver(()=>{ if (__applyLaundryResetCloseIcon__()) { try{ mo.disconnect(); }catch(_){ } } });
    mo.observe(document.documentElement || document.body, { childList:true, subtree:true });
    setTimeout(()=>{ try{ mo.disconnect(); }catch(_){ } }, 15000);
  }catch(_){ }
  window.addEventListener('pageshow', run);
  document.addEventListener('click', (e)=>{
    const t = e && e.target && e.target.closest ? e.target.closest('#goPulizie, #goLavanderia, #topLaundryBtn, #cleanPrev, #cleanNext, #cleanToday') : null;
    if (t) setTimeout(run, 0);
  }, true);
})();


/* dDAE_2.575 — Fix selezione card grafico statistiche spese */
function normalizeGuestDialPhone(raw){
  let s = String(raw || '').trim();
  if (!s) return '';
  s = s.replace(/[^\d+]/g, '');
  if (!s) return '';
  if (s.startsWith('00')) s = '+' + s.slice(2);
  if (s.startsWith('+')) s = '+' + s.slice(1).replace(/\D/g, '');
  else s = s.replace(/\D/g, '');
  return s;
}

function closeGuestPhoneActionsModal(){
  try{
    const modal = document.getElementById('guestPhoneActionsModal');
    if (!modal) return;
    modal.hidden = true;
    modal.setAttribute('aria-hidden', 'true');
  }catch(_){ }
}

function openGuestPhoneWhatsApp(rawPhone){
  try{
    const raw = String(rawPhone || document.getElementById('guestPhone')?.value || '').trim();
    const normalized = normalizeWhatsAppPhone(raw);
    if (!normalized) return;
    const url = 'https://wa.me/' + encodeURIComponent(normalized);
    try{ window.location.href = url; }catch(_){ window.open(url, '_blank', 'noopener'); }
  }catch(_){ }
}

function openGuestPhoneActionsModal(){
  try{
    const phoneEl = document.getElementById('guestPhone');
    const isView = !!(state && state.page === 'ospite' && state.guestMode === 'view');
    if (!phoneEl || !isView) return;
    const raw = String(phoneEl.value || '').trim();
    if (!raw) return;
    const modal = document.getElementById('guestPhoneActionsModal');
    const titleEl = document.getElementById('guestPhoneActionsTitle');
    if (!modal || !titleEl) return;
    modal.dataset.phoneRaw = raw;
    modal.dataset.phoneDial = normalizeGuestDialPhone(raw);
    modal.dataset.phoneWhatsapp = normalizeWhatsAppPhone(raw);
    titleEl.textContent = raw;
    try{ __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
    modal.hidden = false;
    modal.setAttribute('aria-hidden', 'false');
  }catch(_){ }
}

function triggerGuestPhoneAction(action){
  try{
    const modal = document.getElementById('guestPhoneActionsModal');
    if (!modal) return;
    const raw = String(modal.dataset.phoneRaw || '').trim();
    const dial = String(modal.dataset.phoneDial || '').trim();
    const wa = String(modal.dataset.phoneWhatsapp || '').trim();
    if (action === 'call'){
      const target = dial || raw;
      if (!target) return;
      window.location.href = 'tel:' + target;
      return;
    }
    if (action === 'sms'){
      const target = dial || raw;
      if (!target) return;
      window.location.href = 'sms:' + target;
      return;
    }
    if (action === 'whatsapp'){
      openGuestPhoneWhatsApp(wa || raw);
      return;
    }
  }catch(_){ }
}

function setupGuestPhoneActionsModal(){
  try{
    const modal = document.getElementById('guestPhoneActionsModal');
    if (!modal || modal.dataset.bound === '1') return;
    modal.dataset.bound = '1';
    const card = modal.querySelector('.guest-phone-actions-card');
    const closeBtn = document.getElementById('guestPhoneActionsClose');
    const bindTap = (el, fn) => {
      if (!el || !fn) return;
      if (typeof bindFastTap === 'function') bindFastTap(el, fn);
      else el.addEventListener('click', fn);
    };
    if (card){
      ['click','pointerdown','pointerup','touchstart','touchend'].forEach((evt) => {
        try{ card.addEventListener(evt, (ev) => { try{ ev.stopPropagation(); }catch(_){ } }, { passive:false }); }catch(_){ }
      });
    }
    bindTap(closeBtn, () => { closeGuestPhoneActionsModal(); });
    modal.querySelectorAll('[data-phone-action]').forEach((btn) => {
      bindTap(btn, (ev) => {
        try{ ev.preventDefault(); }catch(_){ }
        try{ ev.stopPropagation(); }catch(_){ }
        triggerGuestPhoneAction(btn.getAttribute('data-phone-action') || '');
      });
    });
    modal.addEventListener('click', (ev) => {
      try{ if (ev.target === modal) closeGuestPhoneActionsModal(); }catch(_){ }
    });
  }catch(_){ }
}

function syncGuestPhoneWhatsAppLink(isView){
  try{
    setupGuestPhoneActionsModal();
    const phoneEl = document.getElementById('guestPhone');
    if (!phoneEl) return;
    if (!phoneEl.dataset.phoneActionsBound){
      phoneEl.addEventListener('click', function(){
        try{ openGuestPhoneActionsModal(); }catch(_){ }
      });
      phoneEl.addEventListener('keydown', function(ev){
        const key = ev && (ev.key || ev.code || '');
        if (key === 'Enter' || key === ' ' || key === 'Spacebar'){
          try{ ev.preventDefault(); }catch(_){ }
          try{ openGuestPhoneActionsModal(); }catch(_){ }
        }
      });
      phoneEl.dataset.phoneActionsBound = '1';
    }
    const enabled = !!isView && !!String(phoneEl.value || '').trim();
    phoneEl.classList.toggle('guest-phone-action-link', enabled);
    phoneEl.classList.remove('whatsapp-link');
    try{ phoneEl.setAttribute('aria-label', enabled ? 'Apri azioni telefono' : 'Telefono'); }catch(_){ }
    try{ phoneEl.setAttribute('title', enabled ? 'Apri azioni telefono' : ''); }catch(_){ }
    try{ phoneEl.readOnly = !!isView; }catch(_){ }
    try{ phoneEl.tabIndex = enabled ? 0 : -1; }catch(_){ }
  }catch(_){ }
}

(function __bindGuestPhoneActionsModalWatcher__(){
  const run = ()=>{ try{ setupGuestPhoneActionsModal(); }catch(_){ } };
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', run, { once:true });
  else setTimeout(run, 0);
  try{ window.addEventListener('pageshow', run, { passive:true }); }catch(_){ }
})();

/* dDAE_2.579 — Statistiche generali: serie con/senza ricevuta allineate alla data prenotazione */

/* dDAE_2.776 — Impostazioni: scudo anti click-through per popup e popup colore */
(function __settingsModalInteractionShield__(){
  const MODAL_IDS = [
    'settingsConfigModal',
    'settingsBackupModal',
    'settingsYearModal',
    'tagColorModal',
    'operatoriEditorModal',
    'channelEditorModal',
    'laundryCatalogEditorModal',
    'languageModal',
    'settingsLicenseModal',
    'licenseRequestModal',
    'licenseUnlockModal',
    'licenseGeneratorModal',
    'licenseDateRangeModal',
    'themeTransferModal'
  ];
  const EVENTS = ['pointerdown','pointerup','touchstart','touchend','mousedown','mouseup','click'];
  let suppressUntil = 0;

  function openModals(){
    try{
      return MODAL_IDS.map((id)=>document.getElementById(id)).filter((modal)=>!!(modal && !modal.hidden && modal.getAttribute('aria-hidden') !== 'true'));
    }catch(_){ return []; }
  }
  function topOpenModal(){
    try{
      const colorModal = document.getElementById('tagColorModal');
      if (colorModal && !colorModal.hidden && colorModal.getAttribute('aria-hidden') !== 'true') return colorModal;
      const list = openModals();
      return list.length ? list[list.length - 1] : null;
    }catch(_){ return null; }
  }
  function block(ev){
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    try{ ev.stopImmediatePropagation(); }catch(_){ }
  }
  function isInsideAnyOpenModal(target){
    try{ return openModals().some((modal)=>modal.contains(target)); }catch(_){ return false; }
  }
  function updateBodyState(){
    try{ document.body.classList.toggle('ddae-settings-modal-open', openModals().length > 0); }catch(_){ }
  }
  function globalCapture(ev){
    try{
      const target = ev && ev.target;
      const modal = topOpenModal();
      if (modal){
        updateBodyState();
        if (!modal.contains(target)){
          block(ev);
          return;
        }
        return;
      }
      updateBodyState();
      if (Date.now() < suppressUntil && !isInsideAnyOpenModal(target)) block(ev);
    }catch(_){ }
  }
  function markSuppress(){
    try{ suppressUntil = Date.now() + 850; }catch(_){ }
  }
  function bindModal(modal){
    try{
      if (!modal || modal.dataset.ddaeSettingsShield === '1') return;
      modal.dataset.ddaeSettingsShield = '1';
      EVENTS.forEach((evt)=>{
        try{
          modal.addEventListener(evt, (ev)=>{
            try{ updateBodyState(); }catch(_){ }
            try{ if (evt === 'pointerup' || evt === 'touchend' || evt === 'mouseup') markSuppress(); }catch(_){ }
            try{ ev.stopPropagation(); }catch(_){ }
          }, { passive:false });
        }catch(_){ }
      });
    }catch(_){ }
  }
  function setup(){
    try{ MODAL_IDS.forEach((id)=>bindModal(document.getElementById(id))); }catch(_){ }
    try{ updateBodyState(); }catch(_){ }
  }
  try{
    EVENTS.forEach((evt)=>document.addEventListener(evt, globalCapture, true));
  }catch(_){ }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', setup, { once:true });
  else setTimeout(setup, 0);
  try{ window.addEventListener('pageshow', setup, { passive:true }); }catch(_){ }
  try{ setInterval(updateBodyState, 500); }catch(_){ }
})();

/* dDAE_2.823 — iOS: popup colore realmente interattivo */
(function(){
  function __ddae2822ColorModalOpen__(){
    try{
      const modal = document.getElementById('tagColorModal');
      return !!(modal && !modal.hidden && modal.getAttribute('aria-hidden') !== 'true');
    }catch(_){ return false; }
  }
  function __ddae2822Stop__(ev){
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    try{ ev.stopImmediatePropagation(); }catch(_){ }
  }
  let __ddae2822LastColorTap__ = { t:0, key:'' };
  function __ddae2822Debounce__(key){
    try{
      const now = Date.now();
      if (__ddae2822LastColorTap__.key === key && (now - __ddae2822LastColorTap__.t) < 280) return true;
      __ddae2822LastColorTap__ = { t: now, key: key };
    }catch(_){ }
    return false;
  }
  function __ddae2822ApplyColorSpec__(btn){
    try{
      if (!btn) return;
      const spec = __parseOperatoreColorSpec__(btn.dataset.spec || 'blue-3').spec;
      const mode = String(__tagColorPopupState__.mode || 'bg').trim().toLowerCase();
      if (mode === 'bg') __tagColorPopupState__.colors.bg = spec;
      else if (mode === 'border') __tagColorPopupState__.colors.border = spec;
      else __tagColorPopupState__.colors.fg = spec;
      __tagColorPopupReadyAt__ = 0;
      __tagColorPopupSuppressUntil__ = 0;
      try{ if (document.activeElement && document.activeElement.blur) document.activeElement.blur(); }catch(_){ }
      __tagColorPopupRefreshSelection__();
      __tagColorPopupEmitPreview__();
    }catch(_){ }
  }
  function __ddae2822ApplyOpacity__(btn){
    try{
      if (!btn) return;
      __tagColorPopupState__.opacity = __designBgOpacityNormalize__(parseFloat(btn.dataset.opacity || '0.80'));
      __tagColorPopupReadyAt__ = 0;
      __tagColorPopupSuppressUntil__ = 0;
      try{ if (document.activeElement && document.activeElement.blur) document.activeElement.blur(); }catch(_){ }
      __tagColorPopupRefreshSelection__();
      __tagColorPopupEmitPreview__();
    }catch(_){ }
  }
  function __ddae2822ApplyMode__(btn){
    try{
      if (!btn) return;
      const raw = String(btn.dataset.mode || '').trim().toLowerCase();
      const mode = raw === 'bg' ? 'bg' : (raw === 'border' ? 'border' : (raw === 'opacity' ? 'opacity' : (raw === 'bold' ? 'bold' : 'fg')));
      if (mode === 'bg' && !__tagColorPopupState__.supportsBg) return;
      if (mode === 'border' && !__tagColorPopupState__.supportsBorder) return;
      if (mode === 'fg' && !__tagColorPopupState__.supportsFg) return;
      if (mode === 'opacity' && !__tagColorPopupState__.supportsOpacity) return;
      if (mode === 'bold' && !__tagColorPopupState__.supportsBold) return;
      __tagColorPopupReadyAt__ = 0;
      __tagColorPopupSuppressUntil__ = 0;
      if (mode === 'bold'){
        __tagColorPopupState__.mode = 'bold';
        __tagColorPopupState__.bold = !__tagColorPopupState__.bold;
        __tagColorPopupRefreshSelection__();
        __tagColorPopupEmitPreview__();
        try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ }
        return;
      }
      __tagColorPopupState__.mode = mode;
      if (__tagColorPopupState__.supportsBg && __tagColorPopupState__.supportsFg) __tagColorPopupLastDualMode__ = mode;
      __tagColorPopupRefreshSelection__();
      try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ }
    }catch(_){ }
  }
  function __ddae2822HandleColorModalInput__(ev){
    try{
      if (!__ddae2822ColorModalOpen__()) return;
      const modal = document.getElementById('tagColorModal');
      const target = ev && ev.target;
      if (!modal || !target || !modal.contains(target)) return;
      const close = target.closest && target.closest('#tagColorModalClose');
      const ok = target.closest && target.closest('#tagColorModalConfirm');
      const opt = target.closest && target.closest('#tagColorGrid .tag-color-option');
      const mode = target.closest && target.closest('#tagColorModeBar .tag-color-mode-btn');
      const op = target.closest && target.closest('#tagOpacityGrid .tag-opacity-option');
      const actionable = close || ok || opt || mode || op;
      if (!actionable) return;
      __ddae2822Stop__(ev);
      const key = (close ? 'close' : ok ? 'ok' : opt ? ('c:' + (opt.dataset.spec || '')) : mode ? ('m:' + (mode.dataset.mode || '')) : ('o:' + (op.dataset.opacity || '')));
      if (__ddae2822Debounce__(key)) return;
      if (close) return __tagColorPopupClose__();
      if (ok) return void __tagColorPopupConfirm__();
      if (mode) return __ddae2822ApplyMode__(mode);
      if (op) return __ddae2822ApplyOpacity__(op);
      if (opt) return __ddae2822ApplyColorSpec__(opt);
    }catch(_){ }
  }
  try{
    if (!window.__ddae2822ColorModalInputBound__){
      window.__ddae2822ColorModalInputBound__ = true;
      ['pointerup','touchend','click'].forEach(function(evt){
        try{ document.addEventListener(evt, __ddae2822HandleColorModalInput__, true); }catch(_){ }
      });
    }
  }catch(_){ }
  try{
    const oldOpen = (typeof __tagColorPopupOpen__ === 'function') ? __tagColorPopupOpen__ : null;
    if (oldOpen && !oldOpen.__ddae2822InstantReady){
      const wrappedOpen = function(){
        const out = oldOpen.apply(this, arguments);
        try{
          __tagColorPopupReadyAt__ = 0;
          __tagColorPopupSuppressUntil__ = 0;
          const modal = document.getElementById('tagColorModal');
          if (modal){
            modal.style.setProperty('z-index','99999','important');
            modal.style.setProperty('pointer-events','auto','important');
            const card = modal.querySelector('.tag-color-modal-card');
            if (card) card.style.setProperty('pointer-events','auto','important');
            modal.querySelectorAll('button').forEach(function(btn){
              try{ btn.style.setProperty('pointer-events','auto','important'); btn.style.setProperty('touch-action','manipulation','important'); }catch(_){ }
            });
          }
        }catch(_){ }
        return out;
      };
      wrappedOpen.__ddae2822InstantReady = true;
      __tagColorPopupOpen__ = wrappedOpen;
    }
  }catch(_){ }
})();

/* dDAE_2.823 — iOS: input reale sul popup colore sopra modali Impostazioni */
(function(){
  function modalOpen(){
    try{
      var m=document.getElementById('tagColorModal');
      return !!(m && !m.hidden && m.getAttribute('aria-hidden') !== 'true');
    }catch(_){ return false; }
  }
  function stop(ev){
    try{ ev.preventDefault(); }catch(_){ }
    try{ ev.stopPropagation(); }catch(_){ }
    try{ ev.stopImmediatePropagation(); }catch(_){ }
  }
  function xy(ev){
    try{
      var t=(ev.changedTouches && ev.changedTouches[0]) || (ev.touches && ev.touches[0]);
      if (t) return {x:t.clientX, y:t.clientY};
      return {x:ev.clientX, y:ev.clientY};
    }catch(_){ return {x:null,y:null}; }
  }
  function byHit(selector, ev){
    try{
      var p=xy(ev); if (p.x == null || p.y == null) return null;
      var nodes=[].slice.call(document.querySelectorAll(selector));
      for (var i=nodes.length-1;i>=0;i--){
        var el=nodes[i];
        if (!el || el.disabled || el.hidden) continue;
        var cs=getComputedStyle(el);
        if (cs.display === 'none' || cs.visibility === 'hidden' || cs.pointerEvents === 'none') continue;
        var r=el.getBoundingClientRect();
        if (p.x >= r.left && p.x <= r.right && p.y >= r.top && p.y <= r.bottom) return el;
      }
    }catch(_){ }
    return null;
  }
  function closestOrHit(ev, selector){
    try{
      var t=ev && ev.target;
      var el=t && t.closest ? t.closest(selector) : null;
      if (el) return el;
    }catch(_){ }
    return byHit(selector, ev);
  }
  function setMode(btn){
    try{
      var raw=String(btn && btn.dataset && btn.dataset.mode || '').trim().toLowerCase();
      var mode = raw === 'bg' ? 'bg' : (raw === 'border' ? 'border' : (raw === 'opacity' ? 'opacity' : (raw === 'bold' ? 'bold' : 'fg')));
      if (mode === 'bg' && !__tagColorPopupState__.supportsBg) return;
      if (mode === 'border' && !__tagColorPopupState__.supportsBorder) return;
      if (mode === 'fg' && !__tagColorPopupState__.supportsFg) return;
      if (mode === 'opacity' && !__tagColorPopupState__.supportsOpacity) return;
      if (mode === 'bold' && !__tagColorPopupState__.supportsBold) return;
      __tagColorPopupReadyAt__=0; __tagColorPopupSuppressUntil__=0;
      if (mode === 'bold'){
        __tagColorPopupState__.mode='bold';
        __tagColorPopupState__.bold=!__tagColorPopupState__.bold;
        __tagColorPopupRefreshSelection__();
        __tagColorPopupEmitPreview__();
        try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ }
        return;
      }
      __tagColorPopupState__.mode=mode;
      if (__tagColorPopupState__.supportsBg && __tagColorPopupState__.supportsFg) __tagColorPopupLastDualMode__=mode;
      __tagColorPopupRefreshSelection__();
      try{ __tagColorPopupApplyViewportLayout__(); }catch(_){ }
    }catch(_){ }
  }
  function setColor(btn){
    try{
      var spec=__parseOperatoreColorSpec__((btn && btn.dataset && btn.dataset.spec) || 'blue-3').spec;
      var mode=String(__tagColorPopupState__.mode || 'bg').trim().toLowerCase();
      __tagColorPopupReadyAt__=0; __tagColorPopupSuppressUntil__=0;
      if (mode === 'bg') __tagColorPopupState__.colors.bg=spec;
      else if (mode === 'border') __tagColorPopupState__.colors.border=spec;
      else __tagColorPopupState__.colors.fg=spec;
      __tagColorPopupRefreshSelection__();
      __tagColorPopupEmitPreview__();
    }catch(_){ }
  }
  function setOpacity(btn){
    try{
      __tagColorPopupReadyAt__=0; __tagColorPopupSuppressUntil__=0;
      __tagColorPopupState__.opacity=__designBgOpacityNormalize__(parseFloat((btn && btn.dataset && btn.dataset.opacity) || '0.80'));
      __tagColorPopupRefreshSelection__();
      __tagColorPopupEmitPreview__();
    }catch(_){ }
  }
  var last={t:0,k:''};
  function debounce(k){
    try{ var n=Date.now(); if (last.k===k && n-last.t<180) return true; last={t:n,k:k}; }catch(_){ }
    return false;
  }
  function normalizeLayer(){
    try{
      var modal=document.getElementById('tagColorModal'); if (!modal) return;
      if (modal.parentNode !== document.body) document.body.appendChild(modal);
      modal.style.setProperty('z-index','2147483647','important');
      modal.style.setProperty('pointer-events','auto','important');
      modal.style.setProperty('touch-action','auto','important');
      var card=modal.querySelector('.tag-color-modal-card');
      if (card){
        card.style.setProperty('pointer-events','auto','important');
        card.style.setProperty('touch-action','auto','important');
        card.style.setProperty('-webkit-transform','translateZ(0)','important');
        card.style.setProperty('transform','translateZ(0)','important');
      }
      modal.querySelectorAll('button,.tag-color-option,.tag-opacity-option,.tag-color-mode-btn').forEach(function(el){
        try{
          el.style.setProperty('pointer-events','auto','important');
          el.style.setProperty('touch-action','auto','important');
          el.style.setProperty('-webkit-user-select','none','important');
          el.style.setProperty('user-select','none','important');
        }catch(_){ }
      });
      ['operatoriEditorModal','channelEditorModal','laundryCatalogEditorModal'].forEach(function(id){
        try{ var e=document.getElementById(id); if (e && !e.hidden) e.style.setProperty('pointer-events','none','important'); }catch(_){ }
      });
    }catch(_){ }
  }
  function clearLayer(){
    try{
      ['operatoriEditorModal','channelEditorModal','laundryCatalogEditorModal'].forEach(function(id){
        try{ var e=document.getElementById(id); if (e) e.style.removeProperty('pointer-events'); }catch(_){ }
      });
    }catch(_){ }
  }
  function handle(ev){
    try{
      if (!modalOpen()) return;
      normalizeLayer();
      var close=closestOrHit(ev, '#tagColorModalClose');
      var ok=closestOrHit(ev, '#tagColorModalConfirm');
      var mode=closestOrHit(ev, '#tagColorModeBar .tag-color-mode-btn');
      var op=closestOrHit(ev, '#tagOpacityGrid .tag-opacity-option');
      var opt=closestOrHit(ev, '#tagColorGrid .tag-color-option');
      var action=close||ok||mode||op||opt;
      if (!action) return;
      stop(ev);
      var key=close?'close':ok?'ok':mode?('m:'+mode.dataset.mode):op?('o:'+op.dataset.opacity):('c:'+opt.dataset.spec);
      if (debounce(key)) return;
      if (close){ clearLayer(); return __tagColorPopupClose__(); }
      if (ok){ clearLayer(); return void __tagColorPopupConfirm__(); }
      if (mode) return setMode(mode);
      if (op) return setOpacity(op);
      if (opt) return setColor(opt);
    }catch(_){ }
  }
  function bind(){
    try{
      if (window.__ddae2823ColorFallbackBound__) return;
      window.__ddae2823ColorFallbackBound__=true;
      ['touchstart','touchend','pointerdown','pointerup','mousedown','mouseup','click'].forEach(function(evt){
        try{ document.addEventListener(evt, handle, true); }catch(_){ }
      });
    }catch(_){ }
  }
  try{
    var oldOpen=(typeof __tagColorPopupOpen__ === 'function') ? __tagColorPopupOpen__ : null;
    if (oldOpen && !oldOpen.__ddae2823LayerFix){
      var wrapped=function(){
        var out=oldOpen.apply(this, arguments);
        try{ __tagColorPopupReadyAt__=0; __tagColorPopupSuppressUntil__=0; normalizeLayer(); setTimeout(normalizeLayer,0); setTimeout(normalizeLayer,160); }catch(_){ }
        return out;
      };
      wrapped.__ddae2823LayerFix=true;
      __tagColorPopupOpen__=wrapped;
    }
  }catch(_){ }
  try{
    var oldClose=(typeof __tagColorPopupClose__ === 'function') ? __tagColorPopupClose__ : null;
    if (oldClose && !oldClose.__ddae2823LayerFix){
      var c=function(){ var out=oldClose.apply(this, arguments); try{ clearLayer(); }catch(_){ } return out; };
      c.__ddae2823LayerFix=true;
      __tagColorPopupClose__=c;
    }
  }catch(_){ }
  bind();
})();

/* dDAE_2.839 — Ospiti: ripristino tab Channel nei dettagli gruppo stanze */
(function(){
  function pageIsOspite(){
    try{ return !!document.getElementById('page-ospite') && (!state || !state.page || String(state.page).toLowerCase() === 'ospite' || !document.getElementById('page-ospite').hidden); }catch(_){ return false; }
  }
  function rowOf(el){ try{ return el && (el.closest('.subfield') || el.closest('.field') || el); }catch(_){ return el || null; } }
  function setShown(el, show){
    try{
      if (!el) return;
      el.hidden = !show;
      if (show){
        el.removeAttribute('hidden');
        el.style.removeProperty('display');
        el.style.removeProperty('visibility');
        el.style.removeProperty('pointer-events');
      } else {
        el.setAttribute('hidden','');
        el.style.setProperty('display','none','important');
      }
    }catch(_){ }
  }
  function restoreGuestChannel(){
    try{
      if (!pageIsOspite()) return;
      const mode = String(state && state.guestMode || '').toLowerCase();
      const card = document.querySelector('#page-ospite .guest-form-card');
      const active = !!String(state && state.guestGroupActiveId || '').trim();
      const open = !!(card && card.classList && card.classList.contains('guest-group-info-open'));
      const shouldShow = (mode === 'edit' || mode === 'create') || (mode === 'view' && active && open);
      const page = document.getElementById('page-ospite');
      const row = document.getElementById('guestChannelRow');
      setShown(row, shouldShow);
      const booking = document.getElementById('guestBooking');
      const bookingSub = rowOf(booking);
      if (bookingSub && bookingSub !== row) setShown(bookingSub, shouldShow);
      const channel = document.getElementById('guestChannel');
      const channelSub = rowOf(channel);
      if (channelSub && channelSub !== row) setShown(channelSub, shouldShow);
      const commWrap = document.getElementById('guestChannelCommissionWrap');
      if (commWrap) setShown(commWrap, false);
      if (shouldShow){
        try{ populateGuestChannelOptions(channel && channel.value ? channel.value : ''); }catch(_){ }
        try{ if (typeof refreshFloatingLabels === 'function') refreshFloatingLabels(); }catch(_){ }
      }
    }catch(_){ }
  }
  function after(){ try{ restoreGuestChannel(); setTimeout(restoreGuestChannel,0); setTimeout(restoreGuestChannel,120); setTimeout(restoreGuestChannel,360); }catch(_){ } }
  ['__setGuestGroupInfoVisible__','__populateGuestGroupInfoFromBooking__','renderGuestMulti','enterGuestViewMode','enterGuestEditMode','enterGuestCreateMode','updateOspiteHdActions','setGuestFormViewOnly'].forEach(function(name){
    try{
      const fn = window[name] || (typeof globalThis !== 'undefined' ? globalThis[name] : null);
      if (typeof fn === 'function' && !fn.__ddae2839ChannelRestore){
        const wrapped = function(){ const out = fn.apply(this, arguments); after(); return out; };
        wrapped.__ddae2839ChannelRestore = true;
        window[name] = wrapped;
        try{ globalThis[name] = wrapped; }catch(_){ }
      }
    }catch(_){ }
  });
  try{ document.addEventListener('click', function(){ setTimeout(restoreGuestChannel, 0); setTimeout(restoreGuestChannel, 160); }, true); }catch(_){ }
  try{ window.__ddae2839RestoreGuestChannel = restoreGuestChannel; }catch(_){ }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', after, {once:true}); else after();
  try{ window.addEventListener('pageshow', after, {passive:true}); }catch(_){ }
})();


/* dDAE_2.855 — Long press tasti popup impostazioni con popup colore standard */
(function(){
  const IDS = [
    'operatoriEditorDelete','operatoriEditorSaldoBtn','operatoriEditorTagColor','operatoriEditorDotColor','operatoriEditorCancel','operatoriEditorSave',
    'channelEditorDelete','channelEditorGraphColor','channelEditorDotColor','channelEditorCancel','channelEditorSave',
    'laundryCatalogEditorDelete','laundryCatalogEditorTagColor','laundryCatalogEditorDotColor','laundryCatalogEditorCancel','laundryCatalogEditorSave'
  ];
  function bindAll(){
    try{
      IDS.forEach(function(id){
        const btn = document.getElementById(id);
        if (!btn) return;
        try{ __applySingleActionButtonVisual__(btn); }catch(_){ }
        try{ __bindSingleActionButtonColorHold__(btn); }catch(_){ }
      });
      try{ __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
    }catch(_){ }
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', bindAll, { once:true });
  else bindAll();
  try{ window.addEventListener('pageshow', bindAll, { passive:true }); }catch(_){ }
  try{
    const mo = new MutationObserver(function(){ bindAll(); });
    mo.observe(document.documentElement || document.body, { childList:true, subtree:true });
  }catch(_){ }
})();



/* dDAE_2.855 — Popup colore sopra editor Impostazioni: priorità layer e touch iOS */
(function(){
  function isOpen(el){ try{ return !!(el && !el.hidden && el.getAttribute('aria-hidden') !== 'true'); }catch(_){ return false; } }
  function normalize(){
    try{
      var modal=document.getElementById('tagColorModal');
      if (!isOpen(modal)) return;
      if (modal.parentNode !== document.body) document.body.appendChild(modal);
      document.body.classList.add('tag-color-modal-open');
      document.body.classList.add('modal-open');
      modal.classList.add('is-open');
      modal.style.setProperty('z-index','2147483647','important');
      modal.style.setProperty('pointer-events','auto','important');
      modal.style.setProperty('touch-action','auto','important');
      ['operatoriEditorModal','channelEditorModal','laundryCatalogEditorModal'].forEach(function(id){
        var under=document.getElementById(id);
        if (isOpen(under)) under.style.setProperty('pointer-events','none','important');
      });
    }catch(_){ }
  }
  function clear(){
    try{
      var modal=document.getElementById('tagColorModal');
      if (isOpen(modal)) return;
      document.body.classList.remove('tag-color-modal-open');
      ['operatoriEditorModal','channelEditorModal','laundryCatalogEditorModal'].forEach(function(id){
        var under=document.getElementById(id);
        if (under) under.style.removeProperty('pointer-events');
      });
    }catch(_){ }
  }
  try{
    var oldOpen=(typeof __tagColorPopupOpen__ === 'function') ? __tagColorPopupOpen__ : null;
    if (oldOpen && !oldOpen.__ddae2849LayerFix){
      var wrapped=function(){ var out=oldOpen.apply(this, arguments); normalize(); setTimeout(normalize,0); setTimeout(normalize,120); return out; };
      wrapped.__ddae2849LayerFix=true;
      __tagColorPopupOpen__=wrapped;
      try{ globalThis.__tagColorPopupOpen__=wrapped; }catch(_){ }
    }
    var oldClose=(typeof __tagColorPopupClose__ === 'function') ? __tagColorPopupClose__ : null;
    if (oldClose && !oldClose.__ddae2849LayerFix){
      var closed=function(){ var out=oldClose.apply(this, arguments); clear(); return out; };
      closed.__ddae2849LayerFix=true;
      __tagColorPopupClose__=closed;
      try{ globalThis.__tagColorPopupClose__=closed; }catch(_){ }
    }
  }catch(_){ }
  try{ document.addEventListener('visibilitychange', function(){ normalize(); clear(); }, true); }catch(_){ }
})();



/* dDAE_2.863 — Cleanup filtri periodo legacy non più disponibili */
(function(){
  function run(){
    try{ __ddae2862NormalizeFilterMode__(localStorage.getItem('dDAE_guestArrivalFilter') || state.guestArrivalFilter || 'today', 'guest'); }catch(_){}
    try{
      const g=document.getElementById('guestToday');
      if (g){ const m=__ddae2862NormalizeFilterMode__(state.guestArrivalFilter || 'today','guest'); g.dataset.filterState=m; g.textContent=m==='all'?'Tutti':'Oggi'; g.dataset.i18n=g.textContent; __translateTree__(g); }
    }catch(_){}
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', run, {once:true}); else run();
  try{ window.addEventListener('pageshow', run, {passive:true}); }catch(_){}
})();

/* dDAE_2.868 — Fix popup colore pill: conferma categoria stabile su iOS e applicazione globale */
(function(){
  var colorConfirmBusy = false;

  function stopEvent(ev){
    try{ if (ev && ev.cancelable !== false) ev.preventDefault(); }catch(_){ }
    try{ if (ev) ev.stopPropagation(); }catch(_){ }
    try{ if (ev && ev.stopImmediatePropagation) ev.stopImmediatePropagation(); }catch(_){ }
  }

  function pointFromEvent(ev){
    try{
      var t = (ev && ev.changedTouches && ev.changedTouches[0]) || (ev && ev.touches && ev.touches[0]);
      if (t) return { x:t.clientX, y:t.clientY };
      return { x:ev.clientX, y:ev.clientY };
    }catch(_){ return { x:null, y:null }; }
  }

  function byHit(selector, ev){
    try{
      var p = pointFromEvent(ev);
      if (p.x == null || p.y == null) return null;
      var nodes = Array.prototype.slice.call(document.querySelectorAll(selector));
      for (var i = nodes.length - 1; i >= 0; i--){
        var el = nodes[i];
        if (!el || el.disabled || el.hidden) continue;
        var cs = getComputedStyle(el);
        if (cs.display === 'none' || cs.visibility === 'hidden' || cs.pointerEvents === 'none') continue;
        var r = el.getBoundingClientRect();
        if (p.x >= r.left && p.x <= r.right && p.y >= r.top && p.y <= r.bottom) return el;
      }
    }catch(_){ }
    return null;
  }

  function closestOrHit(ev, selector){
    try{
      var t = ev && ev.target;
      var el = t && t.closest ? t.closest(selector) : null;
      if (el) return el;
    }catch(_){ }
    return byHit(selector, ev);
  }

  function forceCloseConfirm(){
    try{ if (typeof __confirmYesNoForceClose__ === 'function') __confirmYesNoForceClose__(); }catch(_){ }
    try{
      var modal = document.getElementById('confirmYesNoModal');
      if (modal){
        modal.hidden = true;
        modal.setAttribute('hidden','');
        modal.setAttribute('aria-hidden','true');
        modal.classList.remove('is-open','show','open','active');
        modal.style.setProperty('display','none','important');
        modal.style.setProperty('pointer-events','none','important');
      }
      document.body.classList.remove('confirm-yesno-open');
      document.documentElement.classList.remove('confirm-yesno-open');
    }catch(_){ }
  }

  try{
    __confirmYesNoForceClose__ = forceCloseConfirm;
  }catch(_){ }

  try{
    confirmYesNo = function(message){
      return new Promise(function(resolve){
        var modal = document.getElementById('confirmYesNoModal');
        var textEl = document.getElementById('confirmYesNoText');
        var yesBtn = document.getElementById('confirmYesNoYes');
        var noBtn = document.getElementById('confirmYesNoNo');
        if (!modal || !textEl || !yesBtn || !noBtn){
          try{ resolve(!!window.confirm(String(message || 'Confermare?'))); }catch(_){ resolve(false); }
          return;
        }

        var settled = false;
        var listeners = [];
        function add(el, type, fn, opts){
          try{ el.addEventListener(type, fn, opts); listeners.push([el,type,fn,opts]); }catch(_){ }
        }
        function removeAll(){
          try{ listeners.forEach(function(x){ try{ x[0].removeEventListener(x[1], x[2], x[3]); }catch(_){} }); }catch(_){ }
          listeners.length = 0;
        }
        function cleanup(val){
          if (settled) return;
          settled = true;
          removeAll();
          try{ __confirmYesNoResolve = null; }catch(_){ }
          try{ __confirmYesNoReadyAt__ = 0; }catch(_){ }
          try{ __confirmYesNoSuppressUntil__ = Date.now() + 180; }catch(_){ }
          forceCloseConfirm();
          try{ setTimeout(forceCloseConfirm, 0); }catch(_){ }
          try{ setTimeout(forceCloseConfirm, 80); }catch(_){ }
          resolve(!!val);
        }
        function handler(ev){
          try{
            if (!modal || modal.hidden || modal.getAttribute('aria-hidden') === 'true') return;
            var yes = closestOrHit(ev, '#confirmYesNoYes');
            var no = closestOrHit(ev, '#confirmYesNoNo');
            if (yes){ stopEvent(ev); cleanup(true); return; }
            if (no){ stopEvent(ev); cleanup(false); return; }
            if (ev && ev.type === 'click' && ev.target === modal){ stopEvent(ev); cleanup(false); return; }
            if (modal.contains(ev.target)) return;
            stopEvent(ev);
          }catch(_){ }
        }

        try{ if (__confirmYesNoResolve) __confirmYesNoResolve(false); }catch(_){ }
        try{ __confirmYesNoResolve = resolve; }catch(_){ }
        forceCloseConfirm();
        textEl.textContent = (typeof __translateText__ === 'function') ? __translateText__(String(message || 'Confermare?')) : String(message || 'Confermare?');
        try{ if (modal.parentNode !== document.body) document.body.appendChild(modal); }catch(_){ }
        modal.hidden = false;
        try{ modal.removeAttribute('hidden'); }catch(_){ }
        modal.setAttribute('aria-hidden','false');
        modal.style.removeProperty('display');
        modal.style.setProperty('z-index','2147483647','important');
        modal.style.setProperty('pointer-events','auto','important');
        try{ document.body.classList.add('confirm-yesno-open'); }catch(_){ }
        try{ if (typeof __tagColorPopupSuppressUntil__ !== 'undefined') __tagColorPopupSuppressUntil__ = 0; }catch(_){ }
        ['touchstart','touchend','pointerdown','pointerup','mousedown','mouseup','click'].forEach(function(evt){ add(document, evt, handler, true); });
        add(yesBtn, 'click', function(ev){ stopEvent(ev); cleanup(true); }, true);
        add(noBtn, 'click', function(ev){ stopEvent(ev); cleanup(false); }, true);
      });
    };
  }catch(_){ }

  try{
    __tagColorPopupConfirm__ = async function(){
      if (colorConfirmBusy) return;
      colorConfirmBusy = true;
      var cb = __tagColorPopupState__.onSelect;
      var applyCategoryCfg = (__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? Object.assign({}, __tagColorPopupState__.applyCategory) : null;
      var payload = __tagColorPopupCurrentPayload__();
      var changed = { bg:false, border:false, fg:false, opacity:false, bold:false, any:true };
      try{ changed = __tagColorPopupChangedFields__(payload); }catch(_){ }
      __tagColorPopupState__.confirmed = true;
      try{ __tagColorPopupClose__(); }catch(_){ }
      try{ if (typeof cb === 'function') await cb(payload); }catch(_){ }
      try{
        if (applyCategoryCfg && typeof applyCategoryCfg.apply === 'function' && changed && changed.any){
          var ok = await confirmYesNo(String(applyCategoryCfg.message || 'Applicare le modifiche a tutta la categoria di questo tasto?'));
          forceCloseConfirm();
          if (ok) await applyCategoryCfg.apply(payload, changed);
        }
      }catch(_){ forceCloseConfirm(); }
      try{ toast('Colore aggiornato'); }catch(_){ }
      colorConfirmBusy = false;
    };
  }catch(_){ colorConfirmBusy = false; }
})();

/* dDAE_2.868 — Conferma colore pill: chiusura immediata SI/NO e propagazione categoria */
(function(){
  var __ddae2868ColorBusy = false;

  function stop(ev){
    try{ if (ev && ev.cancelable !== false) ev.preventDefault(); }catch(_){ }
    try{ if (ev) ev.stopPropagation(); }catch(_){ }
    try{ if (ev && ev.stopImmediatePropagation) ev.stopImmediatePropagation(); }catch(_){ }
  }

  function pureCloseConfirm(){
    try{
      var modal = document.getElementById('confirmYesNoModal');
      if (modal){
        try{ modal.hidden = true; }catch(_){ }
        try{ modal.setAttribute('hidden',''); }catch(_){ }
        try{ modal.setAttribute('aria-hidden','true'); }catch(_){ }
        try{ modal.classList.remove('is-open','show','open','active'); }catch(_){ }
        try{ modal.style.setProperty('display','none','important'); }catch(_){ }
        try{ modal.style.setProperty('pointer-events','none','important'); }catch(_){ }
        try{ modal.style.removeProperty('z-index'); }catch(_){ }
      }
      try{ document.body.classList.remove('confirm-yesno-open'); }catch(_){ }
      try{ document.documentElement.classList.remove('confirm-yesno-open'); }catch(_){ }
      try{ if (typeof __confirmYesNoReadyAt__ !== 'undefined') __confirmYesNoReadyAt__ = 0; }catch(_){ }
      try{ if (typeof __confirmYesNoSuppressUntil__ !== 'undefined') __confirmYesNoSuppressUntil__ = Date.now() + 120; }catch(_){ }
    }catch(_){ }
  }

  try{ __confirmYesNoForceClose__ = pureCloseConfirm; }catch(_){ }
  try{ globalThis.__confirmYesNoForceClose__ = pureCloseConfirm; }catch(_){ }

  function showConfirm(message){
    return new Promise(function(resolve){
      var modal = document.getElementById('confirmYesNoModal');
      var textEl = document.getElementById('confirmYesNoText');
      var yesBtn = document.getElementById('confirmYesNoYes');
      var noBtn = document.getElementById('confirmYesNoNo');
      if (!modal || !textEl || !yesBtn || !noBtn){
        try{ resolve(!!window.confirm(String(message || 'Confermare?'))); }catch(_){ resolve(false); }
        return;
      }

      var settled = false;
      var listeners = [];
      var readyAt = Date.now() + 120;
      function add(el, type, fn, opts){
        try{ el.addEventListener(type, fn, opts); listeners.push([el,type,fn,opts]); }catch(_){ }
      }
      function removeAll(){
        try{ listeners.forEach(function(x){ try{ x[0].removeEventListener(x[1], x[2], x[3]); }catch(_){} }); }catch(_){ }
        listeners.length = 0;
      }
      function finish(value, ev){
        if (ev) stop(ev);
        if (settled) return;
        if (Date.now() < readyAt) return;
        settled = true;
        removeAll();
        try{ __confirmYesNoResolve = null; }catch(_){ }
        pureCloseConfirm();
        try{ requestAnimationFrame(pureCloseConfirm); }catch(_){ }
        try{ setTimeout(pureCloseConfirm, 0); }catch(_){ }
        try{ setTimeout(pureCloseConfirm, 80); }catch(_){ }
        resolve(!!value);
      }
      function yes(ev){ finish(true, ev); }
      function no(ev){ finish(false, ev); }
      function outside(ev){
        try{
          if (ev && ev.target === modal) finish(false, ev);
        }catch(_){ }
      }

      try{ if (typeof __confirmYesNoResolve === 'function') __confirmYesNoResolve(false); }catch(_){ }
      try{ __confirmYesNoResolve = null; }catch(_){ }
      pureCloseConfirm();
      textEl.textContent = (typeof __translateText__ === 'function') ? __translateText__(String(message || 'Confermare?')) : String(message || 'Confermare?');
      try{ if (modal.parentNode !== document.body) document.body.appendChild(modal); }catch(_){ }
      try{ modal.style.removeProperty('display'); }catch(_){ }
      try{ modal.style.setProperty('z-index','2147483647','important'); }catch(_){ }
      try{ modal.style.setProperty('pointer-events','auto','important'); }catch(_){ }
      modal.hidden = false;
      try{ modal.removeAttribute('hidden'); }catch(_){ }
      try{ modal.setAttribute('aria-hidden','false'); }catch(_){ }
      try{ document.body.classList.add('confirm-yesno-open'); }catch(_){ }
      try{ document.documentElement.classList.add('confirm-yesno-open'); }catch(_){ }
      try{ if (typeof __tagColorPopupSuppressUntil__ !== 'undefined') __tagColorPopupSuppressUntil__ = Date.now() + 120; }catch(_){ }

      ['pointerup','touchend','mouseup','click'].forEach(function(type){
        add(yesBtn, type, yes, { capture:true, passive:false });
        add(noBtn, type, no, { capture:true, passive:false });
      });
      add(modal, 'click', outside, { capture:true, passive:false });
      try{ yesBtn.onclick = yes; }catch(_){ }
      try{ noBtn.onclick = no; }catch(_){ }
    });
  }

  try{ confirmYesNo = showConfirm; }catch(_){ }
  try{ globalThis.confirmYesNo = showConfirm; }catch(_){ }

  function changedFields(payload){
    try{
      if (typeof __tagColorPopupChangedFields__ === 'function') return __tagColorPopupChangedFields__(payload);
    }catch(_){ }
    return { bg:true, border:true, fg:true, opacity:true, any:true };
  }

  async function applySingle(cb, payload){
    try{ if (typeof cb === 'function') await cb(payload); }catch(_){ }
  }

  async function applyCategory(cfg, payload, changed){
    try{ if (cfg && typeof cfg.apply === 'function') await cfg.apply(payload, changed || { any:true }); }catch(_){ }
  }

  try{
    __tagColorPopupConfirm__ = async function(){
      if (__ddae2868ColorBusy) return;
      __ddae2868ColorBusy = true;
      var cb = null;
      var cfg = null;
      var payload = null;
      var changed = null;
      try{
        cb = __tagColorPopupState__ ? __tagColorPopupState__.onSelect : null;
        cfg = (__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? Object.assign({}, __tagColorPopupState__.applyCategory) : null;
        payload = (typeof __tagColorPopupCurrentPayload__ === 'function') ? __tagColorPopupCurrentPayload__() : null;
        changed = changedFields(payload);
        if (!changed || typeof changed !== 'object') changed = { any:true };
        if (changed.any !== true && (changed.bg || changed.border || changed.fg || changed.opacity || changed.bold)) changed.any = true;
        try{ __tagColorPopupState__.confirmed = true; }catch(_){ }
        try{ __tagColorPopupClose__(); }catch(_){ }
        await applySingle(cb, payload);
        if (cfg && typeof cfg.apply === 'function' && changed.any){
          var ok = await showConfirm(String(cfg.message || 'Applicare le modifiche a tutta la categoria di questo tasto?'));
          pureCloseConfirm();
          if (ok) await applyCategory(cfg, payload, changed);
        }
        try{ if (typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
        try{ if (typeof __headerActionApplyAll__ === 'function') __headerActionApplyAll__(); }catch(_){ }
        try{ if (typeof __launcherIconApplyAll__ === 'function') __launcherIconApplyAll__(); }catch(_){ }
        try{ toast('Colore aggiornato'); }catch(_){ }
      }catch(_){
        pureCloseConfirm();
      }finally{
        __ddae2868ColorBusy = false;
      }
    };
    try{ globalThis.__tagColorPopupConfirm__ = __tagColorPopupConfirm__; }catch(_){ }
  }catch(_){ __ddae2868ColorBusy = false; }
})();


/* dDAE_2.873 — Debug definitivo popup conferma colore: un solo gestore globale, nessun blocco iOS */
(function(){
  var busyColorConfirm = false;
  var activeConfirm = null;

  function stop(ev){
    try{ if (ev && ev.cancelable !== false) ev.preventDefault(); }catch(_){ }
    try{ if (ev) ev.stopPropagation(); }catch(_){ }
    try{ if (ev && ev.stopImmediatePropagation) ev.stopImmediatePropagation(); }catch(_){ }
  }

  function isOpen(el){
    try{ return !!(el && !el.hidden && el.getAttribute('aria-hidden') !== 'true'); }catch(_){ return false; }
  }

  function closest(target, selector){
    try{ return target && target.closest ? target.closest(selector) : null; }catch(_){ return null; }
  }

  function hardCloseConfirm(){
    try{
      var modal = document.getElementById('confirmYesNoModal');
      if (modal){
        try{ modal.hidden = true; }catch(_){ }
        try{ modal.setAttribute('hidden',''); }catch(_){ }
        try{ modal.setAttribute('aria-hidden','true'); }catch(_){ }
        try{ modal.classList.remove('is-open','show','open','active'); }catch(_){ }
        try{ modal.style.setProperty('display','none','important'); }catch(_){ }
        try{ modal.style.setProperty('pointer-events','none','important'); }catch(_){ }
        try{ modal.style.removeProperty('z-index'); }catch(_){ }
      }
      try{ document.body.classList.remove('confirm-yesno-open'); }catch(_){ }
      try{ document.documentElement.classList.remove('confirm-yesno-open'); }catch(_){ }
      try{ if (typeof __confirmYesNoResolve !== 'undefined') __confirmYesNoResolve = null; }catch(_){ }
      try{ if (typeof __confirmYesNoReadyAt__ !== 'undefined') __confirmYesNoReadyAt__ = 0; }catch(_){ }
      try{ if (typeof __confirmYesNoSuppressUntil__ !== 'undefined') __confirmYesNoSuppressUntil__ = 0; }catch(_){ }
      try{
        var otherOpen = document.querySelector('.modal.is-open:not([hidden]), .modal:not([hidden])[aria-hidden="false"]');
        if (!otherOpen) document.body.classList.remove('modal-open');
      }catch(_){ }
    }catch(_){ }
  }

  function finishConfirm(value, ev){
    if (ev) stop(ev);
    var st = activeConfirm;
    if (!st || st.done) { hardCloseConfirm(); return; }
    st.done = true;
    activeConfirm = null;
    try{ if (st.timer) clearTimeout(st.timer); }catch(_){ }
    hardCloseConfirm();
    try{ requestAnimationFrame(hardCloseConfirm); }catch(_){ }
    try{ setTimeout(hardCloseConfirm, 0); }catch(_){ }
    try{ setTimeout(hardCloseConfirm, 80); }catch(_){ }
    try{ st.resolve(!!value); }catch(_){ }
  }

  function globalConfirmInput(ev){
    try{
      var modal = document.getElementById('confirmYesNoModal');
      if (!activeConfirm || !isOpen(modal)) return;
      var t = ev && ev.target;
      if (closest(t, '#confirmYesNoYes')) { finishConfirm(true, ev); return; }
      if (closest(t, '#confirmYesNoNo')) { finishConfirm(false, ev); return; }
      if (t === modal) { finishConfirm(false, ev); return; }
      if (modal && t && !modal.contains(t)) { stop(ev); return; }
    }catch(_){ }
  }

  function bindGlobalConfirmInput(){
    try{
      if (window.__ddae2869ConfirmInputBound__) return;
      window.__ddae2869ConfirmInputBound__ = true;
      ['touchstart','touchend','pointerdown','pointerup','mousedown','mouseup','click'].forEach(function(evt){
        try{ document.addEventListener(evt, globalConfirmInput, { capture:true, passive:false }); }catch(_){ try{ document.addEventListener(evt, globalConfirmInput, true); }catch(__){ } }
      });
    }catch(_){ }
  }

  function showConfirm(message){
    bindGlobalConfirmInput();
    return new Promise(function(resolve){
      try{
        if (activeConfirm && typeof activeConfirm.resolve === 'function'){
          try{ activeConfirm.resolve(false); }catch(_){ }
        }
        activeConfirm = null;
        hardCloseConfirm();
        var modal = document.getElementById('confirmYesNoModal');
        var textEl = document.getElementById('confirmYesNoText');
        var yesBtn = document.getElementById('confirmYesNoYes');
        var noBtn = document.getElementById('confirmYesNoNo');
        if (!modal || !textEl || !yesBtn || !noBtn){
          try{ resolve(!!window.confirm(String(message || 'Confermare?'))); }catch(_){ resolve(false); }
          return;
        }
        activeConfirm = { resolve: resolve, done:false, timer:null };
        textEl.textContent = (typeof __translateText__ === 'function') ? __translateText__(String(message || 'Confermare?')) : String(message || 'Confermare?');
        try{ if (modal.parentNode !== document.body) document.body.appendChild(modal); }catch(_){ }
        try{ modal.classList.add('is-open'); }catch(_){ }
        try{ modal.style.removeProperty('display'); }catch(_){ }
        try{ modal.style.setProperty('display','flex','important'); }catch(_){ }
        try{ modal.style.setProperty('z-index','2147483647','important'); }catch(_){ }
        try{ modal.style.setProperty('pointer-events','auto','important'); }catch(_){ }
        try{ yesBtn.style.setProperty('pointer-events','auto','important'); noBtn.style.setProperty('pointer-events','auto','important'); }catch(_){ }
        modal.hidden = false;
        try{ modal.removeAttribute('hidden'); }catch(_){ }
        try{ modal.setAttribute('aria-hidden','false'); }catch(_){ }
        try{ document.body.classList.add('confirm-yesno-open'); }catch(_){ }
        try{ document.documentElement.classList.add('confirm-yesno-open'); }catch(_){ }
        try{ if (typeof __confirmYesNoSuppressUntil__ !== 'undefined') __confirmYesNoSuppressUntil__ = 0; }catch(_){ }
        try{ if (typeof __tagColorPopupSuppressUntil__ !== 'undefined') __tagColorPopupSuppressUntil__ = 0; }catch(_){ }
        try{ yesBtn.onclick = function(ev){ finishConfirm(true, ev); }; }catch(_){ }
        try{ noBtn.onclick = function(ev){ finishConfirm(false, ev); }; }catch(_){ }
        activeConfirm.timer = setTimeout(function(){ if (activeConfirm && activeConfirm.resolve === resolve) finishConfirm(false); }, 60000);
      }catch(_){
        hardCloseConfirm();
        try{ resolve(false); }catch(__){ }
      }
    });
  }

  try{ __confirmYesNoForceClose__ = hardCloseConfirm; }catch(_){ }
  try{ globalThis.__confirmYesNoForceClose__ = hardCloseConfirm; }catch(_){ }
  try{ confirmYesNo = showConfirm; }catch(_){ }
  try{ globalThis.confirmYesNo = showConfirm; }catch(_){ }

  function changedFields(payload){
    try{
      if (typeof __tagColorPopupChangedFields__ === 'function') return __tagColorPopupChangedFields__(payload);
    }catch(_){ }
    return { bg:true, border:true, fg:true, opacity:true, any:true, next:(payload && payload.colors) ? payload.colors : null };
  }

  function visualFromPayload(payload, changed){
    try{
      var colors = payload && payload.colors && typeof payload.colors === 'object' ? payload.colors : {};
      var next = (changed && changed.next && typeof changed.next === 'object') ? changed.next : null;
      return {
        bg: (next && next.bg) || colors.bg || 'blue-4',
        border: (next && next.border) || colors.border || colors.bg || 'blue-4',
        fg: (next && next.fg) || colors.fg || 'white',
        opacity: (typeof __designBgOpacityNormalize__ === 'function') ? __designBgOpacityNormalize__(payload && payload.opacity != null ? payload.opacity : (next && next.opacity != null ? next.opacity : 0.80)) : (payload && payload.opacity != null ? payload.opacity : 0.80),
        bold: false
      };
    }catch(_){ return { bg:'blue-4', border:'blue-4', fg:'white', opacity:0.80, bold:false }; }
  }

  async function applyCategorySafely(cfg, payload, changed, target){
    try{
      if (String(target || '') === 'pill-single-button'){
        var visual = visualFromPayload(payload, changed);
        try{ if (typeof __pillThemeWrite__ === 'function') __pillThemeWrite__(visual); }catch(_){ }
        try{ if (typeof __pillThemeOverwriteTargets__ === 'function') __pillThemeOverwriteTargets__(visual); }catch(_){ }
      }
      if (cfg && typeof cfg.apply === 'function') await cfg.apply(payload, changed || { any:true });
    }catch(_){ }
  }

  try{
    __tagColorPopupConfirm__ = async function(){
      if (busyColorConfirm) return;
      busyColorConfirm = true;
      var cb = null, cfg = null, payload = null, changed = null, target = '';
      try{
        target = String((__tagColorPopupState__ && __tagColorPopupState__.target) || '');
        cb = __tagColorPopupState__ ? __tagColorPopupState__.onSelect : null;
        cfg = (__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? Object.assign({}, __tagColorPopupState__.applyCategory) : null;
        payload = (typeof __tagColorPopupCurrentPayload__ === 'function') ? __tagColorPopupCurrentPayload__() : null;
        changed = changedFields(payload);
        if (!changed || typeof changed !== 'object') changed = { any:true };
        if (changed.any !== true && (changed.bg || changed.border || changed.fg || changed.opacity || changed.bold)) changed.any = true;
        try{ __tagColorPopupState__.confirmed = true; }catch(_){ }
        try{ __tagColorPopupClose__(); }catch(_){ }
        try{ if (typeof cb === 'function') await cb(payload); }catch(_){ }
        if (cfg && typeof cfg.apply === 'function' && changed.any){
          var ok = await showConfirm(String(cfg.message || 'Applicare le modifiche a tutta la categoria di questo tasto?'));
          hardCloseConfirm();
          if (ok) await applyCategorySafely(cfg, payload, changed, target);
        }
        try{ if (typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
        try{ if (typeof __headerActionApplyAll__ === 'function') __headerActionApplyAll__(); }catch(_){ }
        try{ if (typeof __launcherIconApplyAll__ === 'function') __launcherIconApplyAll__(); }catch(_){ }
        try{ if (typeof renderRoomSettingsPage === 'function') renderRoomSettingsPage(); }catch(_){ }
        try{ toast('Colore aggiornato'); }catch(_){ }
      }catch(_){
        hardCloseConfirm();
      }finally{
        busyColorConfirm = false;
      }
    };
    try{ globalThis.__tagColorPopupConfirm__ = __tagColorPopupConfirm__; }catch(_){ }
  }catch(_){ busyColorConfirm = false; }

  try{ bindGlobalConfirmInput(); }catch(_){ }
  try{ setTimeout(function(){ var m=document.getElementById('confirmYesNoModal'); if (m && (m.hidden || m.getAttribute('aria-hidden') === 'true')) hardCloseConfirm(); }, 0); }catch(_){ }
})();


/* dDAE_2.873 — Popup conferma colore isolato: nessun blocco iOS dopo SI/NO */
(function(){
  var busy = false;
  var overlayId = 'ddae870ConfirmOverlay';

  function safeCall(fn){ try{ if (typeof fn === 'function') fn(); }catch(_){ } }
  function stop(ev){
    try{ if (ev && ev.cancelable !== false) ev.preventDefault(); }catch(_){ }
    try{ if (ev) ev.stopPropagation(); }catch(_){ }
    try{ if (ev && ev.stopImmediatePropagation) ev.stopImmediatePropagation(); }catch(_){ }
  }
  function removeOldConfirm(){
    try{
      var m = document.getElementById('confirmYesNoModal');
      if (m){
        m.hidden = true;
        m.setAttribute('hidden','');
        m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.setProperty('display','none','important');
        m.style.setProperty('pointer-events','none','important');
      }
    }catch(_){ }
    try{ document.body.classList.remove('confirm-yesno-open'); }catch(_){ }
    try{ document.documentElement.classList.remove('confirm-yesno-open'); }catch(_){ }
    try{ if (typeof __confirmYesNoResolve !== 'undefined') __confirmYesNoResolve = null; }catch(_){ }
    try{ if (typeof __confirmYesNoSuppressUntil__ !== 'undefined') __confirmYesNoSuppressUntil__ = 0; }catch(_){ }
    try{ if (typeof __confirmYesNoReadyAt__ !== 'undefined') __confirmYesNoReadyAt__ = 0; }catch(_){ }
  }
  function removeOverlay(){
    try{ var old = document.getElementById(overlayId); if (old && old.parentNode) old.parentNode.removeChild(old); }catch(_){ }
    removeOldConfirm();
    try{ document.body.classList.remove('ddae870-confirm-open'); }catch(_){ }
    try{ document.documentElement.classList.remove('ddae870-confirm-open'); }catch(_){ }
    try{
      var stillOpen = document.querySelector('.modal.is-open:not([hidden]), .modal:not([hidden])[aria-hidden="false"], #tagColorModal:not([hidden])');
      if (!stillOpen) document.body.classList.remove('modal-open');
    }catch(_){ document.body.classList.remove('modal-open'); }
  }
  function closeTagColorHard(){
    try{ if (typeof __tagColorPopupClose__ === 'function') __tagColorPopupClose__(); }catch(_){ }
    try{
      var m = document.getElementById('tagColorModal');
      if (m){
        m.hidden = true;
        m.setAttribute('hidden','');
        m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.removeProperty('display');
        m.style.setProperty('pointer-events','none','important');
      }
    }catch(_){ }
    try{ document.body.classList.remove('tag-color-modal-open'); }catch(_){ }
    try{ if (typeof __tagColorPopupSuppressUntil__ !== 'undefined') __tagColorPopupSuppressUntil__ = 0; }catch(_){ }
  }
  function pointFrom(ev){
    try{
      var t = (ev && ev.changedTouches && ev.changedTouches[0]) || (ev && ev.touches && ev.touches[0]);
      if (t) return {x:t.clientX, y:t.clientY};
      return {x:ev.clientX, y:ev.clientY};
    }catch(_){ return {x:null,y:null}; }
  }
  function hitButton(ev, selector){
    try{
      var target = ev && ev.target;
      var direct = target && target.closest ? target.closest(selector) : null;
      if (direct) return direct;
      var p = pointFrom(ev);
      if (p.x == null || p.y == null) return null;
      var nodes = Array.prototype.slice.call(document.querySelectorAll(selector));
      for (var i = nodes.length - 1; i >= 0; i--){
        var el = nodes[i];
        var r = el.getBoundingClientRect();
        if (p.x >= r.left && p.x <= r.right && p.y >= r.top && p.y <= r.bottom) return el;
      }
    }catch(_){ }
    return null;
  }
  function isolatedConfirm(message){
    return new Promise(function(resolve){
      try{
        removeOverlay();
        removeOldConfirm();
        var ov = document.createElement('div');
        ov.id = overlayId;
        ov.setAttribute('role','presentation');
        ov.innerHTML = '<div class="ddae870-confirm-card" role="dialog" aria-modal="true" aria-label="Conferma"><div class="ddae870-confirm-text"></div><div class="ddae870-confirm-actions"><button id="ddae870ConfirmYes" type="button">SI</button><button id="ddae870ConfirmNo" type="button">NO</button></div></div>';
        document.body.appendChild(ov);
        var txt = ov.querySelector('.ddae870-confirm-text');
        if (txt) txt.textContent = (typeof __translateText__ === 'function') ? __translateText__(String(message || 'Confermare?')) : String(message || 'Confermare?');
        document.body.classList.add('ddae870-confirm-open');
        document.documentElement.classList.add('ddae870-confirm-open');
        var done = false;
        var listeners = [];
        var timer = null;
        function add(el, type, fn, opts){ try{ el.addEventListener(type, fn, opts); listeners.push([el,type,fn,opts]); }catch(_){ } }
        function cleanup(value, ev){
          if (ev) stop(ev);
          if (done) return;
          done = true;
          try{ listeners.forEach(function(x){ try{ x[0].removeEventListener(x[1], x[2], x[3]); }catch(_){} }); }catch(_){ }
          try{ if (timer) clearTimeout(timer); }catch(_){ }
          removeOverlay();
          try{ requestAnimationFrame(removeOverlay); }catch(_){ }
          try{ setTimeout(removeOverlay, 0); }catch(_){ }
          try{ setTimeout(removeOverlay, 120); }catch(_){ }
          resolve(!!value);
        }
        function handler(ev){
          try{
            if (hitButton(ev, '#ddae870ConfirmYes')) { cleanup(true, ev); return; }
            if (hitButton(ev, '#ddae870ConfirmNo')) { cleanup(false, ev); return; }
            var card = ov.querySelector('.ddae870-confirm-card');
            var target = ev && ev.target;
            if (target === ov || (target && card && !card.contains(target))) { cleanup(false, ev); return; }
            stop(ev);
          }catch(_){ }
        }
        ['touchstart','touchend','pointerdown','pointerup','mousedown','mouseup','click'].forEach(function(evt){ add(document, evt, handler, {capture:true, passive:false}); });
        timer = setTimeout(function(){ cleanup(false); }, 60000);
      }catch(_){
        removeOverlay();
        resolve(false);
      }
    });
  }

  try{ __confirmYesNoForceClose__ = removeOverlay; }catch(_){ }
  try{ globalThis.__confirmYesNoForceClose__ = removeOverlay; }catch(_){ }
  try{ confirmYesNo = isolatedConfirm; }catch(_){ }
  try{ globalThis.confirmYesNo = isolatedConfirm; }catch(_){ }

  function changedFields(payload){
    try{ if (typeof __tagColorPopupChangedFields__ === 'function') return __tagColorPopupChangedFields__(payload); }catch(_){ }
    return { bg:true, border:true, fg:true, opacity:true, any:true };
  }
  function normalizeChanged(changed){
    try{
      if (!changed || typeof changed !== 'object') changed = {};
      if (changed.any !== true && (changed.bg || changed.border || changed.fg || changed.opacity || changed.bold)) changed.any = true;
      if (changed.any !== true) changed.any = true;
    }catch(_){ changed = { any:true }; }
    return changed;
  }
  function visualFromPayload(payload){
    try{
      var colors = payload && payload.colors && typeof payload.colors === 'object' ? payload.colors : {};
      return {
        bg: colors.bg || 'blue-4',
        border: colors.border || colors.bg || 'blue-4',
        fg: colors.fg || 'white',
        opacity: (typeof __designBgOpacityNormalize__ === 'function') ? __designBgOpacityNormalize__(payload && payload.opacity != null ? payload.opacity : 0.80) : (payload && payload.opacity != null ? payload.opacity : 0.80),
        bold: false
      };
    }catch(_){ return { bg:'blue-4', border:'blue-4', fg:'white', opacity:0.80, bold:false }; }
  }
  async function applyCategory(target, cfg, payload, changed){
    try{
      if (String(target || '') === 'pill-single-button'){
        var v = visualFromPayload(payload);
        try{ if (typeof __pillThemeWrite__ === 'function') __pillThemeWrite__(v); }catch(_){ }
        try{ if (typeof __pillThemeOverwriteTargets__ === 'function') __pillThemeOverwriteTargets__(v); }catch(_){ }
      }
      if (cfg && typeof cfg.apply === 'function') await cfg.apply(payload, changed || {any:true});
    }catch(_){ }
  }
  try{
    __tagColorPopupConfirm__ = async function(){
      if (busy) return;
      busy = true;
      var cb = null, cfg = null, payload = null, changed = null, target = '';
      try{
        target = String((__tagColorPopupState__ && __tagColorPopupState__.target) || '');
        cb = __tagColorPopupState__ ? __tagColorPopupState__.onSelect : null;
        cfg = (__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? Object.assign({}, __tagColorPopupState__.applyCategory) : null;
        payload = (typeof __tagColorPopupCurrentPayload__ === 'function') ? __tagColorPopupCurrentPayload__() : null;
        changed = normalizeChanged(changedFields(payload));
        try{ __tagColorPopupState__.confirmed = true; }catch(_){ }
        closeTagColorHard();
        try{ if (typeof cb === 'function') await cb(payload); }catch(_){ }
        if (cfg && typeof cfg.apply === 'function' && changed.any){
          var ok = await isolatedConfirm(String(cfg.message || 'Applicare le modifiche a tutta la categoria di questo tasto?'));
          removeOverlay();
          if (ok) await applyCategory(target, cfg, payload, changed);
        }
        safeCall(typeof __pillApplyAll__ === 'function' ? __pillApplyAll__ : null);
        safeCall(typeof __headerActionApplyAll__ === 'function' ? __headerActionApplyAll__ : null);
        safeCall(typeof __launcherIconApplyAll__ === 'function' ? __launcherIconApplyAll__ : null);
        safeCall(typeof renderRoomSettingsPage === 'function' ? renderRoomSettingsPage : null);
        try{ toast('Colore aggiornato'); }catch(_){ }
      }catch(_){
        removeOverlay();
      }finally{
        busy = false;
        removeOldConfirm();
      }
    };
    try{ globalThis.__tagColorPopupConfirm__ = __tagColorPopupConfirm__; }catch(_){ }
  }catch(_){ busy = false; }
})();


/* dDAE_2.873 — Garanzia riapertura popup colore dopo conferma isolata */
(function(){
  try{
    var oldOpen = (typeof __tagColorPopupOpen__ === 'function') ? __tagColorPopupOpen__ : null;
    if (oldOpen && !oldOpen.__ddae2870ReopenFix){
      var wrapped = function(){
        try{ var m=document.getElementById('tagColorModal'); if (m){ m.style.removeProperty('display'); m.style.removeProperty('pointer-events'); } }catch(_){ }
        var out = oldOpen.apply(this, arguments);
        try{ var m2=document.getElementById('tagColorModal'); if (m2){ m2.style.removeProperty('display'); m2.style.setProperty('pointer-events','auto','important'); } }catch(_){ }
        return out;
      };
      wrapped.__ddae2870ReopenFix = true;
      __tagColorPopupOpen__ = wrapped;
      try{ globalThis.__tagColorPopupOpen__ = wrapped; }catch(_){ }
    }
  }catch(_){ }
})();

/* dDAE_2.873 — Riscrittura flusso colore tasti: niente popup conferma DOM, nessun overlay bloccante */
(function(){
  var __ddae2871Busy = false;

  function __ddae2871RemoveNode(id){
    try{ var n = document.getElementById(id); if (n && n.parentNode) n.parentNode.removeChild(n); }catch(_){ }
  }

  function __ddae2871UnlockUi(){
    try{ __ddae2871RemoveNode('ddae870ConfirmOverlay'); }catch(_){ }
    try{
      var m = document.getElementById('confirmYesNoModal');
      if (m){
        m.hidden = true;
        m.setAttribute('hidden','');
        m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.setProperty('display','none','important');
        m.style.setProperty('pointer-events','none','important');
      }
    }catch(_){ }
    try{ document.body.classList.remove('confirm-yesno-open','ddae870-confirm-open'); }catch(_){ }
    try{ document.documentElement.classList.remove('confirm-yesno-open','ddae870-confirm-open'); }catch(_){ }
    try{ if (typeof __confirmYesNoResolve !== 'undefined') __confirmYesNoResolve = null; }catch(_){ }
    try{ if (typeof __confirmYesNoReadyAt__ !== 'undefined') __confirmYesNoReadyAt__ = 0; }catch(_){ }
    try{ if (typeof __confirmYesNoSuppressUntil__ !== 'undefined') __confirmYesNoSuppressUntil__ = 0; }catch(_){ }
    try{
      var tag = document.getElementById('tagColorModal');
      var tagOpen = !!(tag && !tag.hidden && tag.getAttribute('aria-hidden') !== 'true');
      if (!tagOpen) document.body.classList.remove('tag-color-modal-open');
      var anyOpen = !!document.querySelector('.modal.is-open:not([hidden]), .modal:not([hidden])[aria-hidden="false"]');
      if (!anyOpen && !tagOpen) document.body.classList.remove('modal-open');
    }catch(_){ document.body.classList.remove('modal-open'); }
  }

  function __ddae2871CloseColorPopupWithoutRevert(){
    try{ if (__tagColorPopupState__) __tagColorPopupState__.confirmed = true; }catch(_){ }
    try{ if (typeof __tagColorPopupClose__ === 'function') __tagColorPopupClose__(); }catch(_){ }
    try{
      var m = document.getElementById('tagColorModal');
      if (m){
        m.hidden = true;
        m.setAttribute('hidden','');
        m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.removeProperty('display');
        m.style.removeProperty('pointer-events');
      }
    }catch(_){ }
    try{ document.body.classList.remove('tag-color-modal-open'); }catch(_){ }
    try{ if (typeof __tagColorPopupSuppressUntil__ !== 'undefined') __tagColorPopupSuppressUntil__ = 0; }catch(_){ }
    __ddae2871UnlockUi();
  }

  function __ddae2871ChangedFields(payload){
    try{
      if (typeof __tagColorPopupChangedFields__ === 'function'){
        var c = __tagColorPopupChangedFields__(payload);
        if (c && typeof c === 'object'){
          if (c.any !== true && (c.bg || c.border || c.fg || c.opacity || c.bold)) c.any = true;
          return c;
        }
      }
    }catch(_){ }
    return { bg:true, border:true, fg:true, opacity:true, bold:false, any:true };
  }

  function __ddae2871AskApplyAll(message){
    __ddae2871UnlockUi();
    try{
      var txt = String(message || 'Applicare le modifiche a tutta la categoria di questo tasto?');
      if (typeof __translateText__ === 'function') txt = __translateText__(txt);
      return !!window.confirm(txt + '\n\nOK = SI\nAnnulla = NO');
    }catch(_){ return false; }
  }

  async function __ddae2871ApplyCategory(target, cfg, payload, changed){
    try{
      if (cfg && typeof cfg.apply === 'function') await cfg.apply(payload, changed || { any:true, bg:true, border:true, fg:true, opacity:true });
      if (String(target || '') === 'pill-single-button'){
        try{ if (typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
      }
    }catch(_){ }
  }

  try{
    __confirmYesNoForceClose__ = __ddae2871UnlockUi;
    globalThis.__confirmYesNoForceClose__ = __ddae2871UnlockUi;
  }catch(_){ }

  try{
    __tagColorPopupApplyCategoryIfNeeded__ = async function(payload, applyCategoryCfg){
      var cfg = (applyCategoryCfg && typeof applyCategoryCfg === 'object') ? applyCategoryCfg : ((__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? __tagColorPopupState__.applyCategory : null);
      if (!cfg || typeof cfg.apply !== 'function') return;
      var changed = __ddae2871ChangedFields(payload);
      if (!changed.any) return;
      if (!__ddae2871AskApplyAll(cfg.message)) return;
      await __ddae2871ApplyCategory((__tagColorPopupState__ && __tagColorPopupState__.target) || '', cfg, payload, changed);
    };
    globalThis.__tagColorPopupApplyCategoryIfNeeded__ = __tagColorPopupApplyCategoryIfNeeded__;
  }catch(_){ }

  try{
    __tagColorPopupConfirm__ = async function(){
      if (__ddae2871Busy) return;
      __ddae2871Busy = true;
      var cb = null, cfg = null, payload = null, changed = null, target = '';
      try{
        target = String((__tagColorPopupState__ && __tagColorPopupState__.target) || '');
        cb = (__tagColorPopupState__ && typeof __tagColorPopupState__.onSelect === 'function') ? __tagColorPopupState__.onSelect : null;
        cfg = (__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? Object.assign({}, __tagColorPopupState__.applyCategory) : null;
        payload = (typeof __tagColorPopupCurrentPayload__ === 'function') ? __tagColorPopupCurrentPayload__() : { mode:'bg', spec:'blue-4', opacity:0.80, colors:{ bg:'blue-4', border:'blue-4', fg:'white' } };
        changed = __ddae2871ChangedFields(payload);
        __ddae2871CloseColorPopupWithoutRevert();

        if (typeof cb === 'function'){
          try{ await cb(payload); }catch(_){ }
        }

        if (cfg && typeof cfg.apply === 'function' && changed.any){
          var ok = __ddae2871AskApplyAll(cfg.message);
          __ddae2871UnlockUi();
          if (ok) await __ddae2871ApplyCategory(target, cfg, payload, changed);
        }

        try{ if (typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
        try{ if (typeof __headerActionApplyAll__ === 'function') __headerActionApplyAll__(); }catch(_){ }
        try{ if (typeof __launcherIconApplyAll__ === 'function') __launcherIconApplyAll__(); }catch(_){ }
        try{ if (typeof __setupSingleActionButtonPaletteBindings__ === 'function') __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
        try{ if (typeof __applySettingsEditorButtonBoldAndVisuals__ === 'function') __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
        try{ if (typeof renderRoomSettingsPage === 'function') renderRoomSettingsPage(); }catch(_){ }
        try{ if (typeof saveData === 'function') saveData(); }catch(_){ }
        try{ toast('Colore aggiornato'); }catch(_){ }
      }catch(_){
        __ddae2871UnlockUi();
      }finally{
        __ddae2871Busy = false;
        __ddae2871UnlockUi();
        try{ setTimeout(__ddae2871UnlockUi, 0); }catch(_){ }
        try{ setTimeout(__ddae2871UnlockUi, 120); }catch(_){ }
        try{ setTimeout(__ddae2871UnlockUi, 400); }catch(_){ }
      }
    };
    globalThis.__tagColorPopupConfirm__ = __tagColorPopupConfirm__;
  }catch(_){ __ddae2871Busy = false; }

  try{
    var oldOpen = (typeof __tagColorPopupOpen__ === 'function') ? __tagColorPopupOpen__ : null;
    if (oldOpen && !oldOpen.__ddae2871CleanOpen){
      var openClean = function(){
        __ddae2871UnlockUi();
        try{ var m = document.getElementById('tagColorModal'); if (m){ m.style.removeProperty('display'); m.style.removeProperty('pointer-events'); } }catch(_){ }
        return oldOpen.apply(this, arguments);
      };
      openClean.__ddae2871CleanOpen = true;
      __tagColorPopupOpen__ = openClean;
      globalThis.__tagColorPopupOpen__ = openClean;
    }
  }catch(_){ }

  try{ window.addEventListener('pageshow', __ddae2871UnlockUi, { passive:true }); }catch(_){ }
  try{ document.addEventListener('visibilitychange', __ddae2871UnlockUi, true); }catch(_){ }
})();


/* dDAE_2.896 — Popup conferma colore custom SI/NO e anti doppia conferma */
(function(){
  var __ddae2877ConfirmBusy = false;
  var __ddae2877PromptOpen = false;
  var __ddae2877LastOkTap = { t:0 };

  function __ddae2877Stop(ev){
    try{ if (ev && ev.cancelable !== false) ev.preventDefault(); }catch(_){ }
    try{ if (ev) ev.stopPropagation(); }catch(_){ }
    try{ if (ev && ev.stopImmediatePropagation) ev.stopImmediatePropagation(); }catch(_){ }
  }
  function __ddae2877Text(txt){
    try{ return (typeof __translateText__ === 'function') ? __translateText__(String(txt || '')) : String(txt || ''); }catch(_){ return String(txt || ''); }
  }
  function __ddae2877Remove(id){
    try{ var n=document.getElementById(id); if (n && n.parentNode) n.parentNode.removeChild(n); }catch(_){ }
  }
  function __ddae2877ForceCloseLegacy(){
    try{ __ddae2877Remove('ddae2877ConfirmOverlay'); }catch(_){ }
    try{ __ddae2877Remove('ddae870ConfirmOverlay'); }catch(_){ }
    try{
      var m=document.getElementById('confirmYesNoModal');
      if (m){
        m.hidden=true;
        m.setAttribute('hidden','');
        m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.setProperty('display','none','important');
        m.style.setProperty('pointer-events','none','important');
      }
    }catch(_){ }
    try{ document.body.classList.remove('confirm-yesno-open','ddae870-confirm-open','ddae2877-confirm-open'); }catch(_){ }
    try{ document.documentElement.classList.remove('confirm-yesno-open','ddae870-confirm-open','ddae2877-confirm-open'); }catch(_){ }
    try{ if (typeof __confirmYesNoResolve !== 'undefined') __confirmYesNoResolve = null; }catch(_){ }
  }
  function __ddae2877CustomConfirm(message){
    return new Promise(function(resolve){
      try{
        if (__ddae2877PromptOpen){ resolve(false); return; }
        __ddae2877PromptOpen = true;
        __ddae2877ForceCloseLegacy();
        var overlay=document.createElement('div');
        overlay.id='ddae2877ConfirmOverlay';
        overlay.setAttribute('role','presentation');
        overlay.innerHTML = '<div class="ddae2877-confirm-card" role="dialog" aria-modal="true" aria-label="Conferma"><div class="ddae2877-confirm-text"></div><div class="ddae2877-confirm-actions"><button id="ddae2877ConfirmYes" type="button">SÌ</button><button id="ddae2877ConfirmNo" type="button">NO</button></div></div>';
        var text=overlay.querySelector('.ddae2877-confirm-text');
        var yes=overlay.querySelector('#ddae2877ConfirmYes');
        var no=overlay.querySelector('#ddae2877ConfirmNo');
        if (text) text.textContent = __ddae2877Text(message || 'Applicare le modifiche a tutti i tasti della stessa categoria?');
        document.body.appendChild(overlay);
        try{ document.body.classList.add('ddae2877-confirm-open'); }catch(_){ }
        var done=false;
        function finish(val, ev){
          __ddae2877Stop(ev);
          if (done) return;
          done=true;
          try{ __ddae2877Remove('ddae2877ConfirmOverlay'); }catch(_){ }
          try{ document.body.classList.remove('ddae2877-confirm-open'); }catch(_){ }
          __ddae2877PromptOpen=false;
          try{ setTimeout(__ddae2877ForceCloseLegacy, 0); setTimeout(__ddae2877ForceCloseLegacy, 180); }catch(_){ }
          resolve(!!val);
        }
        ['pointerdown','touchstart','mousedown','click'].forEach(function(evt){
          try{ overlay.addEventListener(evt, function(ev){
            var t=ev && ev.target;
            if (t && (t.closest('#ddae2877ConfirmYes') || t.closest('#ddae2877ConfirmNo'))) return;
            __ddae2877Stop(ev);
          }, true); }catch(_){ }
        });
        ['pointerup','touchend','mouseup','click'].forEach(function(evt){
          try{ yes.addEventListener(evt, function(ev){ finish(true, ev); }, true); }catch(_){ }
          try{ no.addEventListener(evt, function(ev){ finish(false, ev); }, true); }catch(_){ }
        });
      }catch(_){ __ddae2877PromptOpen=false; resolve(false); }
    });
  }
  function __ddae2877Changed(payload){
    try{
      if (typeof __tagColorPopupChangedFields__ === 'function'){
        var c=__tagColorPopupChangedFields__(payload);
        if (c && typeof c === 'object'){
          if (c.any !== true && (c.bg || c.border || c.fg || c.opacity || c.bold)) c.any=true;
          return c;
        }
      }
    }catch(_){ }
    return { bg:true, border:true, fg:true, opacity:true, bold:false, any:true };
  }
  async function __ddae2877ApplyCategory(target, cfg, payload, changed){
    try{
      if (cfg && typeof cfg.apply === 'function') await cfg.apply(payload, changed || { any:true, bg:true, border:true, fg:true, opacity:true });
      if (String(target || '') === 'pill-single-button'){
        try{ if (typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
      }
    }catch(_){ }
  }
  function __ddae2877CloseColorPopupWithoutRevert(){
    try{ if (__tagColorPopupState__) __tagColorPopupState__.confirmed = true; }catch(_){ }
    try{ if (typeof __tagColorPopupClose__ === 'function') __tagColorPopupClose__(); }catch(_){ }
    try{
      var m=document.getElementById('tagColorModal');
      if (m){
        m.hidden=true;
        m.setAttribute('hidden','');
        m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.removeProperty('display');
        m.style.removeProperty('pointer-events');
      }
    }catch(_){ }
    try{ document.body.classList.remove('tag-color-modal-open'); }catch(_){ }
  }

  try{
    __tagColorPopupApplyCategoryIfNeeded__ = async function(payload, applyCategoryCfg){
      if (__ddae2877ConfirmBusy) return;
      var cfg = (applyCategoryCfg && typeof applyCategoryCfg === 'object') ? applyCategoryCfg : ((__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? __tagColorPopupState__.applyCategory : null);
      if (!cfg || typeof cfg.apply !== 'function') return;
      var changed = __ddae2877Changed(payload);
      if (!changed.any) return;
      var ok = await __ddae2877CustomConfirm(String(cfg.message || 'Applicare le modifiche a tutti i tasti della stessa categoria?'));
      if (ok) await __ddae2877ApplyCategory((__tagColorPopupState__ && __tagColorPopupState__.target) || '', cfg, payload, changed);
    };
    globalThis.__tagColorPopupApplyCategoryIfNeeded__ = __tagColorPopupApplyCategoryIfNeeded__;
  }catch(_){ }

  try{
    __tagColorPopupConfirm__ = async function(){
      var now=Date.now();
      if (__ddae2877ConfirmBusy) return;
      if (now - (__ddae2877LastOkTap.t || 0) < 650) return;
      __ddae2877LastOkTap.t = now;
      __ddae2877ConfirmBusy = true;
      var cb=null, cfg=null, payload=null, changed=null, target='';
      try{
        target=String((__tagColorPopupState__ && __tagColorPopupState__.target) || '');
        cb=(__tagColorPopupState__ && typeof __tagColorPopupState__.onSelect === 'function') ? __tagColorPopupState__.onSelect : null;
        cfg=(__tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory === 'object') ? Object.assign({}, __tagColorPopupState__.applyCategory) : null;
        payload=(typeof __tagColorPopupCurrentPayload__ === 'function') ? __tagColorPopupCurrentPayload__() : { mode:'bg', spec:'blue-4', opacity:0.80, colors:{ bg:'blue-4', border:'blue-4', fg:'white' } };
        changed=__ddae2877Changed(payload);
        __ddae2877CloseColorPopupWithoutRevert();
        __ddae2877ForceCloseLegacy();
        if (typeof cb === 'function'){
          try{ await cb(payload); }catch(_){ }
        }
        if (cfg && typeof cfg.apply === 'function' && changed.any){
          var ok = await __ddae2877CustomConfirm(String(cfg.message || 'Applicare le modifiche a tutti i tasti della stessa categoria?'));
          if (ok) await __ddae2877ApplyCategory(target, cfg, payload, changed);
        }
        try{ if (typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
        try{ if (typeof __headerActionApplyAll__ === 'function') __headerActionApplyAll__(); }catch(_){ }
        try{ if (typeof __launcherIconApplyAll__ === 'function') __launcherIconApplyAll__(); }catch(_){ }
        try{ if (typeof __setupSingleActionButtonPaletteBindings__ === 'function') __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
        try{ if (typeof __applySettingsEditorButtonBoldAndVisuals__ === 'function') __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
        try{ if (typeof renderRoomSettingsPage === 'function') renderRoomSettingsPage(); }catch(_){ }
        try{ if (typeof saveData === 'function') saveData(); }catch(_){ }
        try{ toast('Colore aggiornato'); }catch(_){ }
      }catch(_){
        __ddae2877ForceCloseLegacy();
      }finally{
        __ddae2877ConfirmBusy=false;
        __ddae2877PromptOpen=false;
        try{ setTimeout(__ddae2877ForceCloseLegacy,0); setTimeout(__ddae2877ForceCloseLegacy,160); setTimeout(__ddae2877ForceCloseLegacy,420); }catch(_){ }
      }
    };
    globalThis.__tagColorPopupConfirm__ = __tagColorPopupConfirm__;
  }catch(_){ __ddae2877ConfirmBusy=false; }

  try{
    if (!window.__ddae2877ConfirmCaptureBound__){
      window.__ddae2877ConfirmCaptureBound__ = true;
      ['pointerup','touchend','click'].forEach(function(evt){
        document.addEventListener(evt, function(ev){
          try{
            var btn = ev && ev.target && ev.target.closest ? ev.target.closest('#tagColorModalConfirm') : null;
            if (!btn) return;
            __ddae2877Stop(ev);
            __tagColorPopupConfirm__();
          }catch(_){ }
        }, true);
      });
    }
  }catch(_){ }

  try{ __confirmYesNoForceClose__ = __ddae2877ForceCloseLegacy; globalThis.__confirmYesNoForceClose__ = __ddae2877ForceCloseLegacy; }catch(_){ }
})();

/* dDAE_2.896 — Conferma colore: fix definitivo layer/touch con cattura coordinate */
(function(){
  var busy=false;
  var active=null;
  var lastTap=0;

  function stop(ev){
    try{ if(ev && ev.cancelable !== false) ev.preventDefault(); }catch(_){ }
    try{ if(ev) ev.stopPropagation(); }catch(_){ }
    try{ if(ev && ev.stopImmediatePropagation) ev.stopImmediatePropagation(); }catch(_){ }
  }
  function txt(s){
    try{ return (typeof __translateText__ === 'function') ? __translateText__(String(s||'')) : String(s||''); }catch(_){ return String(s||''); }
  }
  function remove(id){ try{ var n=document.getElementById(id); if(n && n.parentNode) n.parentNode.removeChild(n); }catch(_){ } }
  function isShown(el){ try{ return !!(el && !el.hidden && el.getAttribute('aria-hidden') !== 'true' && getComputedStyle(el).display !== 'none'); }catch(_){ return !!el; } }
  function hardCloseLegacy(){
    try{ remove('ddae2879ConfirmOverlay'); remove('ddae2877ConfirmOverlay'); remove('ddae870ConfirmOverlay'); }catch(_){ }
    try{
      var m=document.getElementById('confirmYesNoModal');
      if(m){
        m.hidden=true; m.setAttribute('hidden',''); m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.setProperty('display','none','important');
        m.style.setProperty('pointer-events','none','important');
      }
    }catch(_){ }
    try{ document.documentElement.classList.remove('confirm-yesno-open','ddae870-confirm-open','ddae2877-confirm-open','ddae2879-confirm-open'); }catch(_){ }
    try{ document.body.classList.remove('confirm-yesno-open','ddae870-confirm-open','ddae2877-confirm-open','ddae2879-confirm-open'); }catch(_){ }
    try{ if(typeof __confirmYesNoResolve !== 'undefined') __confirmYesNoResolve=null; }catch(_){ }
  }
  function lowerBlockingLayers(){
    try{
      ['tagColorModal','confirmYesNoModal','operatoriEditorModal','channelEditorModal','laundryCatalogEditorModal'].forEach(function(id){
        var el=document.getElementById(id);
        if(!el || id==='ddae2879ConfirmOverlay') return;
        if(id==='confirmYesNoModal'){
          el.hidden=true; el.setAttribute('hidden',''); el.setAttribute('aria-hidden','true');
          el.style.setProperty('display','none','important');
        }
        if(isShown(el)){
          el.style.setProperty('pointer-events','none','important');
          el.style.setProperty('touch-action','none','important');
        }
      });
    }catch(_){ }
  }
  function restoreBlockingLayers(){
    try{
      ['operatoriEditorModal','channelEditorModal','laundryCatalogEditorModal','tagColorModal'].forEach(function(id){
        var el=document.getElementById(id);
        if(el){ el.style.removeProperty('pointer-events'); el.style.removeProperty('touch-action'); }
      });
    }catch(_){ }
  }
  function point(ev){
    try{
      if(ev && ev.changedTouches && ev.changedTouches[0]) return {x:ev.changedTouches[0].clientX, y:ev.changedTouches[0].clientY};
      if(ev && ev.touches && ev.touches[0]) return {x:ev.touches[0].clientX, y:ev.touches[0].clientY};
      if(ev && typeof ev.clientX === 'number') return {x:ev.clientX, y:ev.clientY};
    }catch(_){ }
    return null;
  }
  function inRect(p, el){
    try{
      if(!p || !el) return false;
      var r=el.getBoundingClientRect();
      return p.x >= r.left && p.x <= r.right && p.y >= r.top && p.y <= r.bottom;
    }catch(_){ return false; }
  }
  function finish(val, ev){
    try{ stop(ev); }catch(_){ }
    if(!active || active.done) return;
    var a=active;
    a.done=true;
    active=null;
    try{ if(a.keepAlive) clearInterval(a.keepAlive); }catch(_){ }
    try{ remove('ddae2879ConfirmOverlay'); }catch(_){ }
    try{ document.documentElement.classList.remove('ddae2879-confirm-open'); document.body.classList.remove('ddae2879-confirm-open'); }catch(_){ }
    try{ restoreBlockingLayers(); }catch(_){ }
    try{ setTimeout(hardCloseLegacy,0); setTimeout(hardCloseLegacy,180); }catch(_){ }
    try{ a.resolve(!!val); }catch(_){ }
  }
  function capture(ev){
    try{
      if(!active || active.done) return;
      var overlay=document.getElementById('ddae2879ConfirmOverlay');
      if(!overlay) return;
      var p=point(ev);
      var yes=document.getElementById('ddae2879ConfirmYes');
      var no=document.getElementById('ddae2879ConfirmNo');
      var type=String(ev && ev.type || '');
      var release=/up|end|click/i.test(type);
      var target=ev && ev.target;
      var onYes=!!(target && target.closest && target.closest('#ddae2879ConfirmYes')) || inRect(p, yes);
      var onNo=!!(target && target.closest && target.closest('#ddae2879ConfirmNo')) || inRect(p, no);
      if(onYes || onNo){
        stop(ev);
        if(release) finish(onYes, ev);
        return;
      }
      stop(ev);
    }catch(_){ }
  }
  function customConfirm(message){
    return new Promise(function(resolve){
      try{
        if(active && !active.done){ finish(false); }
        hardCloseLegacy();
        lowerBlockingLayers();
        var overlay=document.createElement('div');
        overlay.id='ddae2879ConfirmOverlay';
        overlay.setAttribute('role','presentation');
        overlay.innerHTML='<div class="ddae2879-confirm-card" role="dialog" aria-modal="true" aria-label="Conferma"><div class="ddae2879-confirm-text"></div><div class="ddae2879-confirm-actions"><button id="ddae2879ConfirmYes" type="button">SÌ</button><button id="ddae2879ConfirmNo" type="button">NO</button></div></div>';
        var text=overlay.querySelector('.ddae2879-confirm-text');
        if(text) text.textContent=txt(message || 'Applicare le modifiche a tutti i tasti della stessa categoria?');
        document.body.appendChild(overlay);
        document.documentElement.classList.add('ddae2879-confirm-open');
        document.body.classList.add('ddae2879-confirm-open');
        active={ resolve:resolve, done:false, keepAlive:null };
        active.keepAlive=setInterval(function(){
          try{
            lowerBlockingLayers();
            var o=document.getElementById('ddae2879ConfirmOverlay');
            if(o && o.parentNode===document.body) document.body.appendChild(o);
          }catch(_){ }
        },120);
        ['pointerdown','pointerup','touchstart','touchend','mousedown','mouseup','click'].forEach(function(evt){
          try{ overlay.addEventListener(evt, capture, true); }catch(_){ }
        });
      }catch(_){ active=null; restoreBlockingLayers(); resolve(false); }
    });
  }
  function changed(payload){
    try{
      if(typeof __tagColorPopupChangedFields__ === 'function'){
        var c=__tagColorPopupChangedFields__(payload);
        if(c && typeof c === 'object'){
          if(c.any !== true && (c.bg || c.border || c.fg || c.opacity || c.bold)) c.any=true;
          return c;
        }
      }
    }catch(_){ }
    return {bg:true,border:true,fg:true,opacity:true,bold:false,any:true};
  }
  async function applyCategory(target,cfg,payload,ch){
    try{ if(cfg && typeof cfg.apply === 'function') await cfg.apply(payload, ch || {any:true,bg:true,border:true,fg:true,opacity:true}); }catch(_){ }
    try{ if(String(target||'')==='pill-single-button' && typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
  }
  function closeColorNoRevert(){
    try{ if(typeof __tagColorPopupState__ !== 'undefined' && __tagColorPopupState__) __tagColorPopupState__.confirmed=true; }catch(_){ }
    try{ if(typeof __tagColorPopupClose__ === 'function') __tagColorPopupClose__(); }catch(_){ }
    try{
      var m=document.getElementById('tagColorModal');
      if(m){
        m.hidden=true; m.setAttribute('hidden',''); m.setAttribute('aria-hidden','true');
        m.classList.remove('is-open','show','open','active');
        m.style.setProperty('display','none','important');
        m.style.setProperty('pointer-events','none','important');
      }
    }catch(_){ }
    try{ document.body.classList.remove('tag-color-modal-open'); }catch(_){ }
  }

  try{
    __tagColorPopupApplyCategoryIfNeeded__=async function(payload, applyCategoryCfg){
      if(busy) return;
      var cfg=(applyCategoryCfg && typeof applyCategoryCfg==='object') ? applyCategoryCfg : ((typeof __tagColorPopupState__ !== 'undefined' && __tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory==='object') ? __tagColorPopupState__.applyCategory : null);
      if(!cfg || typeof cfg.apply !== 'function') return;
      var ch=changed(payload);
      if(!ch.any) return;
      var ok=await customConfirm(String(cfg.message || 'Applicare le modifiche a tutti i tasti della stessa categoria?'));
      if(ok) await applyCategory((typeof __tagColorPopupState__ !== 'undefined' && __tagColorPopupState__ && __tagColorPopupState__.target) || '', cfg, payload, ch);
    };
    globalThis.__tagColorPopupApplyCategoryIfNeeded__=__tagColorPopupApplyCategoryIfNeeded__;
  }catch(_){ }

  try{
    __tagColorPopupConfirm__=async function(){
      var now=Date.now();
      if(busy) return;
      if(now-lastTap<550) return;
      lastTap=now;
      busy=true;
      var cb=null,cfg=null,payload=null,ch=null,target='';
      try{
        target=String((typeof __tagColorPopupState__ !== 'undefined' && __tagColorPopupState__ && __tagColorPopupState__.target) || '');
        cb=(typeof __tagColorPopupState__ !== 'undefined' && __tagColorPopupState__ && typeof __tagColorPopupState__.onSelect==='function') ? __tagColorPopupState__.onSelect : null;
        cfg=(typeof __tagColorPopupState__ !== 'undefined' && __tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory==='object') ? Object.assign({}, __tagColorPopupState__.applyCategory) : null;
        payload=(typeof __tagColorPopupCurrentPayload__ === 'function') ? __tagColorPopupCurrentPayload__() : {mode:'bg',spec:'blue-4',opacity:.80,colors:{bg:'blue-4',border:'blue-4',fg:'white'}};
        ch=changed(payload);
        closeColorNoRevert();
        hardCloseLegacy();
        if(typeof cb === 'function'){
          try{ await cb(payload); }catch(_){ }
        }
        if(cfg && typeof cfg.apply === 'function' && ch.any){
          var ok=await customConfirm(String(cfg.message || 'Applicare le modifiche a tutti i tasti della stessa categoria?'));
          if(ok) await applyCategory(target,cfg,payload,ch);
        }
        try{ if(typeof __pillApplyAll__ === 'function') __pillApplyAll__(); }catch(_){ }
        try{ if(typeof __headerActionApplyAll__ === 'function') __headerActionApplyAll__(); }catch(_){ }
        try{ if(typeof __launcherIconApplyAll__ === 'function') __launcherIconApplyAll__(); }catch(_){ }
        try{ if(typeof __setupSingleActionButtonPaletteBindings__ === 'function') __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
        try{ if(typeof __applySettingsEditorButtonBoldAndVisuals__ === 'function') __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
        try{ if(typeof renderRoomSettingsPage === 'function') renderRoomSettingsPage(); }catch(_){ }
        try{ if(typeof saveData === 'function') saveData(); }catch(_){ }
        try{ toast('Colore aggiornato'); }catch(_){ }
      }catch(_){
        hardCloseLegacy();
      }finally{
        busy=false;
        try{ restoreBlockingLayers(); }catch(_){ }
        try{ setTimeout(hardCloseLegacy,0); setTimeout(hardCloseLegacy,160); setTimeout(hardCloseLegacy,420); }catch(_){ }
      }
    };
    globalThis.__tagColorPopupConfirm__=__tagColorPopupConfirm__;
  }catch(_){ busy=false; }

  try{
    if(!window.__ddae2879GlobalCaptureBound__){
      window.__ddae2879GlobalCaptureBound__=true;
      ['pointerdown','pointerup','touchstart','touchend','mousedown','mouseup','click'].forEach(function(evt){
        document.addEventListener(evt, function(ev){
          try{
            if(active && !active.done){ capture(ev); return; }
            var btn=ev && ev.target && ev.target.closest ? ev.target.closest('#tagColorModalConfirm') : null;
            if(!btn) return;
            stop(ev);
            __tagColorPopupConfirm__();
          }catch(_){ }
        }, true);
      });
    }
  }catch(_){ }
  try{ __confirmYesNoForceClose__=hardCloseLegacy; globalThis.__confirmYesNoForceClose__=hardCloseLegacy; }catch(_){ }
})();

/* dDAE_2.896 — Popup colore Impostazioni: conferma isolata su layer unico con cattura window */
(function(){
  var BUILD_TAG='dDAE_2.914';
  var busy=false;
  var lastStart=0;
  var active=null;

  function t(s){ try{ return (typeof __translateText__==='function') ? __translateText__(String(s||'')) : String(s||''); }catch(_){ return String(s||''); } }
  function stop(ev){
    try{ if(ev && ev.cancelable !== false) ev.preventDefault(); }catch(_){ }
    try{ if(ev) ev.stopPropagation(); }catch(_){ }
    try{ if(ev && ev.stopImmediatePropagation) ev.stopImmediatePropagation(); }catch(_){ }
  }
  function removeEl(id){ try{ var el=document.getElementById(id); if(el && el.parentNode) el.parentNode.removeChild(el); }catch(_){ } }
  function hardHide(id){
    try{
      var el=document.getElementById(id);
      if(!el) return;
      el.hidden=true;
      el.setAttribute('hidden','');
      el.setAttribute('aria-hidden','true');
      el.classList.remove('is-open','show','open','active');
      el.style.setProperty('display','none','important');
      el.style.setProperty('pointer-events','none','important');
      el.style.setProperty('touch-action','none','important');
    }catch(_){ }
  }
  function unlock(){
    try{ document.documentElement.classList.remove('ddae2879-confirm-open','confirm-yesno-open','tag-color-modal-open','ddae2880-confirm-open'); }catch(_){ }
    try{ document.body.classList.remove('ddae2879-confirm-open','confirm-yesno-open','tag-color-modal-open','ddae2880-confirm-open'); }catch(_){ }
    try{ document.querySelectorAll('#tagColorModal,#confirmYesNoModal,#operatoriEditorModal,#channelEditorModal,#laundryCatalogEditorModal,.modal,.overlay,.modal-backdrop').forEach(function(el){
      try{ el.style.removeProperty('touch-action'); }catch(_){ }
      try{ if(el.id!=='ddae2880ConfirmOverlay') el.style.removeProperty('pointer-events'); }catch(_){ }
    }); }catch(_){ }
  }
  function point(ev){
    try{
      var x=(ev && ev.changedTouches && ev.changedTouches[0]) || (ev && ev.touches && ev.touches[0]);
      if(x) return {x:x.clientX,y:x.clientY};
      if(ev && typeof ev.clientX==='number') return {x:ev.clientX,y:ev.clientY};
    }catch(_){ }
    return null;
  }
  function inside(el,p){
    try{ if(!el || !p) return false; var r=el.getBoundingClientRect(); return p.x>=r.left && p.x<=r.right && p.y>=r.top && p.y<=r.bottom; }catch(_){ return false; }
  }
  function closeAllConfirmationLayers(){
    try{ removeEl('ddae2880ConfirmOverlay'); }catch(_){ }
    try{ removeEl('ddae2879ConfirmOverlay'); }catch(_){ }
    try{ hardHide('confirmYesNoModal'); }catch(_){ }
    try{ if(typeof __confirmYesNoResolve!=='undefined') __confirmYesNoResolve=null; }catch(_){ }
    try{ if(typeof __confirmYesNoReadyAt__!=='undefined') __confirmYesNoReadyAt__=0; }catch(_){ }
    try{ if(typeof __confirmYesNoSuppressUntil__!=='undefined') __confirmYesNoSuppressUntil__=0; }catch(_){ }
  }
  function finish(val,ev){
    try{ stop(ev); }catch(_){ }
    if(!active || active.done) return;
    var a=active;
    a.done=true;
    active=null;
    try{ if(a.timer) clearInterval(a.timer); }catch(_){ }
    closeAllConfirmationLayers();
    unlock();
    try{ a.resolve(!!val); }catch(_){ }
  }
  function globalCapture(ev){
    try{
      if(!active || active.done) return;
      var p=point(ev);
      var yes=document.getElementById('ddae2880ConfirmYes');
      var no=document.getElementById('ddae2880ConfirmNo');
      var release=/up|end|click/i.test(String(ev && ev.type || ''));
      var onYes=inside(yes,p);
      var onNo=inside(no,p);
      stop(ev);
      if((onYes || onNo) && release){ finish(onYes,ev); }
    }catch(_){ stop(ev); }
  }
  function installGlobalCapture(){
    if(window.__ddae2880CaptureInstalled__) return;
    window.__ddae2880CaptureInstalled__=true;
    ['pointerdown','pointerup','touchstart','touchend','mousedown','mouseup','click'].forEach(function(evt){
      try{ window.addEventListener(evt, globalCapture, true); }catch(_){ }
      try{ document.addEventListener(evt, function(ev){ if(active && !active.done) globalCapture(ev); }, true); }catch(_){ }
    });
  }
  function lockOtherLayers(){
    try{
      document.querySelectorAll('#tagColorModal,#confirmYesNoModal,#operatoriEditorModal,#channelEditorModal,#laundryCatalogEditorModal,.modal,.overlay,.modal-backdrop').forEach(function(el){
        if(!el || el.id==='ddae2880ConfirmOverlay') return;
        el.style.setProperty('pointer-events','none','important');
        el.style.setProperty('touch-action','none','important');
      });
    }catch(_){ }
  }
  function showConfirm(message){
    return new Promise(function(resolve){
      try{
        installGlobalCapture();
        if(active && !active.done) finish(false);
        closeAllConfirmationLayers();
        lockOtherLayers();
        document.documentElement.classList.add('ddae2880-confirm-open');
        document.body.classList.add('ddae2880-confirm-open');
        var overlay=document.createElement('div');
        overlay.id='ddae2880ConfirmOverlay';
        overlay.setAttribute('data-build',BUILD_TAG);
        overlay.setAttribute('role','presentation');
        overlay.style.cssText='position:fixed!important;inset:0!important;width:100vw!important;height:100vh!important;z-index:2147483647!important;display:flex!important;align-items:center!important;justify-content:center!important;padding:18px!important;background:rgba(0,0,0,.38)!important;pointer-events:auto!important;touch-action:none!important;isolation:isolate!important;';
        var card=document.createElement('div');
        card.setAttribute('role','dialog');
        card.setAttribute('aria-modal','true');
        card.style.cssText='position:relative!important;z-index:2147483647!important;width:min(360px,calc(100vw - 36px))!important;background:#fff!important;color:#111827!important;border:1px solid rgba(0,0,0,.14)!important;border-radius:20px!important;padding:20px!important;box-shadow:none!important;pointer-events:auto!important;touch-action:manipulation!important;';
        var text=document.createElement('div');
        text.textContent=t(message || 'Applicare le modifiche a tutti i tasti della stessa categoria?');
        text.style.cssText='margin:0 0 18px!important;text-align:center!important;font-size:17px!important;line-height:1.25!important;font-weight:900!important;';
        var actions=document.createElement('div');
        actions.style.cssText='display:grid!important;grid-template-columns:1fr 1fr!important;gap:14px!important;';
        var yes=document.createElement('button');
        yes.id='ddae2880ConfirmYes'; yes.type='button'; yes.textContent='SÌ';
        yes.style.cssText='width:100%!important;min-height:50px!important;border:0!important;border-radius:12px!important;background:#188038!important;color:#fff!important;font-size:17px!important;font-weight:900!important;box-shadow:none!important;text-transform:uppercase!important;pointer-events:auto!important;touch-action:manipulation!important;-webkit-tap-highlight-color:transparent!important;';
        var no=document.createElement('button');
        no.id='ddae2880ConfirmNo'; no.type='button'; no.textContent='NO';
        no.style.cssText='width:100%!important;min-height:50px!important;border:0!important;border-radius:12px!important;background:#c5221f!important;color:#fff!important;font-size:17px!important;font-weight:900!important;box-shadow:none!important;text-transform:uppercase!important;pointer-events:auto!important;touch-action:manipulation!important;-webkit-tap-highlight-color:transparent!important;';
        actions.appendChild(yes); actions.appendChild(no); card.appendChild(text); card.appendChild(actions); overlay.appendChild(card); document.body.appendChild(overlay);
        active={resolve:resolve,done:false,timer:null};
        yes.addEventListener('click',function(ev){ finish(true,ev); },true);
        no.addEventListener('click',function(ev){ finish(false,ev); },true);
        overlay.addEventListener('click',function(ev){ if(ev && ev.target===overlay) finish(false,ev); },true);
        active.timer=setInterval(function(){
          try{ lockOtherLayers(); var o=document.getElementById('ddae2880ConfirmOverlay'); if(o && o.parentNode===document.body) document.body.appendChild(o); }catch(_){ }
        },80);
      }catch(_){ unlock(); resolve(false); }
    });
  }
  function closeColorPopupKeepSelection(){
    try{ if(typeof __tagColorPopupState__!=='undefined' && __tagColorPopupState__) __tagColorPopupState__.confirmed=true; }catch(_){ }
    try{ if(typeof __tagColorPopupClose__==='function') __tagColorPopupClose__(); }catch(_){ }
    try{ hardHide('tagColorModal'); }catch(_){ }
    try{ document.body.classList.remove('tag-color-modal-open'); document.documentElement.classList.remove('tag-color-modal-open'); }catch(_){ }
  }
  function changed(payload){
    try{ if(typeof __tagColorPopupChangedFields__==='function'){ var c=__tagColorPopupChangedFields__(payload); if(c && typeof c==='object'){ if(c.any!==true && (c.bg||c.border||c.fg||c.opacity||c.bold)) c.any=true; return c; } } }catch(_){ }
    return {any:true,bg:true,border:true,fg:true,opacity:true,bold:false};
  }
  async function applyCfg(target,cfg,payload,ch){
    try{ if(cfg && typeof cfg.apply==='function') await cfg.apply(payload,ch||{any:true,bg:true,border:true,fg:true,opacity:true}); }catch(_){ }
    try{ if(String(target||'')==='pill-single-button' && typeof __pillApplyAll__==='function') __pillApplyAll__(); }catch(_){ }
  }
  async function confirmColor(){
    var now=Date.now();
    if(busy || (now-lastStart)<650) return;
    lastStart=now; busy=true;
    var target='',cb=null,cfg=null,payload=null,ch=null;
    try{
      target=String((typeof __tagColorPopupState__!=='undefined' && __tagColorPopupState__ && __tagColorPopupState__.target) || '');
      cb=(typeof __tagColorPopupState__!=='undefined' && __tagColorPopupState__ && typeof __tagColorPopupState__.onSelect==='function') ? __tagColorPopupState__.onSelect : null;
      cfg=(typeof __tagColorPopupState__!=='undefined' && __tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory==='object') ? Object.assign({},__tagColorPopupState__.applyCategory) : null;
      payload=(typeof __tagColorPopupCurrentPayload__==='function') ? __tagColorPopupCurrentPayload__() : {mode:'bg',spec:'blue-4',opacity:.8,colors:{bg:'blue-4',border:'blue-4',fg:'white'}};
      ch=changed(payload);
      closeColorPopupKeepSelection();
      closeAllConfirmationLayers();
      if(typeof cb==='function'){ try{ await cb(payload); }catch(_){ } }
      if(cfg && typeof cfg.apply==='function' && ch && ch.any){
        var ok=await showConfirm(String(cfg.message || 'Applicare le modifiche a tutti i tasti della stessa categoria?'));
        if(ok) await applyCfg(target,cfg,payload,ch);
      }
      try{ if(typeof __pillApplyAll__==='function') __pillApplyAll__(); }catch(_){ }
      try{ if(typeof __headerActionApplyAll__==='function') __headerActionApplyAll__(); }catch(_){ }
      try{ if(typeof __launcherIconApplyAll__==='function') __launcherIconApplyAll__(); }catch(_){ }
      try{ if(typeof __setupSingleActionButtonPaletteBindings__==='function') __setupSingleActionButtonPaletteBindings__(); }catch(_){ }
      try{ if(typeof __applySettingsEditorButtonBoldAndVisuals__==='function') __applySettingsEditorButtonBoldAndVisuals__(); }catch(_){ }
      try{ if(typeof renderRoomSettingsPage==='function') renderRoomSettingsPage(); }catch(_){ }
      try{ if(typeof saveData==='function') saveData(); }catch(_){ }
      try{ toast('Colore aggiornato'); }catch(_){ }
    }catch(_){
      closeAllConfirmationLayers(); unlock();
    }finally{
      busy=false;
      try{ closeAllConfirmationLayers(); unlock(); }catch(_){ }
    }
  }
  try{ __confirmYesNoForceClose__=function(){ closeAllConfirmationLayers(); unlock(); }; globalThis.__confirmYesNoForceClose__=__confirmYesNoForceClose__; }catch(_){ }
  try{ confirmYesNo=showConfirm; globalThis.confirmYesNo=showConfirm; }catch(_){ }
  try{ __tagColorPopupApplyCategoryIfNeeded__=async function(payload,applyCategoryCfg){ var cfg=(applyCategoryCfg && typeof applyCategoryCfg==='object') ? applyCategoryCfg : ((typeof __tagColorPopupState__!=='undefined' && __tagColorPopupState__ && __tagColorPopupState__.applyCategory && typeof __tagColorPopupState__.applyCategory==='object') ? __tagColorPopupState__.applyCategory : null); if(!cfg || typeof cfg.apply!=='function') return; var ch=changed(payload); if(!ch.any) return; var ok=await showConfirm(String(cfg.message || 'Applicare le modifiche a tutti i tasti della stessa categoria?')); if(ok) await applyCfg((typeof __tagColorPopupState__!=='undefined' && __tagColorPopupState__ && __tagColorPopupState__.target)||'',cfg,payload,ch); }; globalThis.__tagColorPopupApplyCategoryIfNeeded__=__tagColorPopupApplyCategoryIfNeeded__; }catch(_){ }
  try{ __tagColorPopupConfirm__=confirmColor; globalThis.__tagColorPopupConfirm__=confirmColor; }catch(_){ }
  try{
    if(!window.__ddae2880TagConfirmCapture__){
      window.__ddae2880TagConfirmCapture__=true;
      ['pointerdown','pointerup','touchstart','touchend','mousedown','mouseup','click'].forEach(function(evt){
        window.addEventListener(evt,function(ev){
          try{
            if(active && !active.done) return;
            var btn=ev && ev.target && ev.target.closest ? ev.target.closest('#tagColorModalConfirm') : null;
            if(!btn) return;
            stop(ev);
            confirmColor();
          }catch(_){ }
        },true);
      });
    }
  }catch(_){ }
})();


/* dDAE_2.896 — Scheda ospite: ripristino pill Channel selezionato */
(function(){
  function currentGuestRecord(){
    try{
      const activeId = String(state && state.guestGroupActiveId || '').trim();
      if (activeId && typeof __guestBookingById__ === 'function'){
        const b = __guestBookingById__(activeId);
        if (b) return b;
      }
      return (state && (state.guestViewItem || state.guestEditSourceItem)) || null;
    }catch(_){ return null; }
  }
  function readChannelId(item){
    try{ return String((item && (item.channel_id ?? item.channelId ?? item.channel ?? item.channel_id_ref)) ?? '').trim(); }catch(_){ return ''; }
  }
  function readChannelName(item){
    try{ return String((item && (item.channel_nome ?? item.channelName ?? item.nome_channel ?? item.channel_label ?? item.channel)) ?? '').trim(); }catch(_){ return ''; }
  }
  function setShown(el, show){
    try{
      if (!el) return;
      el.hidden = !show;
      if (show){
        el.removeAttribute('hidden');
        el.style.removeProperty('display');
        el.style.removeProperty('visibility');
        el.style.removeProperty('opacity');
        el.style.removeProperty('pointer-events');
      } else {
        el.setAttribute('hidden','');
        el.style.setProperty('display','none','important');
      }
    }catch(_){ }
  }
  function restoreGuestChannelPill(){
    try{
      const page = document.getElementById('page-ospite');
      if (!page || page.hidden) return;
      const mode = String(state && state.guestMode || '').toLowerCase();
      if (mode !== 'view') return;
      const item = currentGuestRecord();
      const row = document.getElementById('guestChannelRow');
      const sel = document.getElementById('guestChannel');
      const booking = document.getElementById('guestBooking');
      if (!row || !sel || !booking) return;

      const id = readChannelId(item) || String(sel.value || '').trim();
      const name = readChannelName(item);
      try{ populateGuestChannelOptions(id || sel.value || ''); }catch(_){ }
      if (id){
        try{
          if (![...sel.options].some(o => String(o.value) === id)){
            const opt = document.createElement('option');
            opt.value = id;
            opt.textContent = name || id;
            sel.appendChild(opt);
          }
          sel.value = id;
        }catch(_){ }
      } else if (name && !String(sel.value || '').trim()){
        try{
          const opt = document.createElement('option');
          opt.value = name;
          opt.textContent = name;
          sel.appendChild(opt);
          sel.value = name;
        }catch(_){ }
      }
      try{ applySelectedChannelToGuestForm(sel.value || id || '', { preserveManual:true }); }catch(_){ }

      const card = document.querySelector('#page-ospite .guest-form-card');
      const groupInfoOpen = !!(card && card.classList && card.classList.contains('guest-group-info-open') && state && String(state.guestGroupActiveId || '').trim());
      if (!groupInfoOpen){
        setShown(row, false);
        return;
      }
      setShown(row, true);
      const bookingSub = booking.closest('.subfield') || booking.closest('.field');
      const channelSub = sel.closest('.subfield') || sel.closest('.field');
      if (bookingSub && bookingSub !== row) setShown(bookingSub, true);
      if (channelSub && channelSub !== row) setShown(channelSub, true);
      const commWrap = document.getElementById('guestChannelCommissionWrap');
      if (commWrap) setShown(commWrap, false);
      try{ sel.disabled = false; sel.readOnly = true; sel.setAttribute('aria-readonly','true'); }catch(_){ }
      try{ if (typeof refreshFloatingLabels === 'function') refreshFloatingLabels(); }catch(_){ }
    }catch(_){ }
  }
  function after(){
    try{ restoreGuestChannelPill(); setTimeout(restoreGuestChannelPill,0); setTimeout(restoreGuestChannelPill,120); setTimeout(restoreGuestChannelPill,360); }catch(_){ }
  }
  ['enterGuestViewMode','setGuestFormViewOnly','__populateGuestGroupInfoFromBooking__','__setGuestGroupInfoVisible__','renderGuestMulti','updateOspiteHdActions'].forEach(function(name){
    try{
      const fn = window[name] || (typeof globalThis !== 'undefined' ? globalThis[name] : null);
      if (typeof fn === 'function' && !fn.__ddae2881ChannelPillFix){
        const wrapped = function(){ const out = fn.apply(this, arguments); after(); return out; };
        wrapped.__ddae2881ChannelPillFix = true;
        window[name] = wrapped;
        try{ globalThis[name] = wrapped; }catch(_){ }
      }
    }catch(_){ }
  });
  try{ document.addEventListener('click', function(){ setTimeout(restoreGuestChannelPill, 0); setTimeout(restoreGuestChannelPill, 160); }, true); }catch(_){ }
  try{ window.__ddae2881RestoreGuestChannelPill = restoreGuestChannelPill; }catch(_){ }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', after, {once:true}); else after();
  try{ window.addEventListener('pageshow', after, {passive:true}); }catch(_){ }
})();



/* dDAE_2.896 — Range date: tema semplificato in 3 categorie + tema/backup */
(function(){
  'use strict';
  var PARTS_KEY = 'dDAE_date_range_calendar_parts_v1';
  var PART_DEFAULTS = {
    fill:{ bg:'sky-6', border:'sky-6', fg:'gray-1', opacity:1 },
    numbers:{ bg:'gray-1', border:'gray-1', fg:'gray-6', opacity:0 },
    lines:{ bg:'gray-1', border:'gray-3', fg:'gray-6', opacity:0.55 }
  };
  var PART_LABELS = {
    fill:'Calendario - riempimenti tasti e pallini',
    numbers:'Calendario - testo numeri',
    lines:'Calendario - contorni e linee'
  };
  var OLD_PARTS = ['modal','title','checkin','checkout','month','nav','weekdays','days','muteddays','rangedays','selecteddays','cancel','apply'];
  function norm(v, part){
    var d = PART_DEFAULTS[part] || PART_DEFAULTS.fill;
    try{ if (typeof __launcherVisualNormalize__ === 'function') return __launcherVisualNormalize__(v || d, d.fg || 'gray-6'); }catch(_){ }
    v = (v && typeof v === 'object') ? v : {};
    return { bg:v.bg || d.bg, border:v.border || d.border || v.bg || d.bg, fg:v.fg || d.fg, opacity: typeof v.opacity === 'number' ? v.opacity : d.opacity };
  }
  function compact(input){
    input = (input && typeof input === 'object') ? input : {};
    if (input.fill || input.numbers || input.lines){
      return { fill:norm(input.fill,'fill'), numbers:norm(input.numbers,'numbers'), lines:norm(input.lines,'lines') };
    }
    var out = {
      fill:norm(input.selecteddays || input.apply || input.rangedays || null, 'fill'),
      numbers:norm(input.days || input.weekdays || input.muteddays || null, 'numbers'),
      lines:norm(input.modal || input.checkin || input.checkout || input.nav || null, 'lines')
    };
    return out;
  }
  function read(){
    var raw = {};
    try{ raw = JSON.parse(localStorage.getItem(PARTS_KEY) || '{}') || {}; }catch(_){ raw = {}; }
    return compact(raw);
  }
  function write(map){
    try{ localStorage.setItem(PARTS_KEY, JSON.stringify(compact(map))); }catch(_){ }
  }
  function rgba(hex, op){ try{ if (typeof hexToRgba === 'function') return hexToRgba(hex, op); }catch(_){ } return hex || ''; }
  function color(spec){ try{ if (typeof __operatoreColorHex__ === 'function') return __operatoreColorHex__(spec || 'gray-1'); }catch(_){ } return spec || '#ffffff'; }
  function op(v, fallback){
    try{ if (typeof __designBgOpacityNormalize__ === 'function') return __designBgOpacityNormalize__(v && v.opacity != null ? v.opacity : fallback); }catch(_){ }
    var n = Number(v && v.opacity != null ? v.opacity : fallback); return isFinite(n) ? Math.max(0, Math.min(1, n)) : 1;
  }
  function readable(fill){
    try{ return __tagColorTextHex__(fill.bg || 'sky-6', fill.fg || 'gray-1', false) || color(fill.fg || 'gray-1'); }catch(_){ return color(fill.fg || 'gray-1'); }
  }
  function setVar(n,v){ try{ document.documentElement.style.setProperty(n, String(v == null ? '' : v)); }catch(_){ } }
  function applyParts(){
    try{
      var map = read();
      var fill = map.fill, nums = map.numbers, lines = map.lines;
      var fillBg = color(fill.bg), fillFg = readable(fill), numFg = color(nums.fg), line = color(lines.border || lines.bg || 'gray-3');
      var fillOp = Math.max(0.08, op(fill, 1));
      var lineOp = Math.max(0.12, op(lines, 0.55));
      var lineCss = rgba(line, lineOp);
      var fillCss = rgba(fillBg, fillOp);
      var fillSoft = rgba(fillBg, Math.max(0.10, Math.min(0.36, fillOp * 0.34)));
      var fillMid = rgba(fillBg, Math.max(0.18, Math.min(0.55, fillOp * 0.52)));
      var numMuted = rgba(numFg, 0.42);

      setVar('--ddae-range-part-modal-bg', 'var(--ddae-range-modal-bg, rgba(255,255,255,.98))');
      setVar('--ddae-range-part-modal-border', lineCss);
      setVar('--ddae-range-part-modal-fg', 'var(--ddae-range-modal-text, #111827)');
      setVar('--ddae-range-part-title-fg', 'var(--ddae-range-modal-text, #111827)');
      ['checkin','checkout'].forEach(function(k){
        setVar('--ddae-range-part-' + k + '-bg', 'var(--ddae-range-field-bg, rgba(255,255,255,.94))');
        setVar('--ddae-range-part-' + k + '-border', lineCss);
        setVar('--ddae-range-part-' + k + '-fg', 'var(--ddae-range-modal-text, #111827)');
      });
      setVar('--ddae-range-part-month-fg', 'var(--ddae-range-modal-text, #111827)');
      setVar('--ddae-range-part-nav-bg', fillSoft);
      setVar('--ddae-range-part-nav-border', lineCss);
      setVar('--ddae-range-part-nav-fg', fillBg);
      setVar('--ddae-range-part-weekdays-fg', numMuted);
      setVar('--ddae-range-part-days-bg', 'transparent');
      setVar('--ddae-range-part-days-border', 'transparent');
      setVar('--ddae-range-part-days-fg', numFg);
      setVar('--ddae-range-part-muteddays-fg', numMuted);
      setVar('--ddae-range-part-rangedays-bg', fillSoft);
      setVar('--ddae-range-part-rangedays-border', lineCss);
      setVar('--ddae-range-part-rangedays-fg', numFg);
      setVar('--ddae-range-part-selecteddays-bg', fillCss);
      setVar('--ddae-range-part-selecteddays-border', lineCss);
      setVar('--ddae-range-part-selecteddays-fg', fillFg);
      setVar('--ddae-range-part-cancel-bg', fillSoft);
      setVar('--ddae-range-part-cancel-border', lineCss);
      setVar('--ddae-range-part-cancel-fg', fillBg);
      setVar('--ddae-range-part-apply-bg', fillCss);
      setVar('--ddae-range-part-apply-border', lineCss);
      setVar('--ddae-range-part-apply-fg', fillFg);
      setVar('--ddae-range-field-border', lineCss);
      setVar('--ddae-range-field-muted-border', lineCss);
      setVar('--ddae-range-action-apply-bg', fillCss);
      setVar('--ddae-range-action-apply-text', fillFg);
      setVar('--ddae-range-action-cancel-bg', fillSoft);
      setVar('--ddae-range-action-cancel-text', fillBg);
      setVar('--ddae-range-day-selected-bg', fillCss);
      setVar('--ddae-range-day-selected-border', lineCss);
      setVar('--ddae-range-day-selected-text', fillFg);
      setVar('--ddae-range-day-range-bg', fillSoft);
      setVar('--ddae-range-day-range-border', lineCss);
      setVar('--ddae-range-day-range-text', numFg);
      setVar('--ddae-range-day-text', numFg);
      setVar('--ddae-range-day-muted', numMuted);
      setVar('--ddae-range-nav-bg', fillSoft);
      setVar('--ddae-range-nav-border', lineCss);
      setVar('--ddae-range-modal-border', lineCss);
    }catch(_){ }
  }
  var oldApply = (typeof __dateRangeCalendarApplyTheme__ === 'function') ? __dateRangeCalendarApplyTheme__ : null;
  if (oldApply){
    __dateRangeCalendarApplyTheme__ = function(){ try{ oldApply.apply(this, arguments); }catch(_){ } try{ applyParts(); }catch(_){ } };
  }
  function refreshCalendars(){
    try{ applyParts(); }catch(_){ }
    try{ __renderGuestDateRangeCalendar__(); }catch(_){ }
    try{ __renderLaundryDateRangeCalendar__(); }catch(_){ }
    try{ __renderSpeseDateRangeCalendar__(); }catch(_){ }
    try{ __renderLicenseDateRangeCalendar__(); }catch(_){ }
  }
  function openCategory(category){
    try{
      if (!PART_DEFAULTS[category]) category = 'fill';
      var before = read();
      var current = before[category] || norm(null, category);
      var opts = {
        supportsBg: category === 'fill',
        supportsBorder: category === 'lines',
        supportsFg: category === 'numbers',
        supportsOpacity: category !== 'numbers',
        opacity: current.opacity,
        defaultMode: category === 'fill' ? 'bg' : (category === 'lines' ? 'border' : 'fg'),
        fallbackBg: current.bg || 'gray-1',
        onPreview:function(payload, changed){ apply(payload, changed); },
        onRevert:function(){ write(before); refreshCalendars(); }
      };
      function apply(payload, changed){
        var now = read();
        var flags = changed || (category === 'fill' ? {bg:true,opacity:true} : (category === 'lines' ? {border:true,opacity:true} : {fg:true}));
        var next;
        try{ next = __applyDesignPayloadToVisual__(now[category] || current, payload, flags, current.bg || 'gray-1'); }
        catch(_){ next = payload || current; }
        now[category] = norm(next, category);
        write(now);
        refreshCalendars();
      }
      try{ if (typeof toast === 'function') toast(PART_LABELS[category] || 'Calendario'); }catch(_){ }
      __tagColorPopupOpen__('date-range-calendar-' + category, current, function(payload){ apply(payload); }, opts);
    }catch(_){ }
  }
  function categoryFromTarget(t){
    try{
      if (!t || !t.closest) return 'lines';
      if (t.closest('.guest-date-range-actions .btn, .guest-date-range-nav')) return 'fill';
      var day = t.closest('.guest-date-range-day');
      if (day){
        if (day.classList.contains('is-selected') || day.classList.contains('is-edge') || day.classList.contains('is-in-range')) return 'fill';
        return 'numbers';
      }
      if (t.closest('.guest-date-range-weekdays')) return 'numbers';
      return 'lines';
    }catch(_){ return 'lines'; }
  }
  function markModal(modal){
    try{
      if (!modal) return;
      modal.querySelectorAll('.guest-date-range-actions .btn, .guest-date-range-nav, .guest-date-range-day.is-selected, .guest-date-range-day.is-edge, .guest-date-range-day.is-in-range').forEach(function(el){ el.setAttribute('data-range-theme-part','fill'); });
      modal.querySelectorAll('.guest-date-range-day:not(.is-selected):not(.is-edge):not(.is-in-range), .guest-date-range-weekdays span').forEach(function(el){ el.setAttribute('data-range-theme-part','numbers'); });
      modal.querySelectorAll('.guest-date-range-modal-card, .guest-date-range-top-field, .guest-date-range-monthtitle, .guest-date-range-modal-range-title').forEach(function(el){ el.setAttribute('data-range-theme-part','lines'); });
    }catch(_){ }
  }
  function markAll(){ ['guestDateRangeModal','laundryDateRangeModal','speseDateRangeModal','licenseDateRangeModal'].forEach(function(id){ markModal(document.getElementById(id)); }); }
  ['__renderGuestDateRangeCalendar__','__renderLaundryDateRangeCalendar__','__renderSpeseDateRangeCalendar__','__renderLicenseDateRangeCalendar__'].forEach(function(name){
    try{
      var old = (typeof window !== 'undefined' ? window[name] : eval(name));
      if (typeof old === 'function'){
        window[name] = function(){ var r = old.apply(this, arguments); try{ markAll(); applyParts(); }catch(_){ } return r; };
        try{ eval(name + ' = window["' + name + '"]'); }catch(_){ }
      }
    }catch(_){ }
  });
  var downTimer = null, fired = false;
  function isRangeTarget(e){ try{ return e && e.target && e.target.closest && e.target.closest('.guest-date-range-modal-card'); }catch(_){ return false; } }
  function block(e){ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } try{ e.stopImmediatePropagation(); }catch(_){ } }
  document.addEventListener('pointerdown', function(e){
    if (!isRangeTarget(e)) return;
    try{ if (e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    fired = false; clearTimeout(downTimer);
    var category = categoryFromTarget(e.target);
    downTimer = setTimeout(function(){ fired = true; openCategory(category); }, 620);
  }, true);
  ['pointerup','pointercancel','pointerleave','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach(function(evt){
    document.addEventListener(evt, function(e){ if (downTimer){ clearTimeout(downTimer); downTimer = null; } if (fired && isRangeTarget(e)){ block(e); setTimeout(function(){ fired=false; }, 0); } }, true);
  });
  document.addEventListener('contextmenu', function(e){ if (!isRangeTarget(e)) return; block(e); openCategory(categoryFromTarget(e.target)); }, true);
  document.addEventListener('DOMContentLoaded', function(){ try{ var c=compact(read()); write(c); markAll(); applyParts(); }catch(_){ } });
  setTimeout(function(){ try{ var c=compact(read()); write(c); markAll(); applyParts(); }catch(_){ } }, 0);
  try{
    var oldBuild = (typeof __roomSettingsThemePayloadBuild__ === 'function') ? __roomSettingsThemePayloadBuild__ : null;
    if (oldBuild){
      __roomSettingsThemePayloadBuild__ = function(){
        var p = oldBuild.apply(this, arguments) || {};
        p.dateRangeCalendarParts = read();
        try{ p.statsThemeStorage = p.statsThemeStorage || {}; p.statsThemeStorage[PARTS_KEY] = JSON.stringify(read()); }catch(_){ }
        return p;
      };
    }
  }catch(_){ }
  try{
    var oldNorm = (typeof __roomSettingsThemePayloadNormalize__ === 'function') ? __roomSettingsThemePayloadNormalize__ : null;
    if (oldNorm){
      __roomSettingsThemePayloadNormalize__ = function(payload){
        var p = oldNorm.apply(this, arguments) || {};
        var src = (payload && typeof payload === 'object') ? payload : {};
        var parts = src.dateRangeCalendarParts || src.calendarRangeParts || null;
        try{ if (!parts && src.statsThemeStorage && src.statsThemeStorage[PARTS_KEY]) parts = JSON.parse(src.statsThemeStorage[PARTS_KEY]); }catch(_){ }
        p.dateRangeCalendarParts = compact(parts);
        try{ p.statsThemeStorage = p.statsThemeStorage || {}; p.statsThemeStorage[PARTS_KEY] = JSON.stringify(p.dateRangeCalendarParts); }catch(_){ }
        return p;
      };
    }
  }catch(_){ }
  try{
    var oldApplySlot = (typeof __roomSettingsThemeSlotApply__ === 'function') ? __roomSettingsThemeSlotApply__ : null;
    if (oldApplySlot){
      __roomSettingsThemeSlotApply__ = async function(slot){
        var key = String(Math.max(1, Math.min(4, parseInt(slot, 10) || 0)));
        var payload = null;
        try{ payload = __roomSettingsThemeSlotGet__(key); }catch(_){ }
        var res = await oldApplySlot.apply(this, arguments);
        try{ if (payload && payload.dateRangeCalendarParts) write(payload.dateRangeCalendarParts); refreshCalendars(); }catch(_){ }
        return res;
      };
    }
  }catch(_){ }
  try{
    var oldImport = (typeof importThemeFromFile === 'function') ? importThemeFromFile : null;
    if (oldImport){ importThemeFromFile = async function(){ var r = await oldImport.apply(this, arguments); try{ write(read()); applyParts(); }catch(_){ } return r; }; }
  }catch(_){ }
  window.__dateRangeCalendarPartsRead__ = read;
  window.__dateRangeCalendarPartsWrite__ = write;
  window.__dateRangeCalendarPartsApply__ = applyParts;
})();


/* dDAE_2.896 — Range date: popup colore solo con pressione lunga + campi check-in/out ridimensionati */
(function(){
  'use strict';
  var PARTS_KEY = 'dDAE_date_range_calendar_parts_v1';
  var PART_DEFAULTS = {
    fill:{ bg:'sky-6', border:'sky-6', fg:'gray-1', opacity:1 },
    numbers:{ bg:'gray-1', border:'gray-1', fg:'gray-6', opacity:0 },
    lines:{ bg:'gray-1', border:'gray-3', fg:'gray-6', opacity:0.55 }
  };
  var PART_LABELS = {
    fill:'Riempimenti tasti e pallini',
    numbers:'Testo numeri',
    lines:'Contorni e linee'
  };
  function norm(v, key){
    var d = PART_DEFAULTS[key] || PART_DEFAULTS.lines;
    v = (v && typeof v === 'object') ? v : {};
    return { bg:v.bg || d.bg, border:v.border || d.border || v.bg || d.bg, fg:v.fg || d.fg, opacity: typeof v.opacity === 'number' ? v.opacity : d.opacity };
  }
  function compact(input){
    input = (input && typeof input === 'object') ? input : {};
    return { fill:norm(input.fill,'fill'), numbers:norm(input.numbers,'numbers'), lines:norm(input.lines,'lines') };
  }
  function read(){
    try{
      if (window.__dateRangeCalendarPartsRead__) return compact(window.__dateRangeCalendarPartsRead__());
      return compact(JSON.parse(localStorage.getItem(PARTS_KEY) || '{}') || {});
    }catch(_){ return compact({}); }
  }
  function write(map){
    try{ if (window.__dateRangeCalendarPartsWrite__) window.__dateRangeCalendarPartsWrite__(compact(map)); else localStorage.setItem(PARTS_KEY, JSON.stringify(compact(map))); }catch(_){ }
  }
  function refresh(){
    try{ if (window.__dateRangeCalendarPartsApply__) window.__dateRangeCalendarPartsApply__(); }catch(_){ }
    try{ __renderGuestDateRangeCalendar__(); }catch(_){ }
    try{ __renderLaundryDateRangeCalendar__(); }catch(_){ }
    try{ __renderSpeseDateRangeCalendar__(); }catch(_){ }
    try{ __renderLicenseDateRangeCalendar__(); }catch(_){ }
  }
  function openCategory(category){
    try{
      if (!PART_DEFAULTS[category]) category = 'lines';
      var before = read();
      var current = before[category] || norm(null, category);
      function apply(payload, changed){
        var now = read();
        var flags = changed || (category === 'fill' ? {bg:true,opacity:true} : (category === 'numbers' ? {fg:true} : {border:true,opacity:true}));
        var next;
        try{ next = __applyDesignPayloadToVisual__(now[category] || current, payload, flags, current.bg || 'gray-1'); }
        catch(_){ next = payload || current; }
        now[category] = norm(next, category);
        write(now);
        refresh();
      }
      try{ if (typeof toast === 'function') toast(PART_LABELS[category] || 'Calendario'); }catch(_){ }
      __tagColorPopupOpen__('date-range-calendar-' + category, current, function(payload){ apply(payload); }, {
        supportsBg: category === 'fill',
        supportsBorder: category === 'lines',
        supportsFg: category === 'numbers',
        supportsOpacity: category !== 'numbers',
        opacity: current.opacity,
        defaultMode: category === 'fill' ? 'bg' : (category === 'numbers' ? 'fg' : 'border'),
        fallbackBg: current.bg || 'gray-1',
        onPreview:function(payload, changed){ apply(payload, changed); },
        onRevert:function(){ write(before); refresh(); }
      });
    }catch(_){ }
  }
  function categoryFromTarget(t){
    try{
      if (!t || !t.closest) return 'lines';
      if (t.closest('.guest-date-range-actions .btn, .guest-date-range-nav')) return 'fill';
      var day = t.closest('.guest-date-range-day');
      if (day){
        if (day.classList.contains('is-selected') || day.classList.contains('is-edge') || day.classList.contains('is-in-range')) return 'fill';
        return 'numbers';
      }
      if (t.closest('.guest-date-range-weekdays')) return 'numbers';
      return 'lines';
    }catch(_){ return 'lines'; }
  }
  function isRangeTarget(e){ try{ return !!(e && e.target && e.target.closest && e.target.closest('.guest-date-range-modal-card')); }catch(_){ return false; } }
  function block(e){ try{ e.preventDefault(); }catch(_){ } try{ e.stopPropagation(); }catch(_){ } try{ e.stopImmediatePropagation(); }catch(_){ } }
  var timer = null, fired = false, suppressUntil = 0, lastTouchStart = 0;
  function clear(){ if (timer){ clearTimeout(timer); timer = null; } }
  function start(e){
    if (!isRangeTarget(e)) return;
    try{ if (e.type === 'touchstart') lastTouchStart = Date.now(); }catch(_){ }
    try{ if (e.type === 'mousedown' && Date.now() - lastTouchStart < 800) return; }catch(_){ }
    try{ if (e.type === 'pointerdown' && e.pointerType === 'mouse' && e.button !== 0) return; }catch(_){ }
    fired = false;
    clear();
    var category = categoryFromTarget(e.target);
    timer = setTimeout(function(){ fired = true; suppressUntil = Date.now() + 900; openCategory(category); }, 650);
  }
  function stop(e){
    if (!isRangeTarget(e)) { clear(); return; }
    clear();
    if (fired){ block(e); setTimeout(function(){ fired = false; }, 0); }
  }
  function swallow(e){ if (isRangeTarget(e) && (fired || Date.now() < suppressUntil)) block(e); }
  try{ window.__openDateRangeCalendarThemePicker__ = function(){ }; }catch(_){ }
  try{ __openDateRangeCalendarThemePicker__ = function(){ }; }catch(_){ }
  ['pointerdown','touchstart','mousedown'].forEach(function(evt){ try{ document.addEventListener(evt, start, { passive:true, capture:true }); }catch(_){ } });
  ['pointerup','pointercancel','pointerleave','touchend','touchcancel','mouseup','mouseleave','dragstart'].forEach(function(evt){ try{ document.addEventListener(evt, stop, { passive:false, capture:true }); }catch(_){ } });
  ['click','dblclick'].forEach(function(evt){ try{ document.addEventListener(evt, swallow, true); }catch(_){ } });
  try{ document.addEventListener('contextmenu', function(e){ if (!isRangeTarget(e)) return; block(e); openCategory(categoryFromTarget(e.target)); }, true); }catch(_){ }
})();


/* dDAE_2.913 — Statistiche: confronto anni standardizzato su tutti i grafici + layer grafico fisso */
(function(){
  const STAT_COMPARE_TOGGLE_IDS = [
    'statGenCompareToggleBtn','statAmmCompareToggleBtn','statMensiliCompareToggleBtn',
    'statSpeseCompareToggleBtn','statRicevuteCompareToggleBtn','statChannelCompareToggleBtn',
    'statPulizieCompareToggleBtn','statCancellazioniCompareToggleBtn'
  ];
  const STAT_COMPARE_YEAR_IDS = [
    'statGenCompareYearBtn','statAmmCompareYearBtn','statMensiliCompareYearBtn',
    'statSpeseCompareYearBtn','statRicevuteCompareYearBtn','statChannelCompareYearBtn',
    'statPulizieCompareYearBtn','statCancellazioniCompareYearBtn'
  ];
  const STAT_COMPARE_LABELS = {
    statGenCompareToggleBtn:'generali', statAmmCompareToggleBtn:'ricavi', statMensiliCompareToggleBtn:'mensili',
    statSpeseCompareToggleBtn:'spese', statRicevuteCompareToggleBtn:'ricevute', statChannelCompareToggleBtn:'PMS',
    statPulizieCompareToggleBtn:'pulizie', statCancellazioniCompareToggleBtn:'cancellazioni'
  };
  const STAT_GRAPH_PAGES = [
    { page:'statgen', title:'Generali', toggle:'statGenCompareToggleBtn', year:'statGenCompareYearBtn', toggleLabel:'statGenCompareToggleBtnLabel', yearLabel:'statGenCompareYearBtnLabel' },
    { page:'statamministratore', title:'Ricavi', toggle:'statAmmCompareToggleBtn', year:'statAmmCompareYearBtn', toggleLabel:'statAmmCompareToggleBtnLabel', yearLabel:'statAmmCompareYearBtnLabel' },
    { page:'statmensili', title:'Mensili', toggle:'statMensiliCompareToggleBtn', year:'statMensiliCompareYearBtn', toggleLabel:'statMensiliCompareToggleBtnLabel', yearLabel:'statMensiliCompareYearBtnLabel' },
    { page:'statspese', title:'Spese generali', toggle:'statSpeseCompareToggleBtn', year:'statSpeseCompareYearBtn', toggleLabel:'statSpeseCompareToggleBtnLabel', yearLabel:'statSpeseCompareYearBtnLabel' },
    { page:'statprenotazioni', title:'Con / Senza ricevuta', toggle:'statRicevuteCompareToggleBtn', year:'statRicevuteCompareYearBtn', toggleLabel:'statRicevuteCompareToggleBtnLabel', yearLabel:'statRicevuteCompareYearBtnLabel' },
    { page:'statchannel', title:'PMS', toggle:'statChannelCompareToggleBtn', year:'statChannelCompareYearBtn', toggleLabel:'statChannelCompareToggleBtnLabel', yearLabel:'statChannelCompareYearBtnLabel' },
    { page:'statpulizie', title:'Ore pulizia operatori', toggle:'statPulizieCompareToggleBtn', year:'statPulizieCompareYearBtn', toggleLabel:'statPulizieCompareToggleBtnLabel', yearLabel:'statPulizieCompareYearBtnLabel' },
    { page:'statcancellazioni', title:'Cancellazioni', toggle:'statCancellazioniCompareToggleBtn', year:'statCancellazioniCompareYearBtn', toggleLabel:'statCancellazioniCompareToggleBtnLabel', yearLabel:'statCancellazioniCompareYearBtnLabel' }
  ];

  window.__DDAE_STAT_COMPARE_IDS__ = { toggles: STAT_COMPARE_TOGGLE_IDS.slice(), years: STAT_COMPARE_YEAR_IDS.slice() };

  function byId(id){ try{ return document.getElementById(id); }catch(_){ return null; } }
  function allIds(ids){ return ids.map(byId).filter(Boolean); }
  function isExistingLegacyCompareId(id){ return id === 'statGenCompareToggleBtn' || id === 'statGenCompareYearBtn' || id === 'statMensiliCompareToggleBtn' || id === 'statMensiliCompareYearBtn' || id === 'statAmmCompareToggleBtn' || id === 'statAmmCompareYearBtn'; }
  function pageEl(page){ return document.getElementById('page-' + page); }

  function ensureHeadRow(pageCfg){
    const page = pageEl(pageCfg.page);
    const container = page ? page.querySelector('.stats-gen') : null;
    if (!container) return null;
    let head = container.querySelector(':scope > .stats-head-row');
    if (!head){
      const title = container.querySelector(':scope > .stats-title');
      if (!title) return null;
      head = document.createElement('div');
      head.className = 'stats-head-row stats-compare-head-row';
      container.insertBefore(head, title);
      head.appendChild(title);
    }
    head.classList.add('stats-compare-head-row');
    let title = head.querySelector('.stats-title');
    if (!title){
      title = document.createElement('div');
      title.className = 'stats-title';
      title.textContent = pageCfg.title || '';
      head.insertBefore(title, head.firstChild);
    }
    let actions = head.querySelector('.stats-head-actions');
    if (!actions){
      actions = document.createElement('div');
      actions.className = 'stats-head-actions statgen-head-actions';
      actions.setAttribute('aria-label', 'Azioni grafico');
      head.appendChild(actions);
    }
    actions.classList.add('statgen-head-actions', 'stats-compare-actions');
    return { container, head, actions };
  }

  function makeCompareToggle(id, labelId, title){
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.id = id;
    btn.className = 'piscina-action-btn statgen-compare-toggle-btn stats-compare-btn';
    btn.setAttribute('aria-label', 'Confronto anno grafico ' + (title || ''));
    const span = document.createElement('span');
    span.id = labelId;
    span.className = 'statgen-compare-toggle-label';
    span.textContent = 'ON';
    btn.appendChild(span);
    return btn;
  }
  function makeCompareYear(id, labelId, title){
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.id = id;
    btn.className = 'piscina-action-btn statgen-compare-year-btn stats-compare-btn';
    btn.setAttribute('aria-label', 'Seleziona anno confronto grafico ' + (title || ''));
    const span = document.createElement('span');
    span.id = labelId;
    span.className = 'statgen-compare-year-value';
    span.textContent = '2025';
    btn.appendChild(span);
    return btn;
  }

  function ensureCompareButtons(){
    STAT_GRAPH_PAGES.forEach((cfg)=>{
      try{
        const ctx = ensureHeadRow(cfg);
        if (!ctx) return;
        const { actions } = ctx;
        if (!byId(cfg.toggle)) actions.appendChild(makeCompareToggle(cfg.toggle, cfg.toggleLabel, cfg.title));
        if (!byId(cfg.year)) actions.appendChild(makeCompareYear(cfg.year, cfg.yearLabel, cfg.title));
      }catch(_){ }
    });
  }

  function ensureFixedLayers(){
    STAT_GRAPH_PAGES.forEach((cfg)=>{
      try{
        const page = pageEl(cfg.page);
        const container = page ? page.querySelector('.stats-gen') : null;
        if (!container || container.dataset.statsFixedLayerReady === '1') return;
        const chart = container.querySelector(':scope > .statgen-line-chart-wrap');
        if (!chart) return;
        const children = Array.from(container.children);
        const chartIndex = children.indexOf(chart);
        if (chartIndex < 0) return;
        const fixed = document.createElement('div');
        fixed.className = 'stats-fixed-layer';
        const scroll = document.createElement('div');
        scroll.className = 'stats-scroll-layer';
        container.insertBefore(fixed, children[0] || null);
        children.slice(0, chartIndex + 1).forEach((node)=>fixed.appendChild(node));
        Array.from(container.children).filter((node)=>node !== fixed && node !== scroll).forEach((node)=>scroll.appendChild(node));
        container.appendChild(scroll);
        container.classList.add('stats-layered');
        container.dataset.statsFixedLayerReady = '1';
      }catch(_){ }
    });
  }

  window.__ensureStatsCompareLayerUI__ = function(){
    try{ ensureCompareButtons(); }catch(_){ }
    try{ ensureFixedLayers(); }catch(_){ }
    try{ if (typeof __updateStatGenCompareYearButtonUI__ === 'function') __updateStatGenCompareYearButtonUI__(); }catch(_){ }
  };

  const oldApplyVisual = (typeof window.__applyStatGenCompareYearButtonVisual__ === 'function') ? window.__applyStatGenCompareYearButtonVisual__ : null;
  window.__applyStatGenCompareYearButtonVisual__ = function(){
    try{ ensureCompareButtons(); }catch(_){ }
    try{
      const btns = allIds(STAT_COMPARE_TOGGLE_IDS);
      if (!btns.length) return;
      const enabled = __ensureStatGenCompareEnabled__();
      const visuals = __statGenCompareBtnVisualRead__();
      const pair = enabled ? visuals.on : visuals.off;
      const isDark = !!(__isDarkModeRuntime__ && __isDarkModeRuntime__());
      const bgHex = __graphColorValueToHex__(pair?.bg || (enabled ? '#2b7cb4' : '#d6dee8'), enabled ? '#2b7cb4' : '#d6dee8');
      const borderHex = __graphColorValueToHex__(pair?.border || pair?.bg || bgHex, bgHex);
      const textHex = isDark
        ? __darkModeReadableTextHex__(__graphColorValueToHex__(pair?.fg || __tagColorTextHex__(pair?.bg || bgHex, pair?.fg || '', false), bgHex), enabled ? '#f8fbff' : '#e2e8f0')
        : __tagColorTextHex__(pair?.bg || bgHex, pair?.fg || '', false);
      const opacity = __designBgOpacityNormalize__(pair?.opacity ?? 0.80);
      btns.forEach((btn)=>{
        btn.classList.toggle('is-active', enabled);
        btn.classList.toggle('is-inactive', !enabled);
        btn.style.setProperty('--comparebtn-bg', hexToRgba(bgHex, opacity));
        btn.style.setProperty('--comparebtn-border', borderHex);
        btn.style.setProperty('--comparebtn-fg', textHex);
        btn.style.setProperty('background', hexToRgba(bgHex, opacity), 'important');
        btn.style.setProperty('background-color', hexToRgba(bgHex, opacity), 'important');
        btn.style.setProperty('border', `1px solid ${borderHex}`, 'important');
        btn.style.setProperty('color', textHex, 'important');
        btn.style.setProperty('-webkit-text-fill-color', textHex, 'important');
        btn.setAttribute('aria-pressed', enabled ? 'true' : 'false');
        try{ __applyVisualTextWeight__(btn, pair); }catch(_){ }
      });
    }catch(_){ if (oldApplyVisual) try{ oldApplyVisual(); }catch(__){} }
  };
  // make top-level calls resolve to the replacement too
  try{ __applyStatGenCompareYearButtonVisual__ = window.__applyStatGenCompareYearButtonVisual__; }catch(_){ }

  function redrawAllStatCompareCharts(){
    try{ drawStatGenRegistrationsLineChart('statGenRegChart'); }catch(_){ }
    try{ drawStatMensiliOccupazioneLineChart('statMensiliLineChart'); }catch(_){ }
    try{ drawStatSpesePercentLineChart('statSpeseLineChart'); }catch(_){ }
    try{ drawStatRicevuteLineChart('statRicevuteCanvas'); }catch(_){ }
    try{ drawStatChannelLineChart('statChannelCanvas'); }catch(_){ }
    try{ drawStatPulizieLineChart('statPulizieCanvas', Array.isArray(state && state.statGraficiOperatoriRows) ? state.statGraficiOperatoriRows : []); }catch(_){ }
    try{ drawStatCancellazioniPercentLineChart('statCancellazioniLineChart'); }catch(_){ }
    try{ drawStatAmministratoreLineChart('statAmmRegChart'); }catch(_){ }
  }
  window.__redrawAllStatCompareCharts__ = redrawAllStatCompareCharts;

  window.__updateStatGenCompareYearButtonUI__ = function(){
    const year = __ensureStatGenCompareYear__();
    const enabled = __ensureStatGenCompareEnabled__();
    try{ ensureCompareButtons(); }catch(_){ }
    try{
      STAT_COMPARE_YEAR_IDS.forEach((id)=>{
        const btn = byId(id);
        const label = btn ? btn.querySelector('.statgen-compare-year-value') : null;
        if (label) label.textContent = year;
        if (btn){
          btn.setAttribute('aria-label', `Seleziona anno confronto grafico ${year}`);
          btn.title = `Anno confronto ${year}`;
        }
      });
    }catch(_){ }
    try{
      STAT_COMPARE_TOGGLE_IDS.forEach((id)=>{
        const btn = byId(id);
        const label = btn ? btn.querySelector('.statgen-compare-toggle-label') : null;
        if (label) label.textContent = enabled ? 'ON' : 'OFF';
        if (btn){
          const name = STAT_COMPARE_LABELS[id] || 'grafico';
          btn.setAttribute('aria-label', `Confronto ${name} ${enabled ? 'attivo' : 'disattivo'}`);
          btn.title = `Confronto ${enabled ? 'ON' : 'OFF'}`;
        }
      });
    }catch(_){ }
    try{ __applyStatGenCompareYearButtonVisual__(); }catch(_){ }
  };
  try{ __updateStatGenCompareYearButtonUI__ = window.__updateStatGenCompareYearButtonUI__; }catch(_){ }

  window.__toggleStatGenCompareEnabled__ = function(){
    const next = !__ensureStatGenCompareEnabled__();
    __statGenCompareEnabledWrite__(next);
    __updateStatGenCompareYearButtonUI__();
    if (next){ try{ __loadStatGenCompareGuests__({ force:true }); }catch(_){ } }
    redrawAllStatCompareCharts();
  };
  try{ __toggleStatGenCompareEnabled__ = window.__toggleStatGenCompareEnabled__; }catch(_){ }

  function getCompareSnapshot(){
    try{
      const compareYear = __ensureStatGenCompareYear__();
      const hasValidSnapshot = String(state.statGenCompareSnapshotYear || '') === String(compareYear) && state.statGenCompareSnapshot;
      if (!hasValidSnapshot && !state.statGenCompareLoading) try{ __loadStatGenCompareGuests__({ force:true }); }catch(_){ }
      if (hasValidSnapshot) return state.statGenCompareSnapshot;
      return {
        guests: Array.isArray(state.statGenCompareGuests) ? state.statGenCompareGuests.slice() : [],
        spese: Array.isArray(state.statGenCompareSnapshot?.spese) ? state.statGenCompareSnapshot.spese.slice() : [],
        report: state.statGenCompareSnapshot?.report || null,
        servizi: Array.isArray(state.statGenCompareSnapshot?.servizi) ? state.statGenCompareSnapshot.servizi.slice() : [],
        stanzeRows: Array.isArray(state.statGenCompareSnapshot?.stanzeRows) ? state.statGenCompareSnapshot.stanzeRows.slice() : [],
        operatoriRows: Array.isArray(state.statGenCompareSnapshot?.operatoriRows) ? state.statGenCompareSnapshot.operatoriRows.slice() : [],
        deletedGuests: Array.isArray(state.statGenCompareSnapshot?.deletedGuests) ? state.statGenCompareSnapshot.deletedGuests.slice() : []
      };
    }catch(_){ return { guests:[], spese:[], servizi:[], stanzeRows:[], operatoriRows:[], deletedGuests:[] }; }
  }

  function addCompareSeriesToList(seriesList, pageKey, getCompareRows){
    if (!__ensureStatGenCompareEnabled__()) return seriesList;
    try{
      const compareYear = __ensureStatGenCompareYear__();
      const compareRows = (typeof getCompareRows === 'function') ? (getCompareRows(getCompareSnapshot()) || []) : [];
      if (!Array.isArray(compareRows) || !compareRows.length) return seriesList;
      const currentByKey = {};
      (seriesList || []).forEach((item)=>{ currentByKey[String(item?.key || '')] = item; });
      const selected = __getStatChartFilter__(pageKey);
      compareRows.forEach((row)=>{
        const key = String(row?.key || '').trim();
        if (!key) return;
        if (selected && selected !== key) return;
        const vals = Array.isArray(row.values) ? row.values.slice(0, 12) : [];
        if (!vals.some((value)=>Math.abs(Number(value || 0) || 0) > 0.0001)) return;
        const base = currentByKey[key] || row;
        seriesList.push({
          key: key + '-compare-year',
          label: String(base.label || row.label || 'Confronto') + ' ' + String(compareYear),
          values: vals,
          color: base.color || row.color || '#9aa3af',
          dash: [6,4], pointFill:'#ffffff', lineWidth:1.8, radius:2.5, pointLineWidth:1.4
        });
      });
    }catch(_){ }
    return seriesList;
  }

  const oldLoadCompare = (typeof window.__loadStatGenCompareGuests__ === 'function') ? window.__loadStatGenCompareGuests__ : null;
  if (oldLoadCompare){
    window.__loadStatGenCompareGuests__ = async function(opts){
      const result = await oldLoadCompare(opts || {});
      try{
        const compareYear = __ensureStatGenCompareYear__();
        let snapshot = state.statGenCompareSnapshot || {};
        // completa snapshot con operatori e ospiti eliminati, quando disponibili in IndexedDB/local cache.
        try{
          if (!Array.isArray(snapshot.operatoriRows) || !snapshot.operatoriRows.length){
            const keys = (typeof __kvKeys__ === 'function') ? await __kvKeys__('ctx:') : [];
            const suffixOp = `:${compareYear}:tbl:operatori`;
            const suffixDel = `:${compareYear}:tbl:ospiti_eliminati`;
            const currentUid = String((state && state.session && (state.session.user_id || state.session.id || state.session.username)) || '').trim();
            const pickKey = (suffix) => {
              const arr = keys.filter((k)=>String(k||'').endsWith(suffix));
              arr.sort((a,b)=>{
                const au = String(a||'').split(':')[1] || '';
                const bu = String(b||'').split(':')[1] || '';
                const ap = currentUid && au === currentUid ? 0 : 1;
                const bp = currentUid && bu === currentUid ? 0 : 1;
                if (ap !== bp) return ap-bp;
                return String(a).localeCompare(String(b));
              });
              return arr[0] || '';
            };
            const opKey = pickKey(suffixOp);
            if (opKey){ const rows = await __kvGet__(opKey); if (Array.isArray(rows)) snapshot.operatoriRows = rows.slice(); }
            const delKey = pickKey(suffixDel);
            if (delKey){ const rows = await __kvGet__(delKey); if (Array.isArray(rows)) snapshot.deletedGuests = rows.slice(); }
          }
        }catch(_){ }
        state.statGenCompareSnapshot = snapshot;
        redrawAllStatCompareCharts();
      }catch(_){ }
      return result;
    };
    try{ __loadStatGenCompareGuests__ = window.__loadStatGenCompareGuests__; }catch(_){ }
  }

  const oldDrawSpese = (typeof window.drawStatSpesePercentLineChart === 'function') ? window.drawStatSpesePercentLineChart : drawStatSpesePercentLineChart;
  window.drawStatSpesePercentLineChart = function(canvasId){
    const data = __statSpeseMonthlyBreakdown__();
    const build = (source, compareYear) => [
      { key:'totale-spese', label:'Totale spese', values: __statZeroFutureMonths__(__statMonthlyCumulative__(source.totale), compareYear ? { year:compareYear } : undefined), color: __statChartLineColorFromRenderedCard__('statspese','totale-spese','#2b7cb4') },
      { key:'contanti', label:'Contanti', values: __statZeroFutureMonths__(__statMonthlyCumulative__(source.contanti), compareYear ? { year:compareYear } : undefined), color: __statChartLineColorFromRenderedCard__('statspese','contanti','#2563eb') },
      { key:'tassa-soggiorno', label:'Tassa soggiorno', values: __statZeroFutureMonths__(__statMonthlyCumulative__(source.tassa), compareYear ? { year:compareYear } : undefined), color: __statChartLineColorFromRenderedCard__('statspese','tassa-soggiorno','#c7b198') },
      { key:'iva-22', label:'IVA 22%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(source.iva22), compareYear ? { year:compareYear } : undefined), color: __statChartLineColorFromRenderedCard__('statspese','iva-22','#f29c50') },
      { key:'iva-10', label:'IVA 10%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(source.iva10), compareYear ? { year:compareYear } : undefined), color: __statChartLineColorFromRenderedCard__('statspese','iva-10','#6aa0b3') },
      { key:'iva-4', label:'IVA 4%', values: __statZeroFutureMonths__(__statMonthlyCumulative__(source.iva4), compareYear ? { year:compareYear } : undefined), color: __statChartLineColorFromRenderedCard__('statspese','iva-4','#2b7cb4') }
    ];
    let seriesList = build(data).filter((item)=>__statChartSeriesIsVisible__('statspese', item.key));
    if (!seriesList.length){ try{ __clearStatChartFilter__('statspese'); }catch(_){ } seriesList = build(data); }
    addCompareSeriesToList(seriesList, 'statspese', (snap)=> build(__statSpeseMonthlyBreakdownForRows__(Array.isArray(snap.spese) ? snap.spese : []), __ensureStatGenCompareYear__()));
    __drawSharedMonthlyLineChart__(canvasId, (seriesList[0] && seriesList[0].values) ? seriesList[0].values : new Array(12).fill(0), { mode:'currency', seriesList, bubbleFormatter:(value)=>__statLineChartCompactEuro__(value), yTickFormatter:(value)=>__statLineChartCompactEuro__(value) });
  };
  try{ drawStatSpesePercentLineChart = window.drawStatSpesePercentLineChart; }catch(_){ }

  window.drawStatRicevuteLineChart = function(canvasId){
    const makeRows = (guests)=>{
      const series = __statGuestDualMonthlySeriesForRows__(Array.isArray(guests) ? guests : [], (guest)=>{
        const dep = __statGuestMoney__(guest?.acconto_importo ?? guest?.accontoImporto ?? 0);
        const saldo = __statGuestMoney__(guest?.saldo_pagato ?? guest?.saldoPagato ?? guest?.saldo ?? 0);
        const depRec = _isRicevutaFlag(guest, 'acconto');
        const saldoRec = _isRicevutaFlag(guest, 'saldo');
        return { primary:(depRec ? dep : 0) + (saldoRec ? saldo : 0), secondary:(depRec ? 0 : dep) + (saldoRec ? 0 : saldo) };
      });
      return [
        { key:'con-ricevuta', label:'Con ricevuta', values:series.primary, color:__statChartLineColorFromRenderedCard__('statprenotazioni','con-ricevuta','#6aa0b3') },
        { key:'senza-ricevuta', label:'Senza ricevuta', values:series.secondary, color:__statChartLineColorFromRenderedCard__('statprenotazioni','senza-ricevuta','#9aa3af') }
      ];
    };
    let seriesList = makeRows(Array.isArray(state.statsGuests) ? state.statsGuests : state.guests).filter((item)=>__statChartSeriesIsVisible__('statprenotazioni', item.key));
    if (!seriesList.length){ try{ __clearStatChartFilter__('statprenotazioni'); }catch(_){ } seriesList = makeRows(Array.isArray(state.statsGuests) ? state.statsGuests : state.guests); }
    addCompareSeriesToList(seriesList, 'statprenotazioni', (snap)=>makeRows(snap.guests));
    __drawSharedMonthlyLineChart__(canvasId, (seriesList[0] && seriesList[0].values) ? seriesList[0].values : new Array(12).fill(0), { seriesList, bubbleFormatter:(value)=>__statLineChartCompactEuro__(value), yTickFormatter:(value)=>__statLineChartCompactEuro__(value) });
  };
  try{ drawStatRicevuteLineChart = window.drawStatRicevuteLineChart; }catch(_){ }

  function statChannelSeriesForGuests(guests){
    const backup = { statsGuests:state.statsGuests, guests:state.guests };
    try{ state.statsGuests = Array.isArray(guests) ? guests.slice() : []; state.guests = state.statsGuests; return __statChannelMonthlySeries__(); }
    catch(_){ return []; }
    finally{ state.statsGuests = backup.statsGuests; state.guests = backup.guests; }
  }
  window.drawStatChannelLineChart = function(canvasId){
    const make = (rows)=> statChannelSeriesForGuests(rows).map((item)=>({ key:item.key, label:item.label, values:item.values, color:__statChartLineColorFromRenderedCard__('statchannel', item.key, item.fallback || '#2b7cb4') }));
    let seriesList = make(Array.isArray(state.statsGuests) ? state.statsGuests : state.guests).filter((item)=>__statChartSeriesIsVisible__('statchannel', item.key));
    if (!seriesList.length){ try{ __clearStatChartFilter__('statchannel'); }catch(_){ } seriesList = make(Array.isArray(state.statsGuests) ? state.statsGuests : state.guests); }
    addCompareSeriesToList(seriesList, 'statchannel', (snap)=>make(snap.guests));
    __drawSharedMonthlyLineChart__(canvasId, (seriesList[0] && seriesList[0].values) ? seriesList[0].values : new Array(12).fill(0), { seriesList, bubbleFormatter:(value)=>__statLineChartCompactEuro__(value), yTickFormatter:(value)=>__statLineChartCompactEuro__(value) });
  };
  try{ drawStatChannelLineChart = window.drawStatChannelLineChart; }catch(_){ }

  function statPulizieSeriesForRows(rows, year){
    const backup = { exerciseYear:state.exerciseYear };
    try{ if (year) state.exerciseYear = String(year); return __statPulizieMonthlySeriesByOperator__(Array.isArray(rows) ? rows : []); }
    catch(_){ return []; }
    finally{ state.exerciseYear = backup.exerciseYear; }
  }
  window.drawStatPulizieLineChart = function(canvasId, rows){
    let seriesList = statPulizieSeriesForRows(rows, state.exerciseYear || loadExerciseYear()).filter((item)=>__statChartSeriesIsVisible__('statpulizie', item.key));
    if (!seriesList.length){ try{ __clearStatChartFilter__('statpulizie'); }catch(_){ } seriesList = statPulizieSeriesForRows(rows, state.exerciseYear || loadExerciseYear()); }
    addCompareSeriesToList(seriesList, 'statpulizie', (snap)=>statPulizieSeriesForRows(Array.isArray(snap.operatoriRows) && snap.operatoriRows.length ? snap.operatoriRows : [], __ensureStatGenCompareYear__()));
    __drawSharedMonthlyLineChart__(canvasId, new Array(12).fill(0), { seriesList, bubbleFormatter:(value)=>`${__fmtHours_(value) || '0'}h`, yTickFormatter:(value)=>`${Math.round(Number(value || 0) || 0)}h` });
  };
  try{ drawStatPulizieLineChart = window.drawStatPulizieLineChart; }catch(_){ }

  function cancellationsForDeletedRows(rows, year){
    const yy = String(year || '').trim();
    const cancByMonth = new Array(12).fill(0);
    const list = Array.isArray(rows) ? rows : [];
    const pickIso = (row)=>{
      const candidates = [row?.deletedAt,row?.deleted_at,row?.updatedAt,row?.updated_at,row?.dataCancellazione,row?.data_cancellazione,row?.delete_date,row?.deleteDate,row?.data,row?.date];
      for (const raw of candidates){ let iso=''; try{ iso = (typeof __parseDateFlexibleToISO === 'function') ? __parseDateFlexibleToISO(raw || '') : ''; }catch(_){ iso=''; } if (iso && /^\d{4}-\d{2}-\d{2}$/.test(iso)) return iso; }
      return '';
    };
    list.filter(r=>String(r?.delete_reason || '').toLowerCase() === 'cancellazione' || String(r?.delete_reason || '').trim() === '').forEach((row)=>{
      const iso = pickIso(row); if (!iso) return; if (yy && String(iso.slice(0,4)) !== yy) return;
      const mm = parseInt(iso.slice(5,7),10); if (!Number.isFinite(mm) || mm<1 || mm>12) return; cancByMonth[mm-1] += 1;
    });
    return cancByMonth;
  }
  window.drawStatCancellazioniPercentLineChart = function(canvasId){
    const currentYear = String((state && (state.year || state.exerciseYear)) || loadExerciseYear() || new Date().getFullYear());
    const currentVals = cancellationsForDeletedRows(Array.isArray(state.deletedGuests) ? state.deletedGuests : [], currentYear);
    let seriesList = [{ key:'cancellazioni', label:'Cancellazioni ' + currentYear, values:currentVals, color:__statChartSelectedLineColor__('statcancellazioni','#2B7CB4') }];
    if (__ensureStatGenCompareEnabled__()){
      try{
        const compareYear = __ensureStatGenCompareYear__();
        const snap = getCompareSnapshot();
        const vals = cancellationsForDeletedRows(Array.isArray(snap.deletedGuests) ? snap.deletedGuests : [], compareYear);
        if (vals.some((v)=>Number(v||0)>0)) seriesList.push({ key:'cancellazioni-compare-year', label:'Cancellazioni ' + compareYear, values:vals, color:seriesList[0].color, dash:[6,4], pointFill:'#ffffff', lineWidth:1.8, radius:2.5, pointLineWidth:1.4 });
      }catch(_){ }
    }
    __drawSharedMonthlyLineChart__(canvasId, currentVals, { seriesList, lineColor:seriesList[0].color });
  };
  try{ drawStatCancellazioniPercentLineChart = window.drawStatCancellazioniPercentLineChart; }catch(_){ }

  function bindNewCompareControls(){
    if (window.__statCompareAllDelegationBound) return;
    window.__statCompareAllDelegationBound = true;
    let lpTimer = null;
    let lpFired = false;
    let lpBtn = null;
    const clearLp = ()=>{ if (lpTimer){ clearTimeout(lpTimer); lpTimer = null; } };
    document.addEventListener('pointerdown', (e)=>{
      const btn = e.target && e.target.closest ? e.target.closest('.statgen-compare-toggle-btn') : null;
      if (!btn || !STAT_COMPARE_TOGGLE_IDS.includes(btn.id) || isExistingLegacyCompareId(btn.id)) return;
      lpFired = false; lpBtn = btn; clearLp();
      lpTimer = setTimeout(()=>{ lpFired = true; try{ btn.classList.add('is-pressing'); }catch(_){ } try{ __openStatGenCompareYearButtonColorPicker__(); }catch(_){ } }, 520);
    }, { passive:true });
    ['pointerup','pointercancel','pointerleave'].forEach((evt)=>{
      document.addEventListener(evt, (e)=>{ if (lpBtn){ clearLp(); setTimeout(()=>{ try{ lpBtn.classList.remove('is-pressing'); }catch(_){} lpBtn=null; lpFired=false; }, lpFired ? 80 : 0); } }, { passive:true });
    });
    document.addEventListener('click', (e)=>{
      const toggle = e.target && e.target.closest ? e.target.closest('.statgen-compare-toggle-btn') : null;
      if (toggle && STAT_COMPARE_TOGGLE_IDS.includes(toggle.id) && !isExistingLegacyCompareId(toggle.id)){
        try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
        if (!lpFired) __toggleStatGenCompareEnabled__();
        return false;
      }
      const year = e.target && e.target.closest ? e.target.closest('.statgen-compare-year-btn') : null;
      if (year && STAT_COMPARE_YEAR_IDS.includes(year.id) && !isExistingLegacyCompareId(year.id)){
        try{ e.preventDefault(); e.stopPropagation(); }catch(_){ }
        try{ __openStatGenCompareYearPicker__(); }catch(_){ }
        return false;
      }
    }, true);
  }

  function init(){
    try{ if (typeof __STAT_LANDSCAPE_GRAPH_ONLY_PAGES__ !== 'undefined' && __STAT_LANDSCAPE_GRAPH_ONLY_PAGES__ && __STAT_LANDSCAPE_GRAPH_ONLY_PAGES__.add) __STAT_LANDSCAPE_GRAPH_ONLY_PAGES__.add('statamministratore'); }catch(_){ }
    try{ window.__ensureStatsCompareLayerUI__(); }catch(_){ }
    try{ bindNewCompareControls(); }catch(_){ }
    try{ __updateStatGenCompareYearButtonUI__(); }catch(_){ }
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init, { once:true });
  else init();
  try{ setTimeout(init, 400); }catch(_){ }
})();


/* dDAE_2.913 — Statistiche: redraw grafici dopo rotazione/orientamento */
(function(){
  let pending = 0;
  function redrawStatsAfterLayout(){
    try{ window.__ensureStatsCompareLayerUI__ && window.__ensureStatsCompareLayerUI__(); }catch(_){ }
    try{ window.__redrawAllStatCompareCharts__ && window.__redrawAllStatCompareCharts__(); }catch(_){ }
  }
  function scheduleStatsLandscapeRedraw(){
    try{ clearTimeout(pending); }catch(_){ }
    pending = setTimeout(redrawStatsAfterLayout, 90);
    try{ setTimeout(redrawStatsAfterLayout, 320); }catch(_){ }
  }
  try{ window.addEventListener('orientationchange', scheduleStatsLandscapeRedraw, { passive:true }); }catch(_){ }
  try{ window.addEventListener('resize', scheduleStatsLandscapeRedraw, { passive:true }); }catch(_){ }
  try{ document.addEventListener('visibilitychange', function(){ if (!document.hidden) scheduleStatsLandscapeRedraw(); }, { passive:true }); }catch(_){ }
  try{ window.__scheduleStatsLandscapeRedraw__ = scheduleStatsLandscapeRedraw; }catch(_){ }
})();


/* dDAE_2.913 — Statistiche: resize grafici full-display landscape */
(function(){
  function redraw(){
    try{ window.__ensureStatsCompareLayerUI__ && window.__ensureStatsCompareLayerUI__(); }catch(_){ }
    try{ window.__redrawAllStatCompareCharts__ && window.__redrawAllStatCompareCharts__(); }catch(_){ }
  }
  function schedule(){
    try{ redraw(); }catch(_){ }
    try{ requestAnimationFrame(redraw); }catch(_){ }
    try{ setTimeout(redraw, 120); }catch(_){ }
    try{ setTimeout(redraw, 360); }catch(_){ }
  }
  try{ window.addEventListener('orientationchange', schedule, {passive:true}); }catch(_){ }
  try{ window.addEventListener('resize', schedule, {passive:true}); }catch(_){ }
  try{ document.addEventListener('DOMContentLoaded', schedule, {once:true}); }catch(_){ }
  try{ window.__scheduleStatsFullDisplayRedraw__ = schedule; }catch(_){ }
})();


/* dDAE_2.913 — Statistiche: misura layer grafico fisso e mantiene lo scroll card sotto il grafico */
(function(){
  const STAT_LAYER_PAGES = ['statgen','statmensili','statspese','statprenotazioni','statchannel','statpulizie','statcancellazioni','statamministratore'];
  let timer = 0;
  function pageContainer(page){
    try{ return document.querySelector('#page-' + page + ' .stats-gen.stats-layered'); }catch(_){ return null; }
  }
  function ensureLayers(){
    try{ window.__ensureStatsCompareLayerUI__ && window.__ensureStatsCompareLayerUI__(); }catch(_){ }
  }
  function measureOne(container){
    if (!container) return;
    const fixed = container.querySelector(':scope > .stats-fixed-layer');
    const scroll = container.querySelector(':scope > .stats-scroll-layer');
    if (!fixed || !scroll) return;
    let h = 0;
    try{ h = Math.ceil(fixed.getBoundingClientRect().height || 0); }catch(_){ h = 0; }
    if (!h){
      try{ h = Math.ceil(fixed.scrollHeight || fixed.offsetHeight || 0); }catch(_){ h = 0; }
    }
    if (!h) h = (window.matchMedia && window.matchMedia('(orientation: landscape)').matches) ? Math.max(120, Math.round((window.innerHeight || 0) - 40)) : 188;
    try{ container.style.setProperty('--stats-fixed-layer-height', h + 'px'); }catch(_){ }
  }
  function measureAll(){
    ensureLayers();
    STAT_LAYER_PAGES.forEach((page)=>measureOne(pageContainer(page)));
    try{ window.__redrawAllStatCompareCharts__ && window.__redrawAllStatCompareCharts__(); }catch(_){ }
  }
  function schedule(){
    try{ clearTimeout(timer); }catch(_){ }
    try{ requestAnimationFrame(measureAll); }catch(_){ }
    timer = setTimeout(measureAll, 120);
    try{ setTimeout(measureAll, 360); }catch(_){ }
  }
  try{ window.__ddaeStatsFixedGraphLayerRefresh__ = schedule; }catch(_){ }
  try{ window.addEventListener('resize', schedule, { passive:true }); }catch(_){ }
  try{ window.addEventListener('orientationchange', schedule, { passive:true }); }catch(_){ }
  try{ document.addEventListener('visibilitychange', function(){ if (!document.hidden) schedule(); }, { passive:true }); }catch(_){ }
  try{ document.addEventListener('DOMContentLoaded', schedule, { once:true }); }catch(_){ }
  if (document.readyState !== 'loading') schedule();
  try{ setTimeout(schedule, 500); }catch(_){ }
})();

/* dDAE_2.913 — Statistiche: riparazione layer fisso anche su cancellazioni/ricevute/PMS/pulizie */
(function(){
  const PAGES = ['statgen','statmensili','statspese','statprenotazioni','statchannel','statpulizie','statcancellazioni','statamministratore'];
  let timer = 0;
  function pageEl(page){ try{ return document.getElementById('page-' + page); }catch(_){ return null; } }
  function directChildren(el){ try{ return Array.from(el.children || []); }catch(_){ return []; } }
  function forceOne(page){
    const root = pageEl(page);
    const container = root ? root.querySelector('.stats-gen') : null;
    if (!container) return;
    let fixed = container.querySelector(':scope > .stats-fixed-layer');
    let scroll = container.querySelector(':scope > .stats-scroll-layer');
    const chart = container.querySelector(':scope > .statgen-line-chart-wrap, :scope > .stats-fixed-layer .statgen-line-chart-wrap');
    if (!chart) return;

    if (!fixed || !scroll || !fixed.contains(chart)){
      if (fixed){
        directChildren(fixed).forEach((node)=>container.insertBefore(node, fixed));
        try{ fixed.remove(); }catch(_){ }
      }
      if (scroll){
        directChildren(scroll).forEach((node)=>container.appendChild(node));
        try{ scroll.remove(); }catch(_){ }
      }
      fixed = document.createElement('div');
      fixed.className = 'stats-fixed-layer';
      scroll = document.createElement('div');
      scroll.className = 'stats-scroll-layer';
      const children = directChildren(container);
      const topChart = container.querySelector(':scope > .statgen-line-chart-wrap');
      const index = children.indexOf(topChart);
      if (index < 0) return;
      container.insertBefore(fixed, children[0] || null);
      children.slice(0, index + 1).forEach((node)=>fixed.appendChild(node));
      directChildren(container).filter((node)=>node !== fixed && node !== scroll).forEach((node)=>scroll.appendChild(node));
      container.appendChild(scroll);
    }
    container.classList.add('stats-layered');
    container.dataset.statsFixedLayerReady = '1';
    measure(container);
  }
  function measure(container){
    const fixed = container ? container.querySelector(':scope > .stats-fixed-layer') : null;
    if (!fixed) return;
    let h = 0;
    try{ h = Math.ceil(fixed.getBoundingClientRect().height || 0); }catch(_){ h = 0; }
    if (!h) try{ h = Math.ceil(fixed.scrollHeight || fixed.offsetHeight || 0); }catch(_){ h = 0; }
    if (!h) h = (window.matchMedia && window.matchMedia('(orientation: landscape)').matches) ? Math.max(120, Math.round((window.innerHeight || 0) - 40)) : 188;
    try{ container.style.setProperty('--stats-fixed-layer-height', h + 'px'); }catch(_){ }
  }
  function redraw(){
    try{ window.__ensureStatsCompareLayerUI__ && window.__ensureStatsCompareLayerUI__(); }catch(_){ }
    PAGES.forEach(forceOne);
    try{ window.__redrawAllStatCompareCharts__ && window.__redrawAllStatCompareCharts__(); }catch(_){ }
  }
  function schedule(){
    try{ clearTimeout(timer); }catch(_){ }
    try{ requestAnimationFrame(redraw); }catch(_){ }
    timer = setTimeout(redraw, 80);
    try{ setTimeout(redraw, 260); }catch(_){ }
  }
  try{ window.__ddaeStatsFixedGraphLayerRefresh__ = schedule; }catch(_){ }
  try{ window.addEventListener('resize', schedule, { passive:true }); }catch(_){ }
  try{ window.addEventListener('orientationchange', schedule, { passive:true }); }catch(_){ }
  try{ document.addEventListener('visibilitychange', function(){ if (!document.hidden) schedule(); }, { passive:true }); }catch(_){ }
  try{ document.addEventListener('click', function(e){ if (e && e.target && e.target.closest && e.target.closest('[data-page], .home-main, .topbar, button')) setTimeout(schedule, 80); }, true); }catch(_){ }
  try{
    new MutationObserver(function(){ schedule(); }).observe(document.body, { attributes:true, attributeFilter:['data-page'], childList:true, subtree:false });
  }catch(_){ }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', schedule, { once:true });
  else schedule();
  try{ setTimeout(schedule, 500); }catch(_){ }
})();


/* dDAE_2.913 — Statistiche: refresh finale del layer fisso viewport e misura altezza */
(function(){
  const PAGES = ['statgen','statmensili','statspese','statprenotazioni','statchannel','statpulizie','statcancellazioni','statamministratore'];
  let timer = 0;
  function pageEl(page){ try{ return document.getElementById('page-' + page); }catch(_){ return null; } }
  function children(el){ try{ return Array.from(el.children || []); }catch(_){ return []; } }
  function ensureHead(container, titleText){
    let head = container.querySelector(':scope > .stats-head-row');
    let fixed = container.querySelector(':scope > .stats-fixed-layer');
    if (!head && fixed) head = fixed.querySelector(':scope > .stats-head-row');
    if (!head){
      const title = container.querySelector(':scope > .stats-title') || (fixed ? fixed.querySelector(':scope > .stats-title') : null);
      if (title){
        head = document.createElement('div');
        head.className = 'stats-head-row stats-compare-head-row';
        title.parentNode.insertBefore(head, title);
        head.appendChild(title);
      }
    }
    if (!head){
      head = document.createElement('div');
      head.className = 'stats-head-row stats-compare-head-row';
      const title = document.createElement('div');
      title.className = 'stats-title';
      title.textContent = titleText || '';
      head.appendChild(title);
      container.insertBefore(head, container.firstChild || null);
    }
    head.classList.add('stats-compare-head-row');
    return head;
  }
  const TITLES = { statgen:'Generali', statmensili:'Mensili', statspese:'Spese generali', statprenotazioni:'Con / Senza ricevuta', statchannel:'PMS', statpulizie:'Ore pulizia operatori', statcancellazioni:'Cancellazioni', statamministratore:'Ricavi' };
  function unwrap(container){
    const fixed = container.querySelector(':scope > .stats-fixed-layer');
    const scroll = container.querySelector(':scope > .stats-scroll-layer');
    if (fixed){ children(fixed).forEach(n=>container.insertBefore(n, fixed)); try{ fixed.remove(); }catch(_){ } }
    if (scroll){ children(scroll).forEach(n=>container.appendChild(n)); try{ scroll.remove(); }catch(_){ } }
  }
  function layerOne(page){
    const root = pageEl(page);
    const container = root ? root.querySelector('.stats-gen') : null;
    if (!container) return;
    const anyChart = container.querySelector('.statgen-line-chart-wrap');
    if (!anyChart) return;
    unwrap(container);
    const head = ensureHead(container, TITLES[page]);
    const chart = container.querySelector(':scope > .statgen-line-chart-wrap');
    if (!chart) return;
    const nodes = children(container);
    const chartIndex = nodes.indexOf(chart);
    const fixed = document.createElement('div');
    fixed.className = 'stats-fixed-layer';
    const scroll = document.createElement('div');
    scroll.className = 'stats-scroll-layer';
    container.insertBefore(fixed, container.firstChild || null);
    nodes.slice(0, chartIndex + 1).forEach((node)=>fixed.appendChild(node));
    children(container).filter((node)=>node !== fixed && node !== scroll).forEach((node)=>scroll.appendChild(node));
    container.appendChild(scroll);
    container.classList.add('stats-layered');
    container.dataset.statsFixedLayerReady = '1';
    measure(container);
  }
  function measure(container){
    const fixed = container ? container.querySelector(':scope > .stats-fixed-layer') : null;
    if (!fixed) return;
    let h = 0;
    try{ h = Math.ceil(fixed.getBoundingClientRect().height || 0); }catch(_){ h = 0; }
    if (!h){ try{ h = Math.ceil(fixed.scrollHeight || fixed.offsetHeight || 0); }catch(_){ h = 0; } }
    if (!h) h = (window.matchMedia && window.matchMedia('(orientation: landscape)').matches) ? Math.max(120, Math.round((window.innerHeight || 0) - 40)) : 188;
    try{ container.style.setProperty('--stats-fixed-layer-height', h + 'px'); }catch(_){ }
  }
  function refresh(){
    try{ if (typeof window.__ensureStatsCompareLayerUI__ === 'function') window.__ensureStatsCompareLayerUI__(); }catch(_){ }
    PAGES.forEach(layerOne);
    try{ if (typeof window.__redrawAllStatCompareCharts__ === 'function') window.__redrawAllStatCompareCharts__(); }catch(_){ }
    try{ requestAnimationFrame(function(){ PAGES.forEach(function(p){ const c = pageEl(p)?.querySelector?.('.stats-gen.stats-layered'); measure(c); }); }); }catch(_){ }
  }
  function schedule(){
    try{ clearTimeout(timer); }catch(_){ }
    try{ requestAnimationFrame(refresh); }catch(_){ }
    timer = setTimeout(refresh, 120);
    try{ setTimeout(refresh, 360); }catch(_){ }
  }
  try{ window.__ddaeStatsFixedGraphLayerRefresh__ = schedule; }catch(_){ }
  try{ window.__scheduleStatsFullDisplayRedraw__ = schedule; }catch(_){ }
  try{ window.addEventListener('resize', schedule, { passive:true }); }catch(_){ }
  try{ window.addEventListener('orientationchange', schedule, { passive:true }); }catch(_){ }
  try{ document.addEventListener('visibilitychange', function(){ if (!document.hidden) schedule(); }, { passive:true }); }catch(_){ }
  try{ document.addEventListener('click', function(e){ if (e && e.target && e.target.closest && e.target.closest('button, [data-page], .home-main')) setTimeout(schedule, 60); }, true); }catch(_){ }
  try{ new MutationObserver(function(){ schedule(); }).observe(document.body, { attributes:true, attributeFilter:['data-page'], childList:false, subtree:false }); }catch(_){ }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', schedule, { once:true }); else schedule();
  try{ setTimeout(schedule, 650); }catch(_){ }
})();
